[task]
id = "devolaflow-compress-012"
name = "compression-retention-medium"
description = "summarise_predecessor honours schema_hint='design' priority order: Decision > Consequences > Alternatives in covered_sections on 10K-token design artifact (10 probe facts >=95% retention) per ADR-003 §6 #8"
dimension = "analysis"
difficulty = 3

[task.input]
type = "code"
language = "python"
source = "summarise_predecessor(path, max_tokens=500, schema_hint='design')['covered_sections']"

[task.expected]
type = "text"
value = "summarise_predecessor(path, max_tokens=500, schema_hint='design')['covered_sections']"

[task.golden]
expected_score = 1.0
scorer = "exact"
