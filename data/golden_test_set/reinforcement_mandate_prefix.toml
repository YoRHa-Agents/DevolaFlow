[task]
id = "devolaflow-rein-002"
name = "reinforcement-mandate-prefix"
description = "Reinforcement mandates injected into round N+1 dispatch must begin with 'MUST fix:'"
dimension = "code_quality"
difficulty = 1

[task.input]
type = "code"
language = "python"
source = "MUST fix: coverage below 80% in src/devolaflow/gate/scorer.py"

[task.expected]
type = "text"
value = "MUST fix: coverage below 80% in src/devolaflow/gate/scorer.py"

[task.golden]
expected_score = 1.0
scorer = "exact"
