[task]
id = "devolaflow-compress-013"
name = "compression-retention-hard"
description = "summarise_predecessor max_tokens=500 emits SUMMARY_TRUNCATION_MARKER and sets was_bounded=True when 15K-token ADR predecessor cannot fit (15 probe facts spanning all 8 NER types, retention >=90%) per ADR-003 §6 #9 (stretch goal)"
dimension = "analysis"
difficulty = 4

[task.input]
type = "code"
language = "python"
source = "summarise_predecessor(path, max_tokens=500, schema_hint='adr')['was_bounded']"

[task.expected]
type = "text"
value = "summarise_predecessor(path, max_tokens=500, schema_hint='adr')['was_bounded']"

[task.golden]
expected_score = 1.0
scorer = "exact"
