[task]
id = "devolaflow-bench-009"
name = "benchmark-composite-formula"
description = "EvoBench composite score formula: composite = 0.3*test + 0.3*review + 0.2*arch + 0.2*bench"
dimension = "analysis"
difficulty = 3

[task.input]
type = "code"
language = "python"
source = "composite = 0.3 * test + 0.3 * review + 0.2 * arch + 0.2 * bench"

[task.expected]
type = "text"
value = "composite = 0.3 * test + 0.3 * review + 0.2 * arch + 0.2 * bench"

[task.golden]
expected_score = 1.0
scorer = "fuzzy"
