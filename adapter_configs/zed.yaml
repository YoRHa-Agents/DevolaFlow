# Zed adapter — Zed Industries IDE rules
name: zed
display_name: Zed
version_added: "6.1.3"
tier: tier_1
output:
  base_dir: ".rules"
  files:
    - source: "SKILL.md"
      target: "devola-flow.md"
      transform: strip_frontmatter
    - source: "references"
      target: "references"
      transform: copy_tree
budget:
  type: lines
  max: 600
  target_file: "devola-flow.md"
