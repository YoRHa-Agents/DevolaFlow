# Bootstrap ignore list (hand-written; Ch7 transition exception).
# Replaced by `devola ops render` output from harness/ops/manifest.toml [paths] once the CLI exists.

# Private goal workspace: never enters the repo except through explicit transformation into docs/
.local/

# Rust build output
target/
