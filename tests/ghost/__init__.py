"""Ghost-audit package — S-4 / W-18 enforcement surface (v15-ADR-001).

Domain lints: ``test_rules`` / ``test_schema`` / ``test_registries``.
Per-cycle W-18 feature stanzas: ``test_features_v<MAJ>_<MIN>`` (one
file per MINOR cycle; the append target rotates each cycle).
"""
