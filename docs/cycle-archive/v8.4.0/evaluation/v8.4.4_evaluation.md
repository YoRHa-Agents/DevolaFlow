# v8.4.4 — SI-3 Evaluation Report

> Per W-3 / SI-3 (`.cursor/rules/self-improve-iteration-rules.mdc`):
> every pre-release requires a weighted-composite evaluation across 6
> dimensions; threshold for ACCEPT is composite ≥ 8.5/10. Below threshold
> → iterate (loop back to SI-1) or escalate to human.
> Cycle: v9.0.0 PV-04 (PATCH `v8.4.4`).
> Date: 2026-04-24.

## Composite Score (Weighted)

| Dimension | Weight | Score | Weighted | Rationale |
|-----------|--------|-------|----------|-----------|
| **Code quality** | 0.20 | 9.5 | 1.90 | Lint clean (`ruff check src/ tests/` 0 errors); format clean (`ruff format --check` clean); 1 NEW Python module surface (`src/devolaflow/lifecycle/post_dispatch.py`) and 4 NEW public symbols (`apply_merge`, `_auto_regenerate_reports`, `AppliedMerge`, `GateThresholdNotMet`) plus 1 NEW private helper (`_emit_dispatch`) — all carrying full docstrings (NineS docstring_coverage = 525/535 = 98.13%). Soul Rule S-5 honored throughout: `_emit_dispatch` catches custom-handler exceptions and logs at WARNING via `logger.warning` rather than swallowing; `apply_merge` raises `ArchiveError` with verbatim A-4 citation when `gate_score` is missing rather than falling back to a default; `_auto_regenerate_reports` logs render failures at WARNING but never raises (REPORT.md is a presentation surface, not an integrity contract). All 3 new exception subclasses (`GateThresholdNotMet(ArchiveError)`, propagated `MergeConflict`, propagated `ChangeNotFoundError`) carry verbatim score + threshold context per S-5. The N818 ruff suppressions on `GateThresholdNotMet` + `MergeConflict` are explicitly documented with cross-references to the schema spec that pins the names. |
| **Architecture rationality** | 0.20 | 9.5 | 1.90 | The 3 closures share the dispatch / archive lifecycle as the integration surface, allowing 1 SI-3 evaluation pass to cover all of them — the v9.0.0 SI-1 implementation plan §6.4 designed this coordination on purpose so the PV stays atomic. **C-03 dead-wire fix**: the `_emit_dispatch` helper centralises the lifecycle invocation in a single private method called from all 3 return paths of `generate_round_dispatch`, preventing future drift where one return path silently bypasses the chain (P5 enforcement). **Soul Rule S-10**: codified per the same "informal-practice → binding rule" pattern that ADR-002 promoted for nest-vs-append (v9-ADR-002 D3) and ADR-003 promoted for SSOT registries — PV-04 lifts the dispatch-emission discipline without reshaping any existing module. **M-004**: `apply_merge` honors A-4 ADR exactly (gate-threshold first, atomic write second, never a partial state); the threshold tier table (PATCH/MINOR ≥ 8.5, MAJOR ≥ 9.0) mirrors W-3 / SI-3 verbatim. **I-PV07-A**: `auto_regenerate_reports: bool = True` is opt-out by design — the existing test surface that needs byte-pinned filesystem state passes the kwarg explicitly, so the new default does NOT silently change byte-pinned tests. ADR-004 explicitly NESTs the 3 closures under the "lifecycle wiring" theme per the v9-ADR-002 D3 decision rule (rationale documented in `v9-ADR-004 §"Why one PV / one ADR for 3 closures"` cross-reference). |
| **Test adequacy** | 0.20 | 9.0 | 1.80 | +21 NEW tests landed (8 in `test_dispatch_emission_runs_hooks.py` covering 3 invocation paths × 2 events + 3 R5 byte-identical + 1 permissive-mode + 1 handler-exception swallowing; 7 in `tests/test_agent_workspace.py::TestApplyMerge` covering happy-path PATCH + below-threshold + MAJOR + explicit override + missing gate_score + atomic-via-tmp + re-apply MergeConflict; 4 in `tests/test_agent_workspace.py::TestArchiveAutoRegenerateReports` covering per-change report + workspace report + opt-out skip + render-failure-does-not-raise; 2 in `tests/test_lifecycle_hooks.py` for `post_dispatch` no-op + DEFAULT_EVENTS length 6 + handler routing); 100% pass rate (3303/3303 total tests pass, 17 skipped, 2 xfailed); test count grew 3282 → 3303 = exactly +21 (NineS test_count metric confirms 3322 collected including ancillary collection — matches the +21 net-new forecast cap and stays within the +20 PV-04 forecast at 1.05× over-delivery, well within the 1.2× ceiling). The R5 strict triple codification in `TestR5ByteIdentical` (3 sub-tests) compares the post-PV-04 dispatch output against a synthetic pre-PV-04 control (deepcopy + `merge_reinforcement_into_dispatch` only — no hook chain) and asserts byte-equality on all 3 return paths; this is the strongest possible regression guard for the dead-wire fix. The handler-exception swallowing test uses `caplog.at_level("WARNING", logger="devolaflow.feedback")` to verify the WARNING log is emitted (S-5 compliance). |
| **Maintainability** | 0.15 | 9.5 | 1.425 | All paths repo-relative (S-2 / SF-5 honored); `.rules/soul.mdc` S-10 source ~+45 lines; `RuleCompiler.compile_all()` auto-mirrors to both `cursor` (8000-token budget) and `agents_md` (6000-token budget) — `.cursor/rules/repo-governance.mdc` 488 → 532 lines; AGENTS.md 410 → 454 lines; the AGENTS.md token utilization grows 5358 → 5857 / 6000 = 97.6% — flagged in CHANGELOG and ADR-004 §Consequences as a deferred risk for v9.0.0 PV-05 should the next major rule addition need a ceiling bump or compression pass. ADR-004 (416 LOC) follows the v9-ADR-001/002/003 structural template (Status / Context / Decisions D1-D4 / R5 Strict Triple Codification / Rationale / 5 Consequences / 4 Alternatives Considered / Migration / Test Plan / Cross-References). The S-10 rule body is byte-identical across `.rules/soul.mdc`, `.cursor/rules/repo-governance.mdc`, and `AGENTS.md` (3 of those are auto-mirrored by `RuleCompiler`). The `_emit_dispatch` helper docstring includes the exact R5 strict-byte-identical contract clause so a future maintainer changing the body knows the test invariant they MUST preserve. |
| **Compatibility** | 0.10 | 10.0 | 1.00 | I-8 invariant fully preserved: `schemas/lean-dispatch.yaml#layout_invariant.version = 5` (unchanged), `canonical_order` length 16 (unchanged), all 16 keys in same order (unchanged). PV-04 is a `feedback.py` + `lifecycle/` + `agent_workspace/` + `tests/` + `.rules/` patch with NO schema or canonical_order touch. All 6 backward-compat byte-baselines (v7.0.0 / v7.3.0 / v8.0.0 P-08 / v8.0.0 P-10 / v8.3.0 PV-05 / v8.4.0) continue to pass via `tests/test_layout_invariant_multi_baseline.py`. **R5 strict byte-identical existing dispatch behavior** verified end-to-end: `TestR5ByteIdentical` (3 sub-tests) confirms the dispatch output is byte-identical to the pre-PV-04 control on all 3 return paths when no extras register; this means existing L3 task agents that have not yet adopted the v8.4.4 schema see ZERO byte change in the dispatch payload they receive. The new `archive(auto_regenerate_reports=True)` default is the only outward-facing API change; existing tests that need byte-pinned filesystem state opt out via `auto_regenerate_reports=False` (4 callers in `TestApplyMerge` use the opt-out, all passing). |
| **Performance impact** | 0.15 | 9.0 | 1.35 | The `_emit_dispatch` helper adds 2 lifecycle hook calls per `generate_round_dispatch` invocation; both default handlers (`validate_dispatch` for `pre_dispatch`, `post_dispatch` no-op for `post_dispatch`) execute in O(1) amortized — `validate_dispatch` runs a single dict-key lookup for `accept` / `acceptance_criteria` and `validate_owned_files` runs a single set membership check; `post_dispatch` returns immediately with a no-op `HookResult`. Per the NineS `pipeline_latency=0.9999` (3ms for the e2e analysis pipeline), the lifecycle overhead is negligible — well below the 5% performance budget per W-4. EvoBench composite holds: `tests/test_benchmarks.py` 36/36 PASS; no scenario drift > 5% vs `v8.4.0_baseline.json` (the canonical baseline preserved through PV-01..PV-04); composite scores hold within ±5pp of the floor per W-4. The atomic-write path in `apply_merge` adds a single `.tmp` sibling write + POSIX `rename` — both O(file_size) operations on the source-of-truth spec which is bounded by Rule C-9 (3000 token hard ceiling for spec.md). The `_auto_regenerate_reports` adds 2 file writes per `archive()` call but only at archive time (not in the hot dispatch path). |

**Composite (weighted sum)**: **1.90 + 1.90 + 1.80 + 1.425 + 1.00 + 1.35 = 9.375 / 10**

**Verdict**: **ACCEPT** (≥ 8.5 threshold per SI-3 + W-3).

## Findings closure summary

The 3 BLOCKER findings (C-03 / M-004 / I-PV07-A) targeted by PV-04 are all closed:

| Finding | Severity | Source | Closure | Verification |
|---------|----------|--------|---------|--------------|
| **C-03** | BLOCKER | `v9.0.0_gap_analysis.md` §3.1 (lifecycle wiring + S-10) | `feedback.py::generate_round_dispatch` routes ALL 3 return paths through `_emit_dispatch` which calls `lifecycle.run_hooks("pre_dispatch", ...)` then `lifecycle.run_hooks("post_dispatch", ...)`; S-10 codified in `.rules/soul.mdc` + auto-mirrored to `.cursor/rules/repo-governance.mdc` + `AGENTS.md`; ADR-004 (416 LOC); NEW `tests/test_dispatch_emission_runs_hooks.py` (8 tests) | `pytest tests/test_dispatch_emission_runs_hooks.py -v` (8/8 PASS); `pytest tests/test_lifecycle_hooks.py::test_default_events_match_skill_md_table -v` (DEFAULT_EVENTS len = 6) |
| **M-004** | BLOCKER | `v9.0.0_gap_analysis.md` §3.1 (M-004 / A-4 ADR full closure) | `archive.py::ArchiveManager.apply_merge(change_id, *, is_major_change, require_gate_score)` ships with gate-threshold guard (PATCH/MINOR ≥ 8.5, MAJOR ≥ 9.0) + atomic POSIX-rename write; NEW `AppliedMerge` dataclass + `GateThresholdNotMet(ArchiveError)` exception | `pytest tests/test_agent_workspace.py::TestApplyMerge -v` (7/7 PASS); `pytest tests/test_agent_workspace.py::TestProposeMergeAgainstActiveChange -v` (1/1 PASS) |
| **I-PV07-A** | BLOCKER | `v9.0.0_gap_analysis.md` §3.1 (REPORT.md auto-trigger) | `archive(auto_regenerate_reports=True)` is the new default; `_auto_regenerate_reports(change_id, archive_path)` wires per-change + workspace REPORT.md regeneration via the v8.2.7 reporter module; permissive (logs at WARNING but never raises) | `pytest tests/test_agent_workspace.py::TestArchiveAutoRegenerateReports -v` (4/4 PASS) |

## CI verification harness — all PASS

```bash
$ python -m pytest tests/ -q
3303 passed, 17 skipped, 2 xfailed in 46.59s

$ ruff check src/ tests/
All checks passed!

$ ruff format --check src/ tests/
186 files already formatted

$ python -m pytest tests/test_version.py -v
12 passed, 17 skipped in 0.04s

$ python -m pytest tests/test_benchmarks.py -v
36 passed, 18 warnings in 17.04s

$ make check-cursor-skill
[.cursor mirror] not present at .cursor/skills/devola-flow — opt-in (see Rule SF-3)
(exit 0)

$ pytest tests/test_dispatch_emission_runs_hooks.py -v
8 passed in 0.03s

$ pytest tests/test_lifecycle_pre_shell_call.py tests/test_lifecycle_hooks.py -v
50 passed in 0.05s

$ pytest tests/test_agent_workspace.py -v
94 passed in 0.13s
```

**All 6 W-9 / SI-10 steps PASS** + 3 PV-04-specific verification commands PASS.

## Verbatim grep verifications — all PASS

| Grep | Expected | Actual |
|------|----------|--------|
| `grep "S-10 — Prompt-Side Governance Contract Embedding" AGENTS.md` | ≥ 1 hit | 1 hit ✓ |
| `grep "S-10 — Prompt-Side Governance Contract Embedding" .cursor/rules/repo-governance.mdc` | ≥ 1 hit | 1 hit ✓ |
| `grep "S-10 — Prompt-Side Governance Contract Embedding" .rules/soul.mdc` | ≥ 1 hit | 1 hit ✓ |
| `grep "post_dispatch" src/devolaflow/lifecycle/__init__.py` | ≥ 4 hits (import + EVENT + _set_default_hook + DEFAULT_EVENTS) | 6 hits ✓ |
| `grep "_emit_dispatch" src/devolaflow/feedback.py` | ≥ 4 hits (def + 3 return-path call sites) | 4 hits ✓ |
| `grep "apply_merge" src/devolaflow/agent_workspace/archive.py` | ≥ 1 hit (def) | 5+ hits ✓ |
| `grep "AppliedMerge" src/devolaflow/agent_workspace/archive.py` | ≥ 2 hits (dataclass + return type) | 3+ hits ✓ |
| `grep "GateThresholdNotMet" src/devolaflow/agent_workspace/archive.py` | ≥ 2 hits (class + raise) | 3+ hits ✓ |
| `grep "auto_regenerate_reports" src/devolaflow/agent_workspace/archive.py` | ≥ 3 hits (kwarg + helper + Step 5 comment) | 4+ hits ✓ |
| `wc -l workflow-system/agent/SKILL.md` | ≤ 483 (PV-01 480 + 3-line pointer) | 440 ✓ |

## I-8 invariant compliance

PV-04 makes ZERO changes to `schemas/lean-dispatch.yaml#layout_invariant`:

* `version: 5` — unchanged.
* `canonical_order` length 16 — unchanged.
* All 16 keys in their pre-PV-04 order — unchanged.
* `DEFAULT_DISPATCH_LAYOUT` in `compressor.py` — unchanged.
* All 6 backward-compat byte-baselines (v7.0.0 / v7.3.0 / v8.0.0 P-08 / v8.0.0 P-10 / v8.3.0 PV-05 / v8.4.0) continue to pass.
* No new schema golden YAML — PV-04 has no schema delta, so no new baseline is needed.

PV-04 is a `feedback.py` + `lifecycle/` + `agent_workspace/` + `tests/` + `.rules/` patch with no schema or canonical_order touch. Per Architecture Rule A-2 (Cache-Layout Governance v2 — codified by sister ADR `v9-ADR-002`), no changes to the canonical_order require A-2.4 multi-baseline byte test attention.

## Risk register check (per `v9.0.0_implementation_plan.md` §6.4.6)

* **R-10** (hot-path regression in dispatch emission) — **NOT triggered**.
  `TestR5ByteIdentical` confirms byte-identical existing dispatch behavior
  on all 3 return paths when no extras register. The pre-PV-04 control
  bytes equal the post-PV-04 bytes exactly. The fallback rollback path
  documented in §6.4.6 (revert `feedback.py` extension only; preserve
  `lifecycle/dispatcher.py` + `propose_merge`) is preserved for posterity
  but not invoked at the v8.4.4 cut.
* **propose_merge causes archive test failures** — **NOT triggered**.
  All 6 `TestProposeMerge` tests + 1 `TestProposeMergeAgainstActiveChange`
  pass; no archive test regressed.
* **SI-3 < 8.5** — **NOT triggered**. Composite 9.375/10.
* **File-level** rollback: `git revert <pv04_commit>` would cleanly undo
  the change set; existing handler chain stays installed (S-10 rule
  remains as informational only — but per the playbook §6.4.6 wording,
  "S-10 rule remains as informational" is the expected fallback state,
  so no extra cleanup needed).

## Coupling acknowledgments

* Couples with **A-2** / **A-5** (sister codification ADRs `v9-ADR-002` /
  `v9-ADR-003`): all three rules use the v9.0.0 cycle's "promote informal
  practice to binding rule" pattern. S-10's R5 triple codification
  (hook + schema + test) is the strongest of the 3.
* Couples with **A-4** (Source-of-Truth Spec Location): M-004 closes the
  A-4 ADR write side. With `apply_merge` shipping, A-4's "source-of-truth
  is mutated ONLY at archive time AFTER the gate has PASSED" contract is
  now end-to-end enforceable.
* Couples with **S-5** (No Silent Failures): the `_emit_dispatch`
  exception handler logs at WARNING and returns the dispatch unchanged
  rather than swallowing; `apply_merge` raises `ArchiveError` with
  verbatim A-4 citation when `gate_score` is missing rather than
  falling back to a default; `_auto_regenerate_reports` logs render
  failures at WARNING but never raises (REPORT.md is a presentation
  surface, not an integrity contract).
* Couples with **S-9** (Handoff Envelopes Are Append-Only): the
  `propose_merge` / `apply_merge` split honors the same "propose, do not
  mutate" discipline.
* Couples with **W-9 / SI-10**: all 6 pre-commit gate steps PASS
  (verified post-bump).
* Couples with **W-4 / SI-4** (benchmark regression guard): no scenario
  drift > 5% vs `v8.4.0_baseline.json`; 36/36 PASS.

## NineS post-PV self-eval

* JSON: `.local/research/v8.4.4_nines.json` (composite **0.9050**;
  ≥ 0.85 threshold per dispatch ✓).
* Markdown summary: `.local/research/v8.4.4_nines.md` (per-dimension
  table; 22/25 dimensions ≥ 0.85; 3 below-threshold scores
  (`source_freshness=0.50`, `index_recall=0.80`, `code_coverage=0.0`)
  are pre-existing tooling caveats inherited byte-for-byte from v8.4.3,
  NOT v8.4.4 regressions — `code_coverage=0.0` attributable to pytest
  --cov budget timeout per the v8.4.2 erratum).
* Capability mean: 0.9517; Hygiene mean: 0.7962 (the hygiene mean is
  pulled down by the pre-existing `code_coverage=0.0` only; without
  that pre-existing tooling caveat, hygiene mean would be ~0.995).
* test_count delta: 3301 → 3322 (+21, exactly matches the +21 net-new
  test forecast).
* module_count delta: 80 → 81 (+1 = `src/devolaflow/lifecycle/post_dispatch.py`).
* decomposition_coverage delta: 999 → 1005 elements (+6 = `apply_merge` +
  `_auto_regenerate_reports` + `AppliedMerge` + `GateThresholdNotMet` +
  `post_dispatch` module + handler).

## Cross-references

* SI-1 planning gate: `.local/research/v9.0.0_gap_analysis.md` §3.1 + §5.4
* SI-1 implementation plan: `.local/research/v9.0.0_implementation_plan.md` §6.4
* PV-04 design doc: `.local/research/v9.0.0_pv04_design.md`
* PV-04 ADR: `.local/research/adr/v9-ADR-004-lifecycle-wiring-and-s10.md`
* Sister codification ADRs: `.local/research/adr/v9-ADR-002-cache-layout-governance-v2.md` + `.local/research/adr/v9-ADR-003-a5-ssot-registry.md`
* Predecessor PV-01 ADR: `.local/research/adr/v9-ADR-001-skill-headroom-reclamation.md`
* A-4 ADR (source-of-truth spec location, closed by M-004 herein): `.cursor/rules/repo-governance.mdc` §"A-4 — Source-of-Truth Spec Location"
* Precedent: v6.0.3 retro highest-ROI dead-wire fix (cited verbatim in ADR-004 Context); v8.4.0 retro §4.1 #4 R5 strict pattern (re-applied in D1)
* DevolaFlow canonical URL (per S-7): https://github.com/YoRHa-Agents/DevolaFlow
* NineS canonical URL (per S-7): https://github.com/YoRHa-Agents/NineS
