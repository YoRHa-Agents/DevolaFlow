# v13.0.0 — SI-3 Evaluation Report (W-3 / Pre-Release Gate)

**Cycle:** v13.0.0 (MAJOR) — Impeccable Integration + Web-Design Workflow + Bundled Plugin Install
**Date:** 2026-06-01
**Threshold:** weighted composite **≥ 9.0** (MAJOR cycle).
**NineS self-eval:** overall **0.9070** (`.local/research/v13.0.0_nines.json`; baseline 12.5.0 = 0.907 — preserved, no regression).

## Dimension scores

| Dimension | Weight | Score | Rationale |
|---|---|---|---|
| Code quality | 0.20 | 9.5 | `install_plugins(scope)` delegates to `ensure_plugin` (A-5 SSOT, no command duplication); per-plugin failures warn-not-fatal with explicit logging + counts (S-5, never silently swallowed; verified by `test_install_plugins_warn_not_fatal`). `ruff check` + `ruff format --check` clean across `src/` + `tests/`. Registry entries follow the established ui-pro/codegraph shape. |
| Architecture rationality | 0.20 | 9.5 | impeccable mirrors the proven plugin shape across 3 SSOT registries; NEW `ui_refinement` role parallels `code_intelligence` (singular-provider discipline preserved). web-design uses the existing convergence-loop + gate primitives (validates clean, 0 warnings). **Zero schema bumps** — `ensure_plugins` is template config, not a lean-dispatch key; canonical_order stays 17; A-2.4 multi-baseline unchanged. `--no-plugins` is a CLI arg, not a new env flag (W-20 reuse-first preserved at 7). The `npm_then_init` `init_targets:[auto]` decision correctly models impeccable's harness auto-detection. |
| Test adequacy | 0.20 | 9.0 | ~29 net-new test functions (within W-17 +30 cap): impeccable registration (5) + runtime smoke + ui-pro/web-design resolution (3) + degraded-mode PPI001 (1) + web-design template (8) + reference doc (5) + install-plugins gating (5) + 3 W-18 ghost-audit stanzas. 4581 passing. The 13 remaining failures (test_memory_router + test_shell_proxy_commands) are **pre-existing on clean `origin/main`** (verified via a clean worktree) — version/environment-gated, unrelated to this cycle. |
| Maintainability | 0.15 | 9.5 | NEW Tier-2 `references/impeccable.md` (6 anchor sections); degraded-mode §6; CHANGELOG MAJOR entry naming `--no-plugins` (W-20); bilingual EN/ZH workflow-types refreshed in lockstep (ST-3); inline comments cite the upstream-verified behaviour (impeccable@2.3.2). |
| Compatibility | 0.10 | 9.5 | Zero breaking changes; append-only registry/reference additions (A-2); backward-compatible CLI (project-scope installs unchanged; `--global` default-ON is additive). All prior baselines retained on disk. |
| Performance impact | 0.15 | 9.5 | EvoBench 36/36 PASS; `collapse_l0_l3_simple` composite IMPROVED +8.87 (89.83 → 98.7) from the SKILL.md additions + `impeccable_integration` block surfacing more relevant content per query; zero scenario regressions > 5%. W-16 wholesale baseline regen → `v13.0.0_baseline.json`. |

## Weighted composite

```
9.5·0.20 + 9.5·0.20 + 9.0·0.20 + 9.5·0.15 + 9.5·0.10 + 9.5·0.15
= 1.90 + 1.90 + 1.80 + 1.425 + 0.95 + 1.425
= 9.40 / 10
```

**Verdict: PASS** — composite **9.40 ≥ 9.0** (MAJOR threshold), margin +0.40. 0 BLOCKER / 0 CRITICAL findings introduced by this cycle.

## Gate evidence (SI-10 / W-9 6-step)

1. `pytest tests/ -q` — 4581 passed, 13 pre-existing failures (memory_router + shell_proxy_commands; confirmed on clean main), 26 skipped, 2 xfailed.
2. `ruff check src/ tests/` — All checks passed.
3. `ruff format --check src/ tests/` — 316 files already formatted.
4. `pytest tests/test_version.py` — PASS (canonical locations agree at 13.0.0; mirror-parity self-skips).
5. `pytest tests/test_benchmarks.py` — 36 passed (post W-16 regen).
6. `make check-cursor-skill` — exit 0 (mirror absent / opt-in).
7. `build-skill --all` — 16/16 adapters within budget (cursor 491/500, codex 479/500, claude 67/200, copilot 1922/4000).

## Pre-existing failures (NOT introduced by this cycle)

`tests/test_memory_router.py` (6) + `tests/test_shell_proxy_commands.py` (7) fail on a clean `origin/main` checkout (verified via `git worktree add /tmp/df-main origin/main`). Root cause is environment/version-gating in those subsystems, orthogonal to the impeccable / web-design / bundled-install surface. No fix attempted (out of cycle scope; S-6 protected-branch + minimal-diff discipline).
