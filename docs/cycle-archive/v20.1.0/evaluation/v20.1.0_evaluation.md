# v20.1.0 — SI-3 Evaluation Report

> Per W-3 / SI-3, this MINOR release is evaluated across the six required
> weighted dimensions. The MINOR acceptance threshold is composite ≥ 8.5/10.
>
> Cycle: v20.1.0 close (Retro-Digest Evidence).
> Date: 2026-08-29.

## Part A — Built-in Harness Evaluation (BINDING)

### A.1 Machine verdict

The binding end-of-iteration evaluator artifact is
`.local/research/v20.1.0_harness_evaluation_final.json`. It is the final
machine evidence supplied for this close and is preserved verbatim. Its
evaluator invocation is:

```bash
PATH=.venv/bin:$PATH python -m devolaflow.harness evaluate --ledger .local/telemetry/harness.jsonl --repo . --output .local/research/v20.1.0_harness_evaluation_final.json
```

| JSON field | Verbatim value |
|---|---|
| `verdict` | `READY` |
| `composite` | `9.17` / 10 |
| evaluator `threshold` | `8.5` |
| MINOR release threshold | `8.5` |
| `auto_fill_rate` | `1.0` |
| `sampled_at` | `2026-08-28T16:13:23.037833+00:00` |

**SI-3 disposition: ACCEPT.** The machine verdict is `READY`, the reported
composite is `9.17`, and it exceeds the MINOR threshold of `8.5`.

### A.2 Six weighted dimensions

| Dimension | Weight | Score | Weighted contribution | Evidence and assessment |
|---|---:|---:|---:|---|
| Code quality | 0.20 | 10.0 | 2.0000 | `ruff_lint: true`, `ruff_format: true`, coverage value `91.0` |
| Architecture rationality | 0.20 | 9.29 | 1.8580 | `layout_invariant: true`; quantifiable ratio `0.8571428571428571` |
| Test adequacy | 0.20 | 6.67 | 1.3340 | Test suite and coverage score `10.0`; W-17 probe value `51`, score `0.0` |
| Maintainability | 0.15 | 9.86 | 1.4790 | `ruff_format: true`; docstrings score `9.71`, value `97.1264367816092` |
| Compatibility | 0.10 | 10.0 | 1.0000 | `layout_invariant: true`; compatibility suite `true` |
| Performance impact | 0.15 | 10.0 | 1.5000 | Budget compliance ratio `1.0`; p95 headroom `0.28725` |

The exact weighted calculation from the six reported scores is:

```text
10.0×0.20 + 9.29×0.20 + 6.67×0.20 + 9.86×0.15 + 10.0×0.10 + 10.0×0.15
= 9.171, reported as 9.17 / 10
```

The evaluator’s binding machine verdict remains **READY**. The
`test_adequacy` suggestion is preserved exactly:
`score 6.67 below threshold 8.50`.

### A.3 Supporting release-preflight evidence

The supporting close-gate result is:

```text
$ make release-preflight
--- Release preflight PASSED ---
exit code: 0
```

This release-preflight result supports the close evidence but does not
override the binding evaluator verdict or its recorded limitations.

### A.4 Findings closure

| Finding | Severity | Disposition | Verification |
|---|---|---|---|
| W-17 observation is `51` | Process finding | Remains explicit. The evaluator observes a single-base-ref count and cannot establish PV boundaries; this report does not invent a per-PV or per-cycle split. | Final JSON records `scores[2].metadata.subcomponents.w17.value: 51` and `score: 0.0`. |
| Historical aggregate measurements | Advisory evidence gap | Remain `INSUFFICIENT`; current probe values are retained, but no historical measurement delta is asserted. | Final JSON preserves `harness_summary.measurements.*.status: INSUFFICIENT` for `agents_md_tokens`, `suite_wall_seconds`, `cjk_violations`, and `ghost_loc`. |
| Test-adequacy suggestion | Advisory/process finding | Visible and carried forward as a next-cycle evidence action; it does not change the `READY` machine verdict. | Final JSON preserves the exact suggestion `score 6.67 below threshold 8.50`. |

## Part B — Harness telemetry and trend (ADVISORY)

The final evaluator reports 13 harness records across five changes:
`v17-r1-cleanup`, `v17-r2-boundaries`, `v17-r3-injection`,
`v17-r4-focus-loop`, and `v17-r5-model-boundaries`. Current summary values
are preserved exactly:

```json
{
  "records": 13,
  "tokens": {
    "total": 18585,
    "mean": 1429.6153846153845,
    "p50": 1394,
    "p95": 2298,
    "budget_compliance_ratio": 1.0,
    "p95_budget_utilization": 0.28725,
    "host_rule_tokens_mean": 12430.0,
    "slice_savings_pct_mean": 49.61538461538461
  },
  "constraints": {
    "count": 140,
    "quantifiable_ratio": 0.8571428571428571,
    "advisory_folded_ratio": 0.07692307692307693
  }
}
```

The current top-level evaluator probes are:

```json
{
  "agents_md_tokens": {
    "available": true,
    "value": 11021,
    "status": "AVAILABLE",
    "source": "evaluator"
  },
  "suite_wall_seconds": {
    "available": true,
    "value": 208.85621845797868,
    "status": "AVAILABLE",
    "source": "evaluator"
  },
  "cjk_violations": {
    "available": true,
    "value": 0,
    "status": "AVAILABLE",
    "source": "evaluator"
  },
  "ghost_loc": {
    "available": true,
    "value": 12672,
    "status": "AVAILABLE",
    "source": "evaluator"
  }
}
```

Historical comparison is deliberately not fabricated. The final evaluator’s
historical aggregate measurements are `INSUFFICIENT`, and the close therefore
does not claim a historical checklist, reversion/blocker, or matched-metric
trend. W-16 settlement is recorded separately in the baseline artifact.

## Part C — Current-cycle limitations and release disposition

### C.1 Current-cycle limitations

- The final evaluator’s W-17 probe is `51` with score `0.0`; it is a
  single-base-ref observation and does not provide the PV boundary evidence
  needed to calculate a compliant split.
- The historical aggregate measurement records remain `INSUFFICIENT`.
  Available current probe values must not be read as historical deltas.
- The harness ledger summary covers the recorded v17-era changes rather than
  constituting a feature-specific Retro-Digest workload series.
- The final evaluator suggests follow-up work for test adequacy because the
  W-17 subcomponent remains below its threshold; this is visible in the report
  rather than hidden by the passing release gate.

### C.2 Release disposition

- Semantic tier: **MINOR** (`20.1.0`).
- W-3 / SI-3 disposition: **ACCEPT** — exact machine verdict `READY`,
  composite `9.17`, above the `8.5` MINOR threshold.
- Supporting `make release-preflight`: **PASSED**, exit code `0`.
- W-16 settlement: `.local/telemetry/baselines/harness_baseline_v20.1.0.json`
  is settled from the final evaluator without rewriting the prior
  `harness_baseline_v20.0.0.json`.
- W-7 retrospective: `.local/research/v20.1.0_retrospective.md`.
- Retro-Digest dogfood artifact:
  `.local/research/v20.1.0_retro_digest.md`.
- Current-cycle W-18 ghost audit: `tests/ghost/test_features_v20_1.py`.

## Cross-references

- `.local/research/v20.1.0_harness_evaluation_final.json` — binding machine evidence
- `.local/telemetry/harness.jsonl` — append-only telemetry source
- `.local/telemetry/baselines/harness_baseline_v20.1.0.json` — W-16 settled baseline
- `.local/research/v20.1.0_retro_digest.md` — dogfood digest artifact
- `workflow-system/agent/references/retro-digest.md` — Retro-Digest contract
- `AGENTS.md` §W-2 — built-in evaluator and evidence boundary
- `AGENTS.md` §W-3 — SI-3 acceptance threshold
- `AGENTS.md` §W-16 — harness settlement
