# v15.0.0 — SI-3 / W-3 Pre-Release Evaluation (MAJOR cycle close)

* **Date:** 2026-06-12
* **Author:** L3 Task Agent (Review team), task `v15.0.0-T8-evaluation-close`
* **Branch:** `feat/v15.0.0-major-rollup` (release candidate; tree at `__version__ = "15.0.0"`)
* **Scope evaluated:** the full v15-cycle ladder — v14.2.0 (PR #145) → v14.2.1 (#146) →
  v14.2.2 (#147) → v14.3.0 (#148) → v14.4.0 (#149) → v14.5.0 (#150) → v15.0.0 (this branch)
* **Release bar:** **≥ 9.5 / 10 weighted composite — OPERATOR-SET for this MAJOR**, stricter
  than the W-3 default (≥ 9.0 MAJOR / ≥ 8.5 MINOR)
* **Companion artifacts:** `.local/research/v15.0.0_nines.{json,md}` (W-2),
  `.local/research/v15.0.0_s11_evaluation.md` (G-039 / W-21),
  `.local/research/v15.0.0_retrospective.md` (W-7)

---

## §1 Verdict summary

| Dimension | Weight | Score |
|---|---|---|
| §3.1 Code quality | 0.20 | **9.4** |
| §3.2 Architecture rationality (S-11 gate input) | 0.20 | **9.3** |
| §3.3 Test adequacy | 0.20 | **9.2** |
| §3.4 Maintainability | 0.15 | **9.5** |
| §3.5 Compatibility | 0.10 | **9.6** |
| §3.6 Performance impact | 0.15 | **9.2** |
| **Weighted composite** | 1.00 | **9.35** |

```
composite = 0.20·9.4 + 0.20·9.3 + 0.20·9.2 + 0.15·9.5 + 0.10·9.6 + 0.15·9.2
          = 1.880 + 1.860 + 1.840 + 1.425 + 0.960 + 1.380
          = 9.345 → 9.35 (2 dp)
```

**VERDICT: NOT-READY at the operator-set ≥ 9.5 bar (short by 0.15).** The composite CLEARS
the W-3 default MAJOR threshold (≥ 9.0) by +0.35 and is the series-best (calibration:
v11.1.0 close 9.02; v12.0.0 MAJOR 9.125 per
`docs/cycle-archive/v12.0.0/evaluation/v12.0.0_evaluation.md`; v14.0.0 ≈ 8.4). The gap to
9.5 is concrete and small — see §4 gap list. Per the honest-scoring constraint, the score is
not rounded up to meet the bar.

---

## §2 North-star check — "在单独任务上做到极致" (single-task excellence)

The v14.2.0 product review diagnosed (verbatim, its §8): *"the product is an excellent
task-INPUT machine attached to an underbuilt task-OUTPUT machine"* (input quality 8/10,
output verification 4/10). Before/after:

| Surface | v14.2.0 (cycle open) | v15.0.0 (cycle close) |
|---|---|---|
| Output-closure hooks (G-001) | advertised in SKILL.md, **zero** production call sites since v7.6.x | wired permissive v14.3.0 (`lifecycle/runtime_wiring.py`); STRICT v15.0.0 — `file_write`/`task_stop` block under `DEVOLAFLOW_AGENT_WORKSPACE=1` (S-8 "mode: full") |
| Evidence transport (G-002/G-003) | `lean-report.yaml` carries `hdr,state,artifacts,metrics,issues,decisions` only; no per-AC verdicts, no diff stats | `self_check` / `ac_results` / `diff_stats` evidence blocks (v14.3.0) |
| Artifact rubric (G-004) | none — `references/task-quality-score.md`: "Never score subagent outputs" | `references/artifact-quality.md` (25th reference, v14.3.0) + L0-side `gate/artifact_score.py::score_artifact_evidence` (v15.0.0, ADR-007 phase 2) |
| AC auto-generation (G-006) | 2 of 17 profiles | 17 of 17 profiles (v14.4.0) |
| Intra-task convergence (G-005) | stage/wave-level only | L3 self-verify protocol + `gate.intra_task_convergence` NEST + surgical-scope diff verifier (v14.4.0) |
| Dispatch validation | AC-v2 unvalidated at emission | `pre_dispatch` chain STRICT by default — missing/untestable AC, malformed AC-v2 (VD005–VD008), smuggled `quality_score`, banner literal → `HookViolation` blocks emission |
| Input integrity (G-007) | pos-17 dedup refs `"@round-N-1:pred-K"` unresolvable by a fresh round-N L3 | self-contained `digest` per entry (`DEDUP_DIGEST_MAX_CHARS = 320`); refs resolve intra-payload |
| Input token economy (G-008/G-010/G-019) | 62 rules incl. 4 always-on duplicate legacy files; SKILL.md 492/500 | 56 rules, stubs deduped, +1038 token compile headroom; SKILL.md 430/500 (`wc -l` this session) |

**The cycle measurably advances the north star on both sides**: the output side went from
"no transport, no rubric, no enforcement" to "transport + rubric + strict enforcement + L0
scoring", and the input side gained the budget the governance apparatus had been consuming.
The one honest residual: `score_artifact_evidence` is **standalone by design** — the
`evaluate_gate` wiring is an explicitly deferred later slice (CHANGELOG `## [15.0.0]`
§"L0 artifact scoring"), so output excellence is now *measured* but not yet *gated*. That
residual is priced into §3.2.

---

## §3 Per-dimension scoring (evidence verbatim per C-3)

### §3.1 Code quality — 9.4 × 0.20

Evidence (this session unless cited):

* `ruff check src/ tests/` → "All checks passed!"; `ruff format --check src/ tests/` →
  "381 files already formatted".
* Full suite: **4981 passed, 3 skipped, 2 xfailed, 43 warnings in 85.50s** (exit 0).
* docstring_coverage **722/740 = 0.9757** (NineS, `v15.0.0_nines.json`) — 40 of the cycle's
  41 new top-level defs documented.
* S-5 closures shipped: `RuleCompiler` hard-fails on budget overflow + `bump_version.py`
  hard-fails on regex miss (v14.2.1, G-011/G-032); `EvidenceDoctrineError` raises on smuggled
  scores at scoring time (v15.0.0).
* God-files retired: `gate/scorer.py` 2545 lines → 6 owner modules (`gate/cascade.py`,
  `gate/ladder.py`, `gate/acceptance_v2.py`, …, ADR-006, v14.5.0).

Deductions: 18 undocumented defs remain; low-coverage CLI/bridge modules
(`si_chip_bridge/runner.py` 66%, `selector_cli.py` 73%, `lifecycle/runtime_wiring.py` 77%);
43 pre-existing DeprecationWarnings in the suite.

### §3.2 Architecture rationality — 9.3 × 0.20 ← **S-11 gate input (W-21 condition 3)**

Strengths:

* **ADR-first execution**: 8 ADRs (`v15-ADR-001`…`008`) authored at cycle open / ratified in
  `v14.2.0_gap_analysis.md` §7, every one landed at its planned rung — zero mid-cycle
  re-litigations.
* **A-2 cache governance held through a MAJOR**: `canonical_order` stays 17, schema version
  stays 6, all 10 Tier-A `layout_invariant_v*.yaml` goldens untouched and byte-identical
  (multi-baseline test green in the 4981-test run); both v15 schema behaviours landed as
  NESTs per A-2.3 (`gate.intra_task_convergence`; dedup `entries[*].digest`).
* **DEFAULTS-PERMISSIVE-IN-MINOR → STRICT-IN-MAJOR executed exactly as telegraphed**: all 6
  G-038 flips trace to their telegraph notes (v12.2.0/v12.4.0/v14.0.0/v14.1.0 retros), each
  with a per-flip opt-out (§3.5).
* **A-5 SSOT strengthened**: plugin registry census 5 → 4 rows (G-021 dual-owner collapse);
  derived view lint-pinned (`tests/test_plugins.py::TestV15PluginRegistryUnification`).
* **Doctrine coherence end-to-end**: the ADR-007 evidence-vs-scoring split held — L3s emit
  evidence only, the hook chain and `EvidenceDoctrineError` enforce it from both sides.

Deductions (each verifiable):

* `score_artifact_evidence` not wired into `evaluate_gate` — the north-star centerpiece is
  measured-not-gated (explicit deferral, CHANGELOG `## [15.0.0]`).
* Three new indefinite compatibility surfaces: **24 permanent re-export shims** (ADR-006),
  the **16-name composition alias layer** (removal "no earlier than v16.0.0"), and the frozen
  derived `plugins.yaml` view — each carries lint upkeep and retirement promises without
  dated entry criteria.
* Compiled-corpus narrative debt: A-7.1 still carries v11.1.0-era "Soft mode … preserved"
  prose while `gate/cascade.py:105` records the v12.0.0 STRICT graduation — rule text lags
  code on a graduated invariant.

**Score: 9.3 — below the 9.5 floor W-21 condition 3 requires** (consequence in
`v15.0.0_s11_evaluation.md`).

### §3.3 Test adequacy — 9.2 × 0.20

* Coverage (this session): `pytest tests/ -q --cov=devolaflow` → **"Required test coverage
  of 80.0% reached. Total coverage: 93.13%"** (TOTAL 15466 stmts / 1063 miss). S-3 floor 80
  cleared by +13.13pp. Only 0% module is `src/devolaflow/pre_decision/__main__.py` — a
  6-line CLI shim dating to v0.1.0 (not a new-module S-3 violation).
* Suite size: 4986 collected (4981 passed + 3 skipped + 2 xfailed).
* **W-17 discipline (counting basis: `git diff <ref> -- tests/ | grep -cE '^\+\s*def test_'`)**:

| Rung | raw `+def test_` | raw `−def test_` | net | Note |
|---|---|---|---|---|
| v14.2.0 | +6 | 0 | +6 | |
| v14.2.1 | +24 | −18 | +6 | count-pin SSOT rewrites (moves/renames) |
| v14.2.2 | +2 | 0 | +2 | |
| v14.3.0 | +132 | −111 | +21 | ghost-audit split — 111 moved functions are W-17-exempt |
| v14.4.0 | +37 | −3 | **+34** | see flag below |
| v14.5.0 | +24 | −1 | +23 | ADR-006 split (mostly moved tests) |
| v15.0.0 (worktree) | +34 | −10 | +24 | CHANGELOG claims "+34 NEW … ≤ 30/PV honoured per-task" |
| **Cycle total** | **+255** | **−139** | **+116** | **≤ +150 cycle budget — PASS** |

  **Flag (honest)**: v14.4.0 nets **+34 > the +30 per-PV cap** unless ≥4 of its additions are
  move/parametrize-exempt — the PR landed 4 work blocks (NEST, metric runners, verifier,
  AC-gen) which plausibly constituted multiple PVs, but no per-task attribution was recorded.
  The cycle budget itself is comfortably met (+116/150, 77%).
* Ghost-audit architecture: the 12,405-line `test_no_ghost_features.py` monolith → the
  `tests/ghost/` package (v14.3.0, ADR-001); v15.0.0 ghost coverage refreshed BEFORE the
  CHANGELOG entry per W-18 (`tests/ghost/test_features_v15_0.py`, 8 stanzas).
* This task adds **0 new test functions** (constraint honoured).

Deductions: the v14.4.0 per-PV accounting ambiguity; sub-80% modules listed in §3.1.

### §3.4 Maintainability — 9.5 × 0.15

* Rule corpus 62 → **56** (`.rules/index.md:5` verbatim: "Total rules: **56** (cap 60 HARD
  … 4 slots headroom"); compile headroom **+1038 tokens** (ADR-004); per-rule history
  externalized to `v15-ADR-008`.
* SKILL.md **430 lines** (`wc -l`, this session) vs 492 at cycle open — 70 lines of C-4
  headroom restored.
* Baselines in CI 62 → **24 files** (`ls benchmarks/devolaflow_context/baselines/`, this
  session; 41 archived per ADR-005 Tier-B/C; all 10 Tier-A witnesses retained).
* Duplicate prompt mass deleted: 4 legacy rule files → pointer stubs (v14.2.1, G-008).
* Drift now mechanically guarded: D-5 CHANGELOG lint live (`scripts/lint_changelog.py`),
  count-pin SSOT (G-028), env-flag inventory machine-checkable (G-024), version fan-out
  11 → 9 patterns (G-031).

Deductions: 24 shims + 16 aliases + frozen view to track (§3.2); `CHANGELOG.md` at 8341
lines keeps growing with no archival mechanism.

### §3.5 Compatibility — 9.6 × 0.10

All 6 BREAKING flips ship documented opt-outs (CHANGELOG `## [15.0.0]` §BREAKING, verified
against code):

| Flip | Opt-out |
|---|---|
| `pre_dispatch` STRICT | `ProposalGenerator(pre_dispatch_strict=False)` |
| banner hook default-wired | `unregister_pre_dispatch_extra()` / `lifecycle.dispatcher.unregister_hook` |
| wave timeouts default-ON | `timeout_seconds: null` (pre-v15 unbounded behaviour) |
| `DEFAULT_EVENTS` 16 → 17 | additive APPEND at pos 17 per A-2.2; re-pin only |
| S-8 "mode: full" | `strict=False` on `fire_file_write`/`fire_task_stop`; flag absent → byte-identical no-op |
| `legibility_weight` 0.05 | `dataclasses.replace(STANDARD, legibility_weight=0.0)` |

* Template alias layer verified this session: `make validate-templates` → "23 passed,
  0 failed, 7 templates + 16 compositions"; every legacy name resolves with
  `DeprecationWarning`; hard removal ≥ v16.0.0.
* 24 permanent shims preserve the S-10 path contracts
  (`feedback.py::ProposalGenerator.generate_round_dispatch`, `populate_cascade_gate_fields`).
* Tier-A goldens byte-identical (digest-less ledger entries stay valid — absence-canonical).

Deductions: tooling reading the 16 deleted template yaml FILES breaks (registry-API
migration required); `ui-ux-pro-max` → `ui-pro` is a hard rename in the derived view;
`len(DEFAULT_EVENTS) == 16` pins break (documented).

### §3.6 Performance impact — 9.2 × 0.15

* **Benchmark re-equilibration accounting (W-16)**: the content rewrites moved 7 scenarios
  beyond the W-4 5pp envelope vs `v14.5.0_baseline.json` — verbatim: `spike_poc` **−15.23pp**,
  `migration_upgrade` **−8.43**, `compression_retention_hard` **−8.21**,
  `self_update_integration` / `self_update_reference_check` **−7.99** each,
  `feedback_regression` **−7.86**, `convergence_noise_filter` **−7.07** — all attributed to
  sibling content re-equilibration and all above per-scenario floors; wholesale regen produced
  `v15.0.0_baseline.json` (57 scenarios), post-regen **36/36 GREEN, 0 floor failures**
  (re-confirmed inside this session's 4981-test run). `dependency_setup` 87.23 → **99.89**
  (+12.66, expectation-fix attributed).
* **SI-10 chain timing**: full suite WITH coverage 85.50s this session; the v14.5.0 G-033
  single-execution reorg removed the 71 duplicated test executions per chain.
* **Token headrooms**: +1038 compile headroom (ADR-004); `context_profiles.yaml` overlay
  −26.5% (v14.4.0); SKILL.md −62 lines; dedup digests bounded at 320 chars (G-007) cap the
  pos-17 payload cost of self-containment.

Deductions: the 7-scenario drop is real measured regression vs the v14.5.0 equilibrium even
if attributed and floor-clearing — `spike_poc` −15.23pp deserves a scenario-level audit
(§4); self-contained digests add bounded but nonzero bytes to every deduplicated dispatch.

---

## §4 Gap list to reach the 9.5 bar (NOT-READY deltas)

1. **Wire `score_artifact_evidence` into `evaluate_gate`** (ADR-007 phase-2 completion) —
   converts output excellence from measured to gated; lifts §3.2. (~1 focused PV: adapter
   already exists as `ArtifactScore.to_gate_input()`.)
2. **`spike_poc` −15.23pp scenario audit** — confirm content-attribution vs latent selector
   regression with a per-section diff of the scenario's selected context; lifts §3.6.
3. **W-17 v14.4.0 attribution** — forensic move-exemption count or a written L0 waiver for
   the net +34; lifts §3.3.
4. **Shim/alias retirement criteria** — dated v16 entry conditions for the 24 ADR-006 shims +
   16 composition aliases (e.g. "0 deprecation-warning sightings across N cycles"); lifts
   §3.2/§3.4.
5. **Coverage lift** on `si_chip_bridge/runner.py` (66%) / `selector_cli.py` (73%) /
   `lifecycle/runtime_wiring.py` (77%); lifts §3.1/§3.3.

Items 1–3 are the substance; 4–5 are polish. A v15.0.1 follow-up landing items 1–3 with
clean evidence would plausibly re-score §3.2 → 9.5 and §3.6 → 9.4 (composite ≈ 9.5).

---

## §5 Inputs consumed

`.local/research/v15.0.0_nines.{json,md}` · `.local/research/v14.2.0_nines.md` ·
`.local/research/v14.2.0_gap_analysis.md` (41-gap register + §7 L0 ratification) ·
`.local/research/v15-cycle_design_review_{repo,product}.md` · `.local/research/adr/v15-ADR-001…008` ·
`CHANGELOG.md` `## [15.0.0]` … `## [14.2.0]` · git history cc54dda..e9e0118 + worktree ·
this session's runs: pytest (+coverage), ruff ×2, `make validate-templates`, NineS self-eval.

**End of round-1 SI-3 evaluation — composite 9.35 / 10 → NOT-READY at the operator-set
≥ 9.5 bar (clears the W-3 default MAJOR ≥ 9.0 by +0.35); §4 lists the 0.15 of concrete
deltas.**

---

# Round 2 re-evaluation (post-reinforcement, 2026-06-12)

A W-8 reinforcement round (R1–R5) discharged the §4 gap list. Round 1 above is preserved
verbatim as the audit trail; this section re-verifies each discharge against the tree and
re-scores only the dimensions whose evidence changed.

## R2-§1 Per-discharge verification (independent, not summary-trusted)

| Discharge | Verdict | Evidence (verbatim from tree, this session) |
|---|---|---|
| R1 — gate wiring (§4 ①) | **VERIFIED** | `gate/scorer.py:842` `def _attach_artifact_evidence(`, invoked on BOTH verdict paths (`:1247` break-verdict, `:1277` normal); `gate/models.py:245` `artifact_evidence_weight: float = 0.0` (RELAXED inherits 0.0); `gate/profiles.py:109/:141/:184` `artifact_evidence_weight=0.05` (STANDARD/STRICT/AUDIT); `scripts/detect_dead_apis.py:804` NOTE documents the `DEFAULT_ALLOWLIST` entry REMOVED ("the entry was REMOVED when the R1 gate wiring gave it a production caller") |
| R2 — spike_poc root-cause (§4 ②) | **VERIFIED** | `benchmarks/devolaflow_context/scenarios/spike_poc.yaml:9–14` carries the mechanism ("important-tier `context_isolation` (207) no longer fits" the unchanged 2975 budget; expectation → `wave_task_constraints`, "same refresh shape as dependency_setup"); `v15.0.0_baseline.json` spike_poc `"composite": 98.39, "budget": 2975`; `make test-benchmarks` 36 passed |
| R3 — coverage lift (§4 ⑤) | **VERIFIED (partial vs round-1 list)** | Fresh run: TOTAL 15485 stmts / 968 miss = **93.75%** (round 1: 15466/1063 = 93.13%); `si_chip_bridge/runner.py` 66→**95%**, `lifecycle/pre_plugin_invocation_upgrade.py` 75→**100%**, `lifecycle/runtime_wiring.py` 77→**100%**, `skills/slash_commands.py` 78→**98%**. Honest residual: `selector_cli.py` stays **73%** — it was on the round-1 §4 ⑤ list and was NOT lifted |
| R4 — retirement criteria (§4 ④) | **VERIFIED** | `adr/v15-ADR-002` §"Retirement criteria (v15.0.0 R2)" (RC-2.1..2.4: v16.0.0 window, EXTEND-to-v17.0.0 fallback, rg/pytest command anchors) and `adr/v15-ADR-006` §same (RC-6.1..6.3; S-10 permanent exemptions named: `feedback.py::populate_cascade_gate_fields` + `ProposalGenerator.generate_round_dispatch`); both carry honest "Status at authoring: NOT met" lines for the v16-time criteria |
| R5 — W-17 attribution (§4 ③) | **VERIFIED — round-1 ledger CORRECTED** | PR #149 body verbatim (fetched via `gh pr view 149`): "W-17: +34 (PV-1 29, PV-2 4, close 1; cycle ~71/150)" — per-PV 29/4/1, all ≤ 30. The round-1 §3.3 flag ("net +34 > the +30 per-PV cap … no per-task attribution recorded") was an **aggregation artifact**: the tag-to-tag diff counts a RELEASE total which W-17's per-PV cap does not bound. No waiver was ever needed. Correction + going-forward per-PV-split reporting rule recorded at retrospective §4.7 |
| Extra — CHANGELOG fixes | **VERIFIED** | `CHANGELOG.md` `## [15.0.0]` ghost-audit-housekeeping bullet now carries the wiring + allowlist removal + "R2 `spike_poc` same-cycle correction (84.63 → 98.39 …; all other baseline entries byte-identical)"; `python scripts/lint_changelog.py --base-ref origin/main` → "OK — 147 version blocks … clean" |
| Extra — RC-2.3 migration | **VERIFIED, with one audit catch (below)** | `references/shell-proxy.md:375` now reads `workflow_type: "change-driven(feature-enhancement)"` with the ADR-002 migration comment; the ADR's own RC-2.3 rg command prints **nothing** (exit 1 = zero sightings) |

**Audit catch (R2-F1):** the RC-2.3 migration edited the canonical
`workflow-system/agent/references/shell-proxy.md` without re-syncing the Cursor skill
mirror — fresh full-suite run FAILED 1:
`tests/test_version.py::test_cursor_skill_mirror_bytewise_parity[references/shell-proxy.md]`
(`make test-version` exit 2). The evaluator applied the canonical regeneration
(`python scripts/sync_cursor_skill.py` → "synced 1 file(s) to v15.0.0"; single derived
file, P1/S-8 trivial waiver), after which all gates pass. A W-5 coupling miss by the
reinforcement round, caught by the gate working as designed; priced into R2-§3.1 (no
score reduction elsewhere — the defect never reached the release surface).

## R2-§2 Evidence battery (exit codes + tails)

| Gate | Exit | Tail |
|---|---|---|
| `make test-core` | **0** | "4913 passed, 3 skipped, 2 xfailed, 43 warnings in 36.22s" |
| `make test-version` | **2 → 0** | pre-sync: "1 failed, 34 passed" (R2-F1); post-sync: "35 passed in 0.13s" |
| `make test-benchmarks` | **0** | "36 passed, 17 warnings in 1.84s" |
| `make lint` | **0** | "All checks passed!" + "381 files already formatted" |
| `make check-cursor-skill` | **0** | "[.cursor mirror] ok (26 files, stamp 15.0.0)" |
| `python scripts/lint_changelog.py --base-ref origin/main` | **0** | "lint_changelog: OK — 147 version blocks; R1 structure + R3 version-match + R2 immutability (base: origin/main) clean." |

Fresh full coverage run: 4993 passed + 1 failed (R2-F1, fixed) + 3 skipped + 2 xfailed in
84.88s; **coverage 93.75%**. W-17 post-reinforcement: v15.0.0 release raw +42 / −10
(T-waves 34 per the CHANGELOG split + R-round 8, each PV ≤ 30); cycle net **+124/150**.

## R2-§3 Re-scored dimensions (moved only where evidence changed)

| Dimension | Weight | R1 | R2 | Why moved (or not) |
|---|---|---|---|---|
| §3.1 Code quality | 0.20 | 9.4 | **9.5** | Worst modules lifted via behavioral tests (subprocess contracts, S-5/S-8 paths), 93.75%. Residuals: 18 undocumented defs, 43 warnings, `selector_cli.py` 73%, R2-F1 sync miss (caught + fixed) |
| §3.2 Architecture rationality | 0.20 | 9.3 | **9.5** | Both structural deductions discharged: output excellence now *gated* (profile-weighted, absence-safe byte-identical, doctrine parity via `EvidenceDoctrineError` propagation) and compatibility surfaces carry dated, command-anchored exit criteria with S-10 exemptions named. Not higher: stale A-7.1 compiled prose unchanged; the surfaces themselves still exist (criteria ≠ retirement) |
| §3.3 Test adequacy | 0.20 | 9.2 | **9.5** | The W-17 deduction is RETRACTED (R5 — aggregation artifact, cap never breached; per-PV-split reporting rule codified); coverage +0.62pp with behavioral (not line-touching) tests; cycle budget intact at +124/150 incl. the R-round's +8. Residual: `selector_cli.py` 73% |
| §3.4 Maintainability | 0.15 | 9.5 | **9.6** | "Permanent for now" shim/alias tracking replaced by dated RC sections with EXTEND fallback — the §3.4 deduction now has an auditable exit path. Residual: CHANGELOG growth unchanged |
| §3.5 Compatibility | 0.10 | 9.6 | **9.6** | No move — the round-1 deductions (yaml-file readers break, `ui-pro` rename, `DEFAULT_EVENTS` re-pin) are unchanged facts of the MAJOR; retirement criteria improve FUTURE management, not the v15.0.0 surface |
| §3.6 Performance impact | 0.15 | 9.2 | **9.5** | Worst re-equilibration root-caused at mechanism level (G-038 content growth vs fixed 2975 budget — expectation refresh, NOT a selector regression; 84.63 → 98.39; 56 sibling entries untouched, 36/36 green). Residual: 6 remaining content-attributed re-equilibrations un-audited at this depth; bounded digest bytes |

```
composite = 0.20·9.5 + 0.20·9.5 + 0.20·9.5 + 0.15·9.6 + 0.10·9.6 + 0.15·9.5
          = 1.900 + 1.900 + 1.900 + 1.440 + 0.960 + 1.425
          = 9.525 → 9.53 (2 dp)
```

## R2-§4 VERDICT: **READY — 9.53 ≥ 9.5 (operator-set MAJOR bar), by +0.03**

W-8 accounting: score moved 9.35 → 9.53 in one reinforcement round (5 rules ≤ the W-8
max-5 ceiling; no stagnation; no human escalation trigger). Residuals carried to the next
SI-1 (NOT release blockers): `selector_cli.py` 73% coverage; stale A-7.1 compiled prose;
mechanism-level audit of the 6 remaining re-equilibrated scenarios; CHANGELOG archival;
R2-F1 class prevention (reference edits MUST run `make sync-cursor-skill` in the same
change per W-5 coupling).

S-11 consequence: §3.2 = 9.5 now MEETS W-21 condition 3, but the RE-DEFER stands on
condition 2's failure — see the R2 addendum in `v15.0.0_s11_evaluation.md`.
