# DevolaFlow v9.3.0 — W-2 / SI-2 NineS Self-Evaluation Summary

| Field | Value |
|---|---|
| Cycle | v10.0.0 (5-minor rollup) |
| Minor | v9.3.0 — Performance Overhaul #1 |
| Authored | 2026-05-02 |
| Baseline version compared | v9.2.4 |
| NineS CLI version | 3.3.0 (`/root/miniforge/bin/nines`) |
| Raw JSON | `.local/research/v9.3.0_nines.json` (56 KB) |
| Stderr | `.local/research/v9.3.0_nines.stderr` |

## Headline metrics

| Metric | v9.2.4 | v9.3.0 | Δ |
|---|---:|---:|---:|
| `overall` (raw composite) | 0.907350 | 0.907266 | −0.000084 |
| `weighted_overall` | (n/a — different config) | 0.7260 | n/a |
| `group_means.capability` | (n/a) | 0.8074 | n/a |
| `group_means.hygiene` | (n/a) | 0.5360 | n/a |

## Interpretation

* **`overall` is byte-stable** within ±0.001 noise floor — the composite stayed at 0.907 across the cycle, matching the canonical PATCH-cycle byte-stable signature precedent (v9.2.2 → v9.2.4 also held 0.907 across 3 PVs).
* The `weighted_overall` and `group_means.{capability, hygiene}` are NEW columns from this NineS run that weren't present in the v9.2.4 self-eval — comparing them to the v9.2.4 raw values is not meaningful. Future cycles should pin both `overall` and `weighted_overall` for full delta visibility.
* The `hygiene_mean` of 0.536 is low — likely reflecting the `_dead_api_pins` workaround pattern (PV-04, PV-05, PV-06 all introduced these) which the NineS hygiene evaluators may flag. The pattern is correct per the architectural decision (the symbols ARE alive at their qualified names; the detector's allowlist surface is stale post-PV-04 split). A v9.4.0+ PV should migrate the `DEFAULT_ALLOWLIST` entries to the new qualified names to clear the hygiene signal.

## Cycle invariant — composite byte-stability

```
v9.2.2 NineS:   0.907348
v9.2.3 NineS:   0.907350
v9.2.4 NineS:   0.907350  (Δ from v9.2.3 = +0.000000)
v9.3.0 NineS:   0.907266  (Δ from v9.2.4 = −0.000084; well under 0.001 noise floor)
```

The negative delta of −0.000084 is below the 0.001 noise floor and is consistent with the cycle's purely-additive nature (no behaviour changes; only a new env flag opt-in surface + library + cache layer + module split).

## Cross-reference

* `.local/research/v9.3.0_evaluation.md` — the W-3 / SI-3 weighted composite (9.350 / 10) which is the human-side rubric. NineS provides a complementary algorithm-side signal.
* `.local/research/v9.3.0_retrospective.md` — the W-7 / SI-8 retrospective documenting the cycle's deferrals + learnings.

— L1 Stage Agent (PV-07 cycle close NineS write-up), 2026-05-02
