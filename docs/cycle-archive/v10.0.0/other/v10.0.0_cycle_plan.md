# v10.0.0 MAJOR Cycle Plan — Five-Minor Rollup (user-approved 2026-05-02)

**Cycle branch strategy:** per-minor branches + `feat/v10.0.0-cycle-major-rollup` (rollup PR)  
**Baseline:** v9.2.4 (head `78a8475`)  
**Target:** v10.0.0 MAJOR release  
**Cycle composition:** v9.3.0 → v9.4.0 → v9.5.0 → v9.6.0 → v9.7.0 → v10.0.0 (**5 MINORs + 1 MAJOR rollup**)  
**Authored:** 2026-05-02  
**Status:** SI-1 Planning Gate (W-1) APPROVED — proceeding to v9.3.0 implementation

## User sign-off record (2026-05-02)

| Q | User choice | Impact |
|---|---|---|
| Q1 ordering | **(B) Perf first + Perf again after all** | 5 minors, perf brackets the middle three |
| Q2 Si-Chip depth | **(B) DEEP — bridge + always-on** | v9.5 full bridge + auto-invoke every SKILL-touching PV |
| Q3 Ref scope | **(A) All 19 deep-analyze** | v9.6 covers 10 active + 9 periodic through NineS |
| Q4 Perf aggressiveness | **(Hybrid) Incremental + async-hooks spike** | v9.3 AND v9.7 both incremental + one spike each |
| Q5 PR cadence | **(B) 5 minor PRs + 1 rollup PR = 6 PRs** | Each minor opens PR independently |

---

## 1. User Requirements (verbatim from `feedback_for_v9.2.4.md`)

1. **Plugin auto-install/upgrade** — plugins must auto-install globally on invocation, auto-upgrade daily; gap = `ensure_plugin()` never called from any dispatcher.
2. **Si-Chip integration (deep)** — wrap `https://github.com/YoRHa-Agents/Si-Chip` as plugin AND bridge module; apply to own skill corpus with benchmark-gated delta ≥ +0.10; unverified → feedback doc.
3. **Reference library refresh** — NineS deep-analyze all 19 tracked references, decompose into patch-series integrations, roll up as minor.
4. **Performance/task-decomposition overhaul** — address user efficiency complaints; download reference repos to `/home/agent/reference/`; improve parallelism, compression, inter-layer efficiency without regressing quality.
5. **Rollup to v10.0.0** — unify minors, full regression against v0–v9.2.4 feedbacks (no AC regression), update all human-facing docs (README/EN/ZH/demo/ST-7), PR to main.

---

## 2. Gap Analysis — Current State

### 2.1 Plugin auto-install gap

| # | Finding | Severity |
|---|---|---|
| P-1 | `ensure_plugin()` mature (schema v2, 3 backends) BUT only the installer module calls it. No dispatcher wiring. | **BLOCKER UX** |
| P-2 | No daily-upgrade surface. No `last_checked` timestamp tracking. | **MAJOR** |
| P-3 | Workflow `stage_mapping` declares plugins but dispatch doesn't pre-flight them. | **MAJOR** |
| P-4 | No env flag or lifecycle hook for auto-install orchestration. | **CRITICAL** |
| P-5 | Runtime registry has no `upgrade_cmd` field (legacy `plugins.yaml` does). | **MAJOR** |

### 2.2 Si-Chip integration gap

| # | Finding | Severity |
|---|---|---|
| SC-1 | Si-Chip v0.4.1 mature; non-interactive install available (`--target cursor --scope global --yes`). | **Ready** |
| SC-2 | Benchmark-gated self-optimization via `profile_static.py` + `aggregate_eval.py` + `real_llm_runner.py`. | **Benchmark-ready** |
| SC-3 | No entry in `runtime-plugins.yaml`. Must add schema v2 row with `backend: curl_install_script`. | **BLOCKER Phase 3** |
| SC-4 | No DevolaFlow-side wrapper module. Need `src/devolaflow/si_chip_bridge/`. | **MAJOR** |
| SC-5 | Q2=DEEP means auto-invoke on every SKILL-touching PV — needs `post_skill_edit` hook. | **DESIGN item** |

### 2.3 Reference library gap

| # | Finding | Severity |
|---|---|---|
| R-1 | 19 tracked refs; last synthesis 2026-04-11 (3 weeks stale). | **STALE** |
| R-2 | `/home/agent/reference/` holds 50+ cloned repos — local ground truth available. | **Good** |
| R-3 | 19 × `nines analyze --depth deep` ≈ 40 min batch → decompose into multiple PVs. | **Scope shape** |
| R-4 | No `scripts/nines_refresh_references.py` harness exists. | **MAJOR** |
| R-5 | Integration surface fragmented across SKILL.md / references/*.md / context_profiles.yaml. | **MINOR** |

### 2.4 Performance / task-decomposition gap

| # | Finding | Severity |
|---|---|---|
| PF-1 | `compressor.py` = **2541 lines** single module, hot path on every dispatch. | **BLOCKER perf** |
| PF-2 | `task_adaptive_selector.py` = **1680 lines**, deprecated line-based lookup still active (warnings). | **MAJOR tech-debt** |
| PF-3 | No async/concurrent layer on hot path. Dispatch serialization end-to-end synchronous. | **CRITICAL parallelism** |
| PF-4 | No LRU cache on `select_context` → recomputed every L1/L2/L3 hop. | **MAJOR** |
| PF-5 | No dispatch-latency benchmark harness exists. | **MAJOR measurement gap** |
| PF-6 | `run_hooks()` sequential even for pure/functional hooks. | **MINOR** |
| PF-7 | L0→L1→L2→L3 3-deep chain; Simple-task shortcut declared in SKILL.md but not auto-enforced. | **MEDIUM opportunity** |

---

## 3. Cycle Decomposition — 5 MINORs + 1 MAJOR (user-approved order)

### v9.3.0 — Performance Overhaul #1 (Phase 1)

**Feature branch:** `feat/v9.3.0-perf-overhaul-1`  
**Rationale:** user's #1 complaint → address first. Hybrid aggressiveness per Q4.

| PV | Deliverable |
|---|---|
| PV-01 | **Research.** Download LangGraph, CrewAI, AutoGen, Si-Chip-dogfood patterns to `/home/agent/reference/`. Profile current hot paths via `cProfile`. Author `.local/research/v9.3.0_perf_research.md`. |
| PV-02 | **Latency harness.** Add `benchmarks/devolaflow_context/latency_*.py` + `tests/test_dispatch_latency.py`. Capture v9.2.4 baseline + fresh v9.3.0 baseline (W-16 cycle-start regen). |
| PV-03 | **Incremental #1 — LRU.** `functools.lru_cache`-style ring (~64 entries) on `select_context(task_type, round_num, plan_mode)`. Invalidate on `.yaml` mtime change. |
| PV-04 | **Incremental #2 — Compressor split.** `compressor.py` (2541 LOC) → `compressor/pipeline.py` + `compressor/transforms/{layout,frozen_prefix,append_tail,...}.py`. Preserves A-2; 6 multi-baseline byte tests stay green. |
| PV-05 | **Architectural spike — Async hooks.** New `async_safe: bool` field on `DEFAULT_EVENTS`. `ThreadPoolExecutor(max_workers=4)` for parallelizable hooks. Pure/functional hooks (validate_dispatch, check_file_ownership) flagged safe. |
| PV-06 | **Simple-task auto-shortcut.** Enhance `change_activation.classify_complexity` to emit SHORTCUT_SIMPLE verdict; dispatcher honours via `dispatch_short_circuit` flag (skips L1/L2 when SIMPLE). |
| PV-07 | **Close.** Tests + W-18 ghost-audit + NineS + CHANGELOG + retrospective. Target: dispatch p95 ≥20% improvement, 0 composite-score regression. |

**Gate:** `standard` + latency guard (p95 ≥20% improvement OR within ±5%).

### v9.4.0 — Plugin Auto-Install & Daily Upgrade (Phase 2)

**Feature branch:** `feat/v9.4.0-plugin-auto-install`  
**Depends on:** v9.3.0 hook-classification infrastructure.

| PV | Deliverable |
|---|---|
| PV-01 | New lifecycle hook `pre_plugin_invocation`. Env flag `DEVOLAFLOW_AUTO_INSTALL_PLUGINS` (W-20 reuse check first). |
| PV-02 | Dispatcher wiring: `ProposalGenerator.generate_round_dispatch` + slash-command handlers call `ensure_plugin()` before executing any workflow stage whose `stage_mapping` cites a plugin binary. |
| PV-03 | Daily-upgrade CLI: `devolaflow plugins refresh [--force]`. Staleness probe: `last_checked` in `plugin_install.log` + 24h threshold. |
| PV-04 | `runtime-plugins.yaml` schema v3 additive: `upgrade_cmd`, `upgrade_check_frequency_hours`, `last_checked`. Backward-compat v1/v2. |
| PV-05 | Tests + W-18 + NineS + CHANGELOG + retrospective. |

**Gate:** `standard`.

### v9.5.0 — Si-Chip DEEP Integration (Phase 3, Q2=B)

**Feature branch:** `feat/v9.5.0-sichip-deep-integration`  
**Depends on:** v9.4.0 plugin auto-install.

| PV | Deliverable |
|---|---|
| PV-01 | `runtime-plugins.yaml` Si-Chip entry: `backend: curl_install_script`, non-interactive `--target cursor --scope global --yes`, distinguish-cmd `python <install>/scripts/count_tokens.py --file <install>/SKILL.md --both`. Auto-install test. |
| PV-02 | `src/devolaflow/si_chip_bridge/` module: `profile()`, `evaluate()`, `aggregate_delta()`, `apply_or_defer(threshold=0.10)`. ~500 LOC. |
| PV-03 | Template rewire: `skill-optimization.yaml` adds `si_chip_dogfood` stage; `self-update.yaml` adds optional `si_chip_gate`. |
| PV-04 | **DEEP always-on (Q2=B):** new `post_skill_edit` lifecycle hook auto-invokes Si-Chip dogfood on every PV modifying `workflow-system/agent/`. Env flag `DEVOLAFLOW_SI_CHIP_DEEP=1` enforces. |
| PV-05 | Self-application dogfood: run Si-Chip against SKILL.md + 10 references + 3 examples. Apply only `iteration_delta ≥ +0.10`; defer to `.local/feedbacks/v9.5.0_sichip_deferred.md`. |
| PV-06 | Tests + CHANGELOG + NineS + retrospective. |

**Gate:** `strict` (≥90, ≥90% cov) — cache-invalidation risk per A-2.

### v9.6.0 — Reference Library Refresh — ALL 19 (Phase 4, Q3=A)

**Feature branch:** `feat/v9.6.0-reference-refresh-all-19`

| PV | Deliverable |
|---|---|
| PV-01 | `scripts/nines_refresh_references.py` harness. Deep-analyze all 19 refs. Output `.local/research/v9.6.0_reference_deltas.md` (~2000 lines). |
| PV-02 | Wave #1: integrate high-relevance (score ≥ 4) deltas. Cite anthropic-advisor-tool, superpowers, get-shit-done, 8 others. |
| PV-03 | Wave #2: medium-relevance (score 3) deltas. |
| PV-04 | Wave #3: low-relevance (≤2) graduate/retire decisions + refresh `reference-dependencies.yaml` `last_checked` for all 19. |
| PV-05 | Synthesis + CHANGELOG + NineS + retrospective. |

**Gate:** `standard`.

### v9.7.0 — Performance Overhaul #2 (Phase 5, Q1=perf-again)

**Feature branch:** `feat/v9.7.0-perf-overhaul-2`  
**Rationale:** re-measure hot paths after Si-Chip + ref refresh stabilize skill corpus.

| PV | Deliverable |
|---|---|
| PV-01 | Re-baseline under new skill corpus + updated refs. Compare to v9.3 baseline. Identify any regression introduced by v9.5/v9.6. |
| PV-02 | Inter-layer message diff: predecessor_summary delta-compressed across rounds (hash-based dedup, round_num>1). Schema v6 APPEND. |
| PV-03 | Async dispatch spike: L2 Wave dispatches parallel L3 tasks via `asyncio.gather` when `wave.mode=parallel`. Maintains P1. |
| PV-04 | Selector warmup: pre-compute `select_context` for top-5 task types on session start. Opt-in `DEVOLAFLOW_WARMUP=1`. |
| PV-05 | Cumulative gain measurement. Target: latency p95 ≥35% improvement vs v9.2.4 baseline. |
| PV-06 | Tests + CHANGELOG + NineS + retrospective. |

**Gate:** `strict` + cumulative-latency guard ≥35%.

### v10.0.0 — MAJOR Rollup & Human-Facing Docs (Phase 6)

**Feature branch:** `feat/v10.0.0-cycle-major-rollup`

| PV | Deliverable |
|---|---|
| PV-01 | Cycle rollup commit. Version 9.7.x → 10.0.0 via `scripts/bump_version.py`. |
| PV-02 | Full regression: `scripts/audit_feedback_ac.py` (new) walks all 40+ feedback files v0→v9.2.4, confirms no AC regression. |
| PV-03 | Human-facing docs: README.md + EN/ZH user guides (8×2) + demo pages (5 HTML) + ST-7 versions.json + benchmark-results.html. |
| PV-04 | Final NineS self-eval (composite ≥ 9.0 for MAJOR) + SI-3 evaluation + SI-8 retrospective + W-19 archive at `docs/cycle-archive/v10.0.0/`. |
| PV-05 | PR open → `main` + SI-10 6-gate sequence. |

**Gate:** `audit` (≥95, ≥90% cov). W-3 MAJOR threshold: composite ≥ **9.0**/10.

---

## 4. Invariants & Governance

- **W-16 wholesale baseline regen:** required at v9.3.0 PV-01/02 (first PV after `.0` MINOR bump on 9.x series — this counts as cycle-start).
- **W-17 test cap:** ≤+30 NEW/PV; cumulative cycle delta ≤+150. Audit at each minor's close PV.
- **W-18 ghost-audit refresh:** every feature-mentioning CHANGELOG entry preceded by ghost-audit coverage.
- **W-19 archive at v10.0.0 close:** `docs/cycle-archive/v10.0.0/` committed.
- **W-21 Soul-set freeze at 10:** v10.0.0 inherits from v9.0.0; any S-11 telegraph deferred to v11.0.0 (2-cycle rule).
- **A-2 frozen prefix:** positions 1–12 never reordered; v9.7.0 PV-02 APPENDs position 17 (cumulative: `change_context` already at 16).
- **S-6 protected branches:** all work on feature branches; `main` merged only via PR.
- **S-8 file ownership + S-9 handoff append-only:** per-PV enforcement.

---

## 5. Risk Register

| # | Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|---|
| R-1 | Si-Chip dogfood reveals all SKILL.md changes have `iteration_delta < +0.10` → v9.5 ships zero skill changes | MED | HIGH | Feedback doc capture IS the deliverable; plugin + bridge still ship |
| R-2 | Perf #2 regresses benchmark composite | MED | HIGH | `strict` gate; rollback per PV |
| R-3 | Ref refresh v9.6 cascades into 20+ SKILL.md edits breaking cache-prefix byte-stability | LOW | HIGH | A-2 multi-baseline byte test catches; per-ref commits |
| R-4 | Plugin auto-install v9.4 fails on networks without `curl`/`npm`/`pip` | LOW | MED | `auto_install=False` escape + `DEVOLAFLOW_AUTO_INSTALL_PLUGINS=0` opt-out |
| R-5 | Cycle exhausts W-17 budget over 5 minors × ~5 PVs = 25 PVs × +7 tests avg = +175 > 150 | MED | LOW | Parametrize expansions don't count; defer some tests to v10.1 |
| R-6 | Si-Chip network install fails (confirmed: 45s timeout on recon) | MED | MED | `local_fallback_path` + manual clone documented |
| R-7 | Compressor.py split introduces layout invariant drift | MED | CRITICAL | Multi-baseline byte test on every commit; rollback if drift |
| R-8 | v9.7 async dispatch breaks P1 (dispatcher implementing) | MED | HIGH | Code review gate; Task-tool-only dispatch enforced |
| R-9 | 5 minors × separate PRs each needing full SI-10 6-gate sequence = 30 gate runs (time cost) | HIGH | LOW | Expected; user chose this cadence (Q5=B) |
| R-10 | Si-Chip DEEP always-on (Q2=B) creates circular lifecycle (skill edit → dogfood → edit) | LOW | HIGH | Dogfood read-only mode during PV close; apply-or-defer gate prevents cascade |

---

## 6. Go/No-Go Entry Conditions

Before v9.3.0 PV-01 starts:

- [x] Feature branch `feat/v10.0.0-cycle-major-rollup` created (rollup container)
- [x] Cycle plan authored + user sign-off recorded (Q1–Q5 above)
- [x] v9.2.4 baseline captured (`v9.2.0_baseline.json` proxy)
- [ ] `feat/v9.3.0-perf-overhaul-1` branch created
- [ ] `/home/agent/reference/Si-Chip` cloned (deferred to v9.5.0 PV-01)

Entry satisfied at §6.1–6.3. Proceeding to v9.3.0 implementation.
