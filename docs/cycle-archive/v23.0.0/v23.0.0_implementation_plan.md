# v23.0.0 Implementation Plan

This cycle implements the four feedback items from `feedback_for_v21.md`
through six bounded PVs:

1. Make `entrance.md` a first-class artifact across prompt, scaffold, lint, and
   lifecycle enforcement surfaces.
2. Add W-30 for bounded waits, safe ready-work concurrency, and local blocker
   isolation after preflight.
3. Measure token injection before compression, wire real dispatch slices, and
   preserve cache-layout witnesses.
4. Build the six-host harness with fixture replay, captured Cursor evidence,
   real Claude/Codex/Kimi probes, append-only ledger ingestion, aggregation,
   uncertainty metadata, and operator-run calibration.
5. Verify the quantitative targets, compile all host adapters, refresh ghost
   audits and generated documentation, then run the complete release gate.
6. Archive the settled research, harness evidence, evaluation, and
   retrospective under the cycle archive.

The attached cycle plan remains the authoritative planning record; this
artifact is the archive-facing implementation summary and does not replace or
modify that plan.
