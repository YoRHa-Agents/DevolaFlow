# v21.3.0 — SI-3 Evaluation Report

## Evidence status

**Disposition: CARRY_FORWARD / INSUFFICIENT for closeout.**

This report uses the unchanged prior evaluator artifact
`.local/research/v21.3.0_evaluation.json`, whose source filename is
`v21.3-main-post-t2-fix.json`. It is actual pre-close evidence from the
post-T2 evaluation, not a fresh post-release run. The raw cycle ledger
`.local/telemetry/harness.jsonl` is absent, so a fresh evaluator run and W-16
settlement cannot be completed. The prior JSON's `verdict` is `READY`, but
that value is carried forward only and is not a closeout verdict.

The prior artifact records:

- `run_id`: `v21.3-main-post-t2-fix`
- `sampled_at`: `2026-08-30T13:57:06.407969+00:00`
- `generated_at`: `2026-08-30T13:57:06.407969+00:00`
- caller-supplied `salt`: `v21.3-main-post-t2-fix-salt`
- `salt_status`: `AVAILABLE`
- `repo_ref`: `main`
- `repo_sha`: `30b8012e6dd504c2473a1e00ba43effae0a41a8a`
- `base_ref`: `HEAD~1`
- `ledger_path`: `.local/telemetry/harness.jsonl`
- prior artifact `status`: `AVAILABLE`

The attempted closeout command was:

```text
uv run python -m devolaflow.harness evaluate --ledger .local/telemetry/harness.jsonl --repo . --base HEAD~1 --threshold 8.5 --run-id v21.3.0-close-20260831 --salt v21.3.0-close-salt-20260831 --output .local/research/v21.3.0_evaluation.json
```

It exited `2` with:

```text
harness evaluate: .local/telemetry: harness.jsonl is required
```

No synthetic telemetry was used.

## Six W-3 dimensions

The scores and subcomponent values below are copied from the prior JSON.
Weighted values are the direct score × weight calculations shown for
traceability.

| Dimension | Weight | Score | Weighted |
|---|---:|---:|---:|
| Code quality | 0.20 | 10.0 | 2.00 |
| Architecture rationality | 0.20 | 9.29 | 1.858 |
| Test adequacy | 0.20 | 6.67 | 1.334 |
| Maintainability | 0.15 | 9.84 | 1.476 |
| Compatibility | 0.10 | 5.0 | 0.500 |
| Performance impact | 0.15 | 10.0 | 1.500 |
| **Composite** | **1.00** | — | **8.67** |

### Code quality — AVAILABLE, carry-forward

Prior score: `10.0`; weight: `0.20`. The prior JSON records
`ruff_lint={score: 10.0, available: true, value: true}`,
`ruff_format={score: 10.0, available: true, value: true}`, and
`coverage={score: 10.0, available: true, value: 91.0}`.
Weighted contribution: `2.00`.

### Architecture rationality — AVAILABLE, carry-forward with deferred T3 blocker

Prior score: `9.29`; weight: `0.20`. The prior JSON records
`layout_invariant={score: 10.0, available: true, value: true}` and
`quantifiable_ratio={score: 8.57, available: true,
value: 0.8571428571428571}`. The harness summary records `count: 140` and
the tier breakdown `invariant: 40`, `guard: 80`, `advisory: 20`; therefore
the quantifiable numerator is `40 + 80 = 120`, i.e. **120/140 =
0.8571428571428571**. This is below `0.9`, remains **INSUFFICIENT /
DEFERRED**, and is a T3 blocker. This report makes no `>=0.9` claim.
Weighted contribution: `1.858`.

### Test adequacy — AVAILABLE, carry-forward below threshold

Prior score: `6.67`; weight: `0.20`. The prior JSON records
`test_suite={score: 0.0, available: true, value: false}`,
`coverage={score: 10.0, available: true, value: 91.0}`, and
`w17={score: 10.0, available: true, value: 4}`. The available test-suite
signal is false, so this dimension remains below the `8.5` threshold.
Weighted contribution: `1.334`.

### Maintainability — AVAILABLE, carry-forward

Prior score: `9.84`; weight: `0.15`. The prior JSON records
`ruff_format={score: 10.0, available: true, value: true}` and
`docstrings={score: 9.68, available: true,
value: 96.81923972071372}`. Weighted contribution: `1.476`.

### Compatibility — AVAILABLE, carry-forward below threshold

Prior score: `5.0`; weight: `0.10`. The prior JSON records
`layout_invariant={score: 10.0, available: true, value: true}` and
`compatibility_suite={score: 0.0, available: true, value: false}`.
Weighted contribution: `0.500`.

### Performance impact — AVAILABLE, carry-forward

Prior score: `10.0`; weight: `0.15`. The prior JSON records
`budget_compliance_ratio={score: 10.0, available: true, value: 1.0}` and
`p95_headroom={score: 10.0, available: true, value: 0.28725}`.
Weighted contribution: `1.500`.

## Machine result and uncertainty

The prior JSON records `composite: 8.67`, `auto_fill_rate: 1.0`, and
`verdict: READY` at threshold `8.5`. Those are **CARRY_FORWARD** values,
not evidence that the post-release closeout passed. The current closeout
state is **INSUFFICIENT** because the source ledger is missing and no fresh
post-release evaluation can be reproduced.

The prior harness summary contains `records: 13`,
`metadata_records` for five runs, `tokens.budget_compliance_ratio: 1.0`,
`tokens.p95_budget_utilization: 0.28725`, and
`constraints.quantifiable_ratio: 0.8571428571428571`. Its metadata shows
available salts and repository facts for those prior runs, but those runs
are not a substitute for the missing raw ledger or a new closeout run.
Matched real-task quality outcomes, replicated release-delta uncertainty,
per-PV checklist completion, reversion/blocker trends, and quantitative
causality remain unavailable in this closeout context. These limitations
prevent a new W-16 baseline settlement.

## Closeout blockers

1. **Missing raw telemetry:** `.local/telemetry/harness.jsonl` is required
   by the evaluator and is unavailable.
2. **W-16 unsettled:** no
   `.local/telemetry/baselines/harness_baseline_v21.3.0.json` is created.
3. **T3 deferred:** `quantifiable_ratio=0.8571428571428571` (120/140)
   remains below `0.9`; no passing quantitative claim is made.
4. **Prior evidence only:** the copied JSON must remain labeled
   `CARRY_FORWARD` until a real ledger enables a fresh run.

## Cross-references

- `.local/research/v21.3.0_evaluation.json` — unchanged prior evaluator JSON
- `.local/telemetry/harness.jsonl` — missing raw telemetry source
- `docs/cycle-archive/v21.2.0/harness/v21.2.0_harness_baseline.json` —
  prior settled-cycle context
- `workflow-system/agent/references/evaluator-rosetta.md` — six-dimension
  signal cross-walk
- `AGENTS.md` §W-2, §W-3, and §W-16 — evaluator, evidence, and settlement
  contracts
