# v23.1.0 c4 Dedup Follow-up

## Status

The c6 report remains unchanged at
`.local/research/v23.1.0_validation_rerun.md`. Its c4 result is preserved as
`FAIL`, including the original collision finding. This artifact records the
follow-up remediation evidence only; it does not promote ROI or any missing
provider evidence.

## Remediation

The current feature branch contains the c4 ledger deduplication fix in
`src/devolaflow/harness/token_injection.py`:

- event identity no longer depends on the transport artifact SHA;
- equivalent measurements are compared without transport-only provenance
  fields;
- a legacy artifact-bound event ID is recognized through the stable
  `source/run_id/item_id` identity;
- changed measurements with the same stable identity still raise an explicit
  collision error; unrelated legacy ledger events remain readable.

## Reproducible evidence

Executed from the repository root with the checked-in `.venv` interpreter:

- `.venv/bin/python -m pytest tests/harness/test_token_injection.py -q`:
  **12 passed**.
- `.venv/bin/python -m pytest tests/harness/test_cli_probe.py
  tests/harness/test_aggregator.py -q`: **45 passed**.

The focused tests cover artifact-SHA changes, legacy artifact-bound event IDs,
changed-measurement rejection, byte-preserving deduplication, and unrelated
ledger history. These are remediation tests for the c4 failure; they do not
change the c6 12-cell execution, provider-usage, Kimi, skill-loaded, or ROI
evidence statuses.
