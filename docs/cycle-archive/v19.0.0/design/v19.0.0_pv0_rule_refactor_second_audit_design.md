# v19 PV-0 — Rule Refactor Second Audit

Date: 2026-08-28

Scope: read-only verification before any `.rules/` source edit. This report is
the only artifact written by this audit. No rule source, compiled surface,
schema, witness, plan, or gap-analysis file was changed.

## Conclusion

**BLOCKER for applying the refactor.**

The current corpus, index, compiler drift state, canonical dispatch layout, and
Tier-A witness set pass the available local checks. The proposed refactor
cannot be approved from this audit alone because:

1. R1/R2 merges still require explicit operator disposition where heading
   discoverability or consent semantics change.
2. The current source and test documentation contain stale contradictory
   position/count statements that must be dispositioned before treating the
   merged corpus as internally consistent.
3. External-consumer, installed-CLI, host-adapter, and direct operator-usage
   evidence is **INSUFFICIENT**. Compatibility facades and zero-load references
   must therefore be retained.

## 1. Rule census and cap

The exact rule-id heading census from `.rules/*.mdc` is:

| Layer | Source | Exact IDs | Count |
|---|---|---|---:|
| Soul | `.rules/soul.mdc` | `S-1` through `S-10` | 10 |
| Architecture | `.rules/architecture.mdc` | `A-1` through `A-7` | 7 |
| Conventions | `.rules/conventions.mdc` | `C-1` through `C-7`, `C-9` | 8 |
| Workflow | `.rules/workflow.mdc` | `W-1` through `W-9`, `W-16` through `W-25` | 19 |
| Style | `.rules/style.mdc` | `ST-1` through `ST-13` | 13 |
| **Total** | 5 sources |  | **57** |

Current census findings:

- **PASS:** 57 is the exact on-disk baseline.
- **PASS:** `S-1` through `S-10` are present exactly once; there is no Soul
  addition.
- **PASS:** no duplicate rule IDs were found.
- **PASS:** retired `C-8` and `W-10` through `W-15` are absent and have not
  been reused.
- **PASS:** `.rules/index.md` declares 10/7/8/19/13 and total 57, matching
  the source census.
- **PASS, projected only:** `W-26`, `W-27`, and `W-28` are absent from the
  current sources. Adding exactly those three unique headings would produce
  60, at the hard cap. This is not an observed on-disk 60-count; no source
  edit was made.
- **BLOCKER:** the cap leaves zero slots after the three accepted additions.
  Any fourth addition requires an earlier counted heading to be retired or a
  new cap decision/ADR.

The rule index is internally consistent now, but
`tests/ghost/test_rules.py` still contains a stale prose census describing
Soul 10 + Architecture 7 + Conventions 8 + Workflow 18 + Style 13 = 56.
The executable assertion computes the actual source count and passes. This is
a documentation contradiction to correct or explicitly disposition before
the apply phase.

## 2. Frozen dispatch and generated surfaces

`schemas/lean-dispatch.yaml` was parsed without modification:

- `layout_invariant.version`: 6
- `canonical_order` length: 17
- positions 1–12: `hdr`, `task`, `goal`, `assumptions`, `pred`, `files`,
  `rules`, `shared`, `accept`, `reinforce`, `verify_cfg`, `gate`
- `gate`: position 12
- append-only tail: `repos`, `behavioral_guidelines`,
  `acceptance_criteria_v2`, `change_context`, `predecessor_dedup_ledger`

The ten Tier-A witness files are present under
`benchmarks/devolaflow_context/baselines/`:

```text
layout_invariant_v7.0.0.yaml
layout_invariant_v7.3.0.yaml
layout_invariant_v8.0.0.yaml
layout_invariant_v8.3.0.yaml
layout_invariant_v8.4.0.yaml
layout_invariant_v9.2.0.yaml
layout_invariant_v9.3.0.yaml
layout_invariant_v9.7.0.yaml
layout_invariant_v10.2.0.yaml
layout_invariant_v12.0.0.yaml
```

**PASS:** the focused rule/layout/compiler suite passed 110 tests, including
the multi-baseline witness checks. The broader frozen and merged-rule suite
passed 658 tests with 2 skips. The existing test warnings are deprecation
warnings from line-based selector fallback, not failures.

No schema edit, canonical-order edit, or witness regeneration is authorized
by this audit. The following pre-existing wording contradictions remain
disposition items:

- `.rules/architecture.mdc` states that the last canonical slot is position
  16, while the live schema has 17 positions and the last slot is position
  17.
- `schemas/lean-dispatch.yaml` documents cascade default 4 and an
  `L0 → L1 → L2 → L3` chain in historical comments, while the active A-7,
  runtime helper, and tests use default 3 and `L0 → L1 → L2`.
- The schema and historical witness test prose still mention
  `AGENT_POOL_FORWARD`; `src/devolaflow/skills/subagent_pattern.py` states
  Pattern 3 is removed from the runtime selection surface. This must remain
  historical/compatibility wording unless an operator authorizes a separate
  schema/documentation decision. It must not be revived by rule refactoring.
- `tests/test_layout_invariant_multi_baseline.py` opens by saying “ALL 6”
  historical baselines while the live witness set and executable tests cover
  ten files and later describe the 15th baseline. The executable byte checks
  pass, but the prose is stale.

## 3. Compiler targets, budgets, and drift

` .rules/compile-config.yaml` has five configured layers and three targets:

- `cursor` → `.cursor/rules/repo-governance.mdc`, MDC, budget 14,000,
  all five layers
- `agents_md` → `AGENTS.md`, Markdown, budget 14,000, Soul/Architecture/
  Conventions/Workflow
- `style_md` → `docs/STYLE-RULES.md`, Markdown, budget 4,000, Style only

The compiler was run through `RuleCompiler.compile()` only, so it rendered in
memory and did not write targets:

- `cursor`: 11,307 / 14,000 tokens; all five layers included
- `agents_md`: 10,076 / 14,000 tokens; four configured layers included
- `style_md`: 1,190 / 4,000 tokens; Style included

**PASS:** all three configured targets are under budget.

**PASS:** ` .venv/bin/check-rules-drift` reports `cursor`, `agents_md`, and
`style_md` in sync; `.rules/.compile-hashes.json` has all three target hashes.

**PASS:** `AGENTS.md` contains only the Style pointer and no inline Style
heading/body; `.cursor/rules/` contains no retired pointer stubs.

The repository interpreter is available as `.venv/bin/python` (Python
3.13.12). The `python` command is unavailable in this shell and `python3`
resolves to Python 3.9.6, which cannot collect this Python 3.11+ repository.
Therefore Makefile recipes that directly invoke `python` are not runnable as
written in this shell; their repository-interpreter equivalents are runnable.

Commands run successfully with the repository interpreter:

```text
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider tests/ghost/test_rules.py tests/test_rules_index_accuracy.py tests/test_local_compiler.py tests/test_local_drift.py tests/test_self_improve_rules.py tests/test_layout_invariant_multi_baseline.py -q
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider tests/ghost/ -q
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider tests/harness/ -q
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider tests/test_local_archive.py tests/test_local_archive_cli.py tests/test_local_archive_doctor.py tests/ghost/test_features_v17_5.py -q
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider tests/test_grill_mode.py tests/test_domain_awareness.py tests/test_subagent_patterns.py tests/test_demo_nav.py tests/test_demo_i18n_parity.py tests/test_doc_consistency.py tests/test_template_web_design.py tests/test_release_pipeline_alignment.py -q
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv/bin/python -m pytest -p no:cacheprovider tests/test_dispatch_layout_v5.py tests/test_compressor.py tests/test_behavioral_guidelines.py tests/test_cascade_enforcement.py tests/test_layer_budget_assertion.py tests/test_agent_workspace_schemas.py tests/test_lint_human.py tests/test_archiver_refresh_and_subdirs.py -q
.venv/bin/check-rules-drift
.venv/bin/ruff check src/ tests/
.venv/bin/ruff format --check src/ tests/
```

The harness suite passed 96 tests; the local-archive evidence suite passed 32;
the merge-related contract suite passed 129; the complete ghost suite passed
43 tests with 138 intentional skips. Ruff check and format check both passed.

The configured compiler write command remains:

```text
.venv/bin/python -c "from devolaflow.local.compiler import RuleCompiler; RuleCompiler('.rules/compile-config.yaml').compile_all()"
```

It must only be run after approved source edits, because `compile_all()` writes
all configured targets and the hash store. This audit did not run it.

## 4. W-26/W-27/W-28 acceptance audit

The proposed headings are not present yet. Their underlying local-archive
behavior is present in `workflow-system/agent/references/local-archive.md`,
`src/devolaflow/local/archive.py`, `schemas/local-archive.schema.yaml`, and
the current local-archive tests.

### W-26 — Deletion safety

**Safe to apply as an independent Workflow heading, subject to operator
approval and index/ghost updates.** The reference and runtime preserve:

- operator-only deletion;
- no delete action/API or automatic deletion mode;
- report-only planning and approved non-deletion moves;
- no authorization from clean checks or plan approval;
- no `git clean -fdx` equivalent;
- explicit safety findings for dirty, missing, unreadable, nested-repository,
  symlink, protected, and worktree-conflicted conditions.

The tests cover deletion-request refusal, report-only zero-write behavior,
dirty trees, symlinks/traversal, nested repositories, worktree registry,
protected paths, unknown candidates, and approval mismatches. A deterministic
one-to-one test inventory for every proposed refusal adjective is not present;
especially unreadable-source and ambiguous-metadata coverage should be
confirmed when W-26 is landed. Do not add a delete API or a delete action.

### W-27 — Mapping append-only behavior

**Safe to apply as an independent Workflow heading, subject to operator
approval and index/ghost updates.** The implementation and tests preserve the
dedicated `.local/tasks/archive-mappings.yaml` record, fields
`sequence`, `source`, `destination`, `reason`, and `timestamp`, monotonic
sequence allocation, immutable existing bytes, and duplicate source or
destination refusal. It remains distinct from S-9 handoff envelopes.

The local-archive tests cover sequence 1/2 allocation, prefix preservation,
and duplicate refusal. A new current-cycle ghost assertion keyed explicitly
to W-27 is still needed before any future CHANGELOG claim.

### W-28 — Index-generation honesty

**Safe to apply as an independent Workflow heading, subject to operator
approval and index/ghost updates.** The implementation and tests preserve the
generated marker, mapping-ledger authority, approved creation of a missing
index, and refusal to overwrite human-maintained, symlinked, unreadable, or
stale/non-generated index bytes.

The existing CLI and local-archive tests cover generated-index behavior and
nonzero refusal findings. A new current-cycle ghost assertion keyed explicitly
to W-28 is still needed before any future CHANGELOG claim.

The three additions are individually testable and should not be merged into
one counted heading on first landing. If all three are added, the index must
move from 57 to 60 and the cap must be treated as exhausted.

## 5. R1 merge audit

The following dispositions separate semantic safety from discoverability risk.

| Proposed merge | Audit result | Required condition |
|---|---|---|
| A-1 repeated P1 text → S-1 pointer | Safe with a precise pointer | Preserve all P1–P5 hierarchy clauses, the layer budgets, and S-1’s prohibition verbatim in meaning. Do not weaken the dispatcher-not-implementer rule. |
| A-3 + W-6 | **Operator disposition needed** | Preserve explicit task-type budgets, priority allocation, critical/important/supplementary semantics, affected-task verification, and the exact selector command. Decide whether W-6 must remain independently discoverable despite losing its heading. |
| C-2 + C-3 | Safe with clause retention | Keep lean shape, verbatim extraction, source citations, and artifact-transport tests. Retire only the counted heading, not the behavior or lineage. |
| C-4 + C-5 + C-9 | Safe with full subsections | Retain every frontmatter key, all agent-workspace and human-surface budget rows, token units, soft-warning/hard-fail semantics, and both lint commands. |
| W-7 + W-19 | Safe with ordering retained | Keep the four retrospective contents, archive layout, cycle-close timing, idempotence, and the W-16 settlement-before-archive relationship. |
| W-9 + C-1 | **Operator disposition needed** | Preserve all seven SI-10 gates and single-execution semantics. Decide whether moving the always-on pre-commit checklist under Workflow changes required discoverability. |

Existing enforcement mappings are adequate for the behavior portions:
`tests/test_pv07_agents_md_slice.py`, `tests/test_async_wave_dispatch_wired.py`,
`tests/test_subagent_patterns.py`, `tests/test_layer_budget_assertion.py`,
`tests/test_task_adaptive_selector.py`, `tests/test_schema_validation.py`,
`tests/test_lean_report_schema.py`, `tests/test_agent_workspace_schemas.py`,
`tests/test_lint_human.py`, `tests/test_archiver_refresh_and_subdirs.py`,
`tests/test_release_pipeline_alignment.py`, and the ghost suite cover the
listed contracts. The apply task must update direct source-heading assertions
and keep historical behavior tests, changing citations only where behavior is
unchanged.

## 6. R2 merge audit

| Proposed merge | Audit result | Required condition |
|---|---|---|
| W-22 + W-23 | **Operator disposition needed** | Preserve one-question-at-a-time activation, ADR three-condition gate, explicit per-session consent before auto-writes, lazy creation, aliases, project-specific scope, and CONTEXT.md/spec.md separation. The current implementation has no automatic glossary writer, so consent is primarily a normative/documentation contract; no test proves a writer cannot bypass it. |
| ST-1 + ST-5 | Safe with full checklist retention | Keep the complete content registry and every six-point demo consistency check, including bilingual and 640px checks. |
| ST-3 + ST-12 | Safe with surface-specific subsections | Preserve guide parity and showcase-page parity as separate examples; retain both i18n test families. |
| ST-4 + ST-7 | Safe with derived/literal distinction | Keep all C-6 canonical locations, render/load-time derivation behavior, timeline fields, and no-invented-metrics rule. |
| ST-6 + ST-13 | Safe with graph obligations retained | Keep four mandatory routes, deprecation conditions, minimum 12 internal links, four GitHub Skill links, and CTA-cluster integrity. |
| ST-8 + ST-9 + ST-10 | Safe with independent cases | Retain motion live demos/replay/reduced-motion, token dark-mode/TokenSwatch, and component live examples/i18n descriptions. |
| ST-11 | **Keep separate** | This audit found no evidence that SKILL-to-showcase cascade coverage is complete under ST-8. Do not fold it based on thematic similarity. |

The focused R2 tests passed, but passing tests do not resolve the operator
decision about activation/discoverability changes. No external operator usage
evidence exists; that evidence remains **INSUFFICIENT**.

## 7. Apply boundary

Safe after explicit disposition:

1. Add W-26, W-27, and W-28 independently to `.rules/workflow.mdc`.
2. Add their enforcement mappings and update `.rules/index.md` to 60.
3. Add current-cycle ghost assertions before any CHANGELOG claim.
4. Apply only R1/R2 rows marked safe, preserving every listed clause and
   updating lineage references.
5. Recompile through the configured compiler, then run drift, ghost, layout,
   reference/manifest, harness, and applicable release checks.

Blocked until operator disposition:

- whether C-1 and W-6 retain counted compatibility headings;
- whether W-22/W-23 can share one heading without weakening explicit consent;
- how to resolve the stale A-2 position-16 statement and historical cascade/
  Pattern-3 wording without changing the frozen schema in this PV;
- any removal of compatibility facades, legacy names, or zero-load references,
  because external-consumer and installed-CLI evidence is **INSUFFICIENT**.

Not authorized by this audit:

- any Soul addition or S-ID reuse;
- reuse of C-8 or W-10 through W-15;
- schema canonical-order edits;
- Tier-A witness edits or regeneration;
- hand-editing `AGENTS.md`, `.cursor/rules/repo-governance.mdc`, or
  `docs/STYLE-RULES.md`;
- compiler writes before source-edit approval.
