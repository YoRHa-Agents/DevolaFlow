# v19.0.0 PV-5 A5 Marker Evidence

Scope: remove unreachable historical source-shape blocks from compatibility
facades only when the live owner module and focused tests carry the contract.

## Removed markers

- `src/devolaflow/compressor/transforms.py`: removed the `if False` helper
  definitions. The implementations live in
  `src/devolaflow/_compressor_transforms/summary.py`. The static complexity
  checks in `tests/test_compressor.py` read that owner module directly, and
  `tests/test_module_split_shims.py` verifies the facade still re-exports the
  historical helper names and public wrappers.
- `src/devolaflow/agent_workspace/lint.py`: removed the `if False` digest
  budget definitions and the stale Pathfinder comment pin. The live symbols
  are owned by `src/devolaflow/_workspace_lint/api.py` and the Pathfinder
  finding is owned by
  `src/devolaflow/_workspace_lint/advanced_semantics.py`. Ghost and focused
  tests assert those owner-module contracts directly.

The removed marker regions account for exactly 12 lines in
`compressor/transforms.py` and 13 lines in `agent_workspace/lint.py` (25
source lines total, including marker comments and spacing).

## Retained markers

- `src/devolaflow/plugins/installer.py` retains the source-only
  `_LAST_CHECKED_SUCCESSFUL_EVENTS` assignment and `_parse_log_event_timestamp`
  stub. `tests/ghost/test_features_v10_2.py::test_v10_2_4_new_symbols_have_coverage`
  parses this historical public path and requires both names, so removing the
  pins would invalidate the existing W-18 audit.
- `src/devolaflow/agent_workspace/reporter.py` retains its source-only
  `test_results`, `stagnation`, and `_check_digest_budget` comment pins.
  `tests/ghost/test_features_v14_1.py::test_v14_1_0_test_run_join_symbols`
  inspects this historical facade path for the two report inputs. The live
  digest-budget implementation and its call-shape audit are in
  `src/devolaflow/_workspace_reporter/`, while the comments remain as
  historical source-shape context outside this `if False`-only cleanup.

These retained markers do not define runtime symbols: the compatibility
facades route imports to their owner modules. Public imports and exports remain
covered by the focused facade tests.
