# v14.1.0 Implementation Evaluation (W-3 / SI-3)

> Date: 2026-06-04 | Cycle: v14.1.0 (MINOR) | Author-layer: L0
> Readiness bar: weighted composite **≥ 8.5** (MINOR).

## 1. Summary

v14.1.0 completes the v14.0.0 human-surface OUTPUT contract (§6c test-run-artifact join + §4 OUTPUT
conformance) and closes the v14.0.0 review findings. All W-9/SI-10 6 steps GREEN; A-2 untouched
(`canonical_order` 17); zero new env flags / Soul rules.

## 2. W-9 / SI-10 6-step gate (all PASS)

| # | Step | Result |
|---|---|---|
| 1 | `pytest tests/ -q` | **PASS** — 4710 passed, 3 skipped, 2 xfailed (after the demo-version fix; 1 transient FAIL was the demo What's-New version, now resolved) |
| 2 | `ruff check src/ tests/` | **PASS** — All checks passed |
| 3 | `ruff format --check src/ tests/` | **PASS** — 321 files already formatted |
| 4 | `pytest tests/test_version.py -v` | **PASS** — 35 passed (canonical 8 sync 14.0.0 → 14.1.0; mirror parity ok, 25 files, stamp 14.1.0) |
| 5 | `pytest tests/test_benchmarks.py -v` | **PASS** — 36 passed; W-16 `v14.1.0_baseline.json` byte-identical scoring to v14.0.0 (no benchmark-affecting file changed) |
| 6 | `make check-cursor-skill` | **PASS** — exit 0 (`.cursor/skills/devola-flow/` mirror present + in sync) |

Adapter build (W-5/W-12): `build-skill --all` → 16/16 within budget (cursor 492/500, codex 480/500,
claude 67/200, copilot 1922/4000).

## 3. Weighted dimension scoring

| Dimension | Weight | Score | Rationale |
|---|---|---|---|
| Code quality | 0.20 | 9.0 | ruff clean; S-5 loud failures (FileNotFoundError / RequirementsTraceError on missing/malformed report-log); frozen dataclasses (`TestOutcome`); verbatim C-3 evidence; `requirements_trace` 94% coverage |
| Architecture rationality | 0.20 | 9.0 | No dispatch-schema change (A-2.4 multi-baseline untouched); hook stays opt-in (cache-stable lifecycle tuple preserved); union-trace restores S-5 symmetry in BOTH directions; defaults keep field-growth back-compatible |
| Test adequacy | 0.20 | 9.0 | +18 NEW test functions (≤30 W-17); both-direction S-5; join-override regression (failed test overrides optimistic matrix); 4-flavour byte-stability (R3); parse→join end-to-end; S-3 changed-module coverage 94%/87% (≥80%) |
| Maintainability | 0.15 | 9.0 | Clear docstrings + the workflow-close caller contract documented in `references/human-surface.md` §6c; doc-accuracy cluster closed (hook wording, 3 READMEs, archive index, rules count) |
| Compatibility | 0.10 | 9.5 | Fully backward compatible — omitting `test_results` is byte-identical to v14.0.0; new fields defaulted; baseline byte-identical; no breaking signature change |
| Performance impact | 0.15 | 9.0 | No benchmark regression (W-4 36/36; baseline scoring identical); pure-function join is O(REQs); no new IO on the default path |

**Weighted composite** = 9.0·0.20 + 9.0·0.20 + 9.0·0.20 + 9.0·0.15 + 9.5·0.10 + 9.0·0.15
= 1.80 + 1.80 + 1.80 + 1.35 + 0.95 + 1.35 = **9.05 / 10** ≥ 8.5 (MINOR) → **PASS**.

## 4. W-2 / SI-2 NineS self-eval

`nines -f json -c nines.toml self-eval --baseline-version 14.0.0 ...` →
`.local/research/v14.1.0_nines.json`. **overall = 0.9070** (preserved vs the v13.0.0/v14.0.0
equilibrium). `lint_cleanliness 1.0`, `test_count 1.0`, `change_detection 1.0`, `convergence_rate 1.0`,
`docstring_coverage 0.976`. NOTE: `code_coverage 0.0` + `source_freshness 0.5` are harness artifacts —
the `pytest --cov` sub-run timed out at the NineS-internal 54s budget (logged in
`v14.1.0_nines.stderr.log`), not a real coverage regression (the in-cycle S-3 run measured 94%/87% on the
changed modules). These two artifact dimensions have appeared identically since v13.0.0.

## 5. Invariants held

- **A-2** untouched — no new dispatch key; `canonical_order` stays 17; A-2.4 multi-baseline GREEN.
- **W-21** — no new Soul rule (freeze at 10/12).
- **W-20** — no new `DEVOLAFLOW_*` env flag (count stays 7).
- **S-6** — feature branch `feat/v14.1.0-human-surface-output`; PR (never push `main`).
- **S-2** — relative paths only.

## 6. Verdict

Composite **9.05 ≥ 8.5** → **RELEASE-READY** for v14.1.0 (MINOR). No reinforcement round required.
