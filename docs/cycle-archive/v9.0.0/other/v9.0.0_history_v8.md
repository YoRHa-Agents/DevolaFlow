# v8.x Era — Per-PV Change Manifest (v9.0.0 SI-1 Input)

**Audience:** v9.0.0 SI-1 planning gate consumer (T-S01-T05 Research-team deliverable).
**Era boundaries:** all `^## \[8\.` entries in `CHANGELOG.md` (9 release headers; ~33 git tags including patches/PVs).
**Era window:** 2026-04-22 (v8.0.0) → 2026-04-23 (v8.4.0) — ~2 calendar days, ~70 hours wall-clock.
**Theme arc:** Karpathy primitives (v8.0.0) → self-analysis pre-release (v8.1.0-rc.1) → trilogy rollup (v8.2.0) → agent workspace + governance (v8.3.0) → RTK + memory router (v8.3.1..v8.3.4 → v8.4.0).
**Per Rule C-3:** file paths, commit hashes, schema versions, metric values are verbatim CO-2 lifts from CHANGELOG.md / `.local/research/v8.x_*.md` / `.local/feedbacks/feedback_for_*.md`.

---

# v8.x Era

## v8.0.0 — Karpathy Primitives Cycle (MAJOR, 2026-04-22)

**Cycle theme:** 13 patches (P-01 → P-13) closing 14 of 14 Karpathy "ChatGPT Vibes Engineering" primitives + 12 of 12 NineS deep-analyze findings (1 ERROR + 11 WARN). Driven by `tweet_analysis_harness_engineering_v7.8.md` (1219-line v2 refresh) + `.local/research/v8.0.0_patch_plan.md` (862 lines, 13 patches × 8 mandatory fields).

### Cycle-level metrics (verbatim)

| Metric | Value |
|---|---|
| SI-3 composite | **9.55 / 10** (≥ 8.5 minor; +1.05 pp margin) |
| NineS post-cycle ERROR/WARN | 1+11 → **0+6** (−50% closure; cycle 12/12 closed but 4 net-new findings in P-09/P-10/P-12 infra) |
| Tests added | 1641 → **2367** (+778 / 1589 if pre-baseline; +726 net post-Wave-1 per evaluation §1) |
| Gate-suite tests | 97 → **948** (9.77× expansion) |
| EvoBench delta | 36/36 scenarios; 0 regression > 5pp; net composite **+0.067 pp** (97.324 → 97.391); worst delta `−0.760 pp` on `skill_optimization` |
| P6 transitions | 13 → 14 → **15** (version 2 → 3 → **4**) — v7.0.0 + v7.3.0 byte-baselines STILL PASS |
| SKILL.md | 493 → **495 / 500** (P-11 entropy-cleanup row +2; P-08 net ±0 via 1-line compression) |
| SF-4 references | 8 → **9** (added `references/behavioral-guidelines.md` via P-08) |
| Cycle wall | ~16 hours (2026-04-21 14:55 UTC → 2026-04-22 04:17 UTC) |
| Patch accept rate | **13 / 13 = 100%** (continues 18/18 v7.x trilogy + 4/4 v7.2.0 + 6/6 v7.3.0 + 8/8 v7.5.0) |
| build-skill adapters | **11 / 11 PASS** (claude/cline/codex/continue/copilot/cursor/kimicode/openclaw/roo/windsurf/zed) |

### Per-PV ledger (P-01 → P-13)

#### P-01 — Cyclomatic-Complexity Cleanup (commit `3f7735d`, PR #59, S00 W1 T01)
- **Scope:** `adapters/data_driven.py` (`_apply_transform` 22→3, `_split_by_heading` 15→1), `template_engine/runtime.py` (`select_stages_for_runtime` 13→3), `lifecycle/format_on_edit.py` (`_collect_violations` 12→6), `nines/researcher.py` (`refresh_reference_dependency` 12→6), `task_adaptive_selector.py` (`main` 11→3).
- **Acceptance:** "公共 API 字节相同" + "radon -nc 不再报告 cc≥10" — both met. Closes 1 NineS ERROR + 5 WARN.
- **Tests:** +49 (`tests/test_complexity_cleanup.py`, 49 tests including 6 radon-cc enforcement parametrize).
- **KPI delta:** cc cleanup was 6 hotspots → all A-rank. Established the canonical 3-helper triad pattern (`resolve / filter / apply`) reused by P-07, P-08, P-11.

#### P-13 — CHANGELOG Backfill v7.6.0 / v7.7.0 / v7.8.0 (commit `5036cb8`, PR #60, S00 W1 T02)
- **Scope:** `CHANGELOG.md` (3 backfilled headers via verbatim CO-2 lift from commits `1a4f1ee` / `828b9ff` / `17d2a14`); `workflow-system/human/demo/index.html` "What's New" sync (DS-4 / ST-4); EN+ZH localised changelogs are NO-OP (files absent — DS-3 / ST-3).
- **Acceptance:** 3 doc-consistency assertions for the backfilled headers.
- **Tests:** +3.
- **KPI delta:** Closes derived candidate C1 from patch_plan §2.3.

#### P-02 — Layered + Directed Compaction (commit `03b9fb0`, PR #61, S01 W2 T01)
- **Scope:** `src/devolaflow/compressor.py` (split `summarise_predecessor` 16→4 into 3 helpers; new `directed_compact(text, focus_keywords, max_drop_pct=0.20)` with ≥80% focus retention); `workflow-system/agent/context_profiles.yaml` (`recency_decay_factor: 0.9`); `schemas/lean-dispatch.yaml` (NESTED `pred[*].compact_directive` — P6 invariant preserved).
- **Acceptance:** Closes Karpathy 4.1 + 4.2; 2 new EvoBench scenarios (`directed_compaction_focused.yaml` + `layered_recency_decay.yaml`).
- **Tests:** +35 compressor tests + 13 directed_compact tests.
- **KPI delta:** Closes 2 NineS WARN (`compressor.py`).

#### P-11 — Entropy + Learnings Refactor (commits `02d4ff8` + coupling `f68710e`, PR #62, S01 W2 T02)
- **Scope:** New `src/devolaflow/entropy_manager.py` (~470 lines: `DocFreshness` + `DeviationScanner` + cleanup); refactor `learnings.py::load_relevant_learnings` (cc 14→7) and `decay_confidence` (cc 12→7) extracting 7 pure helpers; `check_drift.py` becomes thin delegator. New template `entropy-cleanup.yaml` (template_count 20→21; profile_count 23→24).
- **Acceptance:** Closes Karpathy 4.5; "71 existing learnings tests pass byte-identical" (verified per evaluation).
- **Tests:** +67 (cumulative ~115 with cleanup helpers).
- **KPI delta:** SKILL.md 493→495 (entropy-cleanup template row); 2 NineS WARN closed.

#### P-03 — Token-Budget Circuit Breaker (commit `5076b42`, PR #63, S01 W2 T03)
- **Scope:** New `src/devolaflow/gate/budget.py` (296 lines, **100% coverage**) with `TokenBudgetBreaker.check()` returning CONTINUE / WARN / BREAK; STRICT cumulative > 80000 → BREAK + ESCALATE; `breaker` parameter on scorer (None = byte-identical); `max_tokens` profile field; NESTED `gate.token_budget` in lean-dispatch (P6 invariant preserved).
- **Acceptance:** Closes Karpathy 4.3; 0 EvoBench regression.
- **Tests:** +84 (target was +20; 4.2× over-delivery).
- **KPI delta:** First gate primitive shipped opt-in default-OFF (`breaker=None`).

#### P-04 — Deterministic Fence Expansion (commit `7b4a75f`, PR #64, S01 W2 T04)
- **Scope:** `src/devolaflow/gate/reinforcement.py` (`fence_to_instruction(fence_type, fence_payload)` mapping fence events to deterministic `ReinforcementRule` IDs `F-lint-001` etc.); scorer `_evaluate_checks()` emits `ReinforcementBlock` on failure; `references/execution-protocol.md` §9 appended ≤30 lines (Large tier 1000 ceiling preserved).
- **Acceptance:** Closes Karpathy 4.9; deterministic IDs verified.
- **Tests:** +53 (target was +15; 3.5× over-delivery).
- **KPI delta:** 0 NineS findings; insert-only diff (5 files, +740 / -0).

#### P-05 — Verification Ladder Formalization (commit `a893035`, PR #65, S02 W3 T01)
- **Scope:** New `gate/scorer.py::evaluate_ladder()` with 6 rungs R1..R6; earlier-rung failure short-circuits later rungs (R3 fail → R4-R6 skipped). New `LadderRung` enum + `LadderEvaluation` dataclass; `ladder_enabled: bool = False` profile field (STRICT/AUDIT default True).
- **Acceptance:** Closes Karpathy 4.10; ladder_enabled=False byte-identical to v7.8.0.
- **Tests:** +60 (target +25).
- **KPI delta:** 2nd opt-in primitive defaulting to OFF.

#### P-08 — L3 Behavioral Guidelines Injection (commits `ea782ef` + coupling `c354c28`, PR #66, S02 W3 T02)
- **Scope:** **First P6 schema bump** — `schemas/lean-dispatch.yaml#layout_invariant.canonical_order` 13 → **14** (appended `behavioral_guidelines` at position 14); version 2 → **3**. New `references/behavioral-guidelines.md` (190 lines, 9th canonical reference). Refactors `task_adaptive_selector.py::select_context` (cc 16→7) extracting `_select_behavioral_sections()` + `_compose_behavioral_block()`. `assert_dispatch_layout()` accepts both v2 and v3.
- **Acceptance:** Closes Karpathy 4.12 (function-scope only; line-scope deferred to v8.2.0 PV-04). Closes 1 NineS WARN.
- **Tests:** +51; SKILL.md net ±0 via 1-line compression + 1-line addition.
- **KPI delta:** SF-4 cascading 8→9 references via 2-commit feat+coupling pattern. **R7 line-anchored section registry coupling discovered as deferred Tier 3 row blocker** (largest emergent debt → closed by v8.2.0 PV-05).

#### P-12 — Abstractive Summariser Stage A (commit `9c71bbc`, PR #67, S02 W3 T03)
- **Scope:** `compressor.py::summarise_predecessor(mode='abstractive')` Stage A heuristic (no longer raises NotImplementedError); `_compute_information_density()` returning [0.0, 1.0]. Low-density input → ≤2-line summary; high-density → ≤5-line preserving named entities. Stage B (LLM-assisted) design captured in `.local/research/v8.0.0_p12_abstractive_stage_b_design.md` (12.5 KB, deferred to v8.2.0 PV-01).
- **Acceptance:** Closes Karpathy 4.6 Stage A. `complex_feature.summary_mode='abstractive'` in `context_profiles.yaml`.
- **Tests:** +25.
- **KPI delta:** Pattern set: ship deterministic Stage A in v8.0.0; LLM-assisted Stage B follows in v8.2.0 once data justifies the trade-off.

#### P-06 — Cycle Detection Middleware (commit `52598fc`, PR #68, S02 W3 T04)
- **Scope:** New `gate/cycle_detector.py` (474 lines) with 3 detection paths: `exact_match` / `fuzzy_match` (≥80% similar) / `edit_oscillation` (alternating A→B→A→B). `gate/reinforcement.py::cycle_to_instruction(report)` returns MUST-NOT `ReinforcementRule`. Extends `schemas/lean-report.yaml` with `cycle_detected` + `cycle_details` (lean-report has no canonical_order constraint).
- **Acceptance:** Closes Karpathy 4.4. cycle_detector=None byte-identical.
- **Tests:** +100 (target +30; longest commit-to-merge at ~23 min for 474-line module).
- **KPI delta:** 3rd opt-in primitive.

#### P-07 — Monotonic Ratchet Guarantee (commit `8c2ee61`, PR #69, S03 W4 T01)
- **Scope:** **G13 largest architectural gap closure.** New `gate/ratchet.py` (442 lines, **100% coverage**) with 4 verdict paths (ADVANCE / TOLERATE / ROLLBACK / ESCALATE). `compute_deterministic_oracle_score()` consults ONLY `test_results + lint_status + build_status` (excludes `review_findings` — S/O/R-resistant per Karpathy non-gameable success criteria). `ConvergenceRound.record_round()` hook. Refactors `apply_round_escalation` cc 11 → 2 (single most cc-improved function in cycle).
- **Acceptance:** Closes Karpathy 4.11 (G13). Closes NineS `[CC-448821-0001]`. Append-only `RatchetLogEntry` for forensic auditability.
- **Tests:** +85 (target +25; includes `TestOracleSORResistance` 4-test class).
- **KPI delta:** **Cycle's signature accomplishment** per retrospective §4.12. `convergence_noise_filter` scenario delta ~+1-2 pp (within tolerance).

#### P-09 — Overcomplexity Detector (commit `e89ca51`, PR #70, S03 W4 T02)
- **Scope:** New `gate/complexity_detector.py` (~720 lines) wrapping `nines analyze` subprocess with conservative MOCK fallback when binary unavailable (cycle's first subprocess wrapper pattern). WARNING (cc>10) / CRITICAL (cc>15 OR ERROR finding). 4 task complexity tiers (trivial/simple/standard/complex) with monotonic budgets. `complexity_weight` profile field (STRICT/AUDIT 0.10).
- **Acceptance:** Closes Karpathy 4.13.
- **Tests:** +74 (target +25; 3.0× over-delivery; includes 4 fallback-path tests).
- **KPI delta:** 4th opt-in primitive. **MOCK fallback became canonical pattern** for any subprocess-wrapped primitive (codified in v8.0.0 retro §4.6).

#### P-10 — Automatic AC Generator (commit `b042592`, PR #71, S04 W5 T01)
- **Scope:** **Second P6 schema bump** — canonical_order 14 → **15** (appended `acceptance_criteria_v2` at position 15); version 3 → **4**. New `ac_generator.py` (~561 lines) with 11 deterministic patterns ("fix bug X" → verification_type='test'; "improve performance" → metric+threshold). `score_quality()` 3-dimensional (completeness, testability, specificity). Preserves legacy `acceptance_criteria: list[str]` as deprecated alias (R5 backward-compat — 1500+ pre-cycle tests pass unchanged).
- **Acceptance:** Closes Karpathy 4.14 + derived C6. When `acceptance_criteria_v2` present, scorer auto-evaluates `verification_cmd` per criterion.
- **Tests:** +66 (target +25; coverage 91.25%).
- **KPI delta:** 5th opt-in primitive (`ac_generation.enabled=false` default). 6-test `TestR5LegacyAliasPreserved` + 3-test `TestR5LegacyByteIdenticalFallback` regression pin.

### v8.0.0 release roll-up

> "MAJOR — Harness engineering cycle: 13 patches landing layered context compression, deterministic gate primitives, behavioral guidelines injection, monotonic ratchet, and structured acceptance criteria... All 13 patches ACCEPT (100% accept rate)... 0 REJECT, 0 escalations to human, 0 reinforcement loops triggered. SI-3 release evaluation: composite **9.55/10** ≥ 8.5 minor threshold (+1.05 pp margin)... two additive P6 cache-layout invariant transitions (`schemas/lean-dispatch.yaml#layout_invariant.canonical_order` length 13 → 14 → 15; version 2 → 3 → 4)... v7.0.0 + v7.3.0 byte-baselines STILL PASS (additivity proven across THREE schema generations). The legacy `acceptance_criteria: list[str]` alias is preserved (R5 backward-compat) so the 1500+ pre-cycle tests continue passing unchanged. SKILL.md held at 495/500 throughout (5-line headroom maintained)." (CHANGELOG.md v8.0.0 §preamble)

---

## v8.1.0-rc.1 — Pre-release + v8.2.0 SI-1 Planning Gate (PRE-RELEASE, 2026-04-22)

**Cycle theme:** Self-analysis cycle output — no new feature code. Deliverables are entirely meta-cycle artifacts producing the v8.2.0 SI-1 contract.

### Per-PV summary (1 release commit, 5 self-analysis artifacts)

- **Release commit:** `release(v8.1.0-rc.1): pre-release + v8.2.0 SI-1 planning gate` (commit `658ffee`, PR #73 → merge `629cbe9`).
- **Version bump:** `__version__: 8.0.0 → 8.1.0-rc.1` (R8 confirmed working — `bump_version.py` accepts `-rc.1` suffix per `^\d+\.\d+\.\d+(-[\w.]+)?$`).
- **5 self-analysis artifacts:**
  - `.local/research/v8.1.0_nines_self_eval.json` (51 KB, composite **9.05/10** overall; weighted_overall 0.7044 dragged by `code_coverage: 0.0` artifact from nested `pytest --cov` timing out at 54s budget).
  - `.local/research/v8.1.0_nines_workflow_system.json` (24 KB; **mechanism_count = 8** in `workflow-system/agent/SKILL.md` post-P-08; closes the `[NineS:kp-find-3de75952]` 0-mechanism gap; surfaces warning `AI-24c4f48d-0002` "agent context overhead is high" with `total_agent_context_tokens = 46179`).
  - `benchmarks/devolaflow_context/baselines/v8.1.0_baseline.json` (13.6 KB; 36/36 PASS in 12.78s; 0 regression > 5pp; rebased from v8.0.0 with `rebased_from: v8.0.0` + `rebased_at: 2026-04-22`).
  - `tweet_analysis_harness_engineering_v7.8.md` §8 v3 refresh appended (1219 → 1316 lines).
  - `.local/research/v8.1.0_gap_analysis.md` (33.8 KB, 5-section; 18 candidates across A/B/C/D types; G2 PASS verdict).
- **Source v8.2.0 patch plan:** `.local/research/v8.2.0_patch_plan.md` (562 lines; 5 PVs × 8 mandatory fields + DAG + 10-risk register).
- **Annotated tag deferred:** `v8.1.0` tag deferred to L0 sign-off post-PR review per iteration plan §S07 W02 spec.

### KPI delta

- 0 new feature code; 5 candidates selected for v8.2.0 (PV-01..PV-05) from the 18-item pool; 13 benched to v8.2.x (B3 full, B2 full minus 2, A2/A3/A4, C2/C5, D2/D3/D4/D5).
- 0 v8.0.0 regressions detected; cycle is structurally additive vs v8.0.0 baseline.

---

## v8.2.0 — Karpathy Trilogy G3 Rollup (MINOR, 2026-04-22)

**Cycle theme:** 5 PVs (PV-01 → PV-05) closing Karpathy 4.6/4.7/4.8/4.12 + emergent R7 architectural debt. Driven by `.local/research/v8.2.0_patch_plan.md` (562-line second SI-1 planning gate).

### Cycle-level metrics (verbatim)

| Metric | Value |
|---|---|
| SI-3 composite | **9.42 / 10** (≥ G3 strict major 9.0; +0.42 pp margin) |
| NineS post-cycle composite | **0.9047 (≈ 9.047/10)** (≥ G3 0.90; +0.47 pp margin); capability_mean 0.9517 byte-stable; 1 ERROR + 10 WARN (regression vs v8.0.0's 0+6 — explainable as net-new infra) |
| Tests added | 2367 → ~2900+ (**+~533 net** across 5 PVs) |
| EvoBench scenarios | 36 → **43** (PV-01 + PV-05 added 2 new); 0 regression > 5pp; net composite delta vs v7.8.0 **+0.0072 pp** (ceiling effect) |
| P6 transitions | **0** (canonical_order length stable at 15, version stable at 4) |
| SKILL.md | **495 / 500** held throughout (5-line headroom never breached) |
| Cycle wall | ~10 hours (faster than v8.0.0's ~16 hours) |
| build-skill adapters | 11 / 11 PASS |
| Patch accept rate | **5 / 5 = 100%** (cycle-wide; trilogy 18/18) |

### Per-PV ledger (PV-01 → PV-05)

#### PV-01 — Abstractive Stage B LLM-Assisted (commit `08acde0`, PR #74, S08 W1 T01)
- **Scope:** Closes Karpathy 4.6 Stage B. `compressor.py::summarise_predecessor(mode='abstractive', llm_assist=True)` (no longer raises NotImplementedError). New `src/devolaflow/llm_client.py` (490 lines, MOCK provider default + 7 failure modes logged + Stage A fallback chain per S-5).
- **Acceptance:** New EvoBench scenario `abstractive_llm.yaml` (104 lines, composite ≥85). Default OFF preserves byte-identical v8.0.0 + v8.1.0 behavior; opt-in via `complex_feature.summary_mode='abstractive_llm'`.
- **Tests:** +35 (`test_compressor.py` + `test_llm_client.py`).
- **Micro-retro takeaway:** "7-mode fallback chain F1-F7 ensures Stage A always returned + kill switch — pattern from `.local/research/v8.0.0_p12_abstractive_stage_b_design.md` (260 lines READY) was preserved verbatim".
- **KPI delta:** Closes 4.6 Stage B (deferred from v8.0.0 P-12).

#### PV-02 — Agent Legibility Scoring (commit `c2680a1` + coupling `b0e4598`, PR #75, S08 W1 T02)
- **Scope:** Closes Karpathy 4.7. New `src/devolaflow/legibility/` package (627-line `scorer.py`); 3-dimensional scoring (naming consistency snake/Pascal + comment-to-code ratio + cyclomatic flow). `legibility_weight: 0.05` in STRICT/AUDIT (relaxed/standard 0.0). `pytest.importorskip` for radon-CI absence in coupling commit.
- **Acceptance:** Meta-quality discipline — PV-02 itself MUST score ≥ 0.85 on its own scorer (achieved).
- **Tests:** +67 (`test_legibility.py`).
- **Micro-retro takeaway:** Opt-in default-off; integrated with `gate/scorer.py` via `legibility_enabled` profile flag.
- **KPI delta:** 6th gate primitive (after token_budget_breaker / fence / verification_ladder / cycle_detector / ratchet / complexity_detector / ac_generator).

#### PV-04 — surgical_scope='line' Implementation (commit `0f532a8`, PR #76, S08 W1 T03)
- **Scope:** Extends v8.0.0 P-08 (function-scope only) to line-scope. New `_select_line_level_sections()` helper in `task_adaptive_selector.py`. `behavioral-guidelines.md` extended +43 lines with line-level criteria (Large tier ≤1000 line ceiling preserved at ~233 lines).
- **Acceptance:** `surgical_scope='function'` (default) byte-identical to v8.0.0 P-08 behavior.
- **Tests:** +8 (`test_behavioral_guidelines.py`).
- **Micro-retro takeaway:** Closes Karpathy 4.12 line-level (DEFERRED from v8.0.0 P-08 AC-2). Safest PV in cycle (additive content + R5 preservation).
- **KPI delta:** Completes BG-003 4-tier surgical_scope ladder.

#### PV-03 — Unified Session State Model (commit `f04fa26`, PR #77, S08 W2 T01)
- **Scope:** Closes Karpathy 4.8. New `src/devolaflow/session/` package (694-line `state.py`); `SessionState` unifies learnings + lifecycle + legibility shared state; `SessionStore` JSON persistence to `.local/memory/session_state.json`; `LegibilitySnapshot` for PV-02 integration. R5 backward-compat: `learnings.py` + `lifecycle/test_on_complete.py` refactored to use SessionState internally; existing public API preserved as deprecated aliases.
- **Acceptance:** **All 71+ existing learnings tests pass byte-identical** (verified per evaluation).
- **Tests:** +30 (`test_session_state.py`).
- **Micro-retro takeaway:** Opt-in via `session_state_enabled` profile flag (default off; default on for STRICT/AUDIT).
- **KPI delta:** 1 unified state model replaces 3 distributed holders. Highest-risk closure in cycle (R5 backward-compat scope).

#### PV-05 — Emergent R7 Section-Anchor Registry refactor + B3 partial + B2 partial (commit `cad2cb5`, PR #78, S08 W2 T02)
- **Scope:** Closes R7 (largest emergent architectural debt from v8.0.0 retrospective §3.3 — line-anchored section registry coupling). New `src/devolaflow/section_registry.py` (341 lines, `SectionAnchorRegistry`). `task_adaptive_selector.py` refactored to use symbolic anchors (backward-compat: missing anchors fall back to legacy line-based with `DeprecationWarning`). New EvoBench `r7_section_anchor_registry_no_drift.yaml` (107 lines, composite 91.0).
- **Acceptance:** **B3 partial** — 2 of 7 v8.0.0 opt-in primitives flipped default-on for STRICT (`legibility_enabled` + `cycle_detector_enabled`); 5 remaining → v8.2.x. **B2 partial** — 2 of 10 deferred EvoBench scenarios shipped (PV-01's + PV-05's).
- **Tests:** +20+ (`test_section_registry.py`).
- **Micro-retro takeaway:** SKILL.md unchanged at 495/500 (PV-05 unblocks future edits without changing line count). Section-anchor registry pattern: `reference_navigation: from "## References" to "## Templates"` so SKILL.md edits don't cascade through the line registry.
- **KPI delta:** Single largest NineS metric improvement of cycle: weighted_overall 0.7044 → ≥ 0.88 via A1 cov budget bump (deferred to v8.4.x).

### v8.2.0 release roll-up

> "MAJOR — Karpathy-primitives cycle: 5 forecasted PV patches close the v8.x trilogy with G3 strict gate PASS... PV-05 R7 refactor closes the largest emergent architectural debt from v8.0.0 retrospective (line-anchored section registry coupling) — opens future SKILL.md edit budget by removing line-number coupling. R5 backward-compatibility discipline maintained at the strongest level of the trilogy: PV-03 SessionState refactor unifies 3 previously-scattered domains (learnings + lifecycle + legibility) while preserving the existing `learnings.py` + `lifecycle/` API as deprecated aliases — all 71+ existing learnings tests pass byte-identical." (CHANGELOG.md v8.2.0 §preamble)

---

## v8.3.0 — Agent Workspace + Governance Trilogy (MINOR, 2026-04-23)

**Cycle theme:** 9 PVs (PV-01 → PV-09) closing v8.2.0 user feedback A1-A6 + PATCH (`feedback_for_v8.2.0.md`). Introduces `.local/.agent/{active,handoff,archive}/` scaffold + cache layout v4→v5 + REPORT.md surface + 22nd workflow template (`change-driven`). Driven by `.local/research/v8.3.0_gap_analysis.md` (18-item inventory: 4 critical + 8 high + 6 medium + 4 low; 18 in scope).

### Cycle-level metrics (verbatim)

| Metric | Value |
|---|---|
| SI-3 composite | **9.40 / 10** (≥ G2 minor 8.5; +0.90 pp; ≥ stretch 9.0; +0.40 pp) |
| NineS post-cycle composite | **0.9050 ≈ 9.05/10** (≥ G3 0.90; +0.50 pp; capability_mean 0.9517 byte-stable; hygiene_mean 0.7960 dragged by `code_coverage: 0.0` carry-over) |
| Tests added | 2367 → **3110** (+**743 net**; +31.4% over v8.2.0; **4.7× over forecast +159**) |
| EvoBench scenarios | 43 → **45** (PV-09 added `agent_workspace_active.yaml` composite 99.73 + `handoff_envelope_density.yaml` composite 99.73); 0 regression > 5pp |
| P6 transitions | 15 → **16** (version 4 → **5**; PV-05 appended `change_context` at position 16 byte-identical for positions 1-15) |
| SKILL.md | **498 / 500** (3 micro-compressions in PV-09 absorbed +30 lines via `change-driven` row + new "Agent Workspace" section) |
| SF-4 references | 9 → **10** (`agent-workspace.md` ~487 lines via PV-09) |
| Rules layer | 46 → **50** (Soul 7→9, Architecture 3→4, Conventions 8→9; new: S-8, S-9, A-4, C-9) |
| Cycle wall | ~2 days |
| Patch accept rate | **9 / 9 = 100%** (lifetime 45/45) |

### Per-PV ledger (PV-01 → PV-09)

#### PV-01 (v8.3.0-pv01) — Plugin runtime auto-install for nines + ui-pro (commit `dbf5433`, PR #80)
- **Scope:** Closes H-001. New `src/devolaflow/plugins/installer.py` (two-backend: `pip` for Python tools; `npm_then_init` for Node tools); `runtime-plugins.yaml` registry (schema_version 1) with `min_version` + `local_fallback_path` per plugin; auto-install on demand from `nines-assisted` + `product-verification` workflow precondition stages.
- **Acceptance:** Closes user-flagged PATCH from `feedback_for_v8.2.0.md`. Opt-out via `DEVOLAFLOW_AUTO_INSTALL=0`.
- **Tests:** +9 (`test_plugins.py`).
- **KPI delta:** First runtime-plugins surface; deferred items: `expected_sha256` per plugin (no signatures); strategy abstraction.

#### PV-02 (v8.3.0-pv02) — Rules layer foundation (commit `003544e`, PR #81)
- **Scope:** Closes H-002 + H-003. **4 new hard rules:** **S-8** "no writes outside `owned_files.txt` for L3 Task Agent inside change-driven workflow"; **S-9** "handoff envelopes are append-only — new info → new envelope with `seq+1`"; **A-4** "source-of-truth spec location ADR — `.local/memory/specs/<domain>/spec.md` is canonical; per-change `spec.md` carries DELTAS"; **C-9** "lightweight agent workspace artifact budgets" (per-file soft/hard ceilings: goal.md 200/400, acceptance.md 400/800, spec.md 1500/3000, tasks.md 800/1500, STATUS.yaml 100/200, owned_files.txt 50/100, handoff envelope 600/1200).
- **Acceptance:** `.cursor/rules/repo-governance.mdc` re-compiled; `AGENTS.md` regenerated. Forward-defined for v8.2.6 + v8.2.5 ArchiveManager.propose_merge.
- **Tests:** +3.
- **KPI delta:** Rules total 46 → 50.

#### PV-03 (v8.3.0-pv03) — `.local/.agent/` scaffold + repo-init extension + G-1/G-2 repair (commit `e89105f`, PR #82)
- **Scope:** Closes C-004 + M-001 + M-002. Extended `repo-init.yaml::canonical_manifest` from 5 → **8 paths** (added `.local/.agent/{active,handoff,archive}/`); `src/devolaflow/local/workspace.py::REQUIRED_DIRS` updated to match; idempotent re-scaffold via `make scaffold-agent` repairs G-1 (`.local/index.md` regen) + G-2 (`TRACKER.md` + `MEMORY.md` create-if-missing).
- **Acceptance:** Closes pre-existing G-1 (`.local/index.md` is out-of-date) + G-2 (`TRACKER.md` and `.local/memory/` missing despite `scaffold_local()` promising them).
- **Tests:** +5 (`test_local_workspace.py`).
- **KPI delta:** First materialization of agent workspace tree.

#### PV-04 (v8.3.0-pv04) — 10 agent-workspace YAML schemas + .gitignore Q-5 policy (commit `5d41773`, PR #83)
- **Scope:** Closes C-002 + M-003 + M-005 (schema half). New `schemas/agent-workspace/` directory with 10 YAML schemas (change.yaml, goal.yaml, acceptance.yaml, spec.yaml, tasks.yaml, status.yaml, owned-files.yaml, handoff-envelope.yaml, archive.yaml, report.yaml). `.gitignore` Q-5 policy: TRACKED = `.local/.agent/**` + `memory/specs/**`; UNTRACKED = `operational.jsonl` / `session_state.json` / `prefs.md` / `learnings.jsonl` / `plugin_install.log`.
- **Acceptance:** OpenSpec mature features (delta specs `## ADDED / ## MODIFIED / ## REMOVED Requirements`) introduced.
- **Tests:** +30 (`test_agent_workspace_schemas.py`).
- **KPI delta:** Schema-side completion of C-002.

#### PV-05 (v8.3.0-pv05) — `agent_workspace/` Python API + cache layout v4→v5 (P6) (commit `86f3513`, PR #84)
- **Scope:** Closes C-003 + M-005 (Python half) + M-006. New `src/devolaflow/agent_workspace/` package (~4277 lines): `Change`, `ChangeStore`, `HandoffEnvelope`, `HandoffStore` (append-only with monotonic `seq` counter per S-9), `ArchiveManager`, `lint`, `delta_parser`. **P6 transition v4→v5**: `change_context` appended at position 16 of `schemas/lean-dispatch.yaml#layout_invariant.canonical_order`; positions 1–15 byte-identical; `assert_dispatch_layout()` updated; v7.0.0 + v7.3.0 + v8.0.0 P-08 + P-10 byte-baselines continue passing.
- **Acceptance:** ~60 new tests across `test_agent_workspace.py` + `test_compressor.py::test_dispatch_layout_v5`.
- **Tests:** +60.
- **KPI delta:** **Largest single patch in cycle.** P6 chain: v=2 → v=3 → v=4 → v=5 (4 successive schema generations of additivity proven).

#### PV-06 (v8.3.0-pv06) — `change-driven` workflow template (commit `6bb83fa`, PR #85)
- **Scope:** Closes H-004. New `workflow-system/agent/templates/builtin/change-driven.yaml` (22nd template); 4-stage propose → apply ↔ verify → archive lifecycle (with `apply ↔ verify` convergence loop, max_rounds=5 per W-8). `templates/registry.yaml` updated. Introduced `_DEFERRED_DOC_TEMPLATES_V8_2_9` frozenset to bridge v8.2.6 → v8.2.9 SKILL doc surface coupling — frozenset acts as clear closure marker preventing silent drift.
- **Tests:** +10 (`test_change_driven_template.py`).
- **KPI delta:** 22nd workflow template; deferred frozenset closure pattern (codified per retro §4.1).

#### PV-07 (v8.3.0-pv07) — Auto-generated REPORT.md surface (commit `0059415`, PR #86)
- **Scope:** Closes H-005. New `src/devolaflow/agent_workspace/reporter.py` (4 report types: per-change archive, aggregate workspace, memory state, rules coverage); opt-in CLI `python -m devolaflow.agent_workspace.reporter --all` + new `make agent-reports` target. Idempotency tested (REPORT.md generation 2× → byte-identical).
- **Acceptance:** Auto-trigger from existing workflows DEFERRED per I-PV07-A (planned v8.4.0+).
- **Tests:** +15 (`test_reporter.py`).
- **KPI delta:** First human-readable report layer (per A4 user ask).

#### PV-08 (v8.3.0-pv08) — Memory bridge + change-aware learnings (commit `3572312`, PR #87)
- **Scope:** Closes H-006. **R5 strict**: existing **71/71** `tests/test_learnings.py` tests pass byte-identical (verified). Additive `change_id` parameter on `load_relevant_learnings()` and `capture_session_reflection()` — when omitted, byte-identical to v8.2.0; when set, learnings filtered/scoped to active change. New `src/devolaflow/agent_workspace/memory_bridge.py` (`consolidate_change_on_archive()`, `hydrate_change_context()`).
- **Tests:** +22 (`test_memory_bridge.py`).
- **KPI delta:** Per retro §4.5: "byte-counted" R5 discipline (X/X tests, not X+) becomes v8.4.0+ STATUS report convention.

#### PV-09 (v8.3.0-pv09) — SKILL + 10th reference + 2 EvoBench scenarios + frozenset closure (commit `12f4ea8`, PR #88)
- **Scope:** Closes H-007 + H-008. **SF-4 reference count 9 → 10** with new `references/agent-workspace.md` (~487 lines, within Large tier 1000 ceiling). SKILL.md gains "Workflow Selection" row for `change-driven` + new "Agent Workspace" section (~30 lines net add); 3 micro-compressions in pre-existing sections absorb the additions while holding 498/500. `_DEFERRED_DOC_TEMPLATES_V8_2_9` frozenset CLOSED. **+2 EvoBench scenarios:** `agent_workspace_active.yaml` (composite 99.73) + `handoff_envelope_density.yaml` (composite 99.73). Cascading coupling: `tests/test_no_ghost_features.py::_SF4_REFERENCE_SET` 9→10; `scripts/sync_cursor_skill.py::MIRRORED_FILES` 13→14; cursor adapter golden updated.
- **Acceptance:** **M-007 slash commands DEFERRED to v8.4.0+** per Q-3 RESOLVED. Wholesale baseline regen precedent established (`commit 12f4ea8`).
- **Tests:** +5 + adapter test updates.
- **KPI delta:** Largest cascading-coupling commit in cycle.

### v8.3.0 release roll-up

> "MINOR — Agent workspace + governance trilogy: 9 PV patches close v8.2.0 user feedback A1-A6 + PATCH... All 9 PVs ACCEPT (100% accept rate; v7.x trilogy 18/18 + v8.0.0 13/13 + v8.2.0 5/5 + v8.3.0 9/9 = **45/45 lifetime PV cycles**). SI-3 release evaluation composite **9.40/10**... NineS post-cycle composite **0.9050 ≈ 9.05/10** (real `nines self-eval` run, no manual fallback) ≥ G3 0.90 (margin +0.50 pp). SI-10 6/6 PASS at every patch (9 × 6/6 = 54 individual gate steps, all green) AND at the v8.3.0 final state. The cycle achieves **1 P6 cache-layout invariant transition** (v4→v5, exactly as planned in patch_plan §3 PV-05 only)... additivity proven across **FOUR successive schema generations** (13 → 14 → 15 → 16) plus 1 stable cycle (v8.2.0)." (CHANGELOG.md v8.3.0 §preamble)

---

## v8.3.1 — RTK Plugin Entry (PATCH, 2026-04-23, PV-01 of v8.4.0 cycle)

**Cycle position:** First published patch of the v8.4.0 RTK + memory router cycle (forecast 4 PVs + 1 minor rollup). Per user A3: "通过 8.3.x 系列分别迭代,验证,如果有提升则合入并发布,最终合并后作为 8.4.0".

### Per-PV summary

- **Commit:** `c4d4185`, PR #90, branch `feat/v8.3.1-rtk-plugin`. Closes **R-001** ("RTK is not installable from DevolaFlow").
- **File-level scope (verbatim):** `workflow-system/agent/knowledge/runtime-plugins.yaml` (schema 1 → 2, +rtk entry, +`curl_install_script` backend declaration, +`verify_distinguish_cmd` field); `src/devolaflow/plugins/installer.py` (+`_install_via_curl_script` + `_install_via_cargo` fallback + `_verify_distinguish` pre/post-install probe; `_SUPPORTED_BACKENDS` 2 → 3; `_SUPPORTED_SCHEMA_VERSIONS = {1, 2}`); `tests/test_plugins.py` (+14 new tests across 4 new classes — `TestRtkPluginRegistry` 6 / `TestRtkInstallSubprocess` 3 / `TestRtkSchemaV2` 2 / `TestRtkCurlScriptHelpers` 3); 7 canonical version-sync locations bumped 8.3.0 → 8.3.1.
- **Acceptance:** Mandatory `rtk gain` distinguish-from-rtk-type-kit probe per upstream INSTALL.md collision warning; cargo fallback ALWAYS pins `--git https://github.com/rtk-ai/rtk` (NEVER bare `cargo install rtk`). `min_version: "0.37.2"` (Cargo.toml canonical, NOT README's stale 0.28.2 per SI-2 §3 manual finding 7).
- **Per-PV gates:** SI-3 9.10/10; NineS 0.9050; SI-10 6/6 PASS. R5 strict — 97 pre-PV `test_plugins.py` tests pass byte-identical.
- **Tests:** +14 (cycle-wide test count 3110 → 3124).
- **KPI delta:** 3rd plugin entry (alongside `nines` + `ui-pro`); first `curl_install_script` backend.

---

## v8.3.2 — RTK Shell-Proxy + `pre_shell_call` hook (PATCH, 2026-04-23, PV-02 of v8.4.0 cycle)

### Per-PV summary

- **Commit:** `e83e56f`, PR #93, branch `feat/v8.3.2-shell-proxy`. Closes **R-002** ("Shell-tool calls injected into L3 context are uncompressed — verbose pytest/ruff/git diff blow L3 8K budget per A-3").
- **File-level scope (verbatim):** Created `src/devolaflow/shell_proxy/__init__.py` (49 lines); `src/devolaflow/shell_proxy/proxy.py` (320 lines `ShellProxy` + `ShellProxyConfig` + `_resolve_config` + `_probe_distinguish` + `proxy_command`); `src/devolaflow/shell_proxy/registry.py` (126 lines `WHITELIST` + `Tier` + `match_command` single-source-of-truth pattern mirroring RTK's `src/discover/registry.rs` per SI-2 §4.1); `src/devolaflow/lifecycle/pre_shell_call.py` (148 lines 5th canonical lifecycle hook with PSC001..PSC004 violation codes); `tests/test_shell_proxy.py` (430 lines, 22 tests); `tests/test_shell_proxy_disabled_is_noop.py` (103 lines, 4 tests R5 baseline); `tests/test_lifecycle_pre_shell_call.py` (96 lines, 6 tests); `benchmarks/devolaflow_context/scenarios/shell_proxy_disabled.yaml` (56 lines, composite 91.21 above floor 90). Modified `src/devolaflow/lifecycle/__init__.py` (+13 lines: register new event, export `pre_shell_call` + `PRE_SHELL_CALL_EVENT`); `tests/test_lifecycle_hooks.py::test_default_events_match_skill_md_table` (DEFAULT_EVENTS 4 → 5); `scripts/detect_dead_apis.py::DEFAULT_ALLOWLIST` (+1 entry `proxy_command`); 7 canonical version-sync locations bumped 8.3.1 → 8.3.2.
- **Acceptance:** Tier 1 whitelist = `pytest`, `ruff check`, `git diff`, `git log`, `git status` (the 5 commands the W-9 / SI-10 6-step gate exercises every PV); Tier 2 opt-in via `DEVOLAFLOW_RTK_PROXY_TIER2=1` = `git add`, `git commit`, `git show`, `cargo test`, `npm test`, `make`. R5 strict — 3107 pre-PV tests byte-identical when env-flag off. RTK README's stated ~80-90% token reduction per documented savings.
- **Per-PV gates:** SI-3 9.10/10; NineS 0.9050; SI-10 6/6 PASS. R5 strict triple codification (unit `test_shell_proxy_disabled_is_noop.py` + integration full pytest + EvoBench `shell_proxy_disabled.yaml`).
- **Tests:** +32 (cycle-wide 3107 → 3139).
- **KPI delta:** 5th canonical lifecycle hook (`DEFAULT_EVENTS` 4 → 5). First R5 strict triple codification at unit + integration + EvoBench layers.

---

## v8.3.3 — Fast-path Memory Router (PATCH, 2026-04-23, PV-03 of v8.4.0 cycle)

### Per-PV summary

- **Commit:** `a3f47b6`, PR #95, branch `feat/v8.3.3-memory-router`. Closes **M-001** ("L0 re-derives workflow + stage decomposition from SKILL.md every dispatch (no fast-path lookup); planning consumes ~3K tokens even for repeat patterns").
- **File-level scope (verbatim):** Created `src/devolaflow/memory_router/__init__.py` (72 lines); `src/devolaflow/memory_router/cache.py` (329 lines `MemoryCase` + `MemoryCacheError` + `is_ttl_expired` + `is_version_stale` + `build_case_from_dict` + `today_iso` + 3 TTL constants); `src/devolaflow/memory_router/router.py` (471 lines `MemoryRouter` + `MemoryRouterError` + `lookup_case` + `is_router_enabled` + `_IndexLoadResult` + 2 constants); `schemas/memory-case.yaml` (259 lines canonical schema documenting `index.yaml` multi-level routing keys + recipe markdown frontmatter contract + per-row TTL + 10 lifecycle invariants); `tests/test_memory_router.py` (792 lines, 40 tests); `benchmarks/devolaflow_context/scenarios/memory_router_fastpath.yaml` (73 lines, composite 99.73 — well above floor 90). Modified 7 canonical version-sync locations bumped 8.3.2 → 8.3.3; `benchmarks/devolaflow_context/baselines/v7.8.0_baseline.json` + `v8.3.0_baseline.json` (+1 entry each); NEW `benchmarks/devolaflow_context/baselines/v8.3.3_baseline.json` (47-scenario full coverage).
- **Acceptance:** Cache-hit short-circuits ~2800 tokens of planning context per matched dispatch ≈ **93% reduction** of planning-context block (well above gap analysis ≥ 30% target). `DEVOLAFLOW_MEMORY_ROUTER=1` activation. R5 strict zero-IO codified by `monkeypatch.setattr(Path, "read_text", watcher)` watcher.
- **Per-PV gates:** SI-3 9.10/10; NineS 0.9050; SI-10 6/6 PASS.
- **Tests:** +40 (cycle-wide 3139 → 3179; +10 over the +30 PV cap, "covers additional defensive validation tests for malformed inputs surfaced during implementation").
- **KPI delta:** First fast-path memory router; `MemoryCase` value type single-source-of-truth pattern.

---

## v8.3.4 — RTK-Pattern Command-Output Mapping (PATCH, 2026-04-23, PV-04 of v8.4.0 cycle)

### Per-PV summary

- **Commit:** `dcaa829`, PR #96, branch `feat/v8.3.4-commands-mapping`. Closes **M-002** ("将常见的处理命令按照 rtk 的仓库模式,在 memory 下进行映射和处理,并单独进行调用和路由,即除了原始 rtk 支持能力以外,还拓展了结合实际仓库中的新rtk 能力支持").
- **File-level scope (verbatim):** Created `src/devolaflow/shell_proxy/commands.py` (~600 lines `CommandMapping` + `CommandMappingError` + `FilterRule` + `apply_local_recipe` + `load_command_mappings` + `is_command_mapping_active` + `build_mapping_from_dict` + 6 helpers + 4 constants `DEFAULT_COMMANDS_DIR` / `DEFAULT_TTL_DAYS` / `MIN_TTL_DAYS` / `MAX_TTL_DAYS`); `schemas/command-mapping.yaml` (~280 lines canonical schema mirroring RTK's `[filters.<name>]` TOML schema with YAML for consistency); `tests/test_shell_proxy_commands.py` (~580 lines, 36 tests); `benchmarks/devolaflow_context/scenarios/command_mapping_density.yaml` (90 lines, composite 99.73). Modified `src/devolaflow/shell_proxy/__init__.py` (+re-exports for new public surface); `src/devolaflow/shell_proxy/proxy.py` (+`apply_recipe_to_output` method on `ShellProxy`; `wrap_command` byte-identical R5 strict); 7 canonical version-sync locations bumped 8.3.3 → 8.3.4; NEW `benchmarks/devolaflow_context/baselines/v8.3.4_baseline.json` (48-scenario full coverage WITH tiktoken hidden).
- **Acceptance:** **NO new env-flag — REUSES PV-02's `DEVOLAFLOW_RTK_PROXY` surface**; precedence chain: local recipe → RTK rewrite → passthrough. R5 strict zero-IO contract verified by `TestLoadR5StrictOff::test_env_off_does_not_touch_path_read_text`. Final per the D-001 = SPLIT decision.
- **Per-PV gates:** SI-3 9.10/10; NineS 0.9050; SI-10 6/6 PASS.
- **Tests:** +36 (cycle-wide 3179 → 3215; +21 over the +15 PV cap; cumulative cycle-wide 3110 → 3215 = +105 vs +100 cap, 5 over-cap documented in evaluation).
- **KPI delta:** 4th PV closure; D-001 SPLIT decision validated.

---

## v8.4.0 — RTK + Memory Router Rollup (MINOR, 2026-04-23)

**Cycle theme:** 4 PV patches (PV-01 v8.3.1 + PV-02 v8.3.2 + PV-03 v8.3.3 + PV-04 v8.3.4) close v8.3.0 user feedback A1+A2+A3 (`feedback_for_8.3.0.md`). Driven by `.local/research/v8.4.0_gap_analysis.md` (4-item in-scope inventory: R-001 + R-002 + M-001 + M-002; D-001 = SPLIT decision per §3).

### Cycle-level metrics (verbatim from CHANGELOG and v8.4.0_evaluation.md)

| Metric | Value |
|---|---|
| SI-3 release composite | **9.18 / 10** (≥ G2 minor 8.5; +0.68 pp; ≥ stretch 9.0; +0.18 pp) |
| NineS post-cycle composite | **0.9050 ≈ 9.05/10** (real `nines self-eval`, **byte-stable across 6 measurements** v8.3.0 → v8.3.{1,2,3,4} → v8.4.0); ≥ G3 0.85 (+0.55 pp) and ≥ 0.90 stretch (+0.05 pp) |
| Test count | 3110 → **3216** (+**106 net**; +6 over +100 cycle cap, documented transparently) |
| EvoBench scenarios | 45 → **48** (3 added: shell_proxy_disabled + memory_router_fastpath + command_mapping_density, all composite 91.99 / 99.73 above floor 90) |
| P6 transitions | **0** (length 16 / version 5 stable; `schemas/lean-dispatch.yaml#layout_invariant.canonical_order` byte-identical pre/post v8.4.0; 5 successive schema generations 13→14→15→16→16 + 1 stable cycle) |
| SKILL.md | 498 → **499 / 500** (+1 net via 1 micro-compression + 2 row additions: 1 to "Workflow Selection" for `shell-proxy` + 1 to "Tier 2 — Domain references" for `references/shell-proxy.md`) |
| SF-4 references | 10 → **11** (`references/shell-proxy.md` ~681 lines covering all 4 PVs in 10 sections) |
| Rules layer | **50 → 50** (UNCHANGED — Soul 9 / Architecture 4 / Conventions 9 / Workflow 15 / Style 13) |
| SI-10 gate steps | 4 × 6/6 = **24 individual gate steps + 6 at rollup = 30 green** |
| Patch accept rate | **4 / 4 = 100%** (lifetime **49/49 = 100%**) |
| Cycle wall | ~3 hours (per retrospective) |

### Rollup-only changes (this commit, v8.4.0 release)

- **NEW `workflow-system/agent/references/shell-proxy.md`** (~681 lines, within Large tier 1000 ceiling per SF-1) — 11th canonical reference covering all 4 PVs in one navigable artifact.
- **SKILL.md edit:** +1 row to "Workflow Selection" table for `shell-proxy` workflow; +1 row to "Tier 2 — Domain references" table for `references/shell-proxy.md`; -1 line via micro-compression combining `Composition operators` 2-line block into 1 line. Net **+1 line** (498 → **499/500**).
- **Cascading-coupling updates** (lockstep): `tests/test_no_ghost_features.py::_SF4_REFERENCE_SET` 10→11; `tests/test_version.py::_MIRRORED_SKILL_FILES` 14→15; `scripts/sync_cursor_skill.py::MIRRORED_FILES` 14→15; `tests/test_adapter_golden.py::test_cursor_references_golden` `len(actual) == 11`; `tests/test_reference_size_budgets.py::test_canonical_lists_match_sf3_contract` 10→11.
- **EvoBench rebase** (per v8.3.0 PV-09 commit `12f4ea8` precedent): regenerated `v7.8.0_baseline.json` (canonical V6 path) AND `v8.3.0_baseline.json` AND created `v8.4.0_baseline.json` (NEW; 48-scenario full coverage with `tiktoken` hidden).
- **Annotated `v8.4.0` git tag** pushed post-PR-merge per W-10 / CP-3.
- **Annotated rollup commit:** `a70c0f6` (PR #97).

### v8.4.0 release roll-up

> "**MINOR — RTK + memory router cycle: 4 PV patches close v8.3.0 user feedback A1+A2+A3** (`feedback_for_8.3.0.md`)... All 4 PVs ACCEPT (100% accept rate; v7.x trilogy 18/18 + v8.0.0 13/13 + v8.2.0 5/5 + v8.3.0 9/9 + v8.4.0 4/4 = **49/49 lifetime PV cycles, 100%**). SI-3 release evaluation composite **9.18/10** ≥ G2 minor threshold 8.5 (margin **+0.68 pp**) and ≥ stretch 9.0 (margin +0.18 pp). NineS post-cycle composite **0.9050 ≈ 9.05/10** (real `nines self-eval` run, **byte-stable across 6 measurements** v8.3.0 → v8.3.1 → v8.3.2 → v8.3.3 → v8.3.4 → v8.4.0)... The cycle achieves **0 P6 cache-layout invariant transitions** as planned... additivity proven across **FIVE successive schema generations** (13 → 14 → 15 → 16 → 16 stable for v8.4.0) plus 1 stable cycle. The rules layer is **UNCHANGED** at 50." (CHANGELOG.md v8.4.0 §preamble)

---

## v8 Era summary

**Executive summary:** The v8.x era was the **most architecturally productive era in DevolaFlow history**, shipping 31 PVs across 5 named cycles in ~70 hours wall-clock (v8.0.0 16h + v8.1.0-rc.1 ~1h + v8.2.0 10h + v8.3.0 ~48h + v8.3.1..v8.4.0 ~3h). All 31 PVs ACCEPT (100% accept rate; lifetime 49/49 incl. v7.x trilogy). The era closed all 14 Karpathy "ChatGPT Vibes Engineering" primitives (v8.0.0 P-08 P-10 P-12 + v8.2.0 PV-01 PV-02 PV-03 PV-04), introduced the `.local/.agent/{active,handoff,archive}/` workspace primitive (v8.3.0 PV-03..PV-09), proved P6 cache-layout additivity across 5 successive schema generations (13→14→15→16→16 stable), and established RTK + fast-path memory router as the v8.4.0 capstone. The rules layer grew 46 → 50 (Soul +2, Architecture +1, Conventions +1) entirely in v8.3.0; v8.4.0 deliberately added 0 rules. NineS composite stabilized at **0.9050 byte-stable across 6 measurements** in the v8.4.0 cycle — the strongest measurement-stability outcome in DevolaFlow history.

**Top 7 contributions ranked by impact:**

1. **G13 Monotonic Ratchet closure (v8.0.0 P-07)** — closes the largest architectural gap in the v7.8.0 baseline; 4-verdict state machine (ADVANCE / TOLERATE / ROLLBACK / ESCALATE) with S/O/R-resistant deterministic oracle.
2. **`.local/.agent/{active,handoff,archive}/` agent workspace (v8.3.0 PV-03..PV-09 + S-8/S-9/A-4/C-9 rules)** — first-class workspace primitive for change-driven workflow with append-only handoff envelopes and source-of-truth spec ADR.
3. **P6 cache-layout invariant additivity proof (v8.0.0 P-08 + P-10 + v8.3.0 PV-05)** — 4 transitions (13→14→15→16) preserved positions 1-N byte-identical; v7.0.0 + v7.3.0 byte-baselines all continued passing across the entire era.
4. **Karpathy 4-primitive series + S/O/R triad (v8.0.0 P-07 P-08 P-09 P-10 + v8.2.0 PV-01 PV-02 PV-03 PV-04)** — all 4 Karpathy CLAUDE.md guidelines (Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution) now have first-class enforced primitives.
5. **RTK plugin entry + shell-proxy + memory router + command mapping (v8.4.0 cycle PV-01..PV-04)** — single-source-of-truth registry pattern mirrored from RTK across `WHITELIST` + `MemoryCase` + `CommandMapping`; ~80-90% token reduction on whitelisted commands per RTK README.
6. **R5 strict triple codification at unit + integration + EvoBench layers (v8.4.0 cycle PV-02..PV-04)** — every opt-in feature has the R5 contract codified at THREE layers; `monkeypatch.setattr(Path, "read_text", watcher)` watchers prove zero-IO when env-flag off.
7. **22nd workflow template `change-driven` (v8.3.0 PV-06)** — OpenSpec-inspired propose → apply ↔ verify → archive lifecycle with `apply ↔ verify` convergence loop max_rounds=5 per W-8.

**Carry-forward items (verbatim from v8.4.0 CHANGELOG "Adoption notes" and v8.4.0 retrospective §3.2):**

> "**All 9 carry-forward items from the v8.3.0 retrospective §3.1 remain DEFERRED** per gap_analysis §2.2 dispositions: **M-007 slash commands** → v8.4.x+ or v8.5.0; **I-PV07-A REPORT.md auto-trigger** → v8.4.x+; **B3 partial (5 of 7 v8.0.0 opt-in primitives still default-OFF)** → v8.5.0+; **A1 NineS code_coverage timeout** + **A2 agent_overhead 46179 warning** + **A3 NineS index rebuild** + **A4 golden_test_set refresh** → v8.4.x sustaining (bundle); **M-004 full closure** (`ArchiveManager.propose_merge`) → v8.5.0+; **L-001..L-004** → v8.4.x+ or v8.5.0+. The R7 section-anchor registry partial-coverage debt (carried from v8.0.0 retro §3.3) was exposed by the v8.4.0 rollup SKILL.md edit but absorbed via baseline rebase; no NEW debt added."

Additional v8.4.0-observed deferrals (per retrospective §3.3):
- `runtime-plugins.yaml::expected_sha256` for RTK pinned `null` (deferred to post-release `SHA256SUMS` adoption per v8.2.1 PV-01 precedent).
- `command_mapping_savings` EvoBench scenario (showing measurable token savings) deferred to v8.4.x sustaining.
- Memory router cache invalidation beyond version_stamp + TTL (e.g., reference-counting against active changes per A-4 source-of-truth ADR) deferred to v8.5.0+.
- `shell_proxy/commands.py::apply_local_recipe` multi-pass filter chains (RTK supports filter composition per `filters.toml`) deferred to v8.4.x sustaining.

---

## Data gaps

- **Version without micro-retrospective:** v8.1.0-rc.1 has no per-PV micro-retro (intentional — pre-release with no feature code; gap analysis + nines self-eval + workflow_system scan substitute). v8.3.{1,2,3,4} have per-PV evaluations (`v8.3.X_evaluation.md` + `_nines.md`) but NOT 4-section micro-retros (per v8.4.0 retrospective convention — each PV's CHANGELOG entry serves as the verbatim per-PV record). v8.4.0 itself has SI-8 deferred to next session per CHANGELOG line 24.
- **Missing feedback files:** `feedback_for_v8.0.0.md` does NOT exist (v8.0.0 was driven by `tweet_analysis_harness_engineering_v7.8.md` directly, not user feedback); `feedback_for_v8.1.0.md` does NOT exist (pre-release, no user-facing surface); `feedback_for_v8.4.0.md` does NOT exist at session-end (v8.5.0 SI-1 to capture).
- **Versions.json backfill gap:** per v8.3.0 retro §4.8, `workflow-system/human/demo/version-timeline/versions.json` last entry is v7.2.0; intermediate v7.3.0 / v7.5.0 / v7.7.0 / v7.8.0 / v8.0.0 / v8.1.0-rc.1 / v8.2.0 / v8.3.0 / v8.4.0 are not all backfilled (release rollups append append-only).
- **Per-PV NineS post-evals limited to 6 cycle measurements:** the byte-stable 0.9050 spans v8.3.0 → v8.3.{1,2,3,4} → v8.4.0; v8.0.0 + v8.1.0 + v8.2.0 are separate measurements (not directly comparable to the 6-measurement chain).

---

## v9.0.0-relevant patterns

The following patterns from the v8 era are highly relevant to v9.0.0 design (ranked by likely v9.0.0 impact):

1. **Single-source-of-truth registry pattern (mirrored from RTK in v8.4.0 PV-02/03/04)** — `shell_proxy/registry.py::WHITELIST` + `memory_router/cache.py::MemoryCase` + `shell_proxy/commands.py::CommandMapping` all follow the same shape: one module owns the data structure, all consumers (proxy + hook + tests) delegate to it. Per v8.4.0 retro §4.1 #5: "consider documenting it as a 5th canonical Architecture Rule in v8.5.0 (provisionally A-5 — 'single-source-of-truth for whitelist + recipe registries')". **v9.0.0 should formalize as A-5.**

2. **R5 strict zero-overhead env-flag opt-in with triple-layer codification (v8.4.0 cycle PV-02..PV-04)** — every opt-in feature has the R5 contract codified at unit-test layer + integration layer + EvoBench layer; `monkeypatch.setattr(Path, "read_text", watcher)` watchers PROVE zero IO when env-flag off; existing pre-PV tests pass byte-identical when env-flag off. Per v8.4.0 retro §4.1 #4: "make this the default for all opt-in features in v8.5.0+". **v9.0.0 should adopt as default discipline for ALL new features.**

3. **P6 cache-layout invariant additivity (v8.0.0 P-08 P-10 + v8.3.0 PV-05)** — append-only edits at new positions; never reorder; v7.0.0 + v7.3.0 byte-baselines preserved across 5 successive schema generations. Per v8.3.0 retro §4.2: "the pattern is mature: append-only, never reorder, document at `schemas/lean-dispatch.yaml#layout_invariant.canonical_order`. v8.4.0+ should consider whether to formalize this as an enforcement rule (e.g., `assert_dispatch_layout_byte_identical_for_positions(payload, 1, 15)` on every dispatch)". **v9.0.0 should add explicit byte-identicality assertion + automated regression tests on v7.0.0 + v7.3.0 + v8.0.0 + v8.3.0 byte-baselines.**

4. **100% PV accept rate maintained (49/49 lifetime by v8.4.0)** — per v8.3.0 retro §4.7: "the plan-driven decomposition (SI-1 patch plan with 8 mandatory fields per patch) remains the structural reason". **v9.0.0 must preserve SI-1 8-mandatory-field per-PV decomposition discipline.**

5. **Real published patches > lightweight pvNN tags (v8.4.0 cycle precedent)** — per v8.4.0 retro §4.1 #2: "the v8.4.0 cycle's 'every PV is a real semver patch' cadence (per user A3) gave each PV a clean atomic surface (own CHANGELOG + own SI-3 + own NineS + own tag) that's easier to bisect than v8.3.0's lightweight-tag approach". Recommendation: "for v8.5.0+, default to real published patches when there are ≥ 3 PVs". **v9.0.0 should default to real published patches if cycle has ≥ 3 PVs.**

6. **Test cap discipline improved 4.7× → 1.06× (v8.4.0 vs v8.3.0)** — per v8.4.0 retro §4.1 #3: "explicit per-PV test caps in gap_analysis §4 + mid-cycle test-count audits at PV-02 / PV-03 / PV-04 STATUS reports" reduced over-delivery from 4.7× (v8.3.0 +159 forecast / +743 actual) to 1.06× (v8.4.0 +100 forecast / +106 actual). **v9.0.0 SI-1 should mandate per-PV test caps + mid-cycle audits.**

7. **Wholesale baseline regen at SKILL.md edits (v8.3.0 PV-09 commit `12f4ea8` precedent + v8.4.0 rollup application)** — per v8.4.0 retro §4.1 #6: "codify in CP-6 (or new W-16) — 'any SKILL.md edit triggers wholesale baseline regen, not just additive entries'". The two-view methodology (raw drift vs post-rebase) preserves the W-4 / SI-4 invariant transparently. **v9.0.0 should codify wholesale baseline regen as a new workflow rule.**

**Bonus pattern (v9.0.0 SKILL.md headroom risk):** v8.4.0 retro §4.2 #1: "SKILL.md headroom now at 1 line (499/500) — the v8.4.0 rollup +1 Workflow Selection row + +1 Tier 2 references row (offset by 1-line micro-compression) used the entire 2-line buffer. v8.5.0+ MUST either (a) Promote SKILL.md to a higher tier (SF-1 Large tier 1000), OR (b) Run a dedicated SKILL.md compression PV (likely the A2 carry-forward + R7 section-anchor migration), OR (c) Defer all SKILL.md surface changes to a single 'SKILL.md cycle' PV. Recommendation: schedule a v8.5.0 SKILL.md compression cycle as PV-01 to restore 5-10 line headroom before any other surface changes." **v9.0.0 SI-1 must address SKILL.md headroom as an architectural constraint, not a tactical issue.**

---

## Cross-references (per CO-2)

- v8.0.0 patch plan: `.local/research/v8.0.0_patch_plan.md` (862 lines, 13 patches × 8 mandatory fields + DAG + wave editing + risk register)
- v8.0.0 evaluation: `.local/research/v8.0.0_evaluation.md` (45 KB, composite 9.55/10)
- v8.0.0 retrospective: `.local/research/v8.0.0_retrospective.md` (50 KB, SI-8 4-section)
- v8.0.0 13 micro-retros: `.local/research/v8.0.0_p[01-13]_*_micro_retrospective.md`
- v8.0.0 EvoBench summary: `.local/research/v8.0.0_evobench_summary.md` (8 KB)
- v8.0.0 NineS post-cycle: `.local/research/v8.0.0_nines_post_cycle.{json,md}` (53 KB / 7.5 KB)
- v8.1.0 gap analysis: `.local/research/v8.1.0_gap_analysis.md` (33.8 KB, 5-section)
- v8.1.0 NineS self-eval: `.local/research/v8.1.0_nines_self_eval.json` (51 KB, composite 9.05/10)
- v8.1.0 NineS workflow-system: `.local/research/v8.1.0_nines_workflow_system.json` (24 KB, mechanism_count = 8)
- v8.2.0 patch plan: `.local/research/v8.2.0_patch_plan.md` (562 lines, 5 PVs × 8 mandatory fields + DAG + 10-risk register)
- v8.2.0 evaluation: `.local/research/v8.2.0_evaluation.md` (composite 9.42/10)
- v8.2.0 retrospective: `.local/research/v8.2.0_retrospective.md` (SI-8 3-cycle aggregate)
- v8.2.0 5 PV micro-retros: `.local/research/v8.2.0_pv0[1-5]_*_micro_retrospective.md`
- v8.3.0 gap analysis: `.local/research/v8.3.0_gap_analysis.md` (18-item inventory)
- v8.3.0 design: `.local/research/v8.3.0_design.md`
- v8.3.0 patch plan: `.local/research/v8.3.0_patch_plan.md` (10 increments × 8 mandatory fields)
- v8.3.0 evaluation: `.local/research/v8.3.0_evaluation.md` (composite 9.40/10)
- v8.3.0 retrospective: `.local/research/v8.3.0_retrospective.md` (SI-8 4-section)
- v8.3.0 OpenSpec deep analysis: `.local/research/v8.3.0_openspec_deep_analysis.md` (A5 deliverable; 28 KB)
- v8.3.{1,2,3,4} per-PV evaluations: `.local/research/v8.3.X_evaluation.md`
- v8.3.{1,2,3,4} per-PV NineS: `.local/research/v8.3.X_nines.{json,md}` (all composite 0.9050 byte-stable)
- v8.4.0 gap analysis: `.local/research/v8.4.0_gap_analysis.md` (4-item in-scope inventory; D-001 = SPLIT)
- v8.4.0 RTK NineS analysis: `.local/research/v8.4.0_rtk_nines_analysis.{json,md}` (~52 KB / ~31 KB; SI-2 deliverable)
- v8.4.0 evaluation: `.local/research/v8.4.0_evaluation.md` (composite 9.18/10)
- v8.4.0 retrospective: `.local/research/v8.4.0_retrospective.md` (SI-8 4-section)
- v8.4.0 EvoBench summary: `.local/research/v8.4.0_evobench_summary.md` (two-view methodology)
- v8.4.0 NineS post-cycle: `.local/research/v8.4.0_nines.{json,md}` (composite 0.9050)
- User feedback (v8.3.0 input): `.local/feedbacks/feedback_for_v8.2.0.md` (9 lines, A1-A6 + PATCH)
- User feedback (v8.4.0 input): `.local/feedbacks/feedback_for_8.3.0.md` (9 lines, A1+A2+A3)
- RTK canonical URL (per S-7): https://github.com/rtk-ai/rtk
- DevolaFlow canonical URL (per S-7): https://github.com/YoRHa-Agents/DevolaFlow
- NineS canonical URL (per S-7): https://github.com/YoRHa-Agents/NineS
