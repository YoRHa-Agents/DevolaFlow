# v9.0.0 PV-02 — Reference Cascade Plan (10 refs × hunk LOC)

> S02-T03 design artifact for PV-02 (`v8.4.2`) per
> `.local/research/v9.0.0_implementation_plan.md` §6.2.2 W01-T03.
> Maps each of the 10 reference findings (F-02 / F-03 / F-04 / F-07 / F-08 /
> F-09 / F-10 / F-11 / F-12 / F-13 / F-15) to the specific diff hunk it closes
> in the matching SF-4 reference body, with verbatim source pointers and
> projected LOC delta. Companion: `.local/research/adr/v9-ADR-002-cache-layout-governance-v2.md`.

## Verbatim source-of-truth pointers

| # | Source artifact | Anchor | Authoritative content |
|---|-----------------|--------|-----------------------|
| 1 | `schemas/lean-dispatch.yaml` | `layout_invariant.canonical_order` (lines 427-446) | 16 keys: `hdr → task → goal → assumptions → pred → files → rules → shared → accept → reinforce → verify_cfg → gate → repos → behavioral_guidelines → acceptance_criteria_v2 → change_context`; `version: 5` |
| 2 | `workflow-system/agent/templates/builtin/*.yaml` | 22 files in `ls` order | `change-driven, demo-showcase, dependency-setup, design-only, documentation-only, entropy-cleanup, feature-enhancement, full-pipeline, hotfix, migration, nines-assisted, onboarding, performance-optimization, product-verification, refactoring, repo-init, research-design-review-refine, research-only, security-audit, self-update, skill-optimization, spike-poc` |
| 3 | `src/devolaflow/memory_router/cache.py` | `MemoryCase` dataclass (lines 91-136) | already PV-01 verbatim — F-05 closure body parity |
| 4 | `schemas/command-mapping.yaml` | recipe_fields (lines 170-198) | `pattern` + `replacement` (NOT `note`) — F-06 closure body parity |
| 5 | `src/devolaflow/lifecycle/pre_shell_call.py` | PSC001..PSC004 violation codes | NEW §11 source for execution-protocol F-13 |
| 6 | `src/devolaflow/memory_router/router.py` | `lookup_case` + `lookup_case_strict` + `is_router_enabled` + `DEVOLAFLOW_MEMORY_ROUTER` | NEW §10 source for execution-protocol F-12 |
| 7 | `workflow-system/agent/templates/builtin/change-driven.yaml` | 4-stage propose → apply ↔ verify → archive lifecycle | NEW §12 source for execution-protocol F-11 |

## Hunk plan (10 refs)

### H-01 — `references/context-isolation.md` F-02 §10 + F-15 §15 renumber (~+5 LOC)

**Closes**: F-02 (BLOCKER) + F-15 (CRITICAL).

**Diff hunk 1** — §10 line 320-325 12-keys → 16-keys verbatim:

```diff
-DevolaFlow declares a single canonical top-level key order for every lean
-dispatch. The order is **additive**: each key may be absent, none may be
-reordered, and new top-level keys MUST be appended after `gate` (position 12).
-
-**Canonical order (12 keys):** `hdr` → `task` → `goal` → `assumptions` →
-`pred` → `files` → `rules` → `shared` → `accept` → `reinforce` (round 2+
-only) → `verify_cfg` → `gate`. Source of truth:
-`schemas/lean-dispatch.yaml#layout_invariant`.
+DevolaFlow declares a single canonical top-level key order for every lean
+dispatch. The order is **additive**: each key may be absent, none may be
+reordered, and new top-level keys MUST be appended after the last canonical
+slot (position 16 as of v8.3.0 PV-05). Positions 1-12 form the FROZEN PREFIX
+— reordering any of those 12 keys invalidates the LLM cache prefix and is
+a release blocker per `v9-ADR-002`. Positions 13-16 are append-only.
+
+**Canonical order (16 keys, `version: 5`):** `hdr` → `task` → `goal` →
+`assumptions` → `pred` → `files` → `rules` → `shared` → `accept` →
+`reinforce` (round 2+ only) → `verify_cfg` → `gate` → `repos` (v7.2.6 P-06)
+→ `behavioral_guidelines` (v8.0.0 P-08) → `acceptance_criteria_v2` (v8.0.0
+P-10) → `change_context` (v8.3.0 PV-05). Source of truth:
+`schemas/lean-dispatch.yaml#layout_invariant.canonical_order`.
```

**Diff hunk 2** — line 535 `## 15. Abstractive summariser ...` → `## 16. Abstractive summariser ...` (1-line renumber).

**Diff hunk 3** — §15 first table row line 510 `Freezes the 12-key top-level order` → `Freezes the 16-key top-level order (positions 1-12 frozen, 13-16 append-only)`.

LOC delta: +5 net (replace the 12-keys paragraph + renumber).

### H-02 — `references/message-schemas.md` F-03 §2/§3 rewrite + add §7/§8/§9 (~+71 LOC)

**Closes**: F-03 (BLOCKER).

**Diff hunk 1** — §2 (TaskDispatch Schema) preface rewrite to mandate lean form per CO-1:

> **Canonical lean form** is mandated per CO-1 (`AGENTS.md`). The
> verbose form below is preserved as a deprecated appendix (R5 backward
> compatibility); new dispatch authoring MUST emit the lean form
> documented in `schemas/lean-dispatch.yaml#lean_format_spec`.

Add NEW lean-form sub-section showing the 16-key canonical lean format.

**Diff hunk 2** — §3 (StatusReport Schema) parallel rewrite mandating lean form per `schemas/lean-report.yaml`.

**Diff hunk 3** — NEW §7 "Layout Invariant" (8 LOC) cross-referencing `schemas/lean-dispatch.yaml#layout_invariant` (16 keys, version 5).

**Diff hunk 4** — NEW §8 "Compression Rules" (10 LOC) referencing `schemas/lean-dispatch.yaml#compression_rules` + `BYPASS_CONDITIONS` + envelope policy.

**Diff hunk 5** — NEW §9 "Result Status" (8 LOC) cross-referencing `schemas/lean-report.yaml`.

LOC delta: +71 net (lean-form preface + 3 new sections + verbose appendix label).

### H-03 — `references/behavioral-guidelines.md` F-04 body extension (~+45 LOC)

**Closes**: F-04 body (CRITICAL — SKILL.md row added in PV-01; this PV adds the body extension).

**Diff hunk 1** — NEW "## S-8 Composition Rule" section (~12 LOC) describing how `surgical_scope` interacts with Soul Rule S-8 (No Writes Outside Active Change Owned Files).

**Diff hunk 2** — NEW "## Severity Matrix" section (~14 LOC) tabulating BG-001..BG-004 + LL-001..LL-005 against severity classification (warn/blocker) and on-violation behavior.

**Diff hunk 3** — NEW "## v8.2.x Primitive References" section (~19 LOC) cross-linking the per-version primitives that compose with behavioural rules: `surgical_scope='line'` (v8.2.0 PV-04), `select_context()` integration (v8.0.0 P-08), prompt-side enforcement contract (v8.4.4 PV-04 forward-defined).

LOC delta: +45 net.

### H-04 — `references/meta-framework.md` F-07 §4 alias table 12 → 22 (~+60-80 LOC)

**Closes**: F-07 (CRITICAL).

**Diff hunk 1** — §4 "Alias Mapping Table" extension. Current table covers ~12 templates; missing the 10 added since 2026-04-04: `change-driven`, `demo-showcase`, `dependency-setup`, `entropy-cleanup`, `feature-enhancement`, `nines-assisted`, `onboarding`, `product-verification`, `self-update`, `skill-optimization`. Each row adds the workflow-specific aliases mapped to primitives, with the workflow type cited verbatim from `templates/builtin/<name>.yaml`.

LOC delta: +60-80 net (10 new rows × ~7 lines each, conservative).

### H-05 — `references/decomposition-gate.md` F-08 NEW §5.5 7-primitive table (~+50 LOC)

**Closes**: F-08 (CRITICAL).

**Diff hunk 1** — NEW §5.5 "Gate Primitive Composition (v8.0.0+)" section enumerating the 7 v8.0.0 gate primitives:

| # | Module | Introduced version | Default state per profile | EvoBench scenario | Opt-in env-flag |
|---|--------|--------------------|---------------------------|-------------------|-----------------|
| 1 | `gate/budget.py::TokenBudgetBreaker` | v8.0.0 P-03 | OFF (opt-in standard/relaxed; ESCALATE on strict/audit) | `decomposition_feature` | `DEVOLAFLOW_TOKEN_BUDGET_BREAKER` |
| 2 | `gate/scorer.py::evaluate_ladder` | v8.0.0 P-05 | ON for decomposition profiles | `gate_ladder_score` | (default-on, no flag) |
| 3 | `gate/cycle_detector.py` | v8.0.0 P-06 | OFF (opt-in via `gate_cycle_detection`) | `cycle_detection_round_2` | `DEVOLAFLOW_CYCLE_DETECTOR` |
| 4 | `gate/ratchet.py` | v8.0.0 P-07 | OFF (opt-in via `gate_ratchet_enabled`) | `ratchet_round_3` | `DEVOLAFLOW_GATE_RATCHET` |
| 5 | `gate/complexity_detector.py` | v8.0.0 P-09 | OFF (opt-in; subprocess + MOCK fallback) | `complexity_subprocess` | `DEVOLAFLOW_COMPLEXITY_DETECTOR` |
| 6 | `ac_generator.py` | v8.0.0 P-10 | OFF (opt-in; companion to legacy `accept` list) | `ac_generator_density` | `DEVOLAFLOW_AC_GEN` |
| 7 | `legibility/scorer.py` | v8.2.0 PV-02 | OFF (opt-in via `decomposition.legibility_check`) | `legibility_score` | `DEVOLAFLOW_LEGIBILITY_CHECK` |

LOC delta: +50 net.

### H-06 — `references/team-roles.md` F-10 §7 11 → 22 templates (~+12 LOC)

**Closes**: F-10 (CRITICAL).

**Diff hunk 1** — §7 "Team Participation Matrix" extension to 22 rows covering ALL 22 templates verbatim from `workflow-system/agent/templates/builtin/`. Add the 11 missing: `feature-enhancement`, `demo-showcase`, `performance-optimization`, `dependency-setup`, `onboarding`, `skill-optimization`, `nines-assisted`, `self-update`, `repo-init`, `entropy-cleanup`, `change-driven`.

LOC delta: +12 net (11 new rows + 1-line legend).

### H-07 — `references/execution-protocol.md` F-11 + F-12 + F-13 NEW §10/§11/§12 (~+110 LOC)

**Closes**: F-11 + F-12 + F-13 (CRITICAL × 3).

**Diff hunk 1** — NEW §10 "Memory Router Fast-Path Lookup (v8.3.3+)" (~35 LOC):
- Activation surface: `lookup_case()` safe variant + `lookup_case_strict()` raising variant
- Env-flag: `DEVOLAFLOW_MEMORY_ROUTER=1` (R5 strict default-OFF)
- TTL constants: `DEFAULT_TTL_DAYS = 30`; per-route TTL override
- Invalidation predicates: `is_version_stale()` + `is_ttl_expired()` (anchor priority `last_accessed` → `last_updated`)
- Cache-miss-is-safe path: `MemoryRouterError` swallowed in `lookup_case()`; raised in `lookup_case_strict()`

**Diff hunk 2** — NEW §11 "Shell Proxy + Pre-Shell-Call Hook (v8.3.2+)" (~40 LOC):
- Activation: `DEVOLAFLOW_RTK_PROXY=1` env-flag
- 5th canonical lifecycle event: `pre_shell_call`
- 4 violation codes:
  - PSC001 — command not in WHITELIST
  - PSC002 — RTK rewrite fails (subprocess error)
  - PSC003 — destructive operation detected without `--force`
  - PSC004 — schema validation failure on local recipe
- All violations log + raise `PreShellCallError`; downstream cache-miss-safe

**Diff hunk 3** — NEW §12 "Change-Driven Workflow Envelope Lifecycle (v8.3.0+)" (~35 LOC):
- 22nd template `change-driven.yaml` (v8.3.0 PV-06 / commit `6bb83fa`)
- 4-stage lifecycle: `propose → apply ↔ verify → archive`
- `apply ↔ verify` convergence loop with `max_rounds=5` per W-8
- Append-only handoff envelopes per Rule S-9 (`<from>__<to>__<change-id>__<seq>.yaml`)
- File-ownership constraint per Rule S-8 (writes confined to `owned_files.txt` + change folder + own handoff outbox)

LOC delta: +110 net.

### H-08 — `references/agent-workspace.md` cross-refs + envelope policy (~+11 LOC)

**Closes**: residual cross-link drift.

**Diff hunk 1** — Add 4 cross-refs:
- v8.4.0 RTK + memory-router stack cross-link (1 LOC each → §1 trigger table)
- envelope policy clarification: "envelopes are append-only — see Rule S-9; write once per `seq`; never delete"
- backref to `references/execution-protocol.md §12` (just-added)

LOC delta: +11 net.

### H-09 — `references/shell-proxy.md` frontmatter only (~+0 LOC)

**Closes**: F-09 frontmatter portion (already at 2026-04-23 from PV-01 — verified no-op).

LOC delta: 0 (no edit needed; frontmatter already current).

### H-10 — 11 frontmatter `last_updated: "2026-04-23"` bumps (~+11 LOC, 1-line/file)

**Closes**: F-09 short-term (CRITICAL — long-term CI guard ships in T12).

Files needing bump (11 files; 3 already at 2026-04-23 — verified pre-flight):

| Reference | Current `last_updated` | Action |
|-----------|-------------------------|--------|
| `agent-hierarchy.md` | `2026-04-04` | bump → `2026-04-23` |
| `agent-workspace.md` | `2026-04-23` | (no-op) |
| `behavioral-guidelines.md` | `2026-04-21` | bump → `2026-04-23` |
| `context-isolation.md` | `2026-04-04` | bump → `2026-04-23` |
| `decomposition-gate.md` | `2026-04-04` | bump → `2026-04-23` |
| `execution-protocol.md` | `2026-04-04` | bump → `2026-04-23` |
| `message-schemas.md` | `2026-04-04` | bump → `2026-04-23` |
| `meta-framework.md` | `2026-04-04` | bump → `2026-04-23` |
| `plan-mode-enforcement.md` | `2026-04-23` | (no-op) |
| `repo-modes.md` | `2026-04-04` | bump → `2026-04-23` |
| `shell-proxy.md` | `2026-04-23` | (no-op) |
| `team-roles.md` | `2026-04-04` | bump → `2026-04-23` |

Net: 9 frontmatter bumps + 3 already-current = 12 files at `2026-04-23` post-PV-02.

> **Note for verification harness**: the playbook §6.2.4 expected
> `rg "2026-04-23" workflow-system/agent/references/*.md` = 11 hits;
> after PV-01 added `plan-mode-enforcement.md` as the 12th SF-4 ref the
> count drifts to 12. The PV-02 freshness CI guard
> (`tests/test_reference_frontmatter_freshness.py`) is the
> source-of-truth for the 90-day window; the grep number is informational.

LOC delta: +9 (only files that actually change get a +1 LOC delta on the `last_updated:` line).

## Cumulative LOC summary

| # | Hunk | Closes | LOC delta |
|---|------|--------|-----------|
| H-01 | `context-isolation.md` §10 + §15→§16 | F-02, F-15 | +5 |
| H-02 | `message-schemas.md` §2/§3 + new §7/§8/§9 | F-03 | +71 |
| H-03 | `behavioral-guidelines.md` body extension | F-04 body | +45 |
| H-04 | `meta-framework.md` §4 alias 12→22 | F-07 | +60-80 |
| H-05 | `decomposition-gate.md` NEW §5.5 | F-08 | +50 |
| H-06 | `team-roles.md` §7 11→22 | F-10 | +12 |
| H-07 | `execution-protocol.md` NEW §10/§11/§12 | F-11, F-12, F-13 | +110 |
| H-08 | `agent-workspace.md` cross-refs | (residual) | +11 |
| H-09 | `shell-proxy.md` (no-op) | F-09 portion | 0 |
| H-10 | 11 frontmatter bumps | F-09 short-term | +9 |

**Total reference body LOC delta**: ~+373-393 (consistent with `v9.0.0_reference_review.md` projected +535 across 10 refs minus the F-04 body partial that lands here vs. PV-01 SKILL row addition; minus the PV-08 deferred `repo-modes.md` +24).

## Verification harness (per playbook §6.2.4)

```bash
# Verbatim greps
rg "16 keys" workflow-system/agent/references/context-isolation.md   # >= 1
rg "12 keys" workflow-system/agent/references/context-isolation.md   # 0
rg "lean format" workflow-system/agent/references/message-schemas.md # >= 1
rg "2026-04-23" workflow-system/agent/references/*.md                # 12 (was 11 in playbook; updated for PV-01 +1 ref)
rg "nest-vs-append" .cursor/rules/repo-governance.mdc AGENTS.md      # >= 1 each

# CI tests
pytest tests/test_layout_invariant_multi_baseline.py -v
pytest tests/test_reference_frontmatter_freshness.py -v
pytest tests/test_reference_api_alignment.py -v
pytest tests/test_no_ghost_features.py::test_reference_skill_md_tier2_parity -v
pytest tests/test_compressor.py -v  # I-8 invariant guard (NO drift expected)
```

## Coupling notes

- All 10 ref edits ride in the same PR per playbook §6.2.7 (single-PR coordinated cascade).
- H-09 is a no-op in this PV; if shell-proxy frontmatter date drifts pre-merge it gets pulled in; otherwise the reference stays at PV-01 state.
- H-04 + H-06 use the SAME 22-template ground-truth list (sourced from `ls workflow-system/agent/templates/builtin/`); editing both in the same PV avoids cross-PV consistency drift.
- The new §10/§11/§12 in `references/execution-protocol.md` couple loosely with `references/shell-proxy.md` §3-§6 (already current); cross-link is added in H-07.
- `repo-modes.md` is INTENTIONALLY out of scope for PV-02 (PV-08 sustaining bundle).
- F-01/F-05/F-06/F-14 are PV-01 closures; this plan does NOT re-touch them.

---

*End of v9.0.0 PV-02 reference cascade plan. 10 hunks, 9 ref bumps, ~+373-393
net LOC. All hunks scoped to single files; no cross-file coupling beyond the
shared 22-template / 16-key source-of-truth lists. Companion design artifact:
`.local/research/adr/v9-ADR-002-cache-layout-governance-v2.md` (drafted in
S02-T04, completed in S05-T16).*
