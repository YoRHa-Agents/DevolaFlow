# v10.2.0 Gap Analysis (SI-1)

> **Status:** PV-01 research draft. W-1 / SI-1 planning gate.
> **Author:** L1 Research Stage Agent (composer-2-fast).
> **Date:** 2026-05-03.
> **Scope:** Pre-implementation gap enumeration for the v10.2.0 cycle.
>   No production writes; this artifact gates PV-02 onward.
> **Companion:** `.local/research/v10.2.0_cycle_plan.md` (PV-by-PV plan).
> **External tools (S-7):** DevolaFlow `https://github.com/YoRHa-Agents/DevolaFlow`,
>   NineS `https://github.com/YoRHa-Agents/NineS`,
>   Si-Chip `https://github.com/YoRHa-Agents/Si-Chip`.

## §1 — User Mandate (verbatim)

From `.local/feedbacks/feedback_for_v10.2.0.md` (5 lines, Mandarin):

> 对 plugin 模式和当前的 plugin 功能进行深度 review，验证其自动安装和天级别自动更新的有效性
> 正式集成 si-chip
> 然后对自身仓库结合 nines 进行深入分析，验证 si-chip 迭代有效性
> 然后进行多轮次自迭代与优化（每次bump 一个 patch）
> 更新 pr 并 bump minor 版本到 10.3.0

Working English translation (the Mandarin above remains the source of truth):

> 1. Deep-review plugin mode and current plugin functionality; verify auto-install and daily auto-upgrade effectiveness.
> 2. Formally integrate Si-Chip.
> 3. NineS deep-analyse the self-repo; validate Si-Chip iteration effectiveness.
> 4. Multi-round self-iteration & optimisation (one patch bump per round).
> 5. Update PR and bump MINOR to v10.3.0.

## §2 — Current State Snapshot (post-v10.1.0)

| Surface | State at v10.1.0 close | Verbatim evidence |
|---|---|---|
| Plugins registered | 4 (`nines`, `ui-pro`, `rtk`, `si-chip`) | `workflow-system/agent/knowledge/runtime-plugins.yaml` schema_version `3` |
| Lifecycle hooks | 10 (positions 1-8 frozen since v9.1.3; 9=`pre_plugin_invocation` v9.4.0; 10=`post_skill_edit` v9.5.0) | `tests/test_pre_plugin_invocation.py::test_event_in_default_events` |
| Plugin auto-install env flag | `DEVOLAFLOW_AUTO_INSTALL_PLUGINS=1` (R5 strict default-OFF) | `src/devolaflow/lifecycle/pre_plugin_invocation.py::ENV_FLAG` |
| Si-Chip DEEP env flag | `DEVOLAFLOW_SI_CHIP_DEEP=1` (R5 strict default-OFF) | `src/devolaflow/lifecycle/post_skill_edit.py::ENV_FLAG` |
| Si-Chip bridge LOC | ~1070 across 4 sub-modules (`__init__`, `install_resolver`, `models`, `runner`) | `src/devolaflow/si_chip_bridge/` |
| Plugin upgrade cadence | 24h declared in `defaults.upgrade_check_frequency_hours`; CLI surface `devolaflow plugins refresh [--force]` | `src/devolaflow/plugins/installer.py::_DEFAULT_UPGRADE_CHECK_FREQUENCY_HOURS = 24` |
| v9.5.0 dogfood verdict | DEFER — no `iteration_delta` computable (no with-ability/no-ability eval data) | `.local/feedbacks/v9.5.0_sichip_deferred.md` §1 |
| C-4 line budgets | PASS for all 14 mirror-tracked files | `tests/test_integration.py::test_skill_md_under_500_lines` + `tests/test_reference_size_budgets.py` |
| Si-Chip `count_tokens` budgets | FAIL on 18/18 skill-corpus files (default meta ≤100 / body ≤5000 too tight for DevolaFlow's tiered C-4 regime) | `.local/research/v9.5.0_devola_count_tokens.json` |
| Soul-set count | 10 (W-21 freeze; v10.0.0 retrospective §3.5 telegraphs S-11 "Parallel Wave Dispatch Invariant" as candidate for v10.2.0 SI-1) | `.cursor/rules/repo-governance.mdc` §W-21 |
| Last cycle PR posture | v10.0.0 MAJOR rollup PR #114 + v10.1.0 PR #115 both merged. v10.2.0 has no open PR yet. | `git log --oneline -20` |

## §3 — Gap Enumeration (priority-ranked, file-level scope)

Severity legend (matches `tests/test_no_ghost_features.py::test_severity_lattice`):
**BLOCKER** — must close before MINOR ships;
**CRITICAL** — close in PV scope or escalate;
**MAJOR** — strongly recommend in cycle;
**MINOR** — defer to v10.3.x or v10.4.0.

### §3.1 — D-P-* — Plugin infrastructure deep review

| ID | Sev | Description | Evidence | Fix scope | Effort |
|---|---|---|---|---|---|
| **D-P-1** | **BLOCKER** | `refresh_all` has NEVER been exercised end-to-end against the live registry. Every call path in `tests/test_plugin_upgrade.py` mocks `subprocess.run` or stubs `upgrade_plugin`. `RefreshOutcome(action="upgraded")` never observed against a real plugin install. | `tests/test_plugin_upgrade.py` (~608 lines, all mocked) | NEW `tests/test_plugin_refresh_e2e.py` invokes `refresh_all(force=True, only=["nines"])` against the actual `nines` pip backend. Skip-if-CI-offline guard. | M |
| **D-P-2** | **BLOCKER** | The 24h staleness gate is computed but no scheduler fires `refresh_all` automatically on session start or per-dispatch. Operators must invoke `devolaflow plugins refresh` manually. The user mandate "天级别自动更新" (daily auto-update) is therefore delivered only as a CLI verb, not as runtime automation. | `src/devolaflow/lifecycle/pre_plugin_invocation.py` calls `ensure_plugin` (install path) but never `refresh_all` (upgrade path). No session-start hook. | EXTEND `pre_plugin_invocation` with an opt-in staleness probe OR add a session-start lifecycle slot that runs `refresh_all(force=False)` when `DEVOLAFLOW_AUTO_INSTALL_PLUGINS=1`. W-20 §3 reuse-first applies. | M |
| **D-P-3** | **CRITICAL** | The si-chip `version_check_cmd` is a heuristic shell expression (`test -f $HOME/.cursor/skills/si-chip/SKILL.md \|\| test -f $HOME/.cursor/skills/si-chip/si-chip/SKILL.md && echo si-chip/0.4.0`) that always emits a hard-coded "0.4.0" string regardless of the actual installed version. `_meets_min` cannot detect drift when v0.4.1+ ships. | `workflow-system/agent/knowledge/runtime-plugins.yaml` lines 124-130 | REPLACE with a real probe that reads `~/.cursor/skills/si-chip/[si-chip/]SKILL.md` frontmatter `version:` field. New helper in `si_chip_bridge.install_resolver`. | S |
| **D-P-4** | **CRITICAL** | No drift smoke test. v9.4.0 PV-04 wired schema v3 + 4 plugins; v10.1.0 added zero plugin work. No CI step confirms the 4 registered plugins still resolve cleanly via `resolve_plugin` + `_probe_version`. | No `tests/test_runtime_plugins_smoke.py` exists. | NEW smoke test that walks `load_registry()["plugins"]` and asserts `resolve_plugin(p["id"], registry)` succeeds for every entry. | S |
| **D-P-5** | **MAJOR** | `defaults.upgrade_check_frequency_hours: 24` is hard-coded; no env-var override. Operators on slow networks (Si-Chip recon timed out at 45s in v9.5.0 PV-01) cannot loosen the cadence without forking the YAML. | `src/devolaflow/plugins/installer.py::_load_defaults` | REUSE `DEVOLAFLOW_AUTO_INSTALL_PLUGINS` activation surface; add an optional `DEVOLAFLOW_UPGRADE_FREQUENCY_HOURS` integer override per W-20 §3 orthogonality test (NEW flag candidate; document inline). | S |
| **D-P-6** | **MAJOR** | `is_plugin_stale` returns `True` when `read_last_checked` returns `None` (never-installed → "stale immediately"). Combined with `refresh_all(force=False)` on a fresh clone, this triggers a forced upgrade attempt for every plugin even when the operator only wanted to refresh ONE. The CLI is robust per `RefreshOutcome(action="failed")` aggregation, but the network-traffic shape is unexpected. | `src/devolaflow/plugins/installer.py::is_plugin_stale` lines 1212-1233 | DOCUMENT in `references/env-flags.md` §2 + add `tests/test_plugin_refresh_first_run.py` pinning the never-installed branch. | S |
| **D-P-7** | **MINOR** | The v9.5.0 deferred §4 D-1 upstream Si-Chip installer defect (post-install path mismatch — files land at `<target>/si-chip/<name>` instead of `<target>/<name>`) has no GitHub issue filed at https://github.com/YoRHa-Agents/Si-Chip/issues. Workaround in `install_resolver.py` masks the symptom forever. | `.local/feedbacks/v9.5.0_sichip_deferred.md` §4.D-1 | File upstream issue. NOT a DevolaFlow code change. | S |

### §3.2 — D-S-* — Si-Chip formal integration (the deferred backlog)

| ID | Sev | Description | Evidence | Fix scope | Effort |
|---|---|---|---|---|---|
| **D-S-1** | **BLOCKER** | The v9.5.0 §3 OA-1 blocker is unresolved: no with-ability + no-ability eval run-output dirs exist. Without them, `aggregate_eval.py` cannot run and `iteration_delta` is non-computable. Every dogfood pass to date emits `verdict: DEFER` with note "evaluate: skipped — runs_dir/baseline_dir not supplied". | `src/devolaflow/si_chip_bridge/runner.py::run_dogfood_cycle` lines 544-555 | Generate eval data via Si-Chip's `<install>/scripts/real_llm_runner_quickstart.md` recipe OR substitute NineS self-eval JSON as the eval seed (D-N-1). | L |
| **D-S-2** | **BLOCKER** | `run_dogfood_cycle` API is reachable ONLY via the `post_skill_edit` lifecycle hook (gated by `DEVOLAFLOW_SI_CHIP_DEEP=1`). L0/L1 dispatchers cannot invoke it programmatically during a workflow stage; templates `skill-optimization` + `self-update` declare `si-chip` in `invoked_by_workflows` but no Python entry point invokes the dogfood orchestrator. | `src/devolaflow/feedback.py::generate_round_dispatch` does not import `si_chip_bridge`. `Grep` for `run_dogfood_cycle` returns 2 hits: definition + lifecycle hook. | NEW thin wrapper in `feedback.py` (or a dedicated `dispatch_dogfood_cycle` module) called when `dispatch.workflow == "skill-optimization"` AND `DEVOLAFLOW_SI_CHIP_DEEP=1`. P1 dispatcher-not-implementer preserved (the wrapper enqueues; an L3 task agent runs the actual cycle). | M |
| **D-S-3** | **CRITICAL** | `iteration_delta ≥ +0.10` gate is enforced at runtime via `apply_or_defer` but NOT in CI. The W-9 SI-10 6-gate sequence does not include a Si-Chip iteration-delta regression check. A future PV could regress the `MetricsReport.composite` calculation and CI would not catch it. | `tests/test_benchmarks.py` covers EvoBench scenarios; no test asserts `apply_or_defer(IterationDeltaReport(..., iteration_delta=0.099, threshold=0.10)) == ApplyVerdict.DEFER`. | NEW `tests/test_sichip_iteration_delta_gate.py` with 4 fixtures: above-threshold APPLY, exact-threshold APPLY (epsilon), below-threshold DEFER, zero-delta DEFER. Wire into `Makefile::release-preflight`. | M |
| **D-S-4** | **CRITICAL** | The 18-file count_tokens budget violations from v9.5.0 PV-05 (every skill-corpus file FAILs Si-Chip default budgets) remain unaddressed. v10.1.0 added writing-style transforms but those operate on `human/` + `README` — they do NOT touch `workflow-system/agent/`. Critical files (env-flags.md 9377 body, shell-proxy.md 8985 body, agent-workspace.md 8205 body) still over-budget. | `.local/feedbacks/v9.5.0_sichip_deferred.md` §1.1-1.2 | This is the OA-2..OA-4 action plan. Each shrink is gated by Si-Chip `iteration_delta ≥ +0.10` per the deferred-changes contract. PV-04 + PV-05 self-iteration rounds in this cycle target the top offenders. | L |
| **D-S-5** | **MAJOR** | `post_skill_edit` writes a `sichip_deferred_<timestamp>.md` doc on every DEFER verdict. With DEEP integration always-on (`DEVOLAFLOW_SI_CHIP_DEEP=1`), every skill-corpus commit produces a new feedback doc. No housekeeping rotates / compacts / archives them. `.local/feedbacks/` could fill with low-information DEFER timestamps. | `src/devolaflow/lifecycle/post_skill_edit.py::_write_feedback_doc` | Add a deduplication probe: if the prior DEFER doc carries the same `notes` + `skill_files` + `verdict`, skip the new write (idempotent). NEW test asserts behaviour. | S |
| **D-S-6** | **MAJOR** | The bridge module's `run_dogfood_cycle` defaults `work_dir = Path.cwd() / ".local" / "dogfood" / "v9.5.0"`. The `v9.5.0` literal pinned the path at v9.5.0 PV-02 and never updated. Cycle-level outputs co-mingle in a directory named after a previous version. | `src/devolaflow/si_chip_bridge/runner.py::run_dogfood_cycle` line 533 | REPLACE with `work_dir = Path.cwd() / ".local" / "dogfood" / __version__` so the path tracks `__version__` automatically. | S |
| **D-S-7** | **MINOR** | The bridge does not expose a `--json` flag analogous to `aggregate_eval.py --json`. Operators tailing the bridge during a dogfood pass see human-readable logs only. | `src/devolaflow/si_chip_bridge/runner.py` | OPTIONAL — convert the `notes: list[str]` to a structured `notes: list[dict]` and add `SiChipResult.to_json()`. | S |

### §3.3 — D-N-* — NineS-driven self-analysis surface

| ID | Sev | Description | Evidence | Fix scope | Effort |
|---|---|---|---|---|---|
| **D-N-1** | **CRITICAL** | Whether NineS self-eval data can serve as the with-ability/no-ability eval evidence Si-Chip needs is the **load-bearing question** for D-S-1. NineS V3.3.0's `self-eval` produces 20 capability + 5 hygiene scores; Si-Chip's `aggregate_eval.py` consumes per-task with-ability vs no-ability run output dirs (different shape). The two formats may or may not be reconcilable via an adapter. | `docs/cycle-archive/v10.0.0/nines/v10.0.0_nines.md` (NineS shape) vs `.local/feedbacks/v9.5.0_sichip_deferred.md` §3 OA-1 (Si-Chip shape) | NEW research probe in PV-03: prototype `scripts/nines_to_sichip_eval_adapter.py` that converts NineS golden-task per-sample scores into Si-Chip `runs-dir`/`baseline-dir` layout. APPROVE / REJECT decision blocks D-S-1 closure. | M |
| **D-N-2** | **MAJOR** | The NineS A1 ticket (`code_coverage` collector times out under the 180s `nines.toml` budget) has been open since v8.4.4. v10.0.0 retrospective §3.3 documents the manual fallback (`pytest --cov=devolaflow`). Each cycle close burns ~10 min of human time substituting the value into SI-3. | `.cursor/rules/repo-governance.mdc` §W-2 manual fallback | OUT-OF-SCOPE for code change. NEW `scripts/nines_self_eval_with_cov_substitution.py` wrapper that runs NineS, substitutes the `pytest --cov` value, emits the merged JSON. | S |
| **D-N-3** | **MAJOR** | `src/devolaflow/si_chip_bridge/` has not been NineS-deep-analysed since v9.5.0 PV-01 (initial bridge module landed at `c873667`). The cycle's PV-03 deep-analysis MUST cover this package per the user mandate "对自身仓库结合 nines 进行深入分析". | `git log --oneline src/devolaflow/si_chip_bridge/` shows last touch at v9.5.0; no NineS analysis JSON in `.local/research/v10.*` for this path. | NEW `nines analyze --target-path src/devolaflow/si_chip_bridge --depth deep --agent-impact --keypoints` run in PV-03. Findings feed PV-04/PV-05 self-iteration scope. | S |
| **D-N-4** | **MINOR** | NineS self-eval baseline argument `--baseline-version` defaults to a hard-coded version literal in cycle-close commands. v10.2.0 cycle-close should baseline against v10.1.0; no mechanism auto-tracks the prior tag. | Per W-2 invocation in `.cursor/rules/repo-governance.mdc` | Document in cycle-close runbook (no code change). | S |

### §3.4 — D-I-* — Multi-round iteration mechanism

| ID | Sev | Description | Evidence | Fix scope | Effort |
|---|---|---|---|---|---|
| **D-I-1** | **CRITICAL** | The "multi-round self-iteration" user mandate maps onto W-8 / SI-9 convergence-round reinforcement BUT the W-8 trigger is a SCORE STAGNATION signal (≥2 rounds with no improvement) while Si-Chip's `iteration_delta` is a per-round PER-CHANGE signal. The two may agree or disagree on "should we stop iterating?". | `src/devolaflow/gate/reinforcement.py` (W-8 implementation) vs `src/devolaflow/si_chip_bridge/runner.py::apply_or_defer` (Si-Chip §23 gate) | Codify in PV-02 design doc: each round runs Si-Chip `apply_or_defer`; the cycle-stop predicate is `(round_count >= 2) AND (last_2_rounds_iteration_delta < +0.05)` per W-8 spirit. | M |
| **D-I-2** | **CRITICAL** | Per-round patch bump (one PATCH per iteration) requires a bump-version + CHANGELOG + W-18 ghost-audit refresh + adapter rebuild on every round. With 2-3 rounds per cycle this multiplies SI-10 6-gate runs by 4-5x. | W-9 SI-10 + W-10 + W-12 + W-18 | Codify per-PV bump cost; pre-cache the adapter build via `make build-skill` before round 1 to save ~30s per round; document in PV-04 spec. | S |
| **D-I-3** | **MAJOR** | "What defines a successful round?" is undefined. Candidate predicates: (a) si-chip `iteration_delta ≥ +0.10` on at least one file; (b) NineS overall composite delta ≥ +0.001; (c) `tests/test_writing_style_benchmark_regression.py` passes. The PV-02 design must pick ONE primary signal + tiebreakers. | No spec. | PV-02 design doc explicitly chooses primary signal. Recommendation: Si-Chip `iteration_delta` (matches user mandate "验证 si-chip 迭代有效性") with NineS as cross-check. | S |
| **D-I-4** | **MAJOR** | Per W-8 SI-9 max 5 reinforcement rules per round + escalate-to-human after 2 rounds stagnant. The cycle plan must declare the escalation surface (whom to escalate to, what artifact captures the decision). | `.cursor/rules/repo-governance.mdc` §W-8 | PV-02 design doc + PV-05 explicit escalation contract. | S |

### §3.5 — D-V-* — Validation & gates

| ID | Sev | Description | Evidence | Fix scope | Effort |
|---|---|---|---|---|---|
| **D-V-1** | **CRITICAL** | The SI-10 6-gate sequence does not include Si-Chip iteration-delta regression. Per the user mandate "验证 si-chip 迭代有效性", every PV in this cycle MUST run the gate as a 7th step until it formally lands in `repo-governance.mdc` §W-9. | `.cursor/rules/repo-governance.mdc` §W-9 | DOCUMENT extra step in cycle plan §5 pre-PV-01 checklist. PV-06 cycle-close considers whether to formalize as W-9 step #7 (telegraph for v10.4.0 cycle per W-21 cadence). | S |
| **D-V-2** | **CRITICAL** | `tests/test_writing_style_benchmark_regression.py` (post v10.1.0) pins post-transform naturalness ≥ 73 floor. Future SKILL.md edits in this cycle (e.g. to address D-S-4 budget violations) MUST not regress the writing-style baseline. The gate fires on `human/` + `README` so SKILL.md edits are not directly governed; cross-confirm by running both gates per PV. | `tests/test_writing_style_benchmark_regression.py` (v10.1.0 PV-05) | PV-04 + PV-05 design must call out the dual gate (Si-Chip iteration_delta AND writing-style regression). | S |
| **D-V-3** | **MAJOR** | The PR-update mandate ("更新 pr") implies an in-flight PR exists. v10.0.0 retrospective §3.6 documents the per-MINOR PR ceremony was skipped during cycle execution — the cycle ran on a single rollup branch. v10.1.0 followed the same pattern (PR #115). For v10.2.0 the user appears to want continuous PR updates per round. | v10.0.0 retrospective §3.6, `git log` PR merges | PV-01 opens `feat/v10.2.0-cycle` PR early (draft). Each PV pushes commits to the same PR. PV-06 marks ready-for-review + bumps to v10.3.0. | S |
| **D-V-4** | **MAJOR** | W-16 wholesale baseline regen at cycle-start. v10.2.0 is a `.0` MINOR — the trigger fires. EvoBench + golden-test baselines must be regenerated wholesale BEFORE PV-02 starts iterating. | `.cursor/rules/repo-governance.mdc` §W-16 | PV-01 includes `python -m pytest tests/test_benchmarks.py --regenerate-baselines`. Output → `benchmarks/devolaflow_context/baselines/v10.2.0_baseline.json`. | S |
| **D-V-5** | **MAJOR** | Per W-17 +30/PV cap, this cycle has 6 PVs forecast — budget = 6 × 30 = 180; cycle cap = 150. The mid-cycle audit at PV-05 (per W-17) is mandatory. Test additions per round (D-S-3 4 tests, D-P-1 3 tests, D-P-4 1 test, D-N-1 adapter ~5 tests, D-V-1 gate ~3 tests) total ~16 NEW; plus W-18 W-19 W-21 ~3 each = ~10. Forecast cycle delta ≈ +30-40 within W-17 cycle cap. | `.cursor/rules/repo-governance.mdc` §W-17 | PV-05 mid-cycle audit. PV-04 reservoir conservation if forecast exceeds. | — |

### §3.6 — D-W-* — W-21 Soul-set governance (telegraph carry-forward)

| ID | Sev | Description | Evidence | Fix scope | Effort |
|---|---|---|---|---|---|
| **D-W-1** | **MAJOR** | v10.0.0 retrospective §3.5 telegraphs S-11 candidate "Parallel Wave Dispatch Invariant" for v10.2.0 (cycle N+2) SI-1 assessment. Per W-21 the candidate MUST be evaluated in this gap analysis (i.e. here). Decision: **OUT** for v10.2.0 — the user mandate is plugin + Si-Chip + NineS + iteration; a Soul rule on parallel wave dispatch is orthogonal scope-creep. | v10.0.0 retrospective §3.5 + W-21 §1 | RECORD verdict in v10.2.0 retrospective §3 "deferred" with rationale. The S-11 candidate is re-telegraphed for v10.4.0 (cycle N+2 from v10.2.0) per the W-21 2-cycle deliberation rule. | S |

## §4 — Risk Register

| ID | Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|---|
| **R-1** | Si-Chip eval data adapter (D-N-1) does not produce a usable `runs-dir`/`baseline-dir` layout — `aggregate_eval.py` rejects it. v10.2.0 ships zero `iteration_delta` evidence (re-deferral of v9.5.0 OA-1). | MED | HIGH | PV-03 timeboxed to ≤1 day. Fallback: ship Si-Chip cheap `count_tokens` regression + W-18 ghost-audit pin + DEFER doc per v9.5.0 precedent. The user mandate's "验证 si-chip 迭代有效性" is satisfied by the validation attempt, not by an APPLY verdict. |
| **R-2** | Daily auto-upgrade integration (D-P-2) inadvertently fires `refresh_all` mid-dispatch and a network call stalls the L0 dispatcher. | MED | HIGH | Opt-in only via `DEVOLAFLOW_AUTO_INSTALL_PLUGINS=1`; staleness probe runs in a background-thread guarded by `defaults.network_timeout_seconds=90`. Fallback: per-call timeout default unchanged. Codify in PV-02 design. |
| **R-3** | Self-iteration round 1 (PV-04) lands a SKILL.md edit that violates A-2 frozen prefix invariant (positions 1-12 byte-stable since v7.0.0). The 9-baseline `tests/test_layout_invariant_multi_baseline.py` would catch it but only after CI; iteration cadence may push it through review unnoticed. | LOW | CRITICAL | Pre-bump dry-run per v10.0.0 retrospective §4.3 — every round runs `tests/test_layout_invariant_multi_baseline.py` BEFORE SI-10 gate sequence. |
| **R-4** | W-17 test cap exceeded due to si-chip + nines + plugin probe density. Per v10.0.0 retrospective §3.4 high-information disposition is acceptable but invites W-21-style governance fatigue. | MED | LOW | Mid-cycle audit at PV-05; defer non-blocking tests to v10.4.0. |
| **R-5** | NineS code_coverage A1 ticket continues to drag the hygiene mean down. SI-3 §3 manual substitution is an established pattern but the v10.2.0 evaluation report inherits the same caveat. | HIGH | LOW | Continue manual substitution per v10.0.0 SI-3 §3.3 precedent. |
| **R-6** | Multi-round patch bumps inflate the canonical 7 sync locations + adapter builds + W-18 entries to ~5x baseline. SI-10 6-gate latency × 5 may exceed 8 minutes per round. | MED | MED | Pre-cache adapter build via `make build-skill` before round 1; parallelize where SI-10 6-gate allows. |
| **R-7** | Operator does not generate Si-Chip with-ability/no-ability eval data per OA-1, leaving D-S-1 permanently blocked. Cycle ships with re-deferred dogfood (mirrors v9.5.0 outcome). | MED | HIGH | If R-1 mitigation fails, escalate to operator at PV-03 close. PV-04/PV-05 self-iteration falls back to deterministic D-P-* + D-N-3 fixes that do NOT depend on `iteration_delta`. |
| **R-8** | The W-16 wholesale baseline regen at PV-01 produces unexpectedly large baseline-file diffs (formatter / fixture drift since v9.7.0 cycle-close baseline) and the multi-baseline byte test fails. | MED | MED | Per v8.4.0 retrospective §"R-7 wholesale-vs-piecemeal" precedent: regenerate cleanly + commit baselines as PV-01's first commit; subsequent PVs compare against that. |
| **R-9** | The PR cadence (D-V-3) breaks down — PR #N opens at PV-01 but reviewer focus dilutes across 6 PVs. v10.0.0 retrospective §3.6 found this counterproductive. | MED | LOW | Cycle plan documents draft-PR-throughout, ready-for-review-at-PV-06 cadence. PR description is a living doc updated per PV. |

## §5 — Out-of-Scope (explicit defer list)

The following are deliberately OUT of v10.2.0:

| Item | Reason | Defer to |
|---|---|---|
| Byte-level edits to `workflow-system/agent/SKILL.md` for D-S-4 budget shrink | HIGH risk per v9.5.0 deferred §1.4 (cache-prefix anchor; A-2 frozen prefix invariant). Requires dedicated cycle with operator preview. | v10.4.0 (telegraph in v10.2.0 retrospective §3) |
| S-11 "Parallel Wave Dispatch Invariant" Soul rule addition | W-21 evaluation: not aligned with this cycle's user mandate; re-telegraph for v10.4.0 per 2-cycle rule | v10.4.0 |
| MTLD lexical diversity / quote-aware tokenisation / contributor writing guide (G-B1/G-B2/G-B3 from v10.1.0 retrospective §5) | Quality-of-life items on top of v10.1.0; orthogonal to v10.2.0 user mandate | v10.3.0 or v10.4.0 |
| New env-flag authoring (`DEVOLAFLOW_*`) | W-20 reuse-first applies — D-P-2 + D-P-5 prefer reusing `DEVOLAFLOW_AUTO_INSTALL_PLUGINS` activation surface | — (no new flag expected) |
| File-by-file shrink of the 18 budget offenders (`references/env-flags.md`, `references/shell-proxy.md`, etc.) | Each shrink requires its own Si-Chip iteration_delta gate; the cycle ships the GATE INFRASTRUCTURE first, the per-file shrinks land in v10.4.0+ on a per-file PV cadence | v10.4.0..v10.6.0 |
| Upstream Si-Chip / NineS bug filings (D-P-7, D-N-2 upstream A1) | Not DevolaFlow source code | Operator-tracked GitHub issues |
| Async dispatch refinements / compressor split / additional perf tuning | v10.0.0 cycle delivered cumulative -97.5% `select_context.p95`; v10.2.0 user mandate is plugin + Si-Chip + iteration, not perf | v10.4.0+ if perf regression detected |

## §6 — W-1 Decision Output

The W-1 SI-1 planning gate is **MET**:

1. **(a) Specific deficiencies enumerated:** §3 lists 4 BLOCKER + 7 CRITICAL + 9 MAJOR + 4 MINOR = **24 distinct gap items** across D-P / D-S / D-N / D-I / D-V / D-W lattices.
2. **(b) Priority ranking present:** BLOCKER / CRITICAL / MAJOR / MINOR scheme matches `tests/test_no_ghost_features.py::test_severity_lattice` precedent. The 4 BLOCKERs (D-P-1, D-P-2, D-S-1, D-S-2) gate cycle close.
3. **(c) Proposed fixes with file-level scope:** every gap row in §3 names target files + estimated effort.
4. **W-21 Soul-set telegraph carry-forward addressed:** §3.6 D-W-1 documents the explicit OUT verdict for the v10.0.0-telegraphed S-11 candidate; the freeze remains at 10.

The cycle plan in `.local/research/v10.2.0_cycle_plan.md` consumes this artifact as the W-1 gate output and decomposes the 24 gaps into 6 PVs.

W-1 entry → W-2 (NineS deep analysis is **scheduled at PV-03** against `src/devolaflow/si_chip_bridge/`, `src/devolaflow/plugins/`, `src/devolaflow/lifecycle/post_skill_edit.py`, `src/devolaflow/lifecycle/pre_plugin_invocation.py`).

## §7 — References

- User feedback (verbatim Mandarin source-of-truth): `.local/feedbacks/feedback_for_v10.2.0.md`
- Predecessor cycle plan template: `.local/research/v10.0.0_cycle_plan.md`
- Predecessor retrospective: `.local/research/v10.0.0_retrospective.md`
- v10.1.0 SI-1 precedent: `.local/research/v10.1.0_gap_analysis.md`
- v9.5.0 deferred backlog (the primary input to D-S-*): `.local/feedbacks/v9.5.0_sichip_deferred.md`
- v9.5.0 dogfood plumbing validation: `.local/research/v9.5.0_sichip_dogfood.md`
- v9.5.0 raw count_tokens data: `.local/research/v9.5.0_devola_count_tokens.json`
- W-21 Soul-set governance: `.cursor/rules/repo-governance.mdc` §W-21
- A-2 cache-layout governance: `.cursor/rules/repo-governance.mdc` §A-2 + `schemas/lean-dispatch.yaml#layout_invariant`
- W-9 SI-10 6-gate sequence: `.cursor/rules/repo-governance.mdc` §W-9
- W-16 wholesale baseline regen rule: `.cursor/rules/repo-governance.mdc` §W-16
- Plugin runtime registry (v3 schema, 4 plugins): `workflow-system/agent/knowledge/runtime-plugins.yaml`
- Plugin installer (auto-install + daily-upgrade machinery): `src/devolaflow/plugins/installer.py`
- Pre-plugin-invocation lifecycle hook: `src/devolaflow/lifecycle/pre_plugin_invocation.py`
- Post-skill-edit lifecycle hook (Si-Chip DEEP integration): `src/devolaflow/lifecycle/post_skill_edit.py`
- Si-Chip bridge module: `src/devolaflow/si_chip_bridge/{__init__,install_resolver,models,runner}.py`
- External tools (S-7 compliance):
  - DevolaFlow / EvoBench: https://github.com/YoRHa-Agents/DevolaFlow
  - NineS: https://github.com/YoRHa-Agents/NineS
  - Si-Chip: https://github.com/YoRHa-Agents/Si-Chip

---

*W-1 SI-1 gate VERDICT: PASSED. Proceed to PV-01 implementation per `.local/research/v10.2.0_cycle_plan.md`.*
