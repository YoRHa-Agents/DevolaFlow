# v17.4.1 DSH bridge smoke evidence

- Runtime: `dsh 0.1.1-rc.2`
- Install: `dsh plugin --profile sidecar-dev add packages/dsh-plugin`
- Profile verification: `dsh --profile sidecar-dev --dump-config` contains
  `devolaflow-boundary` from `packages/dsh-plugin/cordis.patch.yml`.
- Hook input: DSH-native `{name: "write", arguments: {file_path: "README.md"}}`.
- Result: `kind: deny`; the S-8 `CFO006` ownership breach was returned.
- Evidence mode: live runtime and plugin callback; no fallback was needed.
