# v9.1.0 SI-1 Gap Analysis — `.local/` & `.rules/` Utilization

**Cycle**: v9.0.1 (sustaining PATCH) → v9.1.0 (MINOR cycle bump)
**Trigger**: User feedback `feedback_for_v9.0.1.md` — `.local/` & `.rules/` underutilized in actual usage
**Date**: 2026-04-29
**Author**: L0 Project Agent (DevolaFlow self-iteration)
**Cycle classification**: MINOR — triggers W-16 wholesale baseline regen + W-17 cumulative test cap

---

## 1. Methodology

Two parallel `explore` subagents performed read-only audits at the request of L0:

* **Audit-A** — `.local/` utilization (`.agent/`, `research/`, `memory/`, `feedbacks/`, `tasks/`)
* **Audit-B** — `.rules/` utilization (compiler, drift, CI, cold-start UX, soul-set freeze)
* **Audit-C** — pre-bump compliance (canonical 7 sync, baselines, mirror, rule counts)

Findings are evidence-based with file/line citations; no inference without grep evidence.

---

## 2. Gap Inventory

### 2.1 Doc-vs-Code Drift (HIGH severity — false claims in governance corpus)

| ID | Symptom | Evidence | Fix scope |
|----|---------|----------|-----------|
| **G-001** | `.rules/soul.mdc` S-9 + `AGENTS.md` + `.cursor/rules/repo-governance.mdc` claim `tests/test_handoff_envelope_immutable.py` enforces append-only — file does **NOT exist** | `rg "test_handoff_envelope_immutable" tests/` returns 0 hits; existing coverage lives in `tests/test_agent_workspace.py` | Create the named test file as a thin facade over existing assertions; alternatively re-author rule text. **Decision: create file** (cheaper than touching 3+ compiled corpora) |
| **G-002** | S-9 also claims `lifecycle/check_envelope_append_only` hook blocks at write time in STRICT mode — module does **NOT exist** in `src/devolaflow/lifecycle/` | `ls src/devolaflow/lifecycle/` shows only `check_file_ownership.py`, `__init__.py`, `validate_dispatch.py`, `test_on_complete.py` | Create the hook module (small) + register in `DEFAULT_EVENTS` |
| **G-003** | `.rules/index.md` lists token budgets `cursor: 8000`, `agents_md: 6000` — `.rules/compile-config.yaml` says `cursor: 12000`, `agents_md: 12000` | `.rules/index.md:25-26` vs `.rules/compile-config.yaml:35,58` | Update `.rules/index.md` table to match config (data-truthful) |
| **G-004** | `.local/.agent/active/README.md` says change-driven workflow "(lands v8.2.6)" — change-driven is **already shipped** in SKILL.md as a first-class workflow type | `.local/.agent/active/README.md:3`; SKILL.md workflow selection table cites `change-driven` | Update `_DIR_README_CONTENT` in `src/devolaflow/local/workspace.py` so all newly-scaffolded repos get accurate text |

### 2.2 Lifecycle Activation Gap (HIGH severity — capability exists but is never used in normal flow)

| ID | Symptom | Evidence | Fix scope |
|----|---------|----------|-----------|
| **G-005** | `.local/.agent/active/`, `handoff/`, `archive/` stay **empty** in normal DevolaFlow usage; `HandoffStore.write_envelope` has **zero production callers** outside tests | `rg "write_envelope\(" src/` returns only the definition site in `agent_workspace/handoff.py` and the read site in `reporter.py::_summarise_handoff_chain` | Document the explicit activation contract + add at least one production code path that exercises envelope writes (e.g., a status-report-bridge that calls `write_envelope` when running under `change-driven` workflow) |
| **G-006** | `.local/memory/specs/` exists only as a README — A-4 source-of-truth tree is empty for every fresh repo, so the A-4 invariant has nothing to govern | `ls .local/memory/specs/` shows only `README.md` | Document explicit activation in README; add a representative example spec template under `templates/` so users see the shape |
| **G-007** | `devola-init local` does **NOT** invoke `RuleCompiler.compile_all()` — fresh repos can have `.rules/` sources but no compiled `repo-governance.mdc` / `AGENTS.md` until the user discovers `devola-init sync-rules` | `src/devolaflow/init_project.py::install_local` calls `scaffold_local()` + seeds `compile-config.yaml`; does not call compiler. Compiler only invoked from `sync_rules_cmd` | Auto-invoke compile after init scaffold (best-effort with try/except + WARN per S-5) |

### 2.3 Discoverability & UX (MEDIUM severity)

| ID | Symptom | Evidence | Fix scope |
|----|---------|----------|-----------|
| **G-008** | `Makefile` has **no** `compile-rules` / `sync-rules` target; `make all` does not invoke rule compilation | `Makefile:4-7` | Add `compile-rules` target; surface in `make all` and `make release-preflight` |
| **G-009** | `make check-drift` checks **human docs vs agent source**, not `.rules/` outputs — naming confusion | `Makefile:75-76` → `cli.py::check_drift_cmd` → `devolaflow.check_drift.check_drift` (not `RuleCompiler.check_drift`) | Add a separate `check-rules-drift` Makefile target wrapping the existing `check-rules-drift` console script; rename for clarity |
| **G-010** | CLAUDE.md has **no** pointer to `.rules/` or `sync-rules` or to the compiled `AGENTS.md` corpus — Claude Code users miss the governance | `CLAUDE.md:1-38` | Add an explicit `## Rules` section pointing Claude Code users at `AGENTS.md` (auto-generated from `.rules/`) |
| **G-011** | README.md mentions `.rules/` only briefly; no contributor-facing edit-then-recompile workflow | `README.md:167` | Add a 5-line "Editing rules" subsection with the recompile command |

### 2.4 Test Coverage Gaps (MEDIUM severity — invariants not enforced by CI)

| ID | Symptom | Evidence | Fix scope |
|----|---------|----------|-----------|
| **G-012** | No automated test asserts "every MINOR/MAJOR bump has `vX.Y.0_gap_analysis.md` + `vX.Y.0_retrospective.md`" — W-1/W-7 process discipline is purely human convention | No matching test in `rg -l "gap_analysis" tests/` | Add `tests/test_research_artifact_discipline.py` — soft lint that warns when CHANGELOG has `## [vX.Y.0]` entry but `docs/cycle-archive/vX.Y.0/gap_analysis.md` is missing (after cycle close per W-19) |
| **G-013** | No test asserts `.rules/index.md` token budgets stay in sync with `.rules/compile-config.yaml` — the very drift catalogued in G-003 was undetected | No matching test | Add `tests/test_rules_index_accuracy.py` — parses both files, asserts token budgets match |
| **G-014** | No test asserts compiled `AGENTS.md` / `repo-governance.mdc` contain **all** configured layers — `_truncate_to_budget` could silently drop layers if budget were lowered | `src/devolaflow/local/compiler.py:229-238` (`_truncate_to_budget` drops non-`always_include` layers) | Add `tests/test_local_layer_completeness.py` — asserts every layer in `compile-config.yaml` produces a non-zero-width section in compiled output |
| **G-015** | No end-to-end test exercises `devola-init local` followed by a `change-driven` task that materializes `.local/.agent/active/<id>/` — only fixture-based unit coverage exists | `_scaffold_active` fixture in `tests/test_agent_workspace.py` | Add `tests/test_change_driven_e2e.py` — integration test that runs init → opens change → writes handoff → archives, end-to-end against tmp dir |
| **G-016** | `tests/test_init_project.py` does not assert that `install_local` produced compiled rule outputs (because today it doesn't) | `tests/test_init_project.py` | Once G-007 ships, extend test to assert `AGENTS.md` exists post-init |

### 2.5 Research Artifact Hygiene (LOW severity — cleanup)

| ID | Symptom | Evidence | Fix scope |
|----|---------|----------|-----------|
| **G-017** | v9.0.1 `.local/research/` has only `evaluation` + `nines.{json,md}` — missing W-1 `gap_analysis` and W-7 `retrospective` (acceptable for sustaining PATCH per W-7 wording, but inconsistent vs v9.0.0 cycle) | `ls .local/research/v9.0.1*` | Document that PATCHes inherit cycle SI-1/SI-8 artifacts; add explicit note in W-7 wording |
| **G-018** | No `docs/cycle-archive/v9.0.1/` — but v9.0.1 is a PATCH within the v9.0.0 cycle, so W-19 archive is correctly delayed to cycle close | W-19 specification: "After a MAJOR or MINOR cycle ships" | No-op; v9.0.0 cycle archive already exists at `docs/cycle-archive/v9.0.0/` |

---

## 3. Priority Ranking & v9.1.0 Scope

### 3.1 In-scope (v9.1.0 — must ship)

**MUST-fix (release blockers — fix the docs-vs-code lies):**

1. **G-001** Create `tests/test_handoff_envelope_immutable.py` — file referenced by Soul rule MUST exist
2. **G-002** Create `lifecycle/check_envelope_append_only.py` hook + register in DEFAULT_EVENTS
3. **G-003** Fix `.rules/index.md` token budget table → match `compile-config.yaml`
4. **G-004** Fix `_DIR_README_CONTENT` "lands v8.2.6" stale text

**MUST-fix (lifecycle activation):**

5. **G-007** Auto-invoke `RuleCompiler.compile_all()` from `devola-init local` (best-effort, S-5 compliant)

**MUST-fix (tests for the above):**

6. **G-013** Add `tests/test_rules_index_accuracy.py`
7. **G-014** Add `tests/test_local_layer_completeness.py`
8. **G-016** Extend `tests/test_init_project.py` for G-007

**SHOULD-fix (UX polish):**

9. **G-008** Add `compile-rules` Makefile target; integrate into `make all`
10. **G-009** Add `check-rules-drift` Makefile target (rename clarity)
11. **G-010** Add `## Rules` section to CLAUDE.md
12. **G-011** Add "Editing rules" subsection to README.md

### 3.2 Deferred to v9.2.0 or later

* **G-005** First production caller for `write_envelope` — needs design ADR; defer to v9.2.0
* **G-006** Spec template under `templates/` — needs careful design; defer to v9.2.0
* **G-012** Research artifact discipline lint — depends on W-19 archive cadence, defer
* **G-015** E2E change-driven integration test — too large for v9.1.0; defer to v9.2.0
* **G-017/018** Documentation polish only — defer

### 3.3 Out-of-scope (intentional)

* Soul-set additions — frozen by W-21 until v9.2.0 (cycle N+2 from v9.0.0)
* New `DEVOLAFLOW_*` env flags — none needed; W-20 reuse-first applies
* Dispatch payload schema bumps — `layout_invariant` stays at length 16 / version 5

---

## 4. Self-Iteration Constraint Compliance

| Rule | Status for v9.1.0 |
|------|-------------------|
| **W-1** SI-1 planning gate | ✅ This document |
| **W-2** NineS analysis | will run pre-release `nines self-eval --baseline-version 9.0.1` |
| **W-3** SI-3 evaluation ≥ 8.5/10 | gate at end of cycle |
| **W-4** Benchmark regression guard | wholesale regen per W-16 (this is a MINOR cycle start) |
| **W-5** Skill format coupling | SKILL.md unchanged ⇒ no coupling triggered |
| **W-9** Test-then-commit (6 checks) | mandatory pre-commit |
| **W-10** Version bump protocol | use `scripts/bump_version.py 9.1.0` |
| **W-16** Wholesale baseline regen | **YES** — MINOR cycle start ⇒ regenerate `v9.1.0_baseline.json` |
| **W-17** Per-PV test cap (≤+30) | this PV adds ~5 NEW test functions: `test_handoff_envelope_immutable` (1), `test_rules_index_accuracy` (~2), `test_local_layer_completeness` (~2), extension to `test_init_project.py` (~1). Total ~6 — WELL under +30 cap |
| **W-18** Ghost-audit refresh | extend `tests/test_no_ghost_features.py` to cover the 2 new lifecycle/test files BEFORE adding CHANGELOG entry |
| **W-19** Research artifact archive | DEFERRED to v9.1.0 cycle close (this is cycle start) |
| **W-21** Soul-set freeze | Soul stays at 10 — no new S-rules in v9.1.0 |

---

## 5. Owned Files Manifest (v9.1.0)

**Source code:**
* `src/devolaflow/lifecycle/__init__.py` — register new hook event
* `src/devolaflow/lifecycle/check_envelope_append_only.py` — **NEW** hook module
* `src/devolaflow/local/workspace.py` — fix `_DIR_README_CONTENT` (G-004)
* `src/devolaflow/init_project.py` — auto-invoke compile (G-007)

**Rule corpus:**
* `.rules/index.md` — fix token budget table (G-003)

**Tests:**
* `tests/test_handoff_envelope_immutable.py` — **NEW** (G-001)
* `tests/test_rules_index_accuracy.py` — **NEW** (G-013)
* `tests/test_local_layer_completeness.py` — **NEW** (G-014)
* `tests/test_init_project.py` — extend (G-016)
* `tests/test_lifecycle_hooks.py` — extend (G-002)
* `tests/test_no_ghost_features.py` — extend per W-18

**Docs / build:**
* `Makefile` — add `compile-rules` + `check-rules-drift` targets (G-008/G-009)
* `CLAUDE.md` — add `## Rules` section (G-010)
* `README.md` — add "Editing rules" subsection (G-011)

**Version + cycle:**
* `src/devolaflow/__init__.py` — `9.0.1` → `9.1.0`
* + 7 canonical sync locations via `bump_version.py`
* `CHANGELOG.md` — add v9.1.0 entry
* `workflow-system/human/demo/version-timeline/versions.json` — add v9.1.0 entry per ST-7
* `benchmarks/devolaflow_context/baselines/v9.1.0_baseline.json` — **NEW** (W-16)
* `tests/test_benchmarks.py` — update `test_runner_prefers_latest_baseline` to expect `v9.1.0_baseline.json`

**Compiled outputs (auto-regenerated by `RuleCompiler.compile_all()` after .rules edits):**
* `.cursor/rules/repo-governance.mdc` — only if `.rules/*.mdc` changed (this PV touches only `.rules/index.md` which is NOT a `.mdc` source, so no recompile expected)
* `AGENTS.md` — same as above

---

## 6. Acceptance Criteria for v9.1.0

The release passes when:

1. All 12 in-scope gap items (G-001..G-004, G-007..G-011, G-013..G-014, G-016) are closed
2. W-9 6-step pre-commit verification passes (tests, lint, format, version, benchmarks, mirror check)
3. SI-3 weighted composite score ≥ 8.5/10 (MINOR floor)
4. Total rule count ≤ 60 (no rule changes in v9.1.0; should remain at 58)
5. Soul count == 10 (W-21 freeze — no S-additions)
6. CHANGELOG `[9.1.0]` entry exists, mentions all 12 gap items, ghost-audit covers all NEW symbols (W-18 precondition)
7. version-timeline `versions.json` carries a v9.1.0 entry (ST-7)
8. `benchmarks/devolaflow_context/baselines/v9.1.0_baseline.json` exists (W-16)

---

## 7. Risk Register

| Risk | Mitigation |
|------|-----------|
| Auto-invoking compile in `devola-init local` could fail on read-only filesystems | Wrap in try/except + WARN log per S-5; document graceful degradation |
| New `check_envelope_append_only` hook could break existing tests if it raises | Permissive default (warn+log), strict opt-in via env flag (W-20 reuse: `DEVOLAFLOW_STRICT_HOOKS=1`) |
| Wholesale baseline regen could shift composite scores ±5pp | Run full regen + compare each scenario; document any deltas in PV report |
| New tests could push coverage below 80% if new modules under-tested | New modules: `check_envelope_append_only.py` ~30 LOC; thin wrapper, easy to cover at 100% |

---

## 8. Decomposition Plan (preview)

* **Stage 1 (Plan)**: this gap analysis + implementation_plan + ADR for hook contract — L0 authors
* **Stage 2 (Impl-A: doc-drift fixes)**: 1 wave, 4 parallel L3 tasks (disjoint files) — G-001/G-002/G-003/G-004
* **Stage 3 (Impl-B: lifecycle activation + tests)**: 1 wave, 3 parallel L3 tasks — G-007 + G-013 + G-014 + G-016
* **Stage 4 (Impl-C: UX + docs)**: 1 wave, 4 parallel L3 tasks — G-008/G-009/G-010/G-011
* **Stage 5 (Baseline regen)**: 1 task — W-16 wholesale regen
* **Stage 6 (Version bump)**: 1 task — `bump_version.py 9.1.0` + CHANGELOG + version-timeline + sync-human-docs
* **Stage 7 (Gate)**: SI-3 evaluation + W-7 retrospective + W-9 6-step verification
* **Stage 8 (Commit)**: feature branch + commit (S-6 — no direct push to protected)

Total ≈ 14 atomic L3 tasks across 7 waves, well within 7-waves-per-stage and 5-tasks-per-wave constraints.

---

**Gate verdict**: PROCEED to Stage 2 (implementation). All W-1 preconditions met.
