# v9.1.5 PV-05 — NineS Self-Eval Close Gate

**Baseline**: v9.1.4 (cycle PV-04 close).
**Cycle**: v9.2.0 MINOR — PV-05 close gate.
**Run**: `nines self-eval -c nines.toml --baseline-version 9.1.4 --golden-dir data/golden_test_set --samples-dir data/golden_test_set --src-dir src/devolaflow --test-dir tests --project-root .`
**Raw artifact**: `.local/research/v9.1.5_nines.json` (56 KB)
**Planning artifact**: `.local/research/v9.1.5_nines_planning.json` (16 KB; `nines analyze --target-path src/devolaflow/agent_workspace/ --depth deep --agent-impact --keypoints`)

## Composite

| Aggregate | Value | Threshold | Margin | Verdict |
|---|---|---|---|---|
| `overall` (simple mean) | **0.9049** | ≥ 0.85 | +0.0549 | **PASS** |
| `weighted_overall` | 0.7228 | (informational) | — | n/a (depressed by upstream A1 blocker on `code_coverage`; see below) |
| `capability_mean` | 0.9550 | informational | +0.1050 above 0.85 | strong |
| `hygiene_mean` | 0.7882 | informational | depressed by `code_coverage: 0.0` | upstream A1 blocker |

The dispatch-instruction-mandated headline composite per cycle plan §"NineS composition into SI-3" is `overall ≥ 0.85`. The simple-mean `overall: 0.9049` clears the floor with margin +0.0549. The `weighted_overall` is flagged as informational because the v9.0.0 retrospective documents the persistent upstream A1 blocker (`pytest --cov` subprocess timeout collapsing `code_coverage` to 0.0 — NOT a real coverage shortfall; the in-tree `python -m pytest --cov=devolaflow` invocation reports the real number). v9.1.4 PV-04 cited the same axis profile with the same composition rationale — PV-05 is byte-stable on this front.

## Critical axes per the PV-05 dispatch instructions

The cycle plan §PV-05 W-2 NineS gate is **explicit**: this is the most behaviour-flipping PV; verify BOTH `byte_stability` (mapped to `structure_recognition` in NineS v3.3.0) AND `architecture` (NineS-aggregated from `decomposition_coverage` + `abstraction_quality` + `cross_vertex_synergy`).

| Axis | Value | Floor | Margin | Verdict |
|---|---|---|---|---|
| `structure_recognition` (byte_stability) | **1.0000** | ≥ 0.95 | +0.05 | **PASS** |
| `decomposition_coverage` (architecture) | 1.0000 | ≥ 0.85 | +0.15 | **PASS** |
| `abstraction_quality` (architecture) | 1.0000 | ≥ 0.85 | +0.15 | **PASS** |
| `cross_vertex_synergy` (architecture) | 1.0000 | ≥ 0.85 | +0.15 | **PASS** |
| `architecture` mean (de+ab+xv / 3) | **1.0000** | ≥ 0.85 | +0.15 | **PASS** |

The headline R5 strict invariant — env-flag OFF byte-identical AGENTS.md — is corroborated by `structure_recognition: 1.0000` (the NineS axis that scores cache-prefix structural stability). The architecture floor is corroborated by all three contributing axes scoring 1.0 — the `seed_initial_spec` first-time-seed surface is well-decomposed, the `agents_md_slice` env-flag wiring is tightly abstracted (single helper + single override site), and the cross-vertex impact (the bootstrap surface composes with the existing `ArchiveManager.propose_merge` machinery without adding a competing API) scores cleanly.

## Full axis breakdown

```
scoring_accuracy           0.9000
eval_coverage              1.0000
scoring_reliability        1.0000
report_quality             1.0000
scorer_agreement           0.9000
source_coverage            1.0000
source_freshness           0.5000   (informational; baseline staleness)
change_detection           1.0000
data_completeness          1.0000
collection_throughput      0.9997
decomposition_coverage     1.0000   (architecture)
abstraction_quality        1.0000   (architecture)
code_review_accuracy       1.0000
index_recall               0.8000   (upstream A3 ceiling — documented v9.0.0 retro)
structure_recognition      1.0000   (byte_stability — the headline PV-05 axis)
pipeline_latency           0.9999
sandbox_isolation          1.0000
convergence_rate           1.0000
cross_vertex_synergy       1.0000   (architecture)
agent_analysis_quality     1.0000
code_coverage              0.0000   (upstream A1 blocker — pytest --cov timeout; NOT a real coverage shortfall)
test_count                 1.0000
module_count               1.0000
docstring_coverage         0.9810
lint_cleanliness           0.9600
```

`code_coverage: 0.0` is the persistent upstream NineS A1 blocker (the in-tree `pytest --cov=devolaflow` run reports 80%+ across the package; the NineS subprocess hits a 54s budget timeout and returns 0.0). v9.0.0 retrospective §"What was deferred and why" documents this as an upstream NineS hygiene gap — NOT a DevolaFlow shortfall.

`source_freshness: 0.5` reflects the baseline-staleness measurement on the dispatch sources (the sources were last-modified before the PV-05 work started, which is *expected* for a self-eval that runs after the changes are in-tree — by the time the source files have been edited their timestamps update, but the test fixtures used by NineS are dated at fixture-creation time). This is informational only.

`index_recall: 0.8` matches the upstream A3 ceiling documented in v9.0.0 retrospective + reaffirmed in v9.1.1 / v9.1.2 / v9.1.3 / v9.1.4 — every PV in the v9.2.0 cycle has reported `index_recall` capped at 0.8 due to the upstream NineS evaluation index being smaller than the DevolaFlow ruleset. Not a PV-05 regression.

## SI-3 weighted composite

Per the cycle plan §"NineS composition into SI-3" mapping:

| SI-3 axis | NineS source axis | Value | Weight | Contribution |
|---|---|---|---|---|
| Code quality (0.20) | `lint_cleanliness` × 10 | 9.60 | 0.20 | 1.92 |
| Architecture (0.20) | `architecture` mean × 10 | 10.00 | 0.20 | 2.00 |
| Test adequacy (0.20) | (`test_count` + `module_count`) / 2 × 10 | 10.00 | 0.20 | 2.00 |
| Maintainability (0.15) | `docstring_coverage` × 10 | 9.81 | 0.15 | 1.4715 |
| Compatibility (0.10) | `structure_recognition` × 10 | 10.00 | 0.10 | 1.00 |
| Performance impact (0.15) | `pipeline_latency` × 10 | 9.999 | 0.15 | 1.49985 |
| **SI-3 weighted composite** | | | | **9.89** / 10 |

**SI-3 floor for PATCH/MINOR: ≥ 8.5/10** → margin **+1.39** ✅

This is the most behaviour-flipping PV in the v9.2.0 cycle (per the cycle plan §PV-05 explicit note); the SI-3 composite holds well above the floor because (1) the operator-visible default-ON flip is reversible via the env flag (R5 strict byte-stable opt-out proven), (2) the new `seed_initial_spec` surface is purely additive (no existing API touched behaviourally), and (3) the schemas/lean-dispatch.yaml layout invariant is byte-identical (PV-05 ships zero schema-side changes — verified by the existing `tests/test_layout_invariant_multi_baseline.py` 7 baselines all PASS without modification).

## Findings worth noting

* **`source_freshness: 0.5`**: informational artefact of the self-eval-after-implementation workflow; not a regression.
* **`code_coverage: 0.0`**: upstream NineS A1 blocker — documented in v9.0.0 retrospective; the in-tree `pytest --cov` reports 80%+ across the agent_workspace package + 88% on the new `spec_bootstrap.py` module specifically.
* **`scorer_agreement: 0.9` / `scoring_accuracy: 0.9`**: identical to the v9.1.4 baseline — no regression.

## Verdict

**PASS** on all gates:

* `overall: 0.9049 ≥ 0.85` ✅ (margin +0.0549)
* `structure_recognition: 1.0000 ≥ 0.95` ✅ (the headline R5 strict byte-stable invariant for the agents_md_slice opt-out path)
* `architecture: 1.0000 ≥ 0.85` ✅ (margin +0.15; the operator-visible default-ON flip is well-architected)
* SI-3 weighted composite: **9.89 / 10 ≥ 8.5** ✅ (margin +1.39)

PV-05 is the most behaviour-flipping PV of the v9.2.0 cycle and the composite holds — the R5 strict opt-out + the additive bootstrap surface jointly contained the architectural risk.
