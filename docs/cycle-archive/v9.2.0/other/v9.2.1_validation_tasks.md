# v9.2.1 Stage 3 — validation-task decomposition

**Author**: L3 PV-07 self-update Stage 3 agent
**Baseline**: v9.2.0 (commit `dd4bcb1`)
**Workflow stage**: 3 of 7 (`self-update` workflow, Design team)
**Runtime**: < 10 minutes (target ≤ 30 min)
**Verdict**: **PASS** — 7 validation tasks ≤ 10 per P4 bounded-retry cap.

## 1. Inputs

* Stage-1 check-refs report (`v9.2.1_check_refs.md`) — all references
  resolve; no follow-up tasks required.
* Stage-2 NineS aggregate (`v9.2.1_nines_aggregate.md`) — 0 HIGH /
  CRITICAL / BLOCKER findings → no NineS-driven tasks.
* Cycle plan §PV-07 Stage-4/5 directives (multi-adapter rebuild +
  4-fixture E2E).
* Cycle plan §PV-07 Stage-7 release checklist (bump, CHANGELOG,
  retrospective, ghost-audit refresh).

## 2. Validation tasks (7 total ≤ 10 cap)

### T1 — Multi-adapter rebuild drift check
**AC**: `make build-skill` (or `python -m devolaflow.build_skill`) produces 4 core adapter outputs (cursor / codex / claude / copilot) within their respective byte budgets per CP-5 / W-12. Git diff on tracked adapter output files is **empty** (idempotency proof — rebuild from unchanged source MUST NOT produce diff).
**Stage**: 4
**Owner**: Implement team

### T2 — Tier-2 adapter rebuild drift check
**AC**: 5 tier-2 adapter outputs (jetbrains / amazon_q / gemini / augment / trae per v9.0.1) rebuild within budget. Git diff on tracked tier-2 adapter outputs is **empty**.
**Stage**: 4
**Owner**: Implement team

### T3 — Rule compilation drift check
**AC**: `make compile-rules` emits **zero diff** against `AGENTS.md` + `.cursor/rules/repo-governance.mdc` (recompile is idempotent; source `.rules/*.mdc` unchanged by this PV). `make check-rules-drift` exits 0.
**Stage**: 4
**Owner**: Implement team

### T4 — 4-fixture E2E parametrization
**AC**: `tests/test_capability_e2e.py` gains parametrized tests covering the 4 fixture shapes (`empty`, `local-only`, `rules-only`, `full-stack`). Each shape asserts the correct `scan_workspace` presence/counts. ≤ 4 NEW test functions per W-17 PV-07 budget (parametrize expansions over EXISTING tests do not count per W-17 verbatim).
**Stage**: 5
**Owner**: Test team

### T5 — Full regression suite green
**AC**: `python -m pytest tests/ -q` → every test from v9.1.1..v9.2.0 still PASSES. No regressions. Baseline ≥ 3561 passed (v9.2.0 close).
**Stage**: 5
**Owner**: Test team

### T6 — Ghost-audit refresh (W-18 precondition)
**AC**: `tests/test_no_ghost_features.py` gains `test_v9_2_1_new_symbols_have_coverage` BEFORE the CHANGELOG `[9.2.1]` entry is authored. The lint asserts: (a) the 5 research artefacts exist with ≥ 200 byte minimum; (b) the meta-validation archive folder has been moved from `active/` to `archive/` by the end of Stage 7.
**Stage**: 5 (author) + 7 (post-archive verification)
**Owner**: Test team (author) + Implement team (verify at Stage 7)

### T7 — Cycle-close NineS self-eval
**AC**: `nines -f json -c nines.toml self-eval --baseline-version 9.2.0 --golden-dir data/golden_test_set --samples-dir data/golden_test_set --src-dir src/devolaflow --test-dir tests --project-root .` emits a composite ≥ **0.85** and an SI-3 weighted composite ≥ **8.5** (PATCH floor matches MINOR floor per W-3).
**Stage**: 6
**Owner**: Review team

## 3. Escalation check (P4 bounded retry)

| Criterion | Status |
|---|---|
| Total task count ≤ 10 | **7** (PASS) |
| NineS HIGH finding count > 10 → escalate | **0** (PASS, no escalation) |
| Any BLOCKER → escalate immediately | **0** (PASS) |

No P4 escalation triggered. Cycle-lead L0 need not be involved until
Stage 6 verdict (if FAIL).

## 4. Wave assignment

* **Wave 1** (Research, Stages 1-3): T0 (Stage 1 check-refs — already
  complete), T0' (Stage 2 NineS — already complete), decomposition
  itself (this document).
* **Wave 2** (Integrate + Test, Stages 4-5): T1, T2, T3, T4, T5, T6
  (author half).
* **Wave 3** (Evaluate + Release, Stages 6-7): T7, T6 (verify half),
  plus Stage-7 release mechanics (bump, CHANGELOG, retrospective,
  archive).

## 5. Cross-references

* Stage 1: `.local/research/v9.2.1_check_refs.md`
* Stage 2: `.local/research/v9.2.1_nines_aggregate.md`
* Cycle plan: `.cursor/plans/workspace-capability-activation_ec560bc8.plan.md` §PV-07

## 6. Verdict

**PASS** — 7 validation tasks decomposed, all within P4 bounded-retry
cap. Proceed to Stage 4 (integrate — multi-adapter rebuild + rules
compile drift check).
