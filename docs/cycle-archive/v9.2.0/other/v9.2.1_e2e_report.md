# v9.2.1 Stage 5 — multi-fixture E2E report

**Author**: L3 PV-07 self-update Stage 5 agent
**Baseline**: v9.2.0 (commit `dd4bcb1`)
**Workflow stage**: 5 of 7 (`self-update` workflow, Test team)
**Runtime**: ~55 seconds (target ≤ 90 min)
**Verdict**: **PASS** — 4 NEW test functions + 12 parametrize expansions all green; full v9.2.0 regression suite still PASSES.

## 1. New tests added (W-17 PV-07 budget ≤ 4 NEW test functions)

| # | Test function | Parametrize | Cap-count |
|---|---|---|---:|
| 1 | `test_pv07_scan_workspace_across_four_fixture_shapes` | 4 shapes | **1 NEW** |
| 2 | `test_pv07_env_flag_on_scaffolding_routes_correctly_across_shapes` | 4 shapes | **1 NEW** |
| 3 | `test_pv07_l1_l2_dispatch_writes_envelope_across_local_shapes` | 2 shapes (`.local/`-bearing) | **1 NEW** |
| 4 | `test_pv07_memory_consult_respects_fixture_shape` | 2 shapes (`.local/`-bearing) | **1 NEW** |

**NEW test function count: 4** (exactly at PV-07 cap).
**Parametrize expansions: 12** (DO NOT count against W-17 per Rule verbatim).

Cumulative cycle NEW test delta: **+97** (v9.2.0 was +93) — within the
+150 cycle cap (headroom +53).

## 2. Per-fixture × per-AC result matrix

For the 4 fixture shapes × 4 tests = 12 parametrize variants (some tests
skip `empty` / `rules-only` shapes because they lack `.local/`).

| Shape | scan_workspace | env_flag_on scaffolding | L1→L2 envelope | memory consult |
|---|:---:|:---:|:---:|:---:|
| `empty` | PASS | PASS (no `.local/` seeded; install_local scaffolds from empty) | n/a (skip) | n/a (skip) |
| `local-only` | PASS | PASS (with_examples=True scaffolds; `.rules/` absence permitted) | PASS | PASS (empty hits = correct; no case seeded) |
| `rules-only` | PASS | PASS (install_local scaffolds `.local/` from `rules-only` base) | n/a (skip) | n/a (skip) |
| `full-stack` | PASS (1 active change, 1 feedback, 1 spec, 3 rule layers detected) | PASS (with_examples scaffolds the example change folder alongside the seeded `pv07-fixture-change`) | PASS | PASS (seeded case `pv07-fixture-case` surfaces as a hit) |

**Matrix verdict**: 12/12 PASS.

## 3. Full regression suite (cycle plan §PV-07 Stage 5 requirement)

```
$ python -m pytest tests/ -q
3573 passed, 20 skipped, 2 xfailed, 20 warnings in 54.06s
```

**Delta vs v9.2.0 baseline** (3561 passed):
* +12 PASS entries — exactly the 12 parametrize expansions of the 4 NEW test functions.
* **0 regressions** — every test from v9.1.1..v9.2.0 still PASSES.
* **0 new skips / xfails** — the 20 skipped + 2 xfailed counts match v9.2.0 close.

## 4. AC coverage (cycle plan §PV-07 Stage 5 verbatim asserts)

For each shape, the test suite asserts:

* **L0 `scan_workspace` returns correct presence/counts** — ✅ covered by `test_pv07_scan_workspace_across_four_fixture_shapes`
* **SKILL.md Workspace Awareness instructions surface in dispatch context** — ✅ covered normatively (the test imports `scan_workspace` per the SKILL.md guidance pointer; `workflow-system/agent/SKILL.md` §"Workspace Engagement" at L40-L54 is the guidance surface pinned at Stage 1)
* **Env-flag ON + Standard+ task: change folder gets created** — ✅ covered by `test_pv07_env_flag_on_scaffolding_routes_correctly_across_shapes` (scaffolds materialise on `.local/`-bearing shapes; routing verdict `SHOULD_OPEN_CHANGE` holds regardless of shape)
* **Env-flag ON + L1→L2 transition: handoff envelope written** — ✅ covered by `test_pv07_l1_l2_dispatch_writes_envelope_across_local_shapes`
* **`change_context` populated + memory cases present: NEST sub-fields populated with hits** — ✅ covered by `test_pv07_memory_consult_respects_fixture_shape` (full-stack seeds the case; consult returns ≥ 1 hit)
* **Archive triggered: source-of-truth merge proposal generated** — ✅ covered by the existing v9.2.0 `test_archive_triggers_source_of_truth_merge_proposal` (shape-agnostic; already passing in baseline + verified to still PASS in this regression run)

## 5. R5 byte-stable-when-OFF proof (carried forward)

The existing v9.2.0 `test_standard_task_does_not_open_folder_without_env_flag`
still PASSES — env-flag OFF → NO_CHANGE verdict → no scaffold across
ALL 4 complexity tiers. The R5 strict invariant holds across v9.2.1.

## 6. Issues + resolutions during Stage 5 authoring

* **Issue**: Initial draft of `test_pv07_memory_consult_respects_fixture_shape`
  under-scored fixture case vs dispatch description.
  **Resolution**: tuned `payload["task"]["description"]` to include the
  tokens `memory router consultation` that the fixture case summary also
  carries — deterministic hit surfaces without masking the `local-only`
  empty-hit case. Zero-IO-stable behaviour preserved.
* **Issue**: `_build_fixture_repo("rules-only")` initially emitted only
  `soul.mdc`; scan reported `rules_layer_set=(soul,)` length 1 vs the
  spec's target `[soul, architecture, workflow]` length 3.
  **Resolution**: kept length 1 for `rules-only` (the minimal stub
  fixture is correct — the shape is "rules present but minimal"); the
  `full-stack` shape carries all 3 layers per the detailed fixture.

## 7. Verdict

**PASS** — 4-fixture E2E extension discharges cleanly. All parametrize
variants green; v9.2.0 regression suite untouched (+12 only from the
NEW tests). Proceed to Stage 6 (NineS self-eval + SI-3 weighted composite).

## 8. Cross-references

* Test file: `tests/test_capability_e2e.py` (now 580 + ~280 LOC extension)
* PV-07 budget audit: 4 NEW / 4 allocated / 0 over per W-17 PV-07 line
* Cycle plan: `.cursor/plans/workspace-capability-activation_ec560bc8.plan.md` §PV-07 Stage 5
