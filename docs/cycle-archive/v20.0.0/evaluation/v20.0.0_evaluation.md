# 20.0.0 — SI-3 Evaluation Report

> Per W-3 / SI-3 (`.rules/workflow.mdc` + `AGENTS.md` §W-3), this MAJOR
> release uses the built-in harness across six weighted dimensions. The MAJOR
> acceptance threshold is composite ≥ 9.0/10.
>
> Cycle: v20.0.0 close (`v20.0.0`, Functional Test System Loop v3).
> Date: 2026-08-28.

## Part A — Built-in Harness Evaluation (BINDING)

### A.1 Machine Verdict

Canonical command, run from the repository root:

```bash
PATH=.venv/bin:$PATH python -m devolaflow.harness evaluate --ledger .local/telemetry/harness.jsonl --repo . --output .local/research/v20.0.0_harness_evaluation_final.json
```

Exit code: `0` (`READY`). This is the final end-of-iteration run, taken after
the module-size refactoring and the passing release-preflight chain; the
earlier interim run (`v20.0.0_harness_evaluation.json`,
`sampled_at 2026-08-28T15:18:04Z`) reported the identical composite and
per-dimension scores.

| JSON field | Verbatim value |
|---|---|
| `verdict` | `READY` |
| `composite` | `9.17` / 10 |
| evaluator `threshold` | `8.5` |
| MAJOR release threshold | `9.0` |
| `auto_fill_rate` | `1.0` |
| `sampled_at` | `2026-08-28T16:13:23.037833+00:00` |

**SI-3 disposition**: **ACCEPT**. The machine verdict is `READY`, composite
`9.17`, and it exceeds the MAJOR threshold `9.0`.

### A.2 Six-Dimension Evidence

| Dimension | Weight | Score | Weighted | Rationale |
|-----------|--------|-------|----------|-----------|
| **Code quality** | 0.20 | 10.0 | 2.0000 | `code_quality`; subcomponent envelope below |
| **Architecture rationality** | 0.20 | 9.29 | 1.8580 | `architecture_rationality`; subcomponent envelope below |
| **Test adequacy** | 0.20 | 6.67 | 1.3340 | `test_adequacy`; single-base-ref W-17 probe remains open |
| **Maintainability** | 0.15 | 9.86 | 1.4790 | `maintainability`; subcomponent envelope below |
| **Compatibility** | 0.10 | 10.0 | 1.0000 | `compatibility`; subcomponent envelope below |
| **Performance impact** | 0.15 | 10.0 | 1.5000 | `performance_impact`; subcomponent envelope below |

**Composite (weighted sum)**:
`10.0×0.20 + 9.29×0.20 + 6.67×0.20 + 9.86×0.15 + 10.0×0.10 + 10.0×0.15 = 9.171`, reported as **9.17 / 10**.

The matching `scores[*].metadata.subcomponents` envelopes are copied
verbatim from the fresh machine report:

```json
{
  "ruff_lint": {
    "score": 10.0,
    "available": true,
    "value": true
  },
  "ruff_format": {
    "score": 10.0,
    "available": true,
    "value": true
  },
  "coverage": {
    "score": 10.0,
    "available": true,
    "value": 91.0
  }
}
```

```json
{
  "layout_invariant": {
    "score": 10.0,
    "available": true,
    "value": true
  },
  "quantifiable_ratio": {
    "score": 8.57,
    "available": true,
    "value": 0.8571428571428571
  }
}
```

```json
{
  "test_suite": {
    "score": 10.0,
    "available": true,
    "value": true
  },
  "coverage": {
    "score": 10.0,
    "available": true,
    "value": 91.0
  },
  "w17": {
    "score": 0.0,
    "available": true,
    "value": 64
  }
}
```

```json
{
  "ruff_format": {
    "score": 10.0,
    "available": true,
    "value": true
  },
  "docstrings": {
    "score": 9.72,
    "available": true,
    "value": 97.24080267558529
  }
}
```

```json
{
  "layout_invariant": {
    "score": 10.0,
    "available": true,
    "value": true
  },
  "compatibility_suite": {
    "score": 10.0,
    "available": true,
    "value": true
  }
}
```

```json
{
  "budget_compliance_ratio": {
    "score": 10.0,
    "available": true,
    "value": 1.0
  },
  "p95_headroom": {
    "score": 10.0,
    "available": true,
    "value": 0.28725
  }
}
```

**Verdict**: **READY** (binding machine verdict from
`.local/research/v20.0.0_harness_evaluation_final.json`).

### A.3 Supporting Verification Evidence

```text
$ make release-preflight
[gate:check-functional-matrix] PASS elapsed=0s
[gate:check-module-size] PASS elapsed=1s
--- Release preflight PASSED ---
(exit code 0; full 7-gate SI-10 chain + 6 release extras, including the
newly landed hard-fail functional matrix gate)

$ make test-core
5367 passed, 151 skipped in 163.25s
[gate:test-core] PASS elapsed=164s
```

These commands support the machine report; they do not override its
`verdict`.

### A.4 Findings Closure

| Finding | Severity | Source | Closure | Verification |
|---------|----------|--------|---------|--------------|
| W-17 observation is `64` | process finding | fresh evaluator `scores[2].metadata.subcomponents.w17` | The probe diffs a single base ref and cannot see PV boundaries; the per-wave split (max 14 ≤ 30 per PV, 46 ≤ 150 per cycle) is documented in `.local/research/loop3_w17_reconciliation.md`, so no evaluator-side split is fabricated | Fresh JSON records `value: 64` and `score: 0.0`; the reconciliation artifact carries the per-file inventory |
| Transient `NOT_READY` mid-close | process finding | evaluator runs during cycle close | Two interim evaluator runs scored 8.5 then 7.75 while version-coupled surfaces (CHANGELOG, demo window, human docs, ghost-audit formatting) were still propagating; the final run on consistent surfaces is `READY` 9.17 | The archived JSON is the final run; the earlier runs were superseded, not averaged |
| Historical trend and matched baseline evidence | advisory evidence gap | fresh evaluator `harness_summary` and telemetry history | Explicitly `INSUFFICIENT`; current measurements are preserved, but no historical delta is asserted | Fresh JSON marks historical aggregate measurement records as `INSUFFICIENT`; W-16 settlement is recorded separately |

## Part B — Harness Telemetry and Trend (ADVISORY)

The fresh report's `harness_summary` records 13 ledger records across five
v17-era changes; token budgets show `budget_compliance_ratio: 1.0` and
`p95_budget_utilization: 0.28725` (L1 0.1846, L2 0.28725). Constraint
accounting reports `count: 140` (40 invariant / 80 guard / 20 advisory),
`quantifiable_ratio: 0.8571428571428571`, and
`advisory_folded_ratio: 0.07692307692307693`.

| Signal | Current | Baseline | Delta | Disposition |
|---|---:|---:|---:|---|
| `tokens.budget_compliance_ratio` | 1.0 | INSUFFICIENT | INSUFFICIENT | Current evaluator value retained; no historical delta |
| `tokens.p95_budget_utilization` | 0.28725 | INSUFFICIENT | INSUFFICIENT | Current evaluator value retained; no historical delta |
| `constraints.quantifiable_ratio` | 0.8571428571428571 | INSUFFICIENT | INSUFFICIENT | Current evaluator value retained; no historical delta |
| `constraints.advisory_folded_ratio` | 0.07692307692307693 | INSUFFICIENT | INSUFFICIENT | Current evaluator value retained; no historical delta |
| checklist completion trend | INSUFFICIENT | INSUFFICIENT | INSUFFICIENT | No matched historical checklist series |
| reversion / blocker trend | INSUFFICIENT | INSUFFICIENT | INSUFFICIENT | No matched historical trend evidence |

The evaluator's top-level measurements are current probe values:

```json
{
  "agents_md_tokens": {
    "available": true,
    "value": 10857,
    "status": "AVAILABLE",
    "source": "evaluator"
  },
  "suite_wall_seconds": {
    "available": true,
    "value": 207.77332387500792,
    "status": "AVAILABLE",
    "source": "evaluator"
  },
  "cjk_violations": {
    "available": true,
    "value": 0,
    "status": "AVAILABLE",
    "source": "evaluator"
  },
  "ghost_loc": {
    "available": true,
    "value": 12414,
    "status": "AVAILABLE",
    "source": "evaluator"
  }
}
```

The evaluator's single suggestion is
`{"dimension": "test_adequacy", "reason": "score 6.67 below threshold 8.50"}`;
its root cause is the single-base-ref `w17` probe closed in §A.4.

## Part C — Release Disposition

- Semantic tier: **MAJOR** (20.0.0). The cycle removes no public API, but the
  adjudicated behavioral closures change observable contract behavior
  (gate skip scoring, dispatch zero-parallelism rejection, handoff
  exclusive-create, proposal path rejection, compression stage protocol,
  archive approval requirement), which the SI-1 adjudication classified as a
  MAJOR trigger.
- W-16 settlement: the v20.0.0 baseline is settled from this same fresh
  report as `.local/telemetry/baselines/harness_baseline_v20.0.0.json`;
  Tier-B window keeps v18/v19/v20.
- W-7 retrospective: `.local/research/v20.0.0_retrospective.md`.
- W-17 reconciliation: `.local/research/loop3_w17_reconciliation.md`.
- W-18 ghost audit: `tests/ghost/test_features_v20_0.py`.
