# v8.3.0 Gap Analysis

> Date: 2026-04-22 | Author: L0 Project Agent | Source: `.local/feedbacks/feedback_for_v8.2.0.md`
> SI-1 Iteration Planning Gate artifact — required before any v8.3.0 implementation begins (per W-1 / SI-1).
> Predecessor cycle: v8.2.0 (5 PVs, 100% accept, SI-3 9.42/10) → see `.local/research/v8.2.0_retrospective.md`.

## 0. Executive Summary

The v8.2.0 user feedback (`feedback_for_v8.2.0.md`, 20 lines) requests a **major workspace + governance redesign** that targets v8.3.0 release (minor bump, not patch). Six concrete asks decompose into:

| # | User Ask (verbatim quote) | Maps To | Severity |
|---|----------------------------|---------|----------|
| A1 | "工作区里其实可以预留出面向 agent 的工作区... `.local/.agent/active/` ... `.local/.agent/handoff/` ... `.local/.agent/archive/`" | New agent-facing workspace tree under `.local/` | **Major** (architectural addition) |
| A2 | "用简约且面向 agent 的格式对目标，验收标准，操作 spec 进行明确规约... 应该尽量轻量，避免占用过多上下文" | Schema for lightweight per-change artifacts (goal/AC/spec/tasks) | **Major** (schema layer) |
| A3 | "结合到 rules 和 memory 体系，用于提供更良好的研发指引以及自我学习" | Rules-layer integration + learnings auto-extract on archive | **Major** (cross-system integration) |
| A4 | "rules 和 memory 体系还需要有面向人类开发者展示的的可读性 report.md" | Auto-generated human-readable REPORT.md per change/aggregate | **Major** (new deliverable surface) |
| A5 | "深度调研 openspec 并进行拆解和分析" | OpenSpec deep-analysis artifact + ADR for adoption decisions | **Critical** (research gate predecessor) |
| A6 | "对于细节的每个提升点，都单独进行 patch 小版本，验证有效后才进行" | Iteration roadmap as N×PV patches, each SI-10 6/6 validated before next | **Critical** (process invariant) |
| **PATCH** | "nines, ui-pro 作为依赖插件，当调用对应功能时，应该先进行安装和更新到全局" | Runtime plugin auto-install (nines + ui-pro) — designated v8.2.1 fast-path patch | **High** (user-flagged priority) |

**Cycle classification:** v8.3.0 minor bump. **Patch count forecast: 9 PVs + 1 release roll-up = 10 increments.** This is heavier than v8.2.0 (5 PVs) and lighter than v8.0.0 (13 PVs); consistent with the user's explicit "validate before next" mandate which forbids batching.

**Companion artifacts** (must be authored as the SI-1 gate; this file is the entry point):
- `.local/research/v8.3.0_openspec_deep_analysis.md` — A5 deliverable (manual NineS-style analysis since OpenSpec is TypeScript and NineS targets Python)
- `.local/research/v8.3.0_design.md` — full design covering A1–A4 + plugin runtime
- `.local/research/v8.3.0_patch_plan.md` — per-patch decomposition (A6 deliverable)

---

## 1. Current-State Baseline (verbatim, no paraphrase)

### 1.1 `.local/` workspace as of v8.2.0 (post-cycle, branch `main`)

`scaffold_local()` in `src/devolaflow/local/workspace.py` (lines 7–8) declares:
```
REQUIRED_DIRS = ["feedbacks", "tasks", "memory"]
ON_DEMAND_DIRS = ["research", "design", "benchmarks", "logs", "scratch"]
```

Actual top-level layout (verbatim from `ls .local/`):
```
acceptance_gate_design.md             feedbacks/                podcast/                          v3.2.0_*.md (×7)
benchmarks/                           go_nogo_decision_matrix.md prototypes/                       wp10_prototypes_summary.md
context_optimization_roadmap.md       index.md                   refinement_*.md (×4)              wp11_benchmark_round1.md
design_updates.md                     nines-analysis/            research/                         wp12_iteration_log.md
designs/                              nines-review/              sandbox/                          wp1..wp7_*.md
evaluation_methodology.md             ...                        skill_plan_mode_report.md         task_*.md
```

**Gap G-1 (S-2 / DS-1 violation risk):** the `index.md` listing (verbatim from `cat .local/index.md`) is **out-of-date** — it does NOT enumerate `feedbacks/`, `memory/`, `nines-analysis/`, `nines-review/`, `podcast/` and lists 9 of the 13 actual subdirectories. Auto-generation has drifted.

**Gap G-2 (CP-1 / S-4 violation):** `scaffold_local()` is documented to call `generate_tracker(local_dir / "feedbacks")` and `generate_memory_index(local_dir / "memory")`, but:
- `.local/feedbacks/TRACKER.md` does **not exist** (verified: `ls .local/feedbacks/TRACKER.md` → "No such file or directory")
- `.local/memory/` directory does **not exist** at all (verified: `ls -la .local/memory/` → no entry; missing from `ls .local`)

These are pre-existing scaffolding gaps; v8.3.0 must repair them as a precondition for A1 (`.local/.agent/` rests on the existing `.local/` substrate).

### 1.2 Existing rules/memory infrastructure

**Rules:**
```
.rules/
├── architecture.mdc   priority 1, always_include true
├── compile-config.yaml
├── conventions.mdc    priority 2
├── index.md
├── soul.mdc           priority 0, always_include true (P0 invariants S-1..S-7)
├── style.mdc          priority 4
└── workflow.mdc       priority 3
```

Compile targets (verbatim from `compile-config.yaml`):
- `.cursor/rules/repo-governance.mdc` (mdc, 8000 token budget, all 5 layers)
- `AGENTS.md` (markdown, 6000 token budget, soul+architecture+conventions+workflow)

Drift detection: `.rules/.compile-hashes.json` exists.

**Memory (operational learnings):**
- Path resolved by `learnings.py::resolve_learnings_path()`: prefers `.local/memory/operational.jsonl` (project-local, gitignored), falls back to `workflow-system/agent/knowledge/learnings/operational.jsonl` (canonical repo path).
- `.local/memory/` does not exist (G-2), so all reads currently fall back to the canonical repo path.
- Public API: `capture_learning`, `load_relevant_learnings`, `consolidate_session`, `decay_confidence`, `pin_learning_for_session`, `dedup_learnings`, `capture_session_reflection`, `prune_learnings`, `promote_learning`, `format_learnings_section`, `log_external_source_review`, `build_session_state_for`, `load_prefs`, `resolve_learnings_path`. **14 public functions** (per `__all__`).

### 1.3 Existing change-tracking surfaces

DevolaFlow has **two** loosely-coupled change-tracking surfaces today, neither of which matches the user's A1–A2 ask:

| Surface | Purpose | Schema | Audience |
|---------|---------|--------|----------|
| `.local/research/<topic>.md` | Free-form research artifacts | None (markdown) | Mixed (devs + agent context for next cycle) |
| TaskDispatch YAML (`schemas/lean-dispatch.yaml`) | Inter-layer dispatch payloads (in-flight only) | Strict YAML (15 keys, schema v4) | Agent-only (machine-readable) |

**Missing surface (A1+A2):** a per-change folder containing **lightweight, agent-targeted, machine-readable** artifacts that survive across sessions, are bound to a specific change-id, and capture goal+AC+spec+tasks+status WITHOUT the verbosity of `.local/research/` markdown.

### 1.4 Plugin/dependency runtime (v8.2.1 PATCH ask)

External tools currently consumed by DevolaFlow workflows:

| Tool | How invoked | Install state | Auto-install today? |
|------|-------------|---------------|---------------------|
| `nines` | `nines -f json -c nines.toml self-eval ...` (per W-2 / SI-2) | `pip show nines` → v3.3.0 (editable from `/home/agent/workspace/NineS`) | **NO** — agent assumes presence |
| `ui-pro` | TBD — referenced in feedback as a dependency | `which ui-pro` → not installed | **NO** — would error at first use |
| EvoBench | `python -m pytest tests/test_benchmarks.py` (in-repo) | Always present (in-repo) | N/A |
| `build-skill` | Repo-local script | Always present (in-repo) | N/A |

`workflow-system/agent/knowledge/reference-dependencies.yaml` tracks **19 external sources** (10 active + 9 periodic) but it is a **monitoring** registry (`update_triggers`, `last_checked`), NOT an **install** registry. There is no `install_cmd` / `version_check_cmd` / `min_version` field, and no Python module that calls `pip install` on demand.

**Gap G-3 (user-flagged):** no runtime plugin install layer; agent has to assume `nines` / `ui-pro` are pre-installed, violating the spirit of S-5 (no silent failures — today the failure mode is `FileNotFoundError` on first use, not a clear actionable error with auto-recovery).

### 1.5 Workflow templates

22 templates registered in `workflow-system/agent/templates/registry.yaml`. The two relevant to v8.3.0 are:

- **`repo-init`** (analyze→scaffold→compile→interview→verify, modes core/standard/full): canonical_manifest in `repo-init.yaml::scaffold.config` lists 5 paths (`.local/{feedbacks,tasks,memory}/`, `.local/index.md`, `.rules/compile-config.yaml`). **A1 will require extending this list to 8 paths** (add `.local/.agent/{active,handoff,archive}/`).
- **`entropy-cleanup`** (scan→propose→review→apply): drift audit. **A4's auto-generated REPORT.md** can be triggered by this workflow as a side effect of "scan" stage.

No template currently models OpenSpec's spec-driven flow (proposal→specs→design→tasks→implement→archive). **A1+A2 implies a new `change-driven` workflow template** as the orchestrator-level binding.

### 1.6 Cache-layout invariant (P6 / A-2)

Per `schemas/lean-dispatch.yaml#layout_invariant.canonical_order`, the dispatch payload has **15 top-level keys at version 4** (post v8.0.0 P-08 + P-10). v8.2.0 cycle achieved **0 P6 transitions**.

**v8.3.0 P6 forecast:** likely **1 transition (→ schema v5)** because A1 plus A2 plus A3 will likely append a new `change_context` top-level field (tracking which `.local/.agent/active/<id>/` the dispatch references). Per A-2 / Rule 6, this MUST be appended at position 16 (NOT inserted), preserving positions 1–15 byte-identical, and the `assert_dispatch_layout()` validator MUST be updated in the same patch.

---

## 2. Gap Inventory (priority-ranked)

### 2.1 Critical (blocking other work)

| ID | Gap | Source ask | Affected files | Patch slot |
|----|-----|------------|----------------|------------|
| **C-001** | OpenSpec deep analysis missing — research gate predecessor for A1+A2 design decisions | A5 | `.local/research/v8.3.0_openspec_deep_analysis.md` (NEW) | SI-1 gate (this conversation) |
| **C-002** | No specification of what `.local/.agent/{active,handoff,archive}/` is supposed to contain — schema-less | A1+A2 | `schemas/agent-workspace/*.yaml` (NEW directory) | v8.2.4 |
| **C-003** | No Python API to manage changes (create/transition-state/handoff/archive) | A1+A2 | `src/devolaflow/agent_workspace/__init__.py` (NEW package) | v8.2.5 |
| **C-004** | `repo-init.yaml::canonical_manifest` does not include `.local/.agent/*` — repo-init bootstrap will not provision the new tree | A1 | `workflow-system/agent/templates/builtin/repo-init.yaml`, `src/devolaflow/local/workspace.py::REQUIRED_DIRS` | v8.2.3 |

### 2.2 High (blocking specific user asks)

| ID | Gap | Source ask | Affected files | Patch slot |
|----|-----|------------|----------------|------------|
| **H-001** | Plugin auto-install runtime missing — nines/ui-pro fail with `FileNotFoundError` on first use, no auto-recovery | PATCH | `src/devolaflow/plugins/installer.py` (NEW), `workflow-system/agent/knowledge/runtime-plugins.yaml` (NEW or section in reference-dependencies.yaml), `workflow-system/agent/templates/builtin/nines-assisted.yaml` (precondition stage), `workflow-system/agent/templates/builtin/product-verification.yaml` (precondition stage) | **v8.2.1** (first patch) |
| **H-002** | No soul rule prohibiting agents from writing outside an active change's `owned_files` (A1's `active/` invariant) | A1+A3 | `.rules/soul.mdc` (add S-8), `.cursor/rules/repo-governance.mdc` (re-compile) | v8.2.2 |
| **H-003** | No conventions rule defining the lightweight artifact format (token budgets per artifact type) | A2 | `.rules/conventions.mdc` (add C-9), `.rules/architecture.mdc` (add A-4 if needed) | v8.2.2 |
| **H-004** | No workflow template binding propose→apply→verify→archive lifecycle to DevolaFlow stage primitives | A1+A2 | `workflow-system/agent/templates/builtin/change-driven.yaml` (NEW), `workflow-system/agent/templates/registry.yaml` (register row) | v8.2.6 |
| **H-005** | No auto-generated human-readable REPORT.md generator | A4 | `src/devolaflow/agent_workspace/reporter.py` (NEW), `.rules/REPORT.md` generator extension | v8.2.7 |
| **H-006** | Memory layer is not change-aware — `load_relevant_learnings()` does not filter by active change-id | A3 | `src/devolaflow/learnings.py` (extend with `change_id` filter), `src/devolaflow/agent_workspace/memory_bridge.py` (NEW) | v8.2.8 |
| **H-007** | SKILL.md does not document the new workflow — agents won't activate it | A1+A2 | `workflow-system/agent/SKILL.md` (Workflow Selection table row + new section), `workflow-system/agent/references/agent-workspace.md` (NEW 10th reference) | v8.2.9 |
| **H-008** | EvoBench has no scenarios validating agent workspace context density (token budget enforcement) | A2 (token budgets) | `benchmarks/devolaflow_context/scenarios/agent_workspace_active.yaml` (NEW), `benchmarks/devolaflow_context/scenarios/handoff_envelope_density.yaml` (NEW) | v8.2.9 |

### 2.3 Medium (quality / robustness improvements)

| ID | Gap | Source ask | Affected files | Patch slot |
|----|-----|------------|----------------|------------|
| **M-001** | Pre-existing G-1: `.local/index.md` is out-of-date | (general hygiene) | `src/devolaflow/local/workspace.py::generate_index()` (call on demand or in CI) | v8.2.3 (bundle with workspace fixes) |
| **M-002** | Pre-existing G-2: `.local/feedbacks/TRACKER.md` and `.local/memory/` missing despite scaffold_local() promising them — first-run only, devs who scaffolded earlier never get the upgrade | (CP-1 / S-4 hygiene) | `src/devolaflow/local/workspace.py::scaffold_local()` (idempotent re-run on every `make scaffold-local`) | v8.2.3 |
| **M-003** | OpenSpec mature feature (delta specs `## ADDED / ## MODIFIED / ## REMOVED Requirements`) not yet in DevolaFlow change format | A2 | `schemas/agent-workspace/change-spec.yaml` (delta sections), `src/devolaflow/agent_workspace/delta_parser.py` (NEW) | v8.2.4 |
| **M-004** | OpenSpec `archive` operation merges deltas into source-of-truth; DevolaFlow has no source-of-truth for "current spec" — needs decision (specs/ vs rules/ vs both) | A1+A3 | ADR in `.local/research/adr/v8.3.0-ADR-001-spec-source-of-truth.md` (NEW) | SI-1 gate (this conversation, in design.md) |
| **M-005** | Handoff envelopes have no inbox/outbox semantics — agents could silently overwrite each other's deliveries | A1 | `schemas/agent-workspace/handoff-envelope.yaml` (NEW with `seq` monotonic counter), `src/devolaflow/agent_workspace/handoff.py::HandoffStore` | v8.2.4+v8.2.5 |
| **M-006** | Cache layout v5 transition required for `change_context` top-level dispatch field | A1 (P6 invariant) | `schemas/lean-dispatch.yaml#layout_invariant`, `src/devolaflow/compressor.py::assert_dispatch_layout` | v8.2.5 (bundled with API patch) |
| **M-007** | Slash-command surface — DevolaFlow lacks `/devola:propose`, `/devola:apply`, `/devola:archive` analogs of OpenSpec `/opsx:*`; users currently invoke workflows via SKILL.md auto-detect | (OpenSpec adaptation) | `workflow-system/agent/commands/devola-*.md` (NEW directory mirroring OpenSpec's `commands/` adapter pattern), per-tool commands generated by `build-skill` | v8.2.9 (couples with SKILL changes) |

### 2.4 Low (deferred / nice-to-have)

| ID | Gap | Source ask | Defer to |
|----|-----|------------|----------|
| **L-001** | Per-tool command files for all 25+ AI assistants supported by OpenSpec | (OpenSpec parity) | v8.3.x (incremental: ship Cursor + Claude + Codex + Copilot first, others later) |
| **L-002** | Telemetry opt-out env var (`DEVOLAFLOW_TELEMETRY=0` à la `OPENSPEC_TELEMETRY=0`) | (OpenSpec parity) | v8.4.0 (no telemetry exists today; out of scope) |
| **L-003** | Web demo refresh showing the new agent workspace + report.md flow | DS-1 / WX-1 cascade | v8.3.x (bundle with WX-1 cascade after v8.3.0 ships) |
| **L-004** | Migration tool for legacy `.local/research/<topic>.md` → `.local/.agent/archive/<date>-<topic>/` | A1 backfill | v8.3.x (opt-in script; not blocking v8.3.0 release) |

**Total inventory:** 4 critical + 8 high + 7 medium + 4 low = **23 gap items**, of which **18 are in v8.3.0 scope** (4C + 8H + 6M; M-007 included; L-* deferred). Comparable to v8.0.0 cycle's 18-item scope (13 selected + 5 deferred).

---

## 3. Mapping Gaps → Patch Versions (forecast for §`v8.3.0_patch_plan.md` to formalize)

| Patch | Gap items closed | Effort class | Risk | Why this slot |
|-------|------------------|--------------|------|---------------|
| **v8.2.1** | H-001 | M (4–6h) | LOW (additive new module) | User-flagged PATCH; ship first to unblock nines/ui-pro CI workflows |
| **v8.2.2** | H-002 + H-003 | S (3h) | LOW (rules-layer addition; recompile only) | Foundational rules MUST land before scaffold so the new rules are present when devs run `repo-init` for the first time on v8.3.0 |
| **v8.2.3** | C-004 + M-001 + M-002 | M (5h) | LOW–MEDIUM (`.local/` substrate change) | Workspace scaffold extension; sets the 8-path canonical manifest |
| **v8.2.4** | C-002 + M-003 + M-005 (schema half) | M (6h) | LOW (schema-only, no Python yet) | Pure schema; validate-able independently before any code consumes them |
| **v8.2.5** | C-003 + M-005 (Python half) + M-006 (cache layout v5) | XL (8–10h) | MEDIUM-HIGH (P6 transition + new package) | Core Python API; the largest patch in the cycle, validated by full test_benchmarks regression sweep |
| **v8.2.6** | H-004 | M (5h) | LOW (additive workflow template) | Wires `change-driven` template into the registry, integrating v8.2.5 API |
| **v8.2.7** | H-005 | M (5h) | LOW (additive reporter module) | Human-readable REPORT.md surface; opt-in default-off |
| **v8.2.8** | H-006 | M (5h) | MEDIUM (touches stable learnings.py) | Memory bridge + change-aware learnings filter, with R5 byte-identical preservation |
| **v8.2.9** | H-007 + H-008 + M-007 | XL (8–10h) | MEDIUM (SKILL.md + adapter rebuild + new EvoBench scenarios) | Skill-format coupling per W-5 / SI-5 (line count + 4 adapters + benchmarks + version) |
| **v8.3.0** | (release roll-up) | M (4h) | LOW (version bump + retrospective + CHANGELOG) | Aggregate; SI-3 evaluation, SI-8 retrospective, version bump 8.2.9 → 8.3.0 |

**Patch count: 9 PVs + 1 release = 10 increments** (vs v8.0.0's 13, v8.2.0's 5). Composition: 1 user-flagged PATCH + 6 architectural patches + 2 docs/skill patches + 1 release.

**Cycle invariants** (carried from v8.0.0/v8.2.0 precedent and enforced per patch):
1. **R1 — SI-10 6/6 PASS at every patch**: full pre-commit sequence per W-9 (pytest -q, ruff check, ruff format --check, test_version, test_benchmarks, make check-cursor-skill).
2. **R2 — 100% PV ACCEPT rate** matching v7.5.0 8/8 + v8.0.0 13/13 + v8.2.0 5/5 + v7.3.0 6/6 + v7.2.0 4/4 precedents.
3. **R3 — At most 1 P6 transition** (v8.2.5 only); positions 1–15 byte-identical for backward compat.
4. **R4 — SKILL.md held at ≤ 500 lines** (currently 495/500; v8.2.9 must absorb +new section without breaching).
5. **R5 — Backward-compat byte-identical** for existing 14 `learnings.py` public functions, scaffold_local(), TaskDispatch v4 layout.
6. **R6 — 0 EvoBench regressions > 5pp per W-4 / SI-4** across 43 baseline scenarios; v8.2.9 adds ≥2 new scenarios bringing total to ≥45.
7. **R7 — No absolute paths in any agent-facing file** per S-2.
8. **R8 — Each patch ships in a feature branch** (`feat/v8.3.0-pvNN-<name>`) with single feat commit + ≤1 coupling follow-up commit if CI requires it (v8.2.0 PV-02 precedent).

---

## 4. Risk Register

| Risk | Affected patch | Likelihood | Impact | Mitigation |
|------|---------------|------------|--------|------------|
| **R-A** Cache layout v5 transition breaks downstream consumers | v8.2.5 | LOW | HIGH | R5 backward-compat — append `change_context` at position 16; v7.0.0 + v7.3.0 byte-baselines must continue passing (additivity proven across 3 generations) |
| **R-B** SKILL.md breaches 500-line ceiling at v8.2.9 | v8.2.9 | MEDIUM | HIGH | Compression budget audit pre-v8.2.9 (mirror v8.2.0 PV-05 R7 anchor refactor that opened budget); fall back to splitting into a 10th references file |
| **R-C** Auto-install (v8.2.1) creates supply-chain attack vector | v8.2.1 | LOW | HIGH | Pin `min_version` + `expected_sha256` per plugin in `runtime-plugins.yaml`; refuse install if upstream signature mismatches; opt-out env `DEVOLAFLOW_AUTO_INSTALL=0`; relate to existing trust-tier work in `agent-skills-security` reference (gap_id M6) |
| **R-D** Memory layer regression from change-aware filter | v8.2.8 | MEDIUM | MEDIUM | All 71+ existing learnings tests must pass byte-identical (v8.2.0 PV-03 SessionState precedent) |
| **R-E** New `change-driven` workflow conflicts with existing 22 templates | v8.2.6 | LOW | LOW | Smoke test: instantiate every existing template after registering `change-driven`; cycle the EvoBench suite |
| **R-F** OpenSpec deep analysis (v8.2.0 SI-1 artifact) misidentifies adoptable patterns | SI-1 gate | MEDIUM | MEDIUM | Cross-validate findings against existing `reference-dependencies.yaml` entries (`get-shit-done`, `superpowers`, `karpathy-llm-wiki`) for prior precedent |
| **R-G** User adds new feedback mid-cycle (v8.2.0 cycle precedent) | (cycle-wide) | HIGH | LOW | Capture new feedback in `.local/feedbacks/feedback_for_v8.3.0.md` for next cycle; do NOT scope-creep within v8.3.0 |
| **R-H** `.local/memory/` directory missing today (G-2) breaks v8.2.8 memory bridge tests | v8.2.3 + v8.2.8 | LOW | MEDIUM | v8.2.3 PRE-PATCH must repair G-2 (idempotent re-scaffold); v8.2.8 tests can assume directory exists |
| **R-I** Plugin install in v8.2.1 fails silently when offline (CI runners without internet) | v8.2.1 | MEDIUM | LOW | `runtime-plugins.yaml` carries `local_fallback_path` for editable installs (e.g., `/home/agent/workspace/NineS` for nines); installer prefers fallback when network is unreachable |
| **R-J** Stagnation across 2 rounds in any convergence patch | (any) | LOW | HIGH | Per W-8 / SI-9, escalate to human after 2 stagnant rounds; max 5 reinforcement rules per round per Rule SI-9 |

---

## 5. Verification Plan (per W-9 / SI-10)

Each patch MUST satisfy the 6-step pre-commit sequence before commit:

```
1. python -m pytest tests/ -q                  # all tests pass
2. ruff check src/ tests/                      # no lint errors
3. ruff format --check src/ tests/             # formatting correct
4. python -m pytest tests/test_version.py -v   # version consistency (canonical 7)
5. python -m pytest tests/test_benchmarks.py -v # no regressions > 5pp
6. make check-cursor-skill                     # exit 0 (no-op if mirror absent)
```

Plus **per-patch additional gates** (per the patch-specific risk profile in §3):

| Patch | Additional gates beyond SI-10 |
|-------|-------------------------------|
| v8.2.1 | `pytest tests/test_plugins.py` (NEW), `python -c "from devolaflow.plugins import ensure_plugin; ensure_plugin('nines')"` smoke test |
| v8.2.2 | `python -c "from devolaflow.local.compiler import RuleCompiler; RuleCompiler('.rules/compile-config.yaml').compile_all()"`, AGENTS.md regen diff check |
| v8.2.3 | `python -c "from devolaflow.local.workspace import scaffold_local; scaffold_local('.')"` produces 8 paths; `pytest tests/test_local_workspace.py -v` |
| v8.2.4 | `pytest tests/test_agent_workspace_schemas.py -v` (NEW); schema fixture validation suite |
| v8.2.5 | `pytest tests/test_agent_workspace.py -v` (NEW); `pytest tests/test_compressor.py::test_dispatch_layout_v5 -v`; round-trip serialization tests |
| v8.2.6 | Smoke test: instantiate `change-driven` workflow in `nines-assisted` precondition path |
| v8.2.7 | `pytest tests/test_reporter.py -v` (NEW); idempotency test (REPORT.md generation 2× → byte-identical) |
| v8.2.8 | All 71+ existing `tests/test_learnings.py` tests MUST pass byte-identical; new `tests/test_memory_bridge.py` |
| v8.2.9 | `wc -l workflow-system/agent/SKILL.md` ≤ 500; `make build-skill` PASS for all 4 adapters; ≥45 EvoBench scenarios PASS; SF-4 9 → 10 reference docs |
| v8.3.0 | SI-3 evaluation report (≥ 8.5/10 minor threshold); SI-8 retrospective; CHANGELOG aggregate roll-up; annotated `v8.3.0` git tag |

---

## 6. SI-1 Gate Verdict

**PASS** — this gap analysis enumerates:
- (a) **18 specific deficiencies** (4C + 8H + 6M) directly traceable to user feedback A1–A6 + PATCH;
- (b) **priority ranking** (Critical / High / Medium / Low) with explicit "blocking" semantics;
- (c) **proposed fixes with file-level scope** (every gap row lists target files);
- (d) **patch-level decomposition** (10 increments) with cycle invariants R1–R8.

Per Rule W-1 / SI-1, this gate is the entry point for SI-2 through SI-10. The next required artifacts are:
1. `.local/research/v8.3.0_openspec_deep_analysis.md` — closes C-001
2. `.local/research/v8.3.0_design.md` — comprehensive design across A1–A4 + PATCH
3. `.local/research/v8.3.0_patch_plan.md` — formal per-patch decomposition (A6 deliverable)
4. ADR `.local/research/adr/v8.3.0-ADR-001-spec-source-of-truth.md` — closes M-004 (deferred to design.md if a single ADR section suffices)

After all 4 SI-1 artifacts exist, L0 presents to USER for sign-off. NO IMPLEMENTATION before sign-off.

---

## Cross-references

- **User feedback**: `.local/feedbacks/feedback_for_v8.2.0.md` (20 lines, dated 2026-04-22)
- **Predecessor retrospective (W-7 / SI-8)**: `.local/research/v8.2.0_retrospective.md`
- **Predecessor evaluation (W-3 / SI-3)**: `.local/research/v8.2.0_evaluation.md`
- **Predecessor patch plan**: `.local/research/v8.2.0_patch_plan.md` (562 lines, style precedent)
- **OpenSpec source**: `https://github.com/Fission-AI/OpenSpec` (cloned to `/home/agent/reference/openspec` at runtime per S-7 — local clone path NOT hardcoded in any agent-facing file)
- **NineS source**: `https://github.com/YoRHa-Agents/NineS` (per S-7)
- **DevolaFlow source**: `https://github.com/YoRHa-Agents/DevolaFlow` (per S-7)
