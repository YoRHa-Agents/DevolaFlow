# v17.0.0 R7 设计 — 证据收口（B5 真实账本 + W-16 结算 + v17 vs v16 对照 + W-3 评估 + 发布）

* 日期：2026-08-25
* 输入：G17-B5（`.local/research/v17.0.0_gap_analysis.md`）+ R1–R5 全部合并后的 main
* 分支（待建）：`feat/v17-release`（R5 合并后自 main）
* 性质：L0 收口轮——无新特性，只有证据生产、评估、版本工序与发布。

## 1. 前置状态（R5 合并时点）

* R1 清理（#185）/ R2 边界（#186）/ R3 注入（#187）/ R4 focus/loop（#188）/ R5 模型边界（待定 PR）/ R6 npm（#184）全部合并。
* 各轮 CHANGELOG 备存文本在 `v17.0.0_r[2-5]_design.md` §3.6（R1 的在其 PR 描述与 commit）；W-18 ghost pin 均已先行落地（`tests/ghost/test_features_v17_0.py`）。
* W-17 周期账：R1 6 + R2 27 + R3 10 + R4 12 + R5 ≤12 ≤ 67 ≪ 150（每轮 ≤ 30 亦满足）。
* npm 首发仍卡 @yorha-agents 组织不存在（用户侧手工步骤）；发版 tag 会再触发 npm-publish workflow,组织建好即自动发。

## 2. 工序（顺序执行）

### P1 — 真实账本生产（B5）

在发布分支上以 harness 遥测真实运行生成 v17 账本：
`python -m devolaflow.harness evaluate --ledger .local/telemetry/harness.jsonl --repo . --output .local/research/v17.0.0_harness_evaluation.json`（W-2）。
账本须含 R3/R5 新字段的真实记录（`host_rule_tokens`/`slice_savings_pct`/`capacity_profile`）——若现存账本记录早于 R3，追加若干真实 dispatch 记录（经 `record_dispatch_telemetry` 实录,非手写）。

### P2 — v17 vs v16 效率对照表

对照维度（写入 `.local/research/v17.0.0_evaluation.md` §对照）：
1. token 注入：AGENTS.md 全量 vs slice（12267 → 按 task_type 3645 等,slice_savings_pct 均值）
2. 测试规模/时长：v16 5100 → v17 全量数与耗时
3. 代码规模：R1 清理净删行数（git diff --stat v16.0.0..HEAD 的 src/ 汇总）
4. 边界强制面：v16 提示位 vs v17 五 host 桥 + 层预算断言 + session resume（运行时可执行强制点数量）
5. 六维 harness 评估：v16 composite 9.70 vs v17 composite（P1 产出）
6. 交叉验证：`harness cross-validate` v16 账本 vs v17 账本（M5b 建的 CLI）

### P3 — W-3 评估 + W-16 结算

* `.local/research/v17.0.0_evaluation.md`：六维加权（代码质量 .20/架构 .20/测试 .20/可维护 .15/兼容 .10/性能 .15），MAJOR 阈值 ≥ 9.0。
* W-16：冻结 `.local/telemetry/baselines/harness_baseline_v17.0.0.json`;Tier-B 窗口保 v15/v16/v17,更早者按 Tier-C 归档。

### P4 — 版本工序

1. `python scripts/bump_version.py 17.0.0`（7 文件；npm package.json 在同步集内）→ `make test-version` 绿。
2. CHANGELOG `## [17.0.0]`：汇总 R1–R6 备存文本 + BREAKING 段（nines 删除、legacy schema 删除、shim 退役、cascade legacy-4 面值化、L3 命名清除）+ 操作者可见行为变更段（DEVOLAFLOW_HOST_ENFORCE、session resume、_M3 pin 语义、npm 安装径）。W-18：所有符号 ghost pin 已在各轮先行,收口时跑 `python -m pytest tests/ghost/ -q` 复核。
3. `make release-preflight` 全绿。

### P5 — retrospective + W-19 归档 + 发布

1. `.local/research/v17.0.0_retrospective.md`（含 W-21 观察:本周期无 Soul 增补提案;W-24.2 re-defer 至 v18+ 的 §3 deferral 记录——若 v18 要动 Soul 层此处即 2-cycle telegraph 起点）。
2. `python scripts/archive_research_artifacts.py v17.0.0` → `docs/cycle-archive/v17.0.0/` 提交。
3. PR → CI → 合并 → `git tag v17.0.0` + GitHub Release（正文取 CHANGELOG 段）→ npm-publish workflow 触发（组织未建则记录失败原因,不阻塞 GitHub 发布）。

## 3. 边界

* 收口轮零新特性、零 schema 变更;若 P2/P3 暴露回归,开修复小轮(R5.x)而非在收口分支夹带修复。
* W-17：本轮新增测试函数目标 0(允许 ≤ 3,仅限评估工具修缺)。
* Tier-A witnesses 绝不触碰。

## 4. 完成判据

1. `v17.0.0_harness_evaluation.json` composite ≥ 9.0(MAJOR 阈值)且无 INSUFFICIENT 维度。
2. 对照表 6 维度全部有数、正向或有解释。
3. release-preflight 全绿 + CI 全绿 + tag/Release 发布完成。
4. `docs/cycle-archive/v17.0.0/` 已提交。
