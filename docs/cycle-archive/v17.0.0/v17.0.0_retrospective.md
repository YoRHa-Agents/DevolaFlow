# v17.0.0 Retrospective

* 日期:2026-08-25
* 周期:v16.0.0 → v17.0.0(MAJOR;6 个实施轮 + 1 个证据收口轮,PR #184–#189)

## 1. 识别的 gap(W-1/SI-1,`v17.0.0_gap_analysis.md`)

1. v16 弃用面(nines、legacy schema、ADR-006 shims、L3 命名、cascade legacy-4)全部到期未清。
2. 编排内核六项核查:边界保持是纯提示位(0 运行时强制)、token 注入无账目、goal/loop 无绑定、容量/阈值散落硬编码、W-24.2 措辞过期。
3. 无 npm 安装面;安装依赖 curl+bash,Windows 不可用。

## 2. 实施内容(全部落地)

| 轮 | 内容 | PR | 新测试 |
|---|---|---|---|
| R1 清理 | nines/legacy schema/23 shims/48 allowlist/L3 命名/cascade legacy-4 全退役 | #185 | 6 |
| R2 边界 | devolaflow.hostbridge 五 host 桥 + DEVOLAFLOW_HOST_ENFORCE + 层预算断言(ALB001) | #186 | 27 |
| R6 npm | @yorha-agents/devola-flow 薄安装器 + tag 触发 publish CI + C-6 第 7 同步位 | #184 | 10 |
| R3 注入 | AGENTS.md slice 接通 select_context + host_rule_tokens/slice_savings_pct 账本 + fold 配置化 | #187 | 10 |
| R4 focus/loop | session resume 适配(Cursor/Claude)+ checkpoint_round_pass + GOAL_DRIFT | #188 | 12 |
| R5 模型边界 | meta.capacity SSOT reader + meta.probe_models sweep + W-24.2 re-defer 裁决 | #189 | 13 |
| R7 收口 | 真实账本(13 条)+ W-2 评估 9.84 + 交叉验证 PASS + W-16 结算 + 对照表 | 本 PR | 0 |

W-17 周期账:78 ≤ 150;每轮 ≤ 30。W-18:六轮 ghost pin 全部先于 CHANGELOG。A-2:Tier-A witnesses 全周期未触碰。

## 3. 延期项及原因(W-21 telegraph 位)

1. **Pattern 3 pool 运行时(W-24.2)**:正式 re-defer 至 v18+,门槛 = SI-1 立据 + 持久态 schema 设计 + 独立 pool 执行器路径。本周期出具裁决并修正 v12+ 过期措辞——若 v18 议案涉及 Soul 层调整,本条即 2-cycle telegraph 起点。
2. **capacity guard 化的 tier 计数整合**:R5 刻意不改 quantifiable_ratio 计数(避免重 pin 30+ fixture);v18 SI-1 候选。
3. **Codex/Kimi/DSH session 位接入**:R4 降级(Codex trust 摩擦、Kimi 用户级 Beta、DSH 走插件后续);`references/host-bridges.md` §8 有据。
4. **npm 首发**:@yorha-agents 组织未建(用户侧一次性手工步骤);publish workflow 已就位,组织就绪后 tag 自动发。
5. **计时 flake 加固**:`test_dispatch_parallel_completes_in_max_not_sum` 1.8× 壁钟断言建议 v17.0.1 改逻辑重叠断言。

## 4. 关键学习

1. **暗配置模式成为标准动作**:advisory_fold(R3)→ capacity(R5)→ probe_models(R5)三处同构——reader 先行、YAML 键缺省、行为字节等价、提案 allowlist 后接。新配置面零迁移成本。
2. **W-18 ghost pin 先行 + 每轮 CHANGELOG 备存文本**让 MAJOR 收口的 rollup 变成纯粘贴:六轮文本在各 design doc §3.6 备好,收口零回忆成本。
3. **每轮独立 PR + admin merge** 的串行节奏(设计 → 派发 → 门禁 → PR → 合并 → 下轮分支)与研究并行(下轮调研在上轮实现期间跑)配合,7 轮在单日完成且互不污染。
4. **真实账本经真实代码路径**(build_dispatch_record)生成,新字段(host_rule_tokens/slice_savings_pct/capacity_profile)由 v17 代码自动计算——证据与实现天然一致,交叉验证无需手工对齐。
5. **边界收紧的顺序**(先 R2 桥/断言,再 R3 账目,再 R4 闸内自动化)是正确的依赖序:每轮复用上一轮的面(session 适配复用 hostbridge 安装器;账本字段复用遥测管道)。

## 5. 数字

* 测试:5100 → 5148(+78 新函数,−30 删除折抵);全量 94.6s。
* 代码:src 77 文件 +2943/−1797;tests 64 文件 +3217/−1901。
* 运行时强制面:0 → 7。
* 注入节省:slice 均值 49.6%(documentation 83.0 / feature 35.8 / refactor 33.5)。
* 评估:机器 composite 9.84(READY)/人工 9.57;交叉验证 vs v16 companion max delta 1.0 PASS。
