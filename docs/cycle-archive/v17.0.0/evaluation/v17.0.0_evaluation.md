# v17.0.0 W-3 / SI-3 发布评估

* 日期：2026-08-25
* 评估人：L0 cycle-lead
* 阈值：MAJOR ≥ 9.0(加权综合)
* 机器证据:`.local/research/v17.0.0_harness_evaluation.json`(W-2 内建评估器,composite 9.84 READY)
* 对照数据:`.local/research/v17.0.0_efficiency_comparison.json`
* 交叉验证:`.local/research/v17.0.0_harness_w3_cross_validation.json`(vs v16 W-3 companion,max delta 1.0,PASS)

## 1. 六维评分

| 维度 | 权重 | 机器分 | 人工判定 | 依据 |
|---|---|---|---|---|
| 代码质量 | 0.20 | 10.0 | 9.6 | ruff lint/format 全绿;coverage 87.33%(CI);R1 净清理 src −1797 行删除;机器分未计入 CI 计时 flake(test_async_dispatch_executor 1.8× 断言在共享 runner 抖动)——人工下调 0.4 |
| 架构合理性 | 0.20 | 9.29 | 9.5 | 五轮全部走 NEST/暗配置/A-5 单 owner 模式;canonical_order 全周期不动;W-24.2 出具正式 re-defer 裁决修正过期措辞;quantifiable_ratio 账本值压低机器分,但 guard 化路径(capacity/probe 配置面)是刻意的克制设计 |
| 测试充分性 | 0.20 | 10.0 | 9.7 | 5148 通过;周期 +78 新测试函数 ≤ 150(每轮 ≤ 30);全部触及模块 ≥ 80% 覆盖;W-18 ghost pin 六轮先行;扣 0.3:host 桥的五 host 中 Codex/Kimi 仅配置面测试,无端到端宿主验证 |
| 可维护性 | 0.15 | 9.86 | 9.6 | 23 shim 退役 + 48 allowlist 清除 + L3 命名清零;规则源(.rules/)与编译面全程零漂移;暗配置模式统一(advisory_fold/capacity/probe_models 三处同构) |
| 兼容性 | 0.10 | 10.0 | 9.4 | 五个 BREAKING 面全部在 MAJOR 语义内且有 LegacyChangeLayoutError 类显式失败;旧 checkpoint(无 goal_hash)/旧账本(无新字段)/旧 dispatch(无 cascade 字段)三面向后兼容测试齐;扣 0.6:cascade legacy-4 与 legacy schema 是硬移除,依赖 v16 已弃用一个 MAJOR 的前置 |
| 性能影响 | 0.15 | 10.0 | 9.5 | 全量测试 90→94.6s(+5%,主要来自 +48 测试);运行时新增面全部惰性(暗配置 reader 缓存、session 零 IO 闸、hostbridge fail-open);slice 注入节省均值 49.6% 是净收益 |

**加权综合(人工)= 9.6×0.20 + 9.5×0.20 + 9.7×0.20 + 9.6×0.15 + 9.4×0.10 + 9.5×0.15 = 9.57 ≥ 9.0 → 通过**
(机器综合 9.84;两者取更保守的人工判定为准。)

## 2. v17 vs v16 效率对照(G17 内核六项核查闭环)

| 维度 | v16 | v17 | 判定 |
|---|---|---|---|
| 主 agent 边界保持 | 提示位规则(0 个运行时强制点) | 7 个运行时强制面(五 host 桥 + 层预算断言 + session resume 闸) | 正向,质变 |
| 派发约束与上下文管控 | 层预算仅遥测记录 | pre_dispatch 强制断言(ALB001 blocker)+ capacity/stop-guard 配置化 | 正向 |
| token 注入约束 | AGENTS.md 全量注入(≈12,430) | slice 账目接通:均值节省 49.6%(documentation 83.0 / feature 35.8 / refactor 33.5),账本可审计 | 正向 |
| goal/loop 工具结合 | goal.md 无绑定,checkpoint 手动 | goal_hash 漂移检测(GOAL_DRIFT)+ checkpoint_round_pass 组合 API + host session 注入 | 正向 |
| 对历史版本效率收益 | composite 9.70 | composite 9.84(机器)/9.57(人工);测试 5100→5148(+48);src 净变化 +2943/−1797(清理与新增并行);交叉验证 PASS | 正向 |
| harness/skill 与模型能力边界 | advisory fold 硬编码双档 | fold/capacity/probe_models 三面配置驱动 + 提案闭环按 model_hint 调优 + W-24.2 正式裁决 | 正向 |

## 3. 结论

**v17.0.0 READY**:机器 9.84 / 人工 9.57,双双越过 MAJOR 阈值 9.0;六项内核核查全部正向;无 INSUFFICIENT 维度。已知残留(不阻塞发布,入 retrospective §3):
1. npm 首发待 @yorha-agents 组织创建(用户侧手工步骤;tag 触发的 publish workflow 将在组织就绪后随下一 tag 或手动 re-run 完成)。
2. `test_dispatch_parallel_completes_in_max_not_sum` 的 1.8× 壁钟断言在共享 CI runner 有复发风险,建议 v17.0.1 加固(放宽比值或改用逻辑重叠断言)。
3. Codex/Kimi host 桥无端到端宿主验证;session 位五 host 中仅接 Cursor/Claude(R4 §D-R4-1 降级说明)。
