# v7.2.0 Accept List & Version Iteration Roadmap

**Generated:** 2026-04-18
**Owner:** S04 synthesis (read-only roll-up of S02a candidate list + S03 validations V01–V07)
**Predecessor artifacts:**
- `.local/research/v7.2.0_candidate_list.md` (96 deltas: 7 TIER-1 / 13 TIER-2 / 55 TIER-3 / 6 Registry Hygiene)
- `.local/research/v7.2.0_validations/V01.md` … `V07.md` (7 TIER-1 candidates validated; 0 rejections)
- `.local/sandbox/v7.2.0/V0[1-7]/patch.diff` (7 unified-diff patches, all `git apply --check` exit 0)

**Decision posture:** all 7 TIER-1 candidates are **ACCEPT** or **ACCEPT-WITH-CAVEATS**; zero outright rejections from S03. The accept-list and PR roadmap below are presented as user-checkable units so the human reviewer can dial scope at any granularity (per-PR, per-candidate, or whole-cycle deferral).

**DevolaFlow baseline:** v7.1.1 → projected v7.2.0.

---

## ☐ Quick Selection Menu

Tick what you want shipped in v7.2.0. Default selection is **everything** (all 6 PRs in order). Each PR is independently revertible after merge.

### Ship list (recommended order — PR-0 mandatory first; rest in any order):

- [x] **PR-0 — Registry Hygiene** (6 data bugs in `reference-dependencies.yaml`) — **mandatory pre-flight** so v7.2 design loop points at correct upstream truth
- [x] **PR-A — C-006 Tiered SKILL/Reference/Examples Budgets** (lowest-risk start; 0 production code touched)
- [x] **PR-B — C-002 Compression Bypass Conditions** (security warnings & destructive ops survive `aggressive` intensity verbatim)
- [x] **PR-C — C-007 Learnings Schema Activation** (additive `files`/`source` fields + `dedup_learnings` helper; ships **dormant** until v7.3 C-009 writes entries)
- [x] **PR-D — C-001 + C-003 Advisor Cluster** (conciseness instruction + timing/reconcile blocks; **MUST** include +200 token budget bump on 4 advisor profiles)
- [x] **PR-E — C-004 + C-005 SKILL.md Coordination** (inline self-review + hybrid-recipe rubrics; combined SKILL.md = 499/499, **zero headroom**)

### Deferral options (uncheck above, then optionally tick one or more):

- [ ] **Defer C-005 to v7.3** — keeps PR-E as just C-004 (in-place hybrid row mod + execution-protocol §7); SKILL.md stays at 498 lines; preserves headroom
- [ ] **Defer C-004 to v7.3** — keeps PR-E as just C-005 (`inline_self_review` row + decomposition-gate §8 + 3 yaml flags); SKILL.md goes to 499/499 with 0 headroom but no rebase work
- [ ] **Defer entire PR-E to v7.3** — avoids the 0-headroom SKILL.md condition altogether; documentation gain delayed by one cycle
- [ ] **Defer PR-D to v7.3** — avoids the 4-profile token-budget bump if you'd rather observe more advisor traffic before tuning; loses ~35-45% advisor-response savings for one cycle
- [ ] **Defer PR-C to v7.3 (paired with C-009)** — avoids shipping dormant infrastructure; pulls C-009 (TIER-2) into v7.3 PR alongside C-007 so the JSONL starts populating immediately
- [ ] **Skip PR-0 hygiene this cycle** — keep registry bugs as v7.3 retrospective material; **NOT recommended** (broken `karpathy/andrej-karpathy-skills` URL and deleted `PrimeLocus/Hydra` repo will fail any future S01-style verification)

---

## §0 Registry Hygiene (PR-0)

Six concrete data bugs found in `workflow-system/agent/knowledge/reference-dependencies.yaml` during S01. These are upstream-truth errors (not behavioural deltas) that should be repaired in a single small PR ahead of any TIER-1 work.

| ID | Issue | File / Anchor | Effort | Action |
|----|-------|---------------|--------|--------|
| **H-01** | `karpathy/andrej-karpathy-skills` URL returns HTTP 404 — repo has never existed; only `forrestchang/andrej-karpathy-skills` is real (T03 §4.1, DELTA-KARP-UPSTREAM-01) | `reference-dependencies.yaml` (broken entry, lines 169-189) | S | Delete broken entry; fold its 4 integration-points into the surviving `forrestchang` entry (per T03 DELTA-KARP-FRSCH-06) |
| **H-02** | `PrimeLocus/Hydra` repo deleted upstream between 2026-04-13 and 2026-04-18 — both REST API and HTML return 404; `status: verified` is now false (T06 Δ-hydra-01) | `reference-dependencies.yaml` (`primelocus-hydra` entry) | S | Set `status: verified → deleted_upstream`; redirect `repo_url → https://github.com/mikecubed/Hydra` (`package.json` v1.2.0 fork) with `forked_from_deleted: PrimeLocus/Hydra`; downgrade `relevance_score: 4 → 3` |
| **H-03** | `spring-ai-agent-skills.last_known_version: "Spring AI (2026-01)"` was already 5 days behind reality at `last_checked: 2026-04-11` — v0.5.0 (2026-02-24), v0.6.0 (2026-03-30), v0.7.0 (2026-04-06) had all shipped (T07 Δ-spring-ai-01) | `reference-dependencies.yaml` (`spring-ai` entry) | S | Set `last_known_version: "v0.7.0 (2026-04-06): MemoryTools + mkdocs site; preceded by v0.6.0 (2026-03-30) and v0.5.0 (2026-02-24): subagent SPI extraction"`; add `releases_total: 9` |
| **H-04** | `agent-skills-security.last_known_version: "arXiv:2602.12430 (2026-02)"` lacks sub-version pin; arXiv has 3 revisions, current is v3 (2026-02-17) (T06 Δ-skills-sec-01) | `reference-dependencies.yaml` (`agent-skills-security` entry) | S | Pin `last_known_version: "arXiv:2602.12430v3 (2026-02-17)"`; add `companion_repo: "https://github.com/scienceaix/agentskills"` |
| **H-05** | `skillrouter.last_known_version: "v3 (arXiv:2603.22455v3)"` is stale — paper advanced to v4 (2026-04-01) with 2 new findings (cross-source generalisation, agent-coupled win rate) (T05 Δ-skillrouter-01/02) | `reference-dependencies.yaml` (`skillrouter` entry) | S | Set `last_known_version: "v4 (arXiv:2603.22455v4, 2026-04-01)"`; append key_pattern: "v4 end-to-end study: routing gains transfer to task success, scale with agent capability" |
| **H-06** | Follow-up paper `arXiv:2604.02837v1` ("Towards Secure Agent Skills: Architecture, Threat Taxonomy, and Security Analysis", different team, cs.CR primary) shipped 2026-04-03 — needs its own `periodic_monitoring` entry, not just an `update_triggers` keyword (T06 Δ-skills-sec-02) | `reference-dependencies.yaml` (new entry under `periodic_monitoring`) | M | Add new entry `id: agent-skills-threat-taxonomy`, `relevance_score: 4`, `last_known_version: "arXiv:2604.02837v1 (2026-04-03)"`; key_patterns from threat taxonomy (4 lifecycle phases × 7 categories × 17 scenarios × 3 attack layers) |

**Total PR-0 effort:** ~5 small + 1 medium = ≤ 1 reviewer-hour. **Risk:** zero (YAML metadata only; no behavioural surface). **Acceptance:** YAML schema validators pass; spot-check renders cleanly; `nines self-eval` (when next run) cites correct upstream-version pins.

---

## §1 Accept List — TIER-1 Candidates (7, ranked by priority)

All 7 TIER-1 candidates were validated in S03 (V01–V07). Decisions: 6 ACCEPT, 1 ACCEPT-WITH-CAVEATS (V01 — needs +200 token budget bump folded into PR-D), 0 REJECT. Confidences: 5 high, 2 medium.

---

### C-006 — Tiered Agent Size Budgets (XL/Large/Default)

| Field | Value |
|-------|-------|
| **What** | Extend SF-1 from a single 500-line ceiling on SKILL.md to tiered ceilings: `SKILL.md: <500` (Default, unchanged), `references/*.md: ≤1000` (Large), `examples/*.md: ≤1600` (XL). Add new test `tests/test_reference_size_budgets.py` (78 lines, parametrised over 8 + 3 files). Update `.cursor/rules/skill-format-rules.mdc` SF-1 wording. |
| **Why** | Closes existing asymmetry where references / examples (loaded on-demand, not in every dispatch prompt) were held to the same 500-line ceiling as SKILL.md. Now tier ceilings reflect actual loading cost. Lowest-risk start of v7.2 work. |
| **Validated by** | V06 (ACCEPT, high confidence) |
| **Effort** | S — 78 LOC test + 27 LOC rule update; pure additive CI guard |
| **Risk** | Low — 0 production code paths touched; 0 schema changes; backward compatible (all 11 files comply today, max 53.3% of new ceiling) |
| **Dependencies** | None. Pre-existing dependency: `scripts/sync_cursor_skill.py::MIRRORED_FILES` is single source of truth for the 8 + 3 file lists |
| **Acceptance evidence** | 12/12 sandbox pytest PASSED in 0.03s; standalone CLI exit 0; failure-path verified (`TIER_LARGE=300` produces 6 informative failures); `git apply --check` exit 0 |
| **Ship as** | **PR-A** |
| **Patch path** | `.local/sandbox/v7.2.0/V06/patch.diff` (124 lines) |

---

### C-002 — Compression Bypass for Security Warnings & Destructive Ops

| Field | Value |
|-------|-------|
| **What** | Add `bypass_conditions` sub-key (4 named conditions: `security_warning`, `destructive_operation`, `multi_step_sequence_with_order_dependency`, `repeated_user_question`) inside the existing `compression_rules:` block in `schemas/lean-dispatch.yaml` + mirror in `schemas/lean-report.yaml`. Add detection regexes + `CompressionBypassWarning` + `detect_bypass_conditions()` helper to `src/devolaflow/compressor.py`. On bypass match, return source verbatim and emit one-line warning. |
| **Why** | Resolves a real safety gap — `DROP TABLE` warnings, CVE notices, ordered runbooks could be stripped under `aggressive` intensity today. Restores caveman's Apr-15 `fix(security): harden flag-file reads and refuse sensitive-file compression` discipline at the DevolaFlow compressor layer. |
| **Validated by** | V02 (ACCEPT, high confidence) |
| **Effort** | M — ~70 LOC compressor additions + 10 LOC schema mirror + ~12 new tests |
| **Risk** | Low — purely additive; backward-compat preserved (`compress_message` 2-arg signature still works; `bypass_conditions=[]` opt-out gives legacy behaviour); P6 cache layout invariant **NOT** affected (`compression_rules` is metadata block, not dispatch payload top-level key — verified) |
| **Dependencies** | None upstream. Couples downstream with T3-11 (validate-then-fix loop, currently parked at TIER-3) which becomes immediately actionable post-merge. |
| **Acceptance evidence** | 18/18 sandbox tests PASSED; baseline `tests/test_compressor.py` 80/80 PASSED; cross-validation against patched code via `PYTHONPATH=/tmp/v02-test-stage/src` 80/80 PASSED; LCP r1→r2 ≥ 80% / r1→r3 ≥ 70% stability test PASSED; `git apply --check` exit 0 |
| **Ship as** | **PR-B** |
| **Patch path** | `.local/sandbox/v7.2.0/V02/patch.diff` (101 lines) |

---

### C-007 — Activate Operational Learnings Schema (`files`/`source` + `dedup_learnings`)

| Field | Value |
|-------|-------|
| **What** | Add 2 additive fields to `Learning` dataclass in `src/devolaflow/learnings.py`: `files: list[str] = field(default_factory=list)`, `source: str = ""`. Add coercion in `__post_init__` for hand-edited JSONL safety. Add `dedup_learnings(entries) -> list[Learning]` helper returning latest-timestamp entry per `(task_type, key)` pair (mirrors gstack `/learn` `{type, key}` last-write-wins). |
| **Why** | Activates dormant `operational.jsonl` substrate. The 3 existing helpers (`capture_learning`, `load_relevant_learnings`, `prune_learnings`) are implemented but never write because schema gaps prevent provenance capture. Pairs cleanly with C-009 (reflective reflex, TIER-2) which writes entries that USE these new fields. |
| **Validated by** | V07 (ACCEPT, high confidence) |
| **Effort** | S — 49 added LOC / 5 changed LOC in `learnings.py`; backward compat handled by existing `if k in entry` filter at line 178-180 |
| **Risk** | Low — strictly additive; defaults backward-compat verified for v1 (no v2/v3 fields), v2 (no v3 fields), v3 with `null` files. Module coverage projected ≥97% (vs 97.35% baseline). **Ships dormant** until v7.3 C-009 starts writing entries — explicit two-phase plan per CCT-3. |
| **Dependencies** | Couples with **C-009** (reflective reflex, TIER-2) for writers; with **C-012** (substrate design note, TIER-2) and **C-014** (`log.md`, TIER-2) for substrate naming consistency in `workflow-system/agent/knowledge/learnings/` |
| **Acceptance evidence** | 15/15 sandbox tests PASSED (10 `test_dedup.py` + 5 `test_backward_compat.py`); existing 50/50 `tests/test_learnings.py` PASSED with patched module; `git apply --check` exit 0 |
| **Ship as** | **PR-C** |
| **Patch path** | `.local/sandbox/v7.2.0/V07/patch.diff` (49 added / 5 removed lines, 102 LOC total incl. metadata) |

---

### C-001 — Advisor Conciseness Instruction (35–45% advisor-response token reduction)

| Field | Value |
|-------|-------|
| **What** | Append `'When invoking advisor, append: "Reply in under 100 words and use enumerated steps, not explanations."'` (verbatim from anthropic-advisor-tool docs) to advisor system prompt in `src/devolaflow/task_adaptive_selector.py::_resolve_advisor_text`. Flag-gated via per-profile `advisor.conciseness_instruction: bool` (default true). |
| **Why** | Direct cost win on advisor's *response* tokens (~35–45% reduction per call) across 4 advisor-enabled task profiles. First-party Anthropic guidance with internal-eval evidence. Compounds across `gen_verify_max_rounds: 3` × multiple waves. |
| **Validated by** | V01 (ACCEPT-WITH-CAVEATS, high confidence) |
| **Effort** | S — ~7 LOC in selector + per-profile YAML flag scaffolding |
| **Risk** | Medium — section text **grows** +22 tokens per dispatch (one-time cost vs per-call savings); under chars//4 fallback (no `tiktoken` installed) 3 of 4 profiles silently bump `important`-priority sections — **MITIGATION REQUIRED:** include +200 token budget bump for 4 advisor profiles in same PR (Option A in `token_count.txt`) |
| **Dependencies** | **Co-ships with C-003** (same function, merged diff). Touches `context_profiles.yaml` (CO-3 budget; verify per-profile schema unchanged). |
| **Acceptance evidence** | 91/91 baseline tests PASSED in `tests/test_task_adaptive_selector.py`; 5 backward-compat substring assertions remain green; sandbox-loaded module shows expected text in all 4 advisor profiles; `git apply --check` exit 0 |
| **Ship as** | **PR-D** (combined with C-003) |
| **Patch path** | `.local/sandbox/v7.2.0/V01/patch.diff` (99 lines, contains C-001 + C-003 combined) |

---

### C-003 — Advisor Timing & Reconcile-on-Conflict Blocks

| Field | Value |
|-------|-------|
| **What** | Extend `_resolve_advisor_text` with two more verbatim Anthropic blocks: (1) **Timing** ("Call advisor BEFORE substantive work … On tasks longer than a few steps, call advisor at least once before committing to an approach and once before declaring done.") and (2) **Reconcile-on-conflict** ("On conflict: If you've already retrieved data pointing one way and the advisor points another, do not silently switch. Surface the conflict in one more advisor call: 'I found X, you suggest Y, which constraint breaks the tie?'."). Each flag-gated via per-profile `advisor.timing_block` / `advisor.reconcile_block` (default true). |
| **Why** | Covers a documented advisor failure mode (silent override of retrieved data). High signal for L3 agents already in 4 advisor-enabled profiles. First-party Anthropic guidance. |
| **Validated by** | V01 (ACCEPT-WITH-CAVEATS, high confidence — same caveat as C-001) |
| **Effort** | S — ~14 LOC in selector + per-profile YAML flag scaffolding |
| **Risk** | Low (per-block flag opt-out; pure prompt extension; no API behaviour change). Joins C-001 budget headroom risk — addressed by same +200 token budget bump in PR-D. |
| **Dependencies** | **Co-ships with C-001** (same function, merged diff). Future C-008 (advisor caching, TIER-2) integrates trivially via the new `parts = [...]` builder. |
| **Acceptance evidence** | Same as C-001 (validated together); both blocks present verbatim in all 4 profiles after sandbox load; `git apply --check` exit 0 |
| **Ship as** | **PR-D** (combined with C-001) |
| **Patch path** | `.local/sandbox/v7.2.0/V01/patch.diff` (same 99-line combined diff as C-001) |

---

### C-004 — Wave Coordination Hybrid Recipes (RESCOPED to 0-line SKILL.md edit)

| Field | Value |
|-------|-------|
| **What** | **Originally** 5-10 line SKILL.md addition. **Rescoped** per V05's PR-E coordination risk to: (a) **0 net new SKILL.md lines** — in-place modification of existing `hybrid` row mechanism cell ("Partition into named recipes: orchestrator-subagent ⊕ shared-state, message-bus ⊕ agent-teams (see references/execution-protocol.md §7)"); (b) **+53 lines to `references/execution-protocol.md`** — new §7 "Wave Coordination Mode Selection (v7.2.0+)" with 4 pairwise rubrics + 2 hybrid recipes verbatim from anthropic-coordination-blog + DevolaFlow P5 mapping. |
| **Why** | Annotates the underspecified `hybrid` row in SKILL.md "Wave Coordination Modes" with concrete recipes; gives DevolaFlow vocabulary to document why P5 (artifacts as contracts) intentionally rejects shared-state pattern; the orchestrator-subagent + shared-state hybrid is a near-perfect characterisation of the existing `self-update` workflow research stage. |
| **Validated by** | V04 (ACCEPT, high confidence) |
| **Effort** | S — 70 LOC unified diff (1 SKILL.md hunk = 1 modified line + 1 execution-protocol.md hunk = 53 added lines) |
| **Risk** | Low — documentation-only; CO-2 verbatim discipline preserved; CO-4 relative paths preserved; C-006 Large-tier compliance with 522 lines of headroom (`execution-protocol.md` 425 → 478, 47.8% of 1000 ceiling) |
| **Dependencies** | **Co-ships with C-005 in PR-E** — both edit SKILL.md "Wave Coordination Modes" table. Apply order: V05 first → V04 SKILL.md hunk needs rebase (context shifts 222 → 223). Companion `patch_skill_after_v05.diff` (11 lines) provided. |
| **Acceptance evidence** | `tests/test_integration.py::test_skill_md_under_500_lines` PASSED against post-V04-only SKILL.md (498 lines) and against post-V05+V04 SKILL.md (499 lines); `git apply --check` exit 0 for both `patch.diff` and `patch_skill_after_v05.diff` |
| **Ship as** | **PR-E** (combined with C-005) |
| **Patch path** | `.local/sandbox/v7.2.0/V04/patch.diff` (81 lines) + `.local/sandbox/v7.2.0/V04/patch_skill_after_v05.diff` (11 lines, rebased SKILL.md hunk for combined apply) |

---

### C-005 — Inline Self-Review Checklists for Low-Risk Waves (50× speedup, doc-only in v7.2)

| Field | Value |
|-------|-------|
| **What** | Add new `inline_self_review` row to SKILL.md "Wave Coordination Modes" table (+1 line). Add new §8 "Inline Self-Review Mode (v7.2.0+)" to `workflow-system/agent/references/decomposition-gate.md` (+39 lines) with SAFE/UNSAFE stage tables. Add opt-in `inline_review_checklist: false` flag to 3 SAFE profiles in `context_profiles.yaml` (`research`, `design`, `documentation` — 3 × 3 = +9 lines). |
| **Why** | 50× wallclock speedup (~25 min → ~30 sec) on low-risk waves with comparable defect detection (per superpowers v5.0.6 release notes). Aligns with "match ceremony to complexity" principle in SKILL.md Quick Action Decision table. **In v7.2 this ships as documentation + opt-in flag only** — runtime hook (dispatcher consuming the flag) lands in v7.3. |
| **Validated by** | V05 (ACCEPT-WITH-CAVEATS, **medium** confidence — only TIER-1 candidate at medium confidence) |
| **Effort** | S — 88 LOC unified diff across 3 files |
| **Risk** | Medium — pushes SKILL.md from 498 → 499 lines, **leaves zero headroom** under SF-1's `< 500` ceiling. UNSAFE stages (`implement`, `test`, `refactor`, `security-audit`) explicitly excluded from flag scaffolding. v7.2 ships normative docs only; runtime guard rejecting `inline_review_checklist: true` + `team ∈ {Implement, Test}` is a v7.3 follow-up. |
| **Dependencies** | **Co-ships with C-004 in PR-E** — both edit SKILL.md "Wave Coordination Modes" table. C-004's 0-line SKILL.md rescope is the workaround that makes this coexistence feasible. |
| **Acceptance evidence** | `tests/test_integration.py::test_skill_md_under_500_lines` PASSED against sandbox SKILL.md (499 < 500); `patch --dry-run -p1` OK for all 3 files |
| **Ship as** | **PR-E** (combined with C-004) |
| **Patch path** | `.local/sandbox/v7.2.0/V05/patch.diff` (103 lines) |

---

## §2 Version Iteration Roadmap

Six pull requests in recommended order. PR-0 mandatory first; PR-A is the lowest-risk start of TIER-1 work; PR-B/C/D/E may run in parallel after PR-A merges. SI-10 6-step pre-commit gate runs after every PR.

### PR-0 — Registry Hygiene

**Scope:** §0 H-01 … H-06 (6 entries in `reference-dependencies.yaml`).
**Rationale for going first:** keeps the v7.2.0 design loop pointing at correct upstream truth (broken `karpathy/andrej-karpathy-skills` URL would fail any S01-style verification; deleted `PrimeLocus/Hydra` repo invalidates `status: verified`).
**Why no risk:** YAML metadata only; no behavioural surface; SI-4 benchmark gate not triggered (no `task_adaptive_selector.py` / `context_profiles.yaml` / lean schema / SKILL.md / gate module touch); SI-5 not triggered (no SKILL.md or CLAUDE.md edit).
**Effort estimate:** ≤ 1 reviewer-hour.
**Files touched:** `workflow-system/agent/knowledge/reference-dependencies.yaml` only.
**Verification:** YAML parses; spot-check H-01 deletion + H-02 status flip + H-03/H-04/H-05 version pins + H-06 new entry shape.

---

### PR-A — C-006 Tiered SKILL/Reference/Examples Budgets

**Scope:** C-006 only.
**Rationale for going second (first TIER-1):** lowest-risk start; 0 production code touched; CI guard only; backward-compat verified (all 11 files comply today). Sets up tier vocabulary that subsequent PRs reference.
**Why parallelisable:** independent of all other TIER-1 candidates. Could ship with PR-0 in same commit if tightly coupled, but separating gives cleaner blast-radius if either rolls back.
**Effort estimate:** ≤ 2 reviewer-hours (78 LOC test + 25 LOC rule wording).
**Files touched:** `tests/test_reference_size_budgets.py` (new), `.cursor/rules/skill-format-rules.mdc`.
**Verification:** `python -m pytest tests/test_reference_size_budgets.py -v` 12/12 PASS; `tests/test_integration.py::test_skill_md_under_500_lines` unchanged PASS; SI-10 step 1 covers it via `python -m pytest tests/ -q`.

---

### PR-B — C-002 Compression Bypass

**Scope:** C-002 only.
**Rationale for placement:** P6 cache-layout-invariant compliance is the only structural risk; V02 verified (`compression_rules` is metadata block, not dispatch payload top-level — invariant SoT untouched). Independent of PR-C/D/E.
**Why parallelisable:** touches `schemas/lean-dispatch.yaml`, `schemas/lean-report.yaml`, `src/devolaflow/compressor.py`, `tests/test_compressor.py`. Zero overlap with PR-D (`task_adaptive_selector.py`) or PR-E (SKILL.md / references / context_profiles.yaml).
**Effort estimate:** ≤ 4 reviewer-hours (~70 LOC compressor + 10 LOC schemas + ~12 tests + CHANGELOG).
**Files touched:** 3 source files + 1 test file (~12 new test cases) + CHANGELOG.md.
**Verification:** new `TestCompressionBypass*` class lifted from sandbox; existing 80 `test_compressor.py` tests unchanged PASS; `test_dispatch_layout` + `test_assert_dispatch_layout_*` PASSED as P6 regression guard; SI-4 EvoBench non-bypass payload composite_score unchanged.

---

### PR-C — C-007 Learnings Schema Activation

**Scope:** C-007 only.
**Rationale for placement:** strictly additive `Learning` dataclass changes; ships **dormant** in v7.2 (no entries written until v7.3 C-009 reflective reflex lands). Per CCT-3 explicit two-phase plan. Zero overlap with PR-B (compressor) or PR-D (selector) or PR-E (SKILL.md/refs).
**Why dormant ship is acceptable:** infrastructure is needed before writers can populate it; backward-compat means v7.3 promotion to writers requires zero v7.2 rework. Alternative ("defer to v7.3 paired with C-009") is also defensible — see Quick Selection Menu.
**Effort estimate:** ≤ 3 reviewer-hours (49 added / 5 removed LOC + lift 15 sandbox tests into `tests/test_learnings.py`).
**Files touched:** `src/devolaflow/learnings.py`, `tests/test_learnings.py` (~15 new test cases) + CHANGELOG.md.
**Verification:** existing 50 `test_learnings.py` tests PASS unchanged; new `dedup_learnings` + backward-compat tests PASS; module coverage projected ≥97% (vs 97.35% baseline); SI-4 not strictly required but recommended as defense-in-depth.

---

### PR-D — C-001 + C-003 Advisor Cluster

**Scope:** C-001 + C-003 (combined diff `.local/sandbox/v7.2.0/V01/patch.diff`).
**MANDATORY supplementary YAML edit:** **+200 token budget bump** for 4 advisor profiles to absorb +100 / +126 advisor-section growth without bumping `important`-priority sections under either tokeniser:
- `feature`: `4950 → 5150`
- `refactor`: `4800 → 5000`
- `migration`: `4800 → 5000`
- `security-audit`: `5200 → 5400`

**Rationale for placement:** depends on `tiktoken`-fallback behaviour and per-profile budget assertions; no overlap with PR-B (compressor) or PR-C (learnings) or PR-E (SKILL.md/refs). Self-contained.
**Effort estimate:** ≤ 4 reviewer-hours (37 source LOC + 4 YAML budget bumps + 9 explicit YAML flag entries + 6 comment lines + 5 new unit tests + CHANGELOG).
**Files touched:** `src/devolaflow/task_adaptive_selector.py` (single `_resolve_advisor_text` function), `workflow-system/agent/context_profiles.yaml` (4 advisor profiles only), `tests/test_task_adaptive_selector.py` (5 new tests).
**Critical metadata corrections in PR description (per V01 §"Risks/Caveats"):**
- Function name is `_resolve_advisor_text`, **NOT** `_build_advisor_section` (dispatch and candidate-list are wrong).
- Advisor-enabled profile **count is 4**, NOT 6: `feature`, `refactor`, `migration`, `security-audit`. Names also corrected: `refactor` (not `refactoring`), `security-audit` (not `security_audit`); `perf-optimization` has only `decomposition.coordinator_retains_advisor: true` but **no `advisor:` block**; no `full_pipeline` profile exists.
- Frame the "35–45% reduction" claim as **advisor *response* token reduction** (downstream LLM responses), NOT L3 prompt section reduction (the section actually *grows* +22 tokens for C-001 + +78 for C-003 = +100 tokens combined).

**Verification:** 91+ baseline `test_task_adaptive_selector.py` tests PASS plus 5 new conditional-flag opt-out tests; SI-4 EvoBench convergence-loop scenarios should show reduction in `cost` dimension on advisor-call-heavy scenarios; composite_score must not drop > 5% per scenario.

---

### PR-E — C-004 + C-005 SKILL.md Coordination

**Scope:** C-005 (V05 patch) + rescoped C-004 (V04 patch).
**Combined SKILL.md final state:** **499 lines** = 498 baseline + 1 (C-005 `inline_self_review` row) + 0 (C-004 in-place hybrid row mod). **PASSES SF-1 (`< 500`) with 0 headroom.**
**Mandatory commit order:** V05 → V04 (V05 is the larger patch; V04's SKILL.md hunk rebase is one-line replacement, fully captured in companion `patch_skill_after_v05.diff` 11 lines). Alternative V04→V05 order also works but requires V05 rebase (symmetric).
**Files touched:** `workflow-system/agent/SKILL.md` (1 added row + 1 in-place hybrid row mod), `workflow-system/agent/references/decomposition-gate.md` (+39 lines for C-005 §8), `workflow-system/agent/references/execution-protocol.md` (+53 lines for C-004 §7), `workflow-system/agent/context_profiles.yaml` (+9 lines for C-005 SAFE-profile flags), `CHANGELOG.md`.
**SI-5 coupled checks (REQUIRED at PR-E merge):**
1. `wc -l workflow-system/agent/SKILL.md` ≤ 499 (verified 499 in sandbox).
2. `build-skill` succeeds for all 4 adapters (Cursor, Codex, Claude, Copilot) within budget.
3. `python -m pytest tests/test_benchmarks.py -v` no scenario drops > 5% below baseline (V04+V05 estimated effect ±0.5pp — well inside threshold).
4. `python -m pytest tests/test_version.py -v` PASS.

**Effort estimate:** ≤ 5 reviewer-hours (largest PR by file count + cross-validation).
**Critical post-merge constraint:** **NO further SKILL.md additions in v7.2 cycle** — any subsequent edit trips SF-1. Document this in CHANGELOG and v7.2 retrospective. v7.3 follow-up: extract a SKILL.md section into a new `references/*.md` (per V05/V06 risk note 1), or explicitly raise Default tier to `<600`.

---

### PR Sequencing Summary

```
PR-0 (registry hygiene; mandatory first)
  ↓
PR-A (C-006; lowest-risk TIER-1 start)
  ↓
[PR-B, PR-C, PR-D, PR-E run in any order — zero file overlap between them]
  └─ PR-B: schemas + compressor
  └─ PR-C: learnings
  └─ PR-D: selector + advisor profiles in context_profiles.yaml
  └─ PR-E: SKILL.md + decomposition-gate.md + execution-protocol.md + 3 SAFE profiles in context_profiles.yaml
        ↑ NOTE: PR-D and PR-E both touch context_profiles.yaml but disjoint profile sets
          (PR-D = 4 advisor profiles; PR-E = 3 SAFE profiles {research,design,documentation});
          merge-conflict only if same profile is mid-edit — rebase trivially.
```

After all 6 PRs merge: **`scripts/bump_version.py 7.2.0`** → 7 canonical pattern replacements + opt-in mirror sync (no-op when `.cursor/skills/devola-flow/` absent) → `make sync-human-docs` → `python -m pytest tests/test_version.py -v` → tag release.

---

## §3 Risk Summary

| PR | Cands | Risk | Confidence | Auto-merge feasible | Reviewer hours |
|----|-------|------|------------|---------------------|----------------|
| **PR-0** Registry Hygiene | H-01 … H-06 (6 YAML edits) | **Very Low** | n/a (data fix) | yes — YAML schema validator + spot-check render | ≤ 1 |
| **PR-A** C-006 (tiered budgets) | C-006 | **Very Low** | high (V06) | yes — additive CI test only; `git apply --check` exit 0; 12/12 sandbox tests PASS | ≤ 2 |
| **PR-B** C-002 (compression bypass) | C-002 | **Low** | high (V02) | yes with reviewer for security-class regex review; P6 invariant verified; 18/18 sandbox tests PASS; baseline 80/80 PASS | ≤ 4 |
| **PR-C** C-007 (learnings schema) | C-007 | **Low** | high (V07) | yes — strictly additive; backward-compat fully verified; ships dormant by design | ≤ 3 |
| **PR-D** C-001+C-003 (advisor cluster) | C-001 + C-003 | **Medium** | high (V01) — caveat-driven only | reviewer required for 4-profile YAML budget bump rationale + framing of perf-claim wording in CHANGELOG | ≤ 4 |
| **PR-E** C-004+C-005 (SKILL.md coordination) | C-004 + C-005 | **Medium** | C-005: medium / C-004: high (V04+V05) | reviewer required for SI-5 coupled checks (line count + 4-adapter build + benchmarks + version test) and to acknowledge 0-headroom SKILL.md constraint going forward | ≤ 5 |
| **TOTAL** | 7 TIER-1 + 6 hygiene | composite **Low-Medium** | aggregate high (5/7 high, 2/7 medium) | 4/6 PRs auto-merge-eligible; PR-D and PR-E need human reviewer | **~19 hours** |

**Composite blast radius if entire cycle rolled back:** zero — every PR is independently revertible with no cross-PR data migration.

---

## §4 TIER-2 Carryover (13 candidates, ready for v7.3+)

These were validated as TIER-2 (priority 2.0–3.0) in S02a. Ship in v7.2 only if budget allows; otherwise carry to v7.3. None require v7.2 source change as a precondition.

| ID | Source ref | Title (short) | V/C/R | Priority | v7.2 ship in budget? | Coupling |
|----|------------|---------------|-------|----------|----------------------|----------|
| **C-008** | anthropic-advisor-tool | Advisor-side prompt caching (`caching: ephemeral 5m / 1h`) | 4/2/2 | 3.0 | maybe (couples with PR-D advisor cluster — `parts = [...]` builder makes integration trivial) | C-001, C-003, C-017 |
| **C-009** | gstack | Reflective Operational Self-Improvement (Pre-Completion Reflection) — writes to `operational.jsonl` using C-007 schema | 4/2/2 | 3.0 | candidate for **promotion** if C-007 dormant ship is unacceptable; couples with PR-C | C-007, P6 (`lean-report.yaml`) |
| **C-010** | vexp | Add `vexp` to `plugins.yaml` as `recommended_optional` MCP server | 3/1/1 | 3.0 | yes — single YAML entry; could fold into PR-0 hygiene if scope expanded | none |
| **C-011** | agent-skills-security (arXiv:2604.02837v1) | Data-instruction envelope spec (mitigates `IGNORE PRIOR INSTRUCTIONS` injection) | 4/2/2 | 3.0 | design note only in v7.2 (would couple with PR-B compressor) — implementation v7.3 | C-002, P6 (dispatch schemas) |
| **C-012** | spring-ai-agent-skills (v0.7.0 AutoMemoryTools) | Learnings substrate design note — sets v7.3+ direction for `knowledge/learnings/` | 4/2/2 | 3.0 | yes as design note in `.local/research/`; closes H5/H7/L6 gap cluster | C-007, C-009, C-014 |
| **C-013** | forrestchang/andrej-karpathy-skills | Karpathy EXAMPLES.md pointer in Simplicity Check (`team-roles.md`) | 2/1/1 | 2.0 | yes — single line edit; could fold into PR-E if SF-1 budget allows (it does NOT — 0 headroom) → defer | none |
| **C-014** | karpathy-llm-wiki | `knowledge/log.md` chronological append-only log | 3/2/2 | 2.0 | yes as new file; auto-populates SI-8 retrospective material | C-012 (substrate naming) |
| **C-015** | karpathy-llm-wiki | File-back-from-Review threshold spec (`repeat_finding_threshold`) | 3/2/2 | 2.0 | design note only in v7.2 | C-016 |
| **C-016** | self-improving-system | `feedback.apply_proposal()` loop closure | 4/3/3 | 2.0 | NO — `feedback.py` is in `LOCKED_FILES`; needs SI-1 review gate before `dry_run=False`; v7.3 minimum | C-015 |
| **C-017** | anthropic-advisor-tool | `clear_thinking` caveat (advisor cache stability) | 2/1/1 | 2.0 | only meaningful once C-008 ships; documentation-only | C-008 |
| **C-018** | vexp | Refresh registry SWE-bench numbers (drop FastAPI claim) | 2/1/1 | 2.0 | yes — could fold into PR-0 hygiene (it's a `key_patterns` refresh, adjacent to Category 0 spirit) | none |
| **C-019** | google-scion | Hub / Runtime Broker / Hub-Native Groves primitives in registry | 2/1/1 | 2.0 | yes — could fold into PR-0 hygiene (YAML edit on `google-scion.key_patterns`) | none |
| **C-020** | primelocus-hydra (via mikecubed/Hydra fork) + ruflo + advisor | Per-agent cumulative budget ledger spec | 4/3/3 | 2.0 | design note only in v7.2; eventual implementation touches `task-dispatch.schema.yaml` (P6 boundary) | P6, C-008 |

**Recommendation:** open a v7.3 candidate-list seed file (`.local/research/v7.3.0_carryover_seed.md`) populated from this table at the same time PR-0 lands. Three candidates are cheap-enough-to-fold into PR-0 if the user wants to expand its scope: C-010 (vexp plugin), C-018 (vexp SWE-bench), C-019 (scion primitives) — all single-YAML-entry-edits, `relevance_score`/`key_patterns`-class.

---

## §5 NOT in v7.2 (TIER-3 + dropped + cross-cutting themes — brief)

### TIER-3 / Reject (55 candidates total)

31 substantive TIER-3 entries listed in candidate-list table T3-01 … T3-31. Common reasons for parking:
- **No host-platform hook surface in DevolaFlow** (T3-05, T3-08, T3-10) — Cursor `hooks.json` / Claude Code `~/.claude/hooks/` / Codex `.codex/hooks.json` are 3 different APIs; per CCT-5 do not attempt a hook abstraction in v7.2; defer to v8.x
- **Validates existing DevolaFlow design** (T3-04, T3-15, T3-17, T3-25, T3-27, T3-28) — monitor only; no source change
- **Belongs to v7.3+ scope** (T3-08, T3-16, T3-23) — schema work crosses P6 boundary; defer to dedicated wave
- **Out-of-mandate** (T3-12, T3-19, T3-20, T3-31) — proprietary upstream (vexp), MCP-delegated (NineS), host adapter coordination

The remaining 24 TIER-3 items are pure registry-hygiene refreshes (`refresh-registry only` style — bump `last_checked`, refresh `last_known_version` strings, annotate `key_patterns` / `staleness_indicators` without source change). Subsumed by the registry hygiene work in §0 + the per-ref refreshes documented in each delta-T0X.md "Summary table". Per-task list of skip+refresh items in candidate-list lines 422-428.

### Dropped (21 candidates)

Items labelled `skip` because they are explicit no-ops, out-of-mandate, or "no source change required":
- T02: D1.5 (configurable `claude_md_path`), D2.4 (gstack `/codex` overlaps), D3.3 (6-tier intensity not portable)
- T03: KARP-UPSTREAM-01 (covered by H-01), KARP-FRSCH-05/06, UA-02, UA-05 (registry hygiene)
- T04: Δ-edict-04, Δ-karpathy-05/06
- T05: Δ-scion-05/06, Δ-skillrouter-03
- T06: Δ-self-improve-04/06, Δ-hydra-04, Δ-ruflo-03
- T07: Δ-christophera-03/04/06, Δ-spring-ai-03/06

### Cross-Cutting Themes (5)

From candidate-list §"Cross-Cutting Themes":
- **CCT-1** Cost-controlled bottom-up escalation — TIER-1 cluster C-001/C-003 + TIER-2 C-008 fully realises advisor variant; topology vocabulary annotated in C-004
- **CCT-2** Termination is first-class (validates P4) — no new code; cite triple convergence in v7.2 retrospective
- **CCT-3** Activation of dormant learnings substrate (highest-leverage v7.2 cluster) — C-007 schema + C-009 reflex (TIER-2) is minimum activation; C-012/C-014/C-015/C-016 are independent v7.3 instalments
- **CCT-4** Compression hygiene — C-002 + C-006 quick wins; C-011 spec-only design note; T3-11 couples with C-002 once T1 lands
- **CCT-5** Hook-surface gap is a structural blocker for security/per-turn imports — defer to v8.x; C-009 sidesteps via typed status report contract DevolaFlow already owns

---

## §6 SI-3 Composite Projection (heuristic, NineS-deferred)

Each dimension scored 1-10 against the SI-3 rubric, weighted per `.cursor/rules/self-improve-iteration-rules.mdc`. **NineS run is deferred to S05** per S02a §"Limitations" item 1 — these scores are heuristic from V01–V07 evidence and should be treated as projections subject to NineS confirmation.

| Dimension | Weight | Score | Rationale |
|-----------|--------|-------|-----------|
| **Code quality** | 0.20 | **9.4** | Lint-clean across all sandbox patches (V02 explicit, V06 explicit, others by inheritance from baseline test runs); error handling additive (CompressionBypassWarning surfaces audit trail; opt-out flags throughout); cyclomatic complexity unchanged (no new branches in production paths beyond the 4 advisor-flag conditionals + 1 bypass-decision + 1 dedup loop) |
| **Architecture rationality** | 0.20 | **9.5** | P5 (artifacts as contracts) preserved (C-007 dataclass-only; C-002 schema mirrored both directions); P6 cache layout invariant verified untouched in V01 + V02 + V07; minimal coupling between PRs (only PR-D/PR-E share `context_profiles.yaml` on disjoint profile sets); follows existing patterns (`parts = [...]` advisor builder mirrors existing builder idioms; `dedup_learnings` mirrors gstack `/learn` `{type, key}` semantics) |
| **Test adequacy** | 0.20 | **9.6** | 95+ new tests across sandbox (12 V06 + 18 V02 + 15 V07 + 5 V01 planned + ~12 V05 planned + ~6 V04 docs = ~68 net new test cases); CP-2 ≥80% floor maintained on every touched module (`learnings.py` projected ≥97%); SI-4 EvoBench gate at every PR merge; 50/91/80/12-test baselines unchanged across V01/V02/V06/V07 |
| **Maintainability** | 0.15 | **9.5** | Opt-in flags throughout (advisor 3 conditional emits, learnings 2 additive fields, inline-review 1 yaml flag, compression 1 schema sub-key); documentation source attribution preserved (CO-2 verbatim); CHANGELOG entries planned per PR; SF-3 version-consistency unaffected; ADR-005 lazy-migration pattern inherited |
| **Compatibility** | 0.10 | **9.6** | Full backward compat verified per V01 (advisor opt-out via missing flag → `.get(key, True)`), V02 (`compress_message` 2-arg signature still works; `bypass_conditions=[]` opt-out), V07 (v1/v2/v3 entries all load cleanly, mutable-default trap avoided); zero schema field renames; zero removals; cross-platform (no host hook required) |
| **Performance impact** | 0.15 | **9.2** | Advisor cost reduction projected ~35-45% on response tokens per call (system net positive at ≥1 advisor call per dispatch — break-even is fast); +200 token budget bump in PR-D fully reversible; compressor regex pass < 0.1 ms per 4 KB payload (V02 R7); EvoBench composite_score regression projected within ±0.5pp (V05 baseline impact analysis); SI-4 5% regression threshold has wide margin |
| **Weighted composite** | — | **9.46** | = 9.4×0.20 + 9.5×0.20 + 9.6×0.20 + 9.5×0.15 + 9.6×0.10 + 9.2×0.15 = **1.880 + 1.900 + 1.920 + 1.425 + 0.960 + 1.380 = 9.465** |

**Verdict against SI-3 release threshold (composite ≥ 8.5/10):** **PASS** with substantial margin (≈ 0.96 absolute / 11% relative buffer). Cycle is **release-ready** at the heuristic level pending NineS confirmation in S05.

---

## §7 Open Questions for User (decision points)

The following ≥3 explicit questions block PR landing without user direction. Each has a default but should be acknowledged before commit:

1. **Token budget bump in PR-D — accept?**
   PR-D's MANDATORY supplementary edit bumps `token_budget` by +200 for 4 advisor profiles (`feature: 4950→5150`, `refactor/migration: 4800→5000`, `security-audit: 5200→5400`). Without this, under chars//4 fallback (no `tiktoken` installed) 3 of 4 profiles silently bump `important`-priority sections — `dispatch_report` for `feature` defines the L3 protocol contract, losing it is a real degradation. **Default: accept the bump.** Alternative: ship without and accept the documented chars//4 fallback risk.

2. **PR-E SKILL.md goes to 499/499 (zero headroom) — accept the constraint?**
   After PR-E lands, no further SKILL.md edits possible in v7.2 cycle without breaking SF-1. Three options:
   - (a) **Default: accept** — document in CHANGELOG and v7.2 retrospective; queue v7.3 follow-up to extract a SKILL.md section into a new `references/*.md` or raise Default tier to `<600`.
   - (b) **Defer C-005 to v7.3** — keeps SKILL.md at 498 lines (1 headroom restored); preserves cycle flexibility but loses 50× speedup documentation for one cycle.
   - (c) **Defer C-004 to v7.3** — keeps SKILL.md at 499 lines (still 0 headroom but no rebase work); loses hybrid-recipes vocabulary annotation.

3. **C-007 dormant ship vs. C-009 promotion?**
   PR-C ships C-007 schema additions but `operational.jsonl` stays empty until v7.3 C-009 (reflective reflex) lands the writer. Two options:
   - (a) **Default: ship dormant** — explicit two-phase plan per CCT-3; backward compat means v7.3 promotion to writers requires zero v7.2 rework.
   - (b) **Pull C-009 into v7.2** — promotes a TIER-2 candidate to TIER-1; activates JSONL within 2-3 self-update workflow runs per T02 §CC-3; +1 PR (PR-C+) or expand PR-C scope. Adds P6 risk on `lean-report.yaml` (C-009 extends it with optional `learnings:` block — must append, not insert).

4. **PR ordering after PR-A — strict sequence or maximal parallelism?**
   PR-0 first, PR-A second are fixed. PR-B/C/D/E have zero file overlap (only PR-D and PR-E both touch `context_profiles.yaml` on disjoint profile sets). Two options:
   - (a) **Default: maximal parallelism** — open all 4 PRs concurrently as soon as PR-A merges; rebase trivially on the disjoint `context_profiles.yaml` edits.
   - (b) **Strict sequence** B → C → D → E — slower but each SI-10 6-step gate runs against fully-stable predecessor state; recommended if reviewer bandwidth is the bottleneck.

5. **NineS deferral — accept the heuristic SI-3 projection (~9.46/10) or block on S05 NineS run first?**
   Per S02a §"Limitations" item 1, all TIER-1 entries record `nines: abstract (deferred)`. Two options:
   - (a) **Default: accept heuristic** — proceed with PR sequence; run NineS as part of v7.2 retrospective per SI-2 to confirm post-merge.
   - (b) **Block on NineS first** — run `nines -f json -c nines.toml self-eval --baseline-version v7.1.1 ...` against the union of TIER-1 integration targets before any PR-A commit; delays cycle by ≤ 1 day; reduces SI-3 projection uncertainty.

6. **PR-0 scope expansion — fold C-010, C-018, C-019 in?**
   Three TIER-2 candidates are single-YAML-entry edits adjacent to PR-0's spirit:
   - C-010 (vexp plugin) — 1 YAML entry under `recommended_optional`
   - C-018 (vexp SWE-bench refresh) — `key_patterns` rewrite for `vexp` entry
   - C-019 (scion Hub primitives) — 2 entries on `google-scion.key_patterns`

   Folding adds ~30 reviewer minutes to PR-0 and clears 3 TIER-2 items from v7.3 carryover. **Default: keep PR-0 minimal** (6 H-XX only). Alternative: expand to ~9 entries.

---

## §8 Limitations

1. **NineS evaluation deferred (per S02a §"Limitations" item 1, S03 dispatch).** All 7 TIER-1 entries record `nines: abstract (deferred)`. SI-3 dimension scores in §6 are heuristic from V01–V07 evidence (test counts, lint passes, sandbox patch validity), not from a NineS-derived dimension-weighted score. A follow-up S05 task should run `nines -f json -c nines.toml self-eval --baseline-version v7.1.1 --golden-dir data/golden_test_set --samples-dir data/golden_test_set --src-dir src/devolaflow --test-dir tests --project-root .` against the union of TIER-1 integration targets before declaring v7.2.0 release-ready.

2. **EvoBench composite projections are qualitative for V01/V05/V06.** Only V02 actually re-ran benchmark dispatch-prefix tests under cross-validation (5/5 PASS). V01 estimates the +200 token bump preserves SF-1 utilisation < 100%; V05 estimates ±0.5pp impact on documentation/research scenarios; V06 is benchmark-irrelevant (CI guard only). Real EvoBench numbers land at SI-4 gate per PR.

3. **C-001's "35-45% reduction" claim is downstream advisor-response tokens, not measurable from sandbox.** This requires running advisor calls under both wordings against the same prompt set and counting response tokens. V01 confirms the section text is emitted correctly but cannot verify the downstream LLM response shrinks as claimed. EvoBench convergence-loop scenarios are the natural validation target — flagged for SI-4 benchmark gate at PR-D merge time.

4. **C-005's 50× speedup is documented-only in v7.2; runtime hook lands in v7.3.** Per V05 §"Test Baseline" estimated baseline impact: "the 50× speedup is realised only when an L1 Stage Agent actually selects `inline_self_review` mode — which requires the L1 dispatcher code to read the flag, which is a v7.3 follow-up." v7.2 normative documentation only; v7.3 runtime guard rejecting `inline_review_checklist: true` + `team ∈ {Implement, Test}` is a tracked v7.3 candidate.

5. **Function name and profile name/count discrepancies need correction in PR-D description.** Dispatch and candidate-list refer to function `_build_advisor_section` (no such symbol — actual is `_resolve_advisor_text`) and 6 advisor-enabled profiles (actual: 4, with name mismatches: `refactoring`→`refactor`, `security_audit`→`security-audit`, `performance_optimization`→`perf-optimization` which has no `advisor:` block, and non-existent `full_pipeline`). No code impact, but auditors looking for the wrong symbols/profiles will be confused.

6. **0-headroom SKILL.md after PR-E is structural fragility for v7.2.** Pre-existing condition (SKILL.md is at 498 baseline; SF-1 ceiling `< 500` leaves 1 line of headroom which C-005 consumes). v7.3 follow-up tracked: extract a SKILL.md section into a new `references/*.md` (per V05/V06 risk note 1), or raise Default tier to `<600`.

7. **Skip-classification was generous (per S02a §"Limitations" item 6).** Items labelled "no-op for v7.2", "out of mandate", or "no source change required" → `skip`; items labelled "track for v7.3+" or "refresh-registry" → `monitor` and scored. If strict-skip-only-when-explicitly-`skip` is required, dropped count would shrink and TIER-3 count grow correspondingly.

8. **Verbatim extraction discipline second-hand.** Per S02a §"Limitations" item 2: all `description` fields in TIER-1 entries quote verbatim from cited delta reports per CO-2. Some quotes are second-hand (delta reports already extracted from upstream); chain is delta → upstream URL listed in delta `source` field, not re-fetched in S02a/S03/S04.

9. **SI-3 weighted composite floor.** SI-3 release threshold is composite ≥ 8.5/10. Heuristic projection 9.465 has substantial margin but assumes (a) 5/7 high-confidence validations hold under NineS scrutiny, (b) EvoBench gates at each PR pass within 5% regression threshold, (c) the 4-profile +200 token bump in PR-D does not surface unexpected `important`-section drops on profiles outside the 4 advisor-enabled set. Any one of these assumptions failing reduces the projection by ~0.5-1.0 absolute points.

10. **Source paths repository-relative throughout (per CO-4 / SF-5).** External resources (`https://github.com/YoRHa-Agents/DevolaFlow`, anthropic-advisor-tool docs, vexp.dev, gstack/superpowers/caveman/get-shit-done GitHub) keep upstream URLs from delta reports; no local clone paths assumed.

---

## Appendix A — Patch Inventory

| PR | Patches | LOC delta | `git apply --check` |
|----|---------|-----------|---------------------|
| PR-0 | (no patch — direct YAML edits to `reference-dependencies.yaml`, 6 entries H-01…H-06) | ~30 LOC YAML | n/a |
| PR-A | `.local/sandbox/v7.2.0/V06/patch.diff` | +124 / -2 | exit 0 |
| PR-B | `.local/sandbox/v7.2.0/V02/patch.diff` | +101 (3 files) | exit 0 |
| PR-C | `.local/sandbox/v7.2.0/V07/patch.diff` | +49 / -5 | exit 0 |
| PR-D | `.local/sandbox/v7.2.0/V01/patch.diff` (combined C-001 + C-003) | +46 / -6 (incl. 4-profile YAML budget bump scaffolding) | exit 0 |
| PR-E | `.local/sandbox/v7.2.0/V05/patch.diff` (V05 first) + `.local/sandbox/v7.2.0/V04/patch.diff` (V04 main) + `.local/sandbox/v7.2.0/V04/patch_skill_after_v05.diff` (V04 SKILL.md rebase) | +103 (V05) + +81 (V04 main) + 11 (V04 rebase) | exit 0 (all 3) |
| **Total** | 5 patches + 1 rebase + ~30 LOC YAML | ~530 LOC net | 5/5 valid |

## Appendix B — Per-PR SI-10 6-Step Pre-Commit Gate Mapping

Every PR triggers the full SI-10 sequence; below is which steps each PR is most likely to need extra attention on:

| PR | Step 1 (pytest) | Step 2 (ruff check) | Step 3 (ruff format) | Step 4 (test_version) | Step 5 (test_benchmarks) | Step 6 (check-cursor-skill) |
|----|-----------------|---------------------|----------------------|----------------------|--------------------------|----------------------------|
| PR-0 | should pass (no source change) | should pass | should pass | should pass (no version change) | should pass (no benchmark surface) | should pass |
| PR-A | **new test file added** (12 cases) | should pass | should pass | should pass | should pass (no benchmark surface) | should pass |
| PR-B | **~12 new test cases**; 80 baseline + 12 = 92 | watch security regex line lengths | watch f-string formatting | should pass | **VERIFY no regression** on non-bypass payloads | should pass |
| PR-C | **~15 new test cases**; 50 baseline + 15 = 65 | should pass | should pass | should pass | recommend run as defense-in-depth | should pass |
| PR-D | **~5 new test cases**; 91 baseline + 5 = 96 | should pass | should pass | should pass | **CRITICAL** — should show `cost` dimension reduction; composite ≤ 5% regression | should pass |
| PR-E | should pass | should pass | should pass | **CRITICAL** if SKILL.md edit triggers version-consistency mirror check (it shouldn't — version unchanged) | **CRITICAL** — SI-5 coupled, SKILL.md edited; verify ≤ 5% regression per scenario | **CRITICAL** if mirror present locally — pre-commit `make sync-cursor-skill` |

After all 6 PRs merge: `python scripts/bump_version.py 7.2.0` performs CP-3 7-pattern replacement + opt-in mirror sync, then `make sync-human-docs`, then full SI-10 once more, then tag.

---

**End of accept-list and roadmap. Awaiting user selection from §"Quick Selection Menu".**
