# NineS Post-Cycle Deep Analyze — DevolaFlow v8.0.0

**Generated:** 2026-04-22 (post P-10 merge, HEAD `6c5fdf9`)
**Tool:** `nines v3.3.0` (binary at `/root/miniforge/bin/nines`)
**Command:** `nines -f json analyze --target-path src/devolaflow --depth deep --agent-impact --keypoints`
**Raw output:** `.local/research/v8.0.0_nines_post_cycle.json` (53 KB, gitignored)
**Pre-cycle baseline:** `.local/research/v8.0.0_nines_artifacts/analysis_report.json` (52 KB, gitignored)

## Headline

| Severity | Pre-cycle | Post-cycle | Δ |
|----------|----------:|-----------:|--:|
| ERROR    | 1         | **0**      | **−1** ✓ |
| WARNING  | 11        | **6**      | **−5** ✓ (−45 %) |
| INFO     | 129       | 141        | +12 |
| **Total ERROR+WARN** | **12** | **6** | **−6 (−50 %)** |
| Total findings | 141 | 147 | +6 |

The plan G1 acceptance target was `ERROR+WARN ≤ 2`; **actual is 6**, so **G1 not met**
in absolute terms but the cycle delivered a 50 % reduction (12 → 6) and eliminated the
sole ERROR-class finding. Of the 6 remaining WARN findings, **4 are in net-new
v8.0.0 code** (P-09 / P-10 / P-12) — i.e. the cycle introduced new infrastructure that
itself triggers the cyclomatic-complexity rule.

## Closed Findings (verified against pre-cycle JSON, 10/12 ERROR+WARN closed)

| Pre-cycle ID | Sev. | File:Line | Function (cc) | Closed by |
|--------------|------|-----------|----------------|-----------|
| `CC-70f79c-0000` | error   | `adapters/data_driven.py:117` | `DataDrivenAdapter._apply_transform` (cc=22) | **P-01** |
| `CC-70f79c-0001` | warning | `adapters/data_driven.py:171` | `DataDrivenAdapter._split_by_heading` (cc=15) | **P-01** |
| `CC-39ab83-0001` | warning | `compressor.py:1187`          | `summarise_predecessor` (cc=15)              | **P-02** (partial — see *Discrepancies* below) |
| `CC-448821-0000` | warning | `task_adaptive_selector.py:392` | `select_context` (cc=11)                  | **P-08** |
| `CC-448821-0001` | warning | `task_adaptive_selector.py:543` | `apply_round_escalation` (cc=11)          | **P-07** |
| `CC-448821-0002` | warning | `task_adaptive_selector.py:585` | `main` (cc=12)                            | (organic, not patch-claimed) |
| `CC-631b33-0000` | warning | `learnings.py:202`            | `load_relevant_learnings` (cc=14)            | **P-11** |
| `CC-631b33-0001` | warning | `learnings.py:522`            | `decay_confidence` (cc=12)                   | **P-11** |
| `CC-89508c-0000` | warning | `nines/researcher.py:424`     | `refresh_reference_dependency` (cc=12)       | (organic) |
| `CC-f1cb7a-0000` | warning | `lifecycle/format_on_edit.py:38` | `_collect_violations` (cc=12)             | (organic) |
| `CC-0c2755-0000` | warning | `template_engine/runtime.py:167` | `select_stages_for_runtime` (cc=13)       | (organic) |

11 of 12 high-severity findings retired (10 by the patches above, 4 of which are organic
clean-up not credited to a specific patch).

## Remaining Post-Cycle WARN Findings (6 total)

| Post-cycle ID | File:Line | Function (cc) | Origin |
|---------------|-----------|----------------|--------|
| `CC-bcbae9-0000` | `ac_generator.py:172`            | `_match_patterns` (cc=12)         | **P-10 net-new** |
| `CC-39ab83-0000` | `compressor.py:976`              | `extract_named_entities` (cc=11)  | **pre-existing** (line shifted 945→976) |
| `CC-39ab83-0001` | `compressor.py:1558`             | `_assemble_abstractive_summary` (cc=11) | **organic refactor side-effect** (replaced `summarise_predecessor`) |
| `CC-491428-0000` | `gate/complexity_detector.py:218`| `_parse_nines_payload` (cc=17)    | **P-09 net-new** |
| `CC-491428-0001` | `gate/complexity_detector.py:285`| `wrap_nines_complexity` (cc=16, radon shows cc=19) | **P-09 net-new** |
| `CC-67079a-0000` | `gate/scorer.py:1513`            | `evaluate_gate` (cc=18)           | **P-12 net-new** |

Radon cross-check (top-level `python -m radon cc … -a -s`):

```
gate/complexity_detector.py: wrap_nines_complexity C (19), _parse_nines_payload C (17)
gate/scorer.py:              evaluate_gate C (18), _build_ladder_verdict C (18), …
ac_generator.py:             _match_patterns C (12), _specificity_score C (12)
compressor.py:               extract_named_entities cc=11, _assemble_abstractive_summary cc=11
```

(NineS limits findings to top-K per file; radon surfaces additional cc≥10 functions in
`gate/scorer.py` and `ac_generator.py` that NineS did not flag.)

## Discrepancies vs. Patch Closure Claims

1. **P-02 ↛ `CC-39ab83-0000`** — The patch claimed closure of `extract_named_entities`,
   but the function is **still present at cc=11** (line 945 → 976). The same NineS ID
   re-resolves to the same function on this run. This closure claim was **inaccurate**;
   the function was not actually simplified. Severity: minor (cc=11 is just at the
   warning floor).
2. **P-02 → `CC-39ab83-0001` reused** — `summarise_predecessor` (the original
   cc=15 function) was eliminated as claimed, but a sibling `_assemble_abstractive_summary`
   (cc=11) emerged in the same file and re-used the slot-1 NineS ID. Net effect on
   `compressor.py` complexity: cc 15+11 → 11+11 (improvement of 4 points), so
   patch-level intent was achieved.

## G1 Acceptance vs. Plan §S05

Plan §S05 G1 acceptance criterion (per dispatch prompt): `total ERROR+WARN ≤ 2`.

| Metric | Target | Actual | Verdict |
|--------|-------:|-------:|---------|
| ERROR count | 0 | 0 | ✓ |
| ERROR+WARN total | ≤ 2 | 6 | ✗ (above target by 4) |
| ERROR+WARN reduction vs pre-cycle | — | −50 % | improvement |

**Decision input for L0/Stage Agent:** G1 is not strictly met. Three options:

- **A (defer)** — accept the 4 net-new WARN findings as cycle debt and bump v8.0.0
  with a documented carry-over for v8.1.0 (covers `gate/complexity_detector.py`,
  `gate/scorer.py:evaluate_gate`, `ac_generator.py:_match_patterns`).
- **B (extend cycle)** — open follow-up patches P-14…P-17 to extract helpers from the
  4 new offenders before tagging v8.0.0. Estimated 1 wave of 4 task agents.
- **C (relax G1)** — ratify a revised G1 of `ERROR ≤ 0 AND WARN ≤ 6` (matches actual)
  and proceed to release. Justification: 50 % reduction + ERROR elimination is the
  meaningful invariant; absolute count includes net-new code that didn't exist
  pre-cycle.

This task agent has **no opinion** on which option to take — escalating to L0/Stage
Agent per Rule P4 bounded retry.

## Methodology Notes

- Pre-cycle baseline `analysis_report.json` was captured 2026-04-21 11:22 against
  the same `src/devolaflow/` target before any P-* patch landed.
- Post-cycle run executed 2026-04-22 against `main` at `6c5fdf9` (P-10 merge).
- Both runs used identical flags (`--depth deep --agent-impact --keypoints`).
- NineS finding IDs are file-stable (hash of `<path>` plus 0-indexed slot for
  cc-flagged callables in that file), so the same ID may resolve to a different
  function across runs when a higher-cc sibling appears.
- **Manual fallback (W-2/SI-2):** not required — NineS binary worked first try,
  exit 0, no stderr, 53 KB structured JSON output.

## Acceptance Verdict

| Criterion | Result |
|-----------|--------|
| NineS binary available and usable | ✓ |
| Post-cycle JSON produced and parsed | ✓ |
| Pre-cycle ERROR (CC-70f79c-0000) closed | ✓ |
| WARN reduction vs pre-cycle | ✓ (−5 / −45 %) |
| Plan §S05 G1 absolute target (≤2) | ✗ (actual 6) — escalate to L0 |
| Closure-claim audit completed | ✓ — see *Discrepancies* §|
