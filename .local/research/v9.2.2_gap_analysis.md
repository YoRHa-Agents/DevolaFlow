# v9.2.2 SI-1 Gap Analysis — v9.2.1 Feedback Triage + 3-PV PATCH Cycle Plan

**Cycle**: v9.2.1 → v9.2.2 → v9.2.3 → v9.2.4 (3-PV PATCH series).
**Branch**: `feat/v9.2.2-cycle-feedback-fixes`.
**Source artefact**: `.local/feedbacks/feedback_for_v9.2.1.md` (4 issues + 2
operator notes catalogued by the parent agent during a `repo-init core`
invocation against `/home/agent/workspace/UniLark`).
**Author**: L3 v9.2.2 PV-01 cycle-lead.
**Date**: 2026-05-02.

---

## §1. Gaps identified (verbatim from `feedback_for_v9.2.1.md`)

Each row below extracts the issue ID, severity, root-cause sentence, and the
suggested fix from the source feedback file. No paraphrasing.

| ID | Severity | Symptom (verbatim) | Root cause (verbatim) | Suggested fix (priority) |
|---|---|---|---|---|
| **I-001** | **critical** | `devola-init local --no-compile` prints `Error: Agent source not found at /root/miniforge/lib/python3.12/workflow-system/agent` then exits 1; the hint recommends `pip install devolaflow` which is exactly the install path the operator already followed. | `devolaflow/init_project.py::_find_agent_dir()` searches `pkg_dir.parent.parent / "workflow-system" / "agent"` and `Path.cwd() / "workflow-system" / "agent"`; the wheel ships only `devolaflow/` — `workflow-system/` is excluded from `MANIFEST.in` / wheel `data_files`. `main()` then unconditionally hard-fails on `if not (agent_dir / "SKILL.md").exists()` BEFORE dispatching to any per-target installer, including `install_local` which does NOT need `agent_dir`. | (1) Defer the `SKILL.md` check until it's needed — only `cursor` / `claude` / `copilot` / `codex` consume `agent_dir`. (2) Ship `workflow-system/` inside the wheel as package-data (deferred to v9.3.0). (3) Improve the error message — when neither candidate exists AND the user installed via wheel, say so explicitly instead of recommending the same `pip install` they already ran. |
| **I-002** | **major** | `pip install --upgrade git+...` against `mirrors.baidubce.com/pypi/simple/` errors with `Could not find a version that satisfies the requirement setuptools>=68.0`. | The corporate baidubce mirror serves an older snapshot of PyPI that does not carry `setuptools>=68.0`. PEP 517 build-isolation creates a fresh venv and requires the version range pinned by DevolaFlow's `[build-system].requires`; the truncated mirror cannot satisfy it. | Document in README troubleshooting: prefer `--index-url https://pypi.org/simple/` for build-isolation needs; suggest `~/.pip/pip.conf` with `extra-index-url` fallback. External issue (mirror operator side); DevolaFlow-side fix is documentation-only. |
| **I-003** | **minor** | UniLark's prior-session `.gitignore` lines 81–84 ignore `.local/.agent/active/` + `.local/.agent/handoff/` + `.local/.agent/archive/` wholesale, so the convention READMEs that `scaffold_local()` writes never reach version control on a clone. | Prior-session scaffold (NOT DevolaFlow itself; the previous `git init` step authored the `.gitignore`); `scaffold_local()` happily writes through existing gitignore rules without surfacing the mismatch. | Make `scaffold_local()` log a one-line warning when any newly-created path falls under an existing `.gitignore` rule (low priority). Recommend whitelisting `!.local/.agent/<dir>/README.md` while keeping runtime contents ignored. |
| **I-004** | **minor** | SKILL.md §"Version & Update" recommends `pip install --upgrade git+...` which silently lands operators in I-001. | Documentation gap — install hint does not warn about the wheel/CLI mismatch. | Add a one-line note in SKILL.md install section about the wheel vs `workflow-system/` mismatch + the `local` fallback that works on wheel-only installs + the clone install path for other targets. |
| **Note A** | enhancement | Repeated `repo-init` calls stack `--no-compile` / `--with-examples` flags; a single `--mode={core,standard,full}` shorthand mapping 1:1 onto the SKILL.md mode table would reduce cognitive cost. | Operator UX request, not a bug. | Add `devola-init local --mode={core,standard,full}` flag (PV-02 scope). |
| **Note B** | enhancement | `scaffold_local()` writes through an existing `.gitignore` without heads-up. | Same as I-003 root cause; surfaces as a separate request for diagnostic logging. | Tied to I-003 fix (PV-02). |

### 1.1 v9.2.1 environment context (read-only, for traceability)

* Host: Ubuntu Linux 5.15.0-124-generic
* Python: 3.12 via `/root/miniforge`
* Install command:
  `pip install --index-url https://pypi.org/simple/ --upgrade git+https://github.com/YoRHa-Agents/DevolaFlow.git`
* Resolved commit: `0c725279…` → wheel `devolaflow-9.2.1`
* Skill bundle stamp at issue time: `~/.cursor/skills/devola-flow/SKILL.md`
  declared version `9.1.0` (informational — internally consistent).

---

## §2. Decomposition into 3 PVs (v9.2.2 → v9.2.3 → v9.2.4)

The user's scope choice in the feedback file is `all-four-plus-notes` +
`v922-cycle` — i.e. all 4 issues plus the 2 enhancement notes ship across a
3-PV PATCH series. Per W-3 (`PATCH floor composite ≥ 8.5/10`) every PV gates
on the same SI-3 + W-2 NineS thresholds.

### PV-01 ships v9.2.2 (this PV) — **critical I-001 + doc note I-004**

**Scope**: surgical CLI fix + documentation note + W-1 SI-1 planning artefact
(this file).

**AC**:

1. `devola-init local --no-compile` on a pip-wheel install (no parent
   `workflow-system/`) → exits 0 with all 8 canonical paths scaffolded.
2. `devola-init cursor` on a pip-wheel install → exits 1 with informative
   error message that DOES NOT contain `pip install devolaflow` (the
   misleading recommendation that landed users in I-001).
3. `devola-init --list` → succeeds in all install scenarios (regression
   preserved).
4. SKILL.md ≤ 500 lines (C-4 Default tier; current 458 → ~462 expected).
5. W-9 SI-10 6/6 PASS; coverage ≥ 80% on the modified `main` deferred-check
   path.
6. NineS composite ≥ 0.85; `byte_stability` axis ≥ 0.95 (surgical fix
   preserves the existing API surface — `agent_dir` still passed through to
   per-target installers, only the abort timing changes).
7. Test budget ≤ +30 (W-17); PV-01 target: +8 NEW (6 in
   `tests/test_init_project_pip_wheel.py` + 2 in
   `tests/test_no_ghost_features.py`).
8. Backward compat: existing `devola-init cursor / claude / codex / copilot`
   flows behave byte-identically when SKILL.md IS present.

**File-level scope**:

| File | Op | Why |
|---|---|---|
| `.local/research/v9.2.2_gap_analysis.md` | NEW | This file (W-1 SI-1 artefact) |
| `src/devolaflow/init_project.py` | EDIT | Defer the `SKILL.md` check; introduce `AGENT_DIR_REQUIRED_TARGETS = {"cursor","claude","copilot","codex"}`; rewrite the error message |
| `workflow-system/agent/SKILL.md` | EDIT | I-004 one-line note in §"Version & Update" |
| `tests/test_init_project_pip_wheel.py` | NEW | 6 tests pinning the deferred-check + error-message + regression invariants |
| `tests/test_init_project.py` | EDIT | +2 regression assertions (deferred-check surface + `AGENT_DIR_REQUIRED_TARGETS` membership) |
| `tests/test_no_ghost_features.py` | EDIT | W-18 ghost-audit refresh — `_V9_2_2_NEW_FILES` + `test_v9_2_2_new_symbols_have_coverage` + `test_v9_2_2_local_target_no_workflow_system_dependency` |
| `src/devolaflow/__init__.py` | EDIT | Version bump 9.2.1 → 9.2.2 (via `scripts/bump_version.py`) |
| `pyproject.toml` | EDIT | Version sync (auto via bump script) |
| `workflow-system/agent/workflow-skill.yaml` | EDIT | Version sync (auto) |
| `scripts/generate_human_docs.py` | EDIT | Version sync (auto) |
| `tests/test_smoke.py` | EDIT | Version sync (auto) |
| `README.md` | EDIT | Version sync (auto) |
| `workflow-system/human/demo/benchmark-results/index.html` | EDIT | Version sync (auto) |
| `workflow-system/human/demo/version-timeline/versions.json` | EDIT | ST-7 entry |
| `workflow-system/human/en/`, `workflow-system/human/zh/` | EDIT | `make sync-human-docs` regen |
| `CHANGELOG.md` | EDIT | `[9.2.2]` PATCH entry |

### PV-02 ships v9.2.3 — I-002 doc fix + I-003 logging + `--mode` shorthand

**Scope**: README troubleshooting block for the baidubce mirror; warn-on-write
diagnostic in `scaffold_local`; `--mode` flag wiring.

**AC**:

1. README ships a §"Troubleshooting installs" subsection mentioning the
   `setuptools>=68.0` mirror gap + the `--index-url https://pypi.org/simple/`
   workaround.
2. `scaffold_local(cwd)` emits a one-line `WARN scaffold path X is ignored
   by .gitignore rule Y` when any newly-created path falls under an existing
   gitignore rule (per I-003 + Note B).
3. `devola-init local --mode={core,standard,full}` works as a shorthand for
   `--no-compile` / `--with-examples` per the SKILL.md mode table (Note A).
4. W-9 SI-10 6/6 PASS; +12 NEW tests max (within W-17 cycle budget).

### PV-03 ships v9.2.4 — cycle-close validation + retrospective

**Scope**: `self-update`-style validation pass; W-7 / SI-8 retrospective for
the v9.2.2 → v9.2.4 mini-series; W-19 archive idempotent re-run.

**AC**:

1. NineS cycle-close composite ≥ baseline; SI-3 ≥ 8.5 PATCH floor.
2. W-7 retrospective at `.local/research/v9.2.4_retrospective.md` with the
   4 mandatory sections.
3. W-19 re-archive `docs/cycle-archive/v9.2.0/` (idempotent — cycle-archive
   convention is by MAJOR/MINOR cycle; PATCH series rolls into the parent
   cycle directory).
4. Cycle-aggregate test delta ≤ +30 (well within W-17 +150 cycle cap).

---

## §3. Cycle invariants (preserved across all 3 PVs)

| Invariant | v9.2.1 baseline | v9.2.2 target | Source |
|---|---:|---:|---|
| Soul-set count | 10 | **10 (frozen per W-21)** | `.rules/soul.mdc` |
| Architecture rule count | 6 | 6 | `.rules/architecture.mdc` |
| Conventions rule count | 9 | 9 | `.rules/conventions.mdc` |
| Workflow rule count | 21 | 21 | `.rules/workflow.mdc` |
| Style rule count | 13 | 13 | `.rules/style.mdc` |
| **Total rule count** | **59** | **59 (no S-11; no new architecture rule)** | aggregate |
| `DEFAULT_EVENTS` length | 8 | 8 (byte-stable) | `lifecycle/__init__.py` |
| `canonical_order` length | 16 | 16 (byte-stable) | `schemas/lean-dispatch.yaml#layout_invariant` |
| Layout schema version | 5 | 5 (byte-stable) | same |
| New env flags | 0 (cycle aggregate v9.2.0) | **0** (W-20 reuse-first across the entire 3-PV PATCH series) | `references/env-flags.md` |
| `tests/` count | 3573 | 3573 + 8 → ~3581 (PV-01) | suite collection |

The W-21 Soul-set freeze gate is honoured automatically — v9.2.2 introduces
no new immutable invariant; the I-001 fix is a behaviour CORRECTION (the old
behaviour was buggy), not a new contract.

---

## §4. Per-PV NineS gate plan (W-2)

Each PV runs the canonical W-2 NineS planning + close pair per
`AGENTS.md` §W-2:

```bash
# Planning (early in the PV)
nines -f json analyze \
  --target-path src/devolaflow/init_project.py \
  --depth deep --agent-impact --keypoints \
  > .local/research/v9.2.2_nines_planning.json 2>&1

# Cycle close (immediately before commit)
nines -f json -c nines.toml self-eval \
  --baseline-version 9.2.1 \
  --golden-dir data/golden_test_set --samples-dir data/golden_test_set \
  --src-dir src/devolaflow --test-dir tests --project-root . \
  > .local/research/v9.2.2_nines.json 2>&1
```

Render the close JSON to Markdown at `.local/research/v9.2.2_nines.md`. When
NineS is unavailable on the local toolchain, fall back to **manual analysis**
following the same dimensions (W-2 escape hatch documented in `AGENTS.md`
§W-2 last sentence) and explicitly note `MANUAL_FALLBACK: yes` in the
generated Markdown.

### 4.1 Per-PV expected NineS verdict

| PV | NineS overall | byte_stability | Notes |
|---|---:|---:|---|
| v9.2.2 (this) | ≥ 0.85 | ≥ 0.95 | Surgical CLI fix; existing public API surface unchanged (`install_local` / `install_cursor` / `_auto_detect` signatures byte-identical) |
| v9.2.3 | ≥ 0.85 | ≥ 0.95 | Logging-only `scaffold_local` extension + `--mode` shorthand (additive) |
| v9.2.4 | ≥ baseline | ≥ 0.95 | Validation-only PV; cycle-close composite expected byte-stable ± 0.001 |

### 4.2 SI-3 weighted-composite floors (W-3)

PATCH series → composite ≥ **8.5/10** per W-3. Each PV reports the 6
canonical dimensions (Code quality 0.20 / Architecture 0.20 / Test adequacy
0.20 / Maintainability 0.15 / Compatibility 0.10 / Performance 0.15).

The v9.2.1 baseline closed at SI-3 weighted composite **9.475 / 10**
(`.local/research/v9.2.1_evaluation.md` Part D). v9.2.2's expected SI-3
composite is in the **9.0..9.5** band: the I-001 fix is a Code-quality and
Architecture win (proper separation of concerns — agent-dir-required vs
agent-dir-independent installers), bounded by the small Maintainability
delta from the new `AGENT_DIR_REQUIRED_TARGETS` constant + the deferred
check site.

---

## §5. Open questions / out-of-scope for v9.2.2

* **v9.3.0 follow-up**: bundle `workflow-system/` inside the wheel as
  package-data per I-001 §"Suggested upstream fix" #2. This would let
  `_find_agent_dir()` fall back to `importlib.resources.files("devolaflow_workflow_system")`
  and remove the `AGENT_DIR_REQUIRED_TARGETS` workaround entirely. Needs
  MANIFEST.in + `[tool.setuptools.package-data]` + a CI test to assert the
  wheel contains `SKILL.md`. Telegraphed for v9.3.0 SI-1 gap analysis.
* **W-21 future Soul candidate**: none proposed. The v9.2.2 cycle is
  behaviour-correction-only and does not surface any new immutable
  invariant.
* **R5 strict zero-IO test**: I-001 fix does not introduce a new env flag,
  so the W-20 zero-IO test pattern (`tests/test_<feature>_disabled_is_noop.py`)
  is N/A. The PV-01 test surface is the
  pip-wheel-install scenario (`tests/test_init_project_pip_wheel.py`).

---

## §6. References

* Source feedback artefact: `.local/feedbacks/feedback_for_v9.2.1.md`
* Cycle plan precedent (3-PV PATCH series): the v9.2.0 → v9.2.1 sustaining
  precedent at `.local/research/v9.2.1_retrospective.md`
* W-1 SI-1 rule: `AGENTS.md` §W-1
* W-2 NineS rule: `AGENTS.md` §W-2
* W-3 SI-3 rule: `AGENTS.md` §W-3 (PATCH floor ≥ 8.5)
* W-17 test cap: `AGENTS.md` §W-17 (PV cap ≤ +30; cycle cap ≤ +150)
* W-18 ghost-audit precondition: `AGENTS.md` §W-18
* W-20 env-flag reuse-first: `AGENTS.md` §W-20
* W-21 Soul-set freeze: `AGENTS.md` §W-21
