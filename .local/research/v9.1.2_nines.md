# NineS W-2 / SI-2 Close Gate — v9.1.2 PV-02

Run: `nines -f json -c nines.toml self-eval --baseline-version 9.1.1 --golden-dir data/golden_test_set --samples-dir data/golden_test_set --src-dir src/devolaflow --test-dir tests --project-root .`

Raw JSON: `.local/research/v9.1.2_nines.json` (2167 lines)
Planning JSON: `.local/research/v9.1.2_nines_planning.json` (`nines analyze --target-path src/devolaflow/skills/ --depth deep --agent-impact --keypoints`, 221 lines)

## Composite

| Metric | v9.1.2 | v9.1.1 baseline | Delta | Threshold |
|---|---|---|---|---|
| `overall` | **0.9037** | 0.9073 | -0.0036 | ≥ 0.85 ✓ PASS |
| `weighted_overall` | 0.7208 | (within noise) | — | — |
| `capability_mean` | 0.9550 | (within noise) | — | — |
| `hygiene_mean` | 0.7842 | (within noise) | — | — |

**Verdict**: PASS — composite ≥ 0.85 (margin +0.0537); the -0.0036 delta vs the v9.1.1 baseline (0.9073 → 0.9037) is within the ±0.004 noise floor documented in the v9.0.0 retrospective §"NineS noise envelope".

## PV-02 axis check (per cycle plan §PV-02 AC)

The cycle plan's PV-02 acceptance criterion explicitly calls out the `decomposition` axis check ("the activation heuristic is the primary architectural decision of this PV"). Two related axes surface in the NineS output:

| Axis | v9.1.2 | Threshold |
|---|---|---|
| `decomposition_coverage` | **1.0000** | no > 5pp regression vs v9.1.1 ✓ PASS (delta 0pp) |
| `decomposition` (legacy alias) | (composite mean across decomp signals; encompassed by `decomposition_coverage` per NineS v3.3.0) | — |

`decomposition_coverage` is the canonical NineS axis name (v3.3.0+); the cycle plan's bare `decomposition` is a prose alias. Since the value lands at 1.0 with no observed regression, the PV-02 architectural-decision check PASSES.

## Hygiene axes — upstream blockers (v9.0.0 retrospective §A1/A3)

Hygiene mean 0.7842 is depressed by the same upstream NineS A1 (`code_coverage` subprocess timeout) and A3 (`index_recall` ≈ 0.8 cap) blockers documented in v9.0.0 retro §"NineS upstream blockers". Per the v9.0.0 retrospective these are NineS v3.3.0 binary issues, NOT real DevolaFlow regressions; both are tracked for the upcoming NineS v3.4.0 fix.

## Per-PV deltas vs v9.1.1

* **Architecture rule count** — Architecture layer 5 → 6 (+A-6 "Workspace Engagement Auto-Activation"). Total rule count 58 → 59. NineS structure-recognition axis stays at 1.0 (the new symbols + new rule registered cleanly).
* **NEW symbols** — 5 new on-disk surfaces under the W-18 ghost-audit (`_V9_1_2_NEW_FILES`); all 5 have presence + import-smoke + R5/W-20 contract assertions.
* **W-17 NEW test functions** — +26 in PV-02 (within +30 W-17 cap; cumulative cycle delta +34 of +150 cap).
* **Layout invariant byte-stable** — `schemas/lean-dispatch.yaml#layout_invariant` length 16 / version 5 unchanged through v9.1.2 (I-8 invariant intact).

## SI-3 weighted composite (W-3) — derived

Per the v9.2.0 cycle plan §"NineS composition into SI-3" mapping table:

| SI-3 dimension | Weight | NineS contribution | v9.1.2 |
|---|---|---|---|
| Code quality | 0.20 | `architecture` + `code_quality` mean | (capability cluster) |
| Architecture rationality | 0.20 | `decomposition_coverage` | 1.000 → 10.0 |
| Test adequacy | 0.20 | `test_coverage` + `scoring_accuracy` mean | (capped by upstream A1 — 8.5 floor inherited) |
| Maintainability | 0.15 | `maintainability` axis | (within v9.1.1 envelope) |
| Compatibility | 0.10 | `byte_stability` proxy = `structure_recognition` | 1.000 → 10.0 |
| Performance impact | 0.15 | EvoBench delta | 0 regressions > 5pp |

**Weighted composite (PATCH inheritance from v9.1.1)**: ≥ 8.5 PASS. PV-02 is byte-stable additive on top of v9.1.1's 9.07 composite; no NineS axis regressed > 5pp; therefore the PATCH composite floor is inherited.

## Conclusion

**PV-02 close gate PASS** — NineS overall ≥ 0.85; decomposition axis no regression; SI-3 weighted composite ≥ 8.5 by inheritance. Cleared for the W-9 SI-10 6-step verification + commit.
