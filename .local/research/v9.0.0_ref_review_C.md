# v9.0.0 SI-1 — Reference Review (Batch C)

> Date: 2026-04-23 | Author: L3 Task Agent (Review team) | Task: v9.0.0-S03-T03
> Scope: 3 of 11 SF-4 canonical references — `team-roles.md`, `execution-protocol.md`, `repo-modes.md`
> Output: `.local/research/v9.0.0_ref_review_C.md`
> Companions: Batch A and Batch B (review files, separate tasks)
> Inputs (read-only): the 3 reference files, `workflow-system/agent/SKILL.md`,
> `.local/research/v9.0.0_version_history.md`, `.local/research/v9.0.0_decomposition_analysis.md`
> Constraints: C-2 (lean), C-3 (verbatim quotes), S-2 (relative paths), S-5 (no silent failures), ≤ 500 lines

## Executive Summary

All 3 Batch C references load without parse errors and are within the SF-1 Large
tier 1000-line ceiling (522 / 578 / 282 of 1000). Every file's frontmatter still
declares `last_updated: "2026-04-04"`, but `execution-protocol.md` itself
contains §7 (`v7.2.0+`), §8 (`v7.3.0+`), and §9 (`v8.0.0+`) appendices, so the
frontmatter date drift is unambiguous. The strongest content drift is in
`execution-protocol.md`, which has no coverage of three v8.3.x primitives that
the v9.0.0 SI-1 themes T3 / T4 / T7 explicitly target — `memory_router`
fast-path lookup, `shell_proxy` + `pre_shell_call` lifecycle hook, and the
`change-driven` workflow envelope lifecycle. `team-roles.md` is structurally
intact (the 5-team taxonomy still matches SKILL.md §"AgentTeam Quick
Reference") but its §7 Team Participation Matrix lists only 11 of the 22
workflow templates currently advertised in SKILL.md §"Template
Quick-Reference". `repo-modes.md` is the most stable of the three — the 3-mode
taxonomy and 20-feature matrix are still complete — but `Codeberg` is matched
in §3 regex but is not assigned to a known variant in §1, and §6
"Mode-Aware Stage Behavior" omits the `verify` and `monitor` stage primitives
added in v5.4.0 / earlier. Verdicts: **REFRESH** (team-roles.md), **EXTEND**
(execution-protocol.md), **KEEP** (repo-modes.md). Estimated net LOC delta if
all candidates land: +90..+150 lines across the 3 files, all within tier
budgets — a wash for SF-1 since none touches SKILL.md.

---

## Reference: team-roles.md

**Current line count**: 522 / 1000 (Large tier — 47.8% used)
**Last major content update**: v7.7.0 era (v9.0.0_version_history.md line 1464:
*"Compacted team participation matrix to `references/team-roles.md` to stay
under 500-line SKILL.md budget (493 lines)."*); Test Team output contract
extended in v5.4.0 with the `product-verification` 4 verify fields (visual /
acceptance / interaction / accessibility) per version_history line 311.
Frontmatter `last_updated: "2026-04-04"` is **stale by ≥ 2 cycles** (v8.3.0
team-roles is unchanged, v8.4.0 untouched, but compaction event is older still).
**SKILL.md reference hit**: Y — 2 hits.
- Line 334: *"Full team specifications + participation matrix:
  `references/team-roles.md`"*
- Line 422 (Tier 2 nav table): *"`references/team-roles.md` | Task agent
  config, team capabilities"*

### Content inventory

| § | Title | Lines |
|---|-------|-------|
| 1 | Team Relationship to Layers | 22-39 |
| 2 | Research Team (workflow + I/O contracts + criteria + tools) | 40-97 |
| 3 | Design Team (workflow + I/O contracts + criteria + tools) | 98-158 |
| 4 | Implement Team (workflow + I/O contracts + criteria + tools) | 159-231 |
| 5 | Test Team (workflow + I/O contracts + criteria + tools) | 232-325 |
| 6 | Review Team (workflow + I/O contracts + score formula + simplicity check) | 326-430 |
| 7 | Team Participation Matrix (11 workflow rows × 5 team cols) | 431-449 |
| 8 | Handoff Protocol (deliverable schema + chains + contracts + cycle) | 450-522 |

The 5-team taxonomy (Research / Design / Implement / Test / Review) at §1
matches SKILL.md §"AgentTeam Quick Reference" (lines 326-332) byte-for-byte
in role names. The Test Team output contract at §5 (lines 271-309) does
include the v5.4.0 product-verification fields:
*"`visual_fidelity_score: "number | null"`"*, *"`acceptance_verification_score:
"number | null"`"*, *"`accessibility_violations: critical/serious/moderate/
minor`"* — confirming Test team currency for the 7-dimension EXTENDED gate.

### Drift findings

1. **STALE — Team Participation Matrix is 11 / 22 workflow templates**
   (verbatim from team-roles.md §7 lines 434-446): rows present are
   `research-only`, `design-only`, `hotfix`, `refactoring`, `migration`,
   `spike-poc`, `documentation`, `security-audit`, `RDRR`, `full-pipeline`,
   `product-verification`. SKILL.md §"Template Quick-Reference" (lines
   447-471) advertises **22 templates**, so 11 are missing from the matrix:
   `feature-enhancement`, `demo-showcase`, `performance-optimization`,
   `dependency-setup`, `onboarding`, `skill-optimization`, `nines-assisted`,
   `self-update`, `repo-init`, `entropy-cleanup`, `change-driven`. (The
   `shell-proxy` row in SKILL.md is a workflow keyword + RTK fast-path
   shortcut, not a template — it is not expected as a matrix row.)
2. **STALE — Implement Team Tools/Skills omit two v8.x primitives**
   (verbatim §4 line 229-230): *"`Read`, `Write`, `StrReplace`, `Shell`,
   `Grep`, `Glob`, `SemanticSearch`, `ReadLints`, code-rules loading
   protocol"*. Missing: `EditNotebook` (cited in S-1 / SKILL.md §"L0 Tool
   Permissions" line 133); `pre_shell_call` lifecycle hook (v8.3.2 — see
   v9.0.0_version_history line 1822: *"`src/devolaflow/lifecycle/
   pre_shell_call.py` (148 lines 5th canonical lifecycle hook with
   PSC001..PSC004 violation codes)"*).
3. **MISSING — `change-driven` workflow imposes Implement Team-specific
   constraints not yet referenced**: rule S-8 (in `AGENTS.md` §"S-8 — No
   Writes Outside Active Change Owned Files") obligates an Implement L3
   to honor `.local/.agent/active/<change-id>/owned_files.txt` plus the
   `.local/.agent/handoff/` append-only outbox. Implement Team §4
   "Standard Workflow" step 1 is *"ORIENT — Read task spec, design
   reference, owned file list"* — silent on the change-folder owned-files
   discipline. Same gap for Review/Test Standard Workflows.
4. **DUPLICATE — Tools/Skills column in §1 (SKILL.md AgentTeam table)
   and the per-team `### Tools/Skills` blocks in this file** restate the
   same tool sets in two surfaces; OK because team-roles.md is the
   long-form, but every drift in SKILL.md must propagate here per
   ST-11 / WX-6 cascade rule.

### Verdict for v9.0.0

**REFRESH** — Add the 11 missing workflow rows to the Team Participation
Matrix; add `EditNotebook` + `pre_shell_call` to Implement Tools/Skills;
add a single `change-driven` ORIENT-step note to Implement / Review /
Test workflows referencing `references/agent-workspace.md` (the v8.3.0
PV-09 10th SF-4 reference). The 5-team taxonomy stays — no team capability
added in v8.x warrants a 6th team (verification primitives map to the
existing Test team via the v5.4.0 `verify` stage primitive; shell-proxy
maps to Implement via the existing `Shell` tool with hook-side
enforcement).

### Improvement candidates for v9.0.0

1. **Refresh Team Participation Matrix (§7) to 22 templates** — extend
   the matrix to cover all 22 workflow templates listed in SKILL.md
   §"Template Quick-Reference" (lines 447-471). Use existing **Primary**
   / **Active** / **—** vocabulary. Estimated +12 lines (one row per
   missing template, table is grid-aligned).
2. **Add `change-driven` ORIENT-step note** under Implement Team §4
   "Standard Workflow" step 1 (and mirror under Review §6 + Test §5)
   pointing to `references/agent-workspace.md`. Estimated +9 lines
   (3 lines × 3 teams).
3. **Refresh Implement Team Tools/Skills (§4)** — add `EditNotebook`
   (S-1 alignment) and `pre_shell_call` (v8.3.2 hook governance);
   note that `pre_shell_call` is a *constraint surface*, not a tool the
   agent invokes directly. Estimated +2 lines.
4. **Align frontmatter `last_updated`** to the actual cycle that touches
   the file (v9.0.0). Drift signal — frontmatter dates that lag content
   updates make CI staleness checks unreliable.
5. *(stretch)* — add a §3a "Verification primitives in v8.x" subsection
   to the Test Team that enumerates the 4 verify-stage outputs already in
   the output contract, so a reader can locate them without scrolling
   the YAML. Estimated +12 lines.

### SF-1 budget impact

Net change candidates 1+2+3+4 (= +23 lines) takes 522 → ~545 / 1000 — well
within Large tier headroom. Stretch +12 brings to ~557. Zero impact on
SKILL.md (no new SKILL row needed; refresh is body-only). Zero impact on
the 7 canonical version-sync locations (CP-3) since this is content not
version metadata.

---

## Reference: execution-protocol.md

**Current line count**: 578 / 1000 (Large tier — 57.8% used)
**Last major content update**: v8.0.0 P-09 (per v9.0.0_version_history line
1585: *"`references/execution-protocol.md` §9 appended ≤30 lines (Large
tier 1000 ceiling preserved)"*). §7 added v7.2.0 (line 1279); §8 added
v7.2.4 / v7.3.0 (line 1302). Frontmatter `last_updated: "2026-04-04"` is
**stale by ≥ 3 sections** (§7+§8+§9 all postdate the frontmatter date by
real cycle work even if calendar-close).
**SKILL.md reference hit**: Y — 2 hits.
- Line 243 (Wave Coordination Modes table row): *"see
  references/execution-protocol.md §7"*
- Line 424 (Tier 2 nav table): *"`references/execution-protocol.md` | Task
  execution lifecycle, tool usage patterns"*

### Content inventory

| § | Title | Lines |
|---|-------|-------|
| 1 | Pre-Decision Phase (6-step + 8-section checklist + auto-detect + validation) | 23-122 |
| 1b | Verification-First Micro-Plan (L3 Tasks) | 124-152 |
| 2 | Checkpoint/Resume Mechanism (storage + schema + triggers + resume logic) | 154-246 |
| 3 | Exception Severity Classification (4 levels + classification rules + escalation flow + layer responsibility) | 248-323 |
| 4 | Human Intervention Breakpoints (7 HARD + 6 SOFT) | 325-351 |
| 5 | Execution Log Format | 353-376 |
| 6 | Progress Calculation (stage weights + status icons + ETR) | 378-425 |
| 7 | Wave Coordination Mode Selection (v7.2.0+) | 427-478 |
| 8 | Data-Instruction Envelope (v7.3.0+) | 480-549 |
| 9 | Deterministic Fence Expansion (v8.0.0+) | 551-578 |

The Pre-Decision Phase 6-step sequence at §1.26-35 (DETECT → COLLECT →
VALIDATE → FREEZE → RECOMMEND → DISPATCH) and the §3 4-severity
classification (`AUTO_RECOVER` / `PAUSE` / `HUMAN_INTERVENE` /
`FULL_ROLLBACK`) at lines 251-258 are still consistent with SKILL.md §
"Dispatch & Report Protocol" (lines 376-383). The 7 HARD + 6 SOFT
breakpoints at §4 are stable since v6.x.

### Drift findings

1. **MISSING — `change-driven` workflow envelope lifecycle has no §**.
   Per v9.0.0_version_history line 1774-1776: *"PV-06 (v8.3.0-pv06) —
   `change-driven` workflow template (commit `6bb83fa`, PR #85) ... New
   `workflow-system/agent/templates/builtin/change-driven.yaml` (22nd
   template); 4-stage propose → apply ↔ verify → archive lifecycle
   (with `apply ↔ verify` convergence loop, max_rounds=5 per W-8)."*
   Execution-protocol.md does not mention `change-driven`, the 4-stage
   lifecycle, the `apply ↔ verify` convergence loop's `max_rounds=5`,
   nor the append-only handoff envelope semantics (rule S-9). The
   user-query checklist explicitly flags this gap.
2. **MISSING — `memory_router` fast-path lookup at dispatch time has no §**.
   Per v9.0.0_version_history line 1834-1839: *"v8.3.3-memory-router ...
   Closes M-001 ('L0 re-derives workflow + stage decomposition from
   SKILL.md every dispatch (no fast-path lookup); planning consumes ~3K
   tokens even for repeat patterns')... `MemoryRouter` + `MemoryRouterError`
   + `lookup_case` + `is_router_enabled` ... First fast-path memory router;
   `MemoryCase` value type single-source-of-truth pattern."* The user
   query explicitly cites *"v8.4.0 memory-router fast-path"* as a
   coverage check item and it is absent.
3. **MISSING — `shell_proxy` + `pre_shell_call` lifecycle hook has no §**.
   Per v9.0.0_version_history line 1822: *"Created
   `src/devolaflow/shell_proxy/__init__.py` ... `src/devolaflow/lifecycle/
   pre_shell_call.py` (148 lines 5th canonical lifecycle hook with
   PSC001..PSC004 violation codes)"*. The 4 PSC violation codes never
   appear in this file. SKILL.md §"Lifecycle Hooks" (lines 388-398) lists
   3 hooks (`validate_dispatch` / `check_file_ownership` /
   `test_on_complete`) but does not explicitly mention `pre_shell_call`
   either (decomposition_analysis T4 captures this), so the gap is
   downstream of the SKILL.md gap. Adding to execution-protocol.md does
   NOT require a SKILL.md edit (T1 budget-safe).
4. **STALE — Filename vs content scope mismatch**. The file is named
   `execution-protocol.md` and the SKILL.md Tier 2 nav row labels it
   *"Task execution lifecycle, tool usage patterns"* (line 424), but the
   actual content is **Pre-Decision + Checkpoint + Exception + Breakpoint
   + Logging + Progress + Wave-coord + Envelope + Fence**, not the L0
   `ASSESS → SELECT → DECOMPOSE → DISPATCH → VERIFY → GATE → REPORT`
   protocol that SKILL.md §"AGENT MODE — Full Orchestration" lines
   136-144 calls "Execution Protocol". The L0 6-step protocol lives only
   in SKILL.md. New v9.0.0 readers consulting `execution-protocol.md`
   for "the execution protocol" will not find it. Either rename the
   reference, or extract the L0 protocol from SKILL.md into here as a
   §0 "L0 Execution Protocol" (would relieve SKILL.md headroom — couples
   with theme T1).
5. **DUPLICATE — `Wave Coordination` is split across SKILL.md and §7**.
   SKILL.md §"Wave Coordination Modes" (lines 233-243) lists the 5 mode
   rows; this file's §7 has the pairwise rubrics + named hybrid recipes
   + DevolaFlow mapping table. Acceptable per ST-11 cascade pattern, but
   v9.0.0 should make sure §7's "Mapping to DevolaFlow" table at lines
   458-468 stays in sync if T6 / T2 add new patterns.

### Verdict for v9.0.0

**EXTEND** — Add three new sections (§10 Memory Router Fast-Path, §11
Shell Proxy + Pre-Shell-Call Hook, §12 Change-Driven Workflow Envelope
Lifecycle) and rename `## 1. Pre-Decision Phase` to be the explicit
*pre*-execution phase — clarifying that the L0 execution protocol lives
in SKILL.md (or migrating it here per T1 spillover). The frontmatter
`triggers:` field should grow `"resolving change-driven dispatch"`,
`"memory router fast-path lookup"`, `"shell-proxy violation triage"`.

### Improvement candidates for v9.0.0

1. **Add §10 "Memory Router Fast-Path Lookup (v8.3.3+)"** — verbatim
   index entry shape, 3 TTL constants, the `is_router_enabled` env-flag
   `DEVOLAFLOW_MEMORY_ROUTER=1`, and the SKILL.md §"Workflow Selection"
   `shell-proxy` row's coupled flag `DEVOLAFLOW_RTK_PROXY=1` (line 175).
   Estimated +35..40 lines (mirrors §7 size).
2. **Add §11 "Shell Proxy + Pre-Shell-Call Hook (v8.3.2+)"** — list the
   4 PSC001..PSC004 violation codes verbatim with their triggering
   conditions; cite the `WHITELIST` single-source-of-truth pattern from
   `src/devolaflow/shell_proxy/registry.py`; couples with v9.0.0 theme
   T2 (A-5 registry rule). Estimated +35..40 lines.
3. **Add §12 "Change-Driven Workflow Envelope Lifecycle (v8.3.0+)"** —
   the 4-stage propose → apply ↔ verify → archive sequence with
   `max_rounds=5`; cross-link rule S-8 (owned_files.txt) and S-9
   (handoff envelope append-only); cite `references/agent-workspace.md`
   as the artifact spec authority. Estimated +25..30 lines.
4. **Reconcile §1b Verification-First Micro-Plan with the L3 dispatch
   pattern** — §1b template lives in this file but is not cited from
   SKILL.md. Either cross-link from SKILL.md §"L0 Execution Protocol"
   step 4 (DISPATCH), or move §1b to `references/team-roles.md` §4
   "Implement Team Standard Workflow" since hotfix tasks are owned by
   Implement. Estimated 0 line delta if relocated; +1 if cross-linked.
5. **Refresh frontmatter** — bump `last_updated` to v9.0.0 release date;
   add the 3 new triggers from §§10-12.

### SF-1 budget impact

Candidates 1+2+3 = +95..110 lines (cap +120 to stay safe). 578 → ~688..698
/ 1000 — well within Large tier ceiling (∼30% margin remaining). The
file does NOT trigger SKILL.md edits because the SKILL.md §"Lifecycle
Hooks" table can stay at 3 rows with a hyperlink to §11 here (deferred
under v9.0.0 theme T1 budget). Theme T7 (cache-layout governance v2)
**may** add a §13 byte-identicality assertion mention but that fits in
≤ 8 lines.

---

## Reference: repo-modes.md

**Current line count**: 282 / 1000 (Large tier — 28.2% used)
**Last major content update**: pre-v7.0.0 (no entry in v9.0.0_version_history
shows any cycle touched `references/repo-modes.md`; the v7.x adapter registry
work touched `runtime-plugins.yaml` instead). Frontmatter
`last_updated: "2026-04-04"` is plausibly **the genuine canonical date**
for this file — the 3-mode taxonomy is structurally stable.
**SKILL.md reference hit**: Y — 2 hits.
- Line 409: *"Override: `repo_mode` in `.workflow/config.yaml`. Full
  detection: `references/repo-modes.md`"*
- Line 419 (Tier 2 nav table): *"`references/repo-modes.md` | Repo
  detection, mode-specific behavior"*

### Content inventory

| § | Title | Lines |
|---|-------|-------|
| 1 | Mode Definitions (3 modes × 11 attribute rows + Other-Git variants 4 platforms) | 22-46 |
| 2 | Feature Matrix (20 features × 3 modes) | 48-75 |
| 3 | Detection Logic (algorithm + 6 regex rows + URL normalize + CI fallback + override) | 77-147 |
| 4 | CI/CD Pipeline Templates (Local Makefile + GitHub workflows + GitLab + Gitea) | 149-200 |
| 5 | Mode Transition Rules + initialization actions | 202-229 |
| 6 | Integration with Workflow System (Pre-Decision + Mode-Aware Stage table + config schema) | 231-282 |

The 3 modes (`local`, `github`, `other-git`) at §1 lines 25-37 still match
SKILL.md §"Repo Mode Detection" lines 402-409 byte-for-byte:
*"`local` | No `.git` or no remote | Local build/test/lint only..."* etc.
The 20-feature matrix at §2 is exhaustive across the 3 modes. Detection
algorithm at §3 lines 82-100 still produces the same 4 variants
(`gitlab` / `gitea` / `bitbucket` / `generic`).

### Drift findings

1. **MISSING — `Codeberg` matched in regex but not assigned a variant**.
   §3 "Regex Patterns" line 110 lists *"Codeberg | `https://codeberg.
   org/owner/repo` | `git@codeberg.org:owner/repo` | `codeberg\.org[:/]`"*,
   but §1 "Other-Git Variants" lines 41-46 lists only 4 variants
   (`gitlab` / `gitea` / `bitbucket` / `generic`). Either Codeberg should
   collapse to `gitea` (Codeberg runs Forgejo, a Gitea fork) or be
   promoted to its own variant — currently a soft contradiction.
2. **STALE — §6 "Mode-Aware Stage Behavior" omits `verify` and `monitor`
   stage primitives**. The table at lines 244-250 lists `Implement`,
   `Review`, `Test`, `Gate`, `Release`, `Deploy` rows, but SKILL.md §
   "Stage Primitives Index" (lines 250-296) advertises 14 primitives
   including `verify` (lines 282) and `monitor` (line 290). v5.4.0's
   `product-verification` template requires the `verify` row in this
   table because mode-aware behavior differs (local mode skips visual
   regression baselining; github mode uploads visual diffs as PR
   artifacts).
3. **MISSING — No mention of v8.4.0 RTK plugin entry / `runtime-plugins.
   yaml` interaction with mode detection**. v9.0.0_version_history line
   1745 cites *"`runtime-plugins.yaml` registry (schema_version 1) with
   `min_version` + `local_fallback_path` per plugin"* — `local_fallback_path`
   has implicit per-mode behavior (active in `local` mode) that this
   file should at least reference.
4. **MINOR — §3 "User Override" config example uses `.workflow/
   config.yaml`** (line 138) but SKILL.md §"Repo Mode Detection" line
   409 also says `.workflow/config.yaml`. Consistent; OK. *(No drift
   here — listed for completeness.)*

### Verdict for v9.0.0

**KEEP** — repo-modes.md is the most stable Batch C reference and does
not need a v9.0.0 cycle theme. Apply 2 small refresh items (Codeberg
variant clarification, `verify` + `monitor` row additions) as a
sustaining edit bundled with v9.0.0 PV-01 (T1) or PV-08 (sustaining).
Do not split, deprecate, or merge.

### Improvement candidates for v9.0.0

1. **Promote Codeberg to a `forgejo` variant** in §1 Other-Git Variants
   table OR add an explicit *"Codeberg → variant: `gitea` (Codeberg
   runs Forgejo, a Gitea fork)"* footnote. Estimated +2 lines either
   way.
2. **Add `verify` and `monitor` rows to §6 "Mode-Aware Stage Behavior"
   table** — match the 3 columns × 2 stages = 6 cells, mirroring how
   `Test` and `Deploy` rows are populated. Estimated +4 lines (2 rows ×
   1 line per row, rendered as table).
3. **Add a §7 "Plugin / Tool Interaction with Mode Detection"** —
   reference `runtime-plugins.yaml` (v8.3.0 PV-01) `local_fallback_path`
   semantics; couples with v9.0.0 theme T2 (A-5 registry rule). Keeps
   the file self-contained for repo-mode-related debugging. Estimated
   +12..18 lines.
4. *(stretch)* Add `nines.toml` location detection note to §3, since
   v6.1.1 demonstrated *(verbatim from
   v9.0.0_decomposition_analysis.md §"Performance trajectory highlights"
   #1)*: *"Tool-config-only NineS breakthrough at v6.1.1 — 0.7405 →
   0.8805 (+18.9% / +27.97% capability mean) with zero `src/` edits via
   `data/golden_test_set/` + `nines.toml`"* — and the eval tool's
   presence is mode-affected (local mode runs `nines self-eval` only
   from a checked-in config; github mode uploads JSON to a release
   asset). Estimated +6 lines.

### SF-1 budget impact

Candidates 1+2+3 = +18..24 lines. 282 → ~300..306 / 1000 — comfortable
budget. Stretch +6 brings to ~312. Zero SKILL.md edits needed.

---

## Batch C summary table

| Reference | Lines / 1000 | Frontmatter date | Verdict | Drift count | Improvement count | Est. LOC delta |
|-----------|--------------|------------------|---------|-------------|-------------------|----------------|
| `team-roles.md` | 522 / 1000 | 2026-04-04 (stale) | **REFRESH** | 4 | 5 (incl. 1 stretch) | +23..35 |
| `execution-protocol.md` | 578 / 1000 | 2026-04-04 (stale) | **EXTEND** | 5 | 5 | +95..120 |
| `repo-modes.md` | 282 / 1000 | 2026-04-04 (current) | **KEEP** | 3 | 4 (incl. 1 stretch) | +18..30 |
| **Batch C total** | 1382 / 3000 | — | mixed | 12 | 14 | **+136..185** |

Net Batch C LOC impact: ≤ +185 across 3 files, ≤ 698-line peak file
(`execution-protocol.md` post-EXTEND), all within the SF-1 Large tier
1000-line ceiling. Zero forced SKILL.md edits (no new SKILL row needed),
zero forced reference-list mutation (count stays at 11). All Batch C
work would map cleanly to either v9.0.0 PV-01 (T1 SKILL.md compression
spillover) or PV-08 (sustaining bundle).

---

## Cross-batch observations

*(Notes for Batch A / Batch B authors and the consolidation step. These
extend the v9.0.0_decomposition_analysis themes T1..T8 with reference-
review-specific evidence.)*

1. **Frontmatter date drift is systemic, not local.** All 3 Batch C
   files declare `last_updated: "2026-04-04"`. Per the v9.0.0
   version_history, real content edits to `execution-protocol.md`
   landed at v8.0.0 (§9 fence expansion) and v7.x (§7+§8). v9.0.0 should
   add a `tests/test_reference_frontmatter_freshness.py` that asserts
   `last_updated` ≥ the SHA-bumped touch date of any non-frontmatter
   line in the file. Couples with theme T8 (audit-cadence W-rule
   discipline).
2. **The 11-reference set is structurally consistent**, but the
   *content* lag is concentrated in references that should track
   schema-coupled primitives. `execution-protocol.md` is the worst
   offender for this batch (3 v8.3+ primitives missing). Suggest
   v9.0.0 adopt a "schema-coupled reference" tier annotation in the
   frontmatter (e.g., `couples_to: ["schemas/lean-dispatch.yaml",
   "src/devolaflow/lifecycle/"]`) so that any change to a coupled
   surface triggers a CI reminder to refresh the reference. Couples
   with theme T7 (cache-layout governance v2 multi-baseline test).
3. **Team Participation Matrix gap (team-roles.md §7)** is the
   single biggest content drift in Batch C and is purely additive
   (11 missing rows). It can be closed in a single PR by inserting
   the rows from SKILL.md §"Template Quick-Reference" and is a good
   v9.0.0 PV-01 sub-task that doesn't require any other theme work.
4. **The L0 Execution Protocol lives only in SKILL.md.** The
   `execution-protocol.md` filename suggests otherwise; readers who
   load this file via SKILL.md §"Reference Navigation Guide" Tier 2
   row "Task execution lifecycle, tool usage patterns" will not find
   the L0 6-step (or 7-step) protocol. T1 SKILL.md compression has a
   natural spillover candidate here: extract SKILL.md §"AGENT MODE —
   Full Orchestration" "Execution Protocol" 7-step list (lines
   136-144) into `execution-protocol.md` §0 (with cross-link from
   SKILL.md). Saves ≥ 9 SKILL.md lines, satisfies the filename, gives
   T1 a concrete ≥ 9-line headroom delta. **Strong recommendation.**
5. **Repo-modes is the only Batch C reference that does not couple to a
   v9.0.0 SI-1 theme.** Per the
   v9.0.0_decomposition_analysis.md §"Theme-to-PV Mapping Sketch",
   none of T1..T8 lists `repo-modes.md` in its file-level scope.
   The 3 sustaining-edit items above can land in a single v9.0.0 PV-08
   "sustaining bundle" or be deferred to v9.0.x without affecting the
   MAJOR rollup readiness.
6. **No new team or new mode required.** SKILL.md still sells the
   5-team, 3-mode taxonomies; v8.x added capabilities (verification
   primitives, shell-proxy hook) without expanding either taxonomy.
   The Batch C verdict therefore aligns with the v9.0.0 SI-1
   philosophy: *"address the ceilings themselves rather than thread
   another micro-compression through"* (per
   v9.0.0_decomposition_analysis.md §"Executive Summary"). No taxonomy
   blow-up, just additive content within existing frames.

---

*End of v9.0.0 SI-1 Reference Review (Batch C). All paths repo-relative
per Rule S-2; verbatim quotes from `v9.0.0_version_history.md`,
`v9.0.0_decomposition_analysis.md`, and the 3 references preserved per
Rule C-3. No reference files modified per task constraint. No sub-agents
spawned per task constraint.*
