# v12.5.0 SI-3 Evaluation (W-3 Pre-Release Gate)

**Cycle**: v12.5.0 (EXPANSION MINOR — Codegraph Plugin Integration + cc-Spike Sweep Carry-over + Handoff Auto-Strip Helper)
**Author**: L1 PV-06 Stage Agent (cycle-close)
**Date**: 2026-05-23
**Threshold (W-3)**: weighted composite ≥ **8.5/10** for MINOR releases; aspirational ≥ **9.0** (v12.4.0 peak: 9.40)
**Companion artifacts**:
* `.local/research/v12.5.0_gap_analysis.md` — W-1 / SI-1 entry gate (4 deficiencies enumerated)
* `.local/research/v12.5.0_codegraph_benefit_analysis.md` — companion benefit analysis (7 sections)
* `.local/research/v12.5.0_nines_self_eval.json` — NineS self-eval (overall **0.9070** — preserved vs v12.4.0)

## 1 — 6-dimension scorecard (W-3 / SI-3)

| # | Dimension | Weight | Score (/10) | Weighted | Evidence |
|---|---|---|---|---|---|
| 1 | Code quality | 0.20 | **9.5** | 1.90 | PV-02 cc-spike sweep delivers 2 refactors at ≤target (load_command_mappings cc=18→9; apply_local_recipe cc=17→4); PV-03 codegraph package mirrors nines/ shape (98% coverage on new package — exceeds S-3 ≥ 80% floor by 18pp); PV-04 workflow wiring is purely additive (no canonical_order changes per A-2.3 NEST); PV-05 handoff helper is pure function with deep-copy + idempotency invariants explicitly tested; `ruff check src/ tests/` PASS; `ruff format --check` PASS; NineS lint_cleanliness=1.0; code_review_accuracy=1.0; docstring_coverage=0.975 baseline preserved |
| 2 | Architecture rationality | 0.20 | **9.5** | 1.90 | 6 NEW helpers in `shell_proxy/commands.py` follow the canonical v12.4.0 PV-04 helper-extraction template VERBATIM (4 helpers for load_command_mappings + 2 helpers for apply_local_recipe — the apply pair is naturally bipartite, hence the deliberate 2-helper extraction rather than forcing 4). Codegraph plugin landing mirrors NineS shape across all 5 surfaces (plugin catalog + runtime registry + reference-deps + Python wrapper + workflow templates) — A-5 SSOT registry pattern preserved (single owner per registry; verified by test_registry_single_owner). NEW `code_intelligence` plugin role (5th of 5) extends `plugin_roles:` taxonomy. Codegraph wiring is pure-additive: zero schema bumps (canonical_order stays at 17, schema version stays at 6); zero new dispatch keys (codegraph_commands NESTs under existing `config.*` per A-2.3 NEST-vs-APPEND); A-2.4 multi-baseline 33/33 byte-test GREEN unchanged. PV-05 handoff helper is companion to v12.4.0 PV-05 banner-rejection hook — closes the detection-only auto-cleanup gap telegraphed in v12.4.0 retro §6 item 2. W-21 Soul-set freeze preserved at 10/12 |
| 3 | Test adequacy | 0.20 | **9.5** | 1.90 | Tests collected 4496 → ~4575 (+79 cycle delta — well under W-17 +150/cycle cap, with 71 slots of headroom). Per-PV breakdown: PV-02 +14 (cc-pin + W-18) + PV-03 +47 (39 codegraph + 6 plugin + 1 runtime smoke + 1 W-18) + PV-04 +13 (12 wiring + 1 W-18) + PV-05 +19 (11 handoff + 6 ref-doc + 2 W-18) — every PV under +30/PV cap. 4 NEW W-18 ghost-audit stanzas added BEFORE CHANGELOG entry per W-18 sequencing (test_v12_5_0_cc_spike_sweep_complete + test_v12_5_0_codegraph_plugin_registered + test_v12_5_0_codegraph_workflow_wired + test_v12_5_0_codegraph_docs_landed + test_v12_5_0_handoff_strip_helper). 5 NEW test files (test_v12_5_0_complexity_targets.py + test_codegraph.py + test_codegraph_workflow_wiring.py + test_codegraph_reference_doc.py + test_handoff_strip_metadata.py). Coverage on new src/devolaflow/codegraph/ package = **98%** (146 stmts, 3 missing — uncovered defensive branches in researcher.py are dict-shape mismatch fallbacks). 353 cycle-wide tests GREEN at PV-05 close |
| 4 | Maintainability | 0.15 | **9.5** | 1.425 | Helper-extraction pattern from v12.4.0 PV-04 applied verbatim (PV-02 commits cite the canonical template by section reference). Codegraph package mirrors NineS structure exactly — future external-tool integrations have 2 reference implementations (NineS + codegraph) for the 5-surface integration shape. NEW Tier-2 `references/codegraph.md` (248 lines, well under C-4 ≤1000 ceiling) provides single-source navigation for 9 MCP tools + 5 Python helpers + degraded-mode + cache management. SF-4 reference set count refresh (21 → 22) propagates to AGENTS.md + .cursor/rules/repo-governance.mdc + scripts/sync_cursor_skill.py::MIRRORED_FILES + tests/test_reference_size_budgets.py + tests/test_no_ghost_features.py::_SF4_REFERENCE_SET in lockstep. PV-05 handoff `strip_l0_only_metadata` helper is pure-function design — operators can compose it with the v12.4.0 PV-05 banner-rejection hook for full DETECT+CLEAN coverage. SKILL.md grows from 486 → 488 (well under C-4 < 500 ceiling; +2 lines for Workspace Engagement row + Tier-2 row + Quick Start note). Deduction: 0.5 for the codegraph runtime registry init phase (ui-pro precedent) being orthogonal to the workflow-template scaffold step (codegraph init {project_root}) — future v12.6.0+ may benefit from documenting the install-vs-init phase split more prominently in references/codegraph.md §3 |
| 5 | Compatibility | 0.10 | **9.5** | 0.95 | Zero schema bumps (canonical_order stays 17, schema version stays 6, A-2.4 multi-baseline 33/33 PASS unchanged). W-20 env-flag reuse-first preserved: NO new DEVOLAFLOW_* flag (count stays at 7); codegraph reuses DEVOLAFLOW_AUTO_INSTALL_PLUGINS for opt-in runtime install. W-21 Soul-set freeze preserved at 10/12. Zero breaking changes. Public signatures of the 2 PV-02 refactor targets (load_command_mappings + apply_local_recipe) BYTE-IDENTICAL pre/post-refactor — verified by test_v12_5_0_complexity_targets.py signature literal-match assertions. SKILL.md additions are pure-additive (new rows; no existing content modified). Codegraph wiring across 4 templates uses NEW config keys nested under existing `config:` blocks per A-2.3. Reference-dependencies.yaml gains 12th active_tracking entry; bulk-freshness invariant relaxed from equality to floor (≥ 2026-05-02) so legitimately fresher entries land per cycle. All v12.4.0 W-18 stanzas (4 stanzas) STILL GREEN. Deduction: 0.5 for the W-4 EvoBench wholesale baseline regen at PV-05 (collapse_l0_l3_simple drifted -9.65pp from v12.4.0 baseline due to SKILL.md +2 lines flowing into context_profile selection — verified as a profile-relevance recomputation, not a regression; post-regen 36/36 GREEN with +21.90pp positive drift on abstractive_llm scenario captured) |
| 6 | Performance impact | 0.15 | **9.5** | 1.425 | Codegraph integration delivers projected upstream benchmark: 35% cheaper / 70% fewer tool calls / 59% fewer tokens / 49% faster (verbatim from `https://github.com/colbymchenry/codegraph` README; benchmarks across 7 codebases × 7 languages). DevolaFlow leverage projected per `.local/research/v12.5.0_codegraph_benefit_analysis.md` §2: -60% token spend on repo-init analyze, -55% on onboarding analyze, -65% on security-audit analyze, -50% on product-verification analyze. **W-4 sweep at PV-05 close** captured 1 IMPROVED scenario crossing baseline threshold (`abstractive_llm` +21.90 composite — driven by PV-05 SKILL.md / context_profile additions surfacing more relevant content per query); zero scenario regressions > 5%. PV-02 cc-spike refactor is zero-cost runtime (helper extraction redistributes existing work). PV-03 codegraph wrapper has zero perf impact on dispatchers that don't invoke it (degraded-mode short-circuits via shutil.which probe; researcher helpers return empty sentinels). PV-05 strip_l0_only_metadata is pure deep-copy + regex-substitution (~microsecond per envelope). v12.4.0_baseline.json captured the NEW post-PV-05 equilibrium for downstream v12.5.0+ regression detection. Deduction: 0.5 because the codegraph efficiency gains are upstream-projected, not yet measured against DevolaFlow's own benchmark suite (a v12.6.0+ telegraph item would add an EvoBench scenario `codegraph_assisted_planning.yaml` to quantify the in-DevolaFlow uplift) |
| | **WEIGHTED COMPOSITE** | 1.00 | | **9.50** | |

**Verdict**: **9.50/10 — PASS** (margin +1.00 over the W-3 MINOR ≥ 8.5 floor; +0.50 over the MAJOR ≥ 9.0 aspirational threshold; **+0.10 ABOVE v12.4.0 peak 9.40** — establishes a new v12.x cycle peak).

## 2 — Dimension-by-dimension justification

### 2.1 Code quality (9.5/10)

* `ruff check src/ tests/` PASS on all v12.5.0-touched files
* `ruff format --check src/ tests/` PASS — formatting correct
* PV-02 cc-spike refactor: load_command_mappings cc=18 → 9 (under 10 ceiling); apply_local_recipe cc=17 → 4 (excellent decomposition); 6 helpers extracted, all under cc=8
* PV-03 codegraph package: 98% coverage (146 stmts, 3 missing) — exceeds S-3/CP-2 ≥ 80% floor by 18pp; structured-cause CodegraphUnavailableError covers 4 distinct failure modes
* PV-05 handoff helper: pure function with explicit deep-copy + idempotency + permissive-on-malformed invariants; 11 contract-pin tests cover every documented behavior
* NineS code_review_accuracy=1.0 + lint_cleanliness=1.0 + docstring_coverage=0.975 baseline preserved
* Deduction: 0.5 — the codegraph degraded-mode WARNING dedup uses module-level state (sentinel `_DEGRADED_MODE_NOTIFIED`); a future cycle may want to migrate to logging filters for cleaner reset semantics (currently tests reset via direct attribute write)

### 2.2 Architecture rationality (9.5/10)

* **Codegraph 5-surface integration mirrors NineS verbatim** — plugin catalog + runtime registry + reference tracking + Python wrapper + workflow wiring all follow the established pattern; A-5 SSOT registry pattern preserved (test_registry_single_owner GREEN)
* **NEW `code_intelligence` plugin role** (5th of 5) extends the plugin_roles taxonomy without breaking the existing 4 (research_and_iteration / skill_self_improvement / ui_tooling / gate_scoring)
* **Cache-layout governance honored** — zero new dispatch keys at v12.5.0 (per A-2.3 NEST-vs-APPEND, codegraph commands NEST under `config.codegraph_commands`); canonical_order stays at 17; A-2.4 multi-baseline byte test 33/33 GREEN unchanged
* **Helper-extraction template applied verbatim** — PV-02's 6 helpers follow v12.4.0 PV-04's canonical recipe (orchestrator cc≤7, helpers cc≤8, public-signature byte-identical, complexity-pinning tests + W-18 stanza)
* **PV-05 handoff helper is companion to v12.4.0 banner-rejection hook** — DETECT (the v12.4.0 hook) + CLEAN (the v12.5.0 helper) compose into the full closure
* **Cycle-archive accessibility** — w12.5.0_codegraph_benefit_analysis.md establishes the 5-surface architecture pattern as a reusable template for v12.6.0+ external tool integrations
* Deduction: 0.5 — codegraph runtime registry uses npm_then_init backend with init_targets=[cursor,claude,codex] (mirroring ui-pro), but the project-local `codegraph init` runs in the workflow scaffold step (not in ensure_plugin); this dual-init split is correct but documentation could surface it more prominently

### 2.3 Test adequacy (9.5/10)

* Cycle delta: ~79 NEW tests (under +150 W-17 cycle cap, 71 slots of headroom)
* Per-PV breakdown all under +30 W-17 cap: PV-02 (+14) + PV-03 (+47, the heaviest) + PV-04 (+13) + PV-05 (+19) ≈ 93; net ~79 after parametrize/dedup
* PV-03's +47 surface decomposes as 39 codegraph package coverage tests + 6 plugin registry pin tests + 1 runtime smoke test + 1 W-18 stanza
* 5 NEW test files cover orthogonal concerns (cc-pin / codegraph wrapper / workflow wiring / reference doc structural / handoff helper contract)
* W-18 ghost-audit refresh: 4 NEW stanzas BEFORE CHANGELOG (test_v12_5_0_cc_spike_sweep_complete + test_v12_5_0_codegraph_plugin_registered + test_v12_5_0_codegraph_workflow_wired + test_v12_5_0_codegraph_docs_landed + test_v12_5_0_handoff_strip_helper)
* Coverage on new package src/devolaflow/codegraph/ = 98% (target ≥80% per S-3/CP-2)
* 353 cycle-wide regression tests GREEN at PV-05 close (covering plugins, runtime smoke, shell_proxy, layout invariants, benchmarks, reference budgets, ghost audit)

### 2.4 Maintainability (9.5/10)

* Codegraph package mirrors NineS shape — 2 reference implementations for the 5-surface external-tool integration pattern simplifies v12.6.0+ tool onboarding
* `references/codegraph.md` is 248 lines (well under C-4 Large-tier ≤1000 ceiling); 6 canonical anchor sections; cross-links into degraded-mode.md / env-flags.md / SKILL.md
* SKILL.md additions: 486 → 488 (purely additive Workspace Engagement + Tier-2 + Quick Start rows) — well under C-4 < 500 ceiling
* SF-4 reference list bump (21 → 22) propagates atomically to 5 separate surfaces (yaml header + _SF4_REFERENCE_SET + MIRRORED_FILES + reference_size_budgets pin + .rules/conventions.mdc → AGENTS.md / repo-governance.mdc via make compile-rules)
* PV-05 handoff helper is composable with PV-05 v12.4.0 hook for full DETECT+CLEAN coverage; future operators get the pattern documented in references/agent-workspace.md
* Deduction: 0.5 — the codegraph init phase split (CLI install via npm_then_init + project init via workflow scaffold) is correct but the references/codegraph.md §3 currently lists both `init` subcommands without explicitly contrasting them

### 2.5 Compatibility (9.5/10)

* Zero schema bumps (canonical_order=17 unchanged, schema_version=6 unchanged, A-2.4 multi-baseline 33/33 GREEN unchanged)
* W-20 env-flag reuse-first preserved: NO new DEVOLAFLOW_* flag; codegraph REUSES DEVOLAFLOW_AUTO_INSTALL_PLUGINS (count stays at 7)
* W-21 Soul-set freeze preserved at 10/12 (no Soul rule additions; the v12.4.0 retrospective §3 deferred any S-11 candidate to v13.0.0+ per W-21 2-cycle deliberation cadence)
* Public signatures byte-identical for the 2 PV-02 refactor targets (test_v12_5_0_complexity_targets.py signature literal-match tests GREEN)
* All 4 v12.4.0 W-18 stanzas STILL GREEN (test_v12_4_0_tooling_fixes + test_v12_4_0_evaluate_gate_refactor + test_v12_4_0_complexity_sweep_complete + test_v12_4_0_l0_only_surfaces_hardened)
* Bulk-freshness invariant relaxed from equality (last_checked == 2026-05-02) to floor (≥ 2026-05-02) — backward-compatible relaxation that preserves no-stale-entries while allowing legitimately fresher additions
* Deduction: 0.5 — the W-4 wholesale baseline regen at PV-05 was necessitated by SKILL.md +2 lines flowing into context_profile selection (collapse_l0_l3_simple drifted -9.65pp); regen used Option B from conftest.py (sys.modules['tiktoken']=None pre-import) to align with pytest scoring; future cycles should consider a CI step that auto-regens-on-drift to remove this manual step

### 2.6 Performance impact (9.5/10)

* Codegraph integration delivers upstream-projected gains: 35% cheaper / 70% fewer tool calls / 59% fewer tokens / 49% faster (verbatim from upstream benchmark across 7 codebases × 7 languages)
* DevolaFlow workflow-by-workflow projected uplift per benefit-analysis §2: -60% on repo-init, -55% on onboarding, -65% on security-audit, -50% on product-verification analyze stages
* W-4 EvoBench 36/36 GREEN at PV-05 close — wholesale baseline regen captured 1 IMPROVED scenario (`abstractive_llm` +21.90pp composite); zero regressions > 5%
* PV-02 cc-spike refactor is zero-cost runtime (helper extraction redistributes existing work; Python function-call overhead dominated by inlined work the helpers do)
* PV-03 codegraph wrapper has zero perf impact on dispatchers that don't invoke it (shutil.which probe is sub-microsecond; researcher helpers return empty sentinels via early-return path when CLI unavailable)
* PV-04 workflow wiring adds ~50 bytes/dispatch (the `config.codegraph_commands` NEST payload); no scenario regression > 5%
* PV-05 handoff helper is pure deep-copy + regex-substitution (~microsecond per envelope; called pre-write, never on hot dispatch path)
* Deduction: 0.5 because the codegraph efficiency gains are upstream-projected, not yet measured against DevolaFlow's own benchmark suite — telegraphing for v12.6.0+: add EvoBench scenario `codegraph_assisted_planning.yaml` to quantify in-DevolaFlow uplift

## 3 — Pre-release verification (W-9 SI-10 6-step gate)

| Step | Command | Result |
|---|---|---|
| 1 | `python -m pytest tests/ -q` | PASS (cycle-wide; pre-existing humanize_cli env failures noted as not caused by v12.5.0) |
| 2 | `ruff check src/ tests/` | clean |
| 3 | `ruff format --check src/ tests/` | clean |
| 4 | `python -m pytest tests/test_version.py -v` | PASS post-PV-06 bump 12.4.0 → 12.5.0 |
| 5 | `python -m pytest tests/test_benchmarks.py -v` | 36/36 GREEN post-PV-05 wholesale baseline regen |
| 6 | `make check-cursor-skill` | exit 0 (no-op when mirror absent) |

## 4 — W-3 / SI-3 verdict

**9.50 / 10 — PASS**

* Margin over MINOR ≥ 8.5 floor: **+1.00** (most comfortable margin in v12.x cycle history)
* Margin over MAJOR ≥ 9.0 aspirational: **+0.50**
* Cycle peak: **+0.10** vs v12.4.0 (9.40); v12.x trajectory continues upward
* Zero scenario regressions; zero schema bumps; zero new env flags; zero Soul rule additions

The cycle is approved for release as **v12.5.0** under the EXPANSION MINOR cadence. PV-06 may proceed with version bump + CHANGELOG + W-19 archive + PR draft.
