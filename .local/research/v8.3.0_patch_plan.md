# v8.3.0 Patch Plan

> Date: 2026-04-22 | Author: L0 Project Agent | Predecessors: `v8.3.0_gap_analysis.md`, `v8.3.0_openspec_deep_analysis.md`, `v8.3.0_design.md`
> Closes A6 (per-improvement patch validation) from `feedback_for_v8.2.0.md`. SI-1 gate completion artifact.

## Cycle Overview

| Metric | v8.3.0 forecast | Comparable: v8.2.0 | Comparable: v8.0.0 |
|--------|-----------------|--------------------|--------------------|
| Patch count (PV) | **9** | 5 | 13 |
| Release roll-up | 1 (v8.3.0) | 1 (v8.2.0) | 1 (v8.0.0) |
| Total increments | **10** | 6 | 14 |
| Estimated effort | ~52h aggregate | ~38h | ~110h |
| Forecast accept rate | 100% (9/9) | 100% (5/5) | 100% (13/13) |
| P6 cache layout transitions | **1** (v4 → v5) | 0 | 2 (v2 → v3 → v4) |
| Net new tests forecast | +400 to +600 | +533 | +726 |
| EvoBench scenarios end-of-cycle | ≥ 45 | 43 | 36 |
| SF-4 reference docs | 9 → **10** | 9 (held) | 8 → 9 |
| Soul rules | 7 → **9** | 7 (held) | 7 (held) |
| Architecture rules | 3 → **4** | 3 (held) | 3 (held) |
| Conventions rules | 8 → **9** | 8 (held) | 8 (held) |
| SKILL.md ceiling | 500 (held at 495) | 500 (held at 495) | 500 (held at 495) |

**Hard cycle gates (per W-9 SI-10) at every patch:**
1. `python -m pytest tests/ -q` — all PASS
2. `ruff check src/ tests/` — clean
3. `ruff format --check src/ tests/` — clean
4. `python -m pytest tests/test_version.py -v` — canonical 7 sync OK
5. `python -m pytest tests/test_benchmarks.py -v` — 0 regression > 5pp
6. `make check-cursor-skill` — exit 0

**Cycle invariants R1–R8** (carried from gap_analysis §3): SI-10 6/6 PASS, 100% accept, ≤ 1 P6 transition, SKILL.md ≤ 500, R5 byte-identical for stable APIs, EvoBench 0 regression, no absolute paths, single feat commit per PV (+ ≤1 coupling commit if CI requires).

---

## Patch Ledger (Forecast — to be retroactively confirmed in CHANGELOG)

### v8.2.1 — Plugin Runtime Auto-Install (USER-FLAGGED PATCH)

| Field | Value |
|-------|-------|
| **Effort** | M (4–6h) |
| **Risk** | LOW (additive new module, no edits to stable surfaces) |
| **Closes** | H-001 |
| **Predecessor in cycle** | v8.2.0 (no patch deps) |
| **Mandate source** | `feedback_for_v8.2.0.md` line 19–20: "nines, ui-pro 作为依赖插件，当调用对应功能时，应该先进行安装和更新到全局" |

**Owns (writable files, ≤ 6 per L3 task — split across waves):**
- `src/devolaflow/plugins/__init__.py` (NEW)
- `src/devolaflow/plugins/installer.py` (NEW, ~250 lines)
- `src/devolaflow/plugins/registry.py` (NEW, ~120 lines)
- `src/devolaflow/plugins/exceptions.py` (NEW, ~40 lines)
- `workflow-system/agent/knowledge/runtime-plugins.yaml` (NEW)
- `workflow-system/agent/templates/builtin/nines-assisted.yaml` (precondition stage added)
- `workflow-system/agent/templates/builtin/product-verification.yaml` (precondition stage added)
- `tests/test_plugins.py` (NEW, ~400 lines, 9+ test cases)

**Read-only:** `workflow-system/agent/knowledge/reference-dependencies.yaml`, `src/devolaflow/__init__.py`, `pyproject.toml`

**Wave decomposition:**

| Wave | Tasks | Parallelism |
|------|-------|-------------|
| W01 | T01 schema (registry yaml) + T02 exceptions module | parallel |
| W02 | T03 installer.py + T04 registry.py | parallel |
| W03 | T05 workflow integration (nines-assisted + product-verification) + T06 tests | parallel (disjoint files) |

**Acceptance criteria:**
- [ ] AC-1: `from devolaflow.plugins import ensure_plugin` resolves; returns version string
- [ ] AC-2: `tests/test_plugins.py` PASS (12+ test cases — +3 for npm_then_init backend) at coverage ≥ 80%
- [ ] AC-3: `runtime-plugins.yaml` schema-validates; both `nines` (backend=pip) + `ui-pro` (backend=npm_then_init, init_targets=[cursor, claude, codex]) entries present
- [ ] AC-4: `nines-assisted.yaml` + `product-verification.yaml` register precondition stage; smoke-test instantiation PASS
- [ ] AC-5: SI-10 6/6 PASS
- [ ] AC-6: 0 EvoBench regressions
- [ ] AC-7: Smoke-test against editable nines install at `/home/agent/workspace/NineS` succeeds (skipped if absent)
- [ ] AC-8: All 8 failure-mode catalog scenarios from design.md §6.5 covered by unit tests (mocked `subprocess.run` for both pip + npm)
- [ ] AC-9: `ensure_plugin('ui-pro')` runs init step for each init_target; any one target failing raises PluginInstallError loudly per S-5

**Q-1 RESOLVED 2026-04-22**: `ui-pro` = `https://github.com/nextlevelbuilder/ui-ux-pro-max-skill` — **npm-based, not pip**. 2-step install: `npm install -g uipro-cli` then `uipro init --ai <platform> --global` for each of cursor/claude/codex. Plugin runtime MUST support TWO backends (pip + npm_then_init per design.md §6.2). `ensure_plugin('ui-pro')` runs init_cmd_template for all init_targets; any per-target failure raises PluginInstallError per S-5.

**Patch invariants:**
- I-PV01-A: No edits to `src/devolaflow/__init__.py` (no version bump until v8.3.0 release)
- I-PV01-B: No edits to `schemas/lean-dispatch.yaml` (no P6 transition this PV)
- I-PV01-C: SKILL.md unchanged (no skill-format coupling per W-5 / SI-5)

**Branch:** `feat/v8.3.0-pv01-plugin-runtime`
**PR title:** `feat(v8.3.0-pv01): runtime plugin auto-install for nines + ui-pro`

---

### v8.2.2 — Rules Layer Foundation (S-8, S-9, A-4, C-9)

| Field | Value |
|-------|-------|
| **Effort** | S (3h) |
| **Risk** | LOW (rules-layer addition; recompile only) |
| **Closes** | H-002 + H-003 + part of M-004 (A-4 ADR rule) |
| **Predecessor in cycle** | v8.2.1 |

**Why this slot (early):** Rules MUST land before scaffold (v8.2.3) so the new soul/architecture rules are present when devs run `repo-init` for the first time on v8.3.0; otherwise the first scaffold writes a `.local/.agent/` tree that violates rules that don't yet exist (chicken-and-egg).

**Owns:**
- `.rules/soul.mdc` — append S-8 + S-9 (~80 lines added; total file growth ~100 lines)
- `.rules/architecture.mdc` — append A-4 (~30 lines added)
- `.rules/conventions.mdc` — append C-9 (~25 lines added)
- `.cursor/rules/repo-governance.mdc` — auto-regenerated by RuleCompiler
- `AGENTS.md` — auto-regenerated by RuleCompiler
- `.rules/.compile-hashes.json` — auto-updated
- `tests/test_rules_compile.py` (extension; +3 test cases for new rules)

**Acceptance criteria:**
- [ ] AC-1: `RuleCompiler('.rules/compile-config.yaml').compile_all()` succeeds
- [ ] AC-2: `.cursor/rules/repo-governance.mdc` re-compile passes drift check
- [ ] AC-3: `AGENTS.md` re-compile passes drift check
- [ ] AC-4: `tests/test_rules_compile.py` PASS (+3 new cases for S-8 / S-9 / A-4 / C-9 mention assertions)
- [ ] AC-5: New rules cite `.local/research/v8.3.0_design.md` §3.1–3.4
- [ ] AC-6: SI-10 6/6 PASS
- [ ] AC-7: AGENTS.md token estimate ≤ 6000 (compile budget); cursor MDC ≤ 8000

**Patch invariants:**
- I-PV02-A: No source code changes (`src/devolaflow/` untouched)
- I-PV02-B: No schema changes
- I-PV02-C: SKILL.md unchanged

**Branch:** `feat/v8.3.0-pv02-rules-layer-foundation`

---

### v8.2.3 — `.local/.agent/` Scaffold + Repo-Init Extension + G-1/G-2 Repair

| Field | Value |
|-------|-------|
| **Effort** | M (5h) |
| **Risk** | LOW–MEDIUM (`.local/` substrate change, but additive) |
| **Closes** | C-004 + M-001 + M-002 |
| **Predecessor in cycle** | v8.2.2 (rules in place before scaffold) |

**Owns:**
- `src/devolaflow/local/workspace.py` — extend `REQUIRED_DIRS` from 3 to 6 (`.agent/{active,handoff,archive}/`); extend manifest; idempotent re-run; repair G-1 (regenerate index.md) + G-2 (TRACKER + memory subdir)
- `workflow-system/agent/templates/builtin/repo-init.yaml` — extend `canonical_manifest` from 5 to 8 paths (add `.local/.agent/active/`, `.local/.agent/handoff/`, `.local/.agent/archive/`)
- `workflow-system/agent/SKILL.md` — update Repo-Init Pre-Dispatch Contract table (5 rows → 8 rows). **WARNING: SKILL.md edit; verify line count ≤ 500.**
- `tests/test_local_workspace.py` — extend with 5+ new test cases
- `tests/test_repo_init_contract.py` — extend canonical manifest assertion
- `Makefile` — add `make scaffold-agent` target (one-shot re-scaffold for existing repos)

**Acceptance criteria:**
- [ ] AC-1: `scaffold_local('.')` produces 8 directories under `.local/` (3 existing + 3 `.agent/*` + `memory/specs/`); idempotent (2nd call no-op)
- [ ] AC-2: `.local/index.md` lists all 13 actual subdirectories (closes G-1)
- [ ] AC-3: `.local/feedbacks/TRACKER.md` exists and contains scaffold-template content (closes G-2)
- [ ] AC-4: `.local/memory/MEMORY.md` exists and contains scaffold-template content (closes G-2)
- [ ] AC-5: `.local/.agent/{active,handoff,archive}/` exist and each has a `.gitkeep` placeholder
- [ ] AC-6: `repo-init.yaml::canonical_manifest` lists 8 paths
- [ ] AC-7: SKILL.md updated; line count ≤ 500
- [ ] AC-8: SI-10 6/6 PASS (including SKILL line-count test, build-skill 4 adapters)
- [ ] AC-9: `tests/test_local_workspace.py` PASS at coverage ≥ 80%

**Patch invariants:**
- I-PV03-A: Existing 3 `REQUIRED_DIRS` + their READMEs unchanged byte-identical (R5)
- I-PV03-B: No schema changes (handoff/active/archive schemas land in v8.2.4)
- I-PV03-C: SKILL.md edit limited to Repo-Init Pre-Dispatch Contract table

**Branch:** `feat/v8.3.0-pv03-agent-workspace-scaffold`

---

### v8.2.4 — Agent Workspace Schemas (Pure YAML, No Python)

| Field | Value |
|-------|-------|
| **Effort** | M (6h) |
| **Risk** | LOW (schema-only, validated independently) |
| **Closes** | C-002 + M-003 + M-005 (schema half) |
| **Predecessor in cycle** | v8.2.3 (`.local/.agent/` exists before schemas land) |

**Owns:**
- `schemas/agent-workspace/__init__.yaml` (NEW dir + index)
- `schemas/agent-workspace/change-goal.yaml` (NEW)
- `schemas/agent-workspace/change-acceptance.yaml` (NEW)
- `schemas/agent-workspace/change-spec.yaml` (NEW; OpenSpec delta sections — ADDED/MODIFIED/REMOVED)
- `schemas/agent-workspace/change-tasks.yaml` (NEW)
- `schemas/agent-workspace/change-status.yaml` (NEW)
- `schemas/agent-workspace/owned-files.yaml` (NEW; one-path-per-line text format)
- `schemas/agent-workspace/handoff-envelope.yaml` (NEW; 3 envelope_kind variants)
- `schemas/agent-workspace/agent-config.yaml` (NEW; per-project config)
- `schemas/agent-workspace/source-of-truth-spec.yaml` (NEW; M-004 binding)
- `.gitignore` — add per Q-5 exceptions: `!.local/.agent/`, `!.local/.agent/**`, `!.local/memory/specs/`, `!.local/memory/specs/**`. Keep gitignored: `operational.jsonl`, `session_state.json`, `prefs.md`, `plugin_install.log` (explicit re-ignores after the blanket exception).
- `tests/test_agent_workspace_schemas.py` (NEW, ~600 lines, 30+ schema validation cases)
- `tests/test_gitignore_policy.py` (NEW, ~80 lines, verify .local/.agent/ is tracked but operational.jsonl is not)
- `docs/agent-workspace-schemas.md` (NEW reference for devs editing artifacts by hand)

**Acceptance criteria:**
- [ ] AC-1: All 9 schema YAML files validate against jsonschema meta-schema
- [ ] AC-2: 30+ schema fixture validation tests PASS
- [ ] AC-3: `change-spec.yaml` correctly enforces OpenSpec delta heading pattern (`## ADDED Requirements`, `## MODIFIED Requirements`, `## REMOVED Requirements`)
- [ ] AC-4: `handoff-envelope.yaml` enforces append-only (S-9) via `seq` monotonic int constraint and discriminated `envelope_kind` union
- [ ] AC-5: `change-status.yaml` enforces FSM state values (PROPOSED/IN_PROGRESS/VERIFYING/ARCHIVED/ESCALATED)
- [ ] AC-6: Token budget assertions encoded as JSON Schema `maxLength` + line-count CI check
- [ ] AC-7: SI-10 6/6 PASS
- [ ] AC-8: No Python code touched (this PV is purely schema)

**Patch invariants:**
- I-PV04-A: NO `src/devolaflow/` edits
- I-PV04-B: NO Python tests beyond schema fixture validation
- I-PV04-C: SKILL.md unchanged

**Branch:** `feat/v8.3.0-pv04-agent-workspace-schemas`

---

### v8.2.5 — `agent_workspace/` Python API + Cache Layout v5 Transition

| Field | Value |
|-------|-------|
| **Effort** | XL (8–10h) |
| **Risk** | MEDIUM-HIGH (P6 transition + new package; the cycle's largest patch) |
| **Closes** | C-003 + M-005 (Python half) + M-006 |
| **Predecessor in cycle** | v8.2.4 (schemas land before code consuming them) |

**Owns:**
- `src/devolaflow/agent_workspace/__init__.py` (NEW)
- `src/devolaflow/agent_workspace/change.py` (NEW; `Change` dataclass + `ChangeStore`, ~400 lines)
- `src/devolaflow/agent_workspace/handoff.py` (NEW; `HandoffEnvelope` + `HandoffStore` with append-only enforcement, ~300 lines)
- `src/devolaflow/agent_workspace/archive.py` (NEW; `ArchiveManager` + delta-merge proposer, ~350 lines)
- `src/devolaflow/agent_workspace/lint.py` (NEW; CLI for token budget enforcement, ~150 lines)
- `src/devolaflow/agent_workspace/delta_parser.py` (NEW; OpenSpec ADDED/MODIFIED/REMOVED parser, ~250 lines)
- `schemas/lean-dispatch.yaml` — append `change_context` at position 16; bump version to 5
- `src/devolaflow/compressor.py` — extend `assert_dispatch_layout()` to accept v4 + v5
- `tests/test_agent_workspace.py` (NEW, ~800 lines, 40+ tests)
- `tests/test_dispatch_layout_v5.py` (NEW, ~250 lines, P6 + backward-compat tests)
- `tests/test_compressor.py` — extend with v5 cases (R5 byte-identical for v4)

**Acceptance criteria:**
- [ ] AC-1: `from devolaflow.agent_workspace import Change, HandoffEnvelope, ChangeStore, HandoffStore, ArchiveManager` resolves
- [ ] AC-2: Round-trip serialization: `Change.from_active_folder(p).to_active_folder(p2)` produces byte-identical files
- [ ] AC-3: `HandoffStore.write_envelope(env)` raises `EnvelopeImmutableError` if `seq` already exists
- [ ] AC-4: `ArchiveManager.archive(change_id)` moves folder, generates STATUS state=ARCHIVED, calls `consolidate_session` for learnings
- [ ] AC-5: `ArchiveManager.propose_merge(change_id)` produces a *proposed* delta-merged source-of-truth spec; does NOT auto-write
- [ ] AC-6: `python -m devolaflow.agent_workspace.lint <change-id>` enforces token budgets; exits non-zero on hard ceiling violation
- [ ] AC-7: `delta_parser.parse_spec(spec_md)` correctly extracts ADDED/MODIFIED/REMOVED requirements with stable headings
- [ ] AC-8: `assert_dispatch_layout(payload_v4)` PASS (R5 backward-compat); `assert_dispatch_layout(payload_v5)` PASS (new); v7.0.0 + v7.3.0 byte-baselines PASS
- [ ] AC-9: `tests/test_agent_workspace.py` PASS at coverage ≥ 80%
- [ ] AC-10: SI-10 6/6 PASS — including `tests/test_compressor.py` regression sweep
- [ ] AC-11: 0 EvoBench regressions > 5pp across 43 baseline scenarios
- [ ] AC-12: `tests/test_no_ghost_features.py` updated for new module exports

**Patch invariants:**
- I-PV05-A: P6 transition is THE ONLY one in this cycle (R3 mandate)
- I-PV05-B: `learnings.py` 14 public functions byte-identical (R5)
- I-PV05-C: `compressor.py::assert_dispatch_layout` accepts both v4 and v5 (R5)
- I-PV05-D: SKILL.md unchanged (skill changes deferred to v8.2.9)

**Branch:** `feat/v8.3.0-pv05-agent-workspace-api-and-layout-v5`

---

### v8.2.6 — `change-driven` Workflow Template

| Field | Value |
|-------|-------|
| **Effort** | M (5h) |
| **Risk** | LOW (additive workflow template) |
| **Closes** | H-004 |
| **Predecessor in cycle** | v8.2.5 (Python API consumed by template instantiation tests) |

**Owns:**
- `workflow-system/agent/templates/builtin/change-driven.yaml` (NEW, ~120 lines per design.md §7.1)
- `workflow-system/agent/templates/registry.yaml` — add row for `change-driven`
- `tests/test_template_change_driven.py` (NEW, ~250 lines, 10+ tests)
- `tests/test_template_registry.py` — extend (template_count 22 → 23)

**Acceptance criteria:**
- [ ] AC-1: `change-driven` template loads via existing template_engine
- [ ] AC-2: Template instantiation produces correct stage DAG: propose → (apply ↔ verify loop) → archive_gate → archive
- [ ] AC-3: `mode: lite` and `mode: full` parameter both validate
- [ ] AC-4: Smoke-test: instantiate `change-driven` with mock dispatch; verify `archive_gate.criteria` enforces composite ≥ 8.5
- [ ] AC-5: Registry row added; total template count 23
- [ ] AC-6: SI-10 6/6 PASS
- [ ] AC-7: All 22 existing templates continue to instantiate (no regression)

**Patch invariants:**
- I-PV06-A: No edits to existing 22 template YAMLs
- I-PV06-B: No edits to `template_engine`
- I-PV06-C: SKILL.md unchanged (skill row added in v8.2.9)

**Branch:** `feat/v8.3.0-pv06-change-driven-template`

---

### v8.2.7 — Auto-Generated REPORT.md Surface

| Field | Value |
|-------|-------|
| **Effort** | M (5h) |
| **Risk** | LOW (additive reporter module; opt-in default-off) |
| **Closes** | H-005 |
| **Predecessor in cycle** | v8.2.6 (workflow template available so reporter can document the lifecycle) |

**Owns:**
- `src/devolaflow/agent_workspace/reporter.py` (NEW, ~400 lines)
- `src/devolaflow/agent_workspace/templates/change_report.md.j2` (NEW Jinja2 template)
- `src/devolaflow/agent_workspace/templates/workspace_report.md.j2` (NEW)
- `src/devolaflow/agent_workspace/templates/memory_report.md.j2` (NEW)
- `src/devolaflow/agent_workspace/templates/rules_report.md.j2` (NEW)
- `tests/test_reporter.py` (NEW, ~500 lines, 15+ tests)
- `Makefile` — add `make agent-reports` target

**Acceptance criteria:**
- [ ] AC-1: `render_change_report('test-change')` produces valid Markdown matching template
- [ ] AC-2: `render_workspace_report()` enumerates active + recent archived changes
- [ ] AC-3: `render_memory_report()` aggregates learnings stats by task_type
- [ ] AC-4: `render_rules_report()` enumerates rule layers + compile target status
- [ ] AC-5: `regenerate_all()` produces 4 REPORT.md files; idempotent (run 2× → byte-identical)
- [ ] AC-6: `python -m devolaflow.agent_workspace.reporter --all` CLI works
- [ ] AC-7: `tests/test_reporter.py` PASS at coverage ≥ 80%
- [ ] AC-8: SI-10 6/6 PASS

**Patch invariants:**
- I-PV07-A: Reports are opt-in; no auto-trigger from existing workflows yet (auto-trigger lands with archive in v8.2.5 — confirm wiring works post-v8.2.7)
- I-PV07-B: No edits to existing modules outside `agent_workspace/`
- I-PV07-C: SKILL.md unchanged

**Branch:** `feat/v8.3.0-pv07-reporter`

---

### v8.2.8 — Memory Bridge + Change-Aware Learnings

| Field | Value |
|-------|-------|
| **Effort** | M (5h) |
| **Risk** | MEDIUM (touches stable `learnings.py` — R5 critical) |
| **Closes** | H-006 |
| **Predecessor in cycle** | v8.2.7 (reporter consumes memory state for `memory_report`) |

**Owns:**
- `src/devolaflow/learnings.py` — extend `load_relevant_learnings` signature with `change_id: str | None = None` (R5 backward-compat additive)
- `src/devolaflow/learnings.py` — extend `capture_session_reflection` with `change_id: str | None = None`
- `src/devolaflow/agent_workspace/memory_bridge.py` (NEW, ~250 lines)
- `tests/test_learnings.py` — all 71+ existing tests MUST PASS byte-identical (R5 invariant)
- `tests/test_learnings_change_aware.py` (NEW, ~400 lines, 12+ tests)
- `tests/test_memory_bridge.py` (NEW, ~350 lines, 10+ tests)

**Acceptance criteria:**
- [ ] AC-1: `load_relevant_learnings('impl', path, change_id='add-foo')` returns merged in-change + global, in-change first
- [ ] AC-2: All 71+ existing `tests/test_learnings.py` PASS BYTE-IDENTICAL — no regression
- [ ] AC-3: `consolidate_change_on_archive('add-foo')` promotes durable learnings to global JSONL via existing `consolidate_session`
- [ ] AC-4: `hydrate_change_context('add-foo')` returns dict with all 7 artifact keys (goal, acceptance, spec, tasks, status, owned_files, learnings)
- [ ] AC-5: `tests/test_memory_bridge.py` PASS at coverage ≥ 80%
- [ ] AC-6: SI-10 6/6 PASS
- [ ] AC-7: 0 EvoBench regressions

**Patch invariants:**
- I-PV08-A: `learnings.py` 14 public functions remain in `__all__`; signatures additive only (R5 strict)
- I-PV08-B: New `change_id` parameter defaults to None — old callers unaffected
- I-PV08-C: SKILL.md unchanged

**Branch:** `feat/v8.3.0-pv08-memory-bridge`

---

### v8.2.9 — SKILL.md + References + Adapter Build + EvoBench Scenarios

> **Q-3 RESOLVED 2026-04-22**: Slash commands DEFERRED to v8.4.0+. M-007 closure deferred. v8.2.9 slimmed accordingly — adapter command files dropped; only adapter-builds for skill content (already supported by `make build-skill`).

| Field | Value |
|-------|-------|
| **Effort** | L (6–8h, was XL — Q-3 simplification) |
| **Risk** | MEDIUM (SKILL.md + new EvoBench + new ref doc) |
| **Closes** | H-007 + H-008 (M-007 deferred per Q-3) |
| **Predecessor in cycle** | v8.2.8 (full feature surface exists before docs/skill propagate) |

**Owns (slimmed per Q-3 — no slash command files):**
- `workflow-system/agent/SKILL.md` — add Workflow Selection row for `change-driven`; add §"Agent Workspace" body section pointing to references. **CRITICAL: line count ≤ 500 (currently 495/500; budget 5).** May need micro-compression elsewhere; budget audit pre-edit.
- `workflow-system/agent/references/agent-workspace.md` (NEW 10th reference, ≤ 1000 lines per SF-1 Large tier)
- `.cursor/rules/skill-format-rules.mdc` — update SF-4 `Current valid references` list (9 → 10)
- `AGENTS.md` (auto-regen via RuleCompiler if SF-4 wording changed)
- `tests/test_no_ghost_features.py` — update `_SF4_REFERENCE_SET` (9 → 10)
- `tests/test_version.py` — update mirror file count if mirror present (12 → 13)
- `scripts/sync_cursor_skill.py` — add new ref to `MIRRORED_FILES`
- `benchmarks/devolaflow_context/scenarios/agent_workspace_active.yaml` (NEW EvoBench scenario)
- `benchmarks/devolaflow_context/scenarios/handoff_envelope_density.yaml` (NEW EvoBench scenario)
- `workflow-system/agent/knowledge/reference-dependencies.yaml` — add OpenSpec entry to `active_tracking`
- `tests/test_skill_md_under_500_lines` (existing) — must continue PASS
- `tests/test_reference_size_budgets.py` — extend for the 10th ref doc

**Acceptance criteria:**
- [ ] AC-1: SKILL.md line count ≤ 500
- [ ] AC-2: `references/agent-workspace.md` ≤ 1000 lines per SF-1 Large tier
- [ ] AC-3: SF-4 reference set updated to 10 (test_no_ghost_features._SF4_REFERENCE_SET)
- [ ] AC-4: `make build-skill` PASS for all 4 adapters; outputs include 3 slash command files per adapter
- [ ] AC-5: 2 new EvoBench scenarios PASS at composite ≥ 85; total scenario count 43 → 45
- [ ] AC-6: 0 EvoBench regressions > 5pp across 45 scenarios
- [ ] AC-7: OpenSpec entry added to `reference-dependencies.yaml` `active_tracking` (relevance_score 5; gap_ids `[C-001, C-002, M-003, M-004]`)
- [ ] AC-8: SI-10 6/6 PASS
- [ ] AC-9: `make sync-cursor-skill` (no-op when mirror absent) succeeds; mirror parity tests self-skip if absent
- [ ] AC-10: AGENTS.md re-compile succeeds with new SF-4 wording

**Patch invariants:**
- I-PV09-A: SKILL.md MUST stay ≤ 500 — if breach, halt and propose budget audit + micro-compression PR before continuing v8.2.9
- I-PV09-B: 0 P6 schema bumps (P6 transition was in v8.2.5 only)
- I-PV09-C: All 11 adapters from v8.2.0 cycle (claude/cline/codex/continue/copilot/cursor/kimicode/openclaw/roo/windsurf/zed) MUST continue building — do not regress v8.2.0's 11/11 PASS

**Branch:** `feat/v8.3.0-pv09-skill-references-adapters-bench-commands`

---

### v8.3.0 — Release Roll-up

| Field | Value |
|-------|-------|
| **Effort** | M (4h) |
| **Risk** | LOW (version bump + retrospective + CHANGELOG) |
| **Closes** | (release closer; aggregates 9 PVs) |
| **Predecessor in cycle** | v8.2.9 |

**Owns:**
- `src/devolaflow/__init__.py` — `__version__` 8.2.9 → 8.3.0
- `python scripts/bump_version.py 8.3.0` — runs canonical 7-location sync per CP-3 / W-10
- `make sync-human-docs` — propagate version into EN/ZH human docs
- `CHANGELOG.md` — aggregate 9 PV bullets + cycle summary
- `.local/research/v8.3.0_evaluation.md` (NEW per W-3 / SI-3, weighted composite ≥ 8.5)
- `.local/research/v8.3.0_retrospective.md` (NEW per W-7 / SI-8, 4-section)
- `.local/research/v8.3.0_nines_self_eval.json` + `.md` summary (per W-2 / SI-2)
- `.local/research/v8.3.0_evobench_summary.md` (per-scenario delta table)
- `benchmarks/devolaflow_context/baselines/v8.3.0_baseline.json` (NEW; rebased from v8.2.0)
- `workflow-system/human/demo/version-timeline/versions.json` — add v8.3.0 entry per WX-2 (rule ST-7)
- 9 lightweight per-patch tags (`v8.3.0-pv01` ... `v8.3.0-pv09`) + annotated `v8.3.0` tag

**Acceptance criteria:**
- [ ] AC-1: `python -m pytest tests/test_version.py -v` PASS — canonical 7 locations all show "8.3.0"
- [ ] AC-2: `python scripts/bump_version.py 8.3.0` succeeds (or mirror auto-sync no-ops if `.cursor/skills/devola-flow/` absent)
- [ ] AC-3: `make sync-human-docs` PASS; human docs reflect v8.3.0
- [ ] AC-4: SI-3 evaluation report composite ≥ 8.5 (target ≥ 9.0 matching v8.0.0/v8.2.0 precedents)
- [ ] AC-5: NineS post-cycle composite ≥ 0.85 (target ≥ 0.90)
- [ ] AC-6: SI-8 retrospective covers all 4 sections (gaps / implemented / deferred / learnings)
- [ ] AC-7: EvoBench v8.3.0 baseline tracked; ≥ 45 scenarios; 0 regressions > 5pp vs v8.2.0
- [ ] AC-8: `versions.json` row added for v8.3.0 per ST-7
- [ ] AC-9: SI-10 6/6 PASS at the v8.3.0 final state
- [ ] AC-10: `build-skill` 11/11 adapters PASS
- [ ] AC-11: All 9 PV branches merged + lightweight tags pushed + annotated `v8.3.0` tag created

**Patch invariants:**
- I-PV10-A: NO new features in this PV (release roll-up only)
- I-PV10-B: Version bump pattern matches v8.0.0 + v8.2.0 precedent (use `bump_version.py`, do NOT hand-edit canonical 7)
- I-PV10-C: CHANGELOG cycle bullets reference per-patch micro-retrospectives

**Branch:** `release/v8.3.0`

---

## Forecasted Cycle Roll-up

### Aggregate test count growth (forecast)

| PV | Net new tests |
|----|---------------|
| v8.2.1 | +9 (test_plugins.py — 9 cases) |
| v8.2.2 | +3 (rule compile assertions) |
| v8.2.3 | +5 (workspace tests) |
| v8.2.4 | +30 (schema fixture validation) |
| v8.2.5 | +60 (workspace + dispatch v5 tests) |
| v8.2.6 | +10 (template tests) |
| v8.2.7 | +15 (reporter tests) |
| v8.2.8 | +22 (memory bridge tests) |
| v8.2.9 | +5 (ref + skill assertions) |
| **Cycle total** | **+159 net new tests** (forecast) |

(For comparison: v8.2.0 cycle added +533; v8.0.0 added +726. v8.3.0 forecast is more modest because patches are mostly additive new modules with focused test counts, not large gate refactors.)

### Aggregate file additions (forecast)

| Category | Count |
|----------|-------|
| New Python modules | 11 (`plugins/__init__.py + 3 plugins/*.py`, `agent_workspace/__init__.py + 6 agent_workspace/*.py`) |
| New schema YAMLs | 9 |
| New EvoBench scenarios | 2 |
| New workflow templates | 1 (`change-driven.yaml`) |
| New reference docs | 1 (`agent-workspace.md`) |
| New slash command sources | 3 |
| New REPORT.md surfaces | 4 |
| New `.local/.agent/` runtime files | (per-change, on-demand) |
| Modified existing files | ~20 |

### Aggregate cross-rule impact

| Rule | v8.2.0 → v8.3.0 |
|------|-----------------|
| Soul (P0) | 7 rules → 9 (S-8 + S-9 added) |
| Architecture (P1) | 3 rules (A-1..A-3) → 4 (A-4 added) |
| Conventions (P2) | 8 rules (C-1..C-8) → 9 (C-9 added) |
| Workflow (P3) | 15 rules → 15 (no change) |
| Style (P4) | 13 rules → 13 (no change) |
| **Total** | 46 → 50 rules |

---

## Plan Constraints Self-Check (per PLAN MODE template)

- [x] Every task row is L3 — Yes (all PV "Owns" lists are file-scope; L3 task agents will own them)
- [x] Stage headers specify L1 agent constraints — Yes (each PV has effort/risk/closes; L1 will dispatch waves)
- [x] Execution model — L0 dispatches PVs sequentially; each PV's L1 decomposes into waves (most patches are 1–3 waves)
- [x] Each wave: ≤ 5 tasks, pairwise disjoint writable files — verified for v8.2.1 (3 waves, 6 tasks)
- [x] Each stage: ≤ 7 waves — verified for all PVs (largest is v8.2.5 at 4 waves)
- [x] Task limits: impl ≤ 30 min, ≤ 6 writable files — verified per PV (largest single task is v8.2.5 T01 ChangeStore at ~400 lines, plan to split if exceeds)
- [x] Stage DAG: no cycles, gate-before-advance — Yes (linear PV chain; each PV's gate must PASS before next PV starts; matches A6 mandate)
- [x] Convergence stages: max_rounds + stagnation rule — `change-driven` template uses 5 max_rounds + escalate
- [x] Predecessors referenced by artifact path, not content copy — Yes (all PVs reference `.local/research/v8.3.0_*.md` paths)
- [x] All paths relative to repo root (S-2) — verified

## Invariants enforced
- [x] **P1**: L0 will dispatch all 9 PVs via `Task` tool; L0 will not implement (P1 trivial waiver applies only to v8.2.2 which is single-file rules edit < 100 lines per file — but EVEN here we delegate to L3 for consistency)
- [x] **P2**: Per-PV token budgets are L3 standard (~8K tok)
- [x] **P3**: All inter-layer messages use TaskDispatch / StatusReport YAML
- [x] **P4**: Each PV has max_iterations and escalation rules (5 rounds in convergence loops)
- [x] **P5**: Layers communicate via `.local/.agent/` artifacts, not conversation history
- [x] **P6**: 1 cache layout transition (v8.2.5 only); positions 1–15 byte-identical

---

## Cross-references

- `.local/research/v8.3.0_gap_analysis.md` (gap inventory + risk register R-A..R-J)
- `.local/research/v8.3.0_openspec_deep_analysis.md` (OpenSpec adoption recommendations §8)
- `.local/research/v8.3.0_design.md` (full design; §11.2 Q-1..Q-5 pending USER input)
- `.local/research/v8.2.0_patch_plan.md` (style precedent — 562 lines, 5 PV × 8 mandatory fields)
- `.local/research/v8.0.0_patch_plan.md` (style precedent — 862 lines, 13 PV)
- `CHANGELOG.md` (current v8.2.0 entry shows expected post-cycle aggregation format)
