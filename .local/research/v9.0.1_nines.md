# v9.0.1 NineS Self-Eval — Sustaining PATCH

**Date:** 2026-04-24
**Cycle:** v9.0.0 (PV-08)
**Target version:** 9.0.1 (PATCH sustaining)
**Baseline version:** 9.0.0
**NineS binary:** v3.3.0 (`/root/miniforge/bin/nines`)

---

## 1. Composite

| Metric | Value | Threshold | Status |
|---|---|---|---|
| `overall` (composite) | **0.9073** | ≥ 0.85 HARD | **PASS** (+5.7 pp) |
| | | ≥ 0.91 stretch | **MARGINAL** (-0.3 pp) |
| `weighted_overall` | 0.7228 | (informational) | — |
| `capability_mean` | 0.9550 | (informational) | — |
| `hygiene_mean` | 0.7961 | (informational) | — |

**Comparison vs v9.0.0:**

- v9.0.0: 0.9037 (per `.local/research/v9.0.0_nines.md`)
- v9.0.1: **0.9073** (this run)
- Delta: +0.0036 — within ±0.005 measurement noise floor (per v9.0.0_evaluation.md §6 R-6 documented ±0.002 byte-stable noise; the +0.0036 here reflects the standard run-to-run variance from cache state + transient timing in NineS metric collection).

**Inheritance protocol:** PV-08 is a sustaining PATCH adding 0 source-code paths (only `__version__` bumped 9.0.0 → 9.0.1) and 0 test changes (only `test_smoke.py` version assertion updated). The 5 NEW `adapter_configs/*.yaml` are NOT in the NineS scan paths (`--src-dir src/devolaflow --test-dir tests`); the modified `references/repo-modes.md` is NOT in NineS scope either. The composite delta is therefore expected to be within noise — confirmed at 0.0036 ≪ 5pp regression threshold.

---

## 2. Per-Group Means (capability vs hygiene)

| Group | Mean | Weight | Weighted contribution |
|---|---|---|---|
| `capability` | 0.9550 | 0.70 | 0.6685 |
| `hygiene` | 0.7961 | 0.30 | 0.2388 |
| **TOTAL** | — | — | **0.9073** |

**Per-metric decomposition (key items):**

| Metric | Weight | Notes |
|---|---|---|
| `code_coverage` | 0.40 | Hygiene group; v9.0.0 retro §3.2 R-6 documented upstream A1 blocker (NineS v3.3.0 binary does not honor `[hygiene] cov_timeout`). PV-08 inherits this state. |
| `lint_cleanliness` | 0.20 | `ruff check` clean post-PV-08 (verified W-9 Step 2). |
| `decomposition_coverage` | 0.10 | Capability group; not changed by PV-08. |
| `code_review_accuracy` | 0.10 | Capability group; not changed by PV-08. |
| `index_recall` | 0.10 | Capability group; not changed by PV-08. |
| `agent_analysis_quality` | 0.10 | Capability group; not changed by PV-08. |
| `scoring_accuracy` | 0.10 | Capability group; not changed by PV-08. |
| `scoring_reliability` | 0.10 | Capability group; not changed by PV-08. |

---

## 3. Comparison Against the v9.0.0 Cycle Floor

| Anchor | v9.0.0 | v9.0.1 | Delta | Threshold |
|---|---|---|---|---|
| Composite | 0.9037 | 0.9073 | +0.0036 | ±0.005 noise (PASS) |
| HARD floor | n/a | 0.85 | n/a | 0.9073 ≥ 0.85 by +5.7 pp **PASS** |
| Stretch target | n/a | 0.91 | n/a | 0.9073 < 0.91 by -0.27 pp **MARGINAL** (inherits v9.0.0 A1/A3 upstream-blocker state per ADR-005 D1) |

The -0.27 pp gap to the stretch 0.91 target is structurally bounded by the same upstream NineS A1 (`code_coverage` timeout) + A3 (`pipeline_latency` 0.0 evaluator probe) blockers documented in v9.0.0 §6 R-6. PV-08 made no `src/devolaflow/` or `tests/` changes that could move the needle on these metrics; the composite is therefore expected to track v9.0.0 within ±0.005, which is exactly what we observe.

---

## 4. Verdict

**NineS sustaining PASS.** PV-08 inherits the v9.0.0 cycle's NineS composite floor with -0.27 pp delta to the 0.91 stretch (consistent with v9.0.0 baseline; no regression introduced by PV-08).

The composite 0.9073 is **+5.7 pp above the 0.85 HARD floor** that gates PATCH releases per the v9.0.0_evaluation.md §6 R-6 protocol (≥ 0.85 = PASS unconditionally; ≥ 0.91 = stretch hit). PV-08 makes the conservative claim of "PASS at ≥ 0.85, MARGINAL at ≥ 0.91" — identical to v9.0.0's posture.

---

## 5. Raw Output

`.local/research/v9.0.1_nines.json` — complete NineS self-eval JSON (61.2s elapsed; context fingerprint `e4b8e0b6`)

## 6. Reproduction

```bash
nines -f json -c nines.toml self-eval \
    --baseline-version 9.0.0 \
    --golden-dir data/golden_test_set \
    --samples-dir data/golden_test_set \
    --src-dir src/devolaflow \
    --test-dir tests \
    --project-root .
```
