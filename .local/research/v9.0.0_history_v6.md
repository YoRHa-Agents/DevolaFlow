# v9.0.0 SI-1 Planning Gate — v6.x Era Manifest

**Scope**: All `## [6.x.y]` release headers in `CHANGELOG.md` plus the planning-only v6.0.0 thesis version and the never-tagged v6.3.x web-redesign milestone.

**Source-of-truth grep**: `grep -nE '^## \[6\.' CHANGELOG.md` returns 13 hits:

```
1582:## [6.2.1] — 2026-04-17
1594:## [6.2.0] — 2026-04-16
1679:## [6.1.5] — 2026-04-16
1697:## [6.1.4] — 2026-04-16
1712:## [6.1.3] — 2026-04-16
1731:## [6.1.2] — 2026-04-16
1757:## [6.1.1] — 2026-04-16
1777:## [6.1.0] — 2026-04-16
1812:## [6.0.5] — 2026-04-16
1837:## [6.0.4] — 2026-04-16
1870:## [6.0.3] — 2026-04-16
1894:## [6.0.2] — 2026-04-16
1912:## [6.0.1] — 2026-04-16
```

**Git-tag coverage**: `git tag --sort=v:refname | grep '^v6'` returns 13 tags from `v6.0.1` to `v6.2.1`. There is no `v6.0.0` tag (planning-only thesis) and no `v6.3.x` tag (web-only redesign that merged via PR #37 but never bumped `__version__`; the next bump went straight to `v7.0.0`). All 13 tags carry creator-date `2026-04-17`, while CHANGELOG headers for v6.0.1 through v6.2.0 read `2026-04-16` — see Data gaps.

**Predecessor outside era**: `## [5.4.2] — 2026-04-15` (`CHANGELOG.md` line 1932).
**Successor outside era**: `## [7.0.0] — 2026-04-17` (`CHANGELOG.md` line 1538) — the v7.0.0 cycle launched immediately after the v6.3 web PR merged; v7.0.0 was tagged 2026-04-17 per git ledger.

---

## v6.0.0 — *planning thesis, never tagged or released*

**Theme**: *"Close the last-mile gap on existing-but-unwired infrastructure, honor the v6.0 deprecation promise, and execute the long-deferred adapter expansion."* (verbatim from `.local/research/v6.0.0_improvement_advice.md` §0)

**Type**: PLANNING (no CHANGELOG section, no git tag — implementation rolled across v6.0.1 → v6.1.0 as 6 waves)

### Key changes (planning artifacts only — no code shipped under this label)
- **SI-1 planning gate** authored: `.local/research/v6.0.0_improvement_advice.md` (282 lines, EN) + `.local/research/v6.0.0_improvement_advice_zh.md` (Chinese mirror) — gap analysis enumerating Tier 1-4 deficiencies + benchmarked improvement projections.
- **Subagent evidence chain** (4 reports under `.local/research/v6.0.0/`): `subagent_tech_debt.md` (a8d3b4b5), `subagent_adapter_expansion.md` (ae305755), `subagent_gate_context_reinforcement.md` (44f42c73), `subagent_test_benchmark_infra.md` (1765ebb5).
- **Reproducible benchmark harness**: `.local/research/v6.0.0/bench_v6_candidates.py` + JSON dump `.local/research/v6.0.0/bench_results.json` covering all 11 v6 candidates with before/after deltas.
- **NineS v3.0.0 self-eval snapshot of v5.4.2**: `.local/research/v6.0.0/nines_self_eval_v5.4.2.json` (overall 0.7405, capability_mean 0.71498, hygiene_mean 0.8000) — the baseline that the entire v6.0+v6.1 rollup measured against.
- Identified the **dead-wire bug class** as the highest-leverage v6.0 intervention: `apply_round_escalation` and `merge_reinforcement_into_dispatch` had passing unit tests since v5.3.0 / v5.1.0-pre but **zero production callers**.

### Metrics (verbatim baseline projections — `v6.0.0_improvement_advice.md` §3 aggregate matrix)
| Dimension | v5.4.2 (baseline) | v6.0.0 target |
|---|---:|---:|
| Tests passing | 818 / 818 | 818 + new |
| Coverage | 91.07% | maintained ≥ 80% floor |
| DeprecationWarnings | 12 | 0 |
| Adapter platforms supported | 4 | 6 (incl. KimiCode, Windsurf) |
| EvoBench scenarios with regression baseline | 3 / 29 | 29 / 29 |
| Rule contradictions | 3 | 0 |
| Adapter-addition cost (LOC) | 89 | 25 (YAML) |
| Late-round convergence budget elasticity | 0% | +20% |
| Reinforcement mandates in round N+1 dispatch | 0 | up to 5 |
| MVP-SKILL.md cross-refs | 141 | 0 |

### Retrospective takeaways
- N/A directly (no v6.0.0 release retrospective). Downstream coverage in `.local/research/retrospective_v6.0_to_v6.1.md` §1: *"P0: Dead-wire bug — `apply_round_escalation` + `merge_reinforcement_into_dispatch` implemented + tested but unwired (no production callers since v5.3.0)."*

### Improvement advice received
- *"v6.0.0 is an unusually disciplined release candidate: every proposed change has either (a) explicit v6.0 roadmap documentation going back to v5.1, (b) benchmarked measurable improvement, or (c) explicit deferral with rationale."* (`v6.0.0_improvement_advice.md` §8)
- *"Closing that dead-wire gap is the single highest-ROI intervention in v6.0.0."* (`v6.0.0_improvement_advice.md` §8)
- Projected SI-3 weighted composite **9.1/10** ≥ 8.5 threshold → READY recommendation.

### User feedback
- No `feedback_for_v6.0.0.md` file exists. Earlier feedback in `.local/feedbacks/feedback_for_v5.0.0.md` carried forward.

### Cross-references
- Improvement advice: `.local/research/v6.0.0_improvement_advice.md` (EN) + `_zh.md` (mirror)
- Subagent reports: `.local/research/v6.0.0/subagent_*.md` (×4)
- Bench: `.local/research/v6.0.0/bench_v6_candidates.py`, `bench_results.json`
- NineS baseline: `.local/research/v6.0.0/nines_self_eval_v5.4.2.json`

---

## v6.0.1 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: MVP-SKILL retirement + plugin loader + rule reconciliation (verbatim from `CHANGELOG.md` line 1651 wave-by-wave reference table).
**Type**: PATCH

### Key changes (verbatim from `CHANGELOG.md` lines 1914–1922)
- **Removed `MVP-SKILL.md` legacy file**: *"Deleted `workflow-system/agent/MVP-SKILL.md` (317 lines) and swept 14 cross-references across README, quickstart (EN/ZH), demo, reference-dependencies, install.sh, build-site.sh, PR template, generate_human_docs.py, and design docs."*
- **`scripts/install.sh` keeps a backward-compat `mvp` alias** documented in-line that routes to `install_standalone`.
- **Removed `_BUILTIN_SPECS` hardcoded plugin duplicate**: *"Removed the 78-line `_BUILTIN_SPECS` list from `src/devolaflow/plugins/loader.py`. `create_default_registry()` now loads from `workflow-system/agent/plugins.yaml` (single source of truth) with auto-discovery; an 8-line emergency NineS stub handles the YAML-absent case with a logged warning."*
- **Rule reconciliation (TD-6)**: *"`.cursor/rules/change-process-rules.mdc` CP-3 rewritten to reference SF-3 as the authoritative version-location list (dropping the stale `CLAUDE.md (frontmatter + banner + body)` claim that contradicted the lightweight 38-line root CLAUDE.md)."*
- 5 `test_builtin_*` tests renamed to `test_repo_yaml_*` and rewritten against the real YAML; 2 new tests cover auto-discovery and emergency-stub fallback.
- **Fixed**: *"3 previously-silent rule contradictions: CP-3 vs SF-3 vs CLAUDE.md version-location counts now consistent."*

### Metrics (verbatim from `CHANGELOG.md` lines 1924–1930)
| Metric | Pre (v5.4.2) | Post (v6.0.1) |
|---|---:|---:|
| Tests passed | 818 | 820 (+2 emergency-stub) |
| EvoBench | 26/26 pass | 26/26 pass, no regressions |
| Lint | 0 errors | ruff check + format clean |
| Net LOC | — | −295 (+156/−451 across 16 files + 1 deleted) |
| MVP-SKILL refs in source tree | 141 | 0 |
| DeprecationWarnings still present | 12 | 12 (removal scheduled for v6.0.2) |

### Retrospective takeaways
> *"Wave 1 v6.0.1 `34bc586`: MVP-SKILL retirement (−141 refs), `_BUILTIN_SPECS` removal (single-source plugins.yaml), rule reconciliation (TD-6)"* (`retrospective_v6.0_to_v6.1.md` §2)

### Improvement advice received (from v6.0.0 SI-1 gate)
- TD-3 MVP-SKILL cleanup: *"Delete `workflow-system/agent/MVP-SKILL.md` (317 LOC); sweep `README.md`, `workflow-system/human/{en,zh}/quickstart.md`, …"* (`v6.0.0_improvement_advice.md` §2.1)
- V6-07/TD-2 `_BUILTIN_SPECS` removal: *"−92 LOC; 2 spec sources → 1"* (same source).

### User feedback
- None for this patch.

### Cross-references
- Improvement advice: `.local/research/v6.0.0_improvement_advice.md` §2.1
- Retrospective: `.local/research/retrospective_v6.0_to_v6.1.md` §2 row 1
- NineS snapshot: `.local/research/v6.0.1/nines_self_eval_v6.0.1.json`

---

## v6.0.2 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: [BREAKING] deprecated API removal + MIGRATION-v6.md (verbatim from `CHANGELOG.md` line 1652 wave reference).
**Type**: PATCH (carries one BREAKING removal documented in `MIGRATION-v6.md`)

### Key changes (verbatim from `CHANGELOG.md` lines 1896–1904)
- **Removed `evaluate_gate_with_nines` (BREAKING)**: *"Removed per v5.1 roadmap item P9. Use `evaluate_gate()` for gates, and call NineS separately via `devolaflow.nines.get_research_advice()` (defined in `devolaflow.nines.advisor`). See `MIGRATION-v6.md`."*
- **Removed `run_nines_advisor` (BREAKING)**: *"Removed. Advisor functionality was tied to the deprecated gate+NineS conflation. Use NineS directly or `devolaflow.nines.get_research_advice()`."*
- **Removed internal advisor helpers** (dead after `run_nines_advisor` removal): `should_invoke_advisor`, `_interpret_result`, `_extract_score`, `_extract_reasoning` and the `_SCORE_KEYS` / `_REASONING_KEYS` / `_APPROVE_STATUSES` / `_SCORE_THRESHOLD` constants; `GateVerdict` and `warnings` imports in `nines/advisor.py` also dropped.
- **5 test classes retired (29 tests total)** from `tests/test_nines.py`: `TestEvaluateGateWithNines` (6), `TestRunNinesAdvisor` (6), `TestShouldInvokeAdvisor` (4), `TestInterpretResult` (11), `TestDeprecationWarnings` (2).
- **Added `MIGRATION-v6.md`**: *"1-page migration guide documenting both removals, the dead helpers, and the stable v6.0 API surface."*

### Metrics (verbatim from `CHANGELOG.md` lines 1906–1910)
| Metric | Pre (v6.0.1) | Post (v6.0.2) |
|---|---:|---:|
| Tests passed | 820 | 791 (−29 retired) |
| EvoBench | 26/26 pass | 26/26 pass, no regressions |
| Lint | clean | ruff check + format clean |
| **DeprecationWarnings** | **12** | **0** |
| Net LOC in core removal (5 files) | — | **−519** (+6 / −525); MIGRATION-v6.md adds 32 lines |

### Retrospective takeaways
> *"Wave 2 v6.0.2 `f4d93fc`: Deprecated API removal (BREAKING), `MIGRATION-v6.md`, DeprecationWarnings 12 → 0"* (`retrospective_v6.0_to_v6.1.md` §2)

### Improvement advice received
- V6-06/TD-1: *"`evaluate_gate_with_nines` + `run_nines_advisor` still exported; explicit `removal in v6.0` comment at `gate/__init__.py:65`"* + *"−11 LOC fn signatures + wrapper, −12 DeprecationWarnings, ~20 tests retired"* (`v6.0.0_improvement_advice.md` §2.1).

### User feedback
- None for this patch.

### Cross-references
- Improvement advice: `.local/research/v6.0.0_improvement_advice.md` §2.1, §7 (risk: users importing removed APIs)
- Retrospective: `.local/research/retrospective_v6.0_to_v6.1.md` §2 row 2
- NineS snapshot: `.local/research/v6.0.2/nines_self_eval_v6.0.2.json`
- Migration guide: `MIGRATION-v6.md`

---

## v6.0.3 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: Dead-wire closure (`apply_round_escalation` + reinforcement merge) (verbatim from `CHANGELOG.md` line 1653 wave reference). **Single highest-ROI intervention in the v6.0 cycle.**
**Type**: PATCH (semantic-minor in spirit; closes v5.3.0 P4 + P8 dead-wire bugs)

### Key changes (verbatim from `CHANGELOG.md` lines 1872–1892)
- **`select_context()` is now round-aware**: *"New keyword argument `round_num: int = 1` (backward-compatible default). When `round_num > 1` the profile is routed through `apply_round_escalation()` before section selection, automatically applying the v5.3.0 P8 escalation defaults (+20% token budget on round 3, `model_hint: quality`, and critical-bumping of `rationalization_prevention` / `convergence_loop` / `gate_mechanism` sections)."*
- **CLI `--round N` flag**: *"`python -m devolaflow.task_adaptive_selector <task> --round 3 [--verbose]` exposes the new round-aware behavior on the command line."*
- **`ProposalGenerator.generate_round_dispatch(base_dispatch, verdict, round_num, target_score=85.0)`** in `src/devolaflow/feedback.py`: *"the production wiring that closes the v5.3.0 reinforcement dead-wire gap. Round 1 is pass-through; round 2+ with findings builds a `ReinforcementBlock` via `findings_to_reinforcement()` and merges it into a deep-copied dispatch via `merge_reinforcement_into_dispatch()`."*
- **`severity_floor` parameter on `generate_reinforcement`**: *"optional kwarg (default `\"major\"`) for explicit severity filtering at generation time."*
- **`tests/test_e2e_convergence.py`** (C2): *"new 7-test end-to-end integration suite that exercises `select_context` + round escalation + `generate_round_dispatch` + reinforcement merge as a realistic 3-round convergence."*
- **Fixed (dead-wire closure)** — *"v5.3.0 P8 finally wired: `apply_round_escalation` existed with passing unit tests since v5.3.0 but had no production callers. Now invoked automatically by `select_context()` on round > 1. v5.3.0 P4 finally wired: `merge_reinforcement_into_dispatch` existed with passing unit tests since v5.1.0-pre but had no production callers."*

### Metrics (verbatim from `CHANGELOG.md` lines 1882–1888)
| Metric | Pre (v6.0.2) | Post (v6.0.3) |
|---|---:|---:|
| Tests | 791 | **812 passed** (+21 new: 8 task-adaptive-selector, 6 feedback-reinforcement, 7 E2E) |
| Round 1 → round 3 budget | flat | **4800 → 5760 (+20% exact)** |
| `model_hint` round 3 | balanced | **balanced → quality** |
| EvoBench | 26/26 | 26/26, no regression |
| DeprecationWarnings | 0 | 0 (maintained) |
| NineS self-eval | 0.7405 | 0.7405 overall (no regression) |

### Retrospective takeaways
> *"Dead-wire bugs are the highest-leverage v6.0 intervention. Two infrastructure pieces (`apply_round_escalation`, `merge_reinforcement_into_dispatch`) had passing unit tests since v5.3.0 but zero production callers. Benchmark-measured ROI before wiring: 0. After wiring: +20% round-3 budget, explicit MUST-fix mandates in dispatch."* (`retrospective_v6.0_to_v6.1.md` §4 #1)
> *"**Next-iteration ask:** add a CI check that fails when a public API has no non-test caller in `src/`."* (same source — directly motivated v6.1.4's G1 dead-API guard)

### Improvement advice received
- V6-01 + V6-02 combined: *"Round N+1 of convergence currently identical to round N: no escalation, no reinforcement. L3 agents receive identical dispatches across failed rounds — the stagnation-detection rule (`gate/scorer.py:503-514`) fires because actual progress is impossible"* (`v6.0.0_improvement_advice.md` §2.2 row 3) → benchmarked delta **+4 rules + 6.7% budget**.

### User feedback
- None for this patch.

### Cross-references
- Improvement advice: `.local/research/v6.0.0_improvement_advice.md` §2.2 (Tier 2 Dead-Wire)
- Retrospective: `.local/research/retrospective_v6.0_to_v6.1.md` §2 row 3, §4 #1
- NineS snapshot: `.local/research/v6.0.3/nines_self_eval_v6.0.3.json`

---

## v6.0.4 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: AdapterRegistry + DataDrivenAdapter + KimiCode + Windsurf (verbatim from `CHANGELOG.md` line 1654).
**Type**: PATCH (semantic-minor in spirit — major architectural addition)

### Key changes (verbatim from `CHANGELOG.md` lines 1840–1857)
- **`AdapterRegistry`** (`src/devolaflow/adapters/registry.py`): *"central registry for all platform adapters with tier classification (`core`, `high_priority`, `tier_1`, `tier_2`), selective build via `build_selected()`, and `create_default_registry()` factory that pre-populates the 4 core adapters."*
- **`DataDrivenAdapter`** (`src/devolaflow/adapters/data_driven.py`): *"generic adapter driven by a YAML config file. Supports 4 transforms (`copy`, `copy_tree`, `copy_with_frontmatter`, `strip_frontmatter`), frontmatter injection, and line/char budget checks."*
- **`--tools` CLI flag**: *"`python -m devolaflow.build_skill --tools cursor,windsurf` builds only the named adapters."*
- **`adapter_configs/kimicode.yaml`** (KimiCode — Moonshot AI VSCode + CLI): *"Writes `SKILL.md` + `references/` + `examples/` under `.kimi/skills/devola-flow/` with platform frontmatter injection. 500-line budget."*
- **`adapter_configs/windsurf.yaml`** (Windsurf — Codeium): *"Writes a single `.windsurfrules` at the repo root with frontmatter stripped. 8000-char budget."* (Ships **with `[WARN]` known limitation** — see Known limitation block.)
- **4 new test modules (+46 tests, 812 → 858)**: `tests/test_adapter_registry.py` (15), `tests/test_data_driven_adapter.py` (18), `tests/test_kimicode_adapter.py` (7), `tests/test_windsurf_adapter.py` (6).

### Metrics (verbatim from `CHANGELOG.md` lines 1862–1868)
| Metric | Pre (v6.0.3) | Post (v6.0.4) |
|---|---:|---:|
| Tests | 812 | 858 (+46) |
| Registry adapters | 4 | 6 (4 core + 2 new via data-driven) |
| New LOC | — | 957 across 8 files |
| Lint | clean | ruff check + format clean |
| EvoBench | 26/26 | 26/26 pass, no regression |
| DeprecationWarnings | 0 | 0 (maintained) |
| NineS self-eval | 0.7405 | 0.7405 stable |

### Known limitation shipped with v6.0.4 (verbatim line 1859)
> *"Windsurf output produces a `[WARN]` status: current `SKILL.md` is ~25 KB, but Windsurf's `.windsurfrules` has an 8 KB char budget. The adapter builds correctly and the budget mechanism reports honestly, but the output exceeds Windsurf's practical size limit. A future release should add a compression transform (e.g. `compress_for_windsurf`) or a Windsurf-specific lean SKILL."*

### Retrospective takeaways
> *"Multi-platform adapter expansion is a YAML problem, not a Python problem. Every new adapter dropped from ~80 LOC Python to ~25 LOC YAML via `DataDrivenAdapter`."* (`retrospective_v6.0_to_v6.1.md` §4 #2)

### Improvement advice received
- R1/D1 (`v6.0.0_improvement_advice.md` §2.3): *"−71.9% LOC reduction per new adapter (89 → 25 lines)"*
- A1 KimiCode + A2 Windsurf identified as the two P1 platforms to validate the registry + data-driven pattern.

### User feedback
- None for this patch (Windsurf WARN later surfaced as a real bug — see v6.1.2).

### Cross-references
- Improvement advice: `.local/research/v6.0.0_improvement_advice.md` §2.3 (Tier 3 Adapter expansion)
- Subagent: `.local/research/v6.0.0/subagent_adapter_expansion.md`
- Retrospective: `.local/research/retrospective_v6.0_to_v6.1.md` §2 row 4, §4 #2
- NineS snapshot: `.local/research/v6.0.4/nines_self_eval_v6.0.4.json`

---

## v6.0.5 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: Schema parity + 29/29 EvoBench baselines (verbatim from `CHANGELOG.md` line 1655).
**Type**: PATCH

### Key changes (verbatim from `CHANGELOG.md` lines 1815–1828)
- **Schema parity test (`tests/test_schema_parity.py`, 6 tests)**: *"enforces field parity across `task-dispatch.schema.yaml`, `lean-dispatch.yaml`, and `gate-report.schema.yaml`. Closes TD-4 drift gap — any future field addition to one schema must be reflected in the other two (or added to an explicit `*_VERBOSE_ONLY` compromise set) or the test fails loudly with a message that points to the exact missing equivalent."*
- **Full EvoBench baseline coverage (`benchmarks/devolaflow_context/baselines/v6.0.5_baseline.json`)**: *"regression baselines for all 29 / 29 scenarios (was 3 / 29). Closes C1 — the 89.7pp regression-detection gap from `.local/research/v6.0.0_improvement_advice.md`."*
- **`benchmarks/devolaflow_context/generate_baseline.py`**: *"CLI utility to regenerate the baseline on demand. Supports `--output` for a custom path and works both directly … and via `-m`. Default output follows `devolaflow.__version__`."*
- **7 new benchmark tests** in `tests/test_benchmarks.py`: `test_v6_baseline_exists`, `test_v6_baseline_covers_all_scenarios` (strict set equality), `test_v6_baseline_scores_positive`, `test_runner_prefers_latest_baseline`, `test_v6_baseline_matches_current_results_within_tolerance` (staleness guard, ±5pp), `test_ten_percent_drop_is_flagged_as_regression`, `test_one_percent_drop_not_flagged`.
- **`runner.py` `load_baseline()`**: *"now prefers the newest `v*_baseline.json` by numeric-version order (e.g. v6.0.5 over v2.1.0). Legacy `v2.1.0_baseline.json` is kept as a fallback when no newer baseline exists."*

### Metrics (verbatim from `CHANGELOG.md` lines 1830–1835)
| Metric | Pre (v6.0.4) | Post (v6.0.5) |
|---|---:|---:|
| Tests | 858 | 871 (+13) |
| EvoBench scenarios with regression baseline | 3 / 29 | **29 / 29** |
| Lint | clean | ruff check + format clean |
| EvoBench | 26/26 | no regressions; baseline now runs on all 29 |
| NineS self-eval | 0.7405 | stable |

### Retrospective takeaways
> *"Regression detection needs 100% coverage. Before v6.0.5, only 3/29 scenarios had baselines — the other 26 could silently regress forever. Fix: auto-generate baselines as a CI step via `benchmarks/devolaflow_context/generate_baseline.py`."* (`retrospective_v6.0_to_v6.1.md` §4 #5)
> *"Schema parity needs enforcement. TD-4 parity tests caught 2 fields that would otherwise have drifted during Wave 4 (new verification fields in `lean-dispatch.yaml` but not `task-dispatch.schema.yaml`)."* (`retrospective_v6.0_to_v6.1.md` §4 #4)

### Improvement advice received
- C1 (`v6.0.0_improvement_advice.md` §2.4): *"Only 3 of 29 EvoBench scenarios have regression baselines (`v2.1.0_baseline.json`); 26 scenarios cannot detect composite regression … +89.7pp coverage gap closed."*
- TD-4 (`v6.0.0_improvement_advice.md` §2.4): *"Verification + reinforcement fields exist in 3 different shapes across `task-dispatch.schema.yaml`, `lean-dispatch.yaml`, `gate-report.schema.yaml`. Naming drift (`reinforcement`/`reinforce`, `severity`/`sev`) risks silent cross-doc bugs."*

### User feedback
- None for this patch.

### Cross-references
- Improvement advice: `.local/research/v6.0.0_improvement_advice.md` §2.4 (Tier 4 schema + test infra)
- Subagent: `.local/research/v6.0.0/subagent_test_benchmark_infra.md`
- Retrospective: `.local/research/retrospective_v6.0_to_v6.1.md` §2 row 5, §4 #4 + #5
- NineS snapshot: `.local/research/v6.0.5/nines_self_eval_v6.0.5.json`

---

## v6.1.0 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: *"Final release of the v6.0 rollup (6 waves, v5.4.2 → v6.1.0)."* (verbatim banner — `CHANGELOG.md` line 1779)
**Type**: MINOR (rollup release stamp closing the first v6 cycle)

### Key changes (verbatim from `CHANGELOG.md` lines 1782–1794)
- **Continue.dev adapter (`adapter_configs/continue.yaml`, A3)**: *"YAML-driven adapter for the OSS Continue IDE extension. Emits `.continue/rules/devola-flow.md` (frontmatter stripped) + `.continue/rules/references/` (full tree). Tier 1, 800-line budget."*
- **OpenClaw adapter (`adapter_configs/openclaw.yaml`, A4)**: *"YAML-driven adapter for the MIT-licensed OSS gateway. Emits `openclaw/SKILL.md` (frontmatter preserved) + `openclaw/references/`. Tier 2, 500-line budget."*
- **Golden snapshot tests for Cursor adapter (`tests/test_adapter_golden.py`, C3)**: *"4 tests locking down structural invariants — required sections, line-count band (400-520), frontmatter keys, must-not-contain list (MVP-SKILL / evaluate_gate_with_nines / run_nines_advisor), references tree (8 files), examples tree (3 files)."*
- **21 new coverage-focused tests** in `tests/test_exercise_modules.py` (C4): 8 for `devolaflow.cli`, 8 for `devolaflow.init_project`, 3 for `template_engine.composer`.
- **SI-8 retrospective artifact** (`.local/research/retrospective_v6.0_to_v6.1.md`): documents all 6 waves, implemented vs deferred items, key learnings, cross-wave metrics evolution, and SI-3 composite **9.23/10**.
- **SKILL.md — Round-aware dispatch note**: *"single-line addition at the end of \"Dispatch & Report Protocol\" (after `Full schemas:` line) documents `select_context(round_num=N)` escalation and `ProposalGenerator.generate_round_dispatch()` reinforcement merging (the v6.0.3 dead-wire closure, surfaced to agents)."*
- **`benchmarks/devolaflow_context/baselines/v6.1.0_baseline.json`**: full 29-scenario baseline regenerated for v6.1.0 (SKILL.md growth from 496 → 498 lines required refresh per SI-4).

### Metrics (verbatim from `CHANGELOG.md` lines 1797–1807)
| Metric | Pre (v6.0.5) | Post (v6.1.0) |
|---|---:|---:|
| Tests | 871 | 896 (+25 across 2 files) |
| Adapters | 6 | **8** (core 4 + KimiCode + Windsurf + **Continue.dev** + **OpenClaw**) |
| Overall coverage | 91.35% | **94.08%** |
| `devolaflow.cli` cov | 49% | **98%** |
| `devolaflow.init_project` cov | 59% | **94%** |
| `devolaflow.template_engine.composer` cov | 66% | **100%** |
| SKILL.md | 496 lines | **498 lines** (budget 500) |
| EvoBench | 29/29 | 29/29 pass, no regressions |
| DeprecationWarnings | 0 | **0** (maintained) |
| NineS self-eval | 0.7405 | **0.7405 stable across v5.4.2 → v6.1.0** |

### Known limitation (unchanged from v6.0.4)
> *"Windsurf output still produces a `[WARN]` status: current `SKILL.md` is ~24 KB, Windsurf's `.windsurfrules` has an 8 KB char budget. Future release should add a compression transform or a Windsurf-specific lean SKILL. Tracked as a next-iteration item."*

### Retrospective takeaways
> *"Honest benchmarks prevent ghost features. V6-04 `unwanted_hints` showed 0% improvement on current scenarios. We explicitly deferred it instead of shipping a feel-good feature. CP-1 (no ghost features) held."* (`retrospective_v6.0_to_v6.1.md` §4 #3)
> *"SKILL.md edits have benchmark-scoring side effects (discovered in Wave 6). The line-range extractor in `context_profiles.yaml` uses hardcoded line numbers that drift silently when SKILL.md grows. Wave 6 learned this the hard way: a 2-line insertion inside the Dispatch & Report Protocol section inflated the extracted token count for `lifecycle_hooks` (range 373-383) enough to squeeze out `rationalization_prevention`, dropping `feedback_regression` composite 99.33 → 90.34."* (`retrospective_v6.0_to_v6.1.md` §4 #6)

### Improvement advice received (forward — feeds v6.2 planning)
- *"Add a CI job 'dead API detector' — flags public APIs in `src/` with no non-test caller. (Follow-up to Wave 3 dead-wire closure learning.)"* (§7)
- *"Windsurf adapter compression transform to bring `.windsurfrules` under the 8 KB char budget (currently WARN at ~24 KB)."* (§7)
- *"Expand golden snapshot coverage to Codex/Claude/Copilot adapters (v6.1.0 C3 only locked Cursor)."* (§7)

### User feedback
- The retrospective §7 cites *"Address the new feedback at `.local/feedbacks/feedback_for_v6.1.0.md`"* — but no such file exists in `.local/feedbacks/`; the actual user feedback that landed for the v6.x era is `feedback_for_v6.2.0.md` (asks for web redesign) and `feedback_for_v6.3.x.md` (asks for v7+ context-management cycle). See Data gaps.

### Cross-references
- Retrospective: `.local/research/retrospective_v6.0_to_v6.1.md` (84 lines, full 6-wave SI-8 artifact)
- NineS snapshot: `.local/research/v6.1.0/nines_self_eval_v6.1.0.json`

---

## v6.1.1 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: NineS tool-config (+18.9% overall — biggest single jump in the entire v6.0+v6.1 rollup) (verbatim from `CHANGELOG.md` line 1657).
**Type**: PATCH (zero source changes; pure tooling/config — biggest score jump of the era)

### Key changes (verbatim from `CHANGELOG.md` lines 1759–1772)
- **`data/golden_test_set/`** (new): *"10 DevolaFlow-relevant NineS golden-test TOML fixtures spanning 3 dimensions (code_quality, analysis, evaluation). Unlocks NineS V1 scoring evaluators (`scoring_accuracy`, `scoring_reliability`, `scorer_agreement`, `eval_coverage`) which were previously 0.0 due to missing fixtures."*
- **`nines.toml`** (new): *"repo-root NineS config with project defaults, self-eval weights, and relative path bindings (`golden_dir`, `samples_dir`, `src_dir`, `test_dir`, `project_root`). Future `nines -c nines.toml self-eval` invocations auto-pick up correct paths."*
- **`tests/test_golden_test_set.py`** (8 tests) + **`tests/test_nines_config.py`** (5 tests): schema validation for the new fixtures + config.
- **Rule SI-2** (`.cursor/rules/self-improve-iteration-rules.mdc`): *"updated with the canonical self-eval invocation including `--golden-dir` and `--samples-dir` flags."*
- **No source changes to `src/devolaflow/`**; bench / coverage unchanged.

### Metrics (verbatim from `CHANGELOG.md` lines 1768–1772)
| Metric | Pre (v6.1.0) | Post (v6.1.1) |
|---|---:|---:|
| **NineS overall** | **0.7405** | **0.8805** (+18.9% — verified via `.local/research/v6.1.1/nines_self_eval_v6.1.1.json`) |
| **NineS capability mean** | **0.7150** | **0.9150** (+27.97%) |
| Tests | 896 | 954 (+58 from 13 new test functions; 10 TOMLs × 5 parametrized + 3 scalar + 5 config) |
| Ruff | clean | `ruff check` + `ruff format --check` clean |
| Source changes to `src/devolaflow/` | — | none |

### Known limitation (verbatim line 1775)
> *"`pipeline_latency` capability remains 0.0: upstream NineS v3.0.0 evaluator looks for `src/nines/__init__.py` inside the target repo (it is probing for its own package, not DevolaFlow code). Cannot be fixed cleanly from the DevolaFlow side without adding a shim file whose sole purpose is to satisfy the probe. Tracked as upstream item for NineS."*

### Retrospective takeaways
> *"Every \"score plateau\" deserves a tool-config audit before assuming code-quality work. v6.0 + v6.1 shipped 7 versions with NineS at 0.7405 — every version's retro recorded \"stable, no regression.\" The v6.2 planning gate broke the plateau by reading the metadata of the zero-scored dimensions and discovering they all said `error: no golden tasks found in data/golden_test_set` — a config issue, not a deficit. **Post-N1 jump: +18.9% overall, +27.97% capability mean. Zero source changes.**"* (`retrospective_v6.1_to_v6.2.md` §4 #1)

### Improvement advice received
- N1 (`v6.2.0_improvement_advice.md` §2.1): *"NineS golden_test_set + nines.toml config — create `data/golden_test_set/` with 10-20 TOML fixtures adapted from NineS upstream samples; create `nines.toml` at repo root with project defaults; update SI-2 rule to reflect canonical invocation"* → projected delta **NineS overall 0.7405 → 0.88+ (+18%+); capability mean +27%** (verified post-ship).

### User feedback
- None for this patch.

### Cross-references
- Improvement advice: `.local/research/v6.2.0_improvement_advice.md` §2.1 (N1) + §1.2 (zero-score diagnosis table)
- Retrospective: `.local/research/retrospective_v6.1_to_v6.2.md` §2 row Wave 7, §4 #1
- NineS snapshot: `.local/research/v6.1.1/nines_self_eval_v6.1.1.json`
- NineS hypothesis: `.local/research/v6.2.0/nines_hypothesis_with_golden.json`

---

## v6.1.2 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: Windsurf compression fix (real bug, all 8 adapters [OK]) (verbatim from `CHANGELOG.md` line 1658).
**Type**: PATCH (closes the `[WARN]`-as-real-bug discovered in v6.0.4)

### Key changes (verbatim from `CHANGELOG.md` lines 1734–1746)
- **Windsurf adapter real bug fix**: *"`.windsurfrules` previously exceeded Windsurf's 8000-char budget (24,625 chars WARN, known-broken since v6.0.4 — customers could not actually install DevolaFlow on Windsurf despite the adapter appearing to build). Now 7,434 chars [OK] via new `keep_sections` transform that extracts only high-value sections of SKILL.md (Quick Action Decision, 4-Layer Agent Hierarchy, Gate Mechanism, Dispatch & Report Protocol) with a compact header prefix pointing users to the full skill on GitHub."*
- **New `keep_sections` transform** in `DataDrivenAdapter` (`src/devolaflow/adapters/data_driven.py`): *"markdown-section-level extraction with optional frontmatter preservation (`include_frontmatter`) and header prefix injection (`header_prefix`). Substring-matches section headings (case-sensitive), respects fenced code blocks (heading-looking lines inside ``` fences are treated as content), and nests H3+ children under their matching H2 parent."*
- **`VALID_TRANSFORMS` frozenset** exported from `data_driven` module — single source of truth for the 5 supported transforms (`copy`, `copy_tree`, `copy_with_frontmatter`, `strip_frontmatter`, `keep_sections`).
- **8 new tests** in `test_data_driven_adapter.py` covering the new transform + **4 new Windsurf tests** (`test_windsurf_under_8000_chars`, `test_windsurf_contains_quick_action`, `test_windsurf_contains_hierarchy`, `test_windsurf_has_header_prefix`).
- **`adapter_configs/windsurf.yaml`**: switched from `strip_frontmatter` to `keep_sections` with 4 high-value section selectors and a 2-line header prefix.

### Compromises (verbatim line 1749)
> *"Only 4 of the 6 task-suggested sections fit under the 8000-char budget. Dropped: Mode Awareness (too large at 4,945 chars — includes full PLAN MODE sub-section) and Context Isolation (wouldn't fit alongside Dispatch & Report Protocol). The 4-Layer Agent Hierarchy section already carries the P1 Dispatcher-Not-Implementer invariant, and the header prefix directs users to the full SKILL at `https://github.com/YoRHa-Agents/DevolaFlow` for the dropped content."*

### Metrics (verbatim from `CHANGELOG.md` lines 1751–1755)
| Metric | Pre (v6.1.1) | Post (v6.1.2) |
|---|---:|---:|
| Tests | 954 | 966 (+12: 9 new in `test_data_driven_adapter.py`, 4 new in `test_windsurf_adapter.py`, −1 tightened assertion) |
| **Windsurf budget** | **24,625 chars WARN** | **7,434 chars OK** (69.8% reduction; 566-char margin) |
| Adapters producing `[OK]` | 7/8 | **8/8** |
| EvoBench | unchanged | no regressions |
| NineS | unaffected | no source changes to NineS / gate / context profiles |

### Retrospective takeaways
> *"Real bugs hide behind `[WARN]` more often than `[ERROR]`. Windsurf shipped broken for 4 versions because `[WARN]` looked like a known limitation rather than an actual install-blocker. The fix took ~1 commit (one new transform + one YAML change) once it was promoted to P0."* (`retrospective_v6.1_to_v6.2.md` §4 #2)

### Improvement advice received
- N2 (`v6.2.0_improvement_advice.md` §2.1): *"Windsurf compression fix — add `keep_sections` transform to `DataDrivenAdapter` that extracts named markdown sections from SKILL.md; update `adapter_configs/windsurf.yaml` to keep only 'Quick Action Decision', '4-Layer Agent Hierarchy', 'Gate Mechanism', and key tables"* → benchmarked Windsurf WARN → OK; chars ≤ 8000.

### User feedback
- None for this patch.

### Cross-references
- Improvement advice: `.local/research/v6.2.0_improvement_advice.md` §2.1 (N2)
- Retrospective: `.local/research/retrospective_v6.1_to_v6.2.md` §2 row Wave 8, §4 #2
- NineS snapshot: `.local/research/v6.1.2/nines_self_eval_v6.1.2.json`

---

## v6.1.3 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: +Zed +Cline +Roo (11 platforms total) (verbatim from `CHANGELOG.md` line 1659).
**Type**: PATCH (proves the data-driven engine scales to zero-Python adapters)

### Key changes (verbatim from `CHANGELOG.md` lines 1715–1721)
- **3 new Tier-1 adapters via data-driven YAML**:
  - `adapter_configs/zed.yaml` — *"Zed editor rules (`.rules/devola-flow.md` + `references/`)"*
  - `adapter_configs/cline.yaml` — *"Cline autonomous agent rules (`.clinerules/devola-flow.md` + `references/`)"*
  - `adapter_configs/roo.yaml` — *"Roo Code per-mode rules (`.roo/rules/devola-flow.md` + `references/`)"*
- *"Each adapter is ≈25 LOC of YAML — no Python source changes required (validates the v6.0.4 data-driven pattern)."*
- **install.sh** support for `zed`, `cline`, `roo` targets; `all` and `update` extended to include them. New `dl_skill_no_frontmatter` shell helper centralises the "download SKILL.md and strip YAML frontmatter" step shared by the 3 new installers.
- **15-21 new tests** across 3 adapter test modules (5-7 tests per adapter).

### Metrics (verbatim from `CHANGELOG.md` lines 1723–1729)
| Metric | Pre (v6.1.2) | Post (v6.1.3) |
|---|---:|---:|
| Adapter count | **8** | **11** (+3 platforms) |
| New YAML LOC | — | ~75 (avg 25 per adapter, vs ~80 LOC of Python in v6.0.4) |
| Tests | 966 | ~985 (target: +15-21 net) |
| All 11 adapters [OK] in `python -m devolaflow.build_skill` | partial | yes |
| Source code changes to `src/devolaflow/` | — | **none** |
| Lint / NineS | clean / stable | clean / stable |

### Retrospective takeaways
> *"Data-driven adapter pattern is even cheaper than v6.0.4 measured. Wave 9 added 3 platforms via 75 LOC of YAML and zero Python. The original v6.0.4 estimate of \"−71.9% LOC per adapter\" was conservative — for adapters that fit the existing transforms (no new logic), it's effectively −100%."* (`retrospective_v6.1_to_v6.2.md` §4 #3)

### Improvement advice received
- A5/A6/A7 (`v6.2.0_improvement_advice.md` §2.2): *"3 Tier-1 adapters land via YAML alone … +3 platforms"* (8 → 11).

### User feedback
- None for this patch.

### Cross-references
- Improvement advice: `.local/research/v6.2.0_improvement_advice.md` §2.2 (A5-A7)
- Retrospective: `.local/research/retrospective_v6.1_to_v6.2.md` §2 row Wave 9, §4 #3
- NineS snapshot: `.local/research/v6.1.3/nines_self_eval_v6.1.3.json`

---

## v6.1.4 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: Dead-API CI guard (G1) (verbatim from `CHANGELOG.md` line 1660).
**Type**: PATCH (CI guard prevents the v6.0.3 dead-wire bug class from regressing)

### Key changes (verbatim from `CHANGELOG.md` lines 1700–1709)
- **`scripts/detect_dead_apis.py`** (G1): *"static analyzer that scans `src/devolaflow/` for public functions/classes with zero non-test callers. Catches the bug class that cost v6.0.3 three versions to fix (`apply_round_escalation` and `merge_reinforcement_into_dispatch` had passing unit tests but no production callers since v5.3.0). Walks `src/devolaflow/**/*.py` with `ast`, collects top-level public `def`/`class` (skipping `_*`, `__dunder__`, `main`, `cli`, `*_cmd`), then verifies each has a real (non-import) reference somewhere in `src/`, `scripts/`, or `benchmarks/`. Re-exports in `__init__.py` are correctly excluded as non-callers."*
- **`tests/test_dead_apis.py`** (11 tests): *"unit tests for the detector — synthetic cases for unused functions, used-via-sibling, test-only callers, allowlist suppression, private/dunder skip, CLI-entry skip, `__init__.py` re-export non-counter, self-use alive — plus the CI-grade `test_devolaflow_codebase_has_no_dead_apis` assertion that fails if any new dead public API ships."*
- **Allowlist**: *"36-entry catalogue of intentionally external-only public APIs in `DEFAULT_ALLOWLIST`, grouped and commented by category — adapter base API, CLI entry-point modules, MIGRATION-v6.md recommended replacements, compressor validators, self-improving feedback module, gate report generators, operational learnings utilities, NineS subsystem, pre-decision phase, and template engine."*
- *"Stdlib only; ~150 LOC of logic plus a documented allowlist."*
- **No source changes to `src/devolaflow/`** — pure CI guard addition.

### Metrics (verbatim from `CHANGELOG.md` lines 1705–1710)
| Metric | Pre (v6.1.3) | Post (v6.1.4) |
|---|---:|---:|
| Tests | ~985 | **988 → 999** (+11) |
| Detection runtime | — | **143 ms** on full DevolaFlow codebase (50 modules) |
| Bug class prevention | none | dead-wire regressions now CI-blocked (`--strict` exit 2) |
| Lint | clean | `ruff check` + `ruff format --check` clean |
| NineS | stable | stable (no source changes to `src/devolaflow/nines/`) |

### Retrospective takeaways
> *"CI guards must capture bug classes, not bug instances. v6.0.3 fixed two dead-wire functions but didn't prevent a third from shipping silently. v6.1.4's `detect_dead_apis.py` makes the bug class structurally impossible to ship — any new public symbol with no caller fails CI."* (`retrospective_v6.1_to_v6.2.md` §4 #4)

### Improvement advice received
- G1 (`v6.2.0_improvement_advice.md` §2.2): *"Dead-API CI detector — script that scans `src/` for public symbols with zero non-test callers; fails CI if any found … Prevents v6.0.3-style dead-wire bugs."*

### User feedback
- None for this patch.

### Cross-references
- Improvement advice: `.local/research/v6.2.0_improvement_advice.md` §2.2 (G1)
- Retrospective: `.local/research/retrospective_v6.1_to_v6.2.md` §2 row Wave 10, §4 #4
- NineS snapshot: `.local/research/v6.1.4/nines_self_eval_v6.1.4.json`

---

## v6.1.5 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: Plan-mode detection wired (V6-03) (verbatim from `CHANGELOG.md` line 1661).
**Type**: PATCH (closes the long-deferred V6-03 from the v6.0.0 advice doc)

### Key changes (verbatim from `CHANGELOG.md` lines 1682–1689)
- **Plan-mode detection in `select_context()` (V6-03)**: *"new `plan_mode: bool | None` parameter. When True (or auto-detected via `DEVOLAFLOW_PLAN_MODE` env var or `.devolaflow_plan_mode` marker file), the active context profile is escalated through `apply_plan_mode_overrides()` BEFORE round-escalation: `agent_hierarchy`, `decomposition_gate`, `rationalization_prevention` priorities lifted to `critical`; `model_hint` upgraded to `quality`; `compression_intensity` set to `minimal`. Composes with the v6.0.3 round-based escalation (plan-mode applies first, round adds budget on top)."*
- **`--plan-mode` / `--no-plan-mode` CLI flags** on `python -m devolaflow.task_adaptive_selector`.
- **`apply_plan_mode_overrides()`** public function in `task_adaptive_selector` (allowlisted in dead-API detector alongside `apply_round_escalation`/`select_context` as documented stable public API).
- **10 new tests** in `test_task_adaptive_selector.py::TestPlanModeDetection` covering auto-detect default off, explicit override priorities, explicit-False short-circuit, env-var detection, marker-file detection, composition with round escalation, result-dict surface keys, profile-immutability, invalid-env-value rejection, and minimal-compression assertion.
- **SKILL.md "Mode Awareness" section**: *"noted the v6.1.5 runtime hook inline on the AGENT-MODE-default line of the detection list — chars-only edit (no new lines), preserving line count and avoiding token-budget knock-on effects in the `feature` profile's `plan_mode_template` selection."*

### Metrics (verbatim from `CHANGELOG.md` lines 1692–1695)
| Metric | Pre (v6.1.4) | Post (v6.1.5) |
|---|---:|---:|
| Tests | 999 | **1009** (+10) |
| Plan-mode detection signals | 0 | **3** (explicit param, env var, marker file) |
| SKILL.md | 498 lines | **498 lines** (chars-only addition) |
| All 11 adapters | [OK] | [OK]; lint clean; NineS stable |
| EvoBench `decomposition_feature` composite | ≥ 95 | ≥ 95 preserved |

### Retrospective takeaways
> *"Plan-mode signals must be both explicit and ambient. A `plan_mode=True` parameter alone is insufficient because dispatching agents may not pass it through. The env var + marker file fallback ensures detection works even when an upstream caller forgets."* (`retrospective_v6.1_to_v6.2.md` §4 #5)

### Improvement advice received
- V6-03 (`v6.2.0_improvement_advice.md` §2.3): *"Plan-mode detection — `select_context(plan_mode: bool | None = None)` auto-detects via env var or explicit param; when True, preference plan-mode sections."* Originally deferred from v6.0.0 advice §2.5 (*"Requires expanding public API and cross-cutting IDE integration; lower ROI than Tier 2 dead-wire work"*), promoted in v6.2.

### User feedback
- None directly. The retrospective noted carrying user feedback for *"address the new feedback at `.local/feedbacks/feedback_for_v6.1.0.md`"*; that file is missing — see Data gaps.

### Cross-references
- Improvement advice: `.local/research/v6.2.0_improvement_advice.md` §2.3 (V6-03)
- Retrospective: `.local/research/retrospective_v6.1_to_v6.2.md` §2 row Wave 11, §4 #5
- NineS snapshot: `.local/research/v6.1.5/nines_self_eval_v6.1.5.json`

---

## v6.2.0 — 2026-04-16 (CHANGELOG) / 2026-04-17 (git tag)

**Theme**: *"Final release of the v6.0 + v6.1 rollup (12 waves, v5.4.2 → v6.2.0)."* (verbatim banner — `CHANGELOG.md` line 1596) — meta/rollup release shipping the SI-8 retrospective.
**Type**: MINOR (rollup; *"ships no new code on top of v6.1.5"*)

### Key changes (verbatim from `CHANGELOG.md` lines 1598–1669)
- **Cumulative code-quality / debt deltas**: *"12 → 0 `DeprecationWarning` lines (P9 honored in v6.0.2); 141 → 0 MVP-SKILL.md cross-references (TD-3 in v6.0.1); 92 LOC of `_BUILTIN_SPECS` removed (TD-2 in v6.0.1); 3 → 0 rule contradictions (TD-6 in v6.0.1); Dead-API CI guard active (G1 in v6.1.4)."*
- **Cumulative NineS self-eval deltas**: *"Overall: 0.7405 → 0.8805 (+18.9% — entirely from the v6.1.1 tool-config fix; zero source changes); Capability mean: 0.7150 → 0.9150 (+27.97%); Hygiene mean: 0.8000 stable (upstream NineS `code_coverage` parser bug remains)."*
- **Cumulative adapter expansion**: *"4 → 11 platforms supported (+7: KimiCode, Windsurf, Continue, OpenClaw, Zed, Cline, Roo Code); New `AdapterRegistry` + `DataDrivenAdapter` engine: simple new adapters now ship as ~25 LOC of YAML (vs ~80 LOC of Python in v5.x); 5 transforms supported: `copy`, `copy_tree`, `copy_with_frontmatter`, `strip_frontmatter`, `keep_sections`."*
- **Cumulative runtime correctness (dead-wire closure)**: *"v5.3.0 P8 finally wired (v6.0.3); v5.3.0 P4 finally wired (v6.0.3); v6.1.5: `apply_plan_mode_overrides` wired into `select_context(plan_mode=True)` with env var + marker file fallback."*
- **Cumulative test/benchmark deltas**: *"818 → 1009 total (+191 net, with 29 deprecated tests retired in v6.0.2); Coverage 91.07% → 93.93%+; tests/test_e2e_convergence.py (7); test_schema_parity.py (6); test_dead_apis.py (11); test_adapter_golden.py (4); 29/29 EvoBench scenarios with regression baseline (was 3/29)."*
- **Cumulative tool config / governance**: *"`nines.toml` codifies the canonical `nines self-eval` invocation; `data/golden_test_set/` (10 TOML fixtures); `.cursor/rules/self-improve-iteration-rules.mdc` SI-2 updated; `MIGRATION-v6.md` documents the v6.0.2 BREAKING removals; 2 SI-8 retrospectives at `.local/research/retrospective_v6.0_to_v6.1.md` and `.local/research/retrospective_v6.1_to_v6.2.md`."*
- **SI-3 evaluation**: *"Weighted composite **9.43 / 10** (threshold ≥ 8.5) — READY for stable release."* (verbatim line 1645)

### Metrics (verbatim from `CHANGELOG.md` lines 1672–1677)
| Metric | Pre (v5.4.2 baseline) | Post (v6.2.0 cumulative) |
|---|---:|---:|
| Tests passed | 818 | **1009** (no change from v6.1.5 — meta-release) |
| All 11 adapters | partial | [OK] |
| Lint | clean | ruff check + format clean |
| DeprecationWarnings | 12 | **0** |
| NineS self-eval | 0.7405 | **0.8805** stable |
| SKILL.md | various | **498 / 500 lines** |

### Retrospective takeaways
> *"Weighted composite: 9.6×0.20 + 9.5×0.20 + 9.3×0.20 + 9.4×0.15 + 9.0×0.10 + 9.5×0.15 = **9.43/10**. Threshold ≥ 8.5 → READY for stable v6.2.0 release."* (`retrospective_v6.1_to_v6.2.md` §6)
> *"Net deltas (v5.4.2 → v6.1.5): Tests: 818 → 1009 (+191 net, +29 retired); NineS overall: 0.7405 → 0.8805 (+18.9%); NineS capability mean: 0.7150 → 0.9150 (+27.97%); Platform adapters: 4 → 11 (+7); DeprecationWarnings: 12 → 0; Hygiene mean: 0.8000 (stable at ceiling — code coverage parser still has the upstream NineS bug)."* (`retrospective_v6.1_to_v6.2.md` §5)

### Improvement advice received (forward — feeds v6.3 / v7 planning)
- *"Address the new feedback at `.local/feedbacks/feedback_for_v6.1.0.md` — user requested a v7.0+ context-management research + iteration cycle (X.com source, Anthropic / OpenAI / Codex / Claude-Code best practices, phased context compression, NineS-driven evaluation methodology, EvoBench feedback artifact). This is the next major-version cycle, not v6.3."* (`retrospective_v6.1_to_v6.2.md` §7)
- *"Tier-2 enterprise adapter batch (JetBrains, Amazon Q, Gemini, Augment, Trae) for v6.3"* (§7)
- *"Investigate whether the upstream NineS `pipeline_latency` evaluator can be tweaked to accept a relative path config (would push overall score from 0.88 → ~0.93)"* (§7)

### User feedback
- `.local/feedbacks/feedback_for_v6.2.0.md` (verbatim, 10 lines): *"对 web 页面进行全面的重新设计和分析并重新更新; 1. 能展示核心设计体系; 2. 每个框架链路要现代化展示设计; 3. 上下文流转路线也要有很好的展示和凸显; 4. 要有研发历程展示，能看到每个版本之间的差别和改进; 先对整个仓库进行核心分析，然后梳理仓库的核心理念并作为 web 端的核心展示目标; 然后将 web 的核心展示思路写入当前仓库迭代规则内，确保后续每次变更都进行相应同步和更新"* — this directly motivated the v6.3.x web-redesign work.

### Cross-references
- Cycle artifacts: `CHANGELOG.md` lines 1664–1669 enumerate all 5 (`v6.0.0_improvement_advice.md`, `_zh.md`, `v6.2.0_improvement_advice.md`, `retrospective_v6.0_to_v6.1.md`, `retrospective_v6.1_to_v6.2.md`)
- Wave-by-wave commit table: `CHANGELOG.md` lines 1649–1662 (12 waves with commit SHAs)
- NineS snapshot: `.local/research/v6.2.0/nines_self_eval_v6.2.0.json`
- Release helper: `.local/research/v6.2.0/create_releases.sh`

---

## v6.2.1 — 2026-04-17

**Theme**: Benchmark staleness-guard CI flake fix (verbatim from `CHANGELOG.md` line 1584 / `v6.3.0_web_redesign_spec.md` §4.4 v6.2.1 row).
**Type**: PATCH (CI-only stabiliser)

### Key changes (verbatim from `CHANGELOG.md` lines 1584–1587)
- **Benchmark staleness-guard CI flake**: *"`tests/test_benchmarks.py::TestBaselineFile::test_v6_baseline_matches_current_results_within_tolerance` was passing locally but failing on CI with a 12.59pp composite drift on `hotfix_jwt`. Root cause: `devolaflow.task_adaptive_selector.estimate_tokens()` uses `tiktoken` when available, otherwise falls back to `len(text) // 4`. Local environment had `tiktoken` installed (incidentally — not in `pyproject.toml` dependencies); CI did not. Different token estimators produce different section selections, which produce different composite scores. The 86.93 baseline was generated with one estimator, current run used the other → 12.59pp drift."*
- **Solution**: *"added an autouse pytest fixture in `tests/conftest.py` that hides `tiktoken` from `sys.modules` for tests in `tests/test_benchmarks.py` only. Both local and CI now consistently exercise the deterministic fallback estimator. Production runtime is unaffected — agents that have `tiktoken` installed still get the more accurate token counts."*
- **Regenerated `v6.1.0_baseline.json`** under the deterministic fallback estimator so `hotfix_jwt` baseline composite is now 99.52 (matches the current fallback-estimator run within 0pp drift).

### Metrics (verbatim from `CHANGELOG.md` lines 1590–1592)
| Metric | Pre (v6.2.0) | Post (v6.2.1) |
|---|---:|---:|
| Tests | 1009 passed | 1009 passed (no count change) |
| Lint | clean | clean |
| NineS | stable | stable |
| CI test job | failing on PR #36 | succeeds on the same commit |

### Retrospective takeaways
- No dedicated retrospective file (`v6.2.1` is a single-fix patch). The cumulative §6.2 retrospective in `retrospective_v6.1_to_v6.2.md` already noted the CI flake risk-class.

### Improvement advice received
- None new — reactive CI fix, no SI-1 gate document for v6.2.1.

### User feedback
- None for this patch.

### Cross-references
- CHANGELOG: `CHANGELOG.md` lines 1582–1592
- PR mentioned: PR #36 (CI failure that triggered the fix)
- Per-version helper: `.local/research/v6.2.1/install_global_local.sh`

---

## v6.3.x — *web-only redesign, never tagged, never bumped `__version__`*

**Theme**: *"Four NEW pages are added under `workflow-system/human/demo/{design-system,framework-chain,context-flow,version-timeline}/`. The shared design tokens are extended (not replaced) with 5 new tokens for layered depth and gate-glow. Three new motion patterns are introduced (`pulse-dispatch`, `flow-down`, `verify-gate-glow`)."* (verbatim from `.local/research/v6.3.0_web_redesign_spec.md` §1 *What changes*)
**Type**: WEB DEMO ONLY (no Python source / no `__version__` bump; merged via PR #37 `feat/v6.3-web-showcase-redesign` then code jumped straight to v7.0.0)

### Key changes (verbatim from `v6.3.0_web_redesign_spec.md`)
- **4 new theme pages** (verbatim §2.2 directory map):
  - `workflow-system/human/demo/design-system/` — `index.html`, `styles.css`, `design-system.js`
  - `workflow-system/human/demo/framework-chain/` — same trio
  - `workflow-system/human/demo/context-flow/` — same trio
  - `workflow-system/human/demo/version-timeline/` — same trio + `versions.json`
- **5 new design tokens** (verbatim §3.2): `--ink-deep`, `--gold-glow`, `--scanline-alpha`, `--rail-track`, `--ember-red`
- **3 new motion patterns** (verbatim §3.3): `pulse-dispatch` (1.4s, `cubic-bezier(.4,.0,.2,1)`), `flow-down` (0.6s per layer, 0.15s stagger, `ease-out`), `verify-gate-glow` (2.0s, `ease-in-out`)
- **8 reusable components** (verbatim §3.4): `LayerNode`, `MessageEnvelope`, `TimelineCard`, `TokenSwatch`, `MotionShowcase`, `GateBadge`, `WaveStrip`, `MetricMicroBar`
- **120 new `data-i18n` keys** (verbatim §6.3 footer): *"Breakdown: 4 (nav) + 4 (page titles) + 14 (landing) + 24 (design-system) + 26 (framework-chain) + 26 (context-flow) + 22 (version-timeline) = 120."*
- **8 new WX-* rules** (verbatim §8 outline): WX-1 four mandatory showcases · WX-2 versions.json same-PR ratchet · WX-3 motion-showcase ratchet · WX-4 token-swatch ratchet · WX-5 component-showcase ratchet · WX-6 SKILL section verbatim sync · WX-7 EN+ZH same-commit · WX-8 cross-page CTA integrity. Authored in `.cursor/rules/web-experience-rules.mdc` (200 lines, 8 rules).

### Metrics (verbatim from `v6.3.0_web_validation_report.md` §1.1 line counts + §3 composite)
| Metric | Pre (v6.2.1) | Post (v6.3 web PR) |
|---|---:|---:|
| Demo HTML pages | 5 | **9** (5 existing + 4 new) |
| `data-i18n` keys (master) | (baseline) | **+120 NEW** (every key has EN + ZH) |
| `versions.json` entries | n/a | **35** (≥ 30 mandated; spans v0.1.0 → v6.2.1 across 5 eras) |
| Standard tests pre-fix | 1009 passed | 1008 passed / 1 failed (`test_demo_index_rules_count`: claimed 34 vs actual 42 rules — blocker B-1) |
| Composite gate score (validation) | — | **92.20 / 100** (test_quality 75 due to B-1; code_review 99; architecture 100; benchmark 100) |
| Pass conditions met | — | 1 of 3 (composite ≥ 85 PASS; B-1 fails the zero-blocker condition) |

### Retrospective takeaways
- **No SI-8 retrospective for v6.3.x**: there is no `retrospective_v6.2_to_v6.3.md` or similar in `.local/research/`. The closest analogue is the §1.13 *"Phases executed"* note in the validation report.
- **Validation outcome (verbatim §5)**: *"FAIL — One blocker (B-1) breaks Rule SI-10 step 1 / Rule CP-7. Composite score is healthy (92.20), and 13 of 14 validation phases pass cleanly; the single failure is a stale numeric literal on the homepage that did not get updated when the WX-* rule family was added."*

### Improvement advice received
- The v6.3 web redesign spec itself (`v6.3.0_web_redesign_spec.md`, 839 lines) is the closest equivalent to an SI-1 gate document for this milestone; it was authored as `S01-T01-design-spec` for the dispatched workflow.

### User feedback
- `.local/feedbacks/feedback_for_v6.2.0.md` (verbatim quote — *the* user ask that motivated v6.3): *"对 web 页面进行全面的重新设计和分析并重新更新 / 1. 能展示核心设计体系 / 2. 每个框架链路要现代化展示设计 / 3. 上下文流转路线也要有很好的展示和凸显 / 4. 要有研发历程展示，能看到每个版本之间的差别和改进"*
- `.local/feedbacks/feedback_for_v6.3.x.md` (verbatim asks — forward-looking, motivated v7.0): *"1. 深度调研和搜索如何进行上下文管理的 best-practice，包括搜索 x.com 上的信息，openai，codex，claude code，Anthropic 等的分享和思考 / 2. 设计如何在 cursor 中的 devola-flow 里应用上下文的阶段性压缩 / 3. 设计评估方式（如何评估阶段性压缩的保持性，如何评估核心内容的持久性等等）/ 4. 将评估模式建议反馈写入 EvoBench / 5. 验证有效性后，对全量上下文结构管理进行深入的调研和发掘 / 从 7.0.0 开始连续迭代多个确定性提升的小版本，直到更新至 7.1.0"*

### Cross-references
- Spec: `.local/research/v6.3.0_web_redesign_spec.md` (839 lines, status `COMPLETE`)
- Validation: `.local/research/v6.3.0_web_validation_report.md` (290 lines, verdict FAIL on B-1 blocker)
- WX rules: `.cursor/rules/web-experience-rules.mdc` (200 lines, WX-1 through WX-8)
- PR: `feat/v6.3-web-showcase-redesign` (PR #37, merged commit `574065c`, child commit `e9a8e66 feat(web-demo): four-theme showcase redesign + WX-1..8 rules`)

---

## v6 Era Summary

The v6.x era is DevolaFlow's **architectural baseline cementation** cycle. It opened with v6.0.0 as a *planning thesis* (no release) targeting three structural debts inherited from v5.x: dead-wire infrastructure (P4/P8 from v5.1/v5.3 had tests but no runtime), deferred deprecation (P9), and the never-executed v4.2 → v5.0 multi-platform adapter proposal. Across **13 tagged versions** (v6.0.1 → v6.2.1) plus the never-tagged v6.3.x web redesign, the cycle delivered: dead-wire closure (v6.0.3, the single highest-ROI intervention), 4 → 11 platform adapters via a new YAML-driven engine (v6.0.4 + v6.1.0 + v6.1.3), 12 → 0 DeprecationWarnings (v6.0.2 BREAKING + MIGRATION-v6.md), 3 → 29 EvoBench regression baselines (v6.0.5), schema parity enforcement (v6.0.5 TD-4), NineS plateau break 0.7405 → 0.8805 via tool-config alone (v6.1.1, +18.9% with zero source changes), Windsurf real-bug fix (v6.1.2), dead-API CI guard (v6.1.4 — *"CI guards must capture bug classes, not bug instances"*), plan-mode runtime hook (v6.1.5), CI flake stabiliser (v6.2.1), and a four-theme web showcase + WX-1..8 rule family (v6.3 web). SI-3 composite for the v6.2.0 rollup landed at **9.43 / 10** vs the 8.5 threshold, and 818 → 1009 tests (+191 net) closed the cycle.

## Carry-forward items (deferred from v6.x, landed in v7+)

- **Tier-2 enterprise adapter batch (JetBrains, Amazon Q, Gemini, Augment, Trae)** — explicitly deferred from v6.2 (`retrospective_v6.1_to_v6.2.md` §3) to v6.3; v6.3 instead consumed by web redesign + v7 cycle pivot. Status in v7+: not yet shipped as of v8.4.0 first-parent log.
- **N3 source-freshness updater** — deferred from v6.2 advice §2.3; would push NineS `source_freshness` 0.5 → 1.0 (`v6.2.0_improvement_advice.md` §2.3).
- **V6-04 `unwanted_hints` negative routing** — deferred 3 cycles (v6.0 advice §2.5, v6.1 retro §3, v6.2 retro §3) — *"EvoBench shows current scenarios are clean — not warranted"*; reassess when profile count crosses 25 / 50 (`retrospective_v6.1_to_v6.2.md` §7).
- **V6-05 SkillRouter semantic profile matching** — deferred 3 cycles; threshold remains *"50+ profiles"* (`retrospective_v6.1_to_v6.2.md` §3).
- **V6-09 Cursor hard-reinforcement hook layer** — depends on V6-01 production telemetry; not yet observable.
- **V6-10 Two-stage gate orchestration (spec → code quality)** — explicitly cited as needing its own RFC (`retrospective_v6.1_to_v6.2.md` §3).
- **V6-11 Skill trust-tier governance** — explicitly *"separate governance initiative"* (multi-cycle).
- **Upstream NineS `pipeline_latency` evaluator fix** — DevolaFlow side is unfixable; tracked as *"upstream item for NineS"* (`CHANGELOG.md` line 1775); would push 0.8805 → ~0.93 (`retrospective_v6.1_to_v6.2.md` §7).
- **`apply_plan_mode_overrides` + `apply_round_escalation` composition into `apply_context_modifiers(profile, *, plan_mode, round_num)`** — proposed in `retrospective_v6.1_to_v6.2.md` §7 as a clarity refactor; not yet shipped.
- **WX-2 ratchet first proof point** — *"when v6.3.0 is finally tagged, ensure `versions.json` includes a v6.3.0 entry (Rule WX-2)"* (`v6.3.0_web_validation_report.md` §6 step 4); the v6.3 PR merged but never received its own version bump.
- **Phased context compression cycle (v7.0 → v7.1)** — directly seeded by `feedback_for_v6.3.x.md`; landed as v7.0.0 *"Cache-Layout Invariant (J.1)"* through v7.1.0 *"staged context compression rollup"* per first-parent log.

## Data gaps

- **No `## [6.0.0]` CHANGELOG section, no `v6.0.0` git tag.** The v6.0.0 thesis lives only in `.local/research/v6.0.0_improvement_advice.md`; implementation rolled across v6.0.1 → v6.1.0 as 6 waves.
- **No `## [6.3.x]` CHANGELOG section, no `v6.3.x` git tag.** The v6.3 web redesign merged via PR #37 (commit `574065c`) but `__version__` jumped from v6.2.1 directly to v7.0.0 without a v6.3 stamp; spec + validation report are the only artifacts.
- **Date discrepancy**: All 13 v6.x git tags carry creator-date `2026-04-17`; CHANGELOG headers for v6.0.1 → v6.2.0 read `2026-04-16`. Only v6.2.1 agrees on `2026-04-17`. Likely UTC-vs-local-tz timestamp drift on the rollup commit; not a content gap.
- **Missing `feedback_for_v6.0.0.md` and `feedback_for_v6.1.0.md`**: both retrospectives (`retrospective_v6.0_to_v6.1.md` §7, `retrospective_v6.1_to_v6.2.md` §7) cite a `feedback_for_v6.1.0.md` artifact that does not exist under `.local/feedbacks/`. The actual era-relevant user feedback files are `feedback_for_v6.2.0.md` (web redesign) and `feedback_for_v6.3.x.md` (v7 context-mgmt cycle).
- **No `retrospective_v6.2_to_v6.3.md`**: the v6.3 web milestone has no SI-8 retrospective; the closest analogue is the §1 phase table in `v6.3.0_web_validation_report.md`.
- **No NineS snapshot for v6.3.x**: per-version `nines_self_eval_*.json` files exist only for v6.0.1 → v6.2.0; v6.2.1 and v6.3.x lack snapshots.
- **`v6.2.0_improvement_advice.md` test projection vs reality**: §3 projected *"Tests: 896 → ~950 (+50 approx)"*; reality landed at 1009 (+113). Conservative projection — not a defect.

## v9.0.0-relevant patterns from v6 era still active today

1. **Dead-API CI guard (`scripts/detect_dead_apis.py` + `tests/test_dead_apis.py`)** introduced in v6.1.4 still gates every commit; the 36-entry `DEFAULT_ALLOWLIST` is the canonical record of intentionally-external public APIs. v9 should review whether the allowlist needs grouping for new agent-workspace symbols (S-8/S-9 Soul Rules from v8.3).
2. **`AdapterRegistry` + `DataDrivenAdapter` + `keep_sections`-style transforms** (v6.0.4 + v6.1.2) remain the only path to add new platform adapters. Any v9 adapter expansion (Tier-2 enterprise batch is still deferred) inherits this contract.
3. **Round-aware `select_context(round_num=N)` + `ProposalGenerator.generate_round_dispatch()`** (v6.0.3) wire SI-9 reinforcement; v8 reinforcement extensions (`src/devolaflow/gate/reinforcement.py`) sit on top of this. v9 multi-round convergence still routes through these v6.0.3 entry points.
4. **`nines.toml` + `data/golden_test_set/` configuration pattern** (v6.1.1) is the canonical SI-2 invocation surface; the +18.9% NineS jump from zero source changes remains the era's most-cited learning (*"Every 'score plateau' deserves a tool-config audit before assuming code-quality work."*).
5. **WX-1..8 web-experience rules + four-theme showcase pages** (v6.3) are now hard-coded into `.cursor/rules/web-experience-rules.mdc` and gated by `tests/test_demo_index_rules_count` — any v9 web changes must respect WX-2 (versions.json same-PR ratchet) and the cross-page CTA integrity contract.
