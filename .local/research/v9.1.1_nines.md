# NineS Self-Eval Summary — v9.1.1 (PV-01 of v9.2.0 cycle)

**Baseline:** v9.1.0
**Run timestamp:** 2026-05-01T04:49:16+00:00
**Duration:** 60.5 s
**Upstream timeouts:** none (timeouts: [])
**NineS CLI:** v3.3.0 (`/root/miniforge/bin/nines`)
**Config:** `nines.toml` + `data/golden_test_set/` (13 fixtures)

## 1. Verdict

**Composite (overall): 0.9073 ≥ 0.85 → PASS**

This matches the CHANGELOG `[9.1.1]` §"Verification Checklist" claim verbatim.
The composite is the unweighted arithmetic mean of all 25 axes (20 capability +
5 hygiene). Weighted_overall (capability_mean × 0.7 + hygiene_mean × 0.3) =
0.7236; the lower weighted figure is dominated by the `code_coverage` hygiene
axis collapsing to 0.0 — a NineS-config artifact (see §4 below), NOT a true
coverage regression.

| Aggregate | Value |
|---|---|
| `overall` (composite, unweighted mean) | **0.9073** |
| `weighted_overall` (capability×0.7 + hygiene×0.3) | 0.7236 |
| `capability_mean` (20 axes) | 0.9550 |
| `hygiene_mean` (5 axes) | 0.7961 |

## 2. Top 5 keypoints from planning

The planning analysis (`v9.1.1_nines_planning.json`, target =
`src/devolaflow/workspace_context.py`) produced 2 key points + 4 findings.
Listed in priority order:

1. **engineering (prio=5)** — Analysis coverage summary: 1 Python file, 13
   knowledge units extracted.
2. **behavioral_shaping (prio=4)** — Finding: `agent_impact` says repository
   contains 0 Agent-facing artifacts across 0 mechanisms.
3. **summary (info)** — `workspace_context.py`: 447 lines, 12 functions,
   1 class, 5 imports, avg complexity 3.8.
4. **dependencies (info)** — Imports: `__future__`, `dataclasses`, `logging`,
   `pathlib`, `typing` (stdlib only — zero new dep surface).
5. **agent_impact (info)** — "No Agent-facing artifacts detected"
   (false-negative: `workspace_context.py` is a discovery API consumed by
   L0 dispatchers via `references/agent-workspace.md`; NineS only walks
   the per-file analysis target and does not see the repo-wide SKILL.md /
   reference linkage).

## 3. Axis-by-axis scores

### Capability (20 axes, mean 0.9550 — all ≥ 0.5)

| Axis | Score |
|---|---|
| scoring_accuracy | 0.90 |
| eval_coverage | 1.00 |
| scoring_reliability | 1.00 |
| report_quality | 1.00 |
| scorer_agreement | 0.90 |
| source_coverage | 1.00 |
| **source_freshness** | **0.50** |
| change_detection | 1.00 |
| data_completeness | 1.00 |
| collection_throughput | 0.9997 |
| decomposition_coverage | 1.00 |
| abstraction_quality | 1.00 |
| code_review_accuracy | 1.00 |
| **index_recall** | **0.80** |
| structure_recognition | 1.00 |
| pipeline_latency | 0.9999 |
| sandbox_isolation | 1.00 |
| convergence_rate | 1.00 |
| cross_vertex_synergy | 1.00 |
| agent_analysis_quality | 1.00 |

### Hygiene (5 axes, mean 0.7961)

| Axis | Score | Notes |
|---|---|---|
| **code_coverage** | **0.00** | NineS-config artifact (see §4) |
| test_count | 1.00 | 3486 tests collected |
| module_count | 1.00 | 84 modules in `src/devolaflow/` |
| docstring_coverage | 0.9805 | 552 / 563 documented |
| lint_cleanliness | 1.00 | 0 ruff violations |

## 4. Notes on outlier axes

- **`code_coverage = 0.00`** — `nines.toml` has `cov_package: nines` (the
  NineS CLI's own package); when run inside DevolaFlow the value collapses
  because `nines` is not the package under test. The CORRECT coverage
  number is verified via W-9 step 1 (`pytest tests/ --cov=devolaflow`)
  and is asserted by `pyproject.toml [tool.coverage]` floor of 80%.
  This is a NineS-side config friction, not a v9.1.1 regression.
- **`source_freshness = 0.50`** — measures source-corpus age relative to
  the baseline; expected to be middling at PATCH boundaries.
- **`index_recall = 0.80`** — within the historical band for DevolaFlow
  self-eval runs; not a regression.

## 5. Upstream NineS blockers

**None.** The CLI (`/root/miniforge/bin/nines` v3.3.0) ran cleanly:
* `analyze --depth deep --agent-impact --keypoints` exit 0 in <1s.
* `self-eval --baseline-version 9.1.0` exit 0 in 60.5s.
* `timeouts: []` field in JSON confirms no per-axis timeout fires.

The v8.x-era NineS subprocess-timeout bug A1 referenced in the dispatch
fallback clause is NOT triggered at v3.3.0 against v9.1.1.

## 6. Cross-reference

* CHANGELOG `## [9.1.1] — 2026-05-01` §"Verification Checklist" —
  composite 0.9073 ≥ 0.85 PASS verbatim match.
* W-2 (NineS-Driven Analysis) — both planning AND close artifacts present
  in `.local/research/`, satisfying the SI-2 storage requirement.
* W-3 (SI-3) — composite 0.9073 ≥ 0.85 PATCH threshold (also clears the
  ≥ 9.0 MAJOR threshold expressed on the 1-10 NineS scale: 9.073/10).
