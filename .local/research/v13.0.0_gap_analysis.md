# v13.0.0 — SI-1 Gap Analysis (W-1 Planning Gate)

**Cycle:** v12.5.0 → **v13.0.0 (MAJOR)**
**Date:** 2026-06-01
**Driver:** `.local/feedbacks/feedback_for_v12.5.0.md` (3 operator asks) + operator plan
`.cursor/plans/impeccable_web-design_v13_22bb4384.plan.md`.

This artifact is the entry point for SI-2..SI-10 per Rule W-1/SI-1. No implementation lands before it exists.

---

## 1. Deficiencies (current state vs. target)

| # | Deficiency | Evidence (current state) | Severity |
|---|---|---|---|
| D-1 | **No `impeccable` plugin** anywhere | `runtime-plugins.yaml` has 5 plugins (nines/ui-pro/rtk/si-chip/codegraph); `plugins.yaml` catalog has no `impeccable`; `reference-dependencies.yaml` has no impeccable pin | BLOCKER (primary ask) |
| D-2 | **No impeccable reference doc** | `references/` has 22 SF-4 files; no `impeccable.md`; SKILL.md Tier-2 nav + `MIRRORED_FILES` lack it | MAJOR |
| D-3 | **No `web-design` workflow** | `templates/builtin/` has no `web-design.yaml`; registry.yaml + workflow-skill.yaml lack it; the only UI-flow templates (`demo-showcase`, `product-verification`) are DEPRECATED | BLOCKER (primary ask) |
| D-4 | **devola install bundles no plugins** | `init_project.py` installs skill files only; `install.sh` downloads skill files only; neither installs runtime plugins | MAJOR (primary ask) |
| D-5 | **No degraded-mode / context-profile coverage for impeccable** | `degraded-mode.md` matrix covers 5 plugins; `context_profiles.yaml` has `ui_integration` + `codegraph_integration` but no `impeccable_integration` | MINOR (coupled to D-1/D-3) |

## 2. Priority ranking

1. **D-1** (plugin registries, A-5 SSOT) — foundation for everything else.
2. **D-3** (web-design workflow) — the user-facing deliverable; depends on D-1 (`invoked_by_workflows`).
3. **D-2 + D-5** (reference doc, SF-4 surfaces, degraded-mode, context profile) — discoverability + correctness.
4. **D-4** (bundled global install) — orthogonal CLI surface; default-ON for `--global`, `--no-plugins` opt-out.

## 3. Proposed fixes (file-level scope)

| Phase | Files | Change |
|---|---|---|
| 1 | `plugins.yaml`, `runtime-plugins.yaml`, `reference-dependencies.yaml` | impeccable catalog block + `ui_refinement` role; runtime entry (`npm_then_init`, `npm install -g impeccable`, init `impeccable skills install --yes`, `init_targets:[auto]`, `version_check_cmd:"impeccable --version"`, min 2.0.0); active_tracking entry (13th); add `web-design` to ui-pro `invoked_by_workflows` |
| 2 | `references/impeccable.md` (NEW), `tests/test_no_ghost_features.py` (`_SF4_REFERENCE_SET`), `SKILL.md` (Tier-2 row), `scripts/sync_cursor_skill.py` (`MIRRORED_FILES`), `references/degraded-mode.md`, `context_profiles.yaml` | reference doc (≤1000 lines) + 4 C-7 surfaces + degraded-mode section/row + `impeccable_integration` block |
| 3 | `templates/builtin/web-design.yaml` (NEW), `templates/registry.yaml`, `workflow-skill.yaml`, `SKILL.md` | design(ui-pro)→implement→refine(impeccable)→verify(impeccable detect) + refine↔verify convergence gate; register in 2 template lists + workflow-selection row |
| 4 | `src/devolaflow/init_project.py`, `scripts/install.sh` | `install_plugins(scope)` registry-driven helper + `--no-plugins` flag (default-ON for `--global`, warn-not-fatal S-5); install.sh `--no-plugins` + delegate to Python path when importable |
| 5 | `tests/test_runtime_plugins_smoke.py`, `tests/test_plugins.py`, `tests/test_degraded_mode.py`, template tests, install tests, `tests/test_no_ghost_features.py` (W-18 stanzas + ref counts) | ≤ +30 NEW functions (est. ~16) |
| 6 | `CHANGELOG.md`, `workflow-system/human/{en,zh}/*`, `versions.json`, demo `index.html` | MAJOR entry (name `--no-plugins`), bilingual lockstep, version timeline |
| 7 | canonical 8 (via `scripts/bump_version.py 13.0.0`) | MAJOR bump |
| 8 | baselines, evaluation, retrospective, archive | W-16 regen, SI-3 ≥ 9.0, SI-8, W-19 |

## 4. MAJOR-cycle obligations (per governance rules)

- **W-16** wholesale EvoBench/golden baseline regen → `benchmarks/devolaflow_context/baselines/v13.0.0_baseline.json`.
- **W-3/SI-3** evaluation composite **≥ 9.0** (MAJOR threshold; minor is ≥ 8.5).
- **W-4/W-6/W-13** benchmark + `task_adaptive_selector` verification (context_profiles.yaml + SKILL.md change).
- **W-5/W-12** `build-skill` for all 4 adapters (SKILL.md change).
- **W-7/SI-8** retrospective; **W-19** archive to `docs/cycle-archive/v13.0.0/`.
- **W-17** ≤ +30 NEW test functions per PV; ≤ +150 cycle.
- **A-2.3/A-2.4** NO new dispatch keys — `ensure_plugins` is template config, not a lean-dispatch top-level
  key; `canonical_order` stays 17; multi-baseline byte test stays GREEN.
- **W-20** NO new `DEVOLAFLOW_*` env flag — `--no-plugins` is a CLI arg; impeccable runtime install reuses
  the existing `DEVOLAFLOW_AUTO_INSTALL_PLUGINS` surface.
- **W-21** Soul-set freeze preserved (no new S-* rule).
- **S-6** feature branch `feat/v13.0.0-impeccable-web-design`; never push to `main`; merge via PR.

## 5. Upstream-surface verification (impeccable@2.3.2, verified 2026-06-01)

- npm package `impeccable`; bin `cli/bin/cli.js`. `impeccable --version` prints `pkg.version` → valid
  `version_check_cmd`. Subcommands: `detect [path|url]` (`--json`/`--fast`; exit 0=clean, 2=anti-patterns),
  `skills install|update|check|help`. `skills install` auto-detects harness folders (`.cursor`, `.claude`,
  `.agents`/codex, `.gemini`, ...), supports `--providers=`, `--force`, `--yes`/`-y` — **no `--ai` flag**.
- Decision: `init_cmd_template: "impeccable skills install --yes"`, `init_targets: [auto]` (single
  auto-detecting run; template carries no `{ai_platform}` placeholder so `.format()` is a no-op).
- Role: NEW `ui_refinement` (provider=impeccable); `ui_tooling`'s provider is already ui-ux-pro-max
  (`plugin_roles.provider` is singular).
