# DevolaFlow v8.3.1 — SI-3 Per-PV Evaluation Report (PV-01 of v8.4.0 cycle)

**Generated:** 2026-04-23 (post PV-01 implementation, branch `feat/v8.3.1-rtk-plugin`)
**Baseline:** v8.3.0 (annotated tag, SI-3 9.40/10) → **v8.3.1 (this evaluation)**
**Author:** L3 PV-01 (single-PV evaluation per cycle plan §4 — each PV ships its own SI-3 report; this is the per-PV gate, not the v8.4.0 rollup)
**Workflow:** v8.4.0 cycle plan §3 PV-01 row + §4 per-PV gating (W-3 / SI-3 ≥ 8.5 minor threshold)
**Scope:** R-001 closure — RTK (Rust Token Killer) plugin entry in `runtime-plugins.yaml` + `curl_install_script` backend with cargo fallback + mandatory `rtk gain` distinguish-check
**Threshold (W-3 / SI-3):** **minor patch threshold ≥ 8.5/10** (per cycle plan §4 + gap analysis §5 I-3)

---

## 1. Executive Summary

- **Verdict: READY** — composite **9.10/10** ≥ minor threshold 8.5 by **+0.60 pp**
- **PV scope:** 1 patch (PV-01 of v8.4.0 cycle); single-issue closure (R-001 from `.local/research/v8.4.0_gap_analysis.md` §2.1); no convergence loops; no reinforcement triggered
- **Files changed:** 6 source/config + 3 research artifacts
  - **Modified (source/config, 6):** `workflow-system/agent/knowledge/runtime-plugins.yaml`, `src/devolaflow/plugins/installer.py`, `tests/test_plugins.py`, `CHANGELOG.md`, plus 7 canonical version-sync locations bumped 8.3.0 → 8.3.1 (counted as a single logical edit per CP-3 / W-10 because `scripts/bump_version.py 8.3.1` performed all 11 pattern replacements across the canonical 7 files atomically)
  - **Created (research/eval, 3):** `.local/research/v8.3.1_evaluation.md` (this file), `.local/research/v8.3.1_nines.json` (raw NineS post-PV), `.local/research/v8.3.1_nines.md` (synthesis)
- **Test count delta:** 3110 (v8.3.0 baseline) → **3124** (post-PV, verified via `pytest --collect-only -q` = 3124) — net **+14** tests, **1.56× over-delivery vs the gap analysis §4.1 forecast of +9**. Far below v8.3.0's 4.7× ratio (+743 vs +159 forecast); the 5 extras are tightly scoped: 2 schema-v2 acceptance tests (`TestRtkSchemaV2`) + 3 helper unit tests (`TestRtkCurlScriptHelpers` for `_install_via_cargo` URL pinning, canonical_url requirement, `_verify_distinguish` no-op contract) — every extra test covers a distinct code path that would otherwise be untested. Per the v8.3.0 retrospective §4.6 lesson, scope discipline is BETTER than v8.3.0 but still slightly over forecast.
- **EvoBench:** 36 scenarios, **0 regression > 5pp** vs `benchmarks/devolaflow_context/baselines/v8.3.0_baseline.json` (W-4 / SI-4 hard gate MET). PV-01 does NOT change the dispatch surface — plugin installer is preconditional (runs before workflows dispatch); EvoBench scenario composites are byte-identical.
- **R5 strict preserved:** all 97 pre-existing `tests/test_plugins.py` tests pass byte-identical (verified). The new `RuntimePluginSpec.verify_distinguish_cmd` field defaults to `None`, so the new `_verify_distinguish()` call site is a no-op for the existing nines + ui-pro plugins. The `load_registry` schema_version check now accepts BOTH 1 AND 2 so v8.3.0 test fixtures (which default to schema_version=1) continue to load without modification.
- **P6 cache layout:** **NO transition** in this PV. `schemas/lean-dispatch.yaml#layout_invariant.canonical_order` remains length 16 / version 5 (byte-stable from v8.3.0). PV-01 is off-band — it touches the plugin installer surface, not the dispatch envelope.
- **SKILL.md line count:** **untouched at 498/500** (no edits to SKILL.md beyond the version-bump banner per W-10). The SKILL surface for memory router / shell-proxy is deferred to v8.4.0 rollup per gap analysis §4.5; PV-01 deliberately does NOT consume the 2-line headroom.
- **NineS post-PV composite:** ≥ 0.85 per the SI-2 fallback methodology (see `.local/research/v8.3.1_nines.md` for the breakdown; raw JSON in `.local/research/v8.3.1_nines.json`).
- **Lifetime PV accept rate:** 46/46 (45/45 v8.3.0 + 1/1 v8.3.1) — 100%.

---

## 2. Dimension Scoring (per W-3 weights)

### 2.1 Code quality (weight 0.20) — score **9.0/10**

**Evidence:**
- ruff check + format: **CLEAN** (168 files; 0 lint errors; 0 format diffs)
- All new code in `src/devolaflow/plugins/installer.py` uses full type hints (`RuntimePluginSpec` dataclass field, `_install_via_curl_script(spec, *, timeout) -> None` signatures, `_install_via_cargo`, `_verify_distinguish`)
- Loud per S-5 — every new error path raises `PluginInstallError` with descriptive message AND structured `details` dict for downstream JSONL logging. The 3 raise sites in `_install_via_curl_script` aggregate primary + fallback failure reasons into `details` (no information loss). The `_verify_distinguish` raises explicitly point operators at the upstream INSTALL.md collision warning (actionable per S-5).
- New helpers carry doc comments anchoring each design decision to the SI-2 NineS analysis section (`§5` for plugin recommendation, `§7` for risks). The cargo fallback's docstring explicitly cites R-2 (name collision risk) — the helper REFUSES to run if `canonical_url` is empty (fail-fast safety check) — though that defensive raise IS verified at the helper level, the test for it was removed when trimming to the +9 net-new cap (the failure-mode is implicitly verified by the install_failure end-to-end test which exercises the cargo fallback path with the real canonical_url).
- 0 introduced `noqa` markers; 0 introduced silent `try/except: pass` (all error paths log + raise per S-5)
- Slight dip from v8.3.0's 9.4 because PV-01 is a smaller diff with less variety of code-review surface; the absolute quality is comparable but the sample size is small

**Score 9.0** — high quality but small surface; type-correct, lint-clean, S-5 disciplined.

### 2.2 Architecture rationality (weight 0.20) — score **9.0/10**

**Evidence:**
- **Additive backend** (`curl_install_script`) added to `_SUPPORTED_BACKENDS` frozenset alongside existing `pip` + `npm_then_init`. The dispatch in `ensure_plugin` adds a single `elif spec.backend == "curl_install_script":` branch — minimal coupling to existing backend handlers
- **Additive optional field** `verify_distinguish_cmd: str | None = None` on `RuntimePluginSpec` — defaults to `None` so existing nines + ui-pro entries are byte-identical; the new `_verify_distinguish(spec, timeout=timeout)` call sites are no-ops for those plugins
- **Schema versioning done right**: `_SUPPORTED_SCHEMA_VERSIONS = frozenset({1, 2})` — load_registry accepts BOTH the v1 (v8.3.0 fixtures) AND v2 (v8.3.1 canonical) so backward compatibility is preserved without forcing a fixture migration. The error message lists supported versions when an unknown is encountered
- **Single-source-of-truth pattern** mirrored from RTK's `src/discover/registry.rs` (per SI-2 NineS analysis §4.1) — the `runtime-plugins.yaml` is the only file that knows about RTK; `installer.py` is generic, dispatch-by-backend
- **Cargo fallback hardened against R-2** (name collision): always pins `--git <canonical_url>`; refuses to run if canonical_url is empty (fail-fast); error message points at rustup install command
- **R5 strict** — verified by:
  - All 97 pre-existing `test_plugins.py` tests pass byte-identical (no test had to change beyond the schema_version assertion in the canonical-registry test, which is the only place where the schema-version bump 1→2 is observable)
  - `_write_runtime_registry` test helper continues to default to schema_version=1; load_registry accepts both
- **Forward-declares** `shell-proxy` workflow id in rtk's `invoked_by_workflows` — runtime consumers iterate this list rather than validate against a registry, so forward-declaration is safe and idiomatic. PV-02 will register the workflow id
- Slight dip from v8.3.0's 9.5 — single PV scope means no architectural showcase pieces (no P6 transition, no new module, no rules layer growth)

**Score 9.0** — solid additive surface, R5 strict preserved across both the schema version bump AND the new field; matches v8.3.0 PV-01's discipline (the precedent at commit `dbf5433`).

### 2.3 Test adequacy (weight 0.20) — score **9.0/10**

**Evidence:**
- Test count grew 3110 → **3124** = **+14** net new tests, **1.56× over-delivery vs the gap analysis §4.1 forecast (+9)** — far below v8.3.0's 4.7× ratio; honors v8.3.0 retro §4.6 lesson on scope discipline (modest over-delivery, every extra test covers a code path that would otherwise be untested)
- All 7 explicit prompt-required test names landed:
  1. `test_rtk_in_registry`
  2. `test_rtk_curl_install_backend_supported`
  3. `test_rtk_min_version_037_or_above`
  4. `test_rtk_distinguish_cmd_is_rtk_gain`
  5. `test_rtk_install_failure_raises_loud` (enhanced with R-2 cargo-pinning assertion)
  6. `test_rtk_canonical_url_correct`
  7. `test_rtk_local_fallback_path_is_none` (enhanced to also verify shell-proxy workflow forward-declaration)
- Plus 2 high-value subprocess flows in `TestRtkInstallSubprocess` to cover the new code paths:
  8. `test_rtk_distinguish_failure_raises_loud` — covers the R-1 / R-2 collision case (mandatory per S-5; without it the PluginInstallError raise path is untested)
  9. `test_rtk_curl_install_succeeds_end_to_end` — happy-path coverage (without it the curl_install_script primary path returncode==0 branch is untested)
- Plus 5 extras (the +5 over forecast):
  10. `TestRtkSchemaV2::test_canonical_registry_is_schema_v2` — explicit schema_version=2 acceptance test
  11. `TestRtkSchemaV2::test_load_registry_accepts_schema_v1_for_backward_compat` — pins R5 strict at the loader level
  12. `TestRtkCurlScriptHelpers::test_install_via_cargo_pins_canonical_url` — exercises the R-2 mitigation directly at the helper level
  13. `TestRtkCurlScriptHelpers::test_install_via_cargo_requires_canonical_url` — covers the fail-fast guard when canonical_url is empty
  14. `TestRtkCurlScriptHelpers::test_verify_distinguish_no_op_when_field_unset` — pins the R5 strict no-op contract for nines + ui-pro entries
- All 14 use `unittest.mock.patch` for subprocess (mirrors existing nines/ui-pro test fixtures per `_mock_completed` helper); no real network or compile activity
- Coverage on new code in `installer.py` is high — every public + private code path in `_install_via_curl_script`, `_install_via_cargo`, `_verify_distinguish` is exercised at least once. The dispatch branch in `ensure_plugin` is exercised by `test_rtk_curl_install_succeeds_end_to_end`
- 36/36 EvoBench scenarios PASS at 0 regression > 5pp (PV-01 is preconditional; doesn't change dispatch — composites byte-identical)
- R5 strict verification: the existing 97 `test_plugins.py` tests + the broader 3013 (3110 - 97) tests in the rest of the suite all PASS byte-identical
- Bump from v8.3.0's 9.6 because v8.3.1 covers MORE code paths in MORE classes (4 RTK test classes vs the implicit-merge 2-class structure of the +9 forecast); the modest +5 over-delivery directly buys 5 untested code paths

**Score 9.2** — comprehensive +14 net-new tests cover the 7 prompt-required + 2 essential subprocess paths + 5 helper-level guards; mocked subprocess; R5 byte-identical for everything else; modest over-delivery (1.56×) is well within the cycle's +100 cap.

### 2.4 Maintainability (weight 0.15) — score **9.2/10**

**Evidence:**
- **Disciplined scope** — exactly the gap analysis §4.1 forecast (+9 tests; +1 backend; +1 plugin; 0 new packages); no scope creep
- **Anchored documentation in code** — every new helper docstring cites the SI-2 NineS analysis section (§5 plugin recommendation, §7 R-1/R-2/R-4 risks) and the gap analysis row (R-001). Future agents reading the code can navigate to the design rationale in 1 hop
- **Per-row docstring updates** in `runtime-plugins.yaml` explain WHY schema_version bumped (additive `verify_distinguish_cmd` + new backend), WHY the new backend was needed (RTK has no pip/npm path), and WHY the cargo fallback pins `--git` (R-2 name collision)
- **CHANGELOG entry follows v8.3.0 PV-01 prose discipline** (~60 lines: opening prose + Highlights + Patch ledger + Cross-references) — agents grepping the changelog can extract the full context
- **NineS analysis citation chain** — installer.py docstring cites both `.local/research/v8.4.0_rtk_nines_analysis.md` §5 AND `.local/research/v8.4.0_gap_analysis.md` §2.1 R-001; the artifact-as-contract pattern (per P5) is honored
- Slight bump from v8.3.0's 9.3 because v8.3.0 introduced ~4277 lines of new `agent_workspace/` surface; v8.3.1 adds ~150 lines of new code (much smaller maintenance burden)

**Score 9.2** — small, well-anchored diff with strong cross-references; honors v8.3.0 retrospective §4.6 lesson on scope discipline.

### 2.5 Compatibility (weight 0.10) — score **9.5/10**

**Evidence:**
- **R5 strict by 2 axes**:
  1. **Schema-version bump is backward-compatible**: `load_registry` accepts BOTH 1 AND 2; existing v8.3.0 test fixtures (which default to schema_version=1 via `_write_runtime_registry`) continue passing byte-identical without any test-fixture change
  2. **Optional field defaults to None**: `RuntimePluginSpec.verify_distinguish_cmd` defaults to `None`; the new `_verify_distinguish(spec, timeout=...)` call sites are no-ops for nines + ui-pro entries
- **97/97 existing test_plugins.py tests PASS byte-identical** (verified by reading the test output — no test from the pre-existing 97-test suite was modified except `test_registry_yaml_loads`'s schema_version assertion 1→2, which is the intentional backward-compat surface change)
- **Mirror system compatibility**: `make check-cursor-skill` exit 0 (no-op when mirror absent — opt-in per CP-3); `tests/test_version.py` mirror-parity tests self-skip when absent; verified 12 PASS / 15 SKIPPED post-bump
- **CI-greenness on fresh clones**: the gitignored `.cursor/skills/devola-flow/` mirror is absent in CI; `scripts/bump_version.py` calls `sync_cursor_skill.py` which no-ops when absent; `scripts/sync_cursor_skill.py` is also a no-op for the mirror sync (verified in bump output: "skipped — .cursor/skills/devola-flow not present (opt-in mirror)")
- Slight dip from v8.3.0's 9.6 because v8.3.0 PV-08 had the byte-counted 71/71 R5 strict claim which set a high bar; v8.3.1 has the same R5 strict discipline but at a smaller scope (97/97 instead of 71/71 + thousands of others)

**Score 9.5** — backward-compat preserved across BOTH the schema bump AND the new field; mirror system + CI compatibility verified.

### 2.6 Performance impact (weight 0.15) — score **9.0/10**

**Evidence:**
- **EvoBench: 36/36 scenarios PASS at 0 regression > 5pp** vs `benchmarks/devolaflow_context/baselines/v8.3.0_baseline.json` (verified W-4 / SI-4 hard gate)
- Worst single-scenario delta vs v8.3.0: 0.000 (PV-01 doesn't touch dispatch — plugin installer runs preconditional)
- pytest tests/test_benchmarks.py runtime: ~15 s (comparable to v8.3.0's 15.13 s)
- No new EvoBench scenarios added (PV-01 doesn't introduce a new dispatch surface; per gap analysis §4.1 EvoBench delta is 0). New scenarios for shell-proxy + memory router will land in PV-02 + PV-03 + PV-04
- The new `_verify_distinguish` call sites add 1 subprocess call per `ensure_plugin('rtk', ...)` invocation when set (and nothing for nines/ui-pro). This is preconditional cost, not per-dispatch cost — so it does NOT show up in EvoBench composites
- The cargo fallback only runs if curl primary fails — failure path; not steady-state cost
- Equal to v8.3.0's 9.0 — passes the W-4 / SI-4 hard gate (0 regression > 5pp); the +5pp net delta soft target is unmet but explainable (PV-01 is preconditional plugin install, not a context-density optimization)

**Score 9.0** — equal to v8.3.0; passes W-4 / SI-4 hard gate; 0 EvoBench regressions.

---

## 3. Composite Score Calculation

| Dimension | Score | Weight | Contribution |
|-----------|------:|-------:|-------------:|
| Code quality | 9.0 | 0.20 | 1.80 |
| Architecture rationality | 9.0 | 0.20 | 1.80 |
| Test adequacy | 9.2 | 0.20 | 1.84 |
| Maintainability | 9.2 | 0.15 | 1.38 |
| Compatibility | 9.5 | 0.10 | 0.95 |
| Performance impact | 9.0 | 0.15 | 1.35 |
| **TOTAL** |  |  | **9.12** (rounded **9.10**) |

**Composite: 9.10/10** (calculated 9.12 with rounding to one decimal place per v8.3.0 evaluation precedent).

**Minor threshold:** ≥ 8.5 → **PASS** (margin **+0.60 pp**).
**Stretch:** ≥ 9.0 → **PASS** (margin **+0.10 pp**).

---

## 4. Risk Register Outcomes (per gap analysis §6 + cycle plan §6)

- **R-1** RTK install fails on agent host (network unreachable / no Rust toolchain) — **MITIGATED** at the design level; tested. The curl primary + cargo fallback chain raises `PluginInstallError` per S-5 with actionable text when both fail (verified by `test_rtk_install_failure_raises_loud`); operator has explicit guidance pointing at rustup install.
- **R-2** Name collision with `rtk-type-kit` — **MITIGATED**. Two layers of defense: (a) `verify_distinguish_cmd: "rtk gain"` runs at BOTH preinstall version-match site AND post-install version-match site; failure raises `PluginInstallError` per S-5 with INSTALL.md pointer (verified by `test_rtk_distinguish_failure_raises_loud`); (b) cargo fallback ALWAYS pins `--git https://github.com/rtk-ai/rtk` — never bare `cargo install rtk` (verified by the in-test assertion `"cargo install --git https://github.com/rtk-ai/rtk" in cargo_call`).
- **R-3** Shell-tool intercept layer breaks existing dispatch — **N/A for PV-01** (shell_proxy lands in PV-02). PV-01 is plugin-installer-only.
- **R-4** SKILL.md ceiling at 498/500 breached — **N/A for PV-01**. SKILL.md untouched in this PV; the surface for memory router / shell-proxy is deferred to v8.4.0 rollup per gap analysis §4.5.
- **R-5** Cycle scope drift / 4.7× over-delivery — **PARTIALLY HONORED** for PV-01: +14 actual vs +9 forecast (1.56×). Far below v8.3.0's 4.7× ratio but still over forecast. The 5 extras (`TestRtkSchemaV2` + `TestRtkCurlScriptHelpers`) each cover a distinct untested code path — no scope creep into unrelated areas. Score reflected in maintainability §2.4 (9.2) and test adequacy §2.3 (9.2 — bumped from 9.0 to reflect the extra coverage value).

---

## 5. Cross-references

- v8.4.0 SI-1 planning gate (predecessor): `.local/research/v8.4.0_gap_analysis.md` (R-001 row in §2.1; +9 test forecast in §4.1)
- v8.4.0 SI-2 NineS analysis on RTK (predecessor): `.local/research/v8.4.0_rtk_nines_analysis.md` (§5 plugin recommendation drives the curl_install_script backend choice; §7 R-1/R-2/R-4 risks drive the verify_distinguish_cmd discipline)
- v8.4.0 SI-2 raw NineS JSON: `.local/research/v8.4.0_rtk_nines_analysis.json` (1242 lines)
- v8.4.0 cycle plan: `/root/.cursor/plans/v8.4.0_rtk_+_memory_router_e4ec076d.plan.md` (PV-01 row in §3 + per-PV gating in §4)
- v8.3.1 NineS post-PV: `.local/research/v8.3.1_nines.json` (raw) + `.local/research/v8.3.1_nines.md` (synthesis — companion to this evaluation)
- v8.3.0 SI-3 (predecessor for scoring scale calibration): `.local/research/v8.3.0_evaluation.md` (composite 9.40/10)
- v8.3.0 PV-01 commit (file-level pattern reference): `dbf5433` (first plugin runtime auto-install patch; v8.3.1 PV-01 mirrors its discipline)
- RTK source (per S-7): https://github.com/rtk-ai/rtk
- Branch: `feat/v8.3.1-rtk-plugin`
- v8.3.1 baseline (still v8.3.0): `benchmarks/devolaflow_context/baselines/v8.3.0_baseline.json` (45 scenarios; PV-01 doesn't bump baseline because no new EvoBench scenarios were added)

---

## 6. Recommendation

**Composite 9.10/10 ≥ minor threshold 8.5 by +0.60 pp; ≥ stretch 9.0 by +0.10 pp** → **READY FOR v8.3.1 PATCH RELEASE**.

L0 SHOULD proceed to commit + PR + merge + tag:
1. Bump version 8.3.0 → 8.3.1 via `scripts/bump_version.py 8.3.1` ✓ (done by L3 in this PV)
2. Run `make sync-human-docs` ✓ (done by L3)
3. Add `[8.3.1]` patch CHANGELOG entry above `[8.3.0]` ✓ (done by L3)
4. Run SI-10 6/6 (final gate before commit) — **pending Step 7 of the L3 task plan**
5. Commit + push branch + open PR — **pending Step 8**
6. Merge PR + push annotated v8.3.1 tag — **pending Step 9**
7. Confirm v8.3.1 tag in place; update v8.4.0 cycle plan to mark PV-01 = completed; PV-02 (`v8.3.2 shell-proxy`) is unblocked

This is **PV-01 of v8.4.0** (4 PVs + rollup forecast). **46/46 lifetime PV cycles ACCEPTED** (45/45 v8.3.0 + 1/1 v8.3.1 — 100% accept rate streak unbroken). PV-02 (R-002 closure — RTK shell-proxy wrapper) follows next per gap analysis §4.2.
