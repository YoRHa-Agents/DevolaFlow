# v18.0.0 Gap Analysis — Consolidation Major (SI-1)

> Status: **PLAN FINALIZED — awaiting human start signal** (grilling: 13 questions + 4 re-asked
> sub-questions, all settled by operator on 2026-08-27; no implementation performed).
> Method: 4 parallel fact-finding audits (coverage / residue / CJK language / design survey)
> + 4 targeted implementation-status probes (version-sync, preflight-speed, HSC Phase 2,
> Pathfinder F-1) + 4 grilling rounds.
> Scope verdict (Q1): **pure consolidation MAJOR** — no feature work except F-1 (accepted
> below with embedded SI-1 section). SI-3 composite floor for MAJOR: ≥ 9.0.

---

## 1. TL;DR

The repo is far healthier than the task brief assumed: coverage is already 91.43%
(floor 80), agent-ingested surfaces are already ~English-only (3 runtime CJK lines),
and `detect_dead_apis` is green. The REAL v18 work inverts the brief's framing:

1. **Coverage** → lock reality in (ratchet floor 80→90, per-module floor 70), not "raise".
2. **Residue** → mostly *additive* debt-paying (5 missing W-16 baselines, missing W-19
   archives for v17.4.0/v15.1.0, stale gitignore cycle pin), plus small true deletions.
3. **English-only** → the deliverable is the *regression gate* (CJK lint), not translation.
4. **Optimization list** → the big wins are token/rules slimming (~2-4K tokens/session),
   ghost-suite tiered retirement (17,144 lines), god-module control, Pattern 3 prewiring
   removal, HSC SSOT truth-alignment, and harness self-bootstrap (ledger has 13 lines).
5. **F-1** (preflight role-ification + `harness_build` task type) — confirmed 0% implemented
   after 2 cycles of written deferral; ACCEPTED into v18 second half (§6).

Every optimization item carries a named verification surface (§4 matrix) per the task
brief's "harness capability" requirement, split per Q6 verdict: boolean invariants →
pytest/CI gates; directional numbers → harness evaluator metrics settled into the W-16
baseline (`harness_baseline_v18.0.0.json`).

---

## 2. Grilling decision record (all settled)

| # | Question | Verdict |
|---|---|---|
| Q1 | v18 positioning | **A: pure consolidation major**; parked features get explicit verdicts only |
| Q2 | v17 cycle-debt settlement | **Archive-only** (W-19 idempotent backfill), missing baselines recorded `INSUFFICIENT` per W-2 (no fabrication); gitignore whitelist policy `v15*` → current major + untrack 10 v15 files after byte-verified archive copies |
| Q3 | Gap-analysis timing | **Write immediately after convergence** (this document) |
| Q4 | Coverage actions | `fail_under` **80→90**; **per-module floor ≥70%** gate; targeted tests for `emdash.py`/`cli.py`/`selector_cli.py`; `pre_decision/__main__.py` shim exempted via pragma/omit |
| Q5 | W-17 test cap | **Respect as-is** (no rule amendment; workload fits) |
| Q6 | CJK gate scope | Clear all stock CJK; **hard-fail** on agent-ingested surfaces; exemption allowlist = `workflow-system/human/**`, `docs/cycle-archive/**`, `.local/**`, ZH install-skill trigger phrases (bilingual feature), marked test fixtures; runtime artifacts warn-first via `agent_workspace.lint` soft/hard tiers |
| Q7 | AGENTS.md slimming | **Aggressive**: historical narration moved out (one-line archive pointers), rules body reduced to normative sentences |
| Q8 | Ghost tiered retirement | **Adopt**: pre-v16 ghost feature tests default-skip locally, `GHOST_FULL=1` runs all (W-20 orthogonality argument required in design), CI release-preflight keeps full run |
| Q9 | God-modules | **Gate + split top 3-5** (new-module ≤800-line hard gate + grandfather ratchet; split list in §4 item O-4, operator veto at execution start) |
| Q10 | Pattern 3 prewiring | **Remove** (delete `AGENT_POOL_FORWARD` advisory path + W-24.2–24.5 prose; ADR + F-2 archive confirmation; schema slot absence-canonical so A-2.4 byte witnesses unaffected) |
| Q11 | Harness bootstrap | **Adopt**: every v18 PV records telemetry; consolidation metrics added to evaluate/baseline |
| Q12-a | Skill budget headroom + import-cycle gate | **Both in** |
| Q12-b | Version-sync + preflight-speed residuals | **Both in as small hygiene items** (each < 1 day; verified already mostly-implemented) |
| Q12-c | HSC disposition | **SSOT truth-alignment in** (flip DSH `broken`→`implemented`, fixture native vocab, provenance; optional Copilot live smoke); **G-3/G-4 written defer** |
| Q12-d | Pathfinder F-1 | **Accept into v18 second half** as own PV; SI-1 precondition satisfied by §6 of this document; skill-budget headroom (Q12-a) is a hard prerequisite |
| Q13 | User-global stale stub `~/.cursor/rules/devola-flow-rules.mdc` | **Delete** (execution-phase action) |

Key facts verified during grilling (execution need not re-check):

- Coverage run: 5,425 passed / 14 skipped / 1 xfailed in 2m15s; TOTAL 91.43% (22,859 stmts / 1,958 miss); 4,358 test functions in 269 files.
- Sub-80% modules (only 4): `pre_decision/__main__.py` 0% (3-stmt shim), `writing_style/transforms/emdash.py` 57%, `cli.py` 70%, `selector_cli.py` 73%. Top absolute miss: `agent_workspace/preflight.py` 165, `cli.py` 120, `local/archive.py` 119.
- Runtime CJK: `task_adaptive_selector.py` fold banner (3 lines, ~25 chars; asserted by `tests/test_behavioral_guidelines.py`) + 1 gloss in `knowledge/reference-dependencies.yaml`. On-demand: `references/task-quality-score.md` (4), `references/human-surface.md` (4), install-skill ZH triggers (9, intentional).
- Rules corpus: AGENTS.md 927 lines ≈ 12.7K tokens; historical narration ≈ 26% (~3.3K tokens); largest slim targets A-5 (~837 tok), W-21 (~516), A-2.3/A-2.4 (~400 each), W-20 (~394), S-10 (~458), W-22..W-24 (~2.3K combined).
- Ghost suite: 43 per-cycle files, 17,144 lines (v9–v12 ≈ 11K lines).
- God modules: 19 files > 800 lines; largest `compressor/transforms.py` 2,319.
- Skill budgets near ceiling: `execution-protocol.md` **1000/1000**, `grill-mode.md` 927/1000, `SKILL.md` 462/500.
- Cycle debt: W-16 baselines exist only for v16.0.0 + v17.0.0 (v17.1–17.5 all missing); `docs/cycle-archive/` missing v17.4.0 and v15.1.0 entirely; HSC design mis-parked under v17.3.0 archive; `.gitignore` still whitelists `.local/research/v15*` (10 tracked files); telemetry ledger = 13 lines.
- Version-sync (probe): mostly hardened (G-032 zero-hit fail, `--dry-run`, fixture tests, npm tag fail-fast). Residual: exact hit-count assertion, full 10-pattern fixture dry-run, README example pin.
- Preflight speed (probe): `precommit-fast`/`precommit-full` + G-033 dedup hold inside release-preflight. Residual: per-gate timing instrumentation, README/SKILL fast-vs-full docs, `release-dry-run` alignment.
- HSC (probe): Phase 2 code shipped v17.4.0–17.4.2 (Copilot contract-complete; DSH live deny smoke in `.local/research/v17.4.1_dsh_smoke.md`), but `hosts.yaml` still declares DSH `broken`/`synthetic`, and `tests/fixtures/hostbridge/dsh.json` cases still use plugin-shaped `write_file`/`run_shell` instead of native `write`/`edit`. G-3 (DSH web UI) and G-4 (Tier-C retention) genuinely parked.
- F-1 (probe): 0% implemented anywhere (38 branches, PRs #165–#204, stash, worktrees all checked); `pathfinder-role/goal.md` lists it Out-of-scope ("next cycle, human gate"); v17.4/v17.5 gave no written disposition — v18 MUST dispose (done: accepted, §6).
- Stale bookkeeping found: `.local/.agent/active/{pathfinder-role,local-archive-v17.5.0}` STATUS still `PROPOSED/0%` though both shipped (PR #200, #204).

---

## 3. Deficiency list (W-1a) with priority (W-1b)

| # | Deficiency | Evidence | Priority |
|---|---|---|---|
| D-1 | Cycle-closure debt: 5 missing W-16 settlements, 2 missing W-19 archive dirs, stale gitignore cycle pin, HSC design mis-parked | §2 facts | **P0** |
| D-2 | Coverage floor (80) lags reality (91.43) — regression head-room of 11pt is an un-guarded invariant | coverage audit | **P0** |
| D-3 | Rules corpus injects ~3.3K tokens/session of non-normative history | design survey | **P0** |
| D-4 | Harness under-instrumented: ledger 13 lines → W-2/W-16 evidence structurally `INSUFFICIENT` | design survey | **P0** |
| D-5 | HSC SSOT contradicts shipped reality (DSH `broken` vs live smoke pass) — W-25 hygiene violation | HSC probe | **P1** |
| D-6 | Ghost suite unbounded accretion (17,144 lines, never retires) inflates suite time + W-17 pressure | design survey | **P1** |
| D-7 | Pattern 3 dead prewiring: 3 versions of advisory-only code + ~1K tokens of rule prose, re-evaluation gate unmet | design survey + W-24.2 | **P1** |
| D-8 | F-1 stagnation: 2 cycles without the mandated written accept/re-defer | F-1 probe | **P1** |
| D-9 | Skill budget saturation: execution-protocol.md 1000/1000 blocks ANY doc change incl. F-1 | design survey | **P1** |
| D-10 | No CJK regression gate (stock is clean by accident, not by contract) | CJK audit | **P1** |
| D-11 | 19 god-modules (> 800 lines) with no growth control | design survey | **P2** |
| D-12 | `harness.telemetry ↔ agent_workspace` import cycle; lifecycle hub fan-out | design survey | **P2** |
| D-13 | Version-sync + dev-loop speed residuals (each < 1 day) | probes | **P2** |
| D-14 | Stale bookkeeping: 2 completed-but-PROPOSED change folders; stale deprecation copy (seed-alias "remove in v17.0.0"; `test_no_ghost_features.py` past its own removal target); `generate_si3_evaluation.py` unwired; 86-entry dead-API allowlist un-aged | residue audit | **P2** |
| D-15 | User-global stale rule stub injects outdated pointers into every Cursor session | residue audit | **P2** (one command) |

---

## 4. Optimization–verification matrix (W-1c: fixes with file-level scope)

Verification tiers per Q6: **[G]** = pytest/CI gate (boolean invariant); **[M]** = harness
evaluator metric tracked in W-16 baseline (directional number). Every [M] lands in
`harness_baseline_v18.0.0.json` at cycle close.

| ID | Work item | Files (primary) | Benefit | Verification |
|---|---|---|---|---|
| O-1 | Cycle-debt settlement (D-1): backfill `docs/cycle-archive/v17.4.0/` + `v15.1.0/`; record v17.1–17.5 baselines `INSUFFICIENT`; gitignore policy bump + untrack v15 research; archive 2 stale change folders; re-park HSC design under v17.4.0 | `scripts/archive_research_artifacts.py` runs, `.gitignore`, `tests/test_gitignore_policy.py`, `.local/.agent/active/*` | Debt zeroed; W-16 window restored | [G] `test_gitignore_policy.py` rewritten; archive dirs exist; W-19 idempotency |
| O-2 | Coverage lock-in (D-2): `fail_under = 90`; per-module floor ≥ 70 gate; tests for `emdash.py`/`cli.py`/`selector_cli.py`; shim omit | `pyproject.toml`, new `tests/test_coverage_floor_per_module.py` (or coverage config), 3 test files | 11pt regression head-room closed | [G] coverage gate; [M] `coverage_pct` (existing evaluator signal) |
| O-3 | AGENTS.md aggressive slim (D-3): strip historical narration from `.rules/*.mdc`, recompile; W-24.2–24.5 prose removal folds in via O-7 | `.rules/{workflow,architecture,soul}.mdc`, `AGENTS.md` (compiled), `.cursor/rules/repo-governance.mdc` (compiled) | −2~4K tokens/session, compounding | [G] compile-rules + check-rules-drift + slice tests; [M] new `agents_md_tokens` metric |
| O-4 | God-module control (D-11): new-module ≤ 800-line hard gate + grandfather ratchet (only shrinks); split top-N. Proposed splits (operator veto at start): `compressor/transforms.py` (2319), `agent_workspace/reporter.py` (1820), `agent_workspace/lint.py` (1777), `plugins/installer.py` (1768); `gate/scorer.py` (1663) optional 5th. `preflight.py` EXCLUDED (F-1 restructures it); `task_adaptive_selector.py` EXCLUDED (hash/verbatim pins) | listed modules + new `tests/test_module_size_budget.py` | Blast-radius + maintainability; stops accretion | [G] size-budget gate + grandfather list ratchet; S-3 ≥80% on split-out modules |
| O-5 | Ghost tiered retirement (D-6): pre-v16 feature files default-skip, `GHOST_FULL=1` full run (W-20 orthogonality documented), CI release-preflight always full | `tests/ghost/conftest.py`, `references/env-flags.md` §2, design note | Dev-loop suite time cut; W-17 pressure relief | [G] `tests/test_ghost_full_disabled_is_noop.py` R5-strict; [M] `suite_wall_seconds` |
| O-6 | CJK regression gate (D-10): clear fold banner (`task_adaptive_selector.py` L1064–1066 + `test_behavioral_guidelines.py`), gloss, 2 reference quotes; add lint with exemption allowlist per Q6 | `task_adaptive_selector.py`, `knowledge/reference-dependencies.yaml`, `references/{task-quality-score,human-surface}.md`, new `tests/test_no_cjk_agent_surfaces.py` | English-only becomes a contract | [G] CJK lint hard-fail; [M] `cjk_violations` |
| O-7 | Pattern 3 prewiring removal (D-7): delete `AGENT_POOL_FORWARD` advisory path, W-24.2–24.5 prose; ADR; F-2/F-3 archive confirmation | `skills/subagent_pattern.py`, `gate/cascade.py`, `.rules/workflow.mdc`, `references/subagent-patterns.md`, new ADR | −~1K rule tokens; no false forward surface | [G] A-2.4 multi-baseline stays GREEN; ghost pins updated; W-18 order respected |
| O-8 | HSC SSOT truth-alignment (D-5): DSH `broken/synthetic` → `implemented/vendor-doc` + `provenance_ref`; `dsh.json` native vocab (`write` + `arguments.file_path`); optional Copilot live smoke artifact | `workflow-system/agent/hosts.yaml`, `tests/fixtures/hostbridge/dsh.json`, `tests/test_hostbridge.py` | Contract tells the truth (W-25) | [G] host-contract parity tests; fixture provenance lint |
| O-9 | Skill budget headroom (D-9): split/trim `execution-protocol.md` (1000/1000) and `grill-mode.md` (927); SKILL.md stays ≤ 500 | `references/execution-protocol.md`, `references/grill-mode.md`, C-7 four-piece wiring if split adds a reference | Unblocks F-1 + future doc changes | [G] `test_reference_size_budgets.py`; [M] budget headroom % |
| O-10 | Import hygiene (D-12): break `harness.telemetry ↔ agent_workspace` cycle; add import-graph gate | the 2 modules + new `tests/test_import_graph.py` (or importlinter) | Layering safety | [G] no-domain-cycles gate |
| O-11 | Harness bootstrap (D-4): telemetry recording mandatory every v18 PV; add consolidation metrics (`agents_md_tokens`, `suite_wall_seconds`, `cjk_violations`, `ghost_loc`, existing `coverage_pct`) to evaluate/aggregate; settle `harness_baseline_v18.0.0.json` at close | `src/devolaflow/harness/{evaluator,aggregator,telemetry}.py`, `.local/telemetry/harness.jsonl` | W-2/W-16 evidence becomes real; anti-regression trend lock | [G] `make test-harness`; [M] all of the above, settled at cycle close |
| O-12 | Small hygiene (D-13, D-14): version-sync exact hit-count + full fixture dry-run + README pin; per-gate timing wrappers + README fast/full docs + `release-dry-run` alignment; stale deprecation copy fixes; `generate_si3_evaluation.py` wire-or-document; allowlist age-review | `scripts/bump_version.py`, `tests/test_bump_version.py`, `Makefile`, `README.md`, `template_engine/registry.py`, `tests/test_no_ghost_features.py`, `scripts/detect_dead_apis.py` | Fragility + observability | [G] existing suites extended; timing feeds `suite_wall_seconds` |
| O-13 | F-1 mechanism unification — see §6 | `references/team-roles.md`, `context_profiles.yaml`, seeds, `execution-protocol.md`, `agent_workspace/preflight*.py` (drafting-labor extraction) | Unifies last 2 inline sidecar mechanisms onto validated L2 role architecture | [G] W-5 full suite + new role tests; [M] telemetry records role dispatches |
| O-14 | Off-repo: delete `~/.cursor/rules/devola-flow-rules.mdc` | user-global file | Stops stale injection | Manual (one command, execution phase) |

---

## 5. PV decomposition (draft sequence)

| PV | Content | W-17 est. (new test fns) |
|---|---|---|
| PV-0 | O-1 debt settlement + O-14 stub delete + O-11 telemetry mandate starts | +3~5 (gitignore policy tests) |
| PV-1 | O-2 coverage lock-in + O-12 version-sync residual | +18~30 |
| PV-2 | O-3 AGENTS.md slim + O-7 Pattern 3 removal (both touch `.rules/`, one recompile wave) | +4~8 (ADR/ghost pins) |
| PV-3 | O-5 ghost tiering + O-12 timing instrumentation + O-4 size gate (grandfather list only) | +4~6 |
| PV-4 | O-4 module splits (top 3-5) + O-10 import gate | +5~10 (mostly moved, not new) |
| PV-5 | O-8 HSC SSOT alignment + O-6 CJK clear + gate | +6~10 |
| PV-6 | O-9 skill budget headroom (F-1 prerequisite) | +2~4 |
| PV-7 | O-13 F-1 implementation | +10~15 |
| PV-8 | Cycle close: O-11 metrics settle, W-2 evaluate, W-3 (≥9.0), W-7 retrospective, W-16 baseline, W-19 archive | +0~4 |

Cumulative estimate: **+52~92 new test functions** — inside the ≤ +150/cycle cap with
margin (Q5: no W-17 amendment). Mid-cycle audit at PV-4/PV-5 boundary per W-17.

---

## 6. F-1 embedded SI-1 section (Q12-d precondition)

**(a) Deficiency.** Preflight drafting labor (~2.4K LOC inline: `agent_workspace/preflight.py`
1714 + `preflight_runtime.py` 509 + `lifecycle/preflight_authorization.py` 164) and
harness-build remediation (ordinary Implement via `harness-construction.yaml` seed) are the
last two sidecar mechanisms NOT expressed as L2 task specializations, while Pathfinder
(`type: pathfind`, PR #200) and Reviewer (`type: review`) already validate the target
architecture. Pathfinder BLOCKERs name `suggested_owner: harness-build` with no such owner
type existing.

**(b) Why no existing rule covers it.** The v17.3.0 design (archived
`docs/cycle-archive/v17.3.0/design/v17.3.0_pathfinder_design.md` §4.6/§6) explicitly
falsified plugins-as-role-host (D-6/D-7: `runtime-plugins.yaml` is external CLI tools) and
mandated a separate cycle with its own SI-1 — this section discharges that mandate.

**(c) Proposed fix, file-level.** Add `type: preflight` (drafting labor only — gate
semantics, human sign-off, stop cards stay L0+human) and `type: harness_build` (BLOCKER
remediation owner) to: `references/team-roles.md`, `context_profiles.yaml` (budgets +
timeout classes), seeds + intent hints, `references/execution-protocol.md` §12 wiring,
`change-driven` template. No new plugin registry, no Pattern 3 pool, no new env flag
(W-20), canonical_order stays 17 (A-2.3 NEST discipline).

**(d) Review trail.** Telegraphed v17.3.0 retrospective §3 (F-1); v17.4/v17.5 gave no
written disposition (stagnation noted); v18 disposes: **ACCEPT, second-half PV, after O-9.**

---

## 7. Explicit verdicts for parked items (anti-stagnation ledger)

| Item | Verdict |
|---|---|
| Pattern 3 pool runtime (W-24.2, F-2/F-3) | **REMOVE prewiring** (O-7) — re-evaluation gate unmet (no SI-1 evidence: ledger 13 lines; no persistent-state design). Future revival = fresh proposal per W-21 cadence |
| HSC G-3 (DSH web/progress UI) | **DEFER** — new runtime surface, conflicts with Q1 pure-consolidation |
| HSC G-4 (Tier-C host retention) | **DEFER** — policy decision needs usage evidence that O-11 will start producing |
| `harness-construction.md` §2.2 suggest channel | **REMAIN DEFERRED** — unrelated to F-1; no new evidence |
| Pathfinder F-1 | **ACCEPT** (§6) |

## 8. Guardrails

- Tier-A byte witnesses, `docs/cycle-archive/` history, handoff envelopes: untouched (S-8/S-9/A-2.4).
- Soul layer: no additions (W-21 freeze holds at S-1..S-10).
- `.local/` beyond the settled debt items: inventory-only; operator adjudicates.
- All rule-body edits go through `.rules/` sources + `make compile-rules` (never hand-edit compiled surfaces).
- Every PV: SI-10 seven-gate chain; release: `make release-preflight`; S-6 feature branches + PRs only.

## 9. Execution status

**Not started by instruction.** This document is the finalized review + list + plan
deliverable. §5 sequence is mechanically executable on human start signal.
