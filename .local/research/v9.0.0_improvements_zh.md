# DevolaFlow v9.0.0 SI-1 — 改进清单 (v9.0.0 规划输入)

> 来源：`v9.0.0_decomposition_analysis.md` + `v9.0.0_conflict_register.md` + `v9.0.0_overlap_matrix.md` + `v9.0.0_perf_trajectory.md` + `v9.0.0_reference_review.md`
> 日期：2026-04-23 | 作者：L3 Task Agent (Design team) | 任务：v9.0.0-S04-T02
> 用途：直接驱动 `.local/research/v9.0.0_gap_analysis.md` (S05-T01) 与 `.local/research/v9.0.0_version_roadmap.md` (S05-T02)
> 约束：C-3 verbatim（技术标识符不翻译，引用原文带 `"..."` 标注）；S-2 全部相对路径；S-5 无臆造 deferral

## 目录

- [执行摘要](#执行摘要)
- [优先级矩阵](#优先级矩阵)
- [按主题分组的改进清单（8 大主题）](#按主题分组的改进清单8-大主题)
  - [主题 T1 — SKILL.md 压缩与余量回收](#主题-t1--skillmd-压缩与余量回收)
  - [主题 T2 — 单一事实源注册表模式 (A-5)](#主题-t2--单一事实源注册表模式-a-5)
  - [主题 T3 — 压缩流水线统一](#主题-t3--压缩流水线统一)
  - [主题 T4 — 生命周期 Hooks 全链路接线 + Plan-Mode 提示侧嵌入](#主题-t4--生命周期-hooks-全链路接线--plan-mode-提示侧嵌入)
  - [主题 T5 — Karpathy 原语 Default-On Flip (B3)](#主题-t5--karpathy-原语-default-on-flip-b3)
  - [主题 T6 — 规则体系重新平衡 + Soul Set Freeze](#主题-t6--规则体系重新平衡--soul-set-freeze)
  - [主题 T7 — Cache-Layout 治理 v2 (Nest-vs-Append)](#主题-t7--cache-layout-治理-v2-nest-vs-append)
  - [主题 T8 — NineS Hygiene 包 A1-A4 收尾 + 审计 W-Rules](#主题-t8--nines-hygiene-包-a1-a4-收尾--审计-w-rules)
- [Reference 层面的改进（来自 S03 review）](#reference-层面的改进来自-s03-review)
- [Carry-Forward 从前代继承的 Deferrals](#carry-forward-从前代继承的-deferrals)
- [v9.0.0 建议的 PV-to-Theme 映射](#v900-建议的-pv-to-theme-映射)
- [人类决策项（S05 必须回答）](#人类决策项s05-必须回答)
- [方法论备注](#方法论备注)

## 执行摘要

本清单合并 5 份 SI-1 前置工件（`v9.0.0_decomposition_analysis.md` 8 主题 + 7-PV sketch + 12 open decisions / `v9.0.0_conflict_register.md` 14 冲突 × 4 严重度 / `v9.0.0_overlap_matrix.md` 14 feature family + 10 consolidation candidates / `v9.0.0_perf_trajectory.md` 13 v9.0.0 数值目标 / `v9.0.0_reference_review.md` 11 refs 裁定 + 15 F-NN 关键发现），输出**共 47 条 v9.0.0 周期内可执行改进项**：T1-T8 主题级 31 项 + Reference 级 15 项 + 9 项 Carry-Forward 处置（部分跨主题），分布为 **P0 = 12 / P1 = 18 / P2 = 11 / P3 = 6**。**v9.0.0 必须以 PATCH 的 PV-01（强制 SKILL.md 压缩 + 4 条 `references/shell-proxy.md` must-fix）启动**，再以 PV-02 一次性吸收 9 条 reference 漂移修复，最后在 PV-07 以 MAJOR 形态完成 T6 规则体系重平衡；总建议 PV 数 **8 个**（PV-01..PV-07 + 可选 PV-08 sustaining）。**最重要的 3 条 P0**：F-01 `references/shell-proxy.md` S-2 绝对路径违规（必须 PV-01 修复，否则不能发 release）、F-02 `references/context-isolation.md` `layout_invariant` 12-keys vs 16-keys 漂移（T7 编纂的前置）、T1 SKILL.md 压缩到 ≤ 480 行（C-02 BLOCKER；任何后续 SKILL 触动的前置）。

**必须人类决策的关键项 8 条**（详见 §人类决策项）：v9.0.0 是否 MAJOR、SF-1 SKILL.md 是否升 Large tier、是否新增 12/13 个 canonical reference、规则体系是否新增第 6 类、B3 5 原语是否打包翻转、M-007 slash commands 处置、`shell-proxy.md` 4 条 must-fix 是否走 v8.4.1 hotfix、NineS Hygiene 包是否单 PV。所有决策项均在 S02/S03 工件中明确标注为 open question，未在本清单内自行裁定。

## 优先级矩阵

使用 4 级严重度分类：

- **P0 (BLOCKER)**：v9.0.0 release 前必须解决（否则无法发布 MAJOR）
- **P1 (CRITICAL)**：v9.0.0 周期内必须完成（可以跨 PV 但不能跨 release）
- **P2 (MAJOR)**：应在周期内完成，可以 defer 到 v9.0.x 小版本
- **P3 (MINOR)**：锦上添花，可以 defer 到 v9.x

| 优先级 | 数量 | 典型示例 |
|---|---|---|
| **P0** | 12 | T1 SKILL.md 压缩到 ≤ 480 (C-02) · T7 cache-layout 治理 v2 + multi-baseline byte test (C-01) · F-01 `shell-proxy.md` S-2 绝对路径违规 · F-02 `context-isolation.md` 12-keys → 16-keys · F-03 `message-schemas.md` 仅 verbose legacy schema · F-05 `MemoryCase` dataclass fab · F-06 recipe `note:` → `replacement:` · F-14 §5.2 fab 级联 · F-04 `behavioral-guidelines.md` SKILL.md Tier-2 orphan |
| **P1** | 18 | T2 A-5 SSOT registry rule + parity test (C-07) · T4 lifecycle.run_hooks dispatch-emission 接线 (C-12) · T4 plan-mode prompt-side embedding 升 S-rule · T6 50-rule 选择性切片机制 (C-08) · T5 B3 5 原语 default-OFF → ON · T8 A1..A4 NineS hygiene bundle · T8 W-16 wholesale-baseline-regen 编纂 (C-13) · T8 W-17 per-PV test 上限 (C-11) · F-07 meta-framework 12/22 alias 漏 10 模板 · F-08 decomposition-gate 缺 7 v8.0.0 gate primitives · F-09 frontmatter freshness CI · F-10 team-roles 11/22 矩阵漏 11 · F-11/F-12/F-13 execution-protocol 缺 change-driven/memory_router/pre_shell_call · F-15 context-isolation §15 重号 · M-004 ArchiveManager.propose_merge 完整闭合 · I-PV07-A REPORT.md auto-trigger · M-007 slash commands 决策（v9.0.0 vs v9.1.0） |
| **P2** | 11 | T8 W-18 ghost-audit refresh 编纂 (C-05) · T8 W-19 research artifact archive (C-10) · T8 W-20 env-flag 复用规则 (C-03) · T8 `@pytest.mark.deferred` 第二 marker class (C-06) · T6 Soul-set-add governance rule (C-09) · NineS gate boundary 拆分（C-04 — SI-3 区块拆 2） · T3 `apply_local_recipe` 多 pass filter chain（v8.4.0 §3.3 deferred） · L-001..L-004 legibility 扩展 · `command_mapping_savings` EvoBench 场景 · `runtime-plugins.yaml::expected_sha256` (RTK SHA256SUMS 采用) · Memory router cache invalidation 超 TTL（A-4 引用计数） |
| **P3** | 6 | C-14 `change-driven` `owned_files` vs L3 trivial waiver 集成 test · Tier-2 企业 adapter 批次（JetBrains/Amazon Q/Gemini/Augment/Trae） · N3 source-freshness updater · V6-04 `unwanted_hints` 负路由 · V6-05 SkillRouter 语义 profile 匹配（"50+ profiles" 阈值） · `references/repo-modes.md` Tier-3 重 tier 化（S03 Open Question #5） |

**说明**：此矩阵为后续 S05 gap_analysis §"Priority allocation" 提供 4 桶分布；具体到每个 PV 的 patch 选择见 §v9.0.0 建议的 PV-to-Theme 映射。

## 按主题分组的改进清单（8 大主题）

### 主题 T1 — SKILL.md 压缩与余量回收

**一句话描述**：在任何其他 SKILL.md 表面修改之前，把 `workflow-system/agent/SKILL.md` 从 499/500 压到 ≤ 480，回收至少 20 行余量并完成 R7 line-anchor → symbolic-anchor 迁移。

**来源**：

- Overlap matrix 发现：OV13（"`workflow-system/agent/SKILL.md` 499/500 — **CRITICAL 1-line buffer**, per v8.4.0 retro §4.2 #1"）
- Conflict register：C-02 (**BLOCKER**) + C-13 (CRITICAL — wholesale baseline regen)
- Decomposition analysis：Theme T1
- Reference review findings：F-04（SKILL.md Tier-2 nav 缺 `behavioral-guidelines.md`，必须先释余量）
- Perf trajectory：§"SKILL.md Line Count Trajectory" 449→293→499/500

**关键痛点**（verbatim 引用）：

- v8.4.0 retro §4.2 #1 原文：*"SKILL.md headroom now at 1 line (499/500) ... v8.5.0+ MUST either (a) Promote SKILL.md to a higher tier (SF-1 Large tier 1000), OR (b) Run a dedicated SKILL.md compression PV (likely the A2 carry-forward + R7 section-anchor migration), OR (c) Defer all SKILL.md surface changes to a single 'SKILL.md cycle' PV."*
- v6.1.0 retro §4 #6 原文：*"a 2-line insertion inside the Dispatch & Report Protocol section inflated the extracted token count for `lifecycle_hooks` (range 373-383) enough to squeeze out `rationalization_prevention`, dropping `feedback_regression` composite 99.33 → 90.34."*（line-anchor 耦合的根因）
- v8.4.0 retro §4.1 #6（C-13 来源）：*"codify in CP-6 (or new W-16) — any SKILL.md edit triggers wholesale baseline regen, not just additive entries"*

**v9.0.0 期望**：PV-01 完成后 `workflow-system/agent/SKILL.md` ≤ 480 行（≥ 20-line 余量），`task_adaptive_selector.py` + `section_registry.py` 全部使用符号 anchor，`benchmarks/devolaflow_context/baselines/v9.0.0_baseline.json` 一次性 wholesale 重生且双视图 diff 归档。

**具体改进项**：

1. **[P0]** SKILL.md 压缩到 ≤ 480 行
   - 涉及文件：`workflow-system/agent/SKILL.md`、`workflow-system/agent/references/plan-mode-enforcement.md`（NEW，吸收 §"Mode Awareness" + §"Reinforcement Rules"）、`workflow-system/agent/context_profiles.yaml`、`src/devolaflow/section_registry.py`
   - 估算 LOC：SKILL.md −20 net；新 reference +350~450 行（Tier-2 Large tier sweet spot per S03 §"Additions A1"）
   - 前置依赖：无（必须最先做）

2. **[P0]** R7 line-anchor → symbolic anchor 全量迁移
   - 涉及文件：`src/devolaflow/section_registry.py`、`workflow-system/agent/context_profiles.yaml`、`benchmarks/devolaflow_context/baselines/v9.0.0_baseline.json`
   - 估算 LOC：±0 net（refactor）
   - 前置依赖：T1 #1（SKILL.md 必须先稳定）

3. **[P0]** Wholesale baseline 重生 + two-view diff 归档（W-16 候选编纂的实施）
   - 涉及文件：`benchmarks/devolaflow_context/baselines/`（regen v7.8.0 + v8.3.0 + v8.4.0 + v9.0.0_baseline.json）、`.local/research/v9.0.0_evobench_summary.md`（NEW two-view diff 归档）
   - 估算 LOC：~+150（diff 归档）
   - 前置依赖：T1 #1 + #2

4. **[P1]** SKILL.md Tier-2 nav 增加 `references/behavioral-guidelines.md` 行（关闭 F-04）
   - 涉及文件：`workflow-system/agent/SKILL.md`（+1 行 — 必须 #1 完成后才有空间）
   - 估算 LOC：+1
   - 前置依赖：T1 #1

5. **[P2]** 把 SKILL.md §"AGENT MODE — Full Orchestration" "Execution Protocol" 7-step list 抽取到 `references/execution-protocol.md` §0（S03 Batch C Cross-Batch §4 建议；可省 ≥ 9 SKILL.md 行）
   - 涉及文件：`workflow-system/agent/SKILL.md`、`workflow-system/agent/references/execution-protocol.md`
   - 估算 LOC：SKILL.md −9；ref +12
   - 前置依赖：T1 #1（同 PV 落地）

**建议 PV 归属**：PV-01 (v8.4.1 PATCH) — 全主题在单 PV 内完成

**风险**：SKILL-edit 级联 benchmark drift（v6.1.0 §4 #6 根因）；通过 R7 anchor 迁移（#2）+ wholesale 重生（#3）在同 PV 内 mitigate；如果 R7 迁移本身引入 > 5pp drift，回退到 v8.2.0 PV-05 的 hybrid（symbolic + line-fallback + DeprecationWarning）模式。

---

### 主题 T2 — 单一事实源注册表模式 (A-5)

**一句话描述**：把 v8.4.0 PV-02/03/04 自然涌现的 SSOT registry pattern 提升为 Architecture Rule A-5，并加 CI parity test 拒绝 domain-SSOT 条目混入 `scripts/detect_dead_apis.py::DEFAULT_ALLOWLIST`。

**来源**：

- Overlap matrix 发现：OV6 (**REDUNDANT** — 4 fragmented registries `plugins.yaml` + `runtime-plugins.yaml` + `WHITELIST` + `MemoryCase` + `CommandMapping`)
- Conflict register：C-07 (CRITICAL — RTK SSOT registry vs `DEFAULT_ALLOWLIST`)
- Decomposition analysis：Theme T2
- Reference review findings：F-13（execution-protocol §11 PSC codes 与 shell-proxy registry 关联）

**关键痛点**（verbatim 引用）：

- v8.4.0 retro Pattern §1 原文：*"Single-source-of-truth registry pattern (mirrored from RTK in v8.4.0 PV-02/03/04) — `shell_proxy/registry.py::WHITELIST` + `memory_router/cache.py::MemoryCase` + `shell_proxy/commands.py::CommandMapping` all follow the same shape: one module owns the data structure, all consumers (proxy + hook + tests) delegate to it. Per v8.4.0 retro §4.1 #5: 'consider documenting it as a 5th canonical Architecture Rule in v8.5.0 (provisionally A-5 — single-source-of-truth for whitelist + recipe registries)'. **v9.0.0 should formalize as A-5.**"*
- C-07 原文：*"Both patterns claim to be 'single source of truth' for their domain, but their contracts overlap (a public API may be allowlisted in `DEFAULT_ALLOWLIST` and *also* be the SSOT for a registry). v9.0.0 expansion of either tree will face an integration choice: do we add a new `public_apis.yaml` registry that supersedes both? Or do we let the duplication grow another release?"*

**v9.0.0 期望**：`AGENTS.md` / `.cursor/rules/repo-governance.mdc` 含 Architecture Rule A-5；引入新 whitelist/recipe 表面而无 single-owner 模块时 CI 失败；`scripts/detect_dead_apis.py::DEFAULT_ALLOWLIST` 不接纳 domain-SSOT 条目。

**具体改进项**：

1. **[P1]** 编纂 Architecture Rule A-5 "Single-Source-of-Truth Registry Pattern"
   - 涉及文件：`.cursor/rules/repo-governance.mdc`、`.rules/architecture.mdc`（如该子文件存在）、`AGENTS.md`（regenerate via `src/devolaflow/local/compiler.py`）
   - 估算 LOC：+30
   - 前置依赖：T1（SKILL.md 余量供 cross-reference 行）

2. **[P1]** 新增 CI test `tests/test_no_ghost_features.py::test_registry_single_owner` — 检测引入新 whitelist/recipe 表面而无 single-owner 模块时失败
   - 涉及文件：`tests/test_no_ghost_features.py`
   - 估算 LOC：+50
   - 前置依赖：T2 #1

3. **[P1]** 新增 parity test 拒绝 domain-SSOT 条目从 `DEFAULT_ALLOWLIST`
   - 涉及文件：`scripts/detect_dead_apis.py`、`tests/test_dead_apis.py`
   - 估算 LOC：+25
   - 前置依赖：T2 #1

4. **[P2]** 可选定义 `Registry[T]` typed protocol 给 4 surfaces 一个共享 `name → entry → entry_schema_version → bypass` 契约（`src/devolaflow/registry/`）
   - 涉及文件：`src/devolaflow/registry/__init__.py`（NEW）、相应类型迁移
   - 估算 LOC：+150（含 typed protocol + 4-surface 适配）
   - 前置依赖：T2 #1

**建议 PV 归属**：PV-03 (v8.4.3 PATCH) — A-5 编纂 + CI test；可选 typed protocol 部分留 PV-06 + 之后

**风险**：Rule-count 增长 +1 与 C-08 BLOCKER（≤ 60-rule 上限）冲突；通过同 PV 退役 / 合并 1 条已死规则 mitigate（候选：`devola-flow-rules.mdc` 与 `workflow-rules.mdc` 二者重叠的 P1-P5 块由 T6 选择性切片承担）。

---

### 主题 T3 — 压缩流水线统一

**一句话描述**：把 `compressor.py` + `llm_client.py` + `shell_proxy/commands.py` 三个独立的 L*→L* 压缩入口统一到 `CompressionPipeline` protocol，把 5 个现有 transform 改造为 `CompressionStage` 实例。

**来源**：

- Overlap matrix 发现：OV1 (**LAYERED-WITH-DEAD-WIRE** — 3 distinct entry surfaces) + Finding #1（multi-pass filter chain dead-wire）
- Conflict register：无直接冲突（纯 refactor）
- Decomposition analysis：Theme T3
- Carry-forward：v8.4.0 §3.3 *"`shell_proxy/commands.py::apply_local_recipe` 多通道 filter chain (RTK 按 `filters.toml` 支持 filter composition) 延后到 v8.4.x sustaining"*

**关键痛点**（verbatim 引用）：

- Overlap Finding #1 原文：*"`compressor.py` (predecessor + tool output + directed + abstractive A/B) + `llm_client.py` (provider abstraction) + `shell_proxy/commands.py` (output recipes) all transform L*→L* token streams but live in 3 packages with no shared `CompressionPipeline` interface."*
- v8.4.0 carry-forward §3.3 原文：*"`shell_proxy/commands.py::apply_local_recipe` multi-pass filter chains (RTK supports filter composition per `filters.toml`) deferred to v8.4.x sustaining"* — 确认存在 dead-wire 风险，command_mapping 当前无法 compose with directed_compact

**v9.0.0 期望**：`src/devolaflow/compression_pipeline.py` 提供 `CompressionStage(name, transform, bypass_conditions, telemetry_key)` 与 `CompressionPipeline.run(stages, payload)` orchestrator；5 个 transform 全部改造为 stage；env-flag 作为 stage-level bypass 条件（per Pattern 2 R5 strict）。

**具体改进项**：

1. **[P1]** 定义 `CompressionStage` + `CompressionPipeline` protocol
   - 涉及文件：`src/devolaflow/compression_pipeline.py`（NEW）、`schemas/compression-pipeline.yaml`（NEW）
   - 估算 LOC：+200
   - 前置依赖：T1（任何新 SKILL.md 行需余量）

2. **[P1]** 把 5 个现有 transform 重构为 `CompressionStage` 实例：`truncate_tool_output` / `summarise_predecessor` / `directed_compact` / `apply_local_recipe` / abstractive A+B
   - 涉及文件：`src/devolaflow/compressor.py`、`src/devolaflow/llm_client.py`、`src/devolaflow/shell_proxy/commands.py`
   - 估算 LOC：+150 net（含适配 + 删除重复 dispatch 逻辑）
   - 前置依赖：T3 #1

3. **[P1]** 新增 `tests/test_compression_pipeline.py`（含全 stages bypassed 时 byte-identical pre/post 测试 — R5 strict）
   - 涉及文件：`tests/test_compression_pipeline.py`（NEW）
   - 估算 LOC：+120
   - 前置依赖：T3 #2

4. **[P2]** 可选 `references/compression-pipeline.md`（A3 候选 — S03 §"Additions A3"，DEFER 到 PV-06 视 T3 完成度）
   - 涉及文件：`workflow-system/agent/references/compression-pipeline.md`（NEW）
   - 估算 LOC：+300~400
   - 前置依赖：T3 #1+#2 全部完成

5. **[P2]** 关闭 `apply_local_recipe` multi-pass filter chain（v8.4.0 §3.3 deferred）
   - 涉及文件：`src/devolaflow/shell_proxy/commands.py`、`schemas/command-mapping.yaml`（schema 1→2 添加 `compose: list[str]`）
   - 估算 LOC：+80
   - 前置依赖：T3 #1（用 stage compose 实现）

**建议 PV 归属**：PV-06 (v8.5.1 MINOR) — additive protocol；可选 PV-06 同时打 A3 reference

**风险**：Refactor 触面广；通过 R5 strict（byte-identical pre/post 在所有 stages bypassed 时）mitigate；公开 API 全部保留为 deprecated alias，跨 1 个 minor 周期再删（v8.2.0 PV-03 SessionState 先例）。

---

### 主题 T4 — 生命周期 Hooks 全链路接线 + Plan-Mode 提示侧嵌入

**一句话描述**：把 v7.4.8 P-05 起 100% 覆盖但无 caller 的 `lifecycle.run_hooks("pre_dispatch", ...)` 接到 `feedback.py::generate_round_dispatch` 的 dispatch-emission 点；同时把 v7.8.0 之后已实战验证的 prompt-side governance contract 模式提升为 Soul Rule（S-10 候选）。

**来源**：

- Overlap matrix 发现：OV12 (**LAYERED-WITH-DEAD-WIRE** — `lifecycle/` 100% covered but no caller) + OV14 (**LAYERED-WITH-DEAD-WIRE** — plan-mode 5+ cycle recurrence) + Finding #4 + #5
- Conflict register：C-12 (CRITICAL — Plan-mode enforcement recurrence v3.2/v3.3/v3.6/v3.7/v6.1.5/v7.4.1/v7.5.0/v7.7.0/v7.8.0)
- Decomposition analysis：Theme T4
- Carry-forward：v7 时代 *"Lifecycle hooks 集成到现有 dispatch（在 L0/L1 emission 接入 `run_hooks(pre_dispatch, payload, strict=False)`）— v7.4.8 P-05 — 仅 additive；未接入 dispatch-emission 点 — v7.6.x 本应"*
- Perf trajectory：§3 *"v6.0.3 dead-wire closure was the highest-ROI single intervention of v6.0"*

**关键痛点**（verbatim 引用）：

- `feedback_for_v7.7.0.md` 原文（C-12 来源）：*"v7.5.0 P-05 把 lifecycle hook 落在 Python runtime 层，但 prompt-only L0（Claude Code / Cursor / Codex）从来不进入 `run_hooks(\"pre_dispatch\", ...)` 通路，hook 永远拦不到。"*
- v7.8.0 retro 原文：*"governance contracts must live in the prompt-loaded surface, not just Python runtime."*
- Overlap Finding #5 原文：*"`lifecycle/` package is fully implemented + 100% covered + has 5 hooks + opt-in strict mode, but the dispatch-emission point (L0/L1) does NOT call `run_hooks(pre_dispatch, payload, strict=False)`. Hooks are passive infrastructure waiting for a caller."*

**v9.0.0 期望**：`feedback.py::generate_round_dispatch` 在 return 前调用 `lifecycle.run_hooks("pre_dispatch", dispatch, strict=False)`；`DEFAULT_EVENTS` 5→6（新增 `pre_dispatch`）；`tests/test_dispatch_emission_runs_hooks.py` 加入 `scripts/detect_dead_apis.py` 守卫；S-10 "prompt-side governance contracts must be embedded in `references/plan-mode-enforcement.md`" 进入 Soul Rule。

**具体改进项**：

1. **[P1]** 把 `lifecycle.run_hooks("pre_dispatch", dispatch, strict=False)` 接到 `feedback.py::generate_round_dispatch` 的 dispatch-emission 点
   - 涉及文件：`src/devolaflow/feedback.py`、`src/devolaflow/lifecycle/__init__.py`（`DEFAULT_EVENTS` 5→6）、`src/devolaflow/lifecycle/dispatcher.py`
   - 估算 LOC：+40
   - 前置依赖：无（独立于 T1）

2. **[P1]** 新增 regression test `tests/test_dispatch_emission_runs_hooks.py`
   - 涉及文件：`tests/test_dispatch_emission_runs_hooks.py`（NEW）
   - 估算 LOC：+80
   - 前置依赖：T4 #1

3. **[P1]** 编纂 Soul Rule S-10 "Prompt-Side Governance Contract Embedding"
   - 涉及文件：`.cursor/rules/repo-governance.mdc`、`AGENTS.md` regenerate
   - 估算 LOC：+25
   - 前置依赖：T1（SKILL.md 3-line pointer 给新 reference）

4. **[P1]** `workflow-system/agent/SKILL.md` 增加 3-line pointer 指向 `references/plan-mode-enforcement.md`（T1 已创建）
   - 涉及文件：`workflow-system/agent/SKILL.md`
   - 估算 LOC：+3
   - 前置依赖：T1 #1（PV-01 创建 plan-mode-enforcement.md）

5. **[P1]** 关闭 M-004 `ArchiveManager.propose_merge`（A-4 ADR 已发；merge 代码 DEFERRED）
   - 涉及文件：`src/devolaflow/agent_workspace/archive.py`、`tests/test_agent_workspace.py`
   - 估算 LOC：+200
   - 前置依赖：T4 #1（hooks 可触发 merge gate）

6. **[P1]** 关闭 I-PV07-A REPORT.md auto-trigger
   - 涉及文件：`src/devolaflow/agent_workspace/reporter.py`、`src/devolaflow/lifecycle/__init__.py`
   - 估算 LOC：+60
   - 前置依赖：T4 #1（auto-trigger 通过 lifecycle hook）

**建议 PV 归属**：PV-04 (v8.4.4 PATCH)

**风险**：Dispatch-emission rewiring 触及热路径；通过 `strict=False` permissive default + opt-in STRICT (v7.4.8 pattern preserved) mitigate。S-10 提升 Soul 类别 触及 C-09 "Soul Rules 'immutable' claim vs open-set growth" — 必须在 PV-04 同时编纂"Soul-set-add governance"（T6 提前预热）。

---

### 主题 T5 — Karpathy 原语 Default-On Flip (B3)

**一句话描述**：把 v8.0.0 P-03/P-05/P-07/P-09/P-10 引入但仍 default-OFF 的 5 个 Karpathy 原语（`token_budget_breaker` / `verification_ladder` / `ratchet` / `complexity_detector` / `ac_generator`）翻转为 STRICT/AUDIT profile 默认 on。

**来源**：

- Overlap matrix 发现：OV3 (LAYERED-COHERENT — 5 of 7 v8.0.0 opt-in primitives still default-OFF) + Consolidation Candidate C-I
- Conflict register：C-06 (MAJOR — `@pytest.mark.deferred` 第二 marker class 需求)
- Decomposition analysis：Theme T5
- Reference review findings：F-08（`decomposition-gate.md` §5.5 "Gate Primitive Composition (v8.0.0+)" 是翻转前置）
- Perf trajectory：§"EvoBench Trajectory" worst-case floor 90+

**关键痛点**（verbatim 引用）：

- Carry-forward 原文：*"B3 partial — v8.0.0 7 个 opt-in 原语中 5 个仍默认 OFF（`token_budget_breaker` / `verification_ladder` / `ratchet` / `complexity_detector` / `ac_generator`）— v8.5.0+"*
- Decomposition analysis Theme T5 原文：*"v8.2.0 PV-05 flipped `legibility_enabled` + `cycle_detector_enabled` to default-ON for STRICT; the remaining 5 (...) + `behavioral_guidelines` are still default-OFF; the deferral list keeps growing."*
- v9.0.0 perf trajectory §"EvoBench Trajectory" obs：*"Worst-case composite floor: converges toward 90+ from v7.1.0 onward (84.13 in v7.1.0 was the single worst; v8.4.0's 91.21 on `shell_proxy_disabled` is the cycle worst)."*（即翻转前需要 `_disabled.yaml` 场景在 floor 90+）

**v9.0.0 期望**：5 个原语在 STRICT 与 AUDIT profile 默认 ON；每个原语都有 (a) `_disabled.yaml` EvoBench 场景在 floor 90+，(b) byte-identical pre-PV test 当 env-flag off，(c) STRICT-vs-AUDIT default profile 更新，(d) operator-visible release-note 行。

**具体改进项**：

1. **[P1]** Flip `token_budget_breaker` default-OFF → ON
   - 涉及文件：`workflow-system/agent/context_profiles.yaml`、`src/devolaflow/gate/budget.py`、`benchmarks/devolaflow_context/scenarios/token_budget_disabled.yaml`（NEW if 缺）
   - 估算 LOC：+20
   - 前置依赖：T8（cap+audit W-rule for STATUS-report discipline）

2. **[P1]** Flip `verification_ladder` default-OFF → ON
   - 涉及文件：`workflow-system/agent/context_profiles.yaml`、`src/devolaflow/gate/scorer.py`、`benchmarks/devolaflow_context/scenarios/verification_ladder_disabled.yaml`
   - 估算 LOC：+20
   - 前置依赖：T8

3. **[P1]** Flip `ratchet` default-OFF → ON
   - 涉及文件：`workflow-system/agent/context_profiles.yaml`、`src/devolaflow/gate/ratchet.py`、`benchmarks/devolaflow_context/scenarios/ratchet_disabled.yaml`
   - 估算 LOC：+20
   - 前置依赖：T8

4. **[P1]** Flip `complexity_detector` default-OFF → ON
   - 涉及文件：`workflow-system/agent/context_profiles.yaml`、`src/devolaflow/gate/complexity_detector.py`、`benchmarks/devolaflow_context/scenarios/complexity_detector_disabled.yaml`
   - 估算 LOC：+20
   - 前置依赖：T8 + F-08（decomposition-gate §5.5 7-primitive 表必须先在 PV-02 落）

5. **[P1]** Flip `ac_generator` default-OFF → ON
   - 涉及文件：`workflow-system/agent/context_profiles.yaml`、`src/devolaflow/acceptance.py`、`benchmarks/devolaflow_context/scenarios/ac_generator_disabled.yaml`
   - 估算 LOC：+20
   - 前置依赖：T8

6. **[P2]** （可选）Flip `behavioral_guidelines` default-OFF → ON（v8.0.0 P-08 canonical_order position 14 — 决策项 #11）
   - 涉及文件：`workflow-system/agent/context_profiles.yaml`、`schemas/lean-dispatch.yaml#layout_invariant`、`src/devolaflow/compressor.py`
   - 估算 LOC：+15
   - 前置依赖：F-04（`behavioral-guidelines.md` SF-4 nav 已修复）+ S05 决策

7. **[P1]** CHANGELOG.md "Adoption notes" 列出 5 个翻转的行为变更与 opt-out env-flag 路径
   - 涉及文件：`CHANGELOG.md`
   - 估算 LOC：+10
   - 前置依赖：T5 #1..#5

**建议 PV 归属**：PV-06 (v8.5.1 MINOR) — coordinated default behavior change，与 T3 同 PV

**风险**：行为变化对 AUDIT profile 用户可见；通过 per-primitive env-flag opt-out（per Pattern 2 R5 strict）mitigate；如果 EvoBench 任一 `_disabled.yaml` 场景跌破 floor 90，回退该单个原语并记 STATUS report。

---

### 主题 T6 — 规则体系重新平衡 + Soul Set Freeze

**一句话描述**：把现状 50 条规则的 3 parallel surface（`.cursor/rules/*.mdc` × 10 + `.rules/*.mdc` 5-layer source + 编译产物 `AGENTS.md`）合并到单一 `.rules/` 5-layer source；Soul set 冻结在 9 项；扩展 `task_adaptive_selector` 按 task profile 切片 AGENTS.md slice；rule count 上限 60。

**来源**：

- Overlap matrix 发现：OV8 (**REDUNDANT** — 3 parallel rule surfaces) + Finding #3
- Conflict register：C-08 (**BLOCKER**) + C-09 (MAJOR — Soul Rules "immutable" claim) + C-04 (MAJOR — NineS gate boundary，rule taxonomy 邻接)
- Decomposition analysis：Theme T6
- Perf trajectory：§"Rule Taxonomy Growth" 19 → 24 → 34 → 46 → 50 monotone-additive

**关键痛点**（verbatim 引用）：

- C-08 原文：*"The growth path 19 → 24 → 34 → 46 → 50 across v3.x → v8.3.0 cycles is monotone-additive; without a 'rule retirement' mechanism (or a tier-based selective-load mechanism on AGENTS.md), v9.0.0 will likely cross 55+ rules and start hitting the L0 ~3K context budget."*
- C-09 原文：*"'Immutable invariants' advertised to L0/L1/L2/L3 agents is a stronger contract than the implementation honors (the set is open; only the *content* of existing entries is stable). Agents that have prompts cached against an older Soul-rule set are silently shown a different set after upgrade."*
- Overlap Finding #3 原文：*"3 parallel surfaces (`.cursor/rules/*.mdc` × 10 files, `.rules/*.mdc` 5-layer source, compiled `AGENTS.md`) carry the same Soul/Architecture/Conventions/Workflow/Style content — drift is only prevented by `src/devolaflow/local/compiler.py` + `src/devolaflow/local/drift.py` SHA-256 hash."*

**v9.0.0 期望**：`.rules/` 5-layer 是唯一 canonical 源；`.cursor/rules/devola-flow-rules.mdc` + `.cursor/rules/workflow-rules.mdc` deprecated 为 cross-reference stub；`task_adaptive_selector` 提供 per-task-type AGENTS.md slice；总规则数 ≤ 60，Soul set ≤ 9 freeze（除非 2-cycle telegraph + retro entry）。

**具体改进项**：

1. **[P0]** `.rules/` 提升为唯一 canonical 源；`.cursor/rules/*.mdc` 重新生成纯 compile output
   - 涉及文件：`.rules/*.mdc`、`src/devolaflow/local/compiler.py`、`src/devolaflow/local/drift.py`、`.cursor/rules/devola-flow-rules.mdc`（deprecate stub）、`.cursor/rules/workflow-rules.mdc`（deprecate stub）
   - 估算 LOC：~−400 across `.cursor/rules/`（deprecate stubs only）+ ~+200 in `.rules/` + ~+100 compiler/drift
   - 前置依赖：T1 + T2 + T7（所有 SKILL/registry/cache-layout 余量需求）

2. **[P1]** 扩展 `task_adaptive_selector` 按 task profile 切片 AGENTS.md slice
   - 涉及文件：`src/devolaflow/task_adaptive_selector.py`、`workflow-system/agent/context_profiles.yaml`
   - 估算 LOC：+150
   - 前置依赖：T6 #1

3. **[P1]** 编纂 Soul-set-add governance rule（要求 2-cycle telegraph + retrospective entry）
   - 涉及文件：`.rules/governance.mdc`（NEW or 现有 governance 文件）、`AGENTS.md` regenerate
   - 估算 LOC：+30
   - 前置依赖：T6 #1

4. **[P1]** 设置 60-rule hard cap + retire-or-merge 强制要求
   - 涉及文件：`.rules/`、`tests/test_no_ghost_features.py::test_rule_count_under_cap`（NEW）
   - 估算 LOC：+50
   - 前置依赖：T6 #1

5. **[P1]** 新增 CI test `tests/test_no_ghost_features.py::test_rule_surfaces_compile_only` 当 `.cursor/rules/*.mdc` 被手动编辑（drift-detected via `local/drift.py` SHA-256 hash）时失败
   - 涉及文件：`tests/test_no_ghost_features.py`
   - 估算 LOC：+40
   - 前置依赖：T6 #1

**建议 PV 归属**：PV-07 (v9.0.0 MAJOR) — 破坏性 rule-surface semantics（per-task-type cached-prefix 用户）

**风险**：周期内最高（触及每层 + 破坏 cached-prefix invariant for rule corpus）；通过 2-cycle telegraph mitigate（PV-04 同步发布"将在 PV-07 翻转"telegraph）；如果某 task type 的 selector 切片导致 EvoBench drift，回退到 full AGENTS.md fallback 并记 W-15 (CO-6) Section Relevance 调整。

---

### 主题 T7 — Cache-Layout 治理 v2 (Nest-vs-Append)

**一句话描述**：在 P6 / Rule 6 已有的 append-only 基础上，新增"任何不需要新 cached-prefix 段的字段必须 nest 在已有 canonical_order entry 下"的决策规则；增加针对全部历史 baseline 的多基线 byte-identicality regression test。

**来源**：

- Overlap matrix 发现：OV7 (LAYERED-COHERENT — 5 transitions / 0 regression — gold standard) + Finding #7
- Conflict register：C-01 (**BLOCKER**)
- Decomposition analysis：Theme T7
- Reference review findings：F-02（`context-isolation.md` 12 keys vs 16 keys 漂移；T7 编纂前置）
- Perf trajectory：§2 *"Cache-layout invariant additivity proven across 5 successive schema generations (12 → 13 → 14 → 15 → 16) — v7.0.0 + v7.3.0 byte-baselines STILL PASS at v8.4.0"*

**关键痛点**（verbatim 引用）：

- C-01 原文：*"The append-only rule is structurally satisfiable (each new field appends after `gate`), but the *surface budget* is not unbounded — every appended key re-enters the L0/L1/L2/L3 dispatch payload and competes for the same per-layer token budget (P2: L0 ~3K / L1 ~5K / L2 ~4K / L3 ~8K). Five successive schema generations have shipped under the additive rule, yet v8.4.0 explicitly held additions at 0 to leave breathing room."*
- v8.3.0 retro §4.2 原文：*"the pattern is mature: append-only, never reorder, document at `schemas/lean-dispatch.yaml#layout_invariant.canonical_order`. v8.4.0+ should consider whether to formalize this as an enforcement rule (e.g., `assert_dispatch_layout_byte_identical_for_positions(payload, 1, 15)` on every dispatch)."*

**v9.0.0 期望**：A-2 扩展含 nest-vs-append rule；`tests/test_layout_invariant_multi_baseline.py` 覆盖 v7.0.0 + v7.3.0 + v8.0.0 P-08 + v8.0.0 P-10 + v8.3.0 PV-05 + v8.4.0 全部历史 baseline 的 positions 1..N byte-identicality；`.local/research/adr/v9-ADR-001-cache-layout-governance-v2.md` 落档。

**具体改进项**：

1. **[P0]** 编纂 A-2 扩展 "Nest-vs-Append Decision Rule"
   - 涉及文件：`.cursor/rules/repo-governance.mdc`、`.rules/architecture.mdc`、`AGENTS.md` regenerate、`.local/research/adr/v9-ADR-001-cache-layout-governance-v2.md`（NEW ADR）
   - 估算 LOC：+45
   - 前置依赖：F-02（PV-02 必须先修 `context-isolation.md` 12-keys → 16-keys）

2. **[P0]** 新增 multi-baseline byte-identicality regression test
   - 涉及文件：`tests/test_layout_invariant_multi_baseline.py`（NEW）、`benchmarks/devolaflow_context/baselines/layout_invariant_v8.0.0.yaml`（NEW golden — currently only v7.0.0 + v7.3.0 exist per perf_trajectory §"Benchmark Baseline File Trajectory"）、`benchmarks/devolaflow_context/baselines/layout_invariant_v8.3.0.yaml`（NEW golden）、`benchmarks/devolaflow_context/baselines/layout_invariant_v8.4.0.yaml`（NEW golden）
   - 估算 LOC：+200（test +90，3 个新 golden 各 +50~70 行）
   - 前置依赖：T7 #1

3. **[P1]** 把 `assert_dispatch_layout` 升级支持 nest-vs-append 检查（detect 新 top-level key 是否本应 nest）
   - 涉及文件：`src/devolaflow/compressor.py::assert_dispatch_layout`、`schemas/lean-dispatch.yaml#layout_invariant`
   - 估算 LOC：+60
   - 前置依赖：T7 #1

4. **[P2]** 把 pattern 扩到 `runtime-plugins.yaml` entries + `references/*.md` listing in SKILL.md（关闭 R7 line-anchor coupling debt 的最后一环）
   - 涉及文件：`workflow-system/agent/runtime-plugins.yaml`、`workflow-system/agent/SKILL.md` Tier-2 nav table
   - 估算 LOC：+30
   - 前置依赖：T1 + T7 #1

**建议 PV 归属**：PV-02 (v8.4.2 PATCH) — 在 PATCH-only 窗口内闭合 BLOCKER C-01

**风险**：低 — 扩展已成熟模式；主要风险是忘记把 v8.4.0 baseline 注册成第 5 个 golden（perf_trajectory §"Benchmark Baseline File Trajectory" 显示当前只有 2 个 `layout_invariant_*.yaml` golden）。

---

### 主题 T8 — NineS Hygiene 包 A1-A4 收尾 + 审计 W-Rules

**一句话描述**：闭合 v8.0.0 起 carry-forward 的 A1..A4 NineS hygiene bundle（cov 超时 / agent_overhead 46179 warning / NineS index rebuild / golden_test_set refresh），并在同 PV 内编纂 5 条治理类 W-rules（W-16..W-20 + `@pytest.mark.deferred` 第二 marker class）。

**来源**：

- Overlap matrix 发现：OV10 (LAYERED-COHERENT — 6-measurement byte-stable 0.9050) + OV11 (Ghost-feature audit cadence) + Consolidation Candidate C-J
- Conflict register：C-03 (CRITICAL) + C-04 (MAJOR) + C-05 (CRITICAL) + C-06 (MAJOR) + C-10 (MAJOR) + C-11 (MAJOR) + C-13 (CRITICAL)
- Decomposition analysis：Theme T8
- Perf trajectory：§1 (tool-config-only breakthrough) + §4 (NineS byte-stability) + §5 (cap-discipline inflection 4.7× → 1.06×)

**关键痛点**（verbatim 引用）：

- v8.4.0 retro §3.2 原文（A1..A4 carry-forward）：*"A1 NineS code_coverage timeout + A2 agent_overhead 46179 warning + A3 NineS index rebuild + A4 golden_test_set refresh → v8.4.x sustaining (bundle)"*
- v6.1.1 retro §4 #1 原文（T8 顶层 lesson）：*"Every 'score plateau' deserves a tool-config audit before assuming code-quality work."*
- C-13 原文：*"v9.0.0 SI-1 has no rule that prevents a future cycle from rebasing AWAY a real regression under cover of 'structural-noise rebase'."*

**v9.0.0 期望**：A1..A4 全部 closed；新增 `references/env-flags.md`（A2 候选 — S03 §"Additions A2"）；W-16/W-17/W-18/W-19/W-20 5 条 W-rule 编纂；`@pytest.mark.deferred(strict=True, reason=...)` marker class 实现；NineS composite 维持 byte-stable ≥ 6 measurements。

**具体改进项**：

1. **[P1]** 闭合 A1（NineS cov timeout）：扩展 `pytest --cov` budget 或 drop nested cov collection
   - 涉及文件：`pyproject.toml [tool.coverage]`、`src/devolaflow/nines/scorer.py`、`nines.toml`
   - 估算 LOC：+15
   - 前置依赖：无

2. **[P1]** 闭合 A2（`agent_overhead` 46179 → ≤ 40000）通过 T1 SKILL.md 压缩 spillover + ref consolidation
   - 涉及文件：`workflow-system/agent/SKILL.md`（T1 同 PV 的 spillover）、`tests/test_agent_context_overhead.py`（NEW — 编纂 ≤ 40K ceiling）
   - 估算 LOC：+50
   - 前置依赖：T1（SKILL.md 必须先压缩）

3. **[P1]** 闭合 A3（NineS index rebuild against `data/golden_test_set/` at HEAD）
   - 涉及文件：`data/golden_test_set/`（refresh）、`src/devolaflow/nines/researcher.py`、`Makefile`（new `make nines-index-rebuild`）
   - 估算 LOC：+30 (Makefile + script) + ~+200 net golden refresh
   - 前置依赖：A1

4. **[P1]** 闭合 A4（golden_test_set refresh per v6.1.1 tool-config-audit lesson）
   - 涉及文件：`data/golden_test_set/`、`nines.toml`
   - 估算 LOC：±0 net（refresh 现有 fixture）
   - 前置依赖：A3

5. **[P1]** 新增 `references/env-flags.md`（A2 候选；canonical 12th SF-4 reference — 关闭 C-03）
   - 涉及文件：`workflow-system/agent/references/env-flags.md`（NEW）、`workflow-system/agent/SKILL.md`（+1 Tier-2 row）、`tests/test_no_ghost_features.py::_SF4_REFERENCE_SET`（11→12）
   - 估算 LOC：+250 (ref) + +1 (SKILL row) + +5 (test)
   - 前置依赖：T1（SKILL.md 余量）+ S05 决策 #2

6. **[P1]** 编纂 W-16 wholesale-baseline-regen rule with two-view archived diff（关闭 C-13）
   - 涉及文件：`.rules/workflow.mdc`（W-16）、`.cursor/rules/repo-governance.mdc`、`AGENTS.md` regenerate
   - 估算 LOC：+30
   - 前置依赖：T1（已实施 wholesale regen）

7. **[P1]** 编纂 W-17 per-PV test cap + mid-cycle STATUS audits rule（关闭 C-11）
   - 涉及文件：`.rules/workflow.mdc`、`AGENTS.md`
   - 估算 LOC：+25
   - 前置依赖：无

8. **[P2]** 编纂 W-18 ghost-audit refresh precondition rule（关闭 C-05）
   - 涉及文件：`.rules/workflow.mdc`、`AGENTS.md`、`tests/test_no_ghost_features.py::test_ghost_audit_refresh_present`（NEW lint）
   - 估算 LOC：+45
   - 前置依赖：无

9. **[P2]** 编纂 W-19 research artifact archive rule（`docs/cycle-archive/v<X>.<Y>.<Z>/` at release time — 关闭 C-10）
   - 涉及文件：`.rules/workflow.mdc`、`AGENTS.md`、`docs/cycle-archive/`（NEW tree）、`scripts/archive_research_artifacts.py`（NEW）
   - 估算 LOC：+150
   - 前置依赖：S05 决策 #8

10. **[P2]** 编纂 W-20 env-flag reuse rule（关闭 C-03 与 #5 的 reference 形成内外双层规约）
    - 涉及文件：`.rules/workflow.mdc`、`AGENTS.md`
    - 估算 LOC：+25
    - 前置依赖：T8 #5

11. **[P2]** 实现 `@pytest.mark.deferred(strict=True, reason=...)` 第二 marker class（关闭 C-06）
    - 涉及文件：`tests/conftest.py`、`tests/test_no_ghost_features.py`、`pyproject.toml [tool.pytest]`
    - 估算 LOC：+60
    - 前置依赖：S05 决策 #9

12. **[P2]** 拆分 SI-3 评估区块为 "Quality Gate (EvoBench-anchored)" + "Research Snapshot (NineS-anchored, advisory)"（关闭 C-04）
    - 涉及文件：`.local/research/v9.0.0_evaluation.md`（template 调整）、`scripts/generate_si3_evaluation.py`（NEW or 调整）
    - 估算 LOC：+40
    - 前置依赖：无

**建议 PV 归属**：PV-05 (v8.5.0 MINOR) — 第一个 MINOR bump，bundle 4 A-items + 6 governance W-items + deferral marker class

**风险**：Rule-count 增长 (+5 W-rules: 50 + 5 = 55) 接近 C-08 60-rule cap；通过 T6 selectivity 机制同周期 ship mitigate；A2 ≤ 40000 阈值（S05 决策 #12）需 benchmark sweep 5 task types 实证。

---

## Reference 层面的改进（来自 S03 review）

从 `v9.0.0_reference_review.md` 中的 15 个 F-NN 发现里提炼，按优先级排列。Total LOC delta：**+535 across 10 refs**（per S03 §"SF-1 Budget Impact Assessment"），无 SPLIT，无 tier 升级；2 个 NEW canonical refs 候选（`plan-mode-enforcement.md` + `env-flags.md`）。

| ID | 优先级 | 文件 | 一句话 | 修复 LOC | 主要 PV | 主题归属 |
|---|---|---|---|---|---|---|
| F-01 | **P0** | `references/shell-proxy.md` | S-2 绝对路径违规 — 行 654 *"Cycle plan: `/root/.cursor/plans/v8.4.0_rtk_+_memory_router_e4ec076d.plan.md`"* 必须删 / 重定向到 `.local/research/cycle_plans/` | 1-line edit | PV-01 | T1 (must-fix bundle) |
| F-02 | **P0** | `references/context-isolation.md` | §10 stale "12 keys" vs 实际 `schemas/lean-dispatch.yaml#layout_invariant.version: 5` 的 16 keys（缺 `repos`/`behavioral_guidelines`/`acceptance_criteria_v2`/`change_context`） | ~5 LOC | PV-02 | T7 (前置) |
| F-03 | **P0** | `references/message-schemas.md` | 仅 documents verbose legacy schema；live `schemas/lean-dispatch.yaml:80-97` 的 lean form 是 canonical（C-2 mandate）；当前覆盖度约 25% | +71 LOC（rewrite §2/§3 + 加 §7 layout_invariant + §8 compression rules + §9 result-status） | PV-02 | T7 + T1 |
| F-04 | **P0** | `references/behavioral-guidelines.md` + `workflow-system/agent/SKILL.md` | SKILL.md Tier-2 nav 漏列（10 vs 应 11；C-7 valid-link 违规） | +1 SKILL.md row + 45 LOC body | PV-01 (SKILL row) + PV-02 (body) | T1 + T5 + T8 |
| F-05 | **P0** | `references/shell-proxy.md` | §5.3 `MemoryCase` dataclass 字段 fab：`dispatch_template`/`expected_savings_pp` 不在 `src/devolaflow/memory_router/cache.py` / `schemas/memory-case.yaml`；required `summary` 缺；`slots=True` decorator 参数 fab | ±0 net (rewrite) | PV-01 | T2 (must-fix bundle) |
| F-06 | **P0** | `references/shell-proxy.md` | §6.3 recipe 例子用 `note:` 而 schema 要 `replacement:`（`schemas/command-mapping.yaml:170-198`）— operator copy-paste 被 schema validation 拒绝 | 4-line edit | PV-01 | T2 (must-fix bundle) |
| F-14 | **P0** | `references/shell-proxy.md` | §5.2 activation 例子用 `case.dispatch_template` / `case.expected_savings_pp` 触发 AttributeError（cascade from F-05） | 6-line edit | PV-01 | T2 (must-fix bundle) |
| F-07 | **P1** | `references/meta-framework.md` | Alias Mapping Table 仅覆盖 12/22 templates（漏 10：`change-driven`/`entropy-cleanup`/`nines-assisted`/`product-verification`/`self-update`/`skill-optimization`/`onboarding`/`dependency-setup`/`demo-showcase`/`research-design-review-refine`） | +60-80 LOC | PV-02 | T6 reserve |
| F-08 | **P1** | `references/decomposition-gate.md` | 缺 7 个 v8.0.0 gate primitives（`TokenBudgetBreaker` / `evaluate_ladder` / `cycle_detector` / `ratchet` / `complexity_detector` / `ac_generator` / `legibility/scorer.py`）— T5 default-on flip 的前置 | +50 LOC（NEW §5.5） | PV-02 | T5 (前置) |
| F-09 | **P1** | 全部 11 refs | `last_updated` frontmatter stale at "2026-04-04"（除 `behavioral-guidelines.md` 是 "2026-04-21"，亦 pre-v8.4.0）；无 CI 守护 | 11 行 frontmatter bump + ~80 LOC NEW `tests/test_reference_frontmatter_freshness.py` | PV-02 + PV-05 (CI 与 T8 W-19 bundle) | T8 |
| F-10 | **P1** | `references/team-roles.md` | §7 Team Participation Matrix 仅覆盖 11/22 templates（漏 11：`feature-enhancement`/`demo-showcase`/`performance-optimization`/`dependency-setup`/`onboarding`/`skill-optimization`/`nines-assisted`/`self-update`/`repo-init`/`entropy-cleanup`/`change-driven`） | +12 LOC | PV-02 | T6 reserve |
| F-11 | **P1** | `references/execution-protocol.md` | 缺 v8.3 `change-driven` workflow envelope lifecycle（22 个模板的最后一个）；缺 `apply ↔ verify` `max_rounds=5` + S-9 envelope semantics | +25-30 LOC（NEW §12） | PV-02 | T4 cross-link |
| F-12 | **P1** | `references/execution-protocol.md` | 缺 v8.3.3 `memory_router` fast-path lookup（93% planning-context reduction surface 完全没文档化） | +35-40 LOC（NEW §10） | PV-02 | T2 + T8 |
| F-13 | **P1** | `references/execution-protocol.md` | 缺 v8.3.2 `pre_shell_call` 5th canonical lifecycle hook + 4 PSC 违规码（PSC001..PSC004） | +35-40 LOC（NEW §11） | PV-02 | T2 + T4 |
| F-15 | **P1** | `references/context-isolation.md` | §15 numbering collision — 行 502 与行 535 都以 `## 15.` 起；anchor uniqueness 违规 | 1-line renumber | PV-02 | T7 reserve |

**Total LOC delta**：+535 across 10 refs（per S03 §"SF-1 Budget Impact Assessment"）；peak file post-cycle = `agent-workspace.md` 721/1000 (72.1%)；**无文件接近 SF-1 Large tier 1000 行天花板**。

**新增 reference**：

- **A1 — `references/plan-mode-enforcement.md`** — Theme T1 + T4 + T6 服务；预期 350-450 行；PV-01 创建
- **A2 — `references/env-flags.md`** — Theme T8 服务；预期 200-280 行；PV-05 创建
- **A3 (DEFER) — `references/compression-pipeline.md`** — Theme T3 服务；预期 300-400 行；PV-06 if T3 lands

如果 A1+A2 都 land，SF-4 valid list 11 → 13；需 S05 决策 #2 + #3 确认是否 2-cycle telegraph（per v8.5.0 → v9.0.0 governance pattern）。

---

## Carry-Forward 从前代继承的 Deferrals

从 `CHANGELOG.md` v8.4.0 "Adoption notes" + `v9.0.0_version_history_zh.md` §"跨时代顺延事项汇总" verbatim 抽取（仅列出 v8.0.0+ 起源、v9.0.0 周期内可处置的 12 项）：

| ID | 起源版本 | 描述（verbatim） | v8.4.0 状态 | v9.0.0 处置建议 | 主题归属 |
|---|---|---|---|---|---|
| **M-007** | v8.3.0 PV-09 | slash commands `/devola:propose`, `/devola:apply`, `/devola:archive` analogs of OpenSpec `/opsx:*` | DEFERRED (Q-3 RESOLVED) | 决策项 #6 — 在 v9.0.0 关闭或明确 defer 到 v9.1.0 | T6 候选 + 新主题 T9 候选（slash-cmd surface） |
| **M-004** | v8.3.0 PV-02 (A-4 ADR) | `ArchiveManager.propose_merge` 完整闭合（A-4 ADR 已发；merge 代码 DEFERRED） | partial | v9.0.0 PV-04 关闭（使能 A-4 ADR） | T4 #5 |
| **A1** | v8.0.0 P-08 / v8.1.0 | NineS `code_coverage: 0.0` 测量超时（cov budget 54s） | DEFERRED bundle (v8.4.x sustaining) | PV-05 关闭 | T8 #1 |
| **A2** | v8.0.0 P-08 / v8.1.0 | `agent_overhead 46179 warning` (NineS workflow-scan `AI-24c4f48d-0002`) | DEFERRED bundle | PV-05 关闭（≤ 40K via T1 spillover） | T8 #2 |
| **A3** | v8.0.0 P-08 / v8.1.0 | NineS index rebuild against `data/golden_test_set/` at HEAD | DEFERRED bundle | PV-05 关闭 | T8 #3 |
| **A4** | v8.0.0 P-08 / v8.1.0 | golden_test_set refresh per v6.1.1 tool-config-audit lesson | DEFERRED bundle | PV-05 关闭 | T8 #4 |
| **B3 partial** | v8.0.0 P-03/P-05/P-06/P-09/P-10 | v8.0.0 7 个 opt-in 原语中 5 个仍默认 OFF（`token_budget_breaker` / `verification_ladder` / `ratchet` / `complexity_detector` / `ac_generator`） | partial (v8.2.0 PV-05 翻 2 个) | PV-06 翻全部 5（决策项 #5 — bundle 与否） | T5 #1..#5 |
| **R7** | v8.0.0 P-08 | line-anchored section registry 完整迁移 | partial (v8.2.0 PV-05 + v8.4.0 baseline rebase 部分吸收) | PV-01 完整迁移 | T1 #2 |
| **I-PV07-A** | v8.3.0 PV-07 | REPORT.md auto-trigger 自动从现有工作流触发 | DEFERRED | PV-04 接入 | T4 #6 |
| **L-001..L-004** | v8.2.0 PV-02 | legibility scoring 扩展（per-tool command files for 25+ assistants / telemetry opt-out / web demo refresh / legacy archive migration tool） | DEFERRED | 分布到 T1-T8 各 PV；细项 P2/P3 | 多主题 |
| **`runtime-plugins.yaml::expected_sha256`** | v8.4.0 PV-01 | RTK SHA256SUMS 采用（pinned `null`） | DEFERRED 到发布后 | v9.x sustaining | T2 (registry) |
| **`command_mapping_savings` EvoBench 场景** | v8.4.0 PV-04 | 显示可测 token 节省的 EvoBench scenario | DEFERRED | v9.x sustaining (decision #8 candidate for archive) | T8 reserve |
| **Memory router cache invalidation 超 TTL** | v8.4.0 PV-03 | 按 A-4 source-of-truth ADR 对 active change 的引用计数 | DEFERRED | v9.x sustaining (跨 minor 改造) | T2 + T4 reserve |
| **`apply_local_recipe` 多 pass filter chain** | v8.4.0 PV-04 | RTK 按 `filters.toml` 支持 filter composition | DEFERRED | PV-06 if T3 #5 land | T3 #5 |

**对仍 OPEN 的 v6.x/v7.x 顺延事项处置**（perf trajectory 与 history 提到但 v9.0.0 周期内不处理的）：

- Tier-2 企业 adapter 批次（JetBrains/Amazon Q/Gemini/Augment/Trae）— P3，建议 v9.x sustaining bundle
- N3 source-freshness updater — P3，与 A3 NineS index rebuild 关联（T8 同周期 ship 后再评估）
- V6-04 / V6-05 — P3，profile 数量阈值未到（`50+ profiles`）
- G-I3 / G-I4 / Composer mode 接入 / Plan-mode L2 propagation / OEM runtime-side 压缩 / `init_interview.py` 8 阶段 — 全部 v7.x P3，无 v9.0.0 主题 owner

---

## v9.0.0 建议的 PV-to-Theme 映射

综合 `v9.0.0_decomposition_analysis.md` §"Theme-to-PV Mapping Sketch" + `v9.0.0_reference_review.md` §"Action Plan by v9.0.0 PV"：

| PV # | 版本 Slice | 类型 | 主要主题 | 主要输出 | 预估 LOC | 关键 acceptance |
|---|---|---|---|---|---|---|
| **PV-01** | v8.4.1 | PATCH | T1 + R7 完整迁移 + Reference must-fix | SKILL.md ≤ 480 lines；新 `references/plan-mode-enforcement.md` (~400 lines)；4 条 `shell-proxy.md` must-fix（F-01/F-05/F-06/F-14）；SKILL.md +1 row F-04；`tests/test_no_ghost_features.py::test_reference_skill_md_tier2_parity`（NEW）；wholesale baseline regen + two-view diff 归档 | SKILL net −19 + ~+400 ref + ~−50 across shell-proxy + ~+150 baseline diff | SF-4 11-entry 与 SKILL Tier-2 11-row 对齐；EvoBench 0 regression > 5pp post-rebase |
| **PV-02** | v8.4.2 | PATCH | Reference Freshness Cascade (T7 + T4 + T5 prep) | 9 refs REFRESH/EXTEND（F-02 / F-03 / F-04 body / F-07 / F-08 / F-10 / F-11 / F-12 / F-13 / F-15）+ 11 frontmatter bumps（F-09）+ 2 新 CI test（`tests/test_reference_frontmatter_freshness.py` + `tests/test_reference_api_alignment.py`） | +535 across 10 refs + ~+150 CI tests | 0 BLOCKER findings open；SI-4 benchmark composite ≥ 90 floor 保持 |
| **PV-03** | v8.4.3 | PATCH | T2 (A-5 SSOT registry rule + parity test) | A-5 编纂 + `tests/test_no_ghost_features.py::test_registry_single_owner` + `DEFAULT_ALLOWLIST` parity test + `references/shell-proxy.md` §11 "Registry SSOT" 加 ~10 LOC | ~+105 + ~+10 ref | 引入新 whitelist/recipe 表面而无 single-owner 模块时 CI 失败 |
| **PV-04** | v8.4.4 | PATCH | T4 (lifecycle wiring + plan-mode S-10 + M-004 + I-PV07-A) | `feedback.py::generate_round_dispatch` 接入 `lifecycle.run_hooks("pre_dispatch", ...)`；`DEFAULT_EVENTS` 5→6；S-10 编纂；`ArchiveManager.propose_merge` 实现；REPORT.md auto-trigger | ~+400 (含 M-004 + I-PV07-A) | `tests/test_dispatch_emission_runs_hooks.py` PASS；S-10 在 AGENTS.md 出现 |
| **PV-05** | v8.5.0 | **MINOR** | T8 (NineS hygiene A1-A4 + W-16..W-20 + deferral marker) | A1..A4 closed；新 `references/env-flags.md` (~250 LOC)；W-16/W-17/W-18/W-19/W-20 5 W-rules；`@pytest.mark.deferred` marker class；SI-3 区块拆分；docs/cycle-archive/ tree | ~+800 (env-flags + golden refresh + W-rules + archive infra) | NineS composite ≥ 0.91；agent_overhead ≤ 40K |
| **PV-06** | v8.5.1 | **MINOR** | T3 (CompressionPipeline) + T5 (B3 default-on flip) + 可选 A3 reference | `CompressionStage` + `CompressionPipeline` protocol；5 stages refactor；5 Karpathy 原语 default-ON；可选新 `references/compression-pipeline.md` | ~+550 (含 T5 翻转 + T3 refactor + 可选 A3) | EvoBench 0 regression > 5pp；5 个 `_disabled.yaml` ≥ floor 90 |
| **PV-07** | v9.0.0 | **MAJOR** | T6 (rule taxonomy 重平衡 + Soul-set freeze + selectivity) | `.rules/` 升 single canonical；`.cursor/rules/devola-flow-rules.mdc` + `workflow-rules.mdc` deprecate stub；per-task-type AGENTS.md slice via `task_adaptive_selector`；60-rule cap；Soul-set-add governance rule | ~+450 (含 selector 扩展 + 新 CI tests) | rule count ≤ 60；EvoBench 0 regression > 5pp post-slice；breaking change 在 release notes 注明 |
| **PV-08** | v9.0.0 sustaining | (optional PATCH) | repo-modes.md sustaining + Tier-2 enterprise adapter batch reserve | `references/repo-modes.md` Codeberg variant + +verify/+monitor 行 + §7 Plugin/Tool interaction；optional Tier-2 adapter batch (JetBrains/Amazon Q/Gemini/Augment/Trae) — 决策项 #11 候选 | ~+24 (refs) + ~+150 (adapters if land) | repo-modes 同步 v9.0.0 mode 集合 |

**Dependency notes**：

- PV-01 必须最先（SKILL.md 余量是其他 PV 的前置）
- PV-02 + PV-03 + PV-04 互独立（除 `.cursor/rules/repo-governance.mdc` 共享 — 可通过 `change-driven` workflow 串行/并行）
- PV-05 需 PV-01 + PV-04 完成（audit-cadence rules 引用 plan-mode-enforcement.md + lifecycle wiring）
- PV-06 需 PV-05（cap-discipline + W-17 mid-cycle audits 是 B3 flip 安全前置）
- PV-07 需 ALL prior PVs（rule selectivity 需要每个其他表面稳定）

**总计**：8 个 PV（PV-08 可选）跨 4 个 PATCH + 2 个 MINOR + 1 个 MAJOR + 可选 1 PATCH。建议总 LOC delta 估算：~+3500 across SKILL/refs/src/tests + 净 SKILL.md −19 + 净 `.cursor/rules/` −400（deprecate stub）。

---

## 人类决策项（S05 必须回答）

整合 `v9.0.0_decomposition_analysis.md` §"Open Decisions for v9.0.0 SI-1" 12 项 + `v9.0.0_reference_review.md` §"Open Questions for S05" 5 项 + 本 S04 新增 1 项，去重后保留 8 项最关键：

### 决策 #1 — v9.0.0 是否必须 MAJOR？

- **背景**：T6 (rule-taxonomy rebalancing) 是唯一 MAJOR-justified 主题（per `decomposition_analysis` Open Decision #1）；如果 T6 deferred 到 v10.0.0，v9.0.0 退化为 MINOR rollup of T1–T5 + T7 + T8。
- **选项**：
  - (a) MAJOR — T6 进 PV-07，破坏 cached-prefix 用户的 rule corpus（需 2-cycle telegraph）
  - (b) MINOR — T6 defer 到 v10.0.0，v9.0.0 仅含 T1-T5 + T7 + T8（5 个 PV）
  - (c) Hybrid — 在 v9.0.0 PV-07 仅 ship "selectivity 切片"（additive；不 deprecate `.cursor/rules/devola-flow-rules.mdc`），把 deprecate 留到 v10.0.0
- **建议**：(c) Hybrid — 既闭合 BLOCKER C-08 又避免 cached-prefix 破坏；如果 EvoBench post-slice 0 drift 则在 v10.0.0 完成 deprecate

### 决策 #2 — v9.0.0 是否新增 12/13 个 canonical reference？

- **背景**：本清单提到 2 个 NEW ref 候选（`plan-mode-enforcement.md` for PV-01 / `env-flags.md` for PV-05）+ 1 deferrable（`compression-pipeline.md` PV-06 if T3 ships）。SF-4 valid list 当前 11；如果两个都 land 则 13。AGENTS.md C-7 是否要求 2-cycle telegraph？（per S03 §"Open Question #1"）
- **选项**：
  - (a) 都 land（11 → 13），声明 "new SF-4 entries 是 additive 不是 breaking"
  - (b) 仅 land plan-mode-enforcement.md（11 → 12），env-flags.md 留 v9.1.0
  - (c) 都 defer 到 v9.1.0
- **建议**：(a) — 都 land，per C-7 的 "valid references 列表" 是 additive；遵循 SF-3 frontmatter migration 的 1-cycle telegraph

### 决策 #3 — 规则体系是否新增第 6 类（"Enterprise" 或 "Plan-Mode"）？

- **背景**：T6 selectivity 引入 per-task-type scoping 时，是否同时新增第 6 类规则空间，或保持 5 类（per `decomposition_analysis` Open Decision #3）；影响 cached-prefix 用户的向后兼容
- **选项**：
  - (a) 保持 5 类 — Soul/Architecture/Conventions/Workflow/Style 不变，只加 selector
  - (b) 新增第 6 类 "Plan-Mode" — 显式区分 prompt-side vs runtime governance
  - (c) 重命名 5 类对齐 selector 维度
- **建议**：(a) — 最小破坏面；新增第 6 类与决策 #1 (c) 的 "additive only" 立场一致

### 决策 #4 — SF-1 SKILL.md tier 是否升级（Default 500 → Large 1000）？

- **背景**：v8.4.0 retro §4.2 #1 列出 (a) 升 Large tier (b) 跑专 compression PV (c) batched-into-single-rollup commit 三条路径；本清单选 (b) PV-01 + R7 完整迁移；S05 须确认是否真的不升 tier（per `decomposition_analysis` Open Decision #2）
- **选项**：
  - (a) 不升级 — 走 PV-01 压缩，SKILL.md ≤ 480；T1 acceptance 包含 ≥ 20-line 余量
  - (b) 升 Large tier — SKILL.md 上限到 1000，PV-01 不需要压缩，但失去"prompt-loaded surface 紧凑"承诺
  - (c) 兜底升 tier — PV-01 压缩 try first，如果失败 fallback 升 Large
- **建议**：(a) — 不升级；保持 prompt-loaded surface 严苛纪律；PV-01 acceptance 失败则触发 (c) bailout

### 决策 #5 — B3 5 原语翻转是否 bundle 在一个 PV？

- **背景**：B3 partial 含 5 个原语（`token_budget_breaker` / `verification_ladder` / `ratchet` / `complexity_detector` / `ac_generator`）；要么一个 PV 全翻，要么拆 2-3 个 PV
- **选项**：
  - (a) Bundle 全 5 在 PV-06 — coordinated default behavior change，单 release-note 行
  - (b) 拆 2 + 3：PV-06 翻 (`token_budget_breaker` + `ratchet`)；PV-06.5 / 下个 minor 翻其余 3
  - (c) 每 PV 翻 1 个 — 5 个 PV 散开 — release-note 长度可控但 tracking 难
- **建议**：(a) — bundle，per S05 决策 #1 倾向 minimize PV count；如果某个原语 EvoBench `_disabled.yaml` 跌破 floor 90，回退该单个原语并记 STATUS report

### 决策 #6 — M-007 slash commands 是 v9.0.0 范围内还是 defer 到 v9.1.0？

- **背景**：M-007 自 v8.3.0 起 DEFERRED；CHANGELOG 写 "v8.4.x+ or v8.5.0"；本清单未为其分配主题（候选新 T9）
- **选项**：
  - (a) v9.0.0 PV-08 sustaining 关闭（new T9 主题）
  - (b) v9.1.0 关闭 — 给 v9.0.0 留 release scope
  - (c) v9.0.0 PV-04 与 T4 lifecycle 同 PV ship（slash cmd 通过 lifecycle hook 触发）
- **建议**：(b) v9.1.0 — slash cmd 是 surface 增量，不阻塞 v9.0.0 BLOCKER；可在 v9.0.0 retrospective 明确 "M-007 → v9.1.0 PV-01"

### 决策 #7 — 4 条 `shell-proxy.md` must-fix（F-01 / F-05 / F-06 / F-14）是否走 v8.4.1 hotfix？

- **背景**：F-01 是 S-2 Soul Rule 违规在 production；F-05/F-14 cause 运行时 AttributeError on copy-paste（per S03 §"Open Question #4"）；strict 解读 S-1 trivial waiver（single file < 20 lines）允许 pre-v8.5.0 emergency patch
- **选项**：
  - (a) 走 v8.4.1 hotfix — 立即修；v9.0.0 PV-01 acceptance 减少 4 条 must-fix
  - (b) 等 v9.0.0 PV-01 — 本清单当前的方案；同 PV bundle T1 + 4 must-fix
  - (c) v8.4.1 hotfix 仅 F-01（Soul Rule 违规），F-05/F-14 等 PV-01
- **建议**：(b) — 维持 SI-1 → SI-3 流程纪律；v8.5.0 周期窗口 ≤ 7 天，hotfix 边际收益小

### 决策 #8 — NineS Hygiene 包 A1-A4 是单独 PV 还是分散？

- **背景**：本清单当前把 A1-A4 + 5 W-rules + deferral marker class bundle 在 PV-05；S03 §"Open Question #2" 提出可拆 PV-02 (CI lint) + PV-05 (W-rule 编纂)
- **选项**：
  - (a) 全部 bundle 在 PV-05 — 当前方案
  - (b) 拆：A1..A4 进 PV-04 (与 T4 同周期；hygiene 早些 land 改善 NineS measurements)；W-rules 留 PV-05
  - (c) 拆 3 PV — A1+A2 (PV-03 NineS-side hygiene) / A3+A4 (PV-04 golden refresh) / W-rules (PV-05)
- **建议**：(a) bundle — minimize PV-count；NineS byte-stable ≥ 6 measurements 是 SI-3 G3 acceptance 前置，bundle 可一次性验证

---

## 方法论备注

- 全部 verbatim 引用按 C-3 保留英文原文，前后用 `"..."` 标注；技术标识符（API 名、文件路径、commit 哈希、env-flag、规则代号）不翻译
- 全部路径按 S-2 / SF-5 repo-relative；外部资源（DevolaFlow / NineS / RTK GitHub）按 S-7 仅用 URL
- 主题 block 8-mandatory-field 结构与 v8.4.0 retro §4.7 一致（一句话 / 来源 / 痛点 / 期望 / 改进项 / PV / 风险 + 优先级标注）
- "建议 PV 归属"列与 §"PV-to-Theme 映射"表保持一致；任何分歧以 §映射表为准
- 优先级矩阵 4 桶分布（P0 12 / P1 18 / P2 11 / P3 6）作为 S05 gap_analysis §"Priority allocation" 的直接输入
- Reference 改进列表保留 F-NN 编号与 S03 工件对齐；任何后续修订只能通过追加 F-16+ 而不能 renumber（per CO-2 stable-anchor 纪律）
- Carry-Forward 表全部 verbatim 抽自 `CHANGELOG.md` v8.4.0 + `v9.0.0_version_history_zh.md` §"跨时代顺延事项汇总"；S-5 不臆造 deferral
- 8 决策项均为 S02 / S03 工件中已显式标记的 open question；本清单仅给 preliminary recommendation，最终决策权在 S05 Project Agent + Human 联合裁定

## 交叉引用

- 前置：`.local/research/v9.0.0_decomposition_analysis.md` (281 行，S02-T04 deliverable)
- 前置：`.local/research/v9.0.0_overlap_matrix.md` (141 行，S02-T01 deliverable)
- 前置：`.local/research/v9.0.0_conflict_register.md` (276 行，S02-T02 deliverable)
- 前置：`.local/research/v9.0.0_perf_trajectory.md` (330 行，S02-T03 deliverable)
- 前置：`.local/research/v9.0.0_reference_review.md` (~700 行，S03-T05 deliverable)
- 前置：`.local/research/v9.0.0_version_history_zh.md` (2071 行，S01 中文翻译)
- 后续消费：`.local/research/v9.0.0_gap_analysis.md` (S05-T01)，`.local/research/v9.0.0_version_roadmap.md` (S05-T02)
- 引用 retro：`.local/research/v8.4.0_retrospective.md` §4.1 #5 (A-5) / §4.1 #6 (W-16) / §4.2 #1 (SKILL.md headroom CRITICAL) / §3.2 (carry-forward 9 items)
- 引用 ADR：`.local/research/adr/v7-ADR-001-cache-layout-invariant.md`
- 外部 canonical URLs（per S-7）：`https://github.com/YoRHa-Agents/DevolaFlow`，`https://github.com/YoRHa-Agents/NineS`，`https://github.com/rtk-ai/rtk`

*v9.0.0 SI-1 改进清单（v9.0.0 规划输入）完。下一消费者：`.local/research/v9.0.0_gap_analysis.md` (S05-T01) 与 `.local/research/v9.0.0_version_roadmap.md` (S05-T02)。全部路径 repo-relative per S-2；verbatim 引用保留 per C-3；不臆造 deferral per S-5。*
