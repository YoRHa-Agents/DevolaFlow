---
title: DevolaFlow v8.4.1 → v9.0.0 — 版本路线图 (Version Roadmap)
generated: 2026-04-23
predecessor_cycle: "v8.4.0 @ HEAD `24d6123` on main, annotated tag `v8.4.0` pushed"
companion_artifacts:
  - ".local/research/v9.0.0_gap_analysis.md"            # 并行 S05-T01（616 行，26 个 in-scope 项）
  - ".local/research/v9.0.0_decomposition_analysis.md"  # S02-T04（280 行，8 主题 T1-T8）
  - ".local/research/v9.0.0_improvements_zh.md"         # S04-T02（703 行，47 项，8-PV 映射）
  - ".local/research/v9.0.0_perf_trajectory.md"         # S02-T03（330 行，13 个 v9.0.0 目标）
  - ".local/research/v9.0.0_reference_review.md"        # S03-T05（633 行，15 个 F-NN 关键发现）
  - ".local/research/v9.0.0_contributions_zh.md"        # S04-T01（523 行，125 个评分项）
format_precedent: ".local/research/retrospective_v7.0_to_v7.1.md（仓库内文件名略有差异）以及 v7.0.0 周期结构（5-PV 周期）"
sit_in_artifact_chain:
  - "v9.0.0_gap_analysis.md (S05-T01, 契约) → v9.0.0_version_roadmap.md (本文件, S05-T02 排序 + 依赖 DAG) → v9.0.0_design.md (延后处理 (deferred) 至 PV-01) → 各 PV 分解"
---

# DevolaFlow v8.4.1 → v9.0.0 — 版本路线图 (Version Roadmap)

> v9.0.0 SI-1 规划关卡（W-1）的 Design 团队交付物。
> 作者角色：Design team L3 Task Agent。
> 日期：2026-04-23。
> 范围：锁定 v9.0.0 MAJOR 周期的逐版本计划。
> 输入：见 [交叉引用 (Cross-references)](#交叉引用-cross-references) 章节。

## 目录 (Contents)

- [摘要 (Summary)](#摘要-summary)
- [周期级度量与目标 (Cycle-level metrics and targets)](#周期级度量与目标-cycle-level-metrics-and-targets)
- [版本图谱（8 个版本）(Version map — 8 versions)](#版本图谱8-个版本-version-map--8-versions)
- [v8.4.1 — SKILL.md 压缩 + R7 收尾 + Reference Must-Fix 包 (PATCH)](#v841--skillmd-压缩--r7-收尾--reference-must-fix-包-patch)
- [v8.4.2 — Reference 刷新级联 + Cache-Layout 治理 v2 (PATCH)](#v842--reference-刷新级联--cache-layout-治理-v2-patch)
- [v8.4.3 — A-5 单一事实源注册表规则 (PATCH)](#v843--a-5-单一事实源注册表规则-patch)
- [v8.4.4 — Lifecycle Hooks 全链路接线 + S-10 + M-004 + I-PV07-A (PATCH)](#v844--lifecycle-hooks-全链路接线--s-10--m-004--i-pv07-a-patch)
- [v8.5.0 — NineS Hygiene A1-A4 + W-16..W-20 审计节奏规则 (MINOR)](#v850--nines-hygiene-a1-a4--w-16w-20-审计节奏规则-minor)
- [v8.5.1 — 压缩流水线统一 + B3 Default-On Flip (MINOR)](#v851--压缩流水线统一--b3-default-on-flip-minor)
- [v9.0.0 — 规则体系重平衡 + Soul-Set 冻结 + 汇总（Rollup）(MAJOR)](#v900--规则体系重平衡--soul-set-冻结--汇总rollup-major)
- [v9.0.1 — Sustaining (OPTIONAL PATCH)](#v901--sustaining-optional-patch)
- [版本提升与 7 个同步位置 (Version bump & 7 sync locations)](#版本提升与-7-个同步位置-version-bump--7-sync-locations)
- [排序依据 (Sequencing rationale)](#排序依据-sequencing-rationale)
- [回滚矩阵 (Rollback matrix)](#回滚矩阵-rollback-matrix)
- [交叉引用 (Cross-references)](#交叉引用-cross-references)

## 摘要 (Summary)

v8.4.1 → v9.0.0 周期交付 `v9.0.0_gap_analysis.md` §3.1 列出的 **26 个 in-scope 项**（9 个 BLOCKER + 12 个 CRITICAL + 5 个 MAJOR），分布在 **8 个版本** 上，每个版本都设计为可独立发布与回退。终版 v9.0.0 是真正的 MAJOR bump，因为 Theme T6（规则体系重平衡 — `v9.0.0_decomposition_analysis.md` §"Theme T6" 第 192 行逐字 *"breaking rule-surface semantics; opt-in cached-prefix users see selective slices"*）改变了每一次 L0/L1/L2/L3 派遣（dispatch）所看到的规则表面，对 prompt-only L0 编排器（Cursor / Claude Code / Codex）破坏了 cached-prefix 语义。叠加 BLOCKER 集群同时撞上三个独立天花板（cache-layout `canonical_order` 长度 16 / SKILL.md 499/500 / 50 条规则 — `v9.0.0_conflict_register.md` §Summary 第 16 行逐字 *"all are direct consequences of 'additive surface forever' running into a hard ceiling"*），以及新增 7 条治理规则（A-5 + S-10 + W-16..W-20）跨过 C-09 保护的 Soul-set 边界，按 `v9.0.0_gap_analysis.md` §1.3 全部五条证据，MAJOR 是合理的。

| 版本 | 主题 (Theme) | 范围（Theme T-n 切片）| LOC（预估）| 工作量 | ADR |
|---|---|---|---|---|---|
| **v8.4.1** | T1 SKILL.md 压缩 + R7 + 4 条 reference must-fix | T1 + must-fix 包 | SKILL net −19 + ~+400 ref + ~+150 baseline diff | M | ADR-009 |
| **v8.4.2** | Reference 刷新级联 + T7 cache-layout v2 | T7 + 9 条 ref 级联 | +535 across 10 refs + ~+150 CI tests + ~+250 schema/test | L | ADR-010 |
| **v8.4.3** | T2 A-5 SSOT 注册表规则 | T2 | ~+105 + ~+10 ref | S | ADR-011 |
| **v8.4.4** | T4 Lifecycle 接线 + S-10 + M-004 + I-PV07-A | T4 | ~+400（含 propose_merge）| M | ADR-012 |
| **v8.5.0** | T8 NineS hygiene A1-A4 + W-16..W-20 + deferred 标记 | T8 | ~+800（env-flags + golden 刷新 + W-rules + archive 基础设施）| XL | ADR-013 |
| **v8.5.1** | T3 CompressionPipeline + T5 B3 default-on flip | T3 + T5 | ~+550（T5 翻转 + T3 重构 + 可选 A3 ref）| L | ADR-014 |
| **v9.0.0** | T6 规则重平衡 + 选择性 + 汇总（Rollup）| T6 + sustaining | ~+450（selector + 新 CI 测试）+ retrospective + evobench archive | XL | ADR-015 |
| **v9.0.1** *(可选)* | repo-modes sustaining + Tier-2 企业 adapter | sustaining | ~+24 refs（adapters 落地则 + ~+150）| S | ADR-016 |

**汇总估算**（来源：`v9.0.0_gap_analysis.md` §3.1 + `v9.0.0_improvements_zh.md` §"v9.0.0 建议的 PV-to-Theme 映射" 第 597 行逐字 *"~+3500 across SKILL/refs/src/tests"* + `v9.0.0_perf_trajectory.md` §"v9.0.0-suitable targets" 第 311–326 行）：

| 度量 | 目标 |
|---|---|
| 周期总 LOC | ~3,000-3,500（按 `v9.0.0_improvements_zh.md` 第 597 行，net SKILL.md −19 + net `.cursor/rules/` −400 stub 化）；相对 v7.0.0 周期扩展 ~1,450 反映了 8-PV vs 5-PV 的范围与重平衡工作 |
| EvoBench composite delta（汇总）| 守住 ≥ 90 worst-case 下限（v8.4.0 中 `shell_proxy_disabled` 91.21）；按 W-4 / SI-4，每场景 0 回归 > 5pp |
| EvoBench 场景数 | 48 → 50-55（+2-7）；如 v8.5.1 PV-06 全部 5 个 B3 `_disabled.yaml` 落地，最少 +5 |
| NineS composite delta | 0.9050 baseline → ≥ 0.91 nominal / ≥ 0.93 stretch（需要 v8.5.0 PV-05 完成 A1-A4 hygiene 收尾）|
| NineS hygiene_mean delta | 0.7962 → ≥ 0.85（A1-A4 之后）；stretch ≥ 0.90 |
| 测试数 delta | 3216 → ≥ 3316（+100）；按 I-11 全周期上限 ≤ +150 net（forecast +100 cap 的 1.2×）|
| v9.0.0 汇总时 SI-3 加权综合分目标 | ≥ **9.0/10** MAJOR 阈值（按 I-3，高于 PV 级 8.5 minor 阈值）；stretch ≥ 9.5 |
| SKILL.md 最终行数 | ≤ 480（从 v8.4.0 的 499/500 下降 — 通过 PV-01 压缩回收 ≥ 19 行）|
| 总规则数 | ≤ 60（从 50 上升，通过 T6 完成 ≥ 3 条退役或合并 — 按 `v9.0.0_perf_trajectory.md` 第 321 行 *"absorb ≤ 3 new rules; deprecate any dead ones"* — net delta ≤ +3）|
| Lifetime PV 接受率 | 保持 **100%**（forecast 8/8 PV 通过，把 lifetime 从 49/49 扩展到 57/57；按 I-10）|
| Agent 上下文开销 | 46179 tokens → ≤ 40000 tokens（T8 #2 通过 T1 SKILL.md spillover 收尾）|

## 周期级度量与目标 (Cycle-level metrics and targets)

下面这些 baseline 在 v8.4.0 切版时设定（捕获于 `benchmarks/devolaflow_context/baselines/v8.4.0_baseline.json`），并在每个后续版本重新测量。单一事实源 (SSOT)：`v9.0.0_gap_analysis.md` §1.1 + §7 + `v9.0.0_perf_trajectory.md` §"v9.0.0-suitable targets" 第 311–326 行。

| 度量 | v8.4.0（当前，逐字）| v8.4.x → v8.5.x（中间）| v9.0.0（目标）|
|---|---|---|---|
| `__version__`（`src/devolaflow/__init__.py`）| `8.4.0` | `8.4.1` → `8.4.4` → `8.5.0` → `8.5.1` | `9.0.0` |
| EvoBench composite（中位数）| 来自 `v8.4.0_baseline.json` | 守住 ≥ 90 下限（W-4 / SI-4 不变量）| 在规则重平衡相关场景上 ≥ +5 |
| EvoBench worst-case composite 下限 | **91.21**（`shell_proxy_disabled`）| 守住 ≥ 90 下限 | 守住 ≥ 90 下限 |
| EvoBench 场景数 | **48** | 48-53（仅按需新增）| **50-55**（如 T5 5 个 `_disabled.yaml` 落地，最少 +5）|
| NineS composite | **0.9050**（v8.3.0 → v8.4.0 6 次测量字节稳定）| 逐 PV 维持 0.9050-0.9100；v8.5.0 A1-A4 收尾后 ≥ 0.91 | ≥ **0.91 nominal** / ≥ 0.93 stretch |
| NineS capability mean | **0.9517**（6 次测量字节稳定）| 维持 ≥ 0.95（保留字节稳定）| ≥ 0.95 |
| NineS hygiene_mean | **0.7962**（被 cov 超时拖累）| 通过 A1-A4 包改善（v8.5.0）| ≥ **0.85**（stretch ≥ 0.90）|
| NineS 测量字节稳定性 | 6 次测量（仓库历史最强）| 周期内维持 ≥ 6 | 维持 ≥ 6 |
| 测试覆盖率（`devolaflow.*`）| (当前按 `pyproject.toml [tool.coverage]`) | ≥ 80%（S-3 下限）| ≥ 85% |
| 总通过测试数 | **3216**（v8.4.0 终版按 retrospective §2.1）| 按每 PV +30 上限增长至 ~3300-3400 | ≤ **3366**（按 I-11，cap 3216 + 150）|
| `workflow-system/agent/SKILL.md` 行数 | **499 / 500**（1 行余量 — CRITICAL）| 全程 ≤ 500（HARD）；PV-01 后 ≤ 480（目标）| ≤ **480** |
| 总规则数（Soul/Arch/Conv/Work/Style）| **50**（9/4/9/15/13）| 中间态 50-58 | ≤ **60**（v9.0.0 PV-07 编纂 cap）|
| `workflow-system/agent/references/` 下的 references 数 | **11** | v8.4.1 后 12（`plan-mode-enforcement.md`）；v8.5.0 后 13（`env-flags.md`）| 13（v8.5.1 中可选 14 — `compression-pipeline.md`）|
| P6 cache-layout（`schemas/lean-dispatch.yaml#layout_invariant.canonical_order`）| 长度 **16** / 版本 **5** | 长度 16 / 版本 5（无 top-level 迁移）| 长度 16 / 版本 5（治理 v2 编纂，无 schema bump）|
| Lifetime PV 接受率 | **49 / 49 = 100%** | 逐 PV 持续 100% | **57 / 57 = 100%**（49 + 8 个 v9.0.0 PV）|
| Agent 上下文开销（NineS workflow scan）| **46179 tokens** 标记 "high"（NineS `AI-24c4f48d-0002`）| 通过 T1 spillover 回收 ≥ 6179 tokens | ≤ **40000 tokens** |
| 测试 cap 超额比 | **1.06×**（v8.4.0 实际 +106 / forecast +100）| 按 W-17 候选守住 ≤ 1.2× | 维持 ≤ 1.2× |

每个 PV 的验收标准都必须包含 "相对 baseline 无回归" 这条针对完整度量集的判据（W-4 / SI-4 回归守卫）。任何综合分掉 > 5% 都会按 W-9 / SI-10 在 commit 之前强制修复。

## 版本图谱（8 个版本）(Version map — 8 versions)

本周期发布形态为 **四个 PATCH（PV-01..PV-04）+ 两个 MINOR（PV-05..PV-06）+ 一个 MAJOR（PV-07）+ 一个可选 sustaining PATCH（PV-08）**。依据按 `v9.0.0_gap_analysis.md` §1.2 第 69 行：

- **v8.4.x patches（PV-01..PV-04）** 处理 BLOCKER 积压（B-01 SKILL ≤480；B-02 cache-layout v2；B-03 延后处理 (deferred) 至 PV-07 MAJOR；B-04..B-09 reference must-fix；C-02 A-5 SSOT 编纂；C-03 lifecycle 接线），不改变公共表面 — 仅追加 (additive-only)、对 opt-out 友好。每个 PATCH 关闭一个 Theme 簇（T1+must-fix → T7+T4-prep → T2 → T4），并重置 SI-10 6 步关卡。
- **v8.5.x minors（PV-05..PV-06）** 引入新原语（NineS hygiene 收尾 + 5 条 W-rules + `references/env-flags.md`；CompressionPipeline 协议；5 个 v8.0.0 原语的 B3 default-on 翻转）— 仅追加 (additive-only)，但带有 operator 可见的行为变化，按 semver 约定为 MINOR。
- **v9.0.0 MAJOR（PV-07）** 是规则重平衡 rollup — 对治理契约的破坏性变更（per-task-type AGENTS.md 切片改变了 cached-prefix L0 dispatchers 看到的规则集合）；附带 SI-3 ≥ 9.0 评估报告、SI-8 4 段式 retrospective、wholesale 重生的 `v9.0.0_baseline.json`、以及 annotated `v9.0.0` git tag。
- **v9.0.1 可选 sustaining（PV-08）** 仅在 v9.0.0 SI-3 ≥ 9.0 留有余量、且 Open Decision §1.7 #11 批准 Tier-2 企业 adapter 批次时落地。

### v8.4.1 — SKILL.md 压缩 + R7 收尾 + Reference Must-Fix 包 (PATCH)

**标题：** SKILL.md 余量回收 + R7 符号 anchor 迁移 + 4 条 Reference Must-Fix。

**范围：** 关闭 Theme T1（SKILL.md 压缩到 ≤ 480 行）+ R7 顺延（`workflow-system/agent/section_registry.py` line-anchor → 符号 anchor 全量迁移，始于 v8.2.0 PV-05）+ `v9.0.0_reference_review.md` 的 4 条 reference must-fix（B-04 F-01 S-2 绝对路径违规、B-07 F-05 `MemoryCase` dataclass 编造、B-08 F-06 recipe `note:`/`replacement:` 混淆、B-09 F-14 `case.dispatch_template` 级联）+ C-04 部分（W-16 wholesale baseline 重生 + two-view diff 归档）。同时为 F-04 SKILL Tier-2 nav 行播种 C-01 行插入。受 ADR-009 治理。

**归属文件 (Owned files)**（按 `v9.0.0_gap_analysis.md` §5.1）：

| # | 路径 | 动作 | LOC（delta）|
|---|---|---|---|
| 1 | `workflow-system/agent/SKILL.md` | 修改 — 微压缩 §"Mode Awareness" + §"Reinforcement Rules"；重新放置 `references/plan-mode-enforcement.md` 的 cross-link；新增 +1 行 F-04 `references/behavioral-guidelines.md` 至 Tier-2 nav | net **−19 行** 目标（499 → 480）|
| 2 | `workflow-system/agent/references/plan-mode-enforcement.md` | 新建 — NEW Tier-2 reference 吸收 SKILL.md §"Mode Awareness" + §"Reinforcement Rules" 的提取；第 12 个 SF-4 canonical reference | +350-450 |
| 3 | `workflow-system/agent/references/shell-proxy.md` | 修改 — 4 条 must-fix：第 654 行（F-01）去除 `/root/.cursor/plans/...`；§5.3（F-05）按 `src/devolaflow/memory_router/cache.py:91-130` 重写 `MemoryCase` 字段列表；§6.3（F-06）`note:` → `replacement:` ×2；§5.2（F-14）把 `case.dispatch_template` / `case.expected_savings_pp` 替换为 `case.summary` / `case.recipe_path` / `case.version_stamp` | ~−50 net（重写）|
| 4 | `workflow-system/agent/context_profiles.yaml` | 修改 — R7 line-anchor → 符号 anchor 全量迁移 | ±0 net（refactor）|
| 5 | `src/devolaflow/section_registry.py` | 修改 — R7 anchor 迁移支持 | ±0 net |
| 6 | `tests/test_no_ghost_features.py::test_reference_skill_md_tier2_parity` | 新建 — NEW CI 测试，断言 SF-4 11 项集合 ≡ SKILL.md Tier-2 nav 表（PV-01 后含新 ref 共 12 项）| +50 |
| 7 | `tests/test_no_ghost_features.py::_SF4_REFERENCE_SET` | 修改 — 11 → 12（含 `plan-mode-enforcement.md`）| +5 |
| 8 | `scripts/sync_cursor_skill.py::MIRRORED_FILES` | 修改 — 14 → 15（含新 reference）| +5 |
| 9 | `tests/test_integration.py::test_skill_md_under_500_lines` | 修改 — 断言改为 ≤ 480（目标）| +2 |
| 10 | `benchmarks/devolaflow_context/baselines/v9.0.0_baseline.json` | 新建 — anchor 迁移后 wholesale 重生 | +50 |
| 11 | `benchmarks/devolaflow_context/baselines/`（重生 v7.8.0 + v8.3.0 + v8.4.0）| 修改 — 按 W-16 候选 wholesale 重生（按 v8.3.0 PV-09 commit `12f4ea8` 先例 + v8.4.0 rollup 应用）| ±0（重生）|
| 12 | `tests/test_benchmarks.py::test_runner_prefers_latest_baseline` | 修改 — 提升至 `v9.0.0_baseline.json` | +5 |
| 13 | `.local/research/v9.0.0_evobench_summary.md` | 新建 — NEW two-view diff 归档（raw drift vs post-rebase），按 W-16 编纂候选 | +150 |
| 14 | `CHANGELOG.md` | 修改 — `## [8.4.1]` patch 条目 | +25 |
| 15 | 7 个 canonical 版本同步位置（按 CP-3 / W-10）| 修改 — `8.4.0` → `8.4.1`（通过 `scripts/bump_version.py`）| +10 (net) |

**验收标准 (Acceptance Criteria)：**
1. `wc -l workflow-system/agent/SKILL.md` ≤ 480（目标）且 ≤ 500（按 I-6 HARD）
2. R7 section-anchor 迁移覆盖所有 v8.0.0 retro §3.3 延后处理 (deferred) 项（line-anchored registry 全量迁移）
3. F-01 绝对路径 — 在 `workflow-system/agent/references/` 中逐字 grep `/root/...` 返回 0 命中
4. F-05 + F-14 由 `pytest tests/test_reference_api_alignment.py -v`（PV-02 中 NEW — PV-01 之前用人工 diff 对照 `src/devolaflow/memory_router/cache.py` 与 `schemas/memory-case.yaml`）验证
5. F-06 由在 `references/shell-proxy.md` §6.3 中字面 grep `replacement:` 返回 ≥ 2 命中、且 `note:` 返回 0 命中验证
6. EvoBench wholesale baseline 重生按 v8.3.0 PV-09 commit `12f4ea8` 先例 — 全场景 post-rebase 0 回归 > 5pp；pre-rebase 与 post-rebase 双 diff 归档至 `.local/research/v9.0.0_evobench_summary.md`
7. SF-4 11 项集合 ≡ SKILL.md Tier-2 nav 表（PV-01 后 12 项），由 `pytest tests/test_no_ghost_features.py::test_reference_skill_md_tier2_parity -v` 验证
8. SI-10 6/6 PASS（按 `repo-governance.mdc` W-9 的全部 6 步）
9. SI-3 综合分 ≥ 8.5（按 I-3 PATCH 阈值）
10. NineS post-PV composite ≥ 0.85（按 I-4 顺延下限）

**ADR：** ADR-009 SKILL.md 余量回收 + R7 符号 anchor 迁移（文件：`.local/research/adr/v9-ADR-001-skill-headroom-reclamation.md`）。

**风险 (Risk)：** R-2，来自 `v9.0.0_gap_analysis.md` §6 — 压缩未能回收 20 行。缓解：把 PV-01 拆成 PV-01a（带 R7 anchor 迁移的压缩尝试）+ PV-01b（按 Open Decision §1.7 #4 选项 (b) 升 SF-1 Large tier 1000 的 fallback）；在 gap-analysis 批准时进行 PV-01 前微压缩预算审计；全程持有 HARD `tests/test_integration.py::test_skill_md_under_500_lines`。

**预估代码行数（LOC）：** SKILL net **−19** + ~+400 ref + ~−50 across shell-proxy + ~+150 baseline diff archive ≈ **+500 net**，跨 15 个文件。

**工作量 (Effort)：** **M**（按 gap_analysis §5.1 cap 预测，1-3 小时实施）。

**分解（3 wave，6 task）：**
- W01 SKILL 压缩 + ref 创建 — 2 个并行 task：
  - T01 [doc]：SKILL.md 压缩到 ≤ 480 + `context_profiles.yaml` 与 `section_registry.py` 中的 section-anchor 迁移
  - T02 [doc]：NEW `references/plan-mode-enforcement.md`（~400 LOC，吸收抽出的章节）
- W02 reference must-fix 包 — 1 个 task：
  - T03 [doc]：`references/shell-proxy.md` 的 4 条 must-fix（F-01 + F-05 + F-06 + F-14）
- W03 baseline 重生 + tests + 发布 — 3 个顺序 task：
  - T04 [bench]：wholesale baseline 重生（v7.8.0 + v8.3.0 + v8.4.0 + v9.0.0）；two-view diff 归档
  - T05 [test]：`test_reference_skill_md_tier2_parity` + `_SF4_REFERENCE_SET` 11→12 + `MIRRORED_FILES` 14→15
  - T06 [release]：`scripts/bump_version.py 8.4.1` + CHANGELOG + SI-10 6/6 验证

### v8.4.2 — Reference 刷新级联 + Cache-Layout 治理 v2 (PATCH)

**标题：** 9 reference 协调刷新 + 11 个 frontmatter bump + Cache-Layout 治理 v2 + Drift CI 守卫。

**范围：** 关闭 Theme T7（BLOCKER B-02 cache-layout 治理 v2 — nest-vs-append 决策规则 + 多 baseline 字节一致回归测试）以及 Reference Cascade BLOCKER+CRITICAL 包（B-05 F-02 12-keys → 16-keys；B-06 F-03 lean schema 重写；C-01 body F-04 behavioral-guidelines 扩展；C-05 F-07 meta-framework alias 12→22；C-06 F-08 decomposition-gate gate primitives §5.5；C-07 F-09 frontmatter freshness；C-08 F-10 team-roles 11→22；C-09 F-11 execution-protocol change-driven；C-10 F-12 execution-protocol memory_router；C-11 F-13 execution-protocol pre_shell_call；C-12 F-15 context-isolation §15 重号）。在一个协调 PR 中触动 11 条 SF-4 canonical references 中的 10 条（`agent-workspace.md` 是唯一未触动的）。同时铺设 T4 + T5 + T6 的前置（decomposition-gate §5.5 必须在 v8.5.1 B3 翻转之前存在；meta-framework + team-roles 22-template 覆盖必须在 v9.0.0 T6 选择性之前存在）。受 ADR-010 治理。

**归属文件**（按 `v9.0.0_gap_analysis.md` §5.2）：

| # | 路径 | 动作 | LOC（delta）|
|---|---|---|---|
| 1 | `workflow-system/agent/references/context-isolation.md` | 修改 — F-02 §10 12-keys → 16-keys（新增 `repos`/`behavioral_guidelines`/`acceptance_criteria_v2`/`change_context`）+ F-15 §15 → §16 重号 | +5 |
| 2 | `workflow-system/agent/references/message-schemas.md` | 修改 — F-03 §2/§3 重写 + 新增 §7 layout_invariant + §8 compression rules + §9 result-status（按 R5 backward-compat 把 verbose 形态保留为 deprecated 附录）| +71 |
| 3 | `workflow-system/agent/references/behavioral-guidelines.md` | 修改 — F-04 body 扩展（S-8 组合 + severity matrix + v8.2.x primitives）| +45 |
| 4 | `workflow-system/agent/references/meta-framework.md` | 修改 — F-07 §4 Alias Mapping Table 12 → 22 templates，逐字来自 `workflow-system/agent/templates/builtin/*.yaml` | +60-80 |
| 5 | `workflow-system/agent/references/decomposition-gate.md` | 修改 — F-08 NEW §5.5 "Gate Primitive Composition (v8.0.0+)" 列举 7 个 primitive × {模块 / 引入版本 / 各 profile 默认状态 / EvoBench 场景 / opt-in env-flag} | +50 |
| 6 | `workflow-system/agent/references/team-roles.md` | 修改 — F-10 §7 Team Participation Matrix 11 → 22 templates | +12 |
| 7 | `workflow-system/agent/references/execution-protocol.md` | 修改 — F-11 NEW §12 change-driven envelope + F-12 NEW §10 memory_router fast-path + F-13 NEW §11 pre_shell_call hook + PSC001..PSC004 | +110 |
| 8 | `workflow-system/agent/references/agent-workspace.md` | 修改 — v8.4.0 cross-refs + envelope 策略澄清 | +11 |
| 9 | `workflow-system/agent/references/shell-proxy.md` | 修改 — frontmatter `last_updated: "2026-04-23"` + 至 PV-01 修复的 cross-refs | +5 |
| 10 | 11 个 frontmatter `last_updated: "2026-04-23"` bump（跨所有 references）| 修改 — F-09 短期方案（长期方案：下面第 11 项 CI 守卫）| +11（每文件 1 行）|
| 11 | `tests/test_reference_frontmatter_freshness.py` | 新建 — NEW CI 守卫，强制 `last_updated` 在 90 天窗口内 | +80 |
| 12 | `tests/test_reference_api_alignment.py` | 新建 — NEW CI 守卫，解析 references 中的 dataclass + YAML 代码块；断言字段匹配 canonical schema 或 source — 直接回应 F-05/F-06 的 schema-vs-doc 编造类问题 | +70 |
| 13 | `tests/test_layout_invariant_multi_baseline.py` | 新建 — NEW 测试覆盖 1..N 位置，针对所有先前 baselines（v7.0.0 + v7.3.0 + v8.0.0 P-08 + v8.0.0 P-10 + v8.3.0 PV-05 + v8.4.0）；按 v8.3.0 retro §4.2 逐字 *"`assert_dispatch_layout_byte_identical_for_positions(payload, 1, 15)` on every dispatch"* | +90 |
| 14 | `benchmarks/devolaflow_context/baselines/layout_invariant_v8.0.0.yaml` | 新建 — NEW golden | +50 |
| 15 | `benchmarks/devolaflow_context/baselines/layout_invariant_v8.3.0.yaml` | 新建 — NEW golden | +60 |
| 16 | `benchmarks/devolaflow_context/baselines/layout_invariant_v8.4.0.yaml` | 新建 — NEW golden | +70 |
| 17 | `schemas/lean-dispatch.yaml#layout_invariant` | 修改 — 仅扩展 `version: 5` 文档注释（无 top-level 迁移）| +10 |
| 18 | `src/devolaflow/compressor.py::assert_dispatch_layout` | 修改 — 扩展 nest-vs-append 检查（T7 #3 — 检测应当 nest 的新 top-level key）| +60 |
| 19 | `.cursor/rules/repo-governance.mdc` + `.rules/architecture.mdc` | 修改 — 用 nest-vs-append 规则扩展 A-2 | +45 |
| 20 | `AGENTS.md` | 修改 — 通过 `src/devolaflow/local/compiler.py` 重新生成 | net +30（A-2 ext）|
| 21 | `.local/research/adr/v9-ADR-002-cache-layout-governance-v2.md` | 新建 — NEW ADR 编纂 nest-vs-append 决策规则 | +200 |
| 22 | `CHANGELOG.md` | 修改 — `## [8.4.2]` patch 条目 | +30 |
| 23 | 7 个 canonical 版本同步位置 | 修改 — `8.4.1` → `8.4.2` | +10 |

**验收标准：**
1. 全部 10 条 references 按 `v9.0.0_reference_review.md` Action Plan 更新（关闭 9 条 BLOCKER+CRITICAL findings + 1 次 freshness pass）
2. PV-01 行插入完成后，SKILL.md Tier-2 表条目匹配全部 11 条 SF-4 ref（由 `pytest tests/test_no_ghost_features.py::test_reference_skill_md_tier2_parity -v` 验证）
3. F-02 逐字 — `references/context-isolation.md` §10 写明 "16 keys"，匹配 `schemas/lean-dispatch.yaml#layout_invariant.version 5 length 16`
4. F-03 lean 形态 — `references/message-schemas.md` §2/§3 把 `schemas/lean-dispatch.yaml:80-97` 中的 lean 格式作为 canonical（按 `v9.0.0_reference_review.md` §F-03 第 109 行的 C-2 强制要求）
5. 新 CI 测试通过：`pytest tests/test_reference_frontmatter_freshness.py -v`（90 天窗口）与 `pytest tests/test_reference_api_alignment.py -v`（字段/签名与 schemas + source 对齐）
6. `pytest tests/test_layout_invariant_multi_baseline.py -v` PASS — 覆盖全部 6 个历史 baselines（v7.0.0 + v7.3.0 + v8.0.0 P-08 + v8.0.0 P-10 + v8.3.0 PV-05 + v8.4.0）
7. 11 个 `last_updated: "2026-04-23"` frontmatter bump 已验证
8. SI-10 6/6 PASS
9. SI-3 ≥ 8.5
10. NineS ≥ 0.85；按 W-4 保留 benchmark composite ≥ 90 下限

**ADR：** ADR-010 Reference 刷新级联 + Cache-Layout 治理 v2（文件：`.local/research/adr/v9-ADR-002-cache-layout-governance-v2.md`）。

**风险：** 大型协调 10-ref PR 可能引出非预期的 SKILL.md 级联效应（按 v6.1.0 retro §4 #6 cascading-coupling 根因）。缓解：PV-01 已完成 wholesale baseline 重生 + R7 anchor 迁移（消除主导漂移源）；2-view 方法（pre-rebase + post-rebase）已归档于 PV-01 的 `.local/research/v9.0.0_evobench_summary.md`；如 PV-02 在 post-rebase 上发现真实 > 5pp 回归，按 W-8 暂停并升级。

**预估代码行数（LOC）：** ~+535 across 10 refs + ~+150 新 CI 测试代码 + ~+250 schema/test/ADR/AGENTS = **~+935 net**，跨 23 个文件。

**工作量：** **L**（按 gap_analysis §5.2 cap 预测，3-8 小时）。

**分解（3 wave，8 task）：**
- W01 reference 级联 — 4 个并行 task：
  - T01 [doc]：`context-isolation.md`（F-02 + F-15）+ `message-schemas.md`（F-03）
  - T02 [doc]：`behavioral-guidelines.md`（F-04 body）+ `meta-framework.md`（F-07）+ `team-roles.md`（F-10）
  - T03 [doc]：`execution-protocol.md`（F-11 + F-12 + F-13）
  - T04 [doc]：`agent-workspace.md`（cross-refs）+ `shell-proxy.md`（frontmatter）+ 11 个 `last_updated` bump
- W02 cache-layout 治理 v2 — 2 个并行 task：
  - T05 [test]：`test_layout_invariant_multi_baseline.py`（NEW）+ 3 个 NEW golden YAMLs（v8.0.0 + v8.3.0 + v8.4.0）；`assert_dispatch_layout` nest-vs-append 扩展
  - T06 [doc]：`repo-governance.mdc` + `.rules/architecture.mdc` A-2 扩展；AGENTS.md 重生；ADR-010 起草
- W03 CI 守卫 + 发布 — 2 个顺序 task：
  - T07 [test]：`test_reference_frontmatter_freshness.py` + `test_reference_api_alignment.py`
  - T08 [release]：`scripts/bump_version.py 8.4.2` + CHANGELOG + SI-10 6/6 验证

### v8.4.3 — A-5 单一事实源注册表规则 (PATCH)

**标题：** A-5 SSOT 注册表模式编纂 + DEFAULT_ALLOWLIST parity 测试。

**范围：** 关闭 Theme T2（CRITICAL C-02 / `v9.0.0_conflict_register.md` §C-07）。把 v8.4.0 涌现的 SSOT 注册表模式（按 v8.4.0 retro §4.1 #5 逐字 *"consider documenting it as a 5th canonical Architecture Rule in v8.5.0 (provisionally A-5 — single-source-of-truth for whitelist + recipe registries)"*）提升为硬性 Architecture Rule，并附带 CI 测试，针对 SSOT 模式对新 whitelist/recipe 表面进行门禁。新增 `tests/test_no_ghost_features.py::test_registry_single_owner` —— 当新 whitelist/recipe 表面没有 single-owner 模块时失败。新增 `scripts/detect_dead_apis.py::DEFAULT_ALLOWLIST` parity 测试 —— 拒绝 domain-SSOT 条目（当前为已知重叠，按 `v9.0.0_conflict_register.md` §C-07 第 119 行逐字 *"Both patterns claim to be 'single source of truth' for their domain, but their contracts overlap"*）。把现有注册表 refactor（`Registry[T]` typed protocol；T2 #4 P2 交付物）的强制执行延后处理 (deferred) 到 v8.5.1，视带宽决定。受 ADR-011 治理。

**归属文件**（按 `v9.0.0_gap_analysis.md` §5.3）：

| # | 路径 | 动作 | LOC（delta）|
|---|---|---|---|
| 1 | `.cursor/rules/repo-governance.mdc` | 修改 — 编纂 Architecture Rule A-5 "Single-Source-of-Truth Registry Pattern" | +30 |
| 2 | `.rules/architecture.mdc`（如该子文件存在；否则折入 compiler 输入）| 修改 — A-5 给 compiler 的源 | +30 |
| 3 | `AGENTS.md` | 修改 — 通过 compiler 重生；+A-5 章节 | +35（net）|
| 4 | `tests/test_no_ghost_features.py::test_registry_single_owner` | 新建 — NEW CI 测试，当无 single-owner 模块就引入新 whitelist/recipe/case 表面时失败 | +50 |
| 5 | `scripts/detect_dead_apis.py` | 修改 — 扩展 `DEFAULT_ALLOWLIST` parity 逻辑 | +15 |
| 6 | `tests/test_dead_apis.py` | 修改 — parity 测试拒绝来自 `DEFAULT_ALLOWLIST` 的 domain-SSOT 条目 | +25 |
| 7 | `workflow-system/agent/references/shell-proxy.md` | 修改 — 扩展 §11 NEW "Registry SSOT"，cross-link `shell_proxy/registry.py` + `memory_router/cache.py` + `shell_proxy/commands.py` | +10 |
| 8 | `.local/research/adr/v9-ADR-003-a5-ssot-registry.md` | 新建 — NEW ADR，A-5 编纂 | +180 |
| 9 | `CHANGELOG.md` | 修改 — `## [8.4.3]` patch 条目 | +20 |
| 10 | 7 个 canonical 版本同步位置 | 修改 — `8.4.2` → `8.4.3` | +10 |

**验收标准：**
1. AGENTS.md 与 `.cursor/rules/repo-governance.mdc` 都逐字包含 Architecture Rule A-5
2. `pytest tests/test_no_ghost_features.py::test_registry_single_owner -v` PASS — 新 whitelist/recipe 表面 MUST 有 single-owner 模块
3. `pytest tests/test_dead_apis.py -v` PASS — `DEFAULT_ALLOWLIST` parity 检查拒绝任何同时是 domain-SSOT 名称的条目
4. `references/shell-proxy.md` §11 cross-link 3 个 SSOT 注册表（`shell_proxy/registry.py::WHITELIST`、`memory_router/cache.py::MemoryCase`、`shell_proxy/commands.py::CommandMapping`）
5. ADR-011 记录 A-5：(a) 模式定义、(b) parity 测试强制、(c) v9 阶段化路径（v8.4.3 informational → 如 R-3 缓解需要则 v8.5.0 strict）
6. SI-10 6/6 PASS
7. SI-3 ≥ 8.5
8. NineS ≥ 0.85
9. 每 PV gate 增项：引入新 whitelist/recipe 表面而无 single-owner MUST CI-fail（通过新增一个故意失败的探针测试再 xfail-移除来验证）

**ADR：** ADR-011 A-5 单一事实源注册表模式（文件：`.local/research/adr/v9-ADR-003-a5-ssot-registry.md`）。

**风险：** R-3，来自 `v9.0.0_gap_analysis.md` §6 — 当 parity 测试拒绝已经是 domain-SSOT 名称的、预先存在的 `DEFAULT_ALLOWLIST` 条目时，A-5 引入对现有 registries 的破坏性变更。缓解：PV-03 先做 allowlist 审计，列出每个会被 parity 拒绝的条目；先把 A-5 编纂为模式指南（informational），强制测试在 PV-03 中作为 `xfail(strict=False)` → 在 PV-05 翻转为 strict（让 operators 有一个周期的迁移时间）。

**预估代码行数（LOC）：** ~+105（rules + tests）+ ~+10 ref + ~+180 ADR + ~+30 CHANGELOG/version = **~+325 net**，跨 10 个文件。

**工作量：** **S**（按 gap_analysis §5.3 cap 预测，1-3 小时）。

**分解（2 wave，4 task）：**
- W01 规则 + tests — 2 个并行 task：
  - T01 [doc]：`.rules/architecture.mdc` + `repo-governance.mdc` 中 A-5 编纂；AGENTS.md 重生
  - T02 [test]：`test_registry_single_owner` + `test_dead_apis` parity；`references/shell-proxy.md` §11 cross-link
- W02 ADR + 发布 — 2 个顺序 task：
  - T03 [doc]：ADR-011 起草（~180 LOC）
  - T04 [release]：`scripts/bump_version.py 8.4.3` + CHANGELOG + SI-10 6/6 验证

### v8.4.4 — Lifecycle Hooks 全链路接线 + S-10 + M-004 + I-PV07-A (PATCH)

**标题：** 派遣发射点 Lifecycle 接线 + Soul Rule S-10 提示侧嵌入 + ArchiveManager 合并 + REPORT.md 自动触发。

**范围：** 关闭 Theme T4（CRITICAL C-03 / `v9.0.0_conflict_register.md` §C-12 — plan-mode 强制执行在 5+ 周期 v3.2/v3.3/v3.6/v3.7/v6.1.5/v7.4.1/v7.5.0/v7.7.0/v7.8.0 中反复出现）。在真正的发射点（`feedback.py::generate_round_dispatch`）接线 `lifecycle.run_hooks("pre_dispatch", dispatch, strict=False)` —— 关闭 v7.4.8 P-05 的 dead-wire（100% 覆盖但无 caller，按 `v9.0.0_overlap_matrix.md` Finding #5）。把 v7.8.0 提示侧治理契约模式提升为 Soul Rule S-10。关闭 M-004 顺延（`ArchiveManager.propose_merge` 把 delta-spec 合入 source-of-truth — A-4 ADR 完整闭合）。关闭 I-PV07-A（通过 lifecycle hook 触发 REPORT.md 自动生成）。镜像 v6.0.3 dead-wire 闭合先例（按 `v9.0.0_perf_trajectory.md` §3 逐字 *"v6.0.3 dead-wire closure was the highest-ROI single intervention of v6.0"*）。受 ADR-012 治理。

**归属文件**（按 `v9.0.0_gap_analysis.md` §5.4）：

| # | 路径 | 动作 | LOC（delta）|
|---|---|---|---|
| 1 | `src/devolaflow/feedback.py::generate_round_dispatch` | 修改 — 扩展为在返回前调用 `lifecycle.run_hooks("pre_dispatch", dispatch, strict=False)` | +25 |
| 2 | `src/devolaflow/lifecycle/__init__.py` | 修改 — `DEFAULT_EVENTS` 5 → 6（新增 `pre_dispatch`）| +5 |
| 3 | `src/devolaflow/lifecycle/dispatcher.py` | 修改 — 注册 `pre_dispatch` 事件处理器 | +10 |
| 4 | `tests/test_dispatch_emission_runs_hooks.py` | 新建 — NEW 回归测试，断言 `feedback.py::generate_round_dispatch` 调用 `lifecycle.run_hooks("pre_dispatch", ...)` | +80 |
| 5 | `src/devolaflow/agent_workspace/archive.py` | 修改 — 实现 `ArchiveManager.propose_merge`，把 delta-spec 合入 source-of-truth（关闭 M-004 / A-4 ADR 完整闭合）| +150 |
| 6 | `tests/test_agent_workspace.py` | 修改 — `propose_merge` 覆盖 | +50 |
| 7 | `src/devolaflow/agent_workspace/reporter.py` | 修改 — 通过 lifecycle hook 自动触发 REPORT.md（关闭 I-PV07-A）| +40 |
| 8 | `.cursor/rules/repo-governance.mdc` | 修改 — 编纂 Soul Rule S-10 "Prompt-Side Governance Contract Embedding" | +25 |
| 9 | `AGENTS.md` | 修改 — 重生；+S-10 章节 | +30（net）|
| 10 | `workflow-system/agent/SKILL.md` | 修改 — +3 行指针指向 `references/plan-mode-enforcement.md`（T4 #4）| +3（在 PV-01 ≥17 行余量内）|
| 11 | `workflow-system/agent/references/plan-mode-enforcement.md`（PV-01 已建）| 修改 — 用 S-10 契约扩展 | +20 |
| 12 | `workflow-system/agent/references/decomposition-gate.md` | 修改 — §6 cross-link 至 `lifecycle.run_hooks` 调用点 | +5 |
| 13 | `.local/research/adr/v9-ADR-004-lifecycle-wiring-and-s10.md` | 新建 — NEW ADR，覆盖 T4 + S-10 + M-004 + I-PV07-A | +200 |
| 14 | `CHANGELOG.md` | 修改 — `## [8.4.4]` patch 条目 | +25 |
| 15 | 7 个 canonical 版本同步位置 | 修改 — `8.4.3` → `8.4.4` | +10 |

**验收标准：**
1. `pytest tests/test_dispatch_emission_runs_hooks.py -v` PASS — `feedback.py::generate_round_dispatch` 在返回前调用 `lifecycle.run_hooks("pre_dispatch", ...)`
2. `lifecycle/__init__.py::DEFAULT_EVENTS` 长度为 6（原 5；+`pre_dispatch`）
3. `pytest tests/test_agent_workspace.py -v` PASS — `ArchiveManager.propose_merge` 集成测试覆盖 delta-spec 合入 source-of-truth（M-004 / A-4 ADR 完整闭合）
4. REPORT.md 自动触发 E2E 测试 PASS — `agent_workspace/reporter.py` 在 apply/archive 时由 lifecycle hook 触发
5. AGENTS.md 与 `.cursor/rules/repo-governance.mdc` 都逐字包含 Soul Rule S-10
6. SKILL.md 在 +3 行指针后 `wc -l` ≤ 483（在 ≥ 17 行 PV-01 余量内）
7. `references/plan-mode-enforcement.md` 用 S-10 契约章节扩展
8. R5 strict 三重编纂（按 v8.4.0 retro §4.1 #4 — unit test + lifecycle integration + EvoBench scenario）— 当无 hook handler 注册时，现有 dispatch 行为字节一致
9. SI-10 6/6 PASS
10. SI-3 ≥ 8.5
11. NineS ≥ 0.85

**ADR：** ADR-012 Lifecycle 接线 + Soul Rule S-10 + M-004 ArchiveManager.propose_merge + I-PV07-A REPORT.md 自动触发（文件：`.local/research/adr/v9-ADR-004-lifecycle-wiring-and-s10.md`）。

**风险：** R-10，来自 `v9.0.0_gap_analysis.md` §6 — `feedback.py::generate_round_dispatch` 扩展把第 6 个 `pre_dispatch` 事件引入生产派遣热路径。缓解：`strict=False` 宽松默认（保留 v7.4.8 P-05 模式）；opt-in STRICT 需要显式 env-flag；`tests/test_dispatch_emission_runs_hooks.py` 在无 hook handler 注册时通过现有 dispatch 字节一致；PV-04 R5 strict 三重编纂（unit + integration + EvoBench），按 v8.4.0 retro §4.1 #4；roll-back 是单次回退 `feedback.py::generate_round_dispatch` 扩展；PV-04 落在自己的 feature branch。

**预估代码行数（LOC）：** ~+25（feedback.py）+ ~+15（lifecycle）+ ~+80（test）+ ~+150（propose_merge）+ ~+50（test_agent_workspace）+ ~+40（reporter）+ ~+25（S-10）+ ~+30（AGENTS）+ ~+25（SKILL/ref）+ ~+200（ADR）+ ~+35（CHANGELOG/version）= **~+400 net**，跨 15 个文件。

**工作量：** **M**（3-5 小时；`propose_merge` 是最大块）。

**分解（3 wave，6 task）：**
- W01 lifecycle 接线 — 2 个并行 task：
  - T01 [impl]：`feedback.py::generate_round_dispatch` 扩展 + `lifecycle/__init__.py` `DEFAULT_EVENTS` 5→6 + `lifecycle/dispatcher.py` handler
  - T02 [test]：`test_dispatch_emission_runs_hooks.py`（NEW；+80 LOC）
- W02 propose_merge + REPORT.md + S-10 — 3 个并行 task：
  - T03 [impl]：`agent_workspace/archive.py::propose_merge`（+150）+ 测试覆盖（+50）
  - T04 [impl]：`agent_workspace/reporter.py` REPORT.md 自动触发（+40）
  - T05 [doc]：`.rules/repo-governance.mdc` 中 S-10 编纂；AGENTS.md 重生；SKILL.md +3 行指针；`references/plan-mode-enforcement.md` 扩展
- W03 ADR + 发布 — 1 个 task：
  - T06 [release]：ADR-012 起草 + `scripts/bump_version.py 8.4.4` + CHANGELOG + SI-10 6/6 验证

### v8.5.0 — NineS Hygiene A1-A4 + W-16..W-20 审计节奏规则 (MINOR)

**标题：** NineS Hygiene 包闭合（A1+A2+A3+A4）+ 5 条 Workflow W-Rules + Deferred Marker Class + SI-3 区块拆分。

**范围：** 关闭 Theme T8（M-01 env-flags 清单 + M-02 W-18 ghost-audit 刷新 + M-03 W-19 research artifact 归档 + M-04 W-17 per-PV 测试 cap + M-05 deferred marker class + C-04 部分拆 SI-3 advisory + C-07 finalization）。关闭 v8.0.0 起源的 A1+A2+A3+A4 NineS hygiene 包（cov 超时 / agent_overhead 46179 警告 / index 重建 / golden_test_set 刷新），按 `v9.0.0_gap_analysis.md` §3.2 顺延处置。编纂 5 条 W-rules（W-16 wholesale-baseline-regen + W-17 per-PV 测试 cap + W-18 ghost-audit 刷新前置 + W-19 research artifact 归档 + W-20 env-flag 复用）。新增 `references/env-flags.md` 作为第 13 个 SF-4 canonical reference。实现 `@pytest.mark.deferred(strict=True, reason=...)` 第二 marker class 来区分 deferral-vs-in-progress。本周期首次 **MINOR** bump。受 ADR-013 治理。

**归属文件**（按 `v9.0.0_gap_analysis.md` §5.5）：

| # | 路径 | 动作 | LOC（delta）|
|---|---|---|---|
| 1 | `workflow-system/agent/references/env-flags.md` | 新建 — NEW Tier-2 reference；第 13 个 SF-4 canonical reference（按 Open Decision §1.7 #2 = A1 + A2 都落地）；包含 `DEVOLAFLOW_RTK_PROXY` + `DEVOLAFLOW_MEMORY_ROUTER` + 5 个 v8.0.0 gate-primitive flags + Karpathy 4-primitive 默认 + R5 strict 模式 flags 的清单 | +200-280 |
| 2 | `tests/test_agent_context_overhead.py` | 新建 — NEW 测试，按 A2 闭合强制 ≤ 40K 天花板 | +50 |
| 3 | `tests/test_no_ghost_features.py::test_ghost_audit_refresh_present` | 新建 — NEW lint，按 W-18 | +30 |
| 4 | `scripts/archive_research_artifacts.py` | 新建 — NEW 按 W-19 | +100 |
| 5 | `docs/cycle-archive/` | 新建 — NEW tracked tree（初始 = `v9.0.0_pre/` 占位；W-19 机制）| +20（占位）|
| 6 | `Makefile` 目标 `nines-index-rebuild` | 新建 — NEW 按 A3 | +10 |
| 7 | `data/golden_test_set/` | 修改 — 按 A3 + A4 刷新（按 v6.1.1 tool-config-audit 经验）| ~+200（刷新现有 fixtures）|
| 8 | `pyproject.toml [tool.coverage]` | 修改 — 按 A1 扩展 cov 预算 | +5 |
| 9 | `src/devolaflow/nines/scorer.py` | 修改 — 按 A1 去掉嵌套 cov 收集 | +10 |
| 10 | `nines.toml` | 修改 — 按 A1 + A4 预算调整 | +15 |
| 11 | `src/devolaflow/nines/researcher.py` | 修改 — 按 A3 支持 index 重建 | +30 |
| 12 | `workflow-system/agent/SKILL.md` | 修改 — Tier-2 +1 行指向 `references/env-flags.md`；net +1 行（在 ≥17 行 PV-01 余量内）| +1 |
| 13 | `tests/test_no_ghost_features.py::_SF4_REFERENCE_SET` | 修改 — 12 → 13（含 `env-flags.md`）| +5 |
| 14 | `scripts/sync_cursor_skill.py::MIRRORED_FILES` | 修改 — 15 → 16 | +5 |
| 15 | `.rules/workflow.mdc` | 修改 — 编纂 W-16（wholesale-baseline-regen 规则）+ W-17（per-PV 测试 cap + 周期内审计）+ W-18（ghost-audit 刷新前置）+ W-19（research artifact 归档）+ W-20（env-flag 复用）| +125 |
| 16 | `.cursor/rules/repo-governance.mdc` | 修改 — 从 `.rules/` 同步（W-16..W-20 编纂）| +125 |
| 17 | `AGENTS.md` | 修改 — 重生；+W-16..W-20 章节 | +130（net）|
| 18 | `tests/conftest.py` | 修改 — 实现 `@pytest.mark.deferred(strict=True, reason=...)` 第二 marker class（按 M-05）| +40 |
| 19 | `tests/test_no_ghost_features.py` | 修改 — 注册 marker | +15 |
| 20 | `pyproject.toml [tool.pytest]` | 修改 — 声明 `deferred` marker | +5 |
| 21 | `scripts/generate_si3_evaluation.py` | 新建或修改 — 把 SI-3 评估区块拆分为 "Quality Gate (EvoBench-anchored)" + "Research Snapshot (NineS-anchored, advisory)"（按 C-04）| +40 |
| 22 | `.local/research/v9.0.0_evaluation.md` | 修改 — 模板格式调整以匹配拆分 | +20 |
| 23 | `.local/research/adr/v9-ADR-005-nines-hygiene-and-w-rules.md` | 新建 — NEW ADR，T8 包 | +250 |
| 24 | `CHANGELOG.md` | 修改 — `## [8.5.0]` MINOR 条目 | +35 |
| 25 | 7 个 canonical 版本同步位置 | 修改 — `8.4.4` → `8.5.0` | +10 |

**验收标准：**
1. A1 闭合：`nines self-eval --baseline-version 8.5.0` 显示 `code_coverage > 0`（原因 cov 超时为 0.0）
2. A2 闭合：`pytest tests/test_agent_context_overhead.py -v` PASS — agent 上下文总开销 ≤ 40000 tokens（从被标记的 46179 下降）
3. A3 闭合：`make nines-index-rebuild` 成功；NineS index recall > 0.8
4. A4 闭合：`data/golden_test_set/` 按 v6.1.1 tool-config-audit 经验刷新 — 由 `nines self-eval` capability_mean ≥ 0.95 字节稳定验证
5. AGENTS.md 逐字包含 W-16、W-17、W-18、W-19、W-20（5 条新 Workflow 规则）
6. SF-4 12 项集合更新为 13，含 `env-flags.md`（由 `pytest tests/test_no_ghost_features.py -v` 验证）
7. SKILL.md 在 +1 行后 `wc -l` ≤ 481（在剩余 ≥ 17 行 PV-01 余量内）
8. `@pytest.mark.deferred(strict=True, reason=...)` marker class 在 `tests/conftest.py` 与 `pyproject.toml [tool.pytest]` 中注册
9. SI-3 评估区块按 C-04 拆分（Quality Gate vs Research Snapshot）— 在 `.local/research/v9.0.0_evaluation.md` 模板中验证
10. NineS composite ≥ **0.91**（A1-A4 之后周期目标可达）；维持 ≥ 6 字节稳定测量
11. SI-10 6/6 PASS
12. SI-3 ≥ 8.5（MINOR 阈值）

**ADR：** ADR-013 NineS Hygiene Bundle A1-A4 + W-16..W-20 编纂 + Deferred Marker Class（文件：`.local/research/adr/v9-ADR-005-nines-hygiene-and-w-rules.md`）。

**风险：** R-6，来自 `v9.0.0_gap_analysis.md` §6 — A1-A4 hygiene 包有外部 blocker（cov 超时需要上游 NineS 修复；`pipeline_latency` 0.0 evaluator 探针在上游侧）。缓解：A1 闭合路径 (a) 扩展 `pyproject.toml [tool.coverage]` 预算，或 (b) 去掉嵌套 cov 收集 — 两者都在 DevolaFlow 侧；`pipeline_latency` 在 v6.1.1 已记录为 known-limitation；v9.0.0 不依赖其闭合来达成 0.91 nominal 目标；按 W-2 fallback，手动 NineS 模式可保留 SI-2 acceptance；PV-05 STATUS 报告里透明记录 A1-A4 闭合状态。附加风险：R-7（第 12/13 个 canonical reference 决策卡 S05 sign-off）— 如 S05 拒绝 `env-flags.md`，fallback 是在 `references/shell-proxy.md` § 中作为附录（仅 cross-link）。

**预估代码行数（LOC）：** ~+250（env-flags.md）+ ~+200（golden 刷新）+ ~+125（W-rules）+ ~+125（AGENTS sync）+ ~+150（archive infra）+ ~+50（deferred marker）+ ~+250（ADR）+ 余 = **~+800 net**，跨 25 个文件。

**工作量：** **XL**（5-8+ 小时；周期内文件数与主题广度最大的 PV）。

**分解（4 wave，9 task；**强制 L3 分解** —— 25 个文件超过 `references/decomposition-gate.md` §4 的单 task 天花板）：**
- W01 NineS hygiene 闭合 A1-A4 — 3 个并行 task：
  - T01 [impl]：A1（cov 预算扩展或去掉嵌套 cov 收集）
  - T02 [test + impl]：A2（`test_agent_context_overhead.py` ≤ 40K + T1 SKILL.md spillover 验证）
  - T03 [bench]：A3 + A4（`make nines-index-rebuild` + `data/golden_test_set/` 刷新 + Makefile 目标）
- W02 W-16..W-20 编纂 — 2 个并行 task：
  - T04 [doc]：5 条 W-rules 在 `.rules/workflow.mdc` + `repo-governance.mdc` 同步；AGENTS.md 重生
  - T05 [doc + impl]：`scripts/archive_research_artifacts.py` + `docs/cycle-archive/` tree（按 W-19）
- W03 env-flags reference + deferred marker — 2 个并行 task：
  - T06 [doc]：`references/env-flags.md`（NEW ~250 LOC）；SKILL.md +1 行；`_SF4_REFERENCE_SET` 12→13；`MIRRORED_FILES` 15→16
  - T07 [test]：`@pytest.mark.deferred` marker 在 `tests/conftest.py` + `pyproject.toml`；`scripts/generate_si3_evaluation.py` 中 SI-3 拆分
- W04 ADR + 发布 — 2 个顺序 task：
  - T08 [doc]：ADR-013 起草（~250 LOC）
  - T09 [release]：`scripts/bump_version.py 8.5.0` + CHANGELOG + SI-10 6/6 验证；按 W-17 周期内审计确认累积测试 cap 纪律

### v8.5.1 — 压缩流水线统一 + B3 Default-On Flip (MINOR)

**标题：** CompressionPipeline 协议 + 5 个 Karpathy 原语 Default-On 翻转 + 多 pass filter chain。

**范围：** 关闭 Theme T3（CompressionPipeline 统一 — 把 3 个入口表面 `compressor.py` + `llm_client.py` + `shell_proxy/commands.py` 整合到单一协议；T3 #1 + T3 #2 + T3 #3）+ Theme T5（B3 default-on 翻转 — 5 个 v8.0.0 原语 `token_budget_breaker` + `verification_ladder` + `ratchet` + `complexity_detector` + `ac_generator` 在 STRICT/AUDIT profiles 中从 default-OFF 翻转到 default-ON；T5 #1..#5）。可选关闭 T3 #4（`references/compression-pipeline.md` 第 14 个 SF-4 reference）+ T3 #5（通过 `compose: list[str]` schema 的 `apply_local_recipe` 多 pass filter chain）。两个主题一起发布是因为各自单独都被 MINOR 论证支持，捆绑后形成单次协调的默认行为变更 MINOR release。受 ADR-014 治理。

**归属文件**（按 `v9.0.0_gap_analysis.md` §5.6）：

| # | 路径 | 动作 | LOC（delta）|
|---|---|---|---|
| 1 | `src/devolaflow/compression_pipeline.py` | 新建 — NEW 模块定义 `CompressionStage(name, transform, bypass_conditions, telemetry_key)` 协议 + `CompressionPipeline.run(stages, payload)` orchestrator | +200 |
| 2 | `schemas/compression-pipeline.yaml` | 新建 — NEW schema | +80 |
| 3 | `tests/test_compression_pipeline.py` | 新建 — NEW（含全 stage bypass 时 byte-identical pre/post — R5 strict）| +120 |
| 4 | `benchmarks/devolaflow_context/scenarios/token_budget_disabled.yaml` | 新建 — NEW B3 `_disabled.yaml` 场景 | +50 |
| 5 | `benchmarks/devolaflow_context/scenarios/verification_ladder_disabled.yaml` | 新建 — NEW B3 `_disabled.yaml` 场景 | +50 |
| 6 | `benchmarks/devolaflow_context/scenarios/ratchet_disabled.yaml` | 新建 — NEW B3 `_disabled.yaml` 场景 | +50 |
| 7 | `benchmarks/devolaflow_context/scenarios/complexity_detector_disabled.yaml` | 新建 — NEW B3 `_disabled.yaml` 场景 | +50 |
| 8 | `benchmarks/devolaflow_context/scenarios/ac_generator_disabled.yaml` | 新建 — NEW B3 `_disabled.yaml` 场景 | +50 |
| 9 | `workflow-system/agent/references/compression-pipeline.md` | 新建（OPTIONAL A3）— NEW Tier-2 reference；按 Open Decision §1.7 #10 候选第 14 个 SF-4 canonical reference — 仅在 T3 干净落地时；否则延后处理 (deferred) 至 v9.0.x | +300-400 |
| 10 | `src/devolaflow/compressor.py` | 修改 — 把 `truncate_tool_output` + `summarise_predecessor` + `directed_compact` + abstractive A+B 重构为具体的 `CompressionStage` 实现 | +50（refactor net）|
| 11 | `src/devolaflow/llm_client.py` | 修改 — 把 abstractive Stage B 重构为 `CompressionStage` | +30 |
| 12 | `src/devolaflow/shell_proxy/commands.py` | 修改 — 把 `apply_local_recipe` 重构为 `CompressionStage`；按 T3 #5 添加多 pass filter chain（RTK `filters.toml` filter 组合）| +80 |
| 13 | `schemas/command-mapping.yaml` | 修改 — schema 1 → 2 添加 `compose: list[str]`（按 T3 #5）| +20 |
| 14 | `workflow-system/agent/context_profiles.yaml` | 修改 — 在 STRICT/AUDIT profiles 中按 T5 #1..#5 把 5 个原语从 default-OFF 翻转到 default-ON | +30 |
| 15 | `src/devolaflow/gate/budget.py` | 修改 — T5 #1 `token_budget_breaker` STRICT 默认 | +5 |
| 16 | `src/devolaflow/gate/scorer.py` | 修改 — T5 #2 `verification_ladder` 默认 | +5 |
| 17 | `src/devolaflow/gate/ratchet.py` | 修改 — T5 #3 `ratchet` 默认 | +5 |
| 18 | `src/devolaflow/gate/complexity_detector.py` | 修改 — T5 #4 `complexity_detector` 默认 | +5 |
| 19 | `src/devolaflow/acceptance.py` | 修改 — T5 #5 `ac_generator` 默认 | +5 |
| 20 | `workflow-system/agent/references/decomposition-gate.md` §5.5（PV-02 已加）| 修改 — 翻转的 5 个原语的默认状态列更新 | +10 |
| 21 | `workflow-system/agent/SKILL.md` | 修改 — Tier-2 +1 行指向 `references/compression-pipeline.md`（仅当 A3 落地）| 0 或 +1（在 ≥16 行余量内）|
| 22 | `tests/test_no_ghost_features.py::_SF4_REFERENCE_SET` | 修改 — 13 → 14（仅当 A3 落地）| 0 或 +5 |
| 23 | `scripts/sync_cursor_skill.py::MIRRORED_FILES` | 修改 — 16 → 17（仅当 A3 落地）| 0 或 +5 |
| 24 | `.local/research/adr/v9-ADR-006-compression-pipeline-and-b3-flip.md` | 新建 — NEW ADR，T3 + T5 包 | +220 |
| 25 | `CHANGELOG.md` | 修改 — `## [8.5.1]` MINOR 条目，含 "Adoption notes" 列出 5 个原语翻转 + opt-out env-flag 路径 | +40 |
| 26 | 7 个 canonical 版本同步位置 | 修改 — `8.5.0` → `8.5.1` | +10 |

**验收标准：**
1. `pytest tests/test_compression_pipeline.py -v` PASS — 含全 stage bypass 时 byte-identical pre/post 测试（R5 strict）
2. 5 个 `_disabled.yaml` EvoBench 场景在 composite ≥ 90 下限通过（按 W-4 / SI-4）
3. EvoBench 场景数：48 → 53（+5 来自 `_disabled.yaml` 场景）
4. `context_profiles.yaml` 翻转已验证 — 5 个原语在 STRICT/AUDIT profiles 中 `default: true`（原 `default: false`）
5. CHANGELOG `## [8.5.1]` "Adoption notes" 逐字列出全部 5 个翻转原语 + opt-out env-flag 路径
6. 重构的 5 个 transform（`truncate_tool_output` / `summarise_predecessor` / `directed_compact` / `apply_local_recipe` / abstractive A+B）全部实现 `CompressionStage` 协议
7. `schemas/command-mapping.yaml` schema_version: 1 → 2（含按 T3 #5 的 `compose: list[str]` 字段）
8. 多 pass filter chain 测试：`apply_local_recipe` 按 `compose:` list 链式 2+ filters
9.（如 A3 落地）`references/compression-pipeline.md` 存在；SF-4 集合 13 → 14；SKILL.md +1 行在余量内
10. SI-10 6/6 PASS
11. SI-3 ≥ 8.5（MINOR 阈值）
12. NineS ≥ 0.91（顺延自 PV-05 周期目标）

**ADR：** ADR-014 CompressionPipeline 协议 + B3 Default-On 翻转 + 多 pass filter chain（文件：`.local/research/adr/v9-ADR-006-compression-pipeline-and-b3-flip.md`）。

**风险：** R-4，来自 `v9.0.0_gap_analysis.md` §6 — B3 5 原语翻转暴露此前仅 opt-in 测试的 primitive 中潜在 bug；一个或多个 `_disabled.yaml` 场景跌破 90 下限。缓解：按 Open Decision §1.7 #5 Hybrid (b) 错峰翻转（每个子 PV 1-2 个，而非一次 5 个）；每个 primitive 的 `_disabled.yaml` ≥ 90 下限是翻转前 HARD 前置；roll-back 是 `context_profiles.yaml` 1 行 + STATUS 报告记录延后处理 (deferred)；如任一翻转失败，延后处理 (deferred) 该单一 primitive，发其他 4 个。附加风险：R-2 spillover — 如 A3 reference 落地，SKILL.md +1 行消耗 PV-01 剩余 17 行余量中的 1 行（编辑后验证 wc ≤ 481）。

**预估代码行数（LOC）：** ~+200（compression_pipeline.py）+ ~+80（schema）+ ~+120（test）+ ~+250（5 个 `_disabled.yaml` 场景）+ ~+200（重构 5 个 transform）+ ~+30（context_profiles 翻转）+ ~+220（ADR）+ ~+50（CHANGELOG/version）= **~+550 net**（不含可选 A3 reference）。含 A3：~+850-950。

**工作量：** **L**（按 gap_analysis §5.6 cap 预测，3-8 小时；同时重构 5 个文件是主要成本）。

**分解（4 wave，8 task；**强制 L3 分解** —— 26 个文件）：**
- W01 CompressionPipeline 协议 — 2 个并行 task：
  - T01 [impl]：`src/devolaflow/compression_pipeline.py`（NEW，~200 LOC）+ `schemas/compression-pipeline.yaml`
  - T02 [test]：`tests/test_compression_pipeline.py`（NEW，~120 LOC，含 byte-identical-when-bypassed）
- W02 5-stage 重构 — 1 个 task：
  - T03 [impl]：把 `truncate_tool_output` + `summarise_predecessor` + `directed_compact` + `apply_local_recipe`（含多 pass filter chain）+ abstractive A+B 重构为 `CompressionStage` 实现；`command-mapping.yaml` schema 1→2
- W03 B3 default-on 翻转 — 2 个并行 task：
  - T04 [config]：`context_profiles.yaml` STRICT/AUDIT 5 个原语 default-on 翻转；5 个 `gate/*.py` 与 `acceptance.py` 内每 primitive 默认更新
  - T05 [bench]：5 个 `_disabled.yaml` EvoBench 场景验证 ≥ 90 下限
- W04 docs + 发布（含可选 A3）— 3 个顺序 task：
  - T06 [doc]：`references/decomposition-gate.md` §5.5 默认状态更新；OPTIONAL `references/compression-pipeline.md`（~300-400 LOC）+ SKILL.md +1 行 + `_SF4_REFERENCE_SET` + `MIRRORED_FILES`
  - T07 [doc]：ADR-014 起草（~220 LOC）
  - T08 [release]：`scripts/bump_version.py 8.5.1` + CHANGELOG with "Adoption notes" + SI-10 6/6 验证

### v9.0.0 — 规则体系重平衡 + Soul-Set 冻结 + 汇总（Rollup）(MAJOR)

**标题：** 规则体系重平衡 + 按任务类型选择性 + Soul-Set 冻结 + 60 条规则硬上限 + SI-8 Retrospective 汇总（Rollup）。

**范围：** 关闭 BLOCKER B-03（Theme T6 — `v9.0.0_conflict_register.md` §C-08 第 132 行逐字 *"50 → 50 (UNCHANGED)"*；增长路径 *"19 → 24 → 34 → 46 → 50 across v3.x → v8.3.0"*；以及 C-09 Soul Rules "immutable" 主张）。把 `.rules/` 5 层 source 提升为 **唯一 canonical** 规则表面；把 `.cursor/rules/devola-flow-rules.mdc` + `.cursor/rules/workflow-rules.mdc` 退化为 cross-reference stub；通过 `src/devolaflow/local/compiler.py` 把 `.cursor/rules/*.mdc` 仅作为编译输出重生 + 通过 `local/drift.py` SHA-256 hash 进行 drift 检测；扩展 `task_adaptive_selector.py` 按任务 profile 过滤每次 L0/L1/L2/L3 派遣加载的 AGENTS.md 切片（按任务类型选择性）；把总规则数 cap 在 **60**，硬性要求规则退役或合并；冻结 Soul Rules 在 **9 项**（如 PV-04 的 S-10 在 PV-07 ratify 则 10 项），任何新增需 2 周期 telegraph + retro 条目。发布周期汇总（rollup）交付物：SI-3 评估报告 ≥ 9.0 MAJOR 阈值、SI-8 4 段式 retrospective、wholesale 重生的 `v9.0.0_baseline.json`、annotated `v9.0.0` git tag、按 ST-7 append `versions.json`、按 W-19 填充 `docs/cycle-archive/v9.0.0/`。受 ADR-015 治理。**MAJOR semver 信号本身就是 cached-prefix L0 dispatchers 看到 per-task-type 规则切片的破坏性变更宣告**。

**归属文件**（按 `v9.0.0_gap_analysis.md` §5.7）：

| # | 路径 | 动作 | LOC（delta）|
|---|---|---|---|
| 1 | `.rules/*.mdc` | 修改 — 把 `.rules/` 5 层 source 提升为唯一 canonical 规则表面；按 T6 #1 | 跨 5 个 layer 文件 +200 |
| 2 | `src/devolaflow/local/compiler.py` | 修改 — 仅把 `.cursor/rules/*.mdc` 作为编译输出生成 | +60 |
| 3 | `src/devolaflow/local/drift.py` | 修改 — `.rules/` source 与编译产出 `.cursor/rules/*.mdc` 之间的 SHA-256 drift 检测 | +40 |
| 4 | `src/devolaflow/task_adaptive_selector.py` | 修改 — per-task-type AGENTS.md 切片过滤（按 T6 #2）| +150 |
| 5 | `workflow-system/agent/context_profiles.yaml` | 修改 — selector 切片 profiles | +30 |
| 6 | `.cursor/rules/devola-flow-rules.mdc` | 修改 — REDUCE 为 cross-reference stub（deprecated）| −300 |
| 7 | `.cursor/rules/workflow-rules.mdc` | 修改 — REDUCE 为 cross-reference stub（deprecated）| −100 |
| 8 | `.rules/governance.mdc`（NEW 或现有）| 修改或新建 — 按 T6 #3 编纂 Soul-set-add 治理规则（2 周期 telegraph + retrospective 条目要求）| +30 |
| 9 | `tests/test_no_ghost_features.py::test_rule_count_under_cap` | 新建 — NEW 按 T6 #4 | +30 |
| 10 | `tests/test_no_ghost_features.py::test_rule_surfaces_compile_only` | 新建 — NEW 按 T6 #5（通过 `local/drift.py` SHA-256 hash 检测；当 `.cursor/rules/*.mdc` 被手工编辑时失败）| +40 |
| 11 | `AGENTS.md` | 修改 — 通过 `src/devolaflow/local/compiler.py` 重生；net 变化反映 T6 deprecation + selector annotations | net 反映性变化 |
| 12 | `CHANGELOG.md` | 修改 — `## [9.0.0]` MAJOR 条目，含显式 "Adoption notes"：(a) 破坏性 rule-surface 语义；(b) `.cursor/rules/devola-flow-rules.mdc` + `workflow-rules.mdc` 退役 → cross-reference stubs；(c) Soul-set 冻结在 9 项（如 S-10 ratify 则 10 项）| +60 |
| 13 | 7 个 canonical 版本同步位置 | 修改 — `8.5.1` → `9.0.0` | +10 |
| 14 | `benchmarks/devolaflow_context/baselines/v9.0.0_baseline.json` | 修改 — wholesale 重生（按 W-16）反映 selector + rule-surface 变更 | ±0（重生）|
| 15 | `workflow-system/human/demo/version-timeline/versions.json` | 修改 — 按 ST-7 append v9.0.0 条目（REQUIRED — major release headline）| +25 |
| 16 | `.local/research/v9.0.0_evaluation.md` | 新建 — SI-3 release 评估（综合分 ≥ 9.0 MAJOR 阈值；≥ 9.5 stretch）| +200 |
| 17 | `.local/research/v9.0.0_retrospective.md` | 新建 — SI-8 4 段式 retrospective（gaps identified / what shipped / what deferred / key learnings）| +400 |
| 18 | `.local/research/v9.0.0_evobench_summary.md` | 修改 — 相对 v8.4.0 的逐场景 delta + 7-PV 累计 | +200 |
| 19 | `.local/research/v9.0.0_nines.json` + `.md` summary | 新建 — NineS post-cycle | +100 |
| 20 | `docs/cycle-archive/v9.0.0/` | 修改 — 通过 `scripts/archive_research_artifacts.py`（PV-05 交付物）填充 | 反映性 |
| 21 | `.local/research/adr/v9-ADR-007-rule-rebalancing-and-rollup.md` | 新建 — NEW ADR，T6 + rollup | +300 |

**验收标准：**
1. `pytest tests/test_no_ghost_features.py::test_rule_count_under_cap -v` PASS — 总规则数 ≤ 60（HARD）
2. `pytest tests/test_no_ghost_features.py::test_rule_surfaces_compile_only -v` PASS — `.cursor/rules/*.mdc` SHA-256 匹配 compiler 输出（无手工编辑）
3. 跨全部 5+ 任务类型通过 per-profile baseline 比对验证 per-task-type 切片 0 EvoBench drift
4. `.cursor/rules/devola-flow-rules.mdc` 与 `.cursor/rules/workflow-rules.mdc` 都退化为 cross-reference stubs（每个 ≤ 50 行，指向 `.rules/` canonical source）
5. AGENTS.md 重生；按任务 profile 含 selector annotations
6. CHANGELOG `## [9.0.0]` "Adoption notes" 逐字声明破坏性变更（按 `v9.0.0_gap_analysis.md` §5.7 acceptance 逐字）
7. `versions.json` 按 ST-7 append v9.0.0 条目（REQUIRED — major release headline）
8. SI-3 评估报告 ≥ **9.0**（按 I-3 MAJOR 阈值）；≥ 9.5 stretch
9. SI-8 retrospective 含全部 4 段必需章节（gaps identified / implemented / deferred / key learnings）按 W-7
10. `v9.0.0_baseline.json` 按 W-16 wholesale 重生
11. `docs/cycle-archive/v9.0.0/` 按 W-19 填充
12. annotated `v9.0.0` git tag 创建
13. SI-10 6/6 PASS
14. NineS ≥ 0.91（周期目标）

**ADR：** ADR-015 规则体系重平衡 + 按任务类型选择性 + Soul-Set 冻结 + 汇总（Rollup）（文件：`.local/research/adr/v9-ADR-007-rule-rebalancing-and-rollup.md`）。

**风险：** R-1（MAJOR 论证在 S05 sign-off 失败 — T6 延后处理 (deferred)；v9.0.0 重分类为 v8.5.0 MINOR，PV-07 取消）— 缓解：S05 sign-off 锁定决策；按 `v9.0.0_improvements_zh.md` §"决策项 #1" 推荐的 Hybrid 路径 (c) 默认走 MAJOR + selectivity-only T6 = additive；如重分类，v8.5.0 MINOR rollup 只是把 PV-07 重命名为跨周期延后处理 (deferred)；PV-01..PV-06 不变。加 R-8（规则数 cap 与 v9.0.0 规则新增冲突 — A-5 + S-10 + W-16..W-20 = +7 net 规则可能跨 50 → 57；T6 退役必须 offset 才能保持 ≤ 60）— 缓解：T6 #1 退役 `.cursor/rules/devola-flow-rules.mdc`（6 条 P1-P6 规则）+ `workflow-rules.mdc`（5 条 P1-P5 规则）= 名义 −11，但按 Overlap Finding #3 它们是 DUPLICATES — 实际 net 规则数变化 +7 net（50 → 57 ≤ 60 OK）；PV-07 之前显式 rule deprecation 审计列出每条退役/合并的规则；`tests/test_no_ghost_features.py::test_rule_count_under_cap` 强制 ≤ 60。加 R-9（per-task-type 规则选择性破坏 cached-prefix 用户）— 缓解：MAJOR semver 显式接受破坏性变更语义；2 周期 telegraph（PV-04 STATUS 宣告，PV-05 `references/env-flags.md` 文档）；当 profile 匹配失败时 selector fallback 到完整 AGENTS.md（R5 strict default-permissive）；PV-07 EvoBench post-slice 0 drift 验证。

**预估代码行数（LOC）：** ~+200（`.rules/*.mdc` 5 个 layer 文件）+ ~+250（compiler/drift/selector + context_profiles）− ~−400（把 2 个 `.cursor/rules/*.mdc` 退化为 stub）+ ~+70（governance + 2 个 NEW 测试）+ ~+60（CHANGELOG）+ ~+200（SI-3 eval）+ ~+400（SI-8 retro）+ ~+200（evobench summary）+ ~+100（NineS）+ ~+300（ADR）+ ~+25（versions.json）+ ~+10（version sync）= **~+1,415 net**（含 ~+700 文档/归档交付物，rollup 独有需要）。仅代码 delta ~+450。

**工作量：** **XL**（8+ 小时；规则重平衡 + selector + 4 个新测试表面 + rollup 交付物；如 PV-04 的 M-004 需要在 MAJOR 阈值上 end-to-end 验证，包含 `propose_merge` 集成）。

**分解（5 wave，11 task；**强制 L3 分解** —— 21 个文件 + 周期汇总（rollup））：**
- W01 规则表面提升 + 退役 — 3 个并行 task：
  - T01 [doc]：`.rules/*.mdc` 5 层 source 提升（5 个文件 ~+200 LOC）
  - T02 [doc]：`.cursor/rules/devola-flow-rules.mdc`（−300）+ `.cursor/rules/workflow-rules.mdc`（−100）→ 仅 cross-reference stubs
  - T03 [doc]：`.rules/governance.mdc` Soul-set-add 治理规则（~+30 LOC）
- W02 selector + compiler + drift — 2 个并行 task：
  - T04 [impl]：`task_adaptive_selector.py` per-task-type AGENTS.md 切片过滤（~+150 LOC）+ `context_profiles.yaml` selector profiles（~+30 LOC）
  - T05 [impl]：`local/compiler.py`（~+60 LOC）+ `local/drift.py` SHA-256（~+40 LOC）；AGENTS.md 重生
- W03 cap + drift CI 测试 — 1 个 task：
  - T06 [test]：`test_rule_count_under_cap`（≤60）+ `test_rule_surfaces_compile_only`（drift 检测）
- W04 周期 rollup 交付物 — 4 个并行 task：
  - T07 [bench]：`v9.0.0_baseline.json` wholesale 重生 + 跨全部任务类型 EvoBench post-slice drift 验证
  - T08 [doc]：SI-3 评估报告 `.local/research/v9.0.0_evaluation.md`（≥ 9.0 MAJOR；≥ 9.5 stretch）
  - T09 [doc]：SI-8 4 段式 retrospective `.local/research/v9.0.0_retrospective.md`
  - T10 [doc]：NineS post-cycle JSON + summary；`versions.json` v9.0.0 条目；`docs/cycle-archive/v9.0.0/` 填充
- W05 ADR + 发布 — 1 个 task：
  - T11 [release]：ADR-015 起草（~+300 LOC）+ `scripts/bump_version.py 9.0.0` + CHANGELOG with "Adoption notes" + SI-10 6/6 验证 + annotated `v9.0.0` git tag

### v9.0.1 — Sustaining (OPTIONAL PATCH)

**标题：** repo-modes.md sustaining + Tier-2 企业 Adapter 批次预留。

**范围：** 关闭 L-003（web demo refresh subset）+ repo-modes.md sustaining 编辑 + （可选）Tier-2 企业 adapter 批次（JetBrains / Amazon Q / Gemini / Augment / Trae），按 Open Decision §1.7 #11（`v9.0.0_improvements_zh.md` §"决策项 #11"）。**条件发布**：仅在 PV-07 SI-3 ≥ 9.0 留有余量、且 Open Decision §1.7 #11 = approve 时。如 S05 拒绝或 PV-07 SI-3 评估恰好落在 9.0（无余量），本 PV 改为 v9.1.0 PV-01。受 ADR-016 治理。

**归属文件**（按 `v9.0.0_gap_analysis.md` §5.8）：

| # | 路径 | 动作 | LOC（delta）|
|---|---|---|---|
| 1 | `workflow-system/agent/references/repo-modes.md` | 修改 — Codeberg 变体澄清（+2 LOC）+ §6 Mode-Aware Stage Behavior 中 `+verify`、`+monitor` 行（+4 LOC）+ §7 Plugin/Tool 与 mode detection 交互（如 PV-03 未做则 +18 LOC）| +24 |
| 2 | `src/devolaflow/adapter_configs/jetbrains.yaml`（OPTIONAL）| 新建 | +25 |
| 3 | `src/devolaflow/adapter_configs/amazon_q.yaml`（OPTIONAL）| 新建 | +25 |
| 4 | `src/devolaflow/adapter_configs/gemini.yaml`（OPTIONAL）| 新建 | +25 |
| 5 | `src/devolaflow/adapter_configs/augment.yaml`（OPTIONAL）| 新建 | +25 |
| 6 | `src/devolaflow/adapter_configs/trae.yaml`(OPTIONAL) | 新建 | +25 |
| 7 | `.local/research/adr/v9-ADR-008-sustaining.md` | 新建 — NEW ADR，sustaining 决策 | +120 |
| 8 | `CHANGELOG.md` | 修改 — `## [9.0.1]` sustaining 条目 | +20 |
| 9 | 7 个 canonical 版本同步位置 | 修改 — `9.0.0` → `9.0.1` | +10 |

**验收标准：**
1. `references/repo-modes.md` 与 v9.0.0 mode set 同步（Codeberg 变体 + `+verify` + `+monitor` 行）
2.（如 adapters 落地）5 个 NEW `adapter_configs/*.yaml` 文件按现有 schema 校验；SF-3 mirror sync 通过
3. SI-10 6/6 PASS
4. SI-3 ≥ 8.5（PATCH 阈值；低于 v9.0.0 MAJOR）
5. NineS ≥ 0.91（sustaining 继承 v9.0.0 下限）
6. 条件发布关卡：PV-07 SI-3 评估 ≥ 9.0 AND Open Decision §1.7 #11 = approve

**ADR：** ADR-016 Sustaining 周期决策（文件：`.local/research/adr/v9-ADR-008-sustaining.md`）。

**风险：** R-12，来自 `v9.0.0_gap_analysis.md` §6 — PV-08 sustaining 决策延迟 release tag。缓解：v9.0.0 在 PV-07（annotated tag）发布；如需 PV-08 即作为 v9.0.1 单独周期；如 S05 拒绝 PV-08，本工作自动延后处理 (deferred) 至 v9.1.0 PV-01。

**预估代码行数（LOC）：** ~+24（refs）+ ~+150（5 个 OPTIONAL adapters）+ ~+120（ADR）+ ~+30（CHANGELOG/version）= **~+200-330**（区间反映可选 adapter 是否发布）。

**工作量：** **S**（1-3 小时；条件性）。

**分解（1 wave，2 task）：**
- W01 sustaining — 2 个并行 task：
  - T01 [doc]：`references/repo-modes.md` sustaining 编辑（~+24 LOC）
  - T02 [config + release]：OPTIONAL Tier-2 企业 adapters（~+125 LOC）+ ADR-016 + `scripts/bump_version.py 9.0.1` + CHANGELOG + SI-10 6/6 验证

## 版本提升与 7 个同步位置 (Version bump & 7 sync locations)

按 Rule W-10 / CP-3（`.cursor/rules/repo-governance.mdc` §"W-10 — Version Bump Protocol (CP-3)"），本周期每次版本提升必须通过 `scripts/bump_version.py` 一致更新所有 **7 个 canonical 同步位置**。单一事实源 (SSOT)：`src/devolaflow/__init__.py`。一次实际 bump 触动的 8 个文件（1 个 source-of-truth + 7 个 canonical 同步位置，按 Rule SF-3 逐字列表）：

| # | 路径 | 更新内容 |
|---|---|---|
| 1 | `src/devolaflow/__init__.py` | `__version__ = "X.Y.Z"`（source of truth）|
| 2 | `workflow-system/agent/SKILL.md` | frontmatter `version:`、banner `Now Using DevolaFlow vX.Y.Z`、body `Current version:` 文本 |
| 3 | `workflow-system/agent/workflow-skill.yaml` | identity 章节 `version:` |
| 4 | `pyproject.toml` | `[project] version = "X.Y.Z"` 字段 |
| 5 | `scripts/generate_human_docs.py` | `SOURCE_VERSION = "X.Y.Z"` |
| 6 | `tests/test_smoke.py` | version 断言常量 |
| 7 | `README.md` | badge + version 示例 |
| 8 | `workflow-system/human/demo/benchmark-results/index.html` | `SAMPLE_DATA.version = "X.Y.Z"` |

根 `CLAUDE.md` 按 CP-3 **豁免**（它是无 version frontmatter 的 project-context 文件；Rule CP-3 第 49 行逐字 *"Root `CLAUDE.md` is a lightweight project-context file and does NOT carry a version"*）。

`.cursor/skills/devola-flow/` 的 opt-in mirror 在本地存在时由 `scripts/bump_version.py` 调用 `scripts/sync_cursor_skill.py` **自动同步**；当 `.cursor/skills/devola-flow/` 不存在时（gitignored）sync 脚本是 **no-op**，所以 CI 与 fresh clone 都干净 bump。mirror 的 14 个字节文件（1 SKILL.md + 10 references + 3 examples）加 `.devola-flow-version` 时间戳，在 PV-01 增长到 **15**（含 `plan-mode-enforcement.md`）、PV-05 增长到 **16**（含 `env-flags.md`）；这些增长在上文 PV-01 与 PV-05 归属文件表中由 `scripts/sync_cursor_skill.py::MIRRORED_FILES` 更新反映。

每次 bump 后，release task 必须按 `repo-governance.mdc` W-9 运行 SI-10 6 步 pre-commit 序列：

```
1. python -m pytest tests/ -q                    # all tests pass
2. ruff check src/ tests/                        # no lint errors
3. ruff format --check src/ tests/               # formatting correct
4. python -m pytest tests/test_version.py -v     # canonical 7 sync locations agree
5. python -m pytest tests/test_benchmarks.py -v  # no benchmark regressions > 5pp
6. make check-cursor-skill                       # exit 0 (no-op if mirror absent; bytewise parity if present)
```

任何步骤失败都会阻塞 commit。v9.0.0 之后运行 `make sync-human-docs` 把新版本按 ST-4 传播到生成的 EN/ZH human docs。

## 排序依据 (Sequencing rationale)

8 个 PV 的排序依据 `v9.0.0_decomposition_analysis.md` §"Theme-to-PV Mapping Sketch" 第 224 行 + `v9.0.0_gap_analysis.md` §5 依赖图 + `v9.0.0_improvements_zh.md` §"v9.0.0 建议的 PV-to-Theme 映射" 第 575 行。下面给出 7 个 PV 间转换的逐项论证。关键排序决策：

- **PV-01 SKILL.md 压缩 FIRST** — 任何后续 PV 都可能需要往 SKILL.md 增行（PV-04 +3 行 plan-mode 指针；PV-05 +1 env-flags 行；PV-06 可选 +1 compression-pipeline 行）；当前 1 行余量（499/500）禁止这种增长。强制依据：`v9.0.0_gap_analysis.md` I-6 不变量 + v8.4.0 retro §4.2 #1 逐字 *"v8.5.0+ MUST either (a) Promote SKILL.md to a higher tier, OR (b) Run a dedicated SKILL.md compression PV, OR (c) Defer all SKILL.md surface changes to a single 'SKILL.md cycle' PV."* 同时关闭 4 条 reference must-fix（B-04..B-09）与 SKILL 工作捆绑 —— 因为 4 条都在同一个文件（`references/shell-proxy.md`），且 must-fix 包小到能与 SKILL 共享一个 PV。
- **PV-02 Reference 级联 SECOND** — 关闭锁定后续 PV 文档的 BLOCKER references（B-05 F-02 12-keys、B-06 F-03 lean-schema 重写）；同时与级联捆绑落地 T7 cache-layout 治理 v2（B-02 BLOCKER），因为 (a) F-02 是 T7 前置（治理规则扩展 A-2 之前文档必须反映 16-keys），(b) `tests/test_layout_invariant_multi_baseline.py` 覆盖与 PV-01 wholesale 重生相同的 baselines（零工作重复），(c) 9-ref refresh + cache-layout v2 ADR 按 v8.3.0 retro §4.2 多 ref 纪律可装入单个协调 PR。T4 + T5 前置（decomposition-gate §5.5；behavioral-guidelines body 扩展）一起搭车，因为是同 PR 内的仅追加 (additive-only) ref 编辑。
- **PV-03 A-5 编纂 THIRD** — 把 v8.4.0 涌现的 SSOT 注册表模式（`v9.0.0_gap_analysis.md` §C-02 + v8.4.0 retro §4.1 #5）正式化为 Architecture Rule A-5。与 PV-04 独立（除 `.cursor/rules/repo-governance.mdc` 外无共享文件）；选择在 PV-04 之前，是因为 A-5 是仅追加 (additive-only) Architecture 规则（无行为变化），而在 PV-05/PV-07 的规则 cap 纪律工作之前编纂它，可让 Soul-set + Architecture-set 增长审计更清爽。在任何后续注册表 refactor 使用之前，模式规则必须存在。
- **PV-04 Lifecycle 接线 FOURTH** — 关闭 3 个版本的 dead-wire（`lifecycle/` 100% 覆盖但无 caller，按 `v9.0.0_overlap_matrix.md` Finding #5）+ 5 周期 plan-mode 反复（按 C-12）。镜像 v6.0.3 dead-wire 闭合先例 — v6.0 单次最高 ROI 干预，按 `v9.0.0_perf_trajectory.md` §3 逐字。选择在 PV-03 之后，因为：(a) plan-mode 提示侧嵌入的新 Soul Rule S-10 跨过 C-09 保护的 Soul-set 边界；PV-03 的 A-5 编纂在跨 Soul-set 之前先建立 rule-addition 纪律；(b) `propose_merge`（M-004）消费 A-5 的 SSOT 注册表模式；(c) PV-04 需要 SKILL.md ≥ 3 行余量给 plan-mode-enforcement.md 指针 — PV-01 已回收 ≥ 17 行，足够。
- **PV-05 NineS hygiene + W-rules FIFTH（首个 MINOR）** — 关闭 4 个 A-items + 5 条 W-rules + deferred marker class。选择为首个 MINOR，因为：(a) W-16 wholesale-baseline-regen 规则把 PV-01 已实践的做法编纂下来（编纂在周期历史中已有参考实现）；(b) W-17 per-PV 测试 cap 规则需要在 PV-06 B3 翻转风险 +N 测试（5 个 `_disabled.yaml` 场景）之前存在（按 W-17 周期内审计抓超额）；(c) W-18..W-20 治理 W-rules 关闭 C-03 + C-05 + C-10 + C-11 CRITICAL+MAJOR 冲突，归入单一 MINOR；(d) NineS hygiene 闭合 A1-A4 可与 W-rules 编纂捆绑为单一 MINOR，因为两者都触动 `.rules/workflow.mdc` + `AGENTS.md` 重生路径；(e) `references/env-flags.md` 在此落地，作为按 Open Decision §1.7 #2 (a) 推荐的第 13 个 SF-4 reference。MINOR semver 信号是 NineS measurement 方法学对 operator 可见的行为变化。
- **PV-06 CompressionPipeline + B3 翻转 SIXTH（第二 MINOR）** — 捆绑 T3（仅追加 (additive-only) 协议 — R5 strict byte-identical-when-bypassed 契约的纯重构）与 T5（B3 5 原语 default-on 翻转 — AUDIT profile 用户协调默认行为变更）。选择在 PV-05 之后，因为：(a) W-17 per-PV 测试 cap 规则（PV-05 编纂）约束 +25 测试预测；(b) W-16 wholesale baseline 重生规则（PV-05 编纂）治理 5 个 `_disabled.yaml` baseline 集成；(c) `references/env-flags.md`（PV-05 交付）必须在 B3 翻转 CHANGELOG "Adoption notes" 能 cross-reference operator opt-out 路径前存在。T3 + T5 共享同一个 MINOR release，因为 T3 统一了 T5 原语全部操作的入口表面（B3 原语是 gate stages；T3 泛化所有 stages，包括未来 post-flip default-ON 情况）。
- **PV-07 v9.0.0 MAJOR LAST（规则重平衡 + rollup）** — 规则重平衡是对治理契约的破坏性变更（per-task-type AGENTS.md 切片），按 `v9.0.0_decomposition_analysis.md` §"Theme T6" 第 192 行逐字 *"breaking rule-surface semantics; opt-in cached-prefix users see selective slices"*；需要全部基础设施稳定。选择 LAST，因为：(a) Soul-set 冻结依赖 PV-04 的 S-10 ratify（冻结在 9 或 10 项）；(b) 规则数 cap ≤ 60 需要 PV-03 的 A-5 + PV-04 的 S-10 + PV-05 的 W-16..W-20 都已计入（50 + 7 = 57 ≤ 60）；(c) per-task-type 选择性需要 PV-02 reference 级联完成（selector 切片引用 `meta-framework.md` 22-template alias map）；(d) v9.0.0 的 wholesale baseline 重生跨 7 个先前 PV 的变更 — rollup release 是合适的位置；(e) MAJOR semver 约定要求背后有实质性累积变更；6 个先前 PV 提供了。
- **PV-08 v9.0.1 sustaining LAST（可选）** — 条件依赖 PV-07 SI-3 ≥ 9.0 留有余量、且 Open Decision §1.7 #11 = approve。Sustaining，因为：(a) `references/repo-modes.md` Codeberg 变体 + `+verify`/`+monitor` 行更新是 sustaining 编辑（不阻塞任何 v9.0.0 BLOCKER）；(b) Tier-2 企业 adapter 批次（JetBrains/Amazon Q/Gemini/Augment/Trae）按 `v9.0.0_improvements_zh.md` 优先级矩阵第 47 行是 P3 项；(c) 如 PV-07 SI-3 恰好落在 9.0（无余量）或 Open Decision §1.7 #11 拒绝，本工作自动延后处理 (deferred) 至 v9.1.0 PV-01。

如任一 PV 的 gate 失败，周期在该 PV 暂停。后续 PV 保持队列状态但不发布，直到失败 gate 收敛或按 W-8 / SI-9 reinforcement 循环升级（每轮最多 5 条 reinforcement 规则，按严重度过滤；如分数停滞 2+ 轮，按 P4 升级人工）。

## 回滚矩阵 (Rollback matrix)

| PV | 版本 | 回滚目标 | 可逆性 | Forward-compat 是否破坏？| 影响半径 / 策略 |
|---|---|---|---|---|---|
| PV-01 | v8.4.1 | v8.4.0 | LOW | NO | 回滚 SKILL.md 编辑 + 还原 `context_profiles.yaml` + `section_registry.py` 中的 section anchors；`references/plan-mode-enforcement.md` 删除是单文件；`references/shell-proxy.md` 中的 F-01/F-05/F-06/F-14 修复回到 PV-01 之前的字符串；SF-4 集合 12 → 11；baselines 回到 v8.4.0 baseline。Mirror sync 运行为 no-op 或从 canonical 重新同步。|
| PV-02 | v8.4.2 | v8.4.1 | MEDIUM | NO | 把 10 条 ref 编辑作为单个 PR 回滚；回滚 2 个 NEW CI 测试文件（`test_reference_frontmatter_freshness.py` + `test_reference_api_alignment.py`）；回滚 `test_layout_invariant_multi_baseline.py` + 3 个 NEW golden YAMLs；A-2 nest-vs-append 规则回到 A-2 v1；ADR-010 归档但不删除（按 SI-8）。|
| PV-03 | v8.4.3 | v8.4.2 | LOW | NO | 回滚 A-5 规则文件 + CI 测试（`test_registry_single_owner` + `test_dead_apis` parity）；现有 registries 在无规则强制下继续工作；`references/shell-proxy.md` §11 cross-link 回滚。|
| PV-04 | v8.4.4 | v8.4.3 | MEDIUM | NO | 回滚 `feedback.py::generate_round_dispatch` 中的 hook 接线（default-off 路径保留）；`DEFAULT_EVENTS` 6 → 5；回滚 `propose_merge`（`agent_workspace/archive.py`）与 REPORT.md 自动触发（`agent_workspace/reporter.py`）；S-10 Soul Rule 回滚（Soul-set 10 → 9）。Hook 接线测试保持绿色，因为 `strict=False` 宽松默认让 hook 缺席为 no-op。|
| PV-05 | v8.5.0 | v8.4.4 | HIGH | NO（仅追加 (additive-only)）| A1-A4 hygiene 修复不回滚，接受部分状态；NineS index 重建可通过 `make nines-index-rebuild` 用先前 fixture 回滚；`references/env-flags.md` 删除 + SF-4 集合 13 → 12 + SKILL.md −1 行；W-16..W-20 从 `.rules/workflow.mdc` + AGENTS.md 重生回滚；`@pytest.mark.deferred` marker class 从 `tests/conftest.py` + `pyproject.toml` 回滚。每 primitive 回滚可行但需仔细同步 `_SF4_REFERENCE_SET` 与 `MIRRORED_FILES`。|
| PV-06 | v8.5.1 | v8.5.0 | HIGH | **PARTIAL**（B3 翻转是 operator 可见的行为变更）| T3 CompressionPipeline 是仅追加 (additive-only) — 老的 3 表面入口保留（`compressor.py` + `llm_client.py` + `shell_proxy/commands.py` legacy callers）；只回滚 orchestrator。T5 B3 翻转是每 primitive — 通过 `context_profiles.yaml` 1 行 toggle 单独回滚；`_disabled.yaml` 场景保留（primitive 再次 OFF 也无害）。CHANGELOG "Adoption notes" 必须修订以反映回滚。可选 A3 reference（`compression-pipeline.md`）删除 + SF-4 集合 14 → 13 + SKILL.md 行移除。|
| PV-07 | v9.0.0 | v8.5.1 | VERY HIGH | **YES**（MAJOR semver 信号此点）| 规则退役级联是破坏性 — `.cursor/rules/devola-flow-rules.mdc` 与 `workflow-rules.mdc` 已退化为 stub，恢复需 PR + 下游通知 + version stamp 澄清（semver MAJOR 回滚到 v8.5.1 意味着下游必须重新 import 完整规则集合）。Per-task-type 选择性回滚：`task_adaptive_selector.py` 扩展回滚；`context_profiles.yaml` selector profiles 回滚；AGENTS.md 重生不带 selector annotations。SI-3 评估 + SI-8 retrospective 归档但不删除。`versions.json` v9.0.0 条目保留（按 ST-7 — 版本永不删除）。annotated `v9.0.0` git tag 成为 "回滚标记" — 后续 v9.0.1 显式承认它。|
| PV-08 | v9.0.1 | v9.0.0 | LOW | NO | 回滚 `references/repo-modes.md` 的 sustaining 编辑；OPTIONAL adapter 配置删除（`src/devolaflow/adapter_configs/` 5 个文件）；ADR-016 归档。本周期最低影响半径回滚（镜像 v7.1.0 rollup 回滚 profile）。|

PV-01..PV-06 + PV-08 全部回滚都包含在一个 patch/minor 版本步长内。**跨周期回滚**（如从 v9.0.0 回到 v8.4.0）需要顺序回滚所有中间版本 — 这是预期的，与 `semver.org` 对 MAJOR releases 的语义一致。v9.0.0 的回滚特别需要 PR + 下游通知，因为规则表面契约是有意破坏性的。

## 交叉引用 (Cross-references)

### v9.0.0 SI-1 前置工件（本路线图的主要输入）

- `.local/research/v9.0.0_gap_analysis.md`（616 行，9 章节，12 不变量，26 个 in-scope 项，8-PV 文件级范围，10 个风险；**并行 S05-T01 同胞 — 主要契约**）
- `.local/research/v9.0.0_decomposition_analysis.md`（280 行，8 主题 T1-T8，7-PV sketch，12 个 open decisions；S02-T04）
- `.local/research/v9.0.0_improvements_zh.md`（703 行，47 项 × 4 优先级桶，8-PV 映射表；S04-T02）
- `.local/research/v9.0.0_perf_trajectory.md`（330 行，7 个 KPI 表，13 个 v9.0.0 数值目标；S02-T03）
- `.local/research/v9.0.0_reference_review.md`（633 行，11-reference 裁定矩阵，15 个 F-NN 关键发现，5 BLOCKER + 10 CRITICAL；S03-T05）
- `.local/research/v9.0.0_contributions_zh.md`（523 行，125 个评分项；S04-T01）
- `.local/research/v9.0.0_overlap_matrix.md`（141 行，14 个 feature family × 4 裁定标签；S02-T01）
- `.local/research/v9.0.0_conflict_register.md`（276 行，14 个冲突 × 4 严重度等级 + 依赖图；S02-T02）
- `.local/research/v9.0.0_version_history.md`（2122 行，S01-T06 多版本回顾）
- `.local/research/v9.0.0_version_history_zh.md`（2071 行，ZH 翻译）

### 前代周期工件（风格先例 + 顺延单一事实源 (SSOT)）

- `.local/research/v8.4.0_retrospective.md` — 4-PV 账本；§3.2 9 项顺延；§4.1-§4.4 过程经验（本路线图全文逐字引用）
- `.local/research/v8.4.0_gap_analysis.md` — 4 项清单；周期不变量模板 I-1..I-10
- `.local/research/v8.4.0_evaluation.md` — 综合分 9.18/10
- `.local/research/v8.4.0_evobench_summary.md` — 逐场景 delta v8.3.0 → v8.4.0
- `.local/research/v8.4.0_nines.md` summary — 综合分 0.9050 字节稳定
- `.local/research/retrospective_v7.0_to_v7.1.md` — 早期 MAJOR 周期 retrospective 格式先例
- `CHANGELOG.md` `## [8.4.0]` rollup 条目 — "Adoption notes" 格式的周期先例

### 格式先例（本路线图镜像）

- v7.0.0 周期路线图（5-PV 周期，止于 v7.1.0）是 task brief 中提到的结构先例；该工件位于 v7.0.0 → v7.1.0 转换处（不在当前 `.local/research/` 快照中，但反映在 v8.4.0 retrospective + CHANGELOG 历史里）。

### 周期内将起草的 NEW v9.0.0 ADRs（共 8 个）

| Anchor | 标题 | 在哪个 PV 起草 | 文件路径 |
|---|---|---|---|
| ADR-009 | SKILL.md 余量回收 + R7 符号 anchor 迁移 | PV-01（v8.4.1）| `.local/research/adr/v9-ADR-001-skill-headroom-reclamation.md` |
| ADR-010 | Reference 刷新级联 + Cache-Layout 治理 v2 | PV-02（v8.4.2）| `.local/research/adr/v9-ADR-002-cache-layout-governance-v2.md` |
| ADR-011 | A-5 单一事实源注册表模式 | PV-03（v8.4.3）| `.local/research/adr/v9-ADR-003-a5-ssot-registry.md` |
| ADR-012 | Lifecycle 接线 + Soul Rule S-10 + M-004 + I-PV07-A | PV-04（v8.4.4）| `.local/research/adr/v9-ADR-004-lifecycle-wiring-and-s10.md` |
| ADR-013 | NineS Hygiene Bundle A1-A4 + W-16..W-20 + Deferred Marker | PV-05（v8.5.0）| `.local/research/adr/v9-ADR-005-nines-hygiene-and-w-rules.md` |
| ADR-014 | CompressionPipeline 协议 + B3 Default-On 翻转 + 多 pass filter chain | PV-06（v8.5.1）| `.local/research/adr/v9-ADR-006-compression-pipeline-and-b3-flip.md` |
| ADR-015 | 规则体系重平衡 + 按任务类型选择性 + Soul-Set 冻结 + 汇总（Rollup）| PV-07（v9.0.0）| `.local/research/adr/v9-ADR-007-rule-rebalancing-and-rollup.md` |
| ADR-016 | Sustaining 周期决策 | PV-08（v9.0.1，OPTIONAL）| `.local/research/adr/v9-ADR-008-sustaining.md` |

编号方案 **ADR-009..ADR-016** 延续先前 `.local/research/adr/v7-ADR-001..v7-ADR-005-*.md` 系列（v7.0.0 周期 5 个 ADR）；v8.x 周期未正式编号新 ADR（按 `.local/research/adr/` 列表）。文件名约定 `v9-ADR-NNN-*.md` 与 v7- 先例保持周期前缀一致。

### 本路线图引用的规则（逐字引用）

- `.cursor/rules/repo-governance.mdc` — Soul Rules S-1..S-9 + S-10 候选（PV-04）；Architecture A-1..A-4 + A-5 候选（PV-03）；A-2 nest-vs-append 扩展（PV-02）；Conventions C-1..C-9；Workflow W-1..W-15 + W-16..W-20 候选（PV-05）；Style ST-1..ST-13（PV-07 中引用 ST-7 versions.json append）。
- `.cursor/rules/self-improve-iteration-rules.mdc` — SI-1 至 SI-10（全文引用的完整周期协议）。
- `.cursor/rules/skill-format-rules.mdc` — SF-1 tier 预算（Default < 500，PV-01 验收中引用）；SF-3 7 个 canonical 同步位置（version-bump 表中引用）；SF-4 valid reference 列表（当前 11；v9.0.0 候选 12 [PV-01] + 13 [PV-05] + 14 [PV-06 可选]）。
- `AGENTS.md` — 自动生成的 compiler 输出；PV-07 重生时带 selectivity annotations。

### 引用的 schemas + cache 不变量

- `schemas/lean-dispatch.yaml#layout_invariant.canonical_order` — 版本 5，长度 16；PV-02 治理 v2 仅编纂 nest-vs-append 扩展（无 top-level 迁移）。
- `schemas/lean-report.yaml`
- `schemas/agent-workspace/*.yaml`（10 个 schemas）
- `schemas/memory-case.yaml` — F-05 BLOCKER 单一事实源 (SSOT)，给 PV-01 引用的 `MemoryCase` 字段列表。
- `schemas/command-mapping.yaml` — F-06 BLOCKER 单一事实源 (SSOT)，给 PV-01 引用的 `replacement:` 字段名；PV-06 中 T3 #5 schema 1 → 2 候选。
- `schemas/compression-pipeline.yaml` — NEW PV-06（T3 交付物）。

### 外部 canonical URLs（按 Rule S-7）

- DevolaFlow source: `https://github.com/YoRHa-Agents/DevolaFlow`
- NineS source: `https://github.com/YoRHa-Agents/NineS`
- RTK source: `https://github.com/rtk-ai/rtk`（仅在 PV-01 must-fix 中引用，用于移除 v8.4.0 绝对路径违规；运行时路径不在任何 agent-facing 文件中硬编码）

### 方法论备注

- Theme-to-PV 映射遵循 `.local/research/v9.0.0_gap_analysis.md` §5（契约工件）作为每 PV 的 theme 分配权威。本路线图的 PV 排序与 user-task-brief 示例模板（建议 v8.5.0=Compression Pipeline / v8.5.1=B3 翻转+A1-A4 / v8.5.2=Cache-layout v2）有差异，差异旗帜逐字记录于此：本路线图遵循 gap_analysis §5 映射（PV-02=Reference Cascade+T7 BLOCKER 闭合；PV-05=NineS hygiene+W-rules；PV-06=CompressionPipeline+B3 翻转），因为 (a) gap_analysis 是 SI-1 sign-off 的正式契约，(b) PV-02 cache-layout v2 时机要求在 PATCH-only 窗口内关闭 BLOCKER C-01（否则会滑到 MINOR PV-05，违反 BLOCKER 修复的 PATCH 语义），(c) 把 NineS hygiene 与 W-rules 捆绑到单一 MINOR（PV-05）保留按 Open Decision §1.7 #8 (a) 推荐的 byte-stable-≥6-measurements 单次验证，以及 (d) 在 PV-06 捆绑 T3 + T5 镜像 gap_analysis 的建议 —— T5 5 原语翻转受益于 T3 协议的 stage-level bypass 条件（R5 strict）。
- 所有 findings 通过 section anchor + 行号区间 cross-reference 至前置工件；按 Rule C-3 / C-2 保留逐字 (verbatim) 引用。
- 所有路径按 Rule S-2 / SF-5 repo-relative；外部 benchmarks/tools 仅按 Rule S-7 用 GitHub URL 引用。
- 8-PV 计数匹配 `v9.0.0_gap_analysis.md` §1.2 第 69 行逐字 *"Cycle size estimate: 8 PVs = 4 PATCH (PV-01..PV-04) + 2 MINOR (PV-05, PV-06) + 1 MAJOR (PV-07) + 1 optional sustaining PATCH (PV-08)"*。PV-07 是 v9.0.0 MAJOR rollup；PV-08 是可选 v9.0.1 sustaining。
- LOC 估算是按 `v9.0.0_improvements_zh.md` line-item LOC 加 10% buffer 的 forecast；最终 PV STATUS 报告必须按 W-17 候选（PV-05 编纂）核对实际值。

*v9.0.0 SI-1 版本路线图结束。下一个消费者：`.local/research/v9.0.0_design.md`（按 v8.4.0 周期先例延后处理 (deferred) 至 PV-01 实施时）。所有路径按 Rule S-2 / SF-5 repo-relative；按 Rule C-3 / C-2 保留逐字 (verbatim) 引用；按 Rule S-5 无 silent failure；外部资源仅按 Rule S-7 用 GitHub URL 引用。Companion artifact：`.local/research/v9.0.0_gap_analysis.md`（并行 S05-T01）— 本路线图按 task brief 把 gap_analysis §5 文件级范围作为权威。*
