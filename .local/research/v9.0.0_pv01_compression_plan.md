# v9.0.0 PV-01 — SKILL.md Compression Plan + R7 Migration + Must-Fix Bundle

> **S02 Design artifact** for `feat/v8.4.1-skill-headroom-reclamation`. Feeds S03
> implementation (T04..T09). Target: SKILL.md 499 → ≤ 480 lines, +1 Tier-2 nav
> row for `references/plan-mode-enforcement.md` (NEW), +1 Tier-2 nav row for
> `references/behavioral-guidelines.md` (F-04 closure), R7 line-anchor →
> symbolic-anchor full migration, 4 shell-proxy must-fixes (F-01/F-05/F-06/F-14),
> wholesale EvoBench baseline regen, G-01 + G-02 erratum.
>
> **Authoritative:** `.local/research/v9.0.0_implementation_plan.md` §6.1 +
> `.local/research/v9.0.0_gap_analysis.md` §5.1 + `.local/research/v9.0.0_reference_review.md` §F-01/F-05/F-06/F-14.

## 1. SKILL.md compression — line-by-line plan

**Pre-state:** `wc -l workflow-system/agent/SKILL.md = 499`. Target ≤ **480**
(stretch — clears ≥ 19-line headroom; HARD ≤ 500 invariant per I-6).

### 1.1 §"Mode Awareness" — `### PLAN MODE` subsection extraction

**Current (lines 59-119, 61 lines)** — header + L0 description + 45-line plan
template fenced block + 8-bullet rules block. Cost: ~820 tokens (per
`workflow-system/agent/context_profiles.yaml` `plan_mode_template`).

**Action:** Extract the entire 45-line plan template + 8-bullet rules to NEW
`workflow-system/agent/references/plan-mode-enforcement.md` §"Plan Output
Template" + §"Plan Mode Rules". Replace SKILL.md §"### PLAN MODE" body with a
4-line compressed summary that preserves the "what" (delegation contract,
L0-only operating mode, do-not-implement contract) and routes to the new
reference for the "how".

**Post-state (≈ 5 lines):**

```text
### PLAN MODE — Design the Plan, Do NOT Execute

**You are L0 (Project Agent).** Design the execution plan as a delegation contract; L0 dispatches → L1 → L2 → L3 (only L3 implements). Plan output template (title → overview → execution model → stages → waves → tasks), Constraints Checklist, Invariants P1–P5, and DO / DO NOT rules: see `references/plan-mode-enforcement.md`.
```

**Reclaim:** 61 → 5 = **56 lines reclaimed**.

### 1.2 §"### Reinforcement Rules (v5.1+)" extraction

**Current (lines 318-322, 5 lines + trailing blank = 6 lines)** — header +
mechanism description + L3 obligation note.

**Action:** Move full mechanism description + L3 obligation matrix +
convergence-loop coupling to `references/plan-mode-enforcement.md`
§"Reinforcement Rules (W-8 / SI-9)". Replace SKILL.md body with 1-sentence
summary + cross-ref pointer.

**Post-state (≈ 3 lines):**

```text
### Reinforcement Rules (v5.1+)
When a stage gate FAILS, next round's dispatch carries top-5 prior-round findings (severity ≥ major) as `applicable_rules.reinforcement` MUST-fix mandates; L3 MUST address all before any new work (failure = automatic blocker next gate). Mechanism + L3 obligation matrix: `references/plan-mode-enforcement.md` §"Reinforcement Rules".
```

**Reclaim:** 6 → 3 = **3 lines reclaimed**.

### 1.3 Tier-2 nav table — F-04 closure (+1 row) + plan-mode-enforcement.md (+1 row)

**Current (lines 411-426)** — `## Reference Navigation Guide` + Tier-2 sub-table
listing 10 references (missing `behavioral-guidelines.md` which is the 9th SF-4
canonical per Rule SF-4; orphaned per F-04 finding). Need to insert 2 new rows
in alphabetical-ish position:

- `references/behavioral-guidelines.md` | L3 think_first / simplicity_check / surgical_scope / goal_loop primitives
- `references/plan-mode-enforcement.md` | Plan-mode L0 contract, plan output template, reinforcement rules, convergence loop

**Insertion delta:** +2 lines.

### 1.4 Net SKILL.md delta

| Section                              | Pre | Post | Reclaim |
| ------------------------------------ | --- | ---- | ------- |
| §"### PLAN MODE" body                | 61  | 5    | 56      |
| §"### Reinforcement Rules"           | 6   | 3    | 3       |
| §"Reference Navigation Guide" Tier-2 | +0  | +2   | -2      |
| **Total**                            | —   | —    | **+57** |

**Final SKILL.md line count:** 499 − 57 = **442 lines** (well under 480 stretch
target; 58-line headroom against 500 HARD ceiling). Conservative buffer to
absorb later v9.0.0 surface additions in PV-02 (revised Load-When cells per
F-04/F-12/F-13 cascade) without breaching ceiling.

## 2. NEW `workflow-system/agent/references/plan-mode-enforcement.md` skeleton

**Tier:** 2 (Large tier ≤ 1000 lines per SF-1). **Target:** ~400 LOC. **ID:**
`plan-mode-enforcement`. **Frontmatter** (per Rule SF-2 / C-5):

```yaml
---
id: plan-mode-enforcement
version: "8.4.1"
purpose: >
  Plan-mode L0 operating contract: when a dispatcher detects plan mode, this
  reference defines the canonical plan output template, plan-mode rules,
  reinforcement-loop mechanics, and stagnation-escalation behavior. Pairs with
  the W-8 / SI-9 convergence-round reinforcement primitive in
  src/devolaflow/gate/reinforcement.py.
tier: 2
token_estimate: 3200
last_updated: "2026-04-23"
---
```

**Section structure (8 sections, ~400 LOC):**

1. **§1 When to Load** — Triggers (system_reminder "Plan mode is active",
   `SwitchMode` mode=plan, explicit user "build a plan" / "design first" /
   "/plan"), AGENTS.md mode-selection trigger row, env flag
   `DEVOLAFLOW_PLAN_MODE=1`, filesystem marker `.devolaflow_plan_mode`. ~30 LOC.
2. **§2 Plan Mode Detection (priority order)** — verbatim from SKILL.md §"Mode
   Awareness" Detection block (4 ordered conditions); v6.1.5+ runtime hook
   (`select_context(plan_mode=True)`); section-priority overrides (escalates
   `agent_hierarchy` / `decomposition_gate` / `rationalization_prevention` to
   `critical`); `model_hint_override: quality`; `compression_intensity: minimal`.
   ~40 LOC.
3. **§3 Plan Output Template** — verbatim 45-line fenced template lifted from
   SKILL.md §"### PLAN MODE — Design the Plan, Do NOT Execute" `Plan output
   format:` block (title → Overview → Execution Model → Stages × {gate_type,
   threshold, coverage, max_rounds, on_stagnation, context_profile,
   deliverables, L1_receives} → Waves × {parallel|sequential|...} → Tasks ×
   {Type, Team, Writable, Read-only, Est., AC} → Constraints Checklist → P1–P5
   Invariants). ~70 LOC.
4. **§4 Constraints Checklist (verbatim from SKILL.md)** — 9 checkbox items
   (every task row L3, stage headers L1 constraints, execution model present,
   wave ≤5 tasks pairwise disjoint writable, stage ≤7 waves, task limits,
   stage DAG no cycles, convergence stages with max_rounds + stagnation rule,
   predecessors by artifact path). ~25 LOC.
5. **§5 Plan Mode Rules — DO and DO NOT** — verbatim 8 bullets from SKILL.md
   §"PLAN MODE rules" + 4 expanded DO NOT items per F-04 + C-12 best-practice
   cluster (do not over-decompose, do not embed tool calls, do not select model
   tier without rationale, do not skip Constraints Checklist verification).
   ~40 LOC.
6. **§6 Reinforcement Rules (W-8 / SI-9) — Mechanism + L3 Obligation** —
   verbatim from SKILL.md §"### Reinforcement Rules (v5.1+)" header + L3
   obligation; expanded with: `applicable_rules.reinforcement` payload shape
   (max 5 entries, severity-filtered ≥ major, deduplication policy);
   `findings_to_reinforcement()` Python API surface in
   `src/devolaflow/gate/reinforcement.py`; `ProposalGenerator.generate_round_dispatch()`
   integration in `src/devolaflow/feedback.py`; round-N+1 dispatch payload
   shape; round-aware escalation table (`_ROUND_ESCALATION_DEFAULTS`). ~70 LOC.
7. **§7 Convergence Loop Mechanics** — Gen-Verify loop verbatim from SKILL.md
   §"Wave Coordination Modes" → Gen-Verify section; max_rounds defaults per
   `gate_profiles` (`relaxed=3`, `standard=5`, `strict=5`, `audit=7`);
   stagnation detection (composite_score Δ < epsilon over 2 rounds);
   reinforcement carrying forward across rounds; verifier criteria sourcing
   from `acceptance_criteria`. ~60 LOC.
8. **§8 Stagnation Escalation (W-8 P4 bounded retry)** — Stagnation triggers
   (no improvement 2+ rounds, max_rounds reached, blocker introduced); P4
   classification (retry / escalate / abort); escalation chain (Task → Wave →
   Stage → Project → Human); per-team dispatch escalation; ExceptionEscalation
   schema usage. ~35 LOC.
9. **§9 Cross-References** — SKILL.md §"Mode Awareness" + §"Reinforcement
   Rules" + §"Wave Coordination Modes" + §"Gate Mechanism";
   `src/devolaflow/gate/reinforcement.py` (W-8 module, ~227 LOC at v8.4.0);
   `src/devolaflow/feedback.py::ProposalGenerator.generate_round_dispatch`;
   `src/devolaflow/task_adaptive_selector.py::_PLAN_MODE_OVERRIDES`; AGENTS.md
   §"W-8 — Convergence Round Reinforcement (SI-9)" + §"P4 Bounded Retry";
   `.cursor/rules/repo-governance.mdc`. ~30 LOC.

**Total estimate:** 400 LOC ± 50.

## 3. R7 line-anchor → symbolic-anchor full migration

**Carry-forward source:** v8.0.0 P-08 retro §3.3 ("R7 carried-forward debt") +
v8.2.0 PV-05 partial migration (only `behavioral_guidelines_reference` registered
in `section_anchors:`). Target: extend the registry to cover **every** SKILL.md
section currently using deprecated `lines: "X-Y"` lookup, eliminating the
deprecation-warning fall-through path that fires for `frontmatter`,
`version_update`, ad-hoc `sec1`/`sec2` test anchors, and the rest of the
`sections:` block.

### 3.1 `workflow-system/agent/context_profiles.yaml` — `section_anchors:` block extension

**Insertion location:** lines 219-222 (after the existing
`behavioral_guidelines_reference` entry; preserves the cached-prefix layout —
only adds new keys after the existing one). Each new entry is the SHORT form
(`<anchor>: <file_path>`) with EXTENDED form (`{file, heading}`) only when the
SKILL.md heading text differs from `snake_case → Title Case` derivation.

**24 new anchors covering every `sections:` key + the deprecation-test
anchors:**

| Anchor                       | Form     | File / heading                                                                |
| ---------------------------- | -------- | ----------------------------------------------------------------------------- |
| `frontmatter`                | Extended | `workflow-system/agent/SKILL.md` heading: (top-of-file metadata; no heading)  |
| `version_update`             | Extended | `workflow-system/agent/SKILL.md` heading: `## Version & Update`               |
| `quick_action_decision`      | Short    | derives `Quick Action Decision`                                               |
| `mode_detection`             | Extended | `workflow-system/agent/SKILL.md` heading: `## Mode Awareness`                 |
| `plan_mode_template`         | Extended | `workflow-system/agent/references/plan-mode-enforcement.md` (NEW; full body)  |
| `agent_mode_protocol`        | Extended | `workflow-system/agent/SKILL.md` heading: `### AGENT MODE — Full Orchestration` |
| `quick_start_workflows`      | Extended | `workflow-system/agent/SKILL.md` heading: `## Quick Start — Workflow Selection` |
| `hierarchy_table`            | Extended | `workflow-system/agent/SKILL.md` heading: `## 4-Layer Agent Hierarchy`        |
| `hierarchy_diagram`          | Extended | `workflow-system/agent/SKILL.md` heading: `## 4-Layer Agent Hierarchy`        |
| `wave_task_constraints`      | Extended | `workflow-system/agent/SKILL.md` heading: `## 4-Layer Agent Hierarchy` (sub) |
| `rationalization_prevention` | Short    | derives `Rationalization Prevention`                                          |
| `wave_coordination`          | Extended | `workflow-system/agent/SKILL.md` heading: `### Wave Coordination Modes`      |
| `stage_primitives`           | Extended | `workflow-system/agent/SKILL.md` heading: `## Stage Primitives Index`        |
| `gate_mechanism`             | Extended | `workflow-system/agent/SKILL.md` heading: `## Gate Mechanism`                |
| `agent_teams`                | Extended | `workflow-system/agent/SKILL.md` heading: `## AgentTeam Quick Reference`     |
| `context_isolation`          | Extended | `workflow-system/agent/SKILL.md` heading: `## Context Isolation`             |
| `dispatch_report`            | Extended | `workflow-system/agent/SKILL.md` heading: `## Dispatch & Report Protocol`    |
| `lifecycle_hooks`            | Extended | `workflow-system/agent/SKILL.md` heading: `## Lifecycle Hooks`               |
| `repo_mode`                  | Extended | `workflow-system/agent/SKILL.md` heading: `## Repo Mode Detection`           |
| `reference_navigation`       | Extended | `workflow-system/agent/SKILL.md` heading: `## Reference Navigation Guide`    |
| `rules_dispatchers`          | Extended | `workflow-system/agent/SKILL.md` heading: `## Reference Navigation Guide`    |
| `convergence_loop`           | Extended | `workflow-system/agent/references/plan-mode-enforcement.md` heading: `## Convergence Loop Mechanics` |
| `task_quality_score`         | Extended | `workflow-system/agent/SKILL.md` heading: `## Task Quality Score`            |
| `template_quick_ref`         | Extended | `workflow-system/agent/SKILL.md` heading: `## Template Quick-Reference`      |

**Notes:**

- `nines_advisor` keeps its `lines: "N/A"` legacy entry — the value `"N/A"`
  yields `""` (no content) by design, so no anchor needed.
- `frontmatter` is special — heading-based discovery returns `""` since the
  YAML block is not a markdown heading. Acceptable: callers handle the empty
  return per S-5 (deliberate skip signal).

### 3.2 `src/devolaflow/section_registry.py` — no API change required

The PV-05 design is already symbolic-anchor-first; the deprecation fall-through
in `_resolve_section_text` (`task_adaptive_selector.py` lines 264-273) only
fires for anchors **not registered** in `section_anchors:`. Once §3.1 lands,
the fall-through becomes a quiescent safety net — no runtime change to
`section_registry.py` itself. The fall-through is preserved for backward
compatibility (any consumer that injects a custom `sections:` block gets the
same legacy path). Per W-9 / SI-10 step 5 (`pytest tests/test_benchmarks.py
-v`) the wholesale baseline regen captures the new symbolic-anchor scoring.

## 4. shell-proxy.md must-fixes (F-01 / F-05 / F-06 / F-14)

### 4.1 F-01 (line 654) — DROP absolute path

**Current line 654:**

```text
- **Cycle plan:** `/root/.cursor/plans/v8.4.0_rtk_+_memory_router_e4ec076d.plan.md`
```

**Action:** REMOVE the entire bullet. The cycle plan is operator-local under
`.cursor/plans/` (gitignored — never reachable by readers). Replacement
context already covered by `**SI-1 gap analysis:**`,
`**SI-2 NineS analysis on RTK:**`, and `**Per-PV evaluations:**` bullets
immediately below.

### 4.2 F-05 (lines 317-332) — REWRITE `MemoryCase` dataclass to match `cache.py:91-130`

**Current:** Decorator `@dataclass(frozen=True, slots=True)`; fabricated fields
`dispatch_template: str` + `expected_savings_pp: int`; missing required field
`summary: str`; uses `tags: tuple[str, ...] = ()` literal initializer.

**Replacement (verbatim from `src/devolaflow/memory_router/cache.py:91-136`):**

```python
@dataclass(frozen=True)
class MemoryCase:
    case_id: str
    workflow_type: str
    task_type: str
    summary: str           # one-sentence verbatim summary, ≤ 160 chars
    recipe_path: str       # MUST start with .local/memory/cases/
    version_stamp: str     # MUST equal devolaflow.__version__
    ttl_days: int = DEFAULT_TTL_DAYS
    last_accessed: str = ""    # ISO-8601 yyyy-mm-dd
    last_updated: str = ""     # ISO-8601 yyyy-mm-dd
    repo_signal: str = ""
    tags: tuple[str, ...] = field(default_factory=tuple)
```

### 4.3 F-06 (lines 430-435) — REPLACE `note:` with `replacement:`

**Current §6.3 example fenced block:** `pre_filters` and `post_filters` use
`note:` keys (NOT in schema for filter items per
`schemas/command-mapping.yaml:170-198` — only top-level `external_reference.note`
exists in scope).

**Action:** Two `note:` keys → `replacement:` keys with realistic substitution
strings.

```yaml
pre_filters:
  - pattern: "^DeprecationWarning:.*$"
    replacement: ""    # drop matched lines outright (verbatim re.sub semantics)
post_filters:
  - pattern: "^.*\\[\\*\\] [0-9]+ fixable.*$"
    replacement: ""    # drop ruff "[*] N fixable" hints from final output
```

### 4.4 F-14 (lines 292-307) — REWRITE §5.2 activation example

**Current:** `case.dispatch_template` and `case.expected_savings_pp` raise
AttributeError against the actual `MemoryCase` (cascade from F-05).

**Replacement attributes:** `case.summary` (replaces `dispatch_template` —
operators consume the verbatim summary), `case.recipe_path` (the canonical
markdown body holding the dispatch shape), `case.version_stamp` (replaces
`expected_savings_pp` — the cycle-stamp invalidation gate).

**Replacement example:**

```python
from devolaflow.memory_router import lookup_case

case = lookup_case(
    workflow_type="full-pipeline",
    task_type="implement",
    repo_signal=None,                # optional namespace narrowing
)
if case is not None:
    # Hit — short-circuit the planning re-derivation. Operators load the
    # verbatim recipe body from case.recipe_path; the summary + version
    # stamp are the L0 audit trail for which recipe routed which dispatch.
    summary = case.summary
    recipe_path = case.recipe_path
    version_stamp = case.version_stamp
else:
    # Miss — fall through to existing planner (R5 strict safe path)
    dispatch_template = derive_from_skill_md(...)
```

## 5. G-01 + G-02 erratum

**G-01:** `v9.0.0_gap_analysis.md` line ~244 mentions
`v9-ADR-001-cache-layout-governance-v2.md` (collision with PV-01's NEW
`v9-ADR-001-skill-headroom-reclamation.md`). **Action:** rename the
gap_analysis reference to `v9-ADR-002-cache-layout-governance-v2.md`.

**G-02:** `v9.0.0_gap_analysis.md` body cites `Open Decision §1.7 #N` (8
references) but the source-of-truth open decisions live in
`decomposition_analysis.md §"Open Decisions for v9.0.0 SI-1"` (12 items, indices
#1–#12). **Action:** `sed -i 's/Open Decision §1\.7 #\([1-8]\)\b/Open Decision
§9.2 #\1/g'` — the §9.2 anchor maps to the renamed
`improvements_zh.md §"人类决策项"` cluster (lines 601-676 per gap_analysis line
96). Indices #10/#11 explicitly retarget to
`decomposition_analysis.md §"Open Decisions for v9.0.0 SI-1" #10/#11` (manual
edit since the sed above doesn't catch them).

Both fixes recorded in `.local/research/v9.0.0_erratum.md` ledger (NEW, ~30
LOC).

## 6. Test surface

### 6.1 Existing test updates

- `tests/test_integration.py::test_skill_md_under_500_lines` — assertion
  changes from `< 500` to `< 500` (HARD ceiling unchanged); kept as the
  enforcement test. New stretch test below.
- `tests/test_no_ghost_features.py::_SF4_REFERENCE_SET` — add
  `"plan-mode-enforcement.md"` (11 → 12).
- `scripts/sync_cursor_skill.py::MIRRORED_FILES` — add
  `"references/plan-mode-enforcement.md"` (15 → 16). Update header comment
  block to extend the historical chain (v9.0.0 PV-01 grew this set 15 → 16).
- `tests/test_version.py::_MIRRORED_SKILL_FILES` — add
  `"references/plan-mode-enforcement.md"` (15 → 16). Update header comment.
- `tests/test_benchmarks.py::test_runner_prefers_latest_baseline` — bump
  expected newest baseline to `v9.0.0_baseline.json`.
- `tests/test_adapter_golden.py::test_cursor_references_golden` — bump
  expected reference count `len(actual) == 11` → `len(actual) == 12`.

### 6.2 NEW tests

- `tests/test_no_ghost_features.py::test_reference_skill_md_tier2_parity` —
  parses SKILL.md `## Reference Navigation Guide` → Tier-2 sub-table, extracts
  the set of `references/*.md` paths, asserts it equals
  `_SF4_REFERENCE_SET` (12 entries post-PV-01). Closes F-04 SKILL.md ↔ SF-4
  parity gap permanently.

### 6.3 Wholesale baseline regen (W-16 candidate, T12)

Per the v8.3.0 PV-09 commit `12f4ea8` precedent:
`benchmarks/devolaflow_context/baselines/v7.8.0_baseline.json` +
`v8.3.0_baseline.json` + `v8.4.0_baseline.json` regenerated against the
post-rebase SKILL.md state. NEW `v9.0.0_baseline.json` for the v9.0.0 cycle
tracker. Use `python -m benchmarks.devolaflow_context.generate_baseline
--output benchmarks/devolaflow_context/baselines/v<X>_baseline.json` for each.
Two-view diff (raw drift vs post-rebase) archived to
`.local/research/v9.0.0_evobench_summary.md`.

### 6.4 W-9 / SI-10 6-step verification

Per `AGENTS.md` §"W-9 — Test-Then-Commit Protocol (SI-10)", all 6 must pass
before commit:

1. `python -m pytest tests/ -q`
2. `ruff check src/ tests/`
3. `ruff format --check src/ tests/`
4. `python -m pytest tests/test_version.py -v`
5. `python -m pytest tests/test_benchmarks.py -v`
6. `make check-cursor-skill`

Plus the §6.1.5 PV-01-specific verbatim grep checks (rg `/root/`, rg `note:`,
rg `replacement:`, rg `Open Decision §1\.7`).

## 7. ADR + erratum ledger + CHANGELOG

- `.local/research/adr/v9-ADR-001-skill-headroom-reclamation.md` — NEW (~200
  LOC) — Context / Decision / Rationale / Consequences / Alternatives /
  References. Codifies the SKILL headroom reclamation pattern as a v9-cycle
  precedent for any future SKILL.md ceiling crisis (R-2 mitigation pattern).
- `.local/research/v9.0.0_erratum.md` — NEW (~30 LOC) — G-01 + G-02 closure
  ledger.
- `CHANGELOG.md` — add `## [8.4.1]` entry above `## [8.4.0]` mirroring the
  v8.4.0 entry format (Highlights → Files Changed → Cross-references).

## 8. Definition of Done

- `wc -l workflow-system/agent/SKILL.md` ≤ 480
- `pytest tests/test_no_ghost_features.py::test_reference_skill_md_tier2_parity -v` PASS
- All 4 shell-proxy must-fixes verified by verbatim grep
- 7 canonical version-sync locations bumped to 8.4.1
- W-9 / SI-10 6/6 PASS
- Annotated tag `v8.4.1` pushed
- ADR-001 + erratum ledger + evobench summary + plan-mode-enforcement.md committed
- SI-3 ≥ 8.5; NineS ≥ 0.85
