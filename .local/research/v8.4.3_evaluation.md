# v8.4.3 — SI-3 Evaluation Report

> Per W-3 / SI-3 (`.cursor/rules/self-improve-iteration-rules.mdc`):
> every pre-release requires a weighted-composite evaluation across 6
> dimensions; threshold for ACCEPT is composite ≥ 8.5/10. Below threshold
> → iterate (loop back to SI-1) or escalate to human.
> Cycle: v9.0.0 PV-03 (PATCH `v8.4.3`).
> Date: 2026-04-24.

## Composite Score (Weighted)

| Dimension | Weight | Score | Weighted | Rationale |
|-----------|--------|-------|----------|-----------|
| **Code quality** | 0.20 | 9.5 | 1.90 | Lint clean (`ruff check src/ tests/` 0 errors); format clean (`ruff format --check` clean); 1 NEW Python module surface (`scripts/detect_dead_apis.py::SSOT_REGISTRY_QUALIFIED_NAMES` + `_check_allowlist_domain_overlap` + import-time guard) with proper docstrings + cross-references to A-5.1/A-5.2 + ADR-003. Soul Rule S-5 honored: import-time guard raises `AssertionError` with verbatim A-5.2 citation rather than swallowing the violation. Helper `_check_allowlist_domain_overlap(allowlist, registry_names)` returns the intersection set; trivial set algebra, deterministic, easily re-usable. |
| **Architecture rationality** | 0.20 | 9.5 | 1.90 | A-5 codified per the same "informal-practice → binding architecture rule" pattern that ADR-002 promoted for nest-vs-append: 5 SSOT registries had been organised under the discipline since v8.2.1 but not lifted to a binding rule. PV-03 lifts it without reshaping any existing module. The 3 sub-clauses (A-5.1 single-owner, A-5.2 allowlist hygiene, A-5.3 staged rollout) cleanly separate orthogonal concerns. The two CI guards check the rule from independent angles (AST source-of-truth uniqueness vs allowlist-overlap)  — a test bypass on one does not bypass the other. ADR-003 explicitly NESTs A-5 sub-clauses per the v9-ADR-002 D3 decision rule (rationale documented in `v9-ADR-003 §"Why nest A-5 sub-clauses…?"`). |
| **Test adequacy** | 0.20 | 9.0 | 1.80 | +3 NEW tests landed (`test_registry_single_owner` + `test_default_allowlist_no_ssot_overlap` + `test_check_allowlist_domain_overlap_helper_detects_collisions`); 100% pass rate (3282/3282 total tests pass, 17 skipped, 2 xfailed); the AST helper (`_module_level_definers` + `_node_defines_symbol`) is exercised across 3 SSOT symbols × 95 src files (~285 AST walks per test invocation, sub-second). The DEFAULT_ALLOWLIST helper has both a positive-case test (synthetic collision) AND a negative-case test (no overlap), plus an implicit "default registry_names argument resolves to SSOT_REGISTRY_QUALIFIED_NAMES" assertion. Test cap honored: ≤ +5 forecast → 3 actual. |
| **Maintainability** | 0.15 | 9.5 | 1.425 | All paths repo-relative (S-2 / SF-5 honored); `.rules/architecture.mdc` A-5 source ~+56 lines; `RuleCompiler.compile_all()` auto-mirrors to both `cursor` (8000-token budget) and `agents_md` (6000-token budget) without hand-edit drift. ADR-003 (423 LOC) follows the v9-ADR-001/002 structural template (Status / Context / Decision D1-D4 / Rationale / Consequences / Alternatives Considered / Migration / Test Plan / Cross-References). The 5-row SSOT registry table appears verbatim in 4 places (`.rules/architecture.mdc`, `.cursor/rules/repo-governance.mdc`, `AGENTS.md`, ADR-003) — 3 of those are auto-mirrored, only the ADR is hand-authored. |
| **Compatibility** | 0.10 | 10.0 | 1.00 | I-8 invariant fully preserved: `schemas/lean-dispatch.yaml#layout_invariant.version = 5` (unchanged), `canonical_order` length 16 (unchanged), all 16 keys in same order (unchanged). PV-03 is a `.rules/` + `tests/` + `scripts/` patch with NO schema or runtime touch. All 6 backward-compat byte-baselines (v7.0.0 / v7.3.0 / v8.0.0 P-08 / v8.0.0 P-10 / v8.3.0 PV-05 / v8.4.0) continue to pass via `tests/test_layout_invariant_multi_baseline.py`. R-3 not triggered → A-5 ships strict with no migration required; existing 188-entry `DEFAULT_ALLOWLIST` is unchanged. |
| **Performance impact** | 0.15 | 9.0 | 1.35 | Two new CI guards run only at test / script-import time. Production dispatch paths byte-identical to v8.4.2. The import-time `AssertionError` guard is a single `frozenset.intersection` of size O(3) × O(188) — negligible (<1 µs). `test_registry_single_owner` AST-walks 95 source files for 3 symbols (~285 walks, sub-second). EvoBench ran clean: 36/36 benchmark tests pass; composite scores hold within ±5pp of `v9.0.0_baseline.json` floor per W-4. No regression in any benchmark scenario. |

**Composite (weighted sum)**: **1.90 + 1.90 + 1.80 + 1.425 + 1.00 + 1.35 = 9.375 / 10**

**Verdict**: **ACCEPT** (≥ 8.5 threshold per SI-3 + W-3).

## Findings closure summary

The 1 BLOCKER (C-02) + 1 reference finding (F-13 cross-link surface) targeted by PV-03 are both closed:

| Finding | Severity | Source | Closure | Verification |
|---------|----------|--------|---------|--------------|
| **C-02** | BLOCKER | `v9.0.0_gap_analysis.md` §5.3 (Theme T2) | A-5 codified in `.rules/architecture.mdc` + auto-mirrored to `.cursor/rules/repo-governance.mdc` + `AGENTS.md`; ADR-003 (423 LOC); 2 CI guards (`test_registry_single_owner` + DEFAULT_ALLOWLIST parity) | `pytest tests/test_no_ghost_features.py::test_registry_single_owner -v` (PASS); `pytest tests/test_dead_apis.py -v` (14 PASS, +3 NEW); `python scripts/detect_dead_apis.py --strict` exit 0 |
| **F-13** | CRITICAL | `v9.0.0_reference_review.md` (cross-link surface) | `references/shell-proxy.md` NEW §11 "Registry SSOT (A-5)" cross-links the 3 stack-local registries with per-registry consumer lists | `rg "shell_proxy/registry.py" workflow-system/agent/references/shell-proxy.md` ≥ 1 hit; `rg "## 11. Registry SSOT" ...` = 1 hit |

## CI verification harness — all PASS

```bash
$ python -m pytest tests/ -q
3282 passed, 17 skipped, 2 xfailed in 48.06s

$ ruff check src/ tests/
All checks passed!

$ ruff format --check src/ tests/
184 files already formatted

$ python -m pytest tests/test_version.py -v
12 passed, 17 skipped in 0.04s

$ python -m pytest tests/test_benchmarks.py -v
36 passed, 18 warnings in 17.65s

$ make check-cursor-skill
[.cursor mirror] not present at .cursor/skills/devola-flow — opt-in (see Rule SF-3)
(exit 0)

$ pytest tests/test_no_ghost_features.py::test_registry_single_owner -v
1 passed in 0.20s

$ pytest tests/test_dead_apis.py -v
14 passed in 0.74s

$ python scripts/detect_dead_apis.py --strict
OK: 0 dead public APIs detected.
(exit 0)
```

**All 6 W-9 / SI-10 steps PASS** + 3 PV-03-specific verification commands PASS.

## Verbatim grep verifications — all PASS

| Grep | Expected | Actual |
|------|----------|--------|
| `rg "A-5 — Single-Source-of-Truth Registry Pattern" AGENTS.md` | ≥ 1 hit | 1 hit ✓ |
| `rg "A-5 — Single-Source-of-Truth Registry Pattern" .cursor/rules/repo-governance.mdc` | ≥ 1 hit | 1 hit ✓ |
| `rg "A-5 — Single-Source-of-Truth Registry Pattern" .rules/architecture.mdc` | ≥ 1 hit | 1 hit ✓ |
| `rg "A-5\.[123]" AGENTS.md` | ≥ 3 sub-clauses (A-5.1 + A-5.2 + A-5.3 headers) | 3 hits ✓ |
| `rg "shell_proxy/registry.py" workflow-system/agent/references/shell-proxy.md` | ≥ 1 hit | 3 hits ✓ (extension table row + step 1 of "add-a-WHITELIST-entry" + §11 owner column) |
| `rg "test_registry_single_owner" workflow-system/agent/references/shell-proxy.md` | ≥ 1 hit | 1 hit ✓ |
| `rg "v9-ADR-003" workflow-system/agent/references/shell-proxy.md` | ≥ 1 hit | 1 hit ✓ |
| `rg "SSOT_REGISTRY_QUALIFIED_NAMES" scripts/detect_dead_apis.py` | ≥ 1 hit | 2 hits ✓ |
| `rg "_check_allowlist_domain_overlap" scripts/detect_dead_apis.py tests/test_dead_apis.py` | ≥ 4 hits (1 def + 1 import-time call + 2 test calls) | 7 hits ✓ (def + import-time guard line + helper call + 5 test calls) |

## I-8 invariant compliance

PV-03 makes ZERO changes to `schemas/lean-dispatch.yaml#layout_invariant`:

* `version: 5` — unchanged.
* `canonical_order` length 16 — unchanged.
* All 16 keys in their pre-PV-03 order — unchanged.
* `DEFAULT_DISPATCH_LAYOUT` in `compressor.py` — unchanged.
* All 6 backward-compat byte-baselines (v7.0.0 / v7.3.0 / v8.0.0 P-08 / v8.0.0 P-10 / v8.3.0 PV-05 / v8.4.0) continue to pass.
* No new schema golden YAML — PV-03 has no schema delta, so no new baseline is needed.

PV-03 is a pure `.rules/` + `tests/` + `scripts/` + `references/` patch
with no schema or runtime touch. Per Architecture Rule A-2 (Cache-Layout
Governance v2 — codified by sister ADR `v9-ADR-002`), no changes to the
canonical_order require A-2.4 multi-baseline byte test attention.

## Risk register check (per `v9.0.0_implementation_plan.md` §6.3.6)

* **R-3** (parity test rejects EXISTING DEFAULT_ALLOWLIST entries) —
  **NOT triggered**. Pre-implementation audit confirmed empty overlap
  between `DEFAULT_ALLOWLIST` (188 entries) and
  `SSOT_REGISTRY_QUALIFIED_NAMES` (3 entries). A-5 ships strict from
  v8.4.3; the staged-rollout contingency documented in
  `v9-ADR-003 §D3` is preserved for posterity but is not invoked.
* **SI-3 < 8.5** — **NOT triggered**. Composite 9.375/10.
* **File-level** rollback: `git revert <pv03_commit>` would cleanly
  undo the change set; existing registries are not touched (rule is
  new codification, not a behavior change).

## Coupling acknowledgments

* Couples with **A-2** (sister codification ADR `v9-ADR-002`): both
  rules are the v9.0.0 cycle's "promote informal practice to binding
  architecture rule" pattern. A-5's NEST decision for sub-clauses
  follows the v9-ADR-002 D3 rule verbatim.
* Couples with **S-5** (No Silent Failures): the import-time guard
  raises `AssertionError` with a precise A-5.2 message instead of
  swallowing the violation.
* Couples with **C-3** (Verbatim Extraction): the 5-row SSOT registry
  table is byte-identical across `.rules/architecture.mdc`,
  `.cursor/rules/repo-governance.mdc`, `AGENTS.md`, and ADR-003 (the
  first 3 are auto-mirrored by `RuleCompiler`).
* Couples with **W-9 / SI-10**: all 6 pre-commit gate steps PASS
  (verified post-bump).
* Couples with **W-4 / SI-4** (benchmark regression guard): no scenario
  drift > 5% vs `v9.0.0_baseline.json`; 36/36 PASS.

## NineS post-PV self-eval

* JSON: `.local/research/v8.4.3_nines.json` (composite **0.9050**;
  ≥ 0.85 threshold per dispatch ✓).
* Markdown summary: `.local/research/v8.4.3_nines.md` (per-dimension
  table; 22/25 dimensions ≥ 0.85; 3 below-threshold scores
  (`source_freshness=0.50`, `index_recall=0.80`, `code_coverage=0.0`)
  are pre-existing tooling caveats inherited byte-for-byte from v8.4.2,
  not v8.4.3 regressions — `code_coverage=0.0` attributable to pytest
  --cov budget timeout per the v8.4.2 erratum).
* Capability mean: 0.9517; Hygiene mean: 0.7962 (the hygiene mean is
  pulled down by the pre-existing `code_coverage=0.0` only; without
  that pre-existing tooling caveat, hygiene mean would be ~0.995).

## Cross-references

* SI-1 planning gate: `.local/research/v9.0.0_gap_analysis.md` §5.3
* SI-1 reference review: `.local/research/v9.0.0_reference_review.md` §F-13
* SI-1 implementation plan: `.local/research/v9.0.0_implementation_plan.md` §6.3
* PV-03 ADR: `.local/research/adr/v9-ADR-003-a5-ssot-registry.md`
* Sister codification ADR: `.local/research/adr/v9-ADR-002-cache-layout-governance-v2.md`
* Predecessor PV-01 ADR: `.local/research/adr/v9-ADR-001-skill-headroom-reclamation.md`
* DevolaFlow canonical URL (per S-7): https://github.com/YoRHa-Agents/DevolaFlow
* NineS canonical URL (per S-7): https://github.com/YoRHa-Agents/NineS
