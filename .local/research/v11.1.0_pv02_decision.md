# v11.1.0 PV-02 — G-CLASSIFY-1 Candidate Decision Memo

> Authored by L1 Stage Agent on `feature/v11.0.2-classifier-redesign`. Per
> A-1 P1 trivial waiver (single file, < 30 lines authored, single author);
> all source/test/YAML writes happen in L3 Task Agents per S-1.
> Cycle: v11.1.0 cascade-restoration MINOR. Sister: D1 §3 G-CLASSIFY-1 +
> D2 §3/§5/§6/§7 + D3 §2/§3 PV-02 + L0 DEC-002.

## §1. Selected candidate: **C — Rule-based 4-tier collapse with new sibling pure function**

**Operator-quotable verdict rule** (one sentence — AC item 4):

> *"STANDARD complexity or higher → cascade required (L0→L1→L2→L3); SIMPLE / TRIVIAL → cascade optional (operators may collapse to a single L3)."*

**Implementation shape**: preserve `Complexity = Literal["TRIVIAL", "SIMPLE", "STANDARD", "COMPLEX"]` byte-stable; add a NEW sibling pure function

```python
def cascade_requirement(complexity: Complexity) -> CascadeRequirement: ...
```

returning a NEW Literal `CascadeRequirement = Literal["CASCADE_REQUIRED", "CASCADE_OPTIONAL"]`. The mapping is fixed (no env-flag, no parameter): STANDARD/COMPLEX → `"CASCADE_REQUIRED"`; TRIVIAL/SIMPLE → `"CASCADE_OPTIONAL"`. Raises `ValueError` on invalid complexity per S-5. Lives alongside `classify_complexity` + `activation_verdict` (same module, same `__all__` block).

## §2. Per-criterion reasoning

* **User intents alignment** — "不能由 L0 直接调动 L3" + "更简明":
  * Cascade is the DEFAULT for STANDARD+ via a single hard rule. The operator never has to reason about thresholds at dispatch time — they read `cascade_requirement(complexity)`'s literal output and obey.
  * "更简明" is satisfied by the SINGLE rule "STANDARD+ ⇒ cascade required" replacing the prior 4-tier × 3-input × 4-threshold matrix's *cascade implication*. The complexity classifier itself remains 4-tier (operators already memorised it; AC item 5 forbids breaking it), but the cascade decision is reduced to a 1-line truth table (truly "更简明").
* **D2 finding alignment** (§3 finding 4 + §5 finding 1 + §6 finding 1 + §7 finding 1):
  * §3 (architecture, 5.0/10): Candidate C surfaces the cascade signal ORTHOGONALLY to the existing classifier verdict — the cascade contract is explicit and testable, not buried in heuristics. Closes the D2 §3 finding 4 "operational enforcement has drifted" verdict.
  * §5 (maintainability, 6.0/10): the new `cascade_requirement` is the single authoritative answer to "do I cascade?" — folding the 5-surface narrative into a one-function lookup. PV-03 SKILL.md surgical edit cites this function as the operator's primary decision surface.
  * §6 (compatibility, 9.0/10): zero `Complexity` Literal break; ALL 13 existing contract pins (`classify_complexity` truth table + `activation_verdict` matrix + `from_env` R5 strict + `force_no_change` orthogonality + verdict-string stability) stay BYTE-STABLE. A-6.1 public contract preserved. Smoothest migration of the three candidates.
  * §7 (performance, 7.0/10): per-tier dispatch-cost trade is unchanged from any candidate (cascade overhead is intrinsic to STANDARD+ being a cascade tier). Candidate C's PV-04 schema impact NESTs `gate.cascade_required` under the existing `gate` block per A-2.3 — `canonical_order` length stays at 17, A-2.4 multi-baseline byte test 32/32 GREEN by construction.
* **AC contract pinning** — D3 §3 PV-02 AC item 3 mandates `cascade_requirement(complexity)` returns `"CASCADE_REQUIRED"` for STANDARD+ and `"CASCADE_OPTIONAL"` for SIMPLE/TRIVIAL. AC item 5 mandates the existing 13 contract pins PRESERVED byte-stable. Both ACs are simultaneously satisfiable ONLY by Candidate C semantics — Candidates A and B both restructure the `Complexity` Literal contract.

## §3. Risk-mitigation note

* **R-1 cascade overhead regression**: PV-05 EvoBench scenarios pin cascade composite ≥ collapsed composite − 5%. The new `cascade_requirement` is a pure-function predicate; runtime cost is O(1) literal compare. No measurable per-dispatch overhead.
* **R-2 operator confusion during transition**: SKILL.md sub-table (PV-02 cells only — line 60-66 region) is updated to cite `cascade_requirement`'s literal output as the operator's reading order. Full SKILL.md cascade restoration (line 64 + 105-107 + 180) lands at PV-03; PV-02 only tightens the `Quick Action Decision` table cells.
* **R-3 cache-layout drift**: PV-02 makes ZERO schema edits. The `gate.cascade_required` NEST sub-field lands at PV-04 per the cycle plan. PV-02 stub at `tests/test_cascade_enforcement.py` exercises the propagation contract via dispatch-payload assertions only (no schema-shape mutations).

## §4. Rejected candidates

* **A — 2-tier with single threshold**: REJECTED. Collapses TRIVIAL into SIMPLE and STANDARD into COMPLEX, breaking the `Complexity` Literal contract. Violates AC item 5 (13 contract pins must stay byte-stable). Loses the conservative `is_cross_cutting → STANDARD` floor that 3 of the existing tests depend on (`test_classify_complexity_cross_cutting_forces_standard_floor` etc.). Maximally simple but the migration cost is borne by the wrong layer (the operator's mental model adapts; the test suite shouldn't have to).
* **B — Default cascade with explicit TRIVIAL opt-out**: REJECTED. Per Candidate B's spec, *only TRIVIAL* opts out of cascade — SIMPLE would map to `CASCADE_REQUIRED`. This contradicts D3 §3 PV-02 AC item 3 which mandates SIMPLE → `CASCADE_OPTIONAL`. AC item 3 was authored after the user's directive that "在中等以上复杂度的任务中" cascade is required (medium+ tier — explicitly excluding SIMPLE). Adopting B would break the user's stated intent. (If the cycle plan's AC and the user's directive ever conflict, the user's directive wins per CO-2 — but here they agree on Candidate C semantics.)

## §5. Cross-references

* `.local/research/v11.1.0_gap_analysis.md` §3 G-CLASSIFY-1 (the 3-candidate trade-off table; my source).
* `.local/research/v11.1.0_nines_analysis.md` §3 finding 4 + §5 finding 1 + §6 finding 1 + §7 finding 1 (D2 evidence).
* `.local/research/v11.1.0_cycle_plan.md` §2 PV-02 row + §3 PV-02 cascade decomposition (the AC contract).
* `.local/research/v11.1.0_l0_decisions.md` DEC-002 (L0 advisory leans B; non-binding; this memo overrides per the AC analysis above).
* `src/devolaflow/skills/change_activation.py` (current `Complexity` Literal + 4 functions; preserved byte-stable + ONE new `cascade_requirement` sibling).

*End of decision memo. L3 T01_cascade_requirement_function dispatched immediately after this memo lands.*
