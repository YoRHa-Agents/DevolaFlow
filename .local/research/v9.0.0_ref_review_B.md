# v9.0.0 SF-4 Reference Review — Batch B

> Author: L3 Task Agent (Review team) | Task: v9.0.0-S03-T02 | Date: 2026-04-23
> Scope: 3 of 11 SF-4 canonical references (Batch B)
> Inputs (read-only): `workflow-system/agent/references/{context-isolation,message-schemas,behavioral-guidelines}.md`,
> `workflow-system/agent/SKILL.md`, `schemas/lean-dispatch.yaml`, `schemas/lean-report.yaml`,
> `.local/research/v9.0.0_version_history.md`, `.local/research/v9.0.0_decomposition_analysis.md`.
> Output: `.local/research/v9.0.0_ref_review_B.md` (this file).
> All paths repo-relative per Rule S-2; verbatim quotes preserved per Rule C-3.

---

## Reference: context-isolation.md

**Current line count**: 559 / 1000 (Large tier ceiling — 44 % budget consumed; 441-line headroom)
**Last major content update**: v8.0.0 P-12 (§15 "Abstractive summariser Stage A" appended). Earlier v7.0.x → v7.1.0 cycle drove the §10–§15 build-out (`v9.0.0_version_history.md:1232` — *"context-isolation.md §§ 12-15 (394 → 533 lines, +139): hierarchical predecessor summariser, persistence probe, operational learnings v2, staged compression end-to-end."*).
**SKILL.md reference hit**: Y — 3 hits at SKILL.md:352 (*"Full context injection spec: `references/context-isolation.md`"*), 353 (*"see `references/context-isolation.md` Cache-Layout Invariant subsection for the `assert_dispatch_layout` validator API"*), 423 (Tier 2 nav row).

### Content inventory

- §1 Isolation Principles (3 failure modes table)
- §2 Isolation Mechanisms (3 mechanisms — fresh context, file ownership, artifact-mediated)
- §3 Context Injection Template (~50 lines YAML schema + section token budgets + worked example)
- §4 What MUST NOT Leak (6-row category table)
- §5 What IS Shared (5-row table)
- §6 Context Budget Management by Layer (L0–L3 budget table + loading strategy + enforcement rules + file limits)
- §7 Context Injection Checklist (ASCII checklist)
- §8 Information Density Optimization (v2.2.0)
- §9 Debugging Context Issues (symptom→cause→fix table)
- §10 Cache Layout Invariant (v7.0.0+) — declares "Canonical order (12 keys)" and *"New top-level keys MUST be appended after `gate` (position 12)"*
- §11 Tool-Output Truncation (v7.0.1+)
- §12 Hierarchical Predecessor Summariser (v7.0.2+)
- §13 Persistence Probe (v7.0.3+)
- §14 Operational Learnings v2 (v7.0.3+)
- §15 Staged Compression End-to-End (v7.1.0+) — table mapping primitives to convergence rounds
- §15 Abstractive Summariser Stage A (P-12, v8.0.0) — DUPLICATE numbering (file has TWO §15 headings at lines 502 and 535)

### Drift findings

- **STALE — schema version mismatch (CRITICAL).** §10 (line 322) declares *"Canonical order (12 keys): `hdr` → `task` → `goal` → `assumptions` → `pred` → `files` → `rules` → `shared` → `accept` → `reinforce` (round 2+ only) → `verify_cfg` → `gate`"* and *"new top-level keys MUST be appended after `gate` (position 12)"*. The live schema at `schemas/lean-dispatch.yaml#layout_invariant` is at `version: 5` with `canonical_order` length **16** — appended in order: `repos` (pos 13, v7.2.6), `behavioral_guidelines` (pos 14, v8.0.0 P-08), `acceptance_criteria_v2` (pos 15, v8.0.0 P-10), `change_context` (pos 16, v8.3.0 PV-05). Drift: 4 missing positions across 4 schema generations.
- **DUPLICATE — §15 numbering collision.** Lines 502 and 535 both begin with `## 15.`. The Stage A abstractive section (line 535) was appended without renumbering — should be §16 (or §15.1 if intentional sub-section). Confuses cross-references and breaks anchor uniqueness.
- **STALE — frontmatter `last_updated: "2026-04-04"`.** Cannot be correct: §15 abstractive Stage A landed in v8.0.0 P-12 (`v9.0.0_version_history.md:1602` commit `9c71bbc`, dated *"2026-04-21 14:55 UTC → 2026-04-22"*); §§ 12–15 staged-compression block landed v7.0.x → v7.1.0 (2026-04-17 → 2026-04-21). Frontmatter understates true last-touched date by ~17 days.
- **STALE — frontmatter `token_estimate: 3400`.** File is 559 lines × ~4 tok/line ≈ 2236 tokens at the low end and ≥ 6000 at the high end (heavy YAML/table content). 3400 estimate predates §§ 11–15 build-out (file was 394 lines per `v9.0.0_version_history.md:1232`).
- **MISSING — no mention of `data_envelope_required` (v7.2.4 P-02) or `bypass_conditions` (v7.2.0 C-002).** `schemas/lean-dispatch.yaml:341-354` declares both as load-bearing nested sub-keys of `compression_rules`. Context-isolation §11 only covers tool-output truncation; injection-attack envelope policy is silent here despite Soul Rule S-5 ("No Silent Failures") relevance.
- **UNCLEAR — §10 references `compute_dispatch_lcp_pct(a, b)` and `tests/test_compressor.py::test_dispatch_prefix_is_stable_across_rounds` without saying which round counts apply.** `schemas/lean-dispatch.yaml:449-450` is the source of truth: `lcp_threshold_round_1_to_2: 0.80`, `lcp_threshold_round_1_to_3: 0.70`. Doc states the values but only as a single SLO line (line 330) without cross-reference to the YAML source.

### Verdict for v9.0.0

**REFRESH** (mandatory) — §10 cache-layout drift is a v9.0.0 BLOCKER per `v9.0.0_decomposition_analysis.md` Theme T7 (Cache-Layout Governance v2). Doc must be re-aligned with `schemas/lean-dispatch.yaml#layout_invariant` BEFORE T7's nest-vs-append rule is codified. Duplicate §15 numbering and STALE frontmatter are sub-blockers.

### Improvement candidates for v9.0.0

- **IC-CI-1 (REFRESH §10 to v5 schema, +5 LOC).** Update *"12 keys"* → *"16 keys"*; expand the canonical_order list to include `repos`, `behavioral_guidelines`, `acceptance_criteria_v2`, `change_context`; cite `schemas/lean-dispatch.yaml#layout_invariant.version: 5`. Couples directly with Theme T7 (`v9.0.0_decomposition_analysis.md:196`).
- **IC-CI-2 (Renumber duplicate §15 → §16, +0/−0 LOC).** Rename the Stage A abstractive heading at line 535 from `## 15.` to `## 16.`. Refresh anchor links if any internal references use `#15-...`.
- **IC-CI-3 (Refresh frontmatter `last_updated` + `token_estimate`, +0 LOC).** Set `last_updated: "2026-04-22"` (v8.0.0 P-12 commit date); set `token_estimate: 6000` to reflect the true file size and avoid downstream budget miscalculation in `task_adaptive_selector.py`.
- **IC-CI-4 (Add §11.5 "Data-Channel Envelope Policy", +15 LOC).** Document `wrap_data_envelope` / `unwrap_data_envelope` / `INJECTION_PATTERNS` per `references/execution-protocol.md §8` (per `v9.0.0_version_history.md:1302` v7.2.4 P-02). Cross-reference `schemas/lean-dispatch.yaml#compression_rules.data_envelope_required` (line 354) and `bypass_conditions` (line 341).
- **IC-CI-5 (Add §10 multi-baseline byte-test note, +6 LOC).** Mention `tests/test_layout_invariant_multi_baseline.py` (NEW, per Theme T7 plan at `v9.0.0_decomposition_analysis.md:202`) and v7.0.0 + v7.3.0 + v8.0.0 + v8.3.0 byte-baseline files in `benchmarks/devolaflow_context/baselines/`.

### SF-1 budget impact

Net delta estimate: **+26 LOC / −1 (no real removal)** ≈ +25 LOC → 559 → 584 / 1000. Stays comfortably inside Large tier (≤ 1000). No SKILL.md edit required if §10 update is purely substantive.

---

## Reference: message-schemas.md

**Current line count**: 391 / 1000 (Large tier — 39 % budget consumed)
**Last major content update**: Frontmatter declares `last_updated: "2026-04-04"`. The body still documents the legacy verbose `task_dispatch` schema (lines 35-73) that pre-dates the v7.0.0 lean-dispatch cutover. NO update has tracked the v7.x → v8.x schema evolution.
**SKILL.md reference hit**: Y — 2 hits at SKILL.md:384 (*"Full schemas: `references/message-schemas.md`"*), 421 (Tier 2 nav row *"Constructing/parsing dispatch/report/escalation"*).

### Content inventory

- §1 Protocol Rules (4 bullets — typed YAML, no free-form, written to project tracking dir, 3 message types)
- §2 TaskDispatch Schema (Parent → Child) — 38-line YAML schema (verbose `task_dispatch` form) + 16-row Field Documentation table + Project→Stage example
- §3 StatusReport Schema (Child → Parent) — 44-line YAML schema (verbose `status_report` form) + 12-row Field Documentation table + Task→Wave completion example
- §4 ExceptionEscalation Schema — 26-line YAML schema + 3-row Error Type Classification table + Task→Wave blocking-error example
- §5 Message Flow Diagram — 28-line ASCII sequence diagram (Human→Project→Stage→Wave→Task) + 5-line escalation flow
- §6 Message Storage Locations — `.local/stages/` filesystem layout

### Drift findings

- **STALE — entire doc covers the LEGACY verbose schema; lean-dispatch / lean-report formats are absent (BLOCKER).** Doc shows `task_dispatch.header.dispatch_id` / `task_dispatch.task.task_id` / etc. Live `schemas/lean-dispatch.yaml:80-97` declares the lean form as canonical: `hdr: { id, parent, layer, timeout }` / `task: { id, type, title }` / `goal:` / `assumptions:` / `pred:` / `files:` / `rules:` / `shared:` / `accept:` / `reinforce:` / `verify_cfg:` / `gate:`. Doc does not mention any lean key. CO-1 (Lean Message Format) explicitly mandates lean is canonical: *"Inter-layer messages (TaskDispatch, StatusReport) must use the lean format defined in `schemas/lean-dispatch.yaml` and `schemas/lean-report.yaml`."* (`AGENTS.md` C-2).
- **MISSING — `layout_invariant` cache-layout invariant (CRITICAL).** `schemas/lean-dispatch.yaml:427-457` declares the canonical_order contract that gates EVERY dispatch (P6 / Rule 6, ADR-001). Doc has zero coverage of `assert_dispatch_layout`, `compute_dispatch_lcp_pct`, byte-baselines, or LCP SLOs. SKILL.md:384 promises this doc is the *"Full schemas"* source — promise unfulfilled.
- **MISSING — 4 v7.x→v8.x dispatch fields (BLOCKER).** Doc never mentions `repos` (v7.2.6 P-06), `behavioral_guidelines` (v8.0.0 P-08), `acceptance_criteria_v2` (v8.0.0 P-10), or `change_context` (v8.3.0 PV-05). All 4 are in the live canonical_order; absence creates drift versus 5 schema generations of additive growth.
- **MISSING — `compression_rules` block.** `schemas/lean-dispatch.yaml:300-354` declares `preserve_list` (8 entries), `drop_list` (9 entries), `intensity_tiers` (minimal/standard/aggressive), `bypass_conditions` (4 entries), `data_envelope_required: true`. Same block mirrored verbatim in `schemas/lean-report.yaml:138-180`. Doc covers none of it.
- **MISSING — lean-report extensions (CRITICAL).** Doc covers only the verbose `status_report`. Missing from doc but present in `schemas/lean-report.yaml`: (a) `tool_results.summary` (lines 266-276 — v7.0.1 ADR-002), (b) `learnings.entries` (lines 290-303 — v7.2.3 P-03), (c) `cycle_detected` + `cycle_details` (lines 318-343 — v8.0.0 P-06), (d) `result_status_spec` enum `{DONE, DONE_WITH_CONCERNS, NEEDS_CONTEXT, BLOCKED}` (lines 184-204), (e) `aggregation_formats.{wave_summary, stage_summary}` (lines 208-257).
- **MISSING — SKILL.md surface fields not documented here.** SKILL.md:366-368 lists `model_hint`, `decomposition_mode`, `compression_intensity`, `verification_config` as part of *"Dispatching a task"* — none documented in this reference. SKILL.md:386 also calls out *"Round-aware dispatch (v6.0.3+)"* and `applicable_rules.reinforcement` injection — undocumented here.
- **MISSING — `acceptance_criteria_v2` per-entry shape.** `schemas/lean-dispatch.yaml:249-258` declares structured AC `{id, description, verification_type, verification_cmd, metric, threshold}` plus the legacy `acceptance_criteria: list[str]` alias preserved per R5 backward-compat (line 397). Doc still treats `criteria` as flat list[str].
- **STALE — frontmatter `last_updated: "2026-04-04"`** vs. v8.3.0 PV-05 schema bump (2026-04-23) extending `canonical_order` 15→16. Doc has not been touched in ≈ 19 days despite 5 schema generations shipping in that window.

### Verdict for v9.0.0

**REFRESH (mandatory)** — magnitude approaches **EXTEND** since the dominant content (verbose schema) is obsolete. Recommend: keep §1 + §5 + §6 verbatim; replace §2/§3 verbose schemas with lean schema authoritative section + collapsed legacy compatibility appendix; add new §7 Cache-Layout Invariant + §8 Compression & Bypass Rules + §9 lean-report extensions.

### Improvement candidates for v9.0.0

- **IC-MS-1 (Replace §2 with lean dispatch authoritative spec, +60 LOC, −38 LOC = +22 net).** Document `hdr/task/goal/assumptions/pred/files/rules/shared/accept/reinforce/verify_cfg/gate/repos/behavioral_guidelines/acceptance_criteria_v2/change_context` from `schemas/lean-dispatch.yaml:80-298` verbatim. Move existing verbose `task_dispatch` example to a §10 *Legacy Verbose Form (deprecated)* appendix with R5 backward-compat note.
- **IC-MS-2 (Replace §3 with lean-report authoritative spec, +50 LOC, −44 LOC = +6 net).** Document the `hdr/state/artifacts/metrics/issues/decisions` lean form per `schemas/lean-report.yaml:88-118` plus the appended blocks (`tool_results`, `learnings`, `cycle_detected`, `cycle_details`).
- **IC-MS-3 (NEW §7 Cache-Layout Invariant Quick Reference, +25 LOC).** Cite `schemas/lean-dispatch.yaml#layout_invariant.version: 5` + 16-key canonical_order + LCP SLOs + `assert_dispatch_layout` validator API. Cross-reference `references/context-isolation.md §10`. Closes the SKILL.md:353 contract that points here for *"Constructing/parsing dispatch/report/escalation"*.
- **IC-MS-4 (NEW §8 Compression & Bypass, +20 LOC).** Document `compression_rules.{preserve_list, drop_list, intensity_tiers}`, `bypass_conditions` (4 cases — security_warning, destructive_operation, multi_step_sequence_with_order_dependency, repeated_user_question), `data_envelope_required: true` per `schemas/lean-dispatch.yaml:300-354`. Mirrors `schemas/lean-report.yaml:138-180` lock-step.
- **IC-MS-5 (NEW §9 Result Status & Aggregation, +18 LOC).** Document `result_status_spec.enum` + `routing_table` (`p4_mapping: retry/escalate`) per `schemas/lean-report.yaml:184-204`; plus `aggregation_formats.{wave_summary, stage_summary}` for L2/L1 rollup discipline.
- **IC-MS-6 (Refresh frontmatter, +0 LOC).** Set `last_updated: "2026-04-23"` (v8.3.0 PV-05 schema-version-5 publish date); leave `token_estimate: 3600` for now (will be re-tuned post-refresh).

### SF-1 budget impact

Net delta estimate: **+71 LOC** (sum of positives minus removals) → 391 → ~462 / 1000. Comfortably inside Large tier. No SKILL.md edit required (the doc title/role unchanged), but SKILL.md:421 nav-row description should be reaffirmed as the lean-format authoritative source.

---

## Reference: behavioral-guidelines.md

**Current line count**: 229 / 1000 (Large tier — 23 % budget consumed; ample headroom)
**Last major content update**: v8.2.0 PV-04 (`v9.0.0_version_history.md:1697` — *"`behavioral-guidelines.md` extended +43 lines with line-level criteria (Large tier ≤1000 line ceiling preserved at ~233 lines)"*). Original creation v8.0.0 P-08 at 190 lines.
**SKILL.md reference hit**: **N — NOT listed in SKILL.md Tier 2 Reference Navigation Guide (lines 417-426).** The 10-row table covers `agent-hierarchy / decomposition-gate / repo-modes / meta-framework / message-schemas / team-roles / context-isolation / execution-protocol / agent-workspace / shell-proxy` and OMITS `behavioral-guidelines.md`. Per SF-4 (`AGENTS.md` C-7) and `v9.0.0_version_history.md:1547` the file is the **9th** of 11 SF-4 canonical references. SKILL.md drift confirmed.

### Content inventory

- Frontmatter (id, version, purpose, triggers, tier=2, token_estimate=1100, dependencies, last_updated="2026-04-21")
- Intro (3-paragraph rationale + Karpathy attribution + opt-in-per-profile note)
- Field Shape (4-key YAML block — `think_first / simplicity_check / surgical_scope / goal_loop`)
- Optional-field semantics + `select_context()` integration note
- Rule Application Matrix (4-tier × 4-rule table — trivial / simple / standard / complex)
- §Rule 1 think_first (`BG-001`, severity warn) — rendered guidance + 3 self-check Qs + skip rule
- §Rule 2 simplicity_check (`BG-002`, severity warn → blocker if 3 YES) — same shape
- §Rule 3 surgical_scope (`BG-003`, severity blocker) — 3-tier ladder table (line / function / module) + verifier rules + v8.2.0 PV-04 line-tier note
- §Rule 4 goal_loop (`BG-004`, severity warn) — guidance + self-check
- §Token Cost (4 rules total ≈ 480 tokens, < 5 % L3 8K budget)
- §Backward Compatibility (byte-identical v7.x output when block absent; baselines preserved)
- §Line-Level Behavioral Criteria (LL-001..LL-005 — per-line max length / cc-delta / cohesion / declared-range / verbatim preservation)
- §See Also (5 cross-refs)

### Drift findings

- **STALE — SKILL.md does not list this reference in the Tier 2 navigation table (CRITICAL coupling failure).** Per `AGENTS.md` Rule C-7 *"Every `references/xxx.md` path mentioned in SKILL.md must correspond to an actual file under `workflow-system/agent/references/`. Valid references: `agent-hierarchy.md`, `agent-workspace.md`, `behavioral-guidelines.md`, ..."* — SKILL.md must list valid references for L1/L2/L3 to discover them. Currently the file is "orphaned" — only discovered indirectly through the dispatch payload's `behavioral_guidelines` field schema (`schemas/lean-dispatch.yaml:177-210`). Confirmed gap: lines 417-426 of SKILL.md enumerate 10 refs, missing this one.
- **MISSING — no coverage of v8.2.0 primitives beyond surgical_scope=line.** v8.2.0 PV-02 shipped agent legibility scoring (`v9.0.0_version_history.md:1689`) and PV-03 shipped Unified Session State (`v9.0.0_version_history.md:1703`). Both are L3-impacting behavioral surfaces. The behavioral block in `schemas/lean-dispatch.yaml:203-210` still lists only the 4 Karpathy primitives. v8.2.0 PV-05 R7 work is similarly absent.
- **MISSING — no interaction with v8.3.0 `change_context` / Rule S-8 (`AGENTS.md` Soul Rules).** When a task agent operates inside `change-driven` workflow with `change_context.owned_files_ref` set, BG-003 surgical_scope and Rule S-8 *"No Writes Outside Active Change Owned Files"* must compose. Doc never mentions this composition. The reader cannot tell whether `surgical_scope='module'` overrides S-8 or both apply (S-8 is the floor).
- **STALE — `token_estimate: 1100` predates v8.2.0 PV-04 line-level addition.** File grew from 190 → 229 lines (+39); estimate should be ~1300 tokens.
- **UNCLEAR — §Rule 3 line-tier verifier note states *"Future v8.2.0 work — currently aliased to `function`"* in the table at line 120, but §Rule 3 *"Note (v8.2.0 PV-04)"* at line 138 explicitly says line-tier verification is implemented.** Internal contradiction within the same section. Reader cannot tell whether line-tier is live or still aliased.
- **MISSING — no env-flag inventory entry.** v9.0.0 Theme T8 (`v9.0.0_decomposition_analysis.md:209-220`) introduces `references/env-flags.md` as a SF-4 canonical inventory; the per-profile `behavioral_guidelines` defaults in `workflow-system/agent/context_profiles.yaml#profiles.<name>.behavioral_guidelines` are an env-flag-adjacent surface that should cross-reference the new doc.
- **MISSING — no LL-rule severity matrix.** §Line-Level Behavioral Criteria states *"Failing any criterion is a blocker finding per the BG-003 severity classification."* But §Rule 2 simplicity_check uses warn→blocker grading; §Rule 1 think_first stays warn even if violated. Severity ladder is inconsistent across rules and never tabulated.

### Verdict for v9.0.0

**EXTEND** (mandatory) — must (a) extend behavioral surface to v8.2.0 PV-02/PV-03/PV-05 primitives, (b) document S-8/S-9 composition with surgical_scope, (c) cross-reference forthcoming Theme T5 default-on flip per `v9.0.0_decomposition_analysis.md:170` (5 of 7 v8.0.0 opt-in primitives flipping default-ON). SKILL.md:417-426 nav table MUST gain a row for this file in the same PV per Rule W-5 (Skill Format Coupling).

### Improvement candidates for v9.0.0

- **IC-BG-1 (Add SKILL.md Tier 2 nav row, +1 SKILL.md LOC).** Insert line into SKILL.md:417-426 table: `| `references/behavioral-guidelines.md` | L3 think_first / simplicity_check / surgical_scope / goal_loop primitives |`. Closes SF-4 valid-reference-link contract per Rule C-7. Trivial 1-line edit; touches `workflow-system/agent/SKILL.md` only. Couples with Theme T1 (SKILL.md compression) — must be applied AFTER T1 frees ≥ 1 line of headroom (SKILL.md is at 499/500 per `v9.0.0_version_history.md:1869`).
- **IC-BG-2 (Add §Rule 5 — composition with S-8 active-change ownership, +25 LOC).** Document the surgical_scope × `change_context.owned_files_ref` composition: when both present, the effective writable set = `owned_files ∩ surgical_scope_diff_set`. Cross-reference `AGENTS.md` Soul Rules S-8 + S-9 + `schemas/lean-dispatch.yaml:289-298` (`change_context` field shape).
- **IC-BG-3 (Refresh §Rule 3 to remove internal contradiction, +0/−2 LOC).** Update line-tier table cell at line 120 from *"Future v8.2.0 work — currently aliased to `function`"* to *"Implemented in v8.2.0 PV-04 (`_select_line_level_sections`)"*. Reconciles with the §Rule 3 v8.2.0 PV-04 note (lines 138-144).
- **IC-BG-4 (Refresh `token_estimate` to 1300, +0 LOC).** Cosmetic frontmatter fix — also align `last_updated` if any new edits land in v9.0.0.
- **IC-BG-5 (Add §Severity Matrix table, +12 LOC).** Tabulate per-rule severity: BG-001 warn / BG-002 warn (escalates to major @2YES, blocker @3YES) / BG-003 blocker / BG-004 warn / LL-001..LL-005 blocker. Eliminates current implicit-only severity coupling.
- **IC-BG-6 (Add §Default-On Flip note, +8 LOC).** Per Theme T5 (`v9.0.0_decomposition_analysis.md:170-181`) the v8.0.0 P-08 `behavioral_guidelines` field is one of 5 opt-in primitives candidate for default-ON in STRICT/AUDIT profiles. Add a forward-looking note: *"v9.0.0 PV-06 candidate flips per-profile defaults from absent → enabled for STRICT (per `v9.0.0_decomposition_analysis.md` Theme T5)."* Open Decision #11 in S05 will resolve scope.
- **IC-BG-7 (Add §See Also row for env-flags.md, +1 LOC).** Forward-reference `references/env-flags.md` (NEW per Theme T8 — `v9.0.0_decomposition_analysis.md:215`) for the per-env-flag inventory of behavioral defaults.

### SF-1 budget impact

Net delta estimate: **+45 LOC** → 229 → ~274 / 1000. Comfortably inside Large tier. SKILL.md edit (+1 LOC) requires Theme T1 PV-01 to land first per the `v9.0.0_decomposition_analysis.md` PV-01 precondition (SKILL.md headroom ≥ 1 needed; currently 499/500).

---

## Batch B Summary Table

| Reference | Lines / 1000 | SKILL.md Hit | Last update | Drift count | Verdict | Δ LOC | Theme alignment |
|---|---|---|---|---|---|---|---|
| `references/context-isolation.md` | 559 | Y (3 hits — SKILL.md:352/353/423) | v8.0.0 P-12 (2026-04-22) | 6 (1 STALE-CRITICAL §10 schema, 1 DUPLICATE §15, 2 STALE frontmatter, 1 MISSING data-envelope, 1 UNCLEAR LCP SLO) | **REFRESH** (mandatory) | +25 | T7 (Cache-Layout Governance v2) BLOCKER C-01 |
| `references/message-schemas.md` | 391 | Y (2 hits — SKILL.md:384/421) | 2026-04-04 (legacy verbose form only) | 8 (1 BLOCKER lean-vs-verbose drift, 1 CRITICAL missing layout_invariant, 4 MISSING fields, 1 MISSING lean-report extensions, 1 STALE frontmatter) | **REFRESH** (borderline EXTEND) | +71 | T7 (Cache-Layout) + adjacent to T1 (SKILL.md) |
| `references/behavioral-guidelines.md` | 229 | **N** (orphaned — SKILL.md Tier 2 nav misses this 9th canonical ref) | v8.2.0 PV-04 (2026-04-22) | 6 (1 CRITICAL SKILL.md missing nav row, 3 MISSING v8.2.x/v8.3.x coverage, 1 STALE token_estimate, 1 UNCLEAR line-tier) | **EXTEND** (mandatory) | +45 (+1 SKILL.md) | T5 (B3 Default-On Flip) + T1 (SKILL.md headroom) + T8 (env-flags.md) |
| **Batch B Total** | **1179 lines** | 5 of 6 expected hits (1 missing) | 3 stale frontmatters | **20 drift findings** | All 3 require change | **+141 LOC + 1 SKILL.md** | 5 themes touched (T1 / T5 / T7 / T8 / governance) |

---

## Cross-Batch Observations

1. **Schema-vs-doc drift is concentrated in cache-layout / dispatch-shape coverage.** `schemas/lean-dispatch.yaml#layout_invariant.version: 5 (canonical_order length 16)` is the load-bearing P6 contract per `AGENTS.md` Rule A-2. Two of three Batch B refs (`context-isolation.md` §10, `message-schemas.md`) document the v7.0.0 baseline (12 keys) or pre-lean verbose form. Drift cumulatively spans 5 schema generations (v7.0.0 → v7.2.6 → v8.0.0 P-08 → v8.0.0 P-10 → v8.3.0 PV-05) — exactly the additivity proof of `v9.0.0_decomposition_analysis.md:198-200` Theme T7. Refresh of `context-isolation.md §10` and `message-schemas.md §2` is a precondition for Theme T7's nest-vs-append rule codification (PV-02 per `v9.0.0_decomposition_analysis.md:229`).

2. **SF-4 valid-reference-link contract has a confirmed gap.** `AGENTS.md` Rule C-7 enumerates 10 valid SF-4 references (post-v8.3.0 PV-09) including `behavioral-guidelines.md`. SKILL.md Tier 2 nav table at lines 417-426 lists 10 entries but OMITS `behavioral-guidelines.md` and INCLUDES `shell-proxy.md` (added v8.4.0 per `v9.0.0_version_history.md:1870`). True canonical ref count is now 11 (with `shell-proxy.md`); SKILL.md should list all 11. This is a Rule W-12 (SKILL Changes Require Adapter Build) risk — orphaned references break dispatcher discoverability for L1/L2/L3 agents trying to load behavioral primitives. Resolution: 1-line SKILL.md edit (IC-BG-1) AFTER Theme T1 PV-01 frees headroom from current 499/500 ceiling.

3. **Frontmatter `last_updated` fields are systematically stale across all 3 refs.** All three carry `last_updated` dates between 2026-04-04 and 2026-04-21, yet each has been substantively touched by v8.0.0–v8.4.0 cycle work (`v9.0.0_version_history.md:1232,1547,1597,1697`). No tooling currently enforces frontmatter freshness. Recommendation: bundle a CI lint into Theme T8 (NineS Hygiene + Audit-Cadence W-Rules) — e.g., `tests/test_reference_size_budgets.py` could add an `assert_frontmatter_last_updated_after_last_git_commit_to_file` check. Couples with C-7 ("Valid Reference Links") + W-19 (research artifact archival) discipline.

4. **`message-schemas.md` is the single largest information-density gap in the canonical reference set surveyed by Batch B.** It is referenced from SKILL.md as the *"Full schemas"* authoritative source (SKILL.md:384) but covers ~25 % of what `schemas/lean-dispatch.yaml` + `schemas/lean-report.yaml` actually declare. The combined 8 missing topics (lean form, layout_invariant, behavioral_guidelines / acceptance_criteria_v2 / change_context / repos fields, compression_rules, lean-report extensions, dispatch surface fields like `model_hint` / `decomposition_mode`) suggest this ref deserves a formal v9.0.0 RE-AUTHORING task rather than incremental REFRESH. Estimated effort upper bound: 1.5× IC-MS estimate (+106 LOC) if the legacy verbose form is preserved as appendix.

5. **All 3 Batch B refs cluster under v9.0.0 Themes T1 + T5 + T7 + T8 dependency graph.** Per `v9.0.0_decomposition_analysis.md:222-243`: T1 (SKILL.md compression PV-01) is the universal precondition; T7 (cache-layout governance v2 PV-02) consumes the IC-CI-1 + IC-MS-3 deliverables; T5 (B3 default-on flip PV-06) consumes IC-BG-6; T8 (env-flags.md + W-rules PV-05) consumes IC-BG-7 + IC-CI-4. Recommendation: schedule Batch B reference REFRESH/EXTEND work AFTER PV-01 (SKILL.md compression) and BEFORE PV-02 (cache-layout governance) to break the SKILL→ref→schema chain in dependency order.

6. **No verbatim drift in §1–§7 of context-isolation.md or §1 / §5 / §6 of message-schemas.md against any predecessor.** The "evergreen" sections (isolation principles, file ownership, MUST-NOT-leak categories, message flow ASCII diagram, storage layout) survive every era unchanged and remain accurate. Refresh/extend scope can therefore be tightly bounded — preserve §1–§7 of context-isolation and §1 / §5 / §6 of message-schemas verbatim per Rule C-3 byte-stable extraction; rewrite only §10–§15 of context-isolation and §2/§3 of message-schemas. This bounds Batch B refactoring risk vs. blast radius (mirrors the v8.4.0 R5 strict pattern per `v9.0.0_decomposition_analysis.md:179`).

---

*End of Batch B Reference Review. Total: 3 refs reviewed, 20 drift findings, 14 improvement candidates, +141 LOC + 1 SKILL.md row, 5 v9.0.0 themes touched. Output budget: this file ≤ 500 lines per S03-T02 acceptance criterion #5. All paths repo-relative per S-2; verbatim quotes from `schemas/lean-dispatch.yaml`, `schemas/lean-report.yaml`, `workflow-system/agent/SKILL.md`, `AGENTS.md`, and the 3 reference files preserved per C-3.*
