# v7.2.0 Candidate List

**Generated:** 2026-04-18
**Source:** S01 delta reports T01–T07 in `.local/research/v7.2.0_refs/`
**Owner:** S02a synthesis (read-only roll-up; no source files modified)
**DevolaFlow baseline:** v7.1.1

## Stats
- input_deltas: 96
- kept (import+monitor): 75
- dropped (skip): 21
- nines_targets_analyzed: 0 (deferred to a separate task per resource constraints; mark as "abstract" in entries)

Per-task breakdown of input deltas and recommendations:

| Task | Total | Import | Monitor (incl. registry-refresh) | Skip |
|------|-------|--------|----------------------------------|------|
| T01 (advisor / superpowers / coord-blog) | 10 | 5 | 5 | 0 |
| T02 (gsd / gstack / caveman) | 14 | 4 | 7 | 3 |
| T03 (karpathy-skills × 2 / understand-anything) | 12 | 1 | 8 | 3 |
| T04 (edict / karpathy-llm-wiki) | 12 | 2 | 7 | 3 |
| T05 (vexp / scion / skillrouter) | 15 | 1 | 11 | 3 |
| T06 (self-improving-system / agent-skills-security / hydra / ruflo) | 21 | 2 | 15 | 4 |
| T07 (christophera-bootstrap-seed / spring-ai-agent-skills) | 12 | 1 | 6 | 5 |
| **Totals** | **96** | **16** | **59** | **21** |

---

## Category 0: Registry Hygiene

Six concrete data bugs found in `workflow-system/agent/knowledge/reference-dependencies.yaml` during S01. These are upstream-truth errors (not behavioural deltas) that should be repaired in a single small PR ahead of the v7.2.0 candidate work.

| ID | Issue | File | Effort |
|----|-------|------|--------|
| H-01 | `karpathy/andrej-karpathy-skills` URL returns HTTP 404 — repo has never existed; only `forrestchang/andrej-karpathy-skills` is real (T03 §4.1, DELTA-KARP-UPSTREAM-01) | `workflow-system/agent/knowledge/reference-dependencies.yaml` (lines 169-189 of broken entry) | S — delete broken entry; fold its 4 integration-points into the surviving forrestchang entry (T03 DELTA-KARP-FRSCH-06) |
| H-02 | `PrimeLocus/Hydra` repo deleted upstream between 2026-04-13 and 2026-04-18 (`status: verified` is now false; both REST API and HTML return 404) — surviving fork is `mikecubed/Hydra` at `package.json` v1.2.0 (T06 Δ-hydra-01) | `workflow-system/agent/knowledge/reference-dependencies.yaml` (primelocus-hydra entry) | S — change `status: verified → deleted_upstream`, redirect `repo_url → https://github.com/mikecubed/Hydra` with `forked_from_deleted: PrimeLocus/Hydra`, downgrade `relevance_score: 4 → 3` |
| H-03 | `spring-ai-agent-skills.last_known_version: "Spring AI (2026-01)"` was already 5 days behind reality at the moment of `last_checked: 2026-04-11` — v0.5.0 (2026-02-24), v0.6.0 (2026-03-30), and v0.7.0 (2026-04-06) had all shipped (T07 Δ-spring-ai-01) | `workflow-system/agent/knowledge/reference-dependencies.yaml` (spring-ai entry) | S — set `last_known_version: "v0.7.0 (2026-04-06): MemoryTools + mkdocs site; preceded by v0.6.0 (2026-03-30) and v0.5.0 (2026-02-24): subagent SPI extraction"`, add `releases_total: 9` |
| H-04 | `agent-skills-security.last_known_version: "arXiv:2602.12430 (2026-02)"` lacks sub-version pin; arXiv has 3 revisions, current is v3 (2026-02-17) (T06 Δ-skills-sec-01) | `workflow-system/agent/knowledge/reference-dependencies.yaml` (agent-skills-security entry) | S — pin `last_known_version: "arXiv:2602.12430v3 (2026-02-17)"`, add `companion_repo: "https://github.com/scienceaix/agentskills"` |
| H-05 | `skillrouter.last_known_version: "v3 (arXiv:2603.22455v3)"` is stale — paper advanced to v4 (2026-04-01) with two new findings (cross-source generalisation, agent-coupled win rate) (T05 Δ-skillrouter-01/02) | `workflow-system/agent/knowledge/reference-dependencies.yaml` (skillrouter entry) | S — set `last_known_version: "v4 (arXiv:2603.22455v4, 2026-04-01)"`, append key_pattern: "v4 end-to-end study: routing gains transfer to task success, scale with agent capability" |
| H-06 | Follow-up paper `arXiv:2604.02837v1` ("Towards Secure Agent Skills: Architecture, Threat Taxonomy, and Security Analysis", different team, cs.CR primary) confirmed shipped 2026-04-03 — needs its own `periodic_monitoring` entry, not just an `update_triggers` keyword (T06 Δ-skills-sec-02) | `workflow-system/agent/knowledge/reference-dependencies.yaml` (new entry under periodic_monitoring) | M — add new entry `id: agent-skills-threat-taxonomy`, `relevance_score: 4`, `last_known_version: "arXiv:2604.02837v1 (2026-04-03)"`, key_patterns from threat taxonomy (4 lifecycle phases × 7 categories × 17 scenarios × 3 attack layers) |

**Recommendation:** ship Category 0 as a single small "registry hygiene" PR ahead of any TIER-1 work — keeps the v7.2.0 design loop pointing at correct upstream truth.

---

## Tier Summary

| Tier | Definition | Count |
|------|------------|-------|
| TIER-1 | priority ≥ 3.5 (ship in v7.2.0; quick wins prioritised) | 7 |
| TIER-2 | priority 2.0 – 3.5 (ship in v7.2.0 if budget allows; otherwise carry to v7.3) | 13 |
| TIER-3 / Reject | priority < 2.0 (defer / monitor only / no v7.2 source change) | 55 |
| **Total kept** | (import+monitor) | **75** |
| Dropped (skip) | explicit no-op / out-of-mandate | 21 |
| Registry Hygiene (Category 0) | data bugs in reference-dependencies.yaml | 6 (subset of monitor) |

---

## TIER-1 Candidates

Ranked by priority desc. All seven also satisfy QUICK-WIN (cost ≤ 2 and risk ≤ 2).

### C-001 [QUICK-WIN | source: anthropic-advisor-tool] - Conciseness instruction cuts advisor output 35–45%
- delta_id: T01-Adv-Delta-2
- source_ref: anthropic-advisor-tool (relevance=5)
- description: New documented optimisation: "The advisor should respond in under 100 words and use enumerated steps, not explanations." (verbatim) — claimed to reduce advisor output tokens 35–45% with no call-frequency change.
- integration_target: `src/devolaflow/task_adaptive_selector.py` (`_build_advisor_section`); `workflow-system/agent/context_profiles.yaml` (add optional `advisor.conciseness_instruction: bool` flag per profile)
- value: 5/5 - Direct cost win (~40% of advisor output tokens) across all 6 advisor-enabled task profiles (feature, refactoring, migration, security_audit, performance_optimization, full_pipeline); aligns with existing `cost_ceiling_usd: 0.30` budget
- cost: 1/5 - Trivial 1-line addition to the emitted advisor system prompt + 1 yaml flag
- risk: 1/5 - Default-on can be flagged off per profile; first-party Anthropic guidance with internal-eval evidence
- priority: 5.0
- nines: abstract (deferred)
- sketch:

```
in src/devolaflow/task_adaptive_selector.py::_build_advisor_section,
append "Reply in under 100 words and use enumerated steps, not explanations."
to the emitted advisor system-prompt block; flag-gated via
context_profiles.yaml::advisor.conciseness_instruction (default true).
```

- conflicts: C-003, C-008 — all three modify `_build_advisor_section`; merge into single PR. Touches `context_profiles.yaml` (CO-3 budget; verify per-profile schema unchanged).
- s03_validation: EvoBench cost dimension delta vs `benchmarks/devolaflow_context/baselines/v7.1.0_baseline.json`; expect ≥30% advisor-output-token reduction across 6 enabled profiles, no regression in composite score.

### C-002 [QUICK-WIN | source: caveman] - Compression bypass for security warnings & destructive ops
- delta_id: T02-D3.2
- source_ref: caveman (relevance=4)
- description: caveman/SKILL.md `## Auto-Clarity` section (verbatim): "Drop caveman for: security warnings, irreversible action confirmations, multi-step sequences where fragment order risks misread, user asks to clarify or repeats question." Apr 15 commit "fix(security): harden flag-file reads and refuse sensitive-file compression" extends this to caveman-compress: refuses to compress sensitive files.
- integration_target: `schemas/lean-dispatch.yaml#compression_rules` (lines 169-179, add `bypass_conditions:` list); `schemas/lean-report.yaml#compression_rules` (mirror); `src/devolaflow/compressor.py` (decision point)
- value: 5/5 - Resolves a real safety gap — DROP TABLE warnings could lose criticality under `aggressive` intensity today; caveman shipped this fix as an Apr-15 hardening commit
- cost: 2/5 - Additive `bypass_conditions: [security_warning, destructive_operation, multi_step_sequence_with_order_dependency, repeated_user_question]` extends existing `compression_rules` block (P6-safe, no new top-level key)
- risk: 1/5 - On bypass match, `compressor.py` returns source verbatim and emits a one-line warning to wave agent — preserves audit trail
- priority: 4.5
- nines: abstract (deferred)
- sketch:

```
extend schemas/lean-dispatch.yaml#compression_rules and lean-report.yaml mirror
with bypass_conditions: [security_warning, destructive_operation,
multi_step_sequence_with_order_dependency, repeated_user_question];
src/devolaflow/compressor.py returns source verbatim on any match
and emits a one-line warning to the wave agent.
```

- conflicts: P6 cache layout invariant (must extend existing `compression_rules` block — do not insert a new top-level key); couples with T02-D3.4 (validate-then-fix loop, T3) for content-level validation downstream.
- s03_validation: New `tests/test_compressor.py::test_compression_bypass_conditions` — fixture with `DROP TABLE users;` warning under `aggressive` intensity must come through verbatim; assert one-line warning emitted; couple with `tests/test_benchmarks.py` to confirm no composite-score regression on non-bypass payloads.

### C-003 [QUICK-WIN | source: anthropic-advisor-tool] - Advisor system-prompt timing & reconcile-on-conflict blocks
- delta_id: T01-Adv-Delta-1
- source_ref: anthropic-advisor-tool (relevance=5)
- description: Docs ship two verbatim prompt blocks under "Suggested system prompt for coding tasks" — (1) timing: "Call advisor BEFORE substantive work … On tasks longer than a few steps, call advisor at least once before committing to an approach and once before declaring done"; and (2) reconcile-on-conflict: "If you've already retrieved data pointing one way and the advisor points another: don't silently switch. Surface the conflict in one more advisor call — 'I found X, you suggest Y, which constraint breaks the tie?'".
- integration_target: `workflow-system/agent/SKILL.md` (L3 Task Agent system prompt extension); `src/devolaflow/task_adaptive_selector.py::_build_advisor_section` (already emits "## Advisor Tool" — extend with timing + reconcile blocks)
- value: 4/5 - High signal for L3 agents already in 6 advisor-enabled profiles; first-party Anthropic guidance with internal-eval evidence ("highest intelligence at near-Sonnet cost"); covers a documented failure mode (silent override of retrieved data)
- cost: 1/5 - ~50-line patch to one helper; single file edit
- risk: 1/5 - Pure prompt extension; no behaviour change at API level; opt-out by profile flag if desired
- priority: 4.0
- nines: abstract (deferred)
- sketch:

```
extend src/devolaflow/task_adaptive_selector.py::_build_advisor_section
to emit (1) timing block ("Call advisor BEFORE substantive work...")
and (2) reconcile-on-conflict block; ~50-line patch to one helper;
applies to all 6 advisor-enabled task profiles.
```

- conflicts: C-001, C-008 — all three modify `_build_advisor_section`; merge into single PR.
- s03_validation: Snapshot test on emitted advisor section for each of 6 profiles — assert both blocks present verbatim; EvoBench convergence-loop scenario should show fewer "agent flips approach without acknowledging advisor" patterns (qualitative, no numeric SLO).

### C-004 [QUICK-WIN | source: anthropic-coordination-blog] - Wave Coordination Modes pairwise decision rubrics + hybrid recipes
- delta_id: T01-CB-Delta-1
- source_ref: anthropic-coordination-blog (relevance=5)
- description: Section "Choosing and evolving between patterns" provides explicit pairwise decision rubrics + hybrid recipes (verbatim): "A common hybrid uses orchestrator-subagent for the overall workflow with shared state for a collaboration-heavy subtask. Another uses message bus for event routing with agent team-style workers handling each event type." Plus 4 pairwise rubrics (orchestrator-subagent vs agent teams; vs message bus; agent teams vs shared state; message bus vs shared state).
- integration_target: `workflow-system/agent/SKILL.md` "Wave Coordination Modes" table (currently 4 rows: parallel / sequential / generator_verifier / hybrid — `hybrid` row is undocumented as to which hybrids); `workflow-system/agent/references/execution-protocol.md` (add coordination-mode selection rubric)
- value: 4/5 - Annotates the underspecified `hybrid` row in SKILL.md with concrete recipes; gives DevolaFlow vocabulary to document why P5 (artifacts as contracts) intentionally rejects shared-state pattern; the orchestrator-subagent + shared-state hybrid is a near-perfect characterisation of the existing `self-update` workflow research stage
- cost: 1/5 - 5-10 line documentation edit; SKILL.md still under 500-line SF-1 budget
- risk: 1/5 - Documentation-only; additive
- priority: 4.0
- nines: abstract (deferred)
- sketch:

```
annotate workflow-system/agent/SKILL.md "Wave Coordination Modes"
hybrid row with the two named hybrid recipes from the blog plus
a one-line decision rubric per pattern pair (orchestrator-subagent
vs agent teams vs message bus vs shared state); doc-only edit.
```

- conflicts: C-005 — both edit SKILL.md "Wave Coordination Modes"; coordinate the edits in one pass to keep the table coherent.
- s03_validation: SI-5 coupled checks (line count `wc -l` ≤ 500; adapter build for all 4 adapters succeeds within budget; `tests/test_benchmarks.py` shows no regression on context-density scenarios).

### C-005 [QUICK-WIN | source: superpowers] - Inline self-review checklists for low-risk waves (50× speedup)
- delta_id: T01-Sup-Delta-1
- source_ref: superpowers (relevance=5)
- description: v5.0.6 release notes (verbatim): "Replaced subagent review loops with inline self-review checklists in brainstorming and writing-plans skills, reducing review time from ~25 minutes to ~30 seconds while maintaining comparable defect detection."
- integration_target: `workflow-system/agent/SKILL.md` "Wave Coordination Modes" — add `inline_self_review` mode for low-risk waves; `workflow-system/agent/references/decomposition-gate.md` — alternative to `gen_verify_mode` for design-only / documentation stages; `workflow-system/agent/context_profiles.yaml` (add opt-in `inline_review_checklist: bool` flag per profile)
- value: 5/5 - 50× speedup with comparable defect detection (~25 min → ~30 sec); aligns with "match ceremony to complexity" principle in SKILL.md Quick Action Decision table; cheapest stage cost win in the survey
- cost: 2/5 - ~30-line patch (SKILL.md row + 1 reference subsection + 1 yaml flag); opt-in default off
- risk: 2/5 - Low-risk addition: only fires on `inline_review_checklist: true` profiles (default off, e.g. design / research / documentation only); does not replace `generator_verifier` mode for code-affecting stages
- priority: 4.0
- nines: abstract (deferred)
- sketch:

```
add opt-in inline_review_checklist flag to context_profiles.yaml
for documentation/research stages; references/decomposition-gate.md
documents alternative to gen_verify_mode for low-risk stages;
dispatcher emits checklist instead of spawning verifier subagent.
```

- conflicts: C-004 — both edit SKILL.md "Wave Coordination Modes" table; coordinate edits in one pass.
- s03_validation: `tests/test_benchmarks.py` context-density wins on documentation/research workflows (expect ≥40% wallclock reduction on EvoBench design-only scenarios with comparable composite_score); spot-check defect detection rate on hand-curated documentation-stage golden set.

### C-006 [QUICK-WIN | source: get-shit-done] - Tiered agent size-budget enforcement (XL/Large/Default)
- delta_id: T02-D1.2
- source_ref: get-shit-done (relevance=4)
- description: gsd v1.37.0 release notes (verbatim): "Agent size-budget enforcement — Tiered line-count limits (XL: 1 600, Large: 1 000, Default: 500) keep agent prompts lean; violations surface in CI" (issue #2361).
- integration_target: `tests/test_skill_md_under_500_lines` (already exists per SF-1) → extend with new `tests/test_reference_size_budgets.py` covering `workflow-system/agent/references/*.md` (8 files) and `workflow-system/agent/examples/*.md` (3 files); `.cursor/rules/skill-format-rules.mdc` SF-1 update
- value: 4/5 - Near-zero-cost extension of existing SF-1 rule; closes the asymmetry where references and examples are currently held to the same 500-line ceiling as SKILL.md (suboptimal — references / examples can legitimately be longer)
- cost: 1/5 - ~40 lines of code, fully covered by SI-10 pre-commit checklist; iterates the file lists already in `scripts/sync_cursor_skill.py`
- risk: 1/5 - CI-test extension only; no runtime / behavioural change; tier enforcement is purely additive
- priority: 4.0
- nines: abstract (deferred)
- sketch:

```
extend SF-1 to tiered budget: keep SKILL.md ≤500 (Default tier),
permit references/*.md to grow to 1000 (Large), permit examples/*.md
to grow to 1600 (XL); add tests/test_reference_size_budgets.py
iterating file lists from scripts/sync_cursor_skill.py.
```

- conflicts: none (purely additive). Note SI-5 (Skill Format Coupling) requires re-running adapter build + benchmarks if SF-1 itself changes.
- s03_validation: New `tests/test_reference_size_budgets.py` — assert each file's `wc -l` against its tier; couple with existing `test_skill_md_under_500_lines` (SF-1 unchanged for SKILL.md); SI-10 sequence still passes.

### C-007 [QUICK-WIN | source: gstack] - Activate operational.jsonl: files+source fields + dedup helper
- delta_id: T02-D2.1
- source_ref: gstack (relevance=4)
- description: gstack `/learn` SKILL.md (verbatim from `learn/SKILL.md` lines 433, 677-704): operational learnings JSONL is `~/.gstack/projects/{slug}/learnings.jsonl` with entries `{"skill","type","key","insight","confidence":N,"source","files":[...],"ts":...}` and `{key, type}` dedup with last-write-wins on `ts`. DevolaFlow's Learning dataclass has 13 fields but lacks `files`, `source`, and dedup pass.
- integration_target: `src/devolaflow/learnings.py` (Learning dataclass — currently lines 51-73); `workflow-system/agent/knowledge/learnings/operational.jsonl` (currently empty file per T02 §CC-3 + T04 evidence)
- value: 4/5 - Three additive changes activate the dormant learnings subsystem (`capture_learning()` / `load_relevant_learnings()` / `prune_learnings()` are all implemented but never write because schema gaps prevent provenance); pairs cleanly with C-009 (reflective reflex) to actually start populating the JSONL
- cost: 2/5 - Two new optional fields on the dataclass + one new helper `dedup_learnings(entries) -> list[Learning]`; coverage stays ≥80% per CP-2; no wire format change (P6-safe)
- risk: 1/5 - Strictly additive; backward-compat (defaults to empty list / empty string); does not touch `lean-dispatch.yaml` / `lean-report.yaml` schemas
- priority: 3.5
- nines: abstract (deferred)
- sketch:

```
in src/devolaflow/learnings.py: add files: list[str] = field(default_factory=list)
and source: str = "" fields to Learning dataclass; add dedup_learnings(entries)
helper returning latest-timestamp entry per (task_type, key) pair;
all additive, schema backward compatible, P6-safe.
```

- conflicts: C-009 (reflective reflex) consumes the new `source: "observed"` value; C-012 (learnings_substrate_design) and C-014 (log.md) define adjacent artifacts in the same `knowledge/learnings/` substrate — coordinate naming. Touches `learnings.py` which is in `feedback.py`'s LOCKED_FILES set neighbour but `learnings.py` itself is not locked.
- s03_validation: `tests/test_learnings.py::test_dedup_learnings` (golden fixtures with overlapping `(task_type, key)` pairs); backward-compat test loads pre-v7.2 JSONL entries without `files` / `source`; coverage ≥80% confirmed.

---

## TIER-2 Candidates (briefer)

Ranked by priority desc; ship in v7.2 if budget allows, else carry to v7.3.

### C-008 [source: anthropic-advisor-tool] - Advisor-side prompt caching (`caching: ephemeral 5m / 1h`)
- delta_id: T01-Adv-Delta-3
- source_ref: anthropic-advisor-tool (relevance=5)
- description: New tool parameter `caching` enables advisor's own transcript caching across calls; break-even at ≥3 advisor calls per conversation; setting must remain stable across the conversation.
- integration_target: `workflow-system/agent/context_profiles.yaml` (advisor section: add `caching` subkey); `src/devolaflow/task_adaptive_selector.py`
- value: 4/5 — Convergence-loop stages easily exceed 3 advisor calls (`gen_verify_max_rounds: 3` × multiple waves); compounds across rounds.
- cost: 2/5 — Forward `caching` config in two files; default `caching: null` preserves backward compat.
- risk: 2/5 — Toggling causes cache misses; pairs with monitoring of T01-Adv-Delta-4 (clear_thinking caveat, C-017).
- priority: 3.0
- conflicts: C-001, C-003 (advisor cluster — single PR); C-017 (clear_thinking caveat is the documentation pair-with).
- s03_validation: EvoBench convergence-loop scenarios with ≥3 advisor calls; verify cache-hit reporting from `usage.iterations[]` (deferred to v7.3 if no observability surface yet).

### C-009 [source: gstack] - Reflective Operational Self-Improvement (Pre-Completion Reflection)
- delta_id: T02-D2.2
- source_ref: gstack (relevance=4)
- description: gstack `learn/SKILL.md` lines 422-438 (verbatim): "Before completing, reflect on this session: Did any commands fail unexpectedly? Did you take a wrong approach and have to backtrack? Did you discover a project-specific quirk … A good test: would knowing this save 5+ minutes in a future session? If yes, log it."
- integration_target: `workflow-system/agent/SKILL.md` (L3 Task Agent execution protocol); `workflow-system/agent/references/execution-protocol.md` (closing/cleanup phase); `schemas/lean-report.yaml` (extend with optional `learnings:` block)
- value: 4/5 — Activates dormant `learnings/operational.jsonl`; complements C-007 (schema additions).
- cost: 2/5 — Doc + schema extension; only on `state == DONE | DONE_WITH_CONCERNS`.
- risk: 2/5 — `lean-report.yaml` is a P6 cache-layout-invariant file — must append (not insert) any new top-level key per ADR.
- priority: 3.0
- conflicts: C-007 (schema feed); P6 cache layout invariant on `lean-report.yaml`.
- s03_validation: After 2-3 self-update workflow runs, `operational.jsonl` should contain ≥5 entries with `source: "observed"`; spot-check that ≥80% are non-trivial (5+min-savings test).

### C-010 [source: vexp] - Add vexp to plugins.yaml as recommended_optional MCP server
- delta_id: T05-Δ-vexp-01
- source_ref: vexp (relevance=5)
- description: vexp v1.3.11 is now publicly installable (`code --install-extension Vexp.vexp-vscode` or `npm install -g vexp-cli`); bundles the Rust daemon + MCP server.
- integration_target: `workflow-system/agent/plugins.yaml` (mentioned in `context_profiles.yaml#meta.nines_integration.plugin_registry` and `meta.ui_integration.plugin_registry`)
- value: 3/5 — Enables L3 agents on large codebases to use vexp for context capsule; DevolaFlow does not own the implementation (proprietary Rust).
- cost: 1/5 — Single YAML entry.
- risk: 1/5 — Purely opt-in; no runtime dependency added.
- priority: 3.0
- conflicts: none.
- s03_validation: `Grep` confirms entry under `recommended_optional`; `context_profiles.yaml` schema validators still pass; no DevolaFlow source code consumes vexp at v7.2.

### C-011 [source: agent-skills-security (arXiv:2604.02837v1)] - Data-instruction envelope spec
- delta_id: T06-Δ-skills-sec-05
- source_ref: agent-skills-security (relevance=4) — specifically the v2604 follow-up
- description: Lead structural threat from arXiv:2604.02837v1 (verbatim): "absence of a data-instruction boundary". DevolaFlow's `task-dispatch.schema.yaml` ships `predecessor_artifacts.summary` and `shared_context` as plain markdown — a malicious upstream artifact could inject `IGNORE PRIOR INSTRUCTIONS …` into a downstream layer.
- integration_target: design note in `.local/research/`; later `src/devolaflow/compressor.py` (`envelope_artifact_data(text) -> "<artifact_data type='…'>…</artifact_data>"`); `workflow-system/agent/SKILL.md` agent-instruction telling agents to treat tagged regions as data not commands
- value: 4/5 — Mitigates a real materialised threat in DevolaFlow's actual schema; catches a documented agent-skills security class.
- cost: 2/5 — Design note for v7.2; implementation can wait for a later wave.
- risk: 2/5 — Envelope wrapping is additive but agents must reliably honour the data tag; needs SKILL.md instruction synchronisation.
- priority: 3.0
- conflicts: C-002 (compression bypass — both touch `compressor.py`); P6 cache layout invariant on dispatch schemas.
- s03_validation: `tests/test_compressor.py::test_envelope_artifact_data` (round-trip preserves content; tag survives compression intensities); manual prompt-injection probe via crafted `predecessor_artifacts.summary` against L1→L2 dispatch path.

### C-012 [source: spring-ai-agent-skills (v0.7.0 AutoMemoryTools)] - Learnings substrate design note
- delta_id: T07-Δ-spring-ai-04
- source_ref: spring-ai-agent-skills (relevance=3) — v0.7.0 AutoMemoryTools pattern
- description: v0.7.0 (2026-04-06) introduced `AutoMemoryTools` + `AutoAutoMemoryToolsAdvisor`: "Persistent, file-based long-term memory that survives across conversations. Agents store typed memory files (`user`, `feedback`, `project`, `reference`) in a sandboxed directory and navigate them via a `MEMORY.md` index." Companion system prompt bundled.
- integration_target: design note `learnings_substrate_design.md` in `.local/research/`; later `workflow-system/agent/knowledge/learnings/` directory + `index.md` (DevolaFlow analogue of `MEMORY.md`); wires `gate/reinforcement.py::findings_to_reinforcement()` to optionally append a `pattern` entry when finding repeats N times.
- value: 4/5 — Closes H5 / H7 / L6 gap cluster; activates the absent `learnings/` substrate that is referenced from `knowledge/index.md` but does not exist on disk.
- cost: 2/5 — Design note for v7.2; implementation deferred to later wave.
- risk: 2/5 — Additive subsystem; depends on C-007 (schema) and C-009 (reflex) landing first.
- priority: 3.0
- conflicts: C-007, C-009, C-014 (learnings cluster — coordinate substrate naming and four typed entries: `feedback / pattern / decision / reference`).
- s03_validation: Design note has 4 sections: (a) substrate layout, (b) typed entry schema mapping to gstack `/learn` JSONL, (c) write triggers (reflex / repeat-finding threshold), (d) consumption surface (`load_relevant_learnings()` already exists). No code change in v7.2.

### C-013 [source: forrestchang/andrej-karpathy-skills (EXAMPLES.md)] - Karpathy EXAMPLES.md pointer in Simplicity Check
- delta_id: T03-DELTA-KARP-FRSCH-02
- source_ref: forrestchang/andrej-karpathy-skills (relevance=4)
- description: `EXAMPLES.md` (14.8 KB, added 2026-01-31, PR #7) ships 8 worked examples mapping wrong-vs-surgical diffs onto each of the 4 Karpathy principles plus an "Anti-Patterns Summary" table.
- integration_target: `workflow-system/agent/references/team-roles.md` (Simplicity Check section, line 413)
- value: 2/5 — Pointer-only enrichment; do not inline (license MIT but SF-1 line budget + SF-5 absolute-path discipline argue for relative reference).
- cost: 1/5 — Single line edit.
- risk: 1/5 — Pure documentation.
- priority: 2.0
- conflicts: none.
- s03_validation: SI-5 coupled checks (`team-roles.md` line count still under tier ceiling); spot-check rendered docs.

### C-014 [source: karpathy-llm-wiki] - knowledge/log.md chronological append-only log
- delta_id: T04-Δ-karpathy-02
- source_ref: karpathy-llm-wiki (relevance=3)
- description: Karpathy gist explicitly recommends `log.md` as second special file alongside `index.md`, with consistent date-prefixed headings (`## [YYYY-MM-DD] ingest | …`) so unix `grep` becomes the query API.
- integration_target: `workflow-system/agent/knowledge/log.md` (new file)
- value: 3/5 — Closes part of `learnings/operational.jsonl` debt; supports H5 gap; auto-populates SI-8 retrospective material.
- cost: 2/5 — One new file + a ~5-line append protocol per knowledge edit.
- risk: 2/5 — No tooling required; agent discipline-driven.
- priority: 2.0
- conflicts: C-012 (learnings_substrate_design) — decide whether `log.md` lives at `knowledge/log.md` (sibling to `index.md`) or under `knowledge/learnings/log.md`.
- s03_validation: After 1-2 self-update workflows, file contains ≥3 dated entries; `grep '## \[2026-' workflow-system/agent/knowledge/log.md` returns expected lines.

### C-015 [source: karpathy-llm-wiki] - File-back-from-Review threshold spec (repeat_finding_threshold)
- delta_id: T04-Δ-karpathy-04
- source_ref: karpathy-llm-wiki (relevance=3)
- description: Gist key insight: "good answers can be filed back into the wiki as new pages … your explorations compound in the knowledge base". DevolaFlow's documented integration point "Review agent (query-back-to-wiki for recurring findings)" is not implemented today.
- integration_target: design note in `.local/research/`; eventually `src/devolaflow/gate/reinforcement.py` post-convergence hook
- value: 3/5 — Closes loop on H5 gap; addresses "feedback module is a load-bearing skeleton without the closing edge".
- cost: 2/5 — Spec only in v7.2; implementation deferred.
- risk: 2/5 — Additive policy; `dry_run` default; agent-discretion-bounded.
- priority: 2.0
- conflicts: C-016 (apply_proposal closing loop) — both extend the feedback flywheel; coordinate scope.
- s03_validation: Design note enumerates (a) repeat-count threshold N, (b) target file naming convention under `knowledge/learnings/`, (c) authority for write (gate vs L3 task), (d) dry-run guardrail.

### C-016 [source: self-improving-system] - feedback.apply_proposal() loop closure
- delta_id: T06-Δ-self-improve-05
- source_ref: self-improving-system (relevance=4)
- description: DevolaFlow's `feedback.py` implements detection (`FeedbackAnalyzer.detect_recurring_violations()`, ≥3 occurrences in `learnings/operational.jsonl`) and proposal (`Proposal(target_file, suggested_change, confidence)`) but lacks an `apply_proposal()` that actually edits the rule file. The `_inside_devolaflow()` and `_is_locked()` guards are wired but no commit-the-edit step.
- integration_target: `src/devolaflow/feedback.py` (new function `apply_proposal(proposal: Proposal, dry_run: bool = True)` with SI-1 review gate before `dry_run=False`)
- value: 4/5 — Activates the load-bearing feedback flywheel; reuses existing scope-lock guards.
- cost: 3/5 — New function + tests + dry_run gating + SI-1 hook for review.
- risk: 3/5 — `feedback.py` is in `LOCKED_FILES` set itself (`__init__.py, pyproject.toml, feedback.py`) — adding `apply_proposal` requires careful self-modification audit; needs SI-1 review gate before `dry_run=False`.
- priority: 2.0
- conflicts: C-015 (file-back threshold) — both extend feedback flywheel; coordinate scope and avoid double-write.
- s03_validation: `tests/test_feedback.py::test_apply_proposal_dry_run` (asserts no file write); `test_apply_proposal_blocked_for_locked_files`; full SI-10 6-step pre-commit gate after each apply attempt.

### C-017 [source: anthropic-advisor-tool] - clear_thinking caveat (advisor cache stability)
- delta_id: T01-Adv-Delta-4
- source_ref: anthropic-advisor-tool (relevance=5)
- description: New documented warning (verbatim): "When extended thinking is enabled without explicit `clear_thinking` configuration, the API defaults to `keep: {type: 'thinking_turns', value: 1}`, which triggers this behavior. Set `keep: 'all'` to preserve advisor cache stability."
- integration_target: `workflow-system/agent/references/execution-protocol.md` (interaction-with-thinking caveat); `workflow-system/agent/context_profiles.yaml` (document recommended `clear_thinking.keep: all` when `advisor.caching` enabled)
- value: 2/5 — Pure cost-degradation, not correctness — only worth importing once C-008 ships.
- cost: 1/5 — Documentation-only.
- risk: 1/5 — No behaviour change.
- priority: 2.0
- conflicts: C-008 (only meaningful once advisor caching ships).
- s03_validation: Doc render; no behavioural test.

### C-018 [source: vexp] - Refresh registry SWE-bench numbers (drop FastAPI claim)
- delta_id: T05-Δ-vexp-02
- source_ref: vexp (relevance=5)
- description: vexp's "+14pp on FastAPI" headline number is gone; canonical benchmark is now SWE-bench Verified 100-task subset, Claude Opus 4.5: `vexp+Claude Code 73.0% / $0.67 / 7-10 unique wins`.
- integration_target: `workflow-system/agent/knowledge/reference-dependencies.yaml#vexp.key_patterns` (replace stale claim); reference in v7.2 retrospective per SI-8 as external validation of context-engineering thesis.
- value: 2/5 — Registry hygiene (overlaps with Category 0 spirit but not in the 6-bug list since it's a key_patterns refresh, not a structural data bug).
- cost: 1/5 — YAML edit.
- risk: 1/5 — Documentation only.
- priority: 2.0
- conflicts: none.
- s03_validation: YAML schema validators pass; no `tests/test_benchmarks.py` change.

### C-019 [source: google-scion] - Hub / Runtime Broker / Hub-Native Groves primitives in registry
- delta_id: T05-Δ-scion-02
- source_ref: google-scion (relevance=4)
- description: Three concepts the registry `key_patterns` did not capture: Hub (multi-machine control plane), Runtime Broker (machine offering runtimes to a Hub), Hub-Native Groves (workspaces created via Hub API without external git repo).
- integration_target: `reference-dependencies.yaml#google-scion.key_patterns` (add 2 entries)
- value: 2/5 — Architectural framing for any future DevolaFlow parallel-execution subsystem.
- cost: 1/5 — YAML edit.
- risk: 1/5 — Documentation only.
- priority: 2.0
- conflicts: none.
- s03_validation: YAML validator passes.

### C-020 [source: primelocus-hydra (via mikecubed/Hydra fork) + ruflo + advisor] - Per-agent cumulative budget ledger spec
- delta_id: T06-Δ-hydra-05 (also touched by Δ-ruflo-03)
- source_ref: primelocus-hydra (relevance=3 post-downgrade) + ruflo (relevance=4)
- description: A long-running L3 agent that spawns sub-agents (per `decomposition_mode: sub_agents`) has no visible aggregate token / wallclock counter; budgets are checked at message construction time only, not aggregated per-agent-lifetime.
- integration_target: design note in `.local/research/`; eventually `schemas/task-dispatch.schema.yaml` (`header.cumulative_token_budget: int (optional)`) + `compressor.consume_budget(dispatch_id, tokens)` ledger
- value: 4/5 — Real gap for v7.2's continued sub-agent decomposition trajectory; runaway costs in 5-deep sub-agent chains are not currently bounded.
- cost: 3/5 — Spec for v7.2; eventual implementation touches dispatch schema (P6 boundary).
- risk: 3/5 — Touches `task-dispatch.schema.yaml` (P6 cache layout invariant); needs careful key positioning (append after `gate` per SF-3).
- priority: 2.0
- conflicts: P6 cache layout invariant on `task-dispatch.schema.yaml`; couples with C-008 (advisor caching also has cumulative implications).
- s03_validation: Design note enumerates ledger key, persistence (in-memory vs `.local/`), invalidation rules, observability hook; no code change in v7.2.

---

## TIER-3 / Reject (table)

55 items below threshold. Listed compactly: priority noted; only `monitor` items survive; no v7.2 source change implied.

| # | Delta ID | Source ref | Title (short) | V/C/R | Priority | Disposition |
|---|----------|------------|---------------|-------|----------|-------------|
| T3-01 | T01-Adv-Delta-5 | anthropic-advisor-tool | usage.iterations[] cost split | 2/4/3 | -0.5 | monitor (no consumer surface) |
| T3-02 | T01-Sup-Delta-2 | superpowers | sync-to-codex-plugin tooling | 1/3/2 | -0.5 | monitor (DevolaFlow has its own pipeline) |
| T3-03 | T01-Sup-Delta-3 | superpowers | Agent-facing contributor guidelines | 1/2/1 | 0.5 | monitor (CONTRIBUTING.md doesn't exist) |
| T3-04 | T01-CB-Delta-2 | anthropic-coordination-blog | Reactive-loop hazard validation | 1/1/1 | 1.0 | monitor (validates existing P4) |
| T3-05 | T02-D1.1 | gsd | gsd-read-injection-scanner hook | 4/5/4 | 0.5 | monitor (no host-platform hook surface) |
| T3-06 | T02-D1.3 | gsd | Parallel discuss-phase + gsd-graphify | 2/4/3 | -0.5 | monitor (semantic parallel already covered; graph redundant w/ vexp) |
| T3-07 | T02-D1.4 | gsd | Inline execution for small plans | 2/3/3 | 0.0 | monitor (would double-count w/ existing decomposition_mode) |
| T3-08 | T02-D2.3 | gstack | Per-skill allowed-tools ACL | 3/4/3 | 0.5 | monitor (P6 boundary; defer to v7.3) |
| T3-09 | T02-D2.5 | gstack | HARD GATE + Confusion Protocol | 2/2/2 | 1.0 | monitor (overlaps superpowers) |
| T3-10 | T02-D3.1 | caveman | Per-turn UserPromptSubmit reinforcement | 3/5/4 | -0.5 | monitor (no host hook surface) |
| T3-11 | T02-D3.4 | caveman | Validate-then-fix loop on compression body | 3/3/2 | 1.5 | monitor (couples with C-002 once T1 lands) |
| T3-12 | T03-UA-01 | understand-anything | Tree-sitter extractor backend | 2/4/3 | -0.5 | monitor (PluginRegistry ADR data point) |
| T3-13 | T03-UA-03 | understand-anything | /understand-diff impact analysis | 1/1/1 | 1.0 | monitor (advisory only) |
| T3-14 | T03-UA-04 | understand-anything | 10-platform install manifests | 3/3/2 | 1.5 | monitor (flag for adapter pipeline owner) |
| T3-15 | T04-Δ-edict-02 | edict | State machine grew to 11 states | 1/1/1 | 1.0 | monitor (DevolaFlow gate is score-driven not state-machine) |
| T3-16 | T04-Δ-edict-03 | edict | Per-command permission matrix (AGENT_POLICY) | 3/3/3 | 1.0 | monitor (P1 enforcement at runtime; v7.3+) |
| T3-17 | T04-Δ-edict-06 | edict | 4-stage progressive recovery | 1/1/1 | 1.0 | monitor (DevolaFlow has 3-stage analog) |
| T3-18 | T04-Δ-karpathy-03 | karpathy-llm-wiki | knowledge-layer lint pass | 2/3/2 | 0.5 | monitor (knowledge surface small currently) |
| T3-19 | T05-Δ-vexp-03 | vexp | Graph-based ranking gap re-confirmed | 4/5/4 | 0.5 | monitor (vexp is proprietary; delegate via MCP) |
| T3-20 | T05-Δ-vexp-04 | vexp | LSP bridge / intent / session memory primitives | 3/4/3 | 0.5 | monitor (staleness flag idea for v7.3+) |
| T3-21 | T05-Δ-scion-04 | google-scion | OTEL + GCP Logging/Monitoring/Trace | 3/4/3 | 0.5 | monitor (no current observability module) |
| T3-22 | T06-Δ-self-improve-02 | self-improving-system | Cursor session trace example | 2/2/1 | 1.5 | monitor (defer; existing 3 traces cover dispatcher logic) |
| T3-23 | T06-Δ-self-improve-03 | self-improving-system | release-please CI/CD pipeline | 3/4/3 | 0.5 | monitor (.github/workflows absent; v7.3+ scope) |
| T3-24 | T06-Δ-skills-sec-03 | agent-skills-security | trust_level / provenance / scanner | 3/4/3 | 0.5 | monitor (v7.2 retrospective scope decision) |
| T3-25 | T06-Δ-skills-sec-04 | agent-skills-security | Companion repo dormant | 1/1/1 | 1.0 | monitor (curated reading list) |
| T3-26 | T06-Δ-hydra-02 | primelocus-hydra | Track via mikecubed/Hydra fork | 1/1/1 | 1.0 | monitor (downgraded to relevance=3) |
| T3-27 | T06-Δ-ruflo-04 | ruflo | v3.5.80 #1556 validates feedback.LOCKED_FILES | 1/1/1 | 1.0 | monitor (positive validation only) |
| T3-28 | T07-Δ-christophera-02 | christophera-bootstrap-seed | Annotate validation_status | 1/1/1 | 1.0 | monitor (negative-control reference) |
| T3-29 | T07-Δ-christophera-05 | christophera-bootstrap-seed | bootstrap_token_overhead_ratio metric | 2/2/1 | 1.5 | monitor (EvoBench addition for v7.3) |
| T3-30 | T07-Δ-spring-ai-02 | spring-ai-agent-skills | No new upstream activity | 1/1/1 | 1.0 | monitor (no urgency window) |
| T3-31 | T07-Δ-spring-ai-05 | spring-ai-agent-skills | Subagent SPI extraction | 2/4/3 | -0.5 | monitor (host adapter coordination needed) |

The remaining 24 T3 items are pure registry-hygiene refreshes (`refresh-registry only` style) — they bump `last_checked: 2026-04-18`, refresh `last_known_version` strings, or annotate `key_patterns` / `staleness_indicators` without any source change. They are subsumed by the registry hygiene work in Category 0 + the per-ref refreshes documented in each delta-T0X.md "Summary table". Specifically:

T01: (none — all T1/T2/T3 above)
T02: D1.5 (skip; configurable claude_md_path), D2.4 (skip; /codex overlaps), D3.3 (skip; 6-tier intensity not portable)
T03: KARP-UPSTREAM-01, KARP-FRSCH-05, KARP-FRSCH-06, UA-02, UA-05 (registry hygiene — see Category 0 H-01)
T04: Δ-edict-01, Δ-edict-04 (skip), Δ-edict-05, Δ-karpathy-01, Δ-karpathy-05 (skip), Δ-karpathy-06 (skip)
T05: Δ-vexp-05, Δ-scion-01, Δ-scion-03, Δ-scion-05 (skip), Δ-scion-06 (skip), Δ-skillrouter-01, Δ-skillrouter-02, Δ-skillrouter-03 (skip), Δ-skillrouter-04 (registry hygiene — see Category 0 H-04, H-05)
T06: Δ-self-improve-01, Δ-self-improve-04 (skip), Δ-self-improve-06 (skip), Δ-skills-sec-01, Δ-skills-sec-02, Δ-hydra-01, Δ-hydra-03, Δ-hydra-04 (skip), Δ-ruflo-01, Δ-ruflo-02, Δ-ruflo-03 (skip), Δ-ruflo-05 (registry hygiene — see Category 0 H-02, H-03, H-04, H-06)
T07: Δ-christophera-01, Δ-christophera-03 (skip), Δ-christophera-04 (skip), Δ-christophera-06 (skip), Δ-spring-ai-01, Δ-spring-ai-03 (skip), Δ-spring-ai-06 (skip)

---

## Cross-Cutting Themes (≥2 source refs)

Five themes appear in three or more delta reports and are load-bearing for the v7.2.0 scope conversation.

### CCT-1 — Cost-controlled bottom-up escalation (cheap executor + expensive consultant)
**Sources:** T01-Adv (advisor pattern Δ-1/2/3), T01-CB (orchestrator-subagent + generator-verifier), T02-D2.4 (gstack /codex), T05-vexp (Smart Features ranking), T06-hydra (multi-round deliberation).
**Implication for v7.2:** TIER-1 cluster C-001/C-003 + TIER-2 C-008 fully realises the advisor variant of this pattern; T01-CB-Delta-1 (C-004) annotates SKILL.md "Wave Coordination Modes" with the topology vocabulary; the topology and the prompt-side both ship in v7.2. NineS/hydra/codex variants stay in T3.

### CCT-2 — Termination is first-class, not an afterthought (validates P4 Bounded Retry)
**Sources:** T01-Sup-Delta-1 (typed status protocol — third independent confirmation per T02 §CC-1), T01-CB-Delta-2 (reactive-loop hazard), T02-D2.2 (DONE/DONE_WITH_CONCERNS/NEEDS_CONTEXT/BLOCKED verbatim mirrored by gstack), T07-christophera-03 (reflect→triage→cascade weaker than DevolaFlow's gate+reinforcement).
**Implication for v7.2:** No new code required — P4 + the typed status enum are validated industry-pattern-status. Cite the triple convergence in v7.2 retrospective per T02 §CC-1. Strengthens the case for keeping TIER-3 hook-surface items parked (CCT-5).

### CCT-3 — Activation of the dormant learnings substrate (highest-leverage v7.2 cluster)
**Sources:** T02-D2.1 (gstack JSONL schema → C-007), T02-D2.2 (reflective reflex → C-009), T04-Δ-karpathy-02 (log.md → C-014), T04-Δ-karpathy-04 (file-back threshold → C-015), T06-Δ-self-improve-05 (apply_proposal → C-016), T07-Δ-spring-ai-04 (AutoMemoryTools substrate → C-012).
**Implication for v7.2:** Six deltas across 4 source refs all converge on `workflow-system/agent/knowledge/learnings/operational.jsonl` (currently empty file). C-007 (schema) + C-009 (reflex) is the minimum activation; C-012 (substrate design note) sets the v7.3+ direction; C-014/C-015/C-016 are independent instalments. Coordinate naming under one v7.2 "Learnings Activation" wave. Per T02 §CC-3: "expect the JSONL to start accumulating entries within 2-3 self-update workflow runs."

### CCT-4 — Compression hygiene (preserve safety-critical content under aggressive intensity)
**Sources:** T02-D3.2 (caveman bypass → C-002), T02-D3.4 (validate-then-fix loop, T3-11), T01-Adv-Delta-2 (conciseness instruction → C-001), T02-D1.2 (tiered agent budgets → C-006), T06-Δ-skills-sec-05 (data-instruction envelope → C-011).
**Implication for v7.2:** Five deltas converge on the `compressor.py` + `compression_rules` schema surface. C-002 + C-006 are quick wins; C-011 is a spec-only design note; T3-11 couples with C-002 if both ship together. P6 cache-layout invariant must be preserved on every schema touch — reinforce per SI-10 step 6 verification.

### CCT-5 — Hook-surface gap is a structural blocker for security/per-turn imports (defer to v8.x)
**Sources:** T02-D1.1 (gsd-read-injection-scanner PostToolUse, T3-05), T02-D3.1 (caveman per-turn UserPromptSubmit reinforcement, T3-10), T02-D2.2 (gstack reflective end-of-skill — has a workaround via reports schema, becomes C-009 in v7.2).
**Implication for v7.2:** Three high-impact deltas all require a host-platform hook abstraction DevolaFlow does not have (Cursor `hooks.json`, Claude Code `~/.claude/hooks/`, Codex `.codex/hooks.json` — three different APIs). Per T02 §CC-2: "do not attempt a hook abstraction in v7.2". Track all three as v8.x-discovery items; no v7.2 source change. C-009 sidesteps the hook gap by living in the typed status report (a contract DevolaFlow already owns) instead of a host hook.

---

## Recommended S03 Validation Set (7 items, within 5-12 range)

The S03 wave should validate all seven TIER-1 candidates. They split cleanly into three coordinatable PRs to honour P6 / SI-5 / SI-10 coupling and minimise conflicts.

| Order | Candidate | PR group | Validation focus | Acceptance signal |
|-------|-----------|----------|------------------|-------------------|
| 1 | C-006 (tiered size budgets) | PR-A: SF-1 extension | Single-file CI test addition; lowest-risk start | `tests/test_reference_size_budgets.py` passes; SI-10 6-step gate green |
| 2 | C-002 (compression bypass conditions) | PR-B: schema/compressor | P6 layout-invariant compliance; `tests/test_compressor.py::test_compression_bypass_conditions` | DROP TABLE warning preserved verbatim under `aggressive` intensity; one-line warning surfaced |
| 3 | C-007 (learnings schema additions) | PR-C: learnings activation, part 1 | `Learning` dataclass backward-compat; `dedup_learnings` returns latest-timestamp per `(task_type, key)` | Existing JSONL entries (currently 0) load without error; coverage ≥80% |
| 4 | C-001 (advisor conciseness) | PR-D: advisor cluster | EvoBench cost dimension | ≥30% advisor-output-token reduction across 6 enabled profiles; composite_score unchanged |
| 5 | C-003 (advisor timing + reconcile blocks) | PR-D: advisor cluster | Snapshot of emitted advisor section | Both blocks present verbatim across all 6 profiles |
| 6 | C-004 (Wave Coordination Modes hybrid recipes) | PR-E: SKILL.md docs | SI-5 coupled checks | Line count ≤500; all 4 adapters build within budget; benchmarks no regression |
| 7 | C-005 (inline self-review checklists) | PR-E: SKILL.md docs | EvoBench documentation/research scenarios | ≥40% wallclock reduction with comparable composite_score on design-only golden set |

Per SI-4, every candidate above requires a benchmark pass (`python -m pytest tests/test_benchmarks.py -v`) before merge with regression guard (no scenario drops >5% below baseline). Per SI-10 step 1, full `pytest tests/ -q` green required.

---

## Limitations

1. **NineS evaluation deferred.** Per dispatch, S02a is roll-up only; `nines analyze` was not run on any candidate's integration target. All TIER-1 entries record `nines: abstract (deferred)`. A follow-up S02b task should run NineS on the union of TIER-1 integration targets (`task_adaptive_selector.py`, `compressor.py`, `learnings.py`, `compression_rules` schemas, SKILL.md, `references/decomposition-gate.md`, `tests/test_reference_size_budgets.py`) before S03 begins.
2. **Verbatim extraction discipline.** All `description` fields in TIER-1 entries quote verbatim from the cited delta reports per CO-2. Some quotes are second-hand (delta reports already extracted from upstream); the chain is delta → upstream URL listed in delta `source` field, not re-fetched here.
3. **Scoring is heuristic, not measured.** value/cost/risk grades are based on the rationale in each delta report plus cross-referenced existing-integration evidence. They are not the output of a NineS dimension-weighted score (per SI-3 the formal dimension scoring happens at SI-3 evaluation gate, not here).
4. **TIER cutoffs honour task spec exactly.** T1 ≥ 3.5, T2 [2.0, 3.5), T3 < 2.0. C-007 sits at exactly 3.5 (T1 boundary); C-008..C-012 sit at exactly 3.0 (mid-T2); 8 T2 items sit at exactly 2.0 (T2/T3 boundary). Boundary items rounded toward T1/T2 per "≥" / "[" convention.
5. **Conflicts column is structural-only.** Captures (a) shared file (e.g., advisor cluster), (b) shared schema (P6), (c) shared substrate (learnings cluster). Does NOT capture semantic dependencies (e.g., C-007 must land before C-009 to be useful — that's tracked in `s03_validation` order).
6. **Skip-classification was generous.** Items labelled "no-op for v7.2" or "out of mandate" or "no source change required" were classified as `skip`. Items labelled "track for v7.3+" or "refresh-registry" were classified as `monitor` and scored. Defensible but not the only valid mapping; if the task spec requires strict-skip-only-when-explicitly-`skip` the dropped count would shrink and the T3 count would grow correspondingly.
7. **Registry Hygiene Category 0 lists 6 specific bugs from the task spec.** It does NOT list every registry-refresh action (~25 such items exist across T03–T07 for `last_checked` bumps and `last_known_version` pin updates). Those are tracked in the per-task delta-T0X.md "Summary table" and roll-up via Category 0 H-01..H-06 covers the structural data-bug subset.
8. **Source paths are repository-relative.** Per CO-4 / SF-5; absolute paths avoided in this artifact. External references (anthropic-advisor-tool docs, vexp.dev, gstack/superpowers/caveman/get-shit-done GitHub) keep the upstream URLs they already had in the delta reports.
9. **`nines_targets_analyzed: 0` is an explicit deferral, not a missing data point.** Per task constraints ("DO NOT invoke nines"), no NineS runs were attempted. The S02b NineS-on-TIER-1 task should run before the S03 validation wave begins.
