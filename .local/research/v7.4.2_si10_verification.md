# v7.4.2 SI-10 Verification Report

**Iteration:** v7.4.1 → v7.4.2 (PATCH) · **Branch:** `feat/v7.4.2-repo-init-fix`
**Gate:** W-9 / SI-10 Test-Then-Commit Protocol (6 steps + 4 coupled checks)
**Dispatch:** `S03-T01-si10-verify-gate` · **Predecessor:** `.local/research/v7.4.2_gap_analysis.md`
**Date:** 2026-04-20 · **Verifier:** L3 Test Task Agent

> **Version still at 7.4.1.** `scripts/bump_version.py 7.4.2` is owned by the
> downstream release stage. Step 4 was expected to PASS at `__version__ =
> 7.4.1` and DID — see §2.4.

---

## §1 — Summary Table

| Step / check | Status | Evidence one-liner |
|---|---|---|
| Step 1 — `pytest tests/ -q` | **FAIL_ESCALATE** | 4 failed / 1339 passed / 13 skipped — 3 doc-consistency + 1 benchmark drift; all 4 require touching forbidden files |
| Step 2 — `ruff check src/ tests/` | **PASS** | `All checks passed!` (0 errors) |
| Step 3 — `ruff format --check src/ tests/` | **PASS** | `104 files already formatted` |
| Step 4 — `pytest tests/test_version.py -v` | **PASS** | 12 passed / 13 skipped at v7.4.1 (mirror tests self-skip per SF-3) |
| Step 5 — `pytest tests/test_benchmarks.py -v` | **FAIL_ESCALATE** | 1 failed / 35 passed; `compression_retention_medium` drift **6.23 pp** (99.95 → 93.72) > 5 pp threshold; root-caused to SKILL.md:173 text edit |
| Step 6 — `make check-cursor-skill` | **PASS** | `[.cursor mirror] not present at .cursor/skills/devola-flow — opt-in (see Rule SF-3)` (no-op) |
| Coupled A — `validate_all_templates(all_flag=True)` | **PASS** | `20 passed, 0 failed, 20 total` (was 19; `repo-init.yaml` PASS clean) |
| Coupled B — coverage `devolaflow.init_project` | **PASS** | **87 %** (≥ 80 %) on 128 statements |
| Coupled B — coverage `devolaflow.template_engine` | **PASS** | **81.95 %** aggregate; Wave 1 modules: `parser.py 94 %`, `models.py 100 %` |
| Coupled C — SKILL.md line budget | **PASS** | **499 / 500** lines |
| Coupled D — `bash -n scripts/install.sh` | **PASS** | exit 0 |

Two SI-10 steps fail; both require interventions outside the Wave 1 owned-write surface. **Final verdict: `BLOCKED_NEED_L0_INTERVENTION`** (see §6).

---

## §2 — Per-Step Details

### §2.1 — Step 1 — Full pytest suite (FAIL_ESCALATE)

`python -m pytest tests/ -q` → `4 failed, 1339 passed, 13 skipped in 25.28s`

#### F-1 · `tests/test_benchmarks.py::TestBaselineFile::test_v6_baseline_matches_current_results_within_tolerance`

```
AssertionError: compression_retention_medium: recorded baseline composite 99.95
drifted 6.23 points from current run (93.72).
Regenerate with: python -m benchmarks.devolaflow_context.generate_baseline
assert 6.230000000000004 <= 5.0
```

Same failure as Step 5 — full triage in §2.5.

#### F-2 · `tests/test_doc_consistency.py::test_readme_workflow_type_count`

```
AssertionError: README table has 19 rows, disk has 20 templates, expected 19
assert 19 == 20
```

Source (`tests/test_doc_consistency.py:29`) hardcodes `assert table_rows == yaml_count == 19`. Wave 1 added `repo-init.yaml` (19 → 20 templates) but `README.md` "Built-in Workflow Types" table was not updated AND the test literal `== 19` is stale. Fix needs both `README.md` (forbidden) and the test file.

#### F-3 · `tests/test_doc_consistency.py::test_readme_template_count_in_dev_setup`

```
AssertionError: README dev setup claims 19 templates, disk has 20
assert 19 == 20
```

Wave 1 added a template; `README.md` "Full Development Setup" still claims `19 templates`. Fix needs `README.md` (forbidden).

#### F-4 · `tests/test_doc_consistency.py::test_workflow_skill_yaml_template_count`

```
AssertionError: workflow-skill.yaml has 19 builtin entries, disk has 20
assert 19 == 20
```

Wave 1 added a template; `workflow-system/agent/workflow-skill.yaml` still has 19 `file: "templates/builtin/..."` entries. Fix needs `workflow-skill.yaml` (forbidden).

**Triage:** All 4 failures are *caused by* the Wave 1 surface but the *fixes* require touching files explicitly forbidden in this dispatch (`README.md`, `workflow-skill.yaml`, `benchmarks/.../v7.4.0_baseline.json`). Per dispatch §Step 1 in-scope rules, these are NOT trivial corrections — they are structural doc/baseline coupling failures owned by the release stage.

---

### §2.2 — Step 2 — Ruff lint (PASS)

`ruff check src/ tests/` → `All checks passed!` — 0 errors across all 3 new test files and 4 modified Wave 1 files.

### §2.3 — Step 3 — Ruff format (PASS)

`ruff format --check src/ tests/` → `104 files already formatted`. No drift.

### §2.4 — Step 4 — Version consistency (PASS)

`pytest tests/test_version.py -v` → `12 passed, 13 skipped in 0.04s`.

The 12 passed cover the canonical 7 sync points at v7.4.1 (pyproject, SKILL frontmatter/banner/body, workflow-skill.yaml, generate_human_docs, README badge, benchmark-results sample, CLI version). The 13 skipped are the opt-in `.cursor/skills/devola-flow/` mirror parity / stamp tests — correctly self-skipped because the mirror is absent (per SF-3). No Wave 1 task accidentally edited a versioned location.

### §2.5 — Step 5 — Benchmark regression guard (FAIL_ESCALATE)

`pytest tests/test_benchmarks.py -v` → `1 failed, 35 passed in 9.04s`. 35/36 individual benchmark tests pass; only the cross-baseline drift sanity check fails.

| Scenario | Recorded (v7.4.0_baseline.json) | Current | Drift (pp) | Threshold |
|---|---|---|---|---|
| `compression_retention_medium` | 99.95 | **93.72** | **6.23** | 5.0 |

**Empirical root-cause:** `git stash push workflow-system/agent/SKILL.md` then re-running this single test PASSED. Restoring the SKILL.md edit reproduces the failure. The drift is deterministically caused by the single-line SKILL.md edit at line 173:

```
- | init repo, ... 初始化仓库 | `repo-init` | analyze → scaffold → compile → verify |
+ | init repo, ... 初始化仓库 | `repo-init` | analyze → scaffold → compile → verify (mode: minimal\|standard\|deep) |
```

The `compression_retention_medium` scenario expects the `quick_start_workflows` section (which contains line 173). The +27-character change shifts character/token density enough that the design profile's section-selection composite drops 6.23 pp.

**Note on dispatch expectation gap:** the dispatch §Step 5 stated "Wave 1 made NO compressor / context_profiles scoring / lean schema / scenario edits". This is correct for the `parameters` field on `WorkflowTemplate`, but the dispatch did NOT account for SKILL.md text-density coupling against the `quick_start_workflows` section. The gap analysis pre-flagged D-7 (context_profiles edits) as a benchmark risk but did not pre-flag D-8 (SKILL.md text edits). This is a missed-coverage in the planning gate, not a Wave 1 implementation defect — the current composite 93.72 is still well above the scenario floor `min_composite: 85.0`; only the **recorded baseline** is now stale.

### §2.6 — Step 6 — Cursor-skill mirror parity (PASS)

`make check-cursor-skill` →

```
python scripts/sync_cursor_skill.py --check
[.cursor mirror] not present at .cursor/skills/devola-flow — opt-in (see Rule SF-3)
```

Exit 0. Mirror is gitignored opt-in and absent on this workspace, so the script no-ops. CI surface unaffected.

---

## §3 — Coupled Checks (A–D)

### §3.A — `validate_all_templates(all_flag=True)` (PASS)

```
20 passed, 0 failed, 20 total
EXIT=0
```

Was 19, now 20. `repo-init.yaml` PASS clean; the `parameters` field on `WorkflowTemplate` parses correctly via the new `parser.py:73` passthrough. Pre-existing dependency-lattice WARNINGS on 8 unrelated templates (`feature-enhancement`, `full-pipeline`, `migration`, `nines-assisted`, `product-verification`, `research-design-review-refine`, `security-audit`) are unchanged from baseline and are non-blocking.

### §3.B — Coverage on touched modules (PASS)

```
src/devolaflow/init_project.py     128     17    87%   31-36, 42-43, 53, 101, 187-190, 196, 199-200, 206
Required test coverage of 80.0% reached. Total coverage: 86.72%
26 passed in 0.20s
```

**87 %** on `devolaflow.init_project` — matches Wave 1's reported figure. Missing lines are pre-existing `_find_agent_dir` fallbacks and CLI error paths.

```
src/devolaflow/template_engine/parser.py  71  4   94%   41, 49, 162, 174
src/devolaflow/template_engine/models.py 115  0  100%
TOTAL                                    748 135  82%
Required test coverage of 80.0% reached. Total coverage: 81.95%
75 passed in 1.55s
```

**81.95 %** aggregate. Wave 1-touched files exceed: **`parser.py` 94 %**, **`models.py` 100 %**. Pre-existing low coverage on `composer.py` (66 %), `nines_bridge.py` (21 %), `validator.py` (73 %) is untouched by Wave 1.

### §3.C — SKILL.md line budget (PASS)

`wc -l workflow-system/agent/SKILL.md` → `499 workflow-system/agent/SKILL.md` — under the C-4 / SF-1 default-tier ceiling (500). Unchanged from baseline (Wave 1 D-8 was a single-row text rewrite, no line delta).

### §3.D — `bash -n scripts/install.sh` syntax (PASS)

`bash -n scripts/install.sh` → `EXIT=0`. The 2-line `auto_detect()` extension at line 469-470 (`if [ ! -d ".local" ]; then install_local; found=1; fi`) parses correctly.

---

## §4 — Fixes Applied

**None.** Steps 2/3/4/6 and Coupled A/B/C/D all passed cleanly. The 4 Step 1 failures and the 1 Step 5 failure all require touching forbidden files (`README.md`, `workflow-skill.yaml`, `benchmarks/devolaflow_context/baselines/v7.4.0_baseline.json`) — they are NOT trivial in-scope corrections per the dispatch's triage rules.

`git diff --stat` after this verification is identical to before — only the Wave 1 surface, no collateral damage:

```
 scripts/install.sh                            |  2 +
 src/devolaflow/init_project.py                | 11 +++-
 src/devolaflow/template_engine/models.py      |  1 +
 src/devolaflow/template_engine/parser.py      |  1 +
 tests/test_init_project.py                    | 88 +++++++++++++++++++++++++++
 workflow-system/agent/SKILL.md                |  2 +-
 workflow-system/agent/templates/registry.yaml |  7 +++
 7 files changed, 110 insertions(+), 2 deletions(-)
```

Plus 3 new untracked files: `tests/test_install_sh.py` (126 LOC), `tests/test_template_repo_init.py` (143 LOC), `workflow-system/agent/templates/builtin/repo-init.yaml` (95 LOC).

---

## §5 — Risks & Recommended Remediation for the Release Stage

### R-1 — Doc-consistency failures (F-2 / F-3 / F-4)

Release stage must:

1. `README.md` — add a `repo-init` row to the "Built-in Workflow Types" table; update the heading number `19 Built-in Workflow Types` → `20`; update "Full Development Setup" text from `19 templates` → `20 templates`.
2. `workflow-system/agent/workflow-skill.yaml` — append a `file: "templates/builtin/repo-init.yaml"` entry alongside the existing 19.
3. `tests/test_doc_consistency.py:29` — change hardcoded `== 19` to `== 20` (or, preferably, drop the literal in favour of `assert table_rows == yaml_count` since the hardcoding pattern is itself a maintenance smell that bit this iteration).

Estimated delta: ~6 LOC across 3 files. Expected outcome: F-2/F-3/F-4 all pass.

### R-2 — Benchmark baseline drift (F-1 / Step 5) — RECOMMEND OPTION A

**Option A (preferred):** Regenerate the v7.4.0 baseline:

```
python -m benchmarks.devolaflow_context.generate_baseline
```

This refreshes `benchmarks/devolaflow_context/baselines/v7.4.0_baseline.json` with the new `compression_retention_medium` composite (99.95 → 93.72). Per W-4 / SI-4 / CO-5, baseline regeneration is the canonical response when a SKILL.md content edit deliberately shifts section density. The current composite 93.72 is well above the scenario floor (`min_composite: 85.0`) — only the recorded baseline is stale. Audit the regen diff to confirm only `compression_retention_medium` shifts (other scenarios should be ≤ 1 pp).

**Option B (NOT recommended):** Revert SKILL.md row text. This would defeat Wave 1's D-8 fix and remove the `mode: minimal|standard|deep` parity messaging from the SKILL Workflow-Selection table — trades a benchmark side-effect for a UX regression.

### R-3 — Retrospective coupling note (W-7 / SI-8)

The gap analysis pre-flagged D-7 (context_profiles edits) as a benchmark-coupling risk but did NOT pre-flag D-8 (SKILL.md text edits). Empirically, even single-row SKILL.md text changes inside a benchmark-scored section (`quick_start_workflows`) cross the 5 pp regression threshold. Recommend adding to `.local/research/v7.4.2_retrospective.md` and to the SI-1 planning-gate template: **"Any SKILL.md text edit inside a section scored by EvoBench scenarios must be paired with a baseline regen plan."**

### R-4 — No other regressions detected

Coverage floors hold (87 % init_project, 81.95 % template_engine, 94 % parser, 100 % models). Template registry integrity holds (20 templates parse, repo-init PASS). SKILL.md line budget holds (499/500). install.sh syntax holds. Version consistency at v7.4.1 holds. No collateral file changes.

---

## §6 — Final Verdict

**`BLOCKED_NEED_L0_INTERVENTION`**

**Why:** 2 of the 6 SI-10 steps fail (Step 1 and Step 5), both blocking. Both failures are deterministic, reproducible, and require interventions on files explicitly forbidden to this verification dispatch.

**Concrete next actions for L0:**

1. Dispatch a release-stage L3 Task Agent to apply R-1 (3 file updates, ~6 LOC).
2. Run `python -m benchmarks.devolaflow_context.generate_baseline` to refresh the v7.4.0 baseline JSON (R-2 Option A); audit the diff to confirm only `compression_retention_medium` shifts and that the new composite ≥ 85 floor (current 93.72 ≫ 85).
3. Re-run this SI-10 protocol from Step 1; expect all 6 steps GREEN.
4. Then proceed to the v7.4.2 version bump (`scripts/bump_version.py 7.4.2`) per CP-3 / W-10, run `make sync-human-docs`, and re-run Step 4 to confirm the canonical-7 sync at the new version.

**Already verified GREEN (no re-touch needed):** all 1339 passing tests outside the 4 listed failures, lint, format, version consistency at v7.4.1, mirror parity (no-op), and all 4 coupled checks (A: 20/20 templates; B: 87 % + 81.95 % coverage; C: 499/500 lines; D: install.sh syntax). Wave 1 surface integrity confirmed: only the expected diff, no collateral damage.

After R-1 + R-2 are completed, this iteration is mechanically ready to proceed to the version-bump + release-stage gate.
