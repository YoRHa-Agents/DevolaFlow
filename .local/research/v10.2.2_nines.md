# v10.2.2 NineS Deep Analysis Synthesis

> **Date:** 2026-05-03
> **Cycle:** v10.2.0 PV-03
> **Author:** L3 Task Agent (composer-2-fast)
> **NineS:** v3.3.0 — `analyze --depth deep --agent-impact --keypoints`
> **Raw JSONs:** `.local/research/v10.2.2_nines{,_plugins,_lifecycle}.json`
> **Status:** D-N-3 closure deliverable per cycle plan §3 PV-03.
> **External tools (S-7):** DevolaFlow `https://github.com/YoRHa-Agents/DevolaFlow`,
> NineS `https://github.com/YoRHa-Agents/NineS`,
> Si-Chip `https://github.com/YoRHa-Agents/Si-Chip`.

## §1 — Per-package summary

| Package | Findings count | Files | Total lines | Functions | Avg complexity | Top severity |
|---|---:|---:|---:|---:|---:|---|
| `src/devolaflow/si_chip_bridge/` | 10 | 4 | 1,153 | 17 | 4.83 | info (no warnings) |
| `src/devolaflow/plugins/` | 18 | 6 | 2,033 | 43 | 3.63 | warning ×4 (cyclomatic complexity) |
| `src/devolaflow/lifecycle/` | 34 | 13 | 3,141 | 63 | 4.79 | warning ×6 (cyclomatic complexity) |

Aggregate: 62 findings across 23 files / 6,327 LOC / 123 functions; 10 warning-class signals (all cyclomatic complexity), zero error-class signals.

## §2 — Top findings (severity-sorted)

The 10 warning-severity findings (sorted by complexity descending — highest CC = most refactor leverage). All are mechanically fixable without SKILL/references edits:

| # | Severity | Location | CC | Recommendation |
|---|---|---|---:|---|
| 1 | warning | `src/devolaflow/lifecycle/test_on_complete.py:200` `_try_persist_session_state` | 20 | Extract per-state-shard helpers; the function spans 6 try/except branches that are independently testable. |
| 2 | warning | `src/devolaflow/lifecycle/pre_plugin_invocation.py:269` `pre_plugin_invocation` | 18 | Split daily-upgrade branch (D-P-2 closure added 7 conditional paths in PV-02) from the install branch into a dedicated helper. |
| 3 | warning | `src/devolaflow/lifecycle/auto_write_handoff.py:83` `_extract_layers` | 16 | Layer-routing dispatch table; collapse 5 isinstance branches into a registry lookup. |
| 4 | warning | `src/devolaflow/lifecycle/pre_plugin_invocation.py:151` `_extract_plugin_ids` | 16 | Same dispatch-table refactor; the 5 dispatch shapes (TaskDispatch dict / Lean dict / lean_dispatch dict / plugin_id str / plugin_ids list) all map onto a parser registry. |
| 5 | warning | `src/devolaflow/plugins/installer.py:1140` `read_last_checked` | 15 | Split TOML/YAML/JSON parser arms into a content-dispatched function table. |
| 6 | warning | `src/devolaflow/plugins/installer.py:851` `ensure_plugin` | 14 | Extract the network-fetch branch from the cache-hit branch; the function currently interleaves both. |
| 7 | warning | `src/devolaflow/lifecycle/post_skill_edit.py:370` `post_skill_edit` | 13 | Split the dedup-fingerprint write-or-skip branch (D-S-5 closure added 4 branches in PV-02) into a dedicated `_resolve_dedup_action` helper. |
| 8 | warning | `src/devolaflow/lifecycle/auto_write_handoff.py:170` `auto_write_handoff` | 12 | Extract envelope-write branch (S-9 append-only enforcement) from the layer-detection branch. |
| 9 | warning | `src/devolaflow/plugins/installer.py:213` `plugins_for_workflow` | 11 | Workflow-name filter table; current branches enumerate explicit cases. |
| 10 | warning | `src/devolaflow/plugins/installer.py:279` `resolve_plugin` | 11 | Extract install-source-priority lookup into a constant tuple; current code threads the priority into 3 if-arms. |

The two PV-02-introduced concentrations (post_skill_edit dedup logic + pre_plugin_invocation daily-upgrade integration) account for findings #2 and #7 — both are direct consequences of the v10.2.1 closure scope. PV-04 self-iteration (if Si-Chip APPROVE allows) would naturally land on either or both.

## §3 — Keypoints (per-package, NineS-extracted)

NineS deep-analyse with `--keypoints --agent-impact` flags reports `agent_impact.mechanisms = []` and `agent_facing_artifacts = []` for all 3 packages. This is the EXPECTED outcome — the three target packages are internal Python implementations (the bridge, the installer machinery, the lifecycle hooks); they do NOT carry agent-facing markdown / SKILL.md / reference files. NineS's agent-impact analysis is designed for skill-corpus targets, not for source code packages.

The keypoints surface is therefore degenerate-empty per NineS V3.3.0's contract for non-agent targets. The DevolaFlow-side keypoints are extracted from the findings + import graph instead:

* **si_chip_bridge** — the bridge module is the smallest and tightest of the 3 (1,153 LOC, 4 files). The largest module is `runner.py` (598 LOC) which carries the entire CLI-wrapping surface for Si-Chip's 3 scripts. Coupling is exclusively internal (only inter-bridge imports + `devolaflow.__version__` + stdlib). Avg complexity 4.83 sits comfortably below the warn-threshold (CC=10). NO complexity warnings — the bridge is well-decomposed at the function level.
* **plugins** — installer.py is the dominant module (1,538 LOC, 23 functions, 4 of the 4 warnings). The 4 high-CC functions are all in the multi-source-format parsers (TOML/YAML/JSON for `read_last_checked`; multi-priority install backend for `ensure_plugin` + `resolve_plugin`). Avg complexity 3.63 across the package is healthy; the warnings concentrate in installer.py because that module owns the entire upstream-source diversity.
* **lifecycle** — 13 modules, broadest surface of the 3. 6 complexity warnings spread across 4 modules. The two highest (CC=20, 18) live in `test_on_complete.py` (session-state persistence) and `pre_plugin_invocation.py` (daily-upgrade integration that PV-02 D-P-2 just landed). The PV-02 additions account for 2 of the 6 warnings — fixing them would naturally fall into PV-04 self-iteration scope.

## §4 — Agent-impact / hygiene sub-scoring (per-package)

Per NineS V3.3.0 `analyze` (NOT `self-eval`), agent-impact economics carry zeros for all 3 targets because the targets aren't agent-facing skill corpora. The composite/hygiene scores below are derived from finding density + complexity distribution per the v10.0.0 NineS shape conventions:

| Package | Findings/file | Warning ratio | Avg CC | Synthesis score (1-10) |
|---|---:|---:|---:|---:|
| si_chip_bridge | 2.5 | 0% | 4.83 | **9.5** — clean bridge module; PV-02 D-S-2/D-S-6 closures landed without complexity regression |
| plugins | 3.0 | 22% | 3.63 | **7.5** — installer.py concentration of 4 complexity warnings; refactor leverage available |
| lifecycle | 2.6 | 18% | 4.79 | **7.0** — 6 complexity warnings across 4 modules; PV-02 D-P-2 + D-S-5 closures contributed 2 of 6 |

The composite (weighted mean: 9.5×0.3 + 7.5×0.35 + 7.0×0.35) = **7.93**, which is BELOW the W-3 ≥8.5 minor-release floor but ABOVE the bare-minimum threshold. This is a NineS-`analyze` synthesis snapshot, NOT the W-3 SI-3 evaluation that runs at PV-06 cycle-close — that evaluation uses NineS `self-eval` against the canonical `data/golden_test_set/` and produces a different, golden-task-driven composite.

The 7.93 synthesis score reflects the cycle's MID-CYCLE health: PV-02's productive code additions naturally introduced new branching, which surfaces as new complexity warnings. PV-04/PV-05 should target findings #2 and #7 first (the two PV-02-introduced ones) so the cycle-close synthesis score returns to the v10.0.0 baseline.

## §5 — Findings flagged for PV-04/PV-05 self-iteration

Mechanically fixable HIGH-leverage findings that do NOT touch SKILL/references/CLAUDE/AGENTS files (per the PV-03 hard requirements):

* **PV-04 candidate #1** — `src/devolaflow/lifecycle/pre_plugin_invocation.py:269` `pre_plugin_invocation` CC=18 → split daily-upgrade branch into `_handle_stale_plugin` helper. Estimated CC reduction: 18 → ≤10. Test surface: `tests/test_pre_plugin_invocation.py::TestDailyUpgradeIntegration` already covers all branches (D-P-2 PV-02 closure shipped 6 NEW tests).
* **PV-04 candidate #2** — `src/devolaflow/lifecycle/post_skill_edit.py:370` `post_skill_edit` CC=13 → extract dedup-fingerprint resolution into `_resolve_dedup_action` helper. Estimated CC reduction: 13 → ≤8. Test surface: `tests/test_sichip_dedup_feedback_doc.py` (D-S-5 PV-02 closure) already pins all dedup branches.
* **PV-05 candidate** — `src/devolaflow/plugins/installer.py:851` `ensure_plugin` CC=14 → extract network-fetch branch from cache-hit branch. Estimated CC reduction: 14 → ≤9. Test surface: `tests/test_runtime_plugins_smoke.py` + `tests/test_plugin_refresh_e2e.py` (PV-01 closures) provide e2e coverage.

The remaining 7 warnings are MEDIUM leverage (mostly multi-format parser dispatch tables). They are deferred to v10.4.0+ per the v10.2.0 OUT-OF-SCOPE list (gap analysis §5).

## §6 — References

* Raw JSON outputs: `.local/research/v10.2.2_nines{,_plugins,_lifecycle}.json` (3 files, 35 KB total)
* NineS shape comparison reference: `docs/cycle-archive/v10.0.0/nines/v10.0.0_nines.json` (the v10.0.0 self-eval baseline)
* v10.2.0 gap analysis §3.3 D-N-3 closure: `.local/research/v10.2.0_gap_analysis.md`
* v10.2.0 cycle plan §3 PV-03 acceptance criterion #3: `.local/research/v10.2.0_cycle_plan.md`
* PV-02 D-P-2 daily-upgrade implementation: `src/devolaflow/lifecycle/pre_plugin_invocation.py` (the source of the PV-02-introduced CC concentration)
* PV-02 D-S-5 dedup implementation: `src/devolaflow/lifecycle/post_skill_edit.py` (the source of finding #7)
* External tools (S-7):
  * DevolaFlow / EvoBench: https://github.com/YoRHa-Agents/DevolaFlow
  * NineS: https://github.com/YoRHa-Agents/NineS
  * Si-Chip: https://github.com/YoRHa-Agents/Si-Chip
