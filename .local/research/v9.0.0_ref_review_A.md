# v9.0.0 SF-4 Reference Review — Batch A (3 of 11)

> Date: 2026-04-23 | Author: L3 Task Agent (Review team) | Task: v9.0.0-S03-T01
> Scope: `agent-hierarchy.md`, `meta-framework.md`, `decomposition-gate.md`
> Methodology: drift-detection against v8.4.0 state per `.local/research/v9.0.0_decomposition_analysis.md` (8 themes T1–T8) and `.local/research/v9.0.0_version_history.md` v8.0.0–v8.4.0 era ledger.
> Verbatim quotes preserved per Rule C-3; all paths repo-relative per Rule S-2.

## Reference: agent-hierarchy.md

**Current line count**: 267 / 1000 (SF-1 Large tier ceiling) — utilization 26.7%
**Last major content update**: frontmatter `last_updated: "2026-04-04"` (PRE v8.0.0; predates the entire v8.x era including agent workspace, behavioral_guidelines, RTK + memory router, and 4 P6 cache-layout transitions)
**SKILL.md reference hit**: YES — line 417 `| references/agent-hierarchy.md | Layer setup, delegation debugging, per-layer contracts |`

### Content inventory

- §1 Architecture Overview (~26 lines, lines 22–47) — ASCII art L0→L1→L2→L3 dispatch/report flow + P1 invariant statement
- §2 Layer Specification Table (~17 lines, lines 49–65) — per-layer aspects (Role/Count/Lifespan/DoesWork?/Context budget/Receives/Produces/Reports to/Delegates to/State owned/Tools)
- §3 Per-Layer Behavioral Contracts (~52 lines, lines 66–117) — L0/L1/L2/L3 procedures + L3 task type specializations (`code | test | review | research | design | benchmark`)
- §4 MUST/MUST NOT Rules (~9 lines, lines 119–127) — per-layer obligations and prohibitions
- §5 Delegation Rules (~45 lines, lines 129–173) — direction/constraints/fail-forward/escalation chain
- §6 Context Budget Details (~20 lines, lines 175–194) — strategy/budget/loaded content + L3 token-injection breakdown
- §7 Layer Comparison Summary (~16 lines, lines 196–211) — ASCII summary box
- §8 File System Layout (~30 lines, lines 213–242) — `.local/` tree
- §9 Convergence Loop (~24 lines, lines 244–267) — 8-phase loop + gate decision

### Drift findings

- **STALE — §8 File System Layout (lines 217–241)**: shows pre-v8.3.0 layout. Verbatim from file: *"`├── handoffs/                          # Handoff deliverable envelopes`"* and *"`│   ├── research_to_design.yaml`"*. The v8.3.0 PV-03 introduced `.local/.agent/{active,handoff,archive}/` per `repo-init.yaml::canonical_manifest` (5→8 paths) and S-8/S-9/A-4/C-9 rules; this entire workspace primitive is invisible in §8. Per v9.0.0 decomposition analysis T6/T8 chain: this is the single highest-impact stale section in the file.
- **STALE — frontmatter `last_updated: "2026-04-04"`**: predates v8.0.0 (2026-04-22), v8.2.0 (2026-04-22), v8.3.0 (2026-04-23), v8.4.0 (2026-04-23). Six release cycles' worth of changes are unrepresented.
- **MISSING — agent-workspace primitives**: no mention of `Change`, `ChangeStore`, `HandoffEnvelope` (append-only with monotonic `seq` per S-9), `ArchiveManager`. These are first-class L3 contract surfaces in `change-driven` workflow. Currently routed only to `references/agent-workspace.md` (~487 lines, 10th SF-4 ref) without cross-link.
- **MISSING — v8.0.0 P-08 behavioral_guidelines injection**: §3 L3 contract item 4 says *"Follow applicable code-rules loaded per context injection"* (line 106) but does not mention the new `behavioral_guidelines` field (canonical_order position 14, schema version 3) or the `surgical_scope: function|line` ladder shipped in v8.2.0 PV-04.
- **MISSING — v5.1+ reinforcement mechanism + v6.0.3 round-aware dispatch**: §9 Convergence Loop describes the 8-phase loop but omits `gate/reinforcement.py::merge_reinforcement_into_dispatch`, `applicable_rules.reinforcement` MUST-fix mandates, and `apply_round_escalation` (+20% budget on round 3, `model_hint → quality`). SKILL.md references both at lines 318–322 and 386 but agent-hierarchy.md does not.
- **MISSING — S-8 owned_files constraint for `change-driven` workflow**: §4 MUST NOT for L3 says *"Spawn sub-agents, delegate work, modify files outside owned set, exceed timeout"* — generic only. Rule S-8 (v8.3.0 PV-02) is `change-driven`-specific and warrants explicit cross-reference for L3 readers entering that workflow.
- **DUPLICATE — §6 Context Budget Details overlaps SKILL.md §"Context Isolation" (lines 336–353)**: the per-layer budget table (3K/5K/4K/8K) appears in 3 places: SKILL.md `4-Layer Agent Hierarchy` table line 207, SKILL.md `Context Isolation` block lines 339–348, and agent-hierarchy.md §2 + §6. Single source of truth opportunity per T6 selectivity work.
- **UNCLEAR — §3 Task Agent type specializations table (lines 110–117)**: 6 types listed (`code | test | review | research | design | benchmark`); `change-driven` workflow's `propose / apply / verify / archive` and `entropy-cleanup`'s `scan / propose / review / apply` are not represented. SKILL.md `Workflow Selection` table covers them.

### Verdict for v9.0.0

**REFRESH** (minor updates to align with v8.4.0 state).

Rationale: Content is structurally correct and the L0–L3 contract has not changed. Drift is concentrated in (a) the file-system layout section (1 paragraph rewrite + cross-link), (b) frontmatter date stamp, and (c) 4 v8.x cross-references. None require restructuring. SPLIT/MERGE/DEPRECATE not warranted; 267/1000 lines leaves abundant headroom (~73%) for EXTEND if T2 (A-5 registry rule) adds tool/workspace constraint rows in v9.0.0 PV-03.

### Improvement candidates for v9.0.0

- **R1**: Replace §8 ASCII tree with v8.3.0 PV-03 canonical 8-path layout including `.local/.agent/{active,handoff,archive}/`, `.local/feedbacks/`, `.local/tasks/`, `.local/memory/`, `.local/index.md` per `repo-init.yaml::canonical_manifest` (verbatim from SKILL.md §"Repo-Init Pre-Dispatch Contract" lines 178–194). Add 1-line cross-ref to `references/agent-workspace.md`. **LOC: −8 / +18 net +10**.
- **R2**: Add §3 L3 sub-section "Workflow-aware constraints" enumerating S-8 (owned_files), S-9 (handoff envelope append-only), and behavioral_guidelines surgical_scope ladder. **LOC: +20**.
- **R3**: Append §9 Convergence Loop note about `applicable_rules.reinforcement` and round-aware dispatch (round_num=N escalation) — reference `gate/reinforcement.py` + `feedback.py::generate_round_dispatch`. **LOC: +12**.
- **R4**: Bump frontmatter `last_updated` to 2026-04-23 + bump `version: "1.0.0" → "1.1.0"` per CO-2 traceability. **LOC: +0**.
- **R5**: Add §3 task-type table row for `change-driven` (propose/apply/verify/archive owners) + entropy-cleanup (scan/propose/review/apply). **LOC: +6**.

### SF-1 budget impact

EXTEND not recommended; REFRESH net **+44 LOC → 311/1000 (31.1%)**. Comfortably within Large tier ceiling. No SF-1 escalation required.

---

## Reference: meta-framework.md

**Current line count**: 519 / 1000 (SF-1 Large tier ceiling) — utilization 51.9%
**Last major content update**: frontmatter `last_updated: "2026-04-04"` (predates entire v8.x era; structurally pre-dates 4 P6 cache-layout transitions and 5 new templates: `entropy-cleanup` v8.0.0 P-11, `change-driven` v8.3.0 PV-06, `shell-proxy` v8.4.0 dispatch-time fast-path, plus `nines-assisted` and `product-verification` from late v7.x)
**SKILL.md reference hit**: YES — TWO hits: line 298 (*"Composition operators... Full alias table + per-workflow sequences: `references/meta-framework.md`"*) AND line 420 Tier-2 table

### Content inventory

- §1 Stage Primitive Catalog (~15 lines, lines 22–36) — ASCII universe across 6 categories (Discover/Shape/Build/Verify/Deliver/Control)
- §2 Primitive Interface Contracts (~194 lines, lines 38–231) — 14 primitives × Input/Output/Preconditions/Postconditions/Config/Team/Duration; includes §2.13 `verify` (v5.4.0 user-facing) with `verify_config` YAML
- §3 Dependency Lattice (~24 lines, lines 233–256) — valid direct-successor edges + override note
- §4 Alias Mapping Table (~43 lines, lines 258–300) — workflow-specific name → primitive + repo-init standalone block
- §5 Composition Operators (~68 lines, lines 302–369) — sequence/parallel/choice/loop/gate with YAML examples
- §6 Formal Grammar BNF (~26 lines, lines 371–396) — Workflow / Stage / Composed grammar
- §7 Key Composition Patterns (~62 lines, lines 398–462) — Quality Loop / Correctness Loop / Knowledge Loop / Gate-Guarded Release
- §8 Nested Composition Example (~56 lines, lines 464–519) — full-pipeline review-test cycle YAML

### Drift findings

- **MISSING — Alias Mapping Table omits 5 templates added since 2026-04-04** (verified against `workflow-system/agent/templates/builtin/` which contains 22 yaml files):
  1. `change-driven` (v8.3.0 PV-06) — stages: `propose → apply ↔ verify → archive` (max_rounds=5 per W-8)
  2. `entropy-cleanup` (v8.0.0 P-11) — stages: `scan → propose → review → apply`
  3. `nines-assisted` — stages: `research → design → plan → impl → review → test → validate → release`
  4. `product-verification` — stages: `analyze → design → implement → test → verify → review → validate`
  5. `self-update` — stages: `check-refs → research-updates → decompose → integrate → test → evaluate`
  Also missing: `skill-optimization`, `onboarding`, `dependency-setup`, `demo-showcase`, `research-design-review-refine`. Total: **10 of 22 templates** unrepresented in the alias table — readers landing on this ref from SKILL.md line 298 cannot trace them to primitive composition.
- **STALE — §4 `repo-init` block (lines 292–300)** existed in `meta-framework.md:292-300` BEFORE the v8.3.0 PV-03 implementation actually shipped; per v9.0.0 version_history.md line 1339: *"`repo-init` workflow: stages `analyze → scaffold → compile → verify`; triggered by 'init repo' / '初始化仓库'. (Declared in `CHANGELOG.md:35`, `SKILL.md:173,470`, `meta-framework.md:292-300`, `context_profiles.yaml:1476-1497` — but template, registry entry, tests, and `src/` support all MISSING — the v7.4.0 ghost.)"*. The ghost has since been closed (v8.3.0 PV-03 + canonical_manifest 8 paths) but §4's prose still implies aspirational status.
- **MISSING — P6 cache-layout invariant lineage in §2 primitive contracts**: the canonical_order grew 13→14→15→16 across v8.0.0 P-08 (`behavioral_guidelines` position 14), v8.0.0 P-10 (`acceptance_criteria_v2` position 15), v8.3.0 PV-05 (`change_context` position 16). `meta-framework.md` claims to be *"the* primitive interface contract"* reference but the dispatch-payload primitive (a meta-primitive that every other primitive depends on) is not listed. Cross-ref to `schemas/lean-dispatch.yaml#layout_invariant` is absent.
- **MISSING — v8.0.0 gate primitive surface in §2.14 `gate`**: Config currently lists *"`on_fail: loop_back|escalate|block`, `loop_back_target?`, `require_human_override`"*. Does not mention v8.0.0+ opt-in primitives that gate composes with: `token_budget_breaker` (P-03), `verification_ladder` (P-05), `cycle_detector` (P-06), `monotonic_ratchet` (P-07), `complexity_detector` (P-09), `ac_generator` (P-10), `legibility` (v8.2.0 PV-02). Per T5 v9.0.0 decomposition theme, 5 of 7 are still default-OFF — this is a discoverability gap.
- **DUPLICATE — §1 Catalog + §2 contracts overlap SKILL.md `Stage Primitives Index` (lines 250–298)**: SKILL.md has its own 6-category breakdown with team annotations. Per Rule CO-1 (lean format) verbatim duplication is the right pattern here, but a one-line "this is the canonical full contract; SKILL.md carries the lean catalog" header would help.
- **UNCLEAR — §3 Dependency Lattice annotation `Templates may override with explicit allow_transition annotations`**: no example of an override or pointer to the override grammar in §6 BNF. Readers cannot determine whether `analyze → release` (transition currently absent) requires template surgery or runtime override.
- **UNCLEAR — version annotations inconsistent**: §2.13 `verify` is implicitly v5.4.0 (per surrounding gate composite formula context); §1 catalog list of 14 primitives is implicitly v8.0.0+ (count went from 13 to 14 with `verify` introduction). No `(introduced vN.M.0)` markers anywhere.

### Verdict for v9.0.0

**EXTEND** (add 10 missing templates to alias table + 1 P6 cache-layout primitive + version annotations).

Rationale: §4 alias table is the public contract for "where does my workflow type live in the primitive lattice?" — currently 12 of 22 templates are reachable; raising coverage to 22/22 is the single largest discoverability win in this batch. Combined with P6 cache-layout lineage and v8.0.0 gate-primitive cross-refs, total addition lands ~80–100 LOC. 519 → 619/1000 still leaves 38% headroom.

### Improvement candidates for v9.0.0

- **R1**: Extend §4 Alias Mapping Table to cover all 22 templates (verbatim names from `workflow-system/agent/templates/builtin/*.yaml`). For each missing template, add 1–3 alias rows + 1 standalone block (matching repo-init pattern at lines 292–300). **LOC: +60–80**.
- **R2**: Add §2.15 `dispatch_payload` (or §9 "Cross-Cutting Primitives") summarizing the canonical_order as a meta-primitive: positions 1–16 with `(introduced vN.M.0)` markers; cross-ref `schemas/lean-dispatch.yaml#layout_invariant` and `devolaflow.compressor.assert_dispatch_layout`. Aligns with T7 (Cache-Layout Governance v2) v9.0.0 PV-02. **LOC: +20**.
- **R3**: Extend §2.14 `gate` Config row to list the 7 v8.0.0 opt-in primitives + their default-on/off status per profile (relaxed/standard/strict/audit). Mark which are flipped in v9.0.0 PV-06 per T5. **LOC: +10**.
- **R4**: Add `(introduced vN.M.0)` annotations across §2 primitives (research/analyze/design from v3.x; verify from v5.4.0; gate from v3.x). **LOC: +5**.
- **R5**: Bump frontmatter `last_updated: 2026-04-23` + `version: 1.0.0 → 1.1.0`. Add §1 prose noting "Composition catalog is canonical; SKILL.md `Stage Primitives Index` carries the lean view." **LOC: +3**.

### SF-1 budget impact

EXTEND projected line count: **519 + 100 = 619 / 1000 (61.9%)**. Comfortably within Large tier ceiling; no SF-1 escalation required. Note: if T6 rule rebalancing (v9.0.0 PV-07 MAJOR) introduces per-task-type slicing, meta-framework.md is the natural seat for the slice-grammar — that work should be deferred to a v9.x-sustaining R6 to avoid coupling with PV-01 SKILL.md compression timeline.

---

## Reference: decomposition-gate.md

**Current line count**: 517 / 1000 (SF-1 Large tier ceiling) — utilization 51.7%
**Last major content update**: frontmatter `last_updated: "2026-04-04"` BUT §8 covers v7.2.0+ inline_self_review (added per v9.0.0 version_history.md line 1279: *"PR-E / C-004+C-005: SKILL.md Wave Coordination Modes extension with inline_self_review row... new references/decomposition-gate.md §8 Inline Self-Review Mode (v7.2.0+)"*); frontmatter date is stale relative to §8 content.
**SKILL.md reference hit**: YES — THREE hits: line 242 (Wave Coordination Modes pointer to §8), line 316 (Gate Mechanism full spec pointer), line 418 Tier-2 table

### Content inventory

- §1 Decomposition Principles (~10 lines, lines 23–32) — D1–D5 monotonic granularity / DAG / atomicity / gate-before-advance / determinism
- §2 Stage Decomposition (~61 lines, lines 34–94) — boundary criteria / schema / naming
- §3 Wave Decomposition (~45 lines, lines 96–140) — formation algorithm / constraints / pattern library
- §4 Task Decomposition (~87 lines, lines 142–228) — sizing rules / schema / description template
- §5 Gate Quality Mechanism (~133 lines, lines 230–362) — types / composite formulas (4-dim + 7-dim) / per-dimension / pass conditions / on-fail / profiles / evaluation flowchart
- §6 Convergence Loop Detail (~24 lines, lines 364–387) — 8-phase loop + gate decision
- §7 Failure Handling Chain (~90 lines, lines 389–478) — classification / flowchart / retry limits / decomposition validation checklist
- §8 Inline Self-Review Mode (v7.2.0+) (~38 lines, lines 480–517) — when SAFE / when KEEP gen_verify / activation

### Drift findings

- **MISSING — v8.0.0 gate primitive surface in §5 Gate Quality Mechanism**: per v9.0.0 version_history.md v8.0.0 ledger lines 1578–1631, **7 new gate primitives shipped**, none mentioned in §5:
  1. `gate/budget.py::TokenBudgetBreaker` (P-03, 296 lines, 100% coverage; STRICT cumulative > 80000 → BREAK + ESCALATE)
  2. `gate/scorer.py::evaluate_ladder` (P-05, 6 rungs R1..R6 short-circuit)
  3. `gate/cycle_detector.py` (P-06, 474 lines; exact_match / fuzzy_match ≥ 80% / edit_oscillation A→B→A→B)
  4. `gate/ratchet.py` (P-07, 442 lines, 100% coverage; **G13 closure**; 4-verdict ADVANCE/TOLERATE/ROLLBACK/ESCALATE; deterministic oracle excludes review_findings per S/O/R-resistance)
  5. `gate/complexity_detector.py` (P-09, ~720 lines; nines analyze subprocess + MOCK fallback)
  6. `ac_generator.py` (P-10, ~561 lines; 11 deterministic patterns; 3-dim quality scoring)
  7. `legibility/scorer.py` (v8.2.0 PV-02, 627 lines; naming + comment-ratio + cyclomatic flow)
  Per T5 v9.0.0 decomposition analysis: 5 of 7 (`token_budget_breaker / verification_ladder / ratchet / complexity_detector / ac_generator`) are still default-OFF; v9.0.0 PV-06 plans the flip — the gate spec must enumerate them before flipping.
- **MISSING — v5.1+ reinforcement mechanism**: §6 Convergence Loop Detail says *"Each phase dispatched as a Wave with 1 Task. Stage Agent orchestrates — never executes phases."* but does not document `gate/reinforcement.py::merge_reinforcement_into_dispatch`, `applicable_rules.reinforcement` MUST-fix mandates injected into round N+1 dispatch (max 5 per round, severity ≥ major). SKILL.md §"Reinforcement Rules (v5.1+)" lines 318–322 covers it briefly; the canonical spec belongs here.
- **MISSING — v6.0.3+ round-aware dispatch**: not mentioned. Per v9.0.0 decomposition analysis Perf-obs §3: *"v6.0.3 dead-wire closure was the highest-ROI single intervention of v6.0 — `apply_round_escalation` + `merge_reinforcement_into_dispatch` had passing unit tests since v5.3.0 / v5.1.0-pre but zero production callers; pre-wiring benchmark ROI was 0; post-wiring delivered +20% round-3 budget."* SKILL.md line 386 references it; this ref does not.
- **MISSING — v8.0.0 P-04 deterministic fence expansion**: `gate/reinforcement.py::fence_to_instruction(fence_type, fence_payload)` mapping fence events to deterministic `ReinforcementRule` IDs (`F-lint-001` etc.); scorer `_evaluate_checks()` emits `ReinforcementBlock` on failure. This IS in `references/execution-protocol.md §9` (added v8.0.0 P-04 per version_history.md line 1585) but cross-ref absent from §5 / §6 here.
- **STALE — §5.4 Gate Profiles table (lines 316–325)**: 4 rows (relaxed/standard/strict/audit) with composite/coverage/blockers/criticals/min_rounds/max_rounds + 3 user-facing columns. Does NOT show which gate primitives each profile activates. Per v9.0.0 T5: STRICT currently has `legibility_enabled=true` + `cycle_detector_enabled=true` (flipped in v8.2.0 PV-05); the other 5 are still default-OFF across all profiles.
- **MISSING — v8.0.0 P-09 MOCK fallback canonical pattern**: per v9.0.0 version_history.md line 1624: *"MOCK fallback became canonical pattern for any subprocess-wrapped primitive (codified in v8.0.0 retro §4.6)"*. §7 Failure Handling Chain does not mention subprocess-wrapped primitive fallback.
- **DUPLICATE — §6 Convergence Loop Detail (lines 364–387) is nearly verbatim agent-hierarchy.md §9 (lines 244–267)**: both reproduce the 8-phase loop + gate decision. Per T6 selectivity work, this is a cross-ref candidate (one canonical site, the other carries a 1-line pointer).
- **UNCLEAR — version annotations inconsistent**: §5.3 says *"User-Facing Verification Dimension Scores (v5.4.0)"* (line 289); §8 says *"Inline Self-Review Mode (v7.2.0+)"* (line 480). Most other sections carry no version stamp. v8.x additions would all need stamps for traceability.
- **UNCLEAR — §5 Pass Conditions item 3** says *"`coverage >= coverage_threshold` (default 80%)"* (line 304). No mention that `coverage` is the legacy `tests_passed/tests_total × 100` OR `coverage_pct` (per §5.3 Standard Composite line 252). Ambiguous when both fields are present in `gate/scorer` payload.

### Verdict for v9.0.0

**EXTEND** (add v8.0.0 gate-primitives section + reinforcement + round-aware dispatch + version annotations).

Rationale: §5 Gate Quality Mechanism is the canonical reference for the entire gate surface; v8.0.0 shipped 7 new primitives that are governance-critical (T5 flip planned for v9.0.0 PV-06). Without enumeration here, the v9.0.0 PV-06 default-on flip lacks a single source of truth. Combined with reinforcement (v5.1+) + round-aware dispatch (v6.0.3+) + fence expansion (v8.0.0 P-04) cross-refs, total addition lands ~100 LOC. 517 → 617/1000 still leaves 38% headroom.

### Improvement candidates for v9.0.0

- **R1**: Add §5.5 "Gate Primitive Composition (v8.0.0+)" enumerating 7 primitives × {module / introduced version / default state per profile / EvoBench scenario / opt-in env-flag}. Couples with T5 (v9.0.0 PV-06 flip) — primitives must be enumerated BEFORE flipping per T8 audit-cadence W-rule. **LOC: +50**.
- **R2**: Extend §6 Convergence Loop Detail with a "Round-Aware Dispatch (v6.0.3+)" subsection covering `apply_round_escalation` + `merge_reinforcement_into_dispatch` + `applicable_rules.reinforcement` injection (max 5 / severity ≥ major / round_num=N escalation / +20% budget round 3 / `model_hint → quality`). **LOC: +20**.
- **R3**: Convert §6 Convergence Loop content into 3-line cross-ref to agent-hierarchy.md §9 (de-duplicate); replace freed lines with R1/R2 content. **LOC: −20 / +0 net**.
- **R4**: Add §5.4 Gate Profiles table 7 new columns (one per v8.0.0 primitive) showing default-on/off per profile. **LOC: +0** (table widening).
- **R5**: Add `(introduced vN.M.0)` markers across all sections; bump frontmatter `last_updated: 2026-04-23`, `version: 1.0.0 → 1.1.0`. **LOC: +5**.
- **R6**: Clarify §5 Pass Conditions item 3 to specify `coverage = coverage_pct` (not `tests_passed/tests_total`) and cross-ref `gate/scorer.py` field semantics. **LOC: +3**.

### SF-1 budget impact

EXTEND projected line count: **517 + 80 (R1+R2+R5+R6 net of R3 dedup) = 597 / 1000 (59.7%)**. Comfortably within Large tier ceiling; no SF-1 escalation required.

---

## Batch A summary table

| Ref | Verdict | LOC impact | v9.0.0 PV mapping (per `.local/research/v9.0.0_decomposition_analysis.md` Theme-to-PV) |
|---|---|---|---|
| `agent-hierarchy.md` | REFRESH | +44 net (267 → 311 / 1000) | PV-01 (T1 SKILL.md compression — adopt agent-workspace cross-link); PV-04 (T4 lifecycle wiring — L3 hook awareness); PV-07 (T6 rule-taxonomy selectivity — L0/L1/L2/L3 slicing) |
| `meta-framework.md` | EXTEND | +100 net (519 → 619 / 1000) | PV-02 (T7 Cache-Layout Governance v2 — add §2.15 dispatch_payload primitive); PV-05 (T8 NineS hygiene — add `(introduced vN.M.0)` markers per W-19 archive policy); PV-06 (T5 default-on flip — list opt-in primitives in §2.14 gate Config) |
| `decomposition-gate.md` | EXTEND | +80 net (517 → 597 / 1000) | PV-04 (T4 lifecycle wiring — reinforcement + round-aware dispatch); PV-06 (T5 default-on flip — §5.5 Gate Primitive Composition is the precondition); PV-07 (T6 selectivity — gate-primitive enumeration per task type) |

**Batch A net LOC**: +224 LOC across 3 refs; total Batch A budget post-v9.0.0: 1303 / 3000 (43.4% of combined Large-tier capacity). No file approaches the 1000-line SF-1 ceiling; no SF-1 tier escalation required.

## Cross-batch observations

- **Pattern P-1 — Universal frontmatter `last_updated: "2026-04-04"` staleness**: all 3 refs predate v8.0.0 (2026-04-22); the 6 release cycles since (v8.0.0 / v8.1.0-rc.1 / v8.2.0 / v8.3.0 / v8.3.{1,2,3,4} / v8.4.0) are unrepresented in any frontmatter despite §8 of `decomposition-gate.md` explicitly citing v7.2.0+ content (last_updated stamp lags actual edits). Likely holds for the 8 refs in Batches B+C — recommend a Batch-Z frontmatter sweep PV.
- **Pattern P-2 — v8.0.0 primitives invisible**: 7 gate primitives + behavioral_guidelines + agent workspace + RTK + memory router + CompressionPipeline candidates add up to a v8.x surface area roughly equal to the v3.x–v7.x cumulative. None of the 3 reviewed refs cross-reference these — they live only in SKILL.md (lines 318, 322, 386, 389, 412, 425, 426) and CHANGELOG. Strongly suggests Batch B (`context-isolation.md`, `execution-protocol.md`, `message-schemas.md`, `team-roles.md`) and Batch C (`agent-workspace.md`, `behavioral-guidelines.md`, `repo-modes.md`, `shell-proxy.md`) carry the same gap on adjacent surfaces.
- **Pattern P-3 — Half-utilized Large-tier capacity**: 3 of 3 refs are 26–52% utilized; SF-1 Large tier (≤1000) is comfortable. The 1000-line ceiling is NOT a v9.0.0 binding constraint for refs (contrast with SKILL.md at 499/500 — the ceiling problem lives in Tier-1, not Tier-2). v9.0.0 EXTEND verdicts can proceed without tier escalation discussion.
- **Pattern P-4 — Cross-ref opportunities for T6 selectivity**: §6 of `decomposition-gate.md` ≈ §9 of `agent-hierarchy.md` ≈ SKILL.md §"Convergence Loop"; same 8-phase loop reproduced 3 times. Per T6 v9.0.0 PV-07 selectivity work, single-source-of-truth for the convergence loop should land at one canonical site (recommend `decomposition-gate.md` §6 since this is the gate-evaluation reference).
- **Pattern P-5 — SF-4 list inconsistency** (cross-batch finding worth flagging to L0 even though out of strict scope): rule SF-4 valid list (10 entries: includes `behavioral-guidelines.md`, excludes `shell-proxy.md`) ≠ SKILL.md Tier-2 table (10 entries: excludes `behavioral-guidelines.md`, includes `shell-proxy.md`) ≠ filesystem (11 files). One of (a) SF-4 rule, (b) SKILL.md Tier-2 table, (c) `behavioral-guidelines.md` deletion is required to reconcile. Coupled to T1 (SKILL.md compression PV-01) decision-making — flag to S05 gap_analysis.

*End of v9.0.0 SF-4 Reference Review — Batch A. Next consumers: S03-T02 (Batch B review), S05 gap_analysis (PV-01 SKILL.md scope decisions), S05 version_roadmap (PV-04/05/06/07 ref-update sequencing). All paths repo-relative per S-2; verbatim quotes preserved per C-3.*
