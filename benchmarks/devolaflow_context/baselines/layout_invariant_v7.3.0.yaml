hdr:
  id: d-baseline-v7.0.0
  parent: stage-baseline
  layer: wave
task:
  id: T-BASELINE-001
  type: code
  title: cache layout baseline
goal: golden rendered dispatch for layout invariant
assumptions:
- yaml renderer preserves insertion order
- utf-8 byte comparison
pred:
- ref: .local/research/adr/v7-ADR-001-cache-layout-invariant.md
  key_facts:
  - canonical 12-key order
  - additive rule for new keys
  - LCP thresholds 0.80 and 0.70
files:
- src/devolaflow/compressor.py
- schemas/lean-dispatch.yaml
rules:
  strategy: standard
  lang: python
  focus:
  - cache-discipline
shared: Python 3.11+, PyYAML, pytest
accept:
- render byte-stable across CI runs
- top-level keys remain in canonical order
- reinforce slot present at position 10
reinforce:
  round: 2
  prior: 78.0
  target: 85
  rules:
  - id: F-LAY-001
    sev: blocker
    mandate: MUST validate via assert_dispatch_layout before send
    file: src/devolaflow/compressor.py
verify_cfg:
  visual: false
  accept: true
  interact: false
  a11y: false
  threshold: 0.85
gate:
  coverage: 85
  quality: 85
  blockers: 0
  retries: 2
repos:
- name: auth-service
  root_path: repos/auth-service
  primary: true
  branch: main
- name: web-frontend
  root_path: repos/web-frontend
  primary: false
  branch: develop
- name: api-gateway
  root_path: repos/api-gateway
  primary: false
  branch: main
