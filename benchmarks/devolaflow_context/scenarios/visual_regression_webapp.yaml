name: "visual_regression_webapp"
task_type: "verify_visual"
description: >
  Web application visual regression testing scenario. Tests context selection
  for a task requiring visual testing of a React dashboard with screenshot
  comparison and visual fidelity scoring.

expected_sections:
  - gate_mechanism
  - dispatch_report
  - agent_teams
  - convergence_loop

unwanted_sections:
  - quick_action_decision
  - mode_detection
  - quick_start_workflows
  - repo_mode

quality_thresholds:
  min_composite: 80.0
  max_noise_ratio: 0.15
  min_relevance: 0.7
