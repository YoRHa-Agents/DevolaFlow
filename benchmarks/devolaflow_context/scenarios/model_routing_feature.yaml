# EvoBench Scenario: Model Routing — Budget Override for Simple Implementation
# Tests that simple_implementation task type routes to feature profile and
# selects triage-relevant sections without planning noise.

name: "model_routing_feature"
task_type: "simple_implementation"
description: "Feature task with model_hint budget override for simple implementation"

expected_sections:
  - quick_action_decision
  - agent_mode_protocol
  - hierarchy_table
  - context_isolation
  - dispatch_report

unwanted_sections:
  - plan_mode_template
  - convergence_loop
  - stage_primitives

quality_thresholds:
  min_composite: 80
  min_relevance: 0.8
