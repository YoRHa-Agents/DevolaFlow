# EvoBench Scenario: Shell Proxy Disabled — R5 Strict Byte-Identical Baseline
# v8.3.2 PV-02 (closes R-002 from `.local/research/v8.4.0_gap_analysis.md` §2.1).
#
# Codifies the R5 strict contract: when the new `DEVOLAFLOW_RTK_PROXY` env-flag
# is unset (default), the L3 dispatch surface MUST receive byte-identical
# context to v8.3.1 — the new `pre_shell_call` lifecycle hook is a near-no-op
# and the `shell_proxy/` package adds zero overhead to the hot path
# (`is_proxy_enabled` short-circuits before any subprocess work).
#
# Section selection mirrors a standard L3 simple_implementation dispatch
# (same expected_sections set as `simple_impl_budget.yaml` baseline so the
# composite is directly comparable to that scenario's 90-floor). The new
# `lifecycle_hooks` row in SKILL.md (which v8.3.2 does NOT touch — SKILL.md
# is deferred to v8.4.0 rollup per cycle plan §4) already covers the hook
# infrastructure surface for L3 agents; PV-02 lands the new `pre_shell_call`
# event under the same row without a SKILL.md edit.
#
# Composite floor 90 (gate `standard`); confirms PV-02 does NOT regress
# the simple_implementation feature-profile section coverage. Pairs with
# `tests/test_shell_proxy_disabled_is_noop.py` which codifies the same
# contract at the unit-test layer.
#
# External canonical URL (per S-7): https://github.com/rtk-ai/rtk

name: "shell_proxy_disabled"
task_type: "simple_implementation"
description: >
  R5 strict baseline for v8.3.2 PV-02 (shell-proxy + pre_shell_call hook).
  When `DEVOLAFLOW_RTK_PROXY` is unset (the default), the L3 dispatch
  surface must receive byte-identical context to v8.3.1 — same expected
  sections as the existing simple_impl_budget scenario, same composite
  floor 90. Validates that the new lifecycle hook + shell_proxy package
  add zero overhead to dispatch when the proxy is OFF.

expected_sections:
  - quick_action_decision
  - plan_mode_template
  - agent_mode_protocol
  - quick_start_workflows
  - hierarchy_table
  - wave_task_constraints
  - rationalization_prevention
  - stage_primitives
  - gate_mechanism
  - agent_teams
  - context_isolation
  - lifecycle_hooks
  - convergence_loop
  - dispatch_report

unwanted_sections: []

quality_thresholds:
  min_composite: 90
  min_relevance: 0.90
  max_noise_ratio: 0.15
