# EvoBench Scenario: Research Survey
# Tests that research profile selects minimal ceremony, dispatch-focused sections.

name: "research_survey"
task_type: "research"
description: "Survey authentication libraries and compare JWT vs session-based approaches"

expected_sections:
  - quick_action_decision
  - hierarchy_table
  - agent_teams
  - context_isolation
  - dispatch_report
  - reference_navigation

unwanted_sections:
  - frontmatter
  - version_update
  - purpose_scope
  - template_quick_ref
  - convergence_loop
  - gate_mechanism

quality_thresholds:
  min_composite: 80.0
  max_noise_ratio: 0.1
  min_relevance: 0.8
