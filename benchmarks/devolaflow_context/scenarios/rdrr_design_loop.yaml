# EvoBench Scenario: RDRR (Research-Design-Review-Refine)
# Tests that RDRR profile loads convergence-heavy, design-iteration sections.

name: "rdrr_design_loop"
task_type: "RDRR"
description: "Iterative API design with research phase, design reviews, and refinement loops"

expected_sections:
  - quick_action_decision
  - quick_start_workflows
  - hierarchy_table
  - wave_task_constraints
  - stage_primitives
  - gate_mechanism
  - agent_teams
  - convergence_loop

unwanted_sections:
  - frontmatter
  - version_update
  - purpose_scope
  - template_quick_ref

quality_thresholds:
  min_composite: 80.0
  max_noise_ratio: 0.1
  min_relevance: 0.8
