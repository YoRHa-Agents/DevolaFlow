# EvoBench Scenario: Demo Showcase
# Tests that demo-showcase profile loads build/polish sections with team roles.

name: "demo_showcase"
task_type: "demo"
description: "Build a presentation-ready demo of the authentication system with polished UI"

expected_sections:
  - quick_action_decision
  - hierarchy_table
  - wave_task_constraints
  - agent_teams
  - context_isolation
  - dispatch_report
  - stage_primitives

unwanted_sections:
  - frontmatter
  - version_update
  - purpose_scope
  - template_quick_ref

quality_thresholds:
  min_composite: 80.0
  max_noise_ratio: 0.1
  min_relevance: 0.8
