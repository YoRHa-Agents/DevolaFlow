# EvoBench Scenario: AC Generator Disabled — R5 Strict Byte-Identical Baseline
# v9.0.0 PV-06 (v8.5.1 — closes Theme T5 #5 from `.local/research/v9.0.0_implementation_plan.md` §6.6).
#
# Codifies the R5 strict opt-out contract for the v8.0.0 P-10 acceptance-
# criteria generator (`src/devolaflow/ac_generator.py::ACGenerator`). The
# PV-06 flip turns the generator ON by default for STRICT/AUDIT
# decomposition profiles via `ac_generation.enabled: true`; this scenario
# validates the opt-out path:
#
#   * env-flag `DEVOLAFLOW_AC_GEN` set EXACTLY to "0" disables the
#     ACGenerator's 11 deterministic-pattern injection on the flipped
#     profile (R5 strict — NOT loose match).
#   * The legacy `acceptance_criteria: list[str]` alias remains the
#     contract for v7.x dispatchers per `patch_plan §3 P-10` R5
#     mitigation, so opt-out preserves the exact pre-flip dispatch shape.
#   * The L3 dispatch surface that follows MUST receive byte-identical
#     context to the pre-flip baseline.
#
# Composite floor 90 — confirms the opt-out path does not regress the
# simple_implementation feature-profile section coverage. The
# ACGenerator's 11-pattern catalogue + 3-dim quality scoring are
# validated separately by `tests/test_ac_generator.py` at the unit-test
# layer.
#
# External canonical URL (per S-7): https://github.com/YoRHa-Agents/DevolaFlow

name: "ac_generator_disabled"
task_type: "simple_implementation"
description: >
  R5 strict opt-out baseline for v9.0.0 PV-06 (v8.5.1) Theme T5 #5
  (ac_generator default-on flip). When `DEVOLAFLOW_AC_GEN` is set to
  "0", the ACGenerator's deterministic-pattern injection is bypassed
  (R5 strict per env-flags.md §2 parsing). The legacy
  `acceptance_criteria: list[str]` alias path remains the dispatch
  contract — preserving the exact v8.5.0 pre-flip dispatch shape. The
  L3 dispatch surface MUST receive byte-identical context to the
  pre-flip baseline. Composite floor 90 confirms no section regression.

expected_sections:
  - quick_action_decision
  - plan_mode_template
  - agent_mode_protocol
  - quick_start_workflows
  - hierarchy_table
  - wave_task_constraints
  - rationalization_prevention
  - stage_primitives
  - gate_mechanism
  - agent_teams
  - context_isolation
  - lifecycle_hooks
  - convergence_loop
  - dispatch_report

unwanted_sections: []

quality_thresholds:
  min_composite: 90
  min_relevance: 0.90
  max_noise_ratio: 0.20
