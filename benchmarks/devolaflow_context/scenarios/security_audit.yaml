# EvoBench Scenario: Security Audit
# Tests that security-audit profile loads threat model, scanning, and remediation sections.

name: "security_audit"
task_type: "security"
description: "Audit API endpoints for OWASP Top 10 vulnerabilities and remediate findings"

expected_sections:
  - quick_action_decision
  - hierarchy_table
  - wave_task_constraints
  - gate_mechanism
  - agent_teams
  - context_isolation
  - dispatch_report
  - convergence_loop

unwanted_sections:
  - frontmatter
  - version_update
  - purpose_scope
  - template_quick_ref

quality_thresholds:
  min_composite: 80.0
  max_noise_ratio: 0.1
  min_relevance: 0.8
