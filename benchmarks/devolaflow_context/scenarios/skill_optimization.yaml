# EvoBench Scenario: Skill Optimization Context Density
# Tests that skill-optimization profile selects benchmark/gate/context sections
# and excludes design/architecture noise.

name: "skill_optimization"
task_type: "skill-optimization"
description: "Optimize SKILL.md context density using EvoBench iteration"

expected_sections:
  - gate_mechanism
  - dispatch_report
  - context_isolation
  - wave_task_constraints
  - hierarchy_table
  - quick_start_workflows
  - convergence_loop

unwanted_sections:
  - frontmatter
  - version_update
  - purpose_scope
  - template_quick_ref

quality_thresholds:
  min_composite: 50.0
  max_noise_ratio: 0.3
  min_relevance: 0.5
